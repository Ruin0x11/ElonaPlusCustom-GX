#deffunc clientguide int clientguide_arg0
	locvar_clientguide_j = 0
	repeat 5
		locvar_clientguide_p = gdata(GDATA_QUEST_POOL + cnt)
		if ( qdata(QDATA_STATE, locvar_clientguide_p) == QUEST_STATE_ONGOING ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				locvar_clientguide_i = -1
				if ( qdata(QDATA_EXIST, locvar_clientguide_p) == QUEST_TYPE_COLLECT ) {
					if ( qdata(QDATA_MAP, locvar_clientguide_p) == gdata(GDATA_AREA) ) {
						locvar_clientguide_i = qdata((10 /*!!!@[QDATA_DEST_CLIENT @@@ QDATA_ENCOUNTER]@!!! */), locvar_clientguide_p)
					}
				}
				if ( qdata(QDATA_EXIST, locvar_clientguide_p) == QUEST_TYPE_DELIVER ) {
					if ( qdata(QDATA_MAP, qdata((10 /*!!!@[QDATA_DEST_CLIENT @@@ QDATA_ENCOUNTER]@!!! */), locvar_clientguide_p)) == gdata(GDATA_AREA) ) {
						locvar_clientguide_i = qdata(QDATA_CLIENT, qdata((10 /*!!!@[QDATA_DEST_CLIENT @@@ QDATA_ENCOUNTER]@!!! */), locvar_clientguide_p))
					}
				}
				if ( locvar_clientguide_i != (-1) ) {
					locvar_clientguide_f = 0
					repeat cnt
						if ( gdata(GDATA_QUEST_POOL + cnt) == locvar_clientguide_p ) {
							locvar_clientguide_f = 1
							break
						}
					loop
					if ( locvar_clientguide_f == 0 ) {
						rtval(locvar_clientguide_j) = locvar_clientguide_i
						locvar_clientguide_j++
					}
				}
			}
		}
	loop
	return locvar_clientguide_j

#deffunc tradecheck int tradecheck_charid
	locvar_clientguide_j = 0
	repeat 5
		locvar_clientguide_p = gdata(GDATA_QUEST_POOL + cnt)
		if ( qdata(QDATA_STATE, locvar_clientguide_p) == QUEST_STATE_ONGOING ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				if ( qdata(QDATA_MAP, locvar_clientguide_p) == gdata(GDATA_AREA) ) {
					if ( tradecheck_charid == qdata((10 /*!!!@[QDATA_DEST_CLIENT @@@ QDATA_ENCOUNTER]@!!! */), locvar_clientguide_p) ) {
						locvar_clientguide_j = 1
						break
					}
				}
			}
		}
	loop
	return locvar_clientguide_j

*chat_refresh
	gsel BUFFER_BUF
	buffer BUFFER_BUF, windoww, 720
	if ( cdata(CDATA_PORTRAIT, tc) < 80 ) {
		picload exedir + "graphic\\face1.bmp", 1
	}
	else {
		picload exedir + "graphic\\face2.bmp", 1
	}
	if ( scenemode ) {
		if ( int(actor(1, rc)) >= 160 ) {
			picload exedir + "graphic\\face2.bmp", 1
		}
		else {
			picload exedir + "graphic\\face1.bmp", 1
		}
	}
	if ( evochat == 0 ) {
		gsel BUFFER_PIC
		picload exedir + "graphic\\ie_chat.bmp"
		gsel BUFFER_SCREEN
	}
	if ( evochat != 0 ) {
		gsel BUFFER_PIC
		picload exedir + "graphic\\ie_chat2.bmp"
		gsel BUFFER_SCREEN
	}
	return

*chat
	if ( cdata(CDATA_TONE_SEX, tc) == 0 ) {
		if ( cdata(CDATA_SEX, tc) == 0 ) {
			cdata(CDATA_TONE_SEX, tc) = 1
		}
		else {
			cdata(CDATA_TONE_SEX, tc) = 2
		}
	}
	keyhalt = 1
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_INN ) {
		if ( cfg_extrahelp ) {
			if ( gdata(STARTING_GDATA_HELP + 7) == 0 ) {
				if ( mode == MODELIST_MAIN ) {
					if ( cdata(CDATA_ACTION_PERIOD, CHARA_PLAYER) == 0 ) {
						gdata(STARTING_GDATA_HELP + 7) = 1
						ghelp = 7
						gosub *elona_help
						if ( 1 == 1 ) {
							screenupdate = -1
							gosub *screen_draw
						}
					}
				}
			}
		}
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_TRAINER ) {
		if ( cfg_extrahelp ) {
			if ( gdata(STARTING_GDATA_HELP + 8) == 0 ) {
				if ( mode == MODELIST_MAIN ) {
					if ( cdata(CDATA_ACTION_PERIOD, CHARA_PLAYER) == 0 ) {
						gdata(STARTING_GDATA_HELP + 8) = 1
						ghelp = 8
						gosub *elona_help
						if ( 1 == 1 ) {
							screenupdate = -1
							gosub *screen_draw
						}
					}
				}
			}
		}
	}
	gosub *set_npcGod
	if ( scenemode == FALSE ) {
		snd SOUNDLIST_CHAT
		gosub *check_renew
	}
	cs = 0
	chatflag = 0
	chatesc = 1
	if ( cbit(CHARA_BIT_LIVESTOCK, tc) == TRUE & gdata(GDATA_AREA) == AREA_DOJO ) {
		if ( evnum == 0 ) {
			txt lang(name(tc) + "は修行で忙しい。", name(tc) + " won't listen.")
			questteleport = 0
			gosub *screen_draw
			return
		}
	}
	if ( cbit(CHARA_BIT_LIVESTOCK, tc) == TRUE & adata(ADATA_ID, gdata(GDATA_AREA)) == AREA_DISCARDED_RANCH ) {
		if ( evnum == 0 ) {
			txt lang(name(tc) + "ははしゃいでいて話を聞かない。", name(tc) + " won't listen.")
			questteleport = 0
			gosub *screen_draw
			return
		}
	}
	if ( cdata(CDATA_RELATION, tc) <= RELATION_DISLIKE ) {
		if ( evnum == 0 ) {
			if ( cbit(CHARA_BIT_LIVESTOCK, tc) != TRUE ) {
				txt lang(name(tc) + "は耳を貸さない。", name(tc) + " won't listen.")
				questteleport = 0
				gosub *screen_draw
				return
			}
		}
	}
	if ( gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 >= cdata(CDATA_INTEREST_RENEW, tc) ) {
		cdata(CDATA_INTEREST, tc) = 100
	}
	if ( cdata(CDATA_ROLE, tc) >= (1000 /*!!!@[ROLE_SHOP_MIN @@@ ROLE_SHOP_WEAPON]@!!! */) & cdata(CDATA_ROLE, tc) < (2000 /*!!!@[ROLE_GUEST_BEGGAR @@@ ROLE_GUEST_MIN]@!!! */) | cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
		bkrestock = cdata(CDATA_ROLE_RESTOCK, tc)
		invfile = cdata(CDATA_ROLE_FILE_ID, tc)
		gosub *shop_init
		if ( gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 >= bkrestock ) {
			if ( cdata(CDATA_ROLE, tc) != ROLE_SHOP_BOOK_RESERVE ) {
				if ( dbg_noautosave == 0 & gdata(GDATA_WIZARD) == 0 ) {
					if ( gdata(GDATA_AREA) != AREA_PET_ARENA & gdata(GDATA_AREA) != AREA_SHOW_HOUSE ) {
						if ( cbit(CHARA_BIT_EVENT_TALK, tc) != TRUE ) {
							gosub *game_save
							txtmore
							txtef COLOR_BLUE
							txt lang(" *保存* ", "*saving*")
						}
					}
				}
			}
		}
	}
	gosub *chat_refresh
	if ( scenemode == TRUE ) {
		goto *chat_scene
	}
	chatval(1) = 0, 1
	if ( cdata(CDATA_QUALITY, tc) == FIX_QUALITY_UNIQUE ) {
		if ( tc >= MAX_CHARA_FOLLOWER | cdata(CDATA_ID, tc) == CREATURE_ID_STRAY_SOUL ) {
			chatval(1) = cdata(CDATA_ID, tc), 0
		}
	}
	if ( evid() == 2 ) {
		goto *chat_evOpening
	}
	if ( evid() == 16 ) {
		goto *chat_evReached
	}
	if ( cdata(CDATA_CONDITION_SLEEP, tc) != 0 ) {
		listmax = 0
		buff = "(" + name(tc) + lang("はぐっすり眠っている…", " is sleeping.") + ")"
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
	}
	if ( birth != 0 ) {
		listmax = 0
		buff = lang(_birth(2), "Happy birthday!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		snd SOUNDLIST_CLAP2F
		goto *chat_end
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE & gdata(GDATA_FLAG_MAIN) == 850 & gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
		goto *chat_unique_orphe
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA & gdata(GDATA_FLAG_MAIN) == 860 & gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
		goto *chat_unique_yayauhqui
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD & gdata(GDATA_FLAG_MAIN) == 870 & gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
		goto *chat_unique_jaldabaoth
	}
	if ( cdata(CDATA_CONDITION_DIM, tc) > 60 ) {
		if ( cdata(CDATA_ID, tc) == CREATURE_ID_ALFRED_THE_CANGNAN_WIND & gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_25) >= 1000 & gdata(GDATA_AREA) == AREA_CHAOS_CRADLE & cdata(CDATA_ALLIED, tc) == 0 ) {
			cdata(CDATA_CONDITION_DIM, tc) = 0
			listmax = 0
			buff = "(" + name(tc) + lang("は錆びた鈴の音に反応して目を開けた", " is stunned.") + ")"
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("この…鈴の音は…ランカータ…？なぜ…ここ…に。いや、違う…！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
	}
	if ( cdata(CDATA_CONDITION_DIM, tc) > 60 ) {
		listmax = 0
		buff = "(" + name(tc) + lang("は気絶している…", " is stunned.") + ")"
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
	}
	if ( cdata(CDATA_ROW_ACT, tc) ) {
		listmax = 0
		buff = "(" + name(tc) + lang("はお取り込み中だ…", " is in the middle of something.") + ")"
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_ALFRED_THE_CANGNAN_WIND & gdata(GDATA_AREA) == AREA_CHAOS_CRADLE & gdata(GDATA_LEVEL) == 173 & cdata(CDATA_ALLIED, tc) == 0 ) {
		listmax = 0
		buff = lang("誰…ですか…貴方…は？", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("冒険者が…ここまで？私は…どれだけの時間…気絶…していた！？", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("私の、身体…ですか？はは…。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("次元炉…を無理矢理身体に埋め込んで…反動を…薬物で強化した肉体…で抑え込んでいましたが…この有様ですよ。ぐっ！？", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("ボロボロに…なった筋肉を…引き裂いて…フレームが嫌な…音を立てていますね…！", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_30) == 4 & gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_25) < 1000 ) {
			goto *chat_unique_lankata
		}
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_MIKRAANESIS & gdata(GDATA_AREA) == AREA_WEST_RASKILIS & gdata(GDATA_LEVEL) == 9 & gdata(GDATA_FLAG_MAIN) == 810 & cdata(CDATA_ALLIED, tc) == 0 ) {
		music = MUSICLIST_MCOPENING3
		gosub *music_play
		listmax = 0
		buff = lang("よく来てくれたね、" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。私達はミクラネシス。ソピアーの姉であり兄であり、捉え方によっては父であり母でもあり祖母でもあり祖父でもある。その節はソピアーがお世話になったね。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("本当は迎えに行きたかったのだけど、予想以上に手一杯でそうもいかなかったよ。正直、連絡用にこの分身体を置くので限界だった。ノルン、よくここまでガイドしてくれたね。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_unique_mikraanesis
	}
	if ( tc == CHARA_PLAYER ) {
		goto *chat_end
	}
	if ( cbit(CHARA_BIT_EVENT_TALK, tc) ) {
		cbitmod CHARA_BIT_EVENT_TALK, tc, FALSE
		goto *chat_event
	}
	if ( chatval(1) != 0 ) {
		if ( gdata(GDATA_AREA) != AREA_SHOW_HOUSE ) {
			if ( tc >= MAX_CHARA_FOLLOWER | cdata(CDATA_ID, tc) == CREATURE_ID_STRAY_SOUL ) {
				goto *chat_unique
			}
		}
	}
	if ( questteleport == 1 ) {
		questteleport = 0
		goto *chat_quest
	}
	buff = ""
	goto *chat_default

*chat_scene
	listmax = 0
	buff = buff
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	goto *chat_end

*chat_event
	listmax = 0
	cc = CHARA_PLAYER
	gdata(GDATA_GUEST)--
	_switch_val = cdata(CDATA_ROLE, tc)
	if ( 0 ) {
		_switch_sw++
	}
	if ( _switch_val == 13 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_MONTH) == 1 & rnd(4) ) {
			listmax = 0
			buff = lang("明けましておめでとう" + _da(2), "Happy new year!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("日ごろの感謝の意をこめてこれをあげる" + _yo(), "I've brought you a gift today, here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_NEW_YEARS_GIFT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			if ( stat != 0 ) {
				inv(INV_ITEM_PARAM3, ci) = cdata(CDATA_IMPRESSION, tc) + rnd(50)
			}
			txt lang(name(tc) + "は" + itemname(ci, 1) + "を置いていった。", name(tc) + " throws you " + itemname(ci, 1) + ".")
			goto *chat_event_SWEND1
		}
		if ( cdata(CDATA_IMPRESSION, tc) < 25 ) {
			listmax = 0
			buff = lang("貴様！見つけた" + _yo(2), "You scum! You won't get away from me now!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("「これでも食らうがいい" + _yo(2) + "」", cnvtalk("Eat this!"))
			if ( rnd(2) == 0 ) {
				repeat 28
					tlocx = cdata(CDATA_X, tc) + rnd(3) - rnd(3)
					tlocy = cdata(CDATA_Y, tc) - rnd(3) + rnd(3)
					if ( tlocx < 0 | tlocy < 0 | tlocx >= mdata(MDATA_WIDTH) | tlocy >= mdata(MDATA_HEIGHT) ) {
						continue
					}
					if ( tlocx == cdata(CDATA_X, tc) ) {
						if ( tlocy == cdata(CDATA_Y, tc) ) {
							continue
						}
					}
					txt lang(name(tc) + "は火炎瓶を投げた。", name(tc) + " throws molotov.")
					snd SOUNDLIST_THROW2
					ccbk = cc
					cc = tc
					animeid = ANIMATION_THROW
					aniref = 223, 0
					anix = tlocx
					aniy = tlocy
					gosub *anime
					cc = ccbk
					addmef tlocx, tlocy, MEF_TYPE_FIRE, xy2pic(24, 0), rnd(15) + 20, 50, tc
					mapitem_fire tlocx, tlocy
				loop
			}
			else {
				repeat 8 + rnd(6)
					flt
					itemcreate -1, ITEM_ID_VOMIT, -1, -1, 0
					txt lang(name(tc) + "は吐いた。", name(tc) + " vomits.")
					snd SOUNDLIST_VOMIT
					await 10
					gosub *screen_draw
				loop
			}
			goto *chat_event_SWEND1
		}
		if ( cdata(CDATA_IMPRESSION, tc) >= 100 ) {
			if ( cbit(CHARA_BIT_TOKEN_FRIEND, tc) == FALSE ) {
				if ( inv_getfreeid(-1) != (-1) ) {
					listmax = 0
					buff = lang("これ、あげ" + _ru(2), "Here, take this!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					cbitmod CHARA_BIT_TOKEN_FRIEND, tc, TRUE
					flt
					itemcreate -1, ITEM_ID_TOKEN_FRIENDSHIP, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					txt lang(name(tc) + "に" + itemname(ci, 1) + "をもらった！", "You receive " + itemname(ci, 1) + ".")
					if ( jp ) {
						txt "友達100人できるかな♪"
					}
					listmax = 0
				}
			}
		}
		if ( rnd(4) == 0 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
				advfavoriteskill tc
				csskill = rtval(rnd(stat))
				if ( cdata(CDATA_IMPRESSION, tc) >= 300 ) {
					if ( rnd(3) == 0 ) {
						advfavoritestat tc
						csskill = stat
					}
				}
				if ( sorg(csskill, CHARA_PLAYER) == 0 ) {
					buff = lang(skillname(csskill) + "のスキルを、友達価格の" + calclearncost(csskill, cc, 1) + strplat + "で教えてあげてもいい" + _ga(3) + "どう" + _kana(1), "I can teach you the art of " + skillname(csskill) + " for a friendly price of " + calclearncost(csskill, cc, 1) + " platinum pieces. Do you want me to train you?")
					if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= calclearncost(csskill, cc, 1) ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("習得する", "Teach me the skill.")
						listmax++
					}
				}
				else {
					buff = lang(skillname(csskill) + "のスキルを、友達価格の" + calctraincost(csskill, cc, 1) + strplat + "で訓練してもいい" + _ga(3) + "どう" + _kana(1), "I can train your " + skillname(csskill) + " skill for a friendly price of " + calctraincost(csskill, cc, 1) + " platinum pieces. Do you want me to train you?")
					if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= calctraincost(csskill, cc, 1) ) {
						list(0, listmax) = 2
						listn(0, listmax) = lang("訓練する", "Train me.")
						listmax++
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("遠慮しとく", "I think I'll pass.")
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( chatval == 0 | chatval == (-1) ) {
					listmax = 0
					buff = lang("わかった" + _yo() + "またしばらくしてから尋ねてみ" + _ru(), "I see. I'll ask you again at some time in the future.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_event_SWEND1
				}
				snd SOUNDLIST_PAYGOLD1
				if ( chatval == 1 ) {
					cdata(CDATA_PLATINUM, CHARA_PLAYER) -= calclearncost(csskill, 0, 1)
					skillgain cc, csskill
					gdata(GDATA_LEARNED)++
					listmax = 0
					buff = lang("新しい技術をどうやら習得できたようだ" + _na() + "役に立てて嬉しい" + _yo(), "Fantastic! You've learned the skill in no time. I'm glad I could help.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 2 ) {
					cdata(CDATA_PLATINUM, CHARA_PLAYER) -= calctraincost(csskill, 0, 1)
					modgrowth cc, csskill, 120
					listmax = 0
					buff = lang("よし、これで訓練は終わり" + _da() + "かなり潜在能力が伸びた" + _yo(2), "Marverous! The traing is now complete. I think you've improved some potential.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_event_SWEND1
			}
		}
		if ( rnd(5) == 0 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
				listmax = 0
				buff = lang("友達の証としてこれをあげ" + _ru(2) + "大事に使って" + _yo(), "As a pledge of friendship, here's something for you!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( inv_getfreeid(-1) == (-1) ) {
					txt lang("部屋が一杯で置けなかった…", "Your home has no empty spot...")
				}
				else {
					if ( rnd(4) ) {
						p = 55
					}
					else {
						p = 622
					}
					flt
					itemcreate -1, p, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					txt lang(name(tc) + "に" + itemname(ci, 1) + "をもらった！", "You receive " + itemname(ci, 1) + ".")
					snd SOUNDLIST_GET1
				}
				goto *chat_event_SWEND1
			}
		}
		if ( rnd(4) == 0 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 100 ) {
				listmax = 0
				buff = lang("近くまで来たので寄ってみた" + _nda() + "ついでだから、土産にこれをあげ" + _ru(), "I just stopped by to see you. Oh, I happen to have a gift for you too.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( inv_getfreeid(CHARA_PLAYER) == (-1) ) {
					txt lang("所持品が一杯で受け取れなかった…", "Your inventory is full...")
				}
				else {
					flt
					itemcreate CHARA_PLAYER, ITEM_ID_GIFT, -1, -1, 0
					txt lang(itemname(ci, 1) + "を受け取った。", "You receive " + itemname(ci, 1) + ".")
					snd SOUNDLIST_GET1
				}
				goto *chat_event_SWEND1
			}
		}
		if ( rnd(5) == 0 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 100 ) {
				listmax = 0
				buff = lang("旅の途中にこんなものを拾った" + _nda() + _kimi(3) + "の役に立つと思って持ってきた" + _yo(), "I found these during my journey. Thought you could find them useful.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txt lang(name(tc) + "は色々なものが詰まった袋を、あなたに手渡した。", name(tc) + " gives you a bag full of materials.")
				efid = SKILL_EFFECT_CREATE_MATERIAL
				efp = 100
				tc = CHARA_PLAYER
				gosub *effect
				goto *chat_event_SWEND1
			}
		}
		if ( rnd(8) == 0 ) {
			advfavoriteskill tc
			csskill = rtval(rnd(stat))
			listmax = 0
			buff = lang(skillname(csskill) + "は" + _ore(3) + "の得意なスキルの内の一つ" + _da(), "" + skillname(csskill) + " is one of my favorite skills.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_event_SWEND1
		}
		if ( rnd(10) == 0 ) {
			advfavoritestat tc
			csskill = stat
			listmax = 0
			buff = lang(_ore(3) + "は" + skillname(csskill) + "が自慢なの" + _da(), "I'm proud of my good " + skillname(csskill) + ".")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_event_SWEND1
		}
		if ( rnd(3) == 0 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 75 ) {
				listmax = 0
				buff = lang("やあ。特に用はない" + _ga(3) + "、なんだか暇を持てましていたら、" + _kimi(3) + "と話がしたくなって" + _na(3) + "、寄ってみた" + _nda(), "Hey " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", how's your journey? I was bored to death so I decided to make a visit to you!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txt lang("あなたと" + name(tc) + "は愉快に語り合った！", "You hold an amusing conversation with " + name(tc) + "!")
				modimp tc, 10
				goto *chat_event_SWEND1
			}
		}
		if ( rnd(3) == 0 ) {
			listmax = 0
			buff = lang("酒でも飲んで親睦を深めよう" + _yo(2), "Let's have a drink and deepen our friendship!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_DRINK1
			txt lang("あなたと" + name(tc) + "は乾杯した！", cnvtalk("Cheers!"))
			txtef COLOR_SKY_BLUE
			txtmore
			txt lang("「うぃっ！」", "*Hic*"), lang("「うまいぜ」", cnvtalk("Ah, good booze.")), lang("「らららー♪」", cnvtalk("La-la-la-la.")), lang("「ひっく」", cnvtalk("I'm going to heaven.")), lang("「ふぅ」", cnvtalk("Whew!")), lang("「たまらないわ」", cnvtalk("I'm revived!")), lang("「んまっ♪」", cnvtalk("Awesome."))
			dmgcon tc, CONDITION_DRUNK, 1000
			dmgcon cc, CONDITION_DRUNK, 1000
			modimp tc, 15
			goto *chat_event_SWEND1
		}
		listmax = 0
		buff = lang("まあ、とくに用もないんだけど" + _na(), "I just wanted to say hi.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 2005 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_GUILD_TRAINER) == gdata(GDATA_MONTH) ) {
			listmax = 0
			buff = lang("今月はもう訓練は終わり" + _da(), "No more training in this month.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_event_SWEND1
		}
		plat = 3
		gdata(GDATA_GUILD_TRAINER) = gdata(GDATA_MONTH)
		buff = lang(guildname() + "の一員足るもの、ギルドの名に恥じないよう、常に己の技量を磨き続けなければならない" + _yo() + "ギルドの一員である" + _kimi(3) + "には、たったのプラチナ" + plat + "枚で潜在能力を伸ばす訓練を施してあげる" + _yo(), "As a member of " + guildname() + " you have to forge your talent to live up to our reputation. For only " + plat + " platinum coins, I'll improve the potential of your talent.")
		if ( gdata(GDATA_FLAG_GUILD_MAGE) != 0 ) {
			p = 16, 15, 14, -1
		}
		else {
			if ( gdata(GDATA_FLAG_GUILD_FIGHTER) != 0 ) {
				p = 10, 11, 12, -1
			}
			else {
				if ( gdata(GDATA_FLAG_GUILD_THIEF) != 0 ) {
					p = 12, 13, 11, 14, -1
				}
				else {
					plat = 4
					p(3) = -1
					i = rnd(6)
					repeat 3
						p(cnt) = 10 + cnt + i
					loop
					buff = lang("鍛えている" + _kana(2) + "冒険者として生き残るには、日ごろの鍛錬が大切" + _da(2) + "わずかプラチナ" + plat + "枚で、潜在能力を伸ばす特別な訓練を施してあげる" + _yo(2), "Training! Training! At the end, only thing that saves your life is training! For only " + plat + " platinum coins, I'll improve the potential of your talent.")
				}
			}
		}
		if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= plat ) {
			repeat 8
				if ( p(cnt) == (-1) ) {
					break
				}
				list(0, listmax) = p(cnt)
				listn(0, listmax) = lang(skillname(p(cnt)) + "を鍛える", "I want to improve " + skillname(p(cnt)) + ".")
				listmax++
			loop
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("訓練しない", "Not today.")
		listmax++
		gosub *chat_select
		if ( chatval == 0 | chatval == (-1) ) {
			listmax = 0
			buff = lang("後悔する" + _yo(2), "You'll regret this!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_event_SWEND1
		}
		cdata(CDATA_PLATINUM, CHARA_PLAYER) -= plat
		snd SOUNDLIST_DING3
		txtef COLOR_GREEN
		txt lang(name(0) + "の" + skillname(chatval) + "の潜在能力が大きく上昇した。", name(0) + your(CHARA_PLAYER) + " potential of " + skillname(chatval) + " greatly expands.")
		modgrowth CHARA_PLAYER, chatval, 120
		listmax = 0
		buff = lang("うむ、なかなか見所がある" + _yo(), "Good. You show a lot of potential.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 2002 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("まあ、とくに用もないんだけど" + _na(), "I just wanted to say hi.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 2000 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) > 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("いい", "Yes.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("だめ", "No.")
		listmax++
		buff = lang("パンを買う金さえない" + _nda() + "恵んで" + _kure(3) + "、おねがい" + _da(2), "I got no money to buy food. Will you spare me some coins?")
		gosub *chat_select
		if ( chatval == 1 ) {
			p = cdata(CDATA_GOLD, CHARA_PLAYER) / 20 + 1
			txt lang("あなたは" + p + "goldを乞食に渡した。", "You spare " + him(tc) + " " + p + " gold pieces.")
			cdata(CDATA_GOLD, CHARA_PLAYER) -= p
			snd SOUNDLIST_PAYGOLD1
			cdata(CDATA_GOLD, tc) += p
			modkarma 0, 2
			listmax = 0
			buff = lang(_thanks(2) + "この恩は一生忘れない" + _yo(), "Thanks! I'll never forget this.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_event_SWEND1
		}
		listmax = 0
		buff = lang("ケチ！", "You're so cheap!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1025 | _switch_sw ) {
		_switch_sw = 0
		tri3 = -1
		tri4 = -1
		tri5 = -1
		tri6 = -1
		tri7 = -1
		tri8 = -1
		tri9 = -1
		tri10 = -1
		item_find ITEM_ID_CARAMEL_OF_TERMINATION, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri3 = stat
		}
		item_find ITEM_ID_HANDMADE_CHOCOLATE, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri4 = stat
		}
		item_find ITEM_ID_FORTUNE_COOKIE, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri5 = stat
		}
		item_find ITEM_ID_ROMIAS, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri6 = stat
		}
		item_find ITEM_ID_VERNIS_ORIGINAL, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri7 = stat
		}
		item_find ITEM_ID_ACORN, ITEM_FIND_MODE_PARAM2_GT_3, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri8 = stat
		}
		item_find ITEM_ID_CHESTNUT, ITEM_FIND_MODE_PARAM2_GT_3, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri9 = stat
		}
		item_find ITEM_ID_API_NUT, ITEM_FIND_MODE_PARAM2_GT_3, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			tri10 = stat
		}
		if ( tri10 != (-1) ) {
			list(0, listmax) = 10
			listn(0, listmax) = lang(itemname(tri10, 1) + "をあげる", "Here, take my " + itemname(tri10, 1) + ".")
			listmax++
		}
		if ( tri9 != (-1) ) {
			list(0, listmax) = 9
			listn(0, listmax) = lang(itemname(tri9, 1) + "をあげる", "Here, take my " + itemname(tri9, 1) + ".")
			listmax++
		}
		if ( tri8 != (-1) ) {
			list(0, listmax) = 8
			listn(0, listmax) = lang(itemname(tri8, 1) + "をあげる", "Here, take my " + itemname(tri8, 1) + ".")
			listmax++
		}
		if ( tri7 != (-1) ) {
			list(0, listmax) = 7
			listn(0, listmax) = lang(itemname(tri7, 1) + "をあげる", "Here, take my " + itemname(tri7, 1) + ".")
			listmax++
		}
		if ( tri6 != (-1) ) {
			list(0, listmax) = 6
			listn(0, listmax) = lang(itemname(tri6, 1) + "をあげる", "Here, take my " + itemname(tri6, 1) + ".")
			listmax++
		}
		if ( tri5 != (-1) ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang(itemname(tri5, 1) + "をあげる", "Here, take my " + itemname(tri5, 1) + ".")
			listmax++
		}
		if ( tri4 != (-1) ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang(itemname(tri4, 1) + "をあげる", "Here, take my " + itemname(tri4, 1) + ".")
			listmax++
		}
		if ( tri3 != (-1) ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang(itemname(tri3, 1) + "をあげる", "Here, take my " + itemname(tri3, 1) + ".")
			listmax++
		}
		list(0, listmax) = 2
		listn(0, listmax) = lang("イタズラされる", "Trick.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("イタズラする", "Treat.")
		listmax++
		buff = lang(_trick(3), "Trick or Treat?")
		chatesc = 0
		gosub *chat_select
		if ( chatval >= 3 & chatval <= 10 ) {
			if ( chatval == 3 ) {
				inv(INV_ITEM_NUM, tri3)--
			}
			if ( chatval == 4 ) {
				inv(INV_ITEM_NUM, tri4)--
			}
			if ( chatval == 5 ) {
				inv(INV_ITEM_NUM, tri5)--
			}
			if ( chatval == 6 ) {
				inv(INV_ITEM_NUM, tri6)--
			}
			if ( chatval == 7 ) {
				inv(INV_ITEM_NUM, tri7)--
			}
			if ( chatval == 8 ) {
				inv(INV_ITEM_NUM, tri8)--
			}
			if ( chatval == 9 ) {
				inv(INV_ITEM_NUM, tri9)--
			}
			if ( chatval == 10 ) {
				inv(INV_ITEM_NUM, tri10)--
			}
			listmax = 0
			buff = lang("わーい！", "Yeah!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = lang("ハッピーハロウィン！", "Happy Halloween!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("コレあげ" + _ru(2), "As a pledge of friendship, here's something for you!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( inv_getfreeid(-1) == (-1) ) {
				txt lang("部屋が一杯で置けなかった…", "Your home has no empty spot...")
			}
			else {
				flt
				itemcreate -1, ITEM_ID_JACK_LANTERN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				txt lang(itemname(ci, 1) + "をもらった！", "You receive " + itemname(ci, 1) + ".")
				snd SOUNDLIST_GET1
			}
		}
		if ( chatval == 2 ) {
			txt lang("「これでも食らうがいい" + _yo(2) + "」", cnvtalk("Eat this!"))
			repeat 28
				tlocx = cdata(CDATA_X, tc) + rnd(3) - rnd(3)
				tlocy = cdata(CDATA_Y, tc) - rnd(3) + rnd(3)
				if ( tlocx < 0 | tlocy < 0 | tlocx >= mdata(MDATA_WIDTH) | tlocy >= mdata(MDATA_HEIGHT) ) {
					continue
				}
				if ( tlocx == cdata(CDATA_X, tc) ) {
					if ( tlocy == cdata(CDATA_Y, tc) ) {
						continue
					}
				}
				txt lang(name(tc) + "は火炎瓶を投げた。", name(tc) + " throws molotov.")
				snd SOUNDLIST_THROW2
				ccbk = cc
				cc = tc
				animeid = ANIMATION_THROW
				aniref = 223, 0
				anix = tlocx
				aniy = tlocy
				gosub *anime
				cc = ccbk
				addmef tlocx, tlocy, MEF_TYPE_FIRE, xy2pic(24, 0), rnd(15) + 20, 50, tc
				mapitem_fire tlocx, tlocy
			loop
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("ひっ…ガード！ガード！", "Guard! Guard! Guard!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = lang("おやおや", "Come on!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			modkarma 0, -2
		}
		cdata(CDATA_ROLE, tc) = ROLE_GUEST_CITIZEN
		cdata(CDATA_RELATION, tc) = RELATION_DISLIKE
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 2001 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 1
		listn(0, listmax) = lang("いい", "Yes.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("だめ", "No.")
		listmax++
		buff = lang("フッ。よく逃げ出さずに戻ってきた" + _na() + "準備はいいか。", "So, are you ready?")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("いく" + _yo(2), "Okay, no turning back now!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = lang("うふふ", "Come on!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gosub *sexsex
			goto *chat_event_SWEND1
		}
		listmax = 0
		buff = lang("ふん！", "Hump!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 2006 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 1
		listn(0, listmax) = lang("いい", "Yes.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("だめ", "No.")
		listmax++
		buff = lang("スターになりたい" + _kana(1), "You want to be a star?")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("よい心がけだ" + _na(2), "Okay, no turning back now!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = lang("うふふ", "Come on!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gosub *sexsex
			goto *chat_event_SWEND1
		}
		listmax = 0
		buff = lang("ふん！", "Hump!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 2003 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("買いたい", "I want to buy something.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("売りたい", "I want to sell something.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("今はいい", "Not now.")
		listmax++
		buff = lang("今日は" + _kimi(3) + "の幸運な日" + _da() + "普段は一般の客には売らない格安の品を、特別に見せてあげ" + _ru() + "覚えておいて" + _kure(3) + "、今日だけだ" + _yo(), "This is your lucky day. I wouldn't normaly show my discounted goods to commoners but since I feel so good today...")
		gosub *chat_select
		if ( chatval == 0 ) {
			invctrl = INVCTRL_SHOP_BUY
			invfile = cdata(CDATA_ROLE_FILE_ID, tc)
			gosub *shop
			screenupdate = -1
			gosub *screen_draw
			cs = 0
			buff = ""
			goto *chat_event
		}
		if ( chatval == 1 ) {
			invctrl = INVCTRL_SHOP_SELL
			invfile = cdata(CDATA_ROLE_FILE_ID, tc)
			gosub *shop
			cc = CHARA_PLAYER
			screenupdate = -1
			gosub *screen_draw
			cs = 0
			buff = ""
			goto *chat_event
		}
		listmax = 0
		buff = lang("後になって後悔しても知らない" + _yo(), "I hope you won't regret it later.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		cdata(CDATA_ROLE, tc) = ROLE_GUEST_CITIZEN
		goto *chat_event_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 23 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_DOCK ) {
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 >= 3 & gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 < 6 ) {
				if ( kurikaesitaisaku == 1 ) {
					kurikaesitaisaku = 0
					modimp tc, -5
					list(0, listmax) = 1
					listn(0, listmax) = lang("用事がありまして…", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("一度帰ったほうが能率が上がると判断しました", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("お前さぁ！みんなが必死こいて頑張ってるのに何帰ってんの？？自分さえよければいいとか考えてんだろ？…おい、聞いてんのかコラァァァ！！！", "Sorry, this is untranslated sentence.")
					chatesc = 0
					gosub *chat_select
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("お前新入りだからって甘えてるだろ。そういうのここじゃ通用しねえからな、言っとくけどよぉ！！ああ？常識ってもんがねえのか？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_event_SWEND1
					}
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("何一人で判断してんだ！！もういい、勝手にしろよ！そっちのほうが能率上がるんだろ、ええ？やってみろよオイ。お前の仕事が終わらなくても俺は知らねえからな！！！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						modimp tc, -5
						goto *chat_event_SWEND1
					}
					goto *chat_event_SWEND1
				}
			}
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 == 2 ) {
				gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM)++
				listmax = 0
				buff = lang("おっと。お前が艦長の推薦でやってきた新入りか。わからないことがあったらリーダーの俺にちゃんと聞くんだぞ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("ＡＢＣの内容を教えてください", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("暴走機械がいるって聞いたんですけど", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("さっそくだがお前の仕事の割り当ては、仕事Ａ：" + gdata(GDATA_FLAG_WORK_A_REMAINING) + "、仕事Ｂ：" + gdata(GDATA_FLAG_WORK_B_REMAINING) + "、仕事Ｃ：" + gdata(GDATA_FLAG_WORK_C_REMAINING) + "だ。頼むぞ。", "Sorry, this is untranslated sentence.")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("あ？お前そんなこともわかんねえのか？せめて自分で調べてから聞いてくれる？？？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					modimp tc, -5
					goto *chat_event_SWEND1
				}
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("何言ってんだお前。無駄口叩いてないで仕事しろ。お前は後から来たんだからスピーディーにやらないと終わんねえからな！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_event_SWEND1
				}
				goto *chat_event_SWEND1
			}
		}
		goto *chat_event_SWEND1
	}

*chat_event_SWEND1
	goto *chat_end

*chat_unique
	listmax = 0
	_switch_val = chatval(1)
	if ( 0 ) {
		_switch_sw++
	}
	if ( _switch_val == 2 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("礼を言うぞ…この地と神々については常闇の眼でも知り得なかったことだ。果たしてお前達に《混沌》を止めることができるのか見物させてもらおう。それにしても…奴はなぜここにいない…？", "So you've made it this far. Evidently, <Chaos> wants no poise even within their own creation...Nefia. Well, it seems they have left me no choice but to whip you!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		else {
			listmax = 0
			buff = lang("ここまで辿り着くとはな…どうやら《混沌》は、自ら創りしネフィアの安定さえも望まぬらしい。しかし、私とてここで死ぬつもりなどないのだ。", "So you've made it this far. Evidently, <Chaos> wants no poise even within their own creation...Nefia. Well, it seems they have left me no choice but to whip you!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 170
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 23 | _switch_sw ) {
		_switch_sw = 0
		if ( evid() == 1 ) {
			listmax = 0
			buff = lang("ああ、聡明なるレシマスの常闇の眼よ！ 新しい主は、どうやらお前の本当の価値を知らないようだぞ。…だが、心配しなくてもいい。この" + _sex(cdata(CDATA_SEX, CHARA_PLAYER)) + "は、そこで死んでいる老人のように、たまに本を開いては下界の嘘に満ちた歴史を嘲笑い、自己満足にふける以外の使い方を、きっと見つけてくれるだろう。", "Oh omnipotent Lesimas' origin of vice! It seems your new master knows not your true worth....You do not need concern yourself. This " + _sex(cdata(CDATA_SEX, CHARA_PLAYER)) + " will surely find another arrogant fool to open the book and sneer down upon the spurious history of this measely world for you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang(" (青年は見下したような笑い声をあげ、あなたに向き直った) さあ、これ以上愚かな詮索の表情を続けて私を落胆させないでくれ。お前が眺めている本には、真実の歴史を刻み、過去の文明の歩みを記録する魔力が秘められていると知ったのなら。", "Now, please, your questioning expressions do nothing but fill me with despondency. The book you gaze upon holds within the true history, if the secrets of ancient civilization's magic be as such.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そう。この本に書かれていることは、全て偽りなき歴史。シエラ・テールの、そして過去の偉大なる文明の栄光と衰退の軌跡が記されている。その価値を、わざわざ説明する必要もあるまい？", "Yes. All that is written is, unfalsified history. Of Sierra Terre, and the grandure of ancient civilizations as well as their decline are all enscribed. That worth, it goes without saying I needn't explain?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ひとつ警告しよう。台座から離れた時、それは魔力を失い、変哲の無い一冊の本と化すのだ。新たな歴史が刻まれることはなく、正当性を証明するすべもなくなるだろう。", "I give you one warning, once removed from the pedistal it loses it's magic and transforms into an ordinary book. It writes no new history and surely also lose all justifiable proof.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("本を所有する者はまた、偽りの歴史を動かす勢力から、自らの命を守る必要にも迫られるだろう。このアーティファクトには、それだけの価値があるということだ。そしてお前には…エレアの風を聴く者と出会い、言葉を交じあわせたお前には…それがどれほどの意味を持つことか、理解していなくてもだ…そうだな、少なくとも我々を楽しませてくれるような使い方を期待しているよ。", "Those who posses this book as well must protect their own lives from the influences of falsified history. This artifact has that much power. Also you... that met and exchanged words with the Eleas, Listener of of wind... What meaning that has, even if you don't understand... Yes that's right, you'll at least bring me entertainment.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("もちろん、本を下界に持ち出すかどうかはお前次第だ…少なくとも、選択という行為に、私が予期できない偶然がある可能性を、お前が信じるのなら。", "Of course if you wish to bring the book to your lower world then it is your choice... Or should I say if you believe that you can make a selection I have not already forseen.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 28 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("よそへいってくれ…", "Leave me alone.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 946 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 780 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("独り言を聴く", "Let's talk to me.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("記憶の灯を使う", "Use Light of memory.")
			listmax++
			buff = lang("…。", "...")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				txt lang("記憶の灯から暖かい光が広がる…。", "Light of storage emit warm light...")
				animeid = ANIMATION_GAMEWIN3
				gosub *anime
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_ZANAN ) {
					gdata(GDATA_TELEPORT_AREA) = AREA_AIMWELL
				}
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_DOLE ) {
					gdata(GDATA_TELEPORT_AREA) = AREA_NORTH_TYRIS
				}
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_NORMAL
				prevsekai = gdata(GDATA_WORLD)
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_ZANAN ) {
					gdata(GDATA_WORLD_RENEW) = TRUE
					gdata(GDATA_WORLD) = AREA_AIMWELL
					gdata(GDATA_WORLD_X) = adata(ADATA_X, 92), adata(ADATA_Y, 92)
				}
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_DOLE ) {
					gdata(GDATA_WORLD_RENEW) = TRUE
					gdata(GDATA_WORLD) = AREA_NORTH_TYRIS
					gdata(GDATA_WORLD_X) = adata(ADATA_X, 5), adata(ADATA_Y, 5)
				}
				fixtransfermap = 1
				chatteleport = 1
				goto *chat_end
			}
			kaiwa = rnd(5)
			if ( kaiwa == 0 ) {
				listmax = 0
				buff = lang("ユーリ…せめて貴様が首を吊らなければな。おおかた獣に喰われてアリシアを忘れるのを恐れたのだろうが…。ザレッタは今でもあの場所で、貴様のことを心配しているだろうよ…！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 1 ) {
				listmax = 0
				buff = lang("大丈夫だ、ドールのことをまだ全部は忘れていない…。つまり国はどうにか持ちこたえているということだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 2 ) {
				listmax = 0
				buff = lang("こんなことになるのなら、ドールを発つ前にザレッタと話をしておくべきだった。…そうか。まだ俺はあいつに未練があったのか。我ながら女々しいことだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 3 ) {
				listmax = 0
				buff = lang("今思えばオズム皇子はエルシアを共通の敵に仕立て上げるのが目的のようだった。青髪のルーファスと結託して何か企んでいたのかもしれん。だが今となってはもうどうでもいいことだ…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 4 ) {
				listmax = 0
				buff = lang("まさか、このような田舎の町で白い鷹が死に、紅血も死を待つだけになるとはな。おいユーリ…俺たちの歯車はどこで狂ってしまったんだろうな？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("…なんだお前は？じろじろ見るな。うせろ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 29 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 780 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("話がしたい", "Let's talk to me.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("記憶の灯を使う", "Use Light of memory.")
			listmax++
			buff = lang("…。", "...")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				txt lang("記憶の灯から暖かい光が広がる…。", "Light of storage emit warm light...")
				animeid = ANIMATION_GAMEWIN3
				gosub *anime
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_ZANAN ) {
					gdata(GDATA_TELEPORT_AREA) = AREA_AIMWELL
				}
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_DOLE ) {
					gdata(GDATA_TELEPORT_AREA) = AREA_NORTH_TYRIS
				}
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_NORMAL
				prevsekai = gdata(GDATA_WORLD)
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_ZANAN ) {
					gdata(GDATA_WORLD_RENEW) = TRUE
					gdata(GDATA_WORLD) = AREA_AIMWELL
					gdata(GDATA_WORLD_X) = adata(ADATA_X, 92), adata(ADATA_Y, 92)
				}
				if ( cdata(CDATA_ID, tc) == CREATURE_ID_LOYTER_THE_CRIMSON_OF_DOLE ) {
					gdata(GDATA_WORLD_RENEW) = TRUE
					gdata(GDATA_WORLD) = AREA_NORTH_TYRIS
					gdata(GDATA_WORLD_X) = adata(ADATA_X, 5), adata(ADATA_Y, 5)
				}
				fixtransfermap = 1
				chatteleport = 1
				goto *chat_end
			}
		}
		if ( cdata(CDATA_FAME, CHARA_PLAYER) < 20000 ) {
			listmax = 0
			buff = lang("邪魔だ。話しかけるな。", "Who the hell are you?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_RED
			txt lang("クエスト必要名声：20000　", "Required Fame: 20000 ")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_NIGHTMARE) >= 1000 ) {
			listmax = 0
			buff = lang("気安く声をかけるな。", "You have the money, now get lost.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_NIGHTMARE) == 1000 ) {
				item_find ITEM_ID_RESEARCHERS_DIARY, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
				if ( stat != (-1) ) {
					ci = stat
					if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("立ち去る", "Sorry.")
						listmax++
						list(0, listmax) = 0
						listn(0, listmax) = lang("研究員の閻魔帳を読ませる", "read this researcher's diary.")
						listmax++
						buff = lang("…離れろ。しつこいぞ。", "...Get lost!")
						gosub *chat_select
						if ( chatval != 0 ) {
							goto *chat_end
						}
						listmax = 0
						buff = lang("このノートを読めと？…読んだらすぐに消えろ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						await 100
						snd SOUNDLIST_BOOK1
						listmax = 0
						buff = lang("これは…。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						snd SOUNDLIST_BOOK1
						await 100
						snd SOUNDLIST_BOOK1
						listmax = 0
						buff = lang("この件は既に解決している。所員は施設内で全員死亡、一人逃げた所長も「処分」済みだ。被験者とこの研究員の遺族には軍から賠償金が払われ、他の研究施設もしかるべき管理体制に移行した。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("以上だ。貴様の介入する余地はない。わかったら消えろ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						gdata(GDATA_FLAG_SUB_NIGHTMARE) = 1001
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_NIGHTMARE) == 0 ) {
			listmax = 0
			buff = lang("おい、ちょっと待て。貴様の面には見覚えがある…" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "…といったか？最近、このティリスの地で名を上げているそうじゃないか。", "Hey you! Hold up! I've seen your face before punk... " + cdatan(CDATAN_AKA, CHARA_PLAYER) + "... Ah yes... I remember now. You've been making quite a name for yourself throughout Tyris, haven't you?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("話を聞こう", "Fine, let's do it.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("止めておこう", "Not a chance, you worm.")
			listmax++
			buff = lang("貴様に金になる仕事をやろう。ザナンのある機関からの要望で、実験のデータをとるために被験者が必要なのだ。俺に言わせれば、被験者というより『生贄』だがな。生き延びて戻ってくれば、老後を遊んで暮らせるだけの報酬はくれてやる。どうするかこの場で決めろ。", "You wanna make some quick cash? I've got a request from the organization of Zanan. They need a test subject in order to get some experimental data. Or perhaps \"sacrifice\" would be a more appropriate term. *If* you survive, I'll pay you enough to party until you waste away. Make your decision quickly.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("骨のないやつだ。うせろ。", "Fine then, piss off you spineless coward.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("いいだろう。死ぬ準備ができたら、声をかけろ。実験場まで案内してやろう。", "Excellent. Let me know when you've finished your preparations for death and I'll take you to the testing grounds.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_NIGHTMARE) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_NIGHTMARE) == 1 | gdata(GDATA_FLAG_SUB_NIGHTMARE) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("いい", "Yes.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("だめ", "No.")
			listmax++
			buff = lang("フッ。よく逃げ出さずに戻ってきたな。準備はいいか。", "So, are you ready?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("早くしろ。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("よし…付いて来い。", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_NIGHTMARE) = 2
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_VERNIS
			gdata(GDATA_TELEPORT_LEVEL) = 5
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 6
			mapstarty = 27
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_NIGHTMARE) == 3 ) {
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_RUBYNUS
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 100000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("驚いた。この実験から生きて帰って来た冒険者は、貴様がはじめてだ。これで、ザナンの上層部が、研究を見直してくれるといいが。あんな不細工な化け物を率いて戦場を駆け回るのは、御免だからな。", "You came back alive... astonishing. You're the first and I swear you'll be the last. Zanan's senior commanders should find this data most intriguing. I do not want such a clumsy beast watching my back on the battlefield.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_NIGHTMARE) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 30 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_SLIME) == 1000 ) {
			listmax = 0
			buff = lang("あら、冒険者さん♪ お茶でもいかが？", "Hi there adventurer! Would you like some tea?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_SLIME) == 0 ) {
			listmax = 0
			buff = lang("あら、冒険者さん。ちょうどいいところに来てくれたわ。", "Ah, you... adventurer, you've definitely come to the right place.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "Sure thing.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("面倒だ", "Not now.")
			listmax++
			buff = lang("最近、朝起きると私のぬいぐるみがぼろぼろになっているの。夜中に起きてこっそり見張ってたらね、なんとプチ…プチがぬいぐるみ食べてるのよ！どうも隣の家の窓を伝って、私の家に入ってくるみたいなの。冒険者さん、お願いだから、ちょっと行って退治してきてくれない？", "Recently, when I wake in the morning my stuffed toys are always chewed up. So I stayed up last night and secretly watched. You know what I found? Putits were chewing up my stuffed animals! I'm pretty sure they're getting into my house through the window to the neighbor's place. I beg of you adventurer, won't you eradicate the Putits for me?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("気が変わったら、また来てね。", "Okay... but... if you change your mind please come back.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("頼もしいよ。問題の家は、すぐ南に隣接しているわ。頑張ってね！", "Fabulous! The trouble is all in the house just south of here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_SLIME) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_SLIME) == 1 ) {
			listmax = 0
			buff = lang("やだ！このぬいぐるみもプチに食べられてる。冒険者さん、はやく退治お願いね。", "Aggh! My stuffed toys are ruined! Hurry up and kill the Putits!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_SLIME) == 2 ) {
			flt calcobjlv(10), calcfixlv(FIX_QUALITY_GREAT)
			itemcreate -1, ITEM_ID_SMALL_SHIELD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt calcobjlv(10), calcfixlv(FIX_QUALITY_GREAT)
			itemcreate -1, ITEM_ID_GIRDLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("え、スライムを退治してくれたの？ありがとうございます♪あたしのぬいぐるみたちも喜んでるわ。これ、役に立つかわからないけど、とっておいてね。", "Eh? You wiped out the slimes? Thanks so much. My stuffed animals are delighted too. Here, you might find this useful.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_SLIME) = 1000
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 31 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_ROGUE) == 1000 ) {
			listmax = 0
			buff = lang("いらっしゃいませ〜♪その節はどうもです。", "Oh it's you, our hero.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_ROGUE) == 0 ) {
			if ( jp ) {
				listmax = 0
				buff = "いらっしゃいませ〜♪"
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "I'd be happy to help.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("面倒だ", "Don't bother me.")
			listmax++
			buff = lang("ちょっとお時間いいですか？バーの酒樽が度々盗まれて、店長困ってるんです。もし手が空いていたら、助けてくださいな。盗みを働いている輩の目星はついています。きっと、ヴェルニースを拠点に活動している、こそ泥の集団です！拠点は確か墓の方にあったはずですよ。", "Excuse me, do you have a moment? The kegs from the bar have been stolen, and the manager is really put out. If you're not busy, could you help out? It was those thugs, I'm sure of it. They've certainly set up a base inside Vernis. With all the mud they leave behind, they're most likely hanging out near the gravesite.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("そうですか…残念です。", "Alright, but please... think about it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("あ…ありがとうございます！よろしくお願いしますね！", "Really? Thanks, we really appreciate it.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_ROGUE) = 1
			cell_featset 48, 5, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 4
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_ROGUE) == 1 ) {
			listmax = 0
			buff = lang("いらっしゃいませ〜♪盗賊団の拠点はみつかりましたか？墓の近くにあるみたいですよ。", "Welcome! Welcome! Have you found the bandit's lair yet? It should be near the graveyard.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_ROGUE) == 2 ) {
			flt
			itemcreate -1, ITEM_ID_ROD_IDENTIFY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv((9 /*!!!@[INV_ITEM_AMMO @@@ INV_ITEM_CHARGE @@@ INV_ITEM_CLIENT @@@ INV_ITEM_FILE @@@ INV_ITEM_NEXT_PERIOD]@!!! */), ci) = 12
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MONSTER_BALL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_PARAM2, ci) = 5
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("ええ、噂はもう届いています。あのごろつき団がいなくなって、私達ほんと安心しました。ありがとうございます。少ないですが、店長がお礼にとこれを♪", "Yes, I've already gotten wind of your deeds. It's a big relief to be rid of those miscreants. I can't thank you enough. I know it isn't much, but please take this as a token of our thanks.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_ROGUE) = 1000
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 33 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_ULM_LESON ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
				listmax = 0
				buff = lang("ヴェセル…。", "Vessel...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			else {
				if ( gdata(GDATA_FLAG_ENCOUNTER_LARNNEIRE_IN_ULM_LESON) == 2 ) {
					listmax = 0
					buff = lang("あなたを見た時は驚いたわ。でもよく考えると、腕の立つ冒険者なのだからロストイルヴァを冒険していてもおかしくないのよね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( gdata(GDATA_FLAG_ENCOUNTER_LARNNEIRE_IN_ULM_LESON) == 1 ) {
					listmax = 0
					buff = lang("あなたは…もしかして" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ごめんなさい、別人のように見えてしまって気づかなかったの。また会えて嬉しいわ。メルガスでメシェーラを研究していた時以来ね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("あれから私達は異形の森の復興に尽力していたのだけれど、ヴェセルを捜しにここまで来たの。もし彼を見かけたら私達がここで待っていることを伝えて欲しいわ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_ENCOUNTER_LARNNEIRE_IN_ULM_LESON) = 2
				}
			}
			goto *chat_end
		}
		list(0, listmax) = 3
		listn(0, listmax) = lang("旅の目的", "What's your mission?")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("ノースティリスについて", "Tell me about North Tyris.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("獣に変えられた王子の童話", "You mentioned a fairy tale.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("無事に意識が戻ってよかったわ。あなたを最初に見たときは、もう手遅れかと思ったほどだもの。", "I'm glad you got well again. I thought you were a goner until just a few minutes ago.")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("あなたは、こんな話を聞いたことがあるかしら。", "You may have heard of this story before...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あるところに、魔法によって醜い獣のような姿に変えられた 王子がいた…彼は自分の姿に絶望し、国を捨て森の中の小屋で暮らしたの。", "There was once a prince who had his shape changed into that of an ugly beast by a witch. Desparing of his bloodcurdling appearince, he hid himself in a forest far from his kingdom.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ある日、狼に襲われ傷ついた女が、小屋に駆け込み倒れこんだ。王子は迷いながらも女を介抱した。彼女はもちろん男の姿におびえて泣いたわ。", "One day a woman rushed into his hut and passed out, having been badly wounded by a pack of wolves. He treated her with the knowledge and fear that his ugly body might scare her. This fear turned into reality soon enough for she cried out as she woke up.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("でも、何日もたち彼女は男の優しさに気付いた。彼の誠実さに、抱える葛藤に、心を打たれたの。怪我の癒えた女は、男のささえとなり共に暮らすことを決心した…", "However, as days passed by and she regained her health, the woman had come to recognize the kindness and generosity that was within the heart of this beast. She decided to live with him.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("二人は様々な困難を力を合わせ乗り越えて、最後には王子にかけられた呪いがとけ、二人が結婚して幸せな結末を迎えるの。どこにでもある物語ね。", "They surmounted various difficulties throughout their lives and the story comes to an end when the curse set upon the prince finally breaks and the beast returned to his former shape, that of a flawlessly handsome prince. The two of them got along very well from that time on. A happy ending... although just an ordinary story, don't you agree?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("わたしは、小さい頃聞かされたこのおとぎ話が、どうしても好きになれなかった。きっと、醜い姿の男に対する愛が、どこかに置き去りにされた気がしたのね。", "The elders of Vindale used to tell me this kind of story. But I never liked this one. Perhaps it is because I felt like the beast that she loved despite the fact that his ugliness had been left behind somewhere.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("アセリア大陸から大洋を隔たち、ティリス大陸の北に位置するのがノースティリスよ。自由と平和の国パルミアの統治の下、古代の遺跡群ネフィアを巡り、多くの旅人や商人がこの地を訪れるの。あなたも、そんな旅人の一人かしら？", "North Tyris is a continent in the western hemisphere of Irva. Lots of merchants and travelers set foot on North Tyris' soil with the dream of making a fortune in the ancient ruins of Nefia.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("目的が決まっていないのなら、南の炭鉱街ヴェルニースに行くといいわ。あなたの探しているものが、見つかるかもしれない。", "If you haven't set your destination yet, you might want to visit Vernis. It's a mining town located south of here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("私達はヴィンデールの森からの使者。公正なるジャビ王と会見し、森とエレアの民に降りかかる嫌疑を晴らすために、王都パルミアに向かっているの。", "We are messengers from Vindale. Our journey ends here in North Tyris at the great city of Palmia where we're supposed to have a talk with King Xabi, a man of fairness and compassion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 868 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_ULM_LESON ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
				listmax = 0
				buff = lang("ヴェセル！ヴェセル！ヴェセル！ヴェセルぅぅうううわぁああああああああああああああああああああああん！！！あぁああああ…ああ…あっあっー！あぁああああああ！！！ヴェセルヴェセルヴェセルぅううぁわぁああああ！！！妄想に出てくるヴェセルかっこよすぎよぅ！！あぁぁああ…あああ…あっあぁああああ！！ふぁぁあああんんっ！！あぁあああああ！かっこいい！ヴェセルぅ！かっこいい！あっああぁああ！夢でも会えて嬉し…いやぁああああああ！！！にゃああああああああん！！ぎゃああああああああ！！ぐあああああああああああ！！！夢なんて現実じゃない！！！！あ…妄想も夢もよく考えたら…いつも私を愛してくれたヴェセルは現実じゃない？にゃあああああああああああああん！！うぁああああああああああ！！そんなぁああああああ！！いやぁぁぁあああああああああ！！はぁああああああん！！この！ちきしょー！やめてやる！！現実なんかやめてやるわ！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			else {
				listmax = 0
				buff = lang("んもうっ、待ち遠しいわヴェセルぅ！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 34 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_ULM_LESON ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
				listmax = 0
				buff = lang("やれやれ。どうやら私はお邪魔虫のようだ。それにしても、ラーネイレはいつ森に帰るつもりなんだ？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			else {
				listmax = 0
				buff = lang("はぁ…ヴェセルの奴が来るまで待ちぼうけか。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ん？誰だか知らないが、そっとしておいてくれ…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( chatflag == 0 ) {
			if ( gdata(GDATA_FLAG_TUTORIAL) == 0 ) {
				buff = lang("見たところ君はノースティリスの人間ではないようだ。余計な世話でなければ、我々が旅を再開する前に、この土地での生活の知恵を授ける程度の時間は割けるのだが。", "It looks like you aren't familiar with this land. Before we leave, I can spare a moment to teach you a few lessons.")
				list(0, listmax) = 1
				listn(0, listmax) = lang("チュートリアルを開始する", "I would like to play the tutorial.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("余計な世話だ", "I can take care of myself.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 1 ) {
				listmax = 0
				buff = lang("移動方法はもうわかっているだろうが、もしカーソルキーを使っているのならば、できるだけテンキーでの移動に変えたほうがいいだろう。テンキーの周りには、床のアイテムを取得する0キー、その場で足踏みする5キー、そして射撃などのターゲットを指定できる*キーがあるからな。", "Pray tell me at least you know how to move. Sure, pressing cursor keys will do. But it's better to use a key pad if you have one. By using a key pad, you can easily acceess keys which are used frequently. Like [0] for picking up stuff, [5] for passing a turn and [*] for targeting.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("全ての行動はz,x,cの三つのキーから選択できる。zキーでは主に魔法やスキルなどの動作、xキーではアイテムの操作、cキーでは能力の確認や装備の変更ができるぞ。", "Although there're many actions you can perform, basically you can access them by using only 3 keys, [z],[x] and [c].")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("まずはxキーを使って、最も基本的で重要な行動である食事をしてみよう。注意しておけ、空腹になると体力の回復速度が下がり、餓死寸前になると当然体力は減って行くからな。食料は店で買ったり、栽培したり、たまに敵が残す死体を食べることもできる。", "Let's try them now. First, you need food to live. If your stomach is empty, you will lose Hp and your action speed slows down. Make sure you always have enough food in your inventory.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				buff = lang("君の足元に食料を置いた。xキーを押して、テンキーを使い「食べる」のメニューに移動して、足元のものを選択するんだ。", "Here, you can have this meat. Press [x], then press [9] a few times to select the <Eat> menu. Once there, select the meat on the ground.")
				list(0, listmax) = 0
				listn(0, listmax) = lang("わかった", "Alright.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("食べた", "I ate it.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 2 ) {
				listmax = 0
				buff = lang("zキーでは、魔法やスキルを使用したり、体当たりや採掘など、やや特殊な行動を行うことができる。体当たりは、鍵のかかったドアを壊したり、果実を木から落としたい時に実行するといい。", "You can perform skills or other actions including bashing and digging by pressing [z]. Here's a tip. You can bash doors to break locks and trees to get some fruits. It can be used to wake someone, but surely they won't be happy.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("zキーの他にも、スペースキーの活用法を覚えておこう。スペースキーは、足元にあるものに応じて自動的に行動を選択してくれるキーだ。例えば足元に階段があれば昇り降りを、宝箱があれば「開く」を、祭壇があれば「捧げる」をなど、自動的に選択してくれる便利なキーだ。", "Also, remember that [space] key is a very useful key. When there's an object under your foot, it automatically chooses a suitable action for you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				buff = lang("そうだな… 「穴掘り」実際に試してみようか。穴掘りは、道を短縮したり、鉱石を採掘したり、家のレイアウトを変える時に使う。とりあえず適当な壁の前に立ち、zキーを押して穴を掘ってみてくれ。", "Let's try it now. Try digging some walls by pressing [z] and choosing <dig>.")
				list(0, listmax) = 0
				listn(0, listmax) = lang("わかった", "Okay.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 3 ) {
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				flt
				itemcreate -1, ITEM_ID_SCROLL_IDENTIFY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_KNOWN, ci) = ITEM_KNOWN_FULL
				listmax = 0
				buff = lang("どうやら何かを掘り当てたようだな。", "Looks like you found something.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TUTORIAL) = 4
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 4 ) {
				listmax = 0
				buff = lang("多くのアイテムは、その本当の価値を知るためには「鑑定」をする必要がある。街にいる魔術士に頼んだり、鑑定の巻物や、魔法を唱えることで鑑定はできる。未鑑定の品は店での売価が低くなり、中には呪われた装備、危険な効果のポーションや巻物もあるので注意が必要だ。", "Many items need to be identified before you can know what exactly they are. You can identify items by reading some scrolls or asking a wizard in a town. Remember that using unidentified potions or scrolls is very dangerous. ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("武器や防具は、長い間所持していると簡単な鑑定がされるが、 装備の持つ魔力や本当の性能を知るためには、やはり鑑定が必要だ。強力な力を秘めたアイテムの中には、上位の鑑定を必要とするものもあるので、未判明のまま店に売ってしまうことは賢明ではないだろう。", "Weapons and armor also need to be identified. If you carry them long enough, you will get a hunch as to how good they are. But to gain full knowledge of the items, you need to identify them.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				buff = lang("では、今足元に置いた鑑定の巻物をxキーで読み、その金塊を鑑定してみるんだ。", "Now I'll give you a scroll of identify. Read it by using [x] and identify the gold bar you just found.")
				list(0, listmax) = 0
				listn(0, listmax) = lang("結果は想像がつくが、やってみるか…", "Alright, I will try.")
				listmax++
				list(0, listmax) = 4
				listn(0, listmax) = lang("鑑定した", "Done...")
				listmax++
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 5 ) {
				buff = lang("アイテムを装備するには、cキーから装備画面を呼び出せばいい。呪われたアイテムを装備してしまうと、装備が外せなくなるだけではなく、様々な悪い効果が発生する。その弓も呪われているので、解呪の巻物で呪いをといてくれ。", "To equip weapons or armor, press [c] and press [9] to select [Wear]. Note that if you wear cursed equipment, they can't be removed normally and cause some unwelcome effects. That bow is cursed. Use this scroll of uncurse to uncurse it.")
				list(0, listmax) = 0
				listn(0, listmax) = lang("わかった", "Will do.")
				listmax++
				list(0, listmax) = 5
				listn(0, listmax) = lang("装備も解呪も終わった", "All done.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 6 ) {
				p = findchara(CREATURE_ID_PUTIT)
				if ( p != 0 ) {
					listmax = 0
					buff = lang("どうした？ まだ全てのプチを倒していないぞ。", "Kill them all.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				listmax = 0
				buff = lang("上出来だ。", "Well done.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TUTORIAL) = 7
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 7 ) {
				listmax = 0
				buff = lang("ノースティリスの地理について少し説明しよう。シエラ・テールには幾多の国が存在するが、ノースティリスはどの国の支配も受けておらず、《ネフィア》と呼ばれる迷宮群が存在する特殊な場所だ。この地では、地殻変動とともに新しい迷宮がしばしば生成される。", "Let's learn a little history of North Tyris. This sacred land governed by Palmia is known for ancient ruins <<Nefia>>. Occasionally new ruins are found and lost by erratic movements of the earths crust.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("迷宮の主を倒すと貴重な物資や財宝が手に入るため、冒険者にとっては格好の収入源になるわけだ。迷宮に辿り着くと、その場所の危険度が表示される。危険度が自分の適正より高い場所を探索することは、まず避けたほうがいいだろう。", "A lord lives at the lowest layer of these ruins, protecting great treasures and therefore attracts numerous adventurers. However, avoid those ruins which exceed your current level. You may gain a lot, but you may lose your life.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_CHEST, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_PARAM1, ci) = 35, 25
				flt
				itemcreate -1, ITEM_ID_LOCKPICK, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				listmax = 0
				buff = lang("迷宮に落ちている宝箱の開け方を知っておく必要があるな。足元に宝箱を置くので、xキー、またはスペースキーで開けてみるんだ。", "You might find chests containing loot in ruins. There's one nearby, open it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TUTORIAL) = 8
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 8 ) {
				listmax = 0
				buff = lang("鍵が掛かっているだろう？宝箱の鍵は、ロックピックを所持していて、鍵開けの技術が開錠に必要な値に達していないと、解除することはできない。君の技量では、おそらくその宝箱を開けることはできないな。宝箱はダンジョンに落ちていることもあるが、生憎と重い代物なので、開けられない時は諦めるしかないだろう。気をつけろ…宝箱に押しつぶされて無様な死をさらした冒険者を、私は何度となく見てきている。", "Notice the chest has a lock? Locked chests require sufficient lockpick skill and lockpicks to open. You need to practice to open that chest. Be aware, those chests are heavy and trust me, give up if you can't open them when you're in dungeons. I saw a fool running around with a chest on his back and he got killed.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("宝箱に限らず、荷物を持ちすぎると速度にペナルティが課せられるため注意が必要だな。必要のないアイテムは、家に保管しておくといい。", "As you explore dungeons, your backpack may get heavier. Remember to leave stuff you don't need in your house. Overweight will slow your movement.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("最後に、家について説明しておこう。家はアイテムを保管したり、安全に魔法を学んだり、また単に装飾して楽しむために利用できる。家の権利書を購入できるようになれば、ノースティリスの好きな場所に家を建てることができるだろう。ただし、家は一つしか持てない。", "Finally, I'm going to explain a bit about your house. As you already know, you can safely store items in your house. And the salary chest periodically gets filled with some gold and items. Eventually you might be able to buy a new house.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("家の中にあるハウスボードを使うことで、壁を作ったり、床の模様を変えたり、滞在者を募ったりできるぞ。後で試してみるといい。", "You can do several things by using a house board in your house. Try it later.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TUTORIAL) = 99
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 99 ) {
				listmax = 0
				buff = lang("よくやった。これでノースティリスで生活するための、基本的な知識は身に付いたわけだ。自分の状態を把握し、慎重に行動すれば、瀕死の状態を高慢なエレアに拾われ、講釈をたれられることも、もうないだろう（ロミアスはニヤリと笑った）。", "Alright, we're finished. You should already know how to survive in North Tyris by now.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("まだ目的が定まっていないのなら、南のヴェルニースを訪れるといい。ネフィアの迷宮群を巡るのも、貴重な経験になるだろう。この世界で何を見て、如何な足跡を残すか決めるのは、君自身だ。", "(You've finished the tutorial!)")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TUTORIAL) = -1
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == (-1) ) {
				buff = lang("まだ、何か用があるのか？", "What?")
				list(0, listmax) = 0
				listn(0, listmax) = lang("何も", "Nothing.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_TUTORIAL) == 0 | gdata(GDATA_FLAG_TUTORIAL) == (-1) ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("そろそろ出発したら？", "You had better leave soon to Palmia.")
				listmax++
			}
			gosub *chat_select
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("賢明な判断だな。まずは最も基本的な動作から教えよう。", "A wise choice. I will start from the beginning.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_CORPSE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_SUB_NAME, ci) = 9
			inv(INV_ITEM_KNOWN, ci) = ITEM_KNOWN_FULL
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gdata(GDATA_FLAG_TUTORIAL) = 1
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			tc = findcharala(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
			if ( tc == CHARA_PLAYER ) {
				tc = findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
				listmax = 0
				buff = lang("ラーネイレ…？ どこにいったんだ？ まさか貴様！", "You...you scum!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cdata(CDATA_EMO_ICON, tc) = 18 + 100 * 4
				txtef COLOR_PURPLE
				txtmore
				txt lang(name(tc) + "は激昂した。", name(tc) + " gets furious!")
				cdata(CDATA_RELATION, tc) = RELATION_ENEMY
				cdata(CDATA_AI_AGGRO, tc) = 80
				cdata(CDATA_TARGET, tc) = 0
				goto *chat_end
			}
			listmax = 0
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "の言うとおり、私達に残されている時間は少ないわ。こうしている間にも、新王国のかの者の計画は着実に進んでいる… 出発しよう、ロミアス。", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + " is right. The time left for us is scarce. We need to depart, Lomias.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ああ、わかった。パルミアまでは長い旅路だ。一時であれ休息をとれて良かったのかも知れないな。", "Yes. Palmia is still far away. Perhaps, It was fortunate that we could have a little rest before everything begins.")
			tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("少ないけれど、いくつかアイテムを残していくから役立てて。…それじゃあ、また巡り会う時まで、" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "、あなたに風の加護のあらんことを。", "It's a little, but I'll leave some items, so please use it. Farewell..until we meet again. May the blessing of Lulwy be with you.")
			tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			chara_vanquish findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
			chara_vanquish findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
			txt lang("二人は荷物をまとめ洞窟を後にした。", "Lomias and Larnneire leave the cave.")
			txtvalid = 0
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			flt
			itemcreate -1, ITEM_ID_POTION_CURE_CORRUPTION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			flt
			itemcreate -1, ITEM_ID_POT_FOR_FUSION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			flt
			itemcreate -1, ITEM_ID_LEASH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			flt
			itemcreate -1, ITEM_ID_STETHOSCOPE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			flt
			itemcreate -1, ITEM_ID_FILTRATION_BOTTLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_PARAM2, ci) = 5
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("…本当に食べてしまったのか？", "...err..you really ate that thing? Oh well..")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = lang("えー", "What!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("食料以外のアイテムも、同様にxキーで操作することになる。例えば巻物や魔法書は「読む」のメニューから、杖は「振る」のメニューから使用できる。", "You can also use other items by pressing [x]. For example, if you want to read a book, press [x], hit [9] a few times to select the <Read> menu, then choose a book you want to read.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( jp ) {
				listmax = 0
				buff = lang("次はzキーでの行動について説明しよう。", "")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_TUTORIAL) = 2
			goto *chat_unique
		}
		if ( chatval == 4 ) {
			listmax = 0
			buff = lang("（ロミアスはにやりと笑った。どうやら、この偽物の金塊は彼がいつの間にか埋めたらしい…）", "(Lomias grins. Looks like he buried it unnoticed.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("次は戦闘について教えるが、戦う前にまずアイテムを装備し、準備を整えなければならない。使い古しだが、君のために弓と矢を用意した。射撃武器のほとんどは、例えば銃なら弾丸のように、対応した矢・弾を装備する必要がある。", "Okay, I will now tell you how to fight. Before the combat starts, you need to equip weapons. Take my old bow and arrows and equip them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_LONG_BOW, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_CURSED
			flt
			itemcreate -1, ITEM_ID_ARROW, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			flt
			itemcreate -1, ITEM_ID_SCROLL_VANISH_CURSE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_KNOWN, ci) = ITEM_KNOWN_FULL
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gdata(GDATA_FLAG_TUTORIAL) = 5
			goto *chat_unique
		}
		if ( chatval == 5 ) {
			listmax = 0
			buff = lang("…よし、いよいよ戦闘の説明だ。", "Good. Now listen carefully.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("相手を打撃で攻撃するには、単に相手の方向のキーを押すだけだ。射撃も、zキーから射撃を選択するだけで、一番近くの敵を自動的に攻撃する。攻撃する敵を予め指定したいときは*キーを押せばいい。", "By moving towards a target, you automatically attack it with your close range weapon. To use your long range weapon, you can either press [z] and choose [Shoot] or simply press [f] (Fire). You will shoot a nearby enemy. If you want to change your target, press [*].")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("では、訓練用のモンスターを召喚するぞ。可能なら、使い勝手を知るために射撃で倒してみてくれ。ただし、弓は近接して打っても威力が低いので注意が必要だ。危なくなった時のために、治療薬を足元に置いておこう…必要ないとは思うが。ポーションの飲み方はわかるな？…そう、xキーだ。", "Get ready. I will summon some weak monsters. Kill them with your bow if possible. Try to stay away from the enemies as bows aren't effective in close range. I've dropped a few potions in case you get hurt. You know how to use them right? Yes, use [x] key.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_SKY_BLUE
			txt lang("ロミアスは隠し持っていたプチを放った。", "Lomias releases tiny cute creatures.")
			repeat 3
				flt
				characreate -1, CREATURE_ID_PUTIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
				cbitmod CHARA_BIT_NO_TARGET, rc, TRUE
			loop
			flt
			itemcreate -1, ITEM_ID_POTION_CURE_MINOR_WOUND, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_KNOWN, ci) = ITEM_KNOWN_FULL
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gdata(GDATA_FLAG_TUTORIAL) = 6
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 79 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 200 ) {
			listmax = 0
			buff = lang("王の死に続き、世界の崩壊の始まり…パルミアは…シエラ・テールは困難な時代にいます。このまま、イルヴァは最後を迎えてしまうのでしょうか。", "The tragedy of our king, and the collapse of our world, Palmia is...Sierre Terre is in difficult times.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) < 90 ) {
			listmax = 0
			buff = lang("パルミアは歴史ある自由と文化の国。大アセリア大陸の戦乱から離れ、静かに育ってきた国です。王制政治が廃止された今でも、この平和を培った代々の王と夫であるジャビ王に、民は忠誠を尽くしてくれています。", "Palmia is an old and historical kingdom which respects peace and freedom. The royalism has been obsoleted long time ago, but people still carry loyalty to the old kings and my husband, King Xabi.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		else {
			listmax = 0
			buff = lang("王の悲報にパルミアは衝撃を受けています。あの方のいない王座は、なんと冷たく感じられることでしょう…", "The whole kingdom is devastated to hear the tragic news of our king. This throne... it feels very cold without him.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 80 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("スターシャは、パルミアは、今どうなっているのだ…？", "Any assistance you provide to us will be most welcome.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 60 ) {
			listmax = 0
			buff = lang("汝の活躍を楽しみにしておるぞ。", "Any assistance you provide to us will be most welcome.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 50 ) {
			listmax = 0
			buff = lang("今の時勢、信頼に足る者の剣を遊ばせておくのは惜しい。もしパルミアの元で働く意志があるのなら、城の図書室にいるエリステアを訪ねるがよい。十分な報酬と名誉を約束しよう。", "In times like these, we can't afford to let those we can trust sit idly on their swords. If you're willing to serve Palmia, then you should pay a visit to Erystia in the library. We will be sure to reward you with honor and riches commiserate with your sacrifice.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 40 ) {
			gdata(GDATA_FLAG_MAIN) = 50
			listmax = 0
			buff = lang("斥候からの知らせを預かっていると？…よかろう、その者の書簡をわしの手元へ。", "What's that you say? You have a message from one of our scouts? ...Very well then, let me see it.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("…なるほど、恐れていたことが現実になったというわけだな。知らせにあるよう、悪しき者がレシマスに眠る秘宝を狙うのであれば、パルミアの王の名誉にかけてそれを阻止せねばなるまい…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "とやら、大儀であった。直ちに謝礼を用意させようではないか。", "I see... then what we feared has indeed come to pass. If evil has set its sights on the artifact hidden within the depths of Lesimas, then for the honor of our King we are bound to stop it... You must be tired, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". Give me a moment to prepare appropriate compensation for your efforts today.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_POTION_CURE_CORRUPTION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_TREASURE_MAP, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_unique
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 139 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 20 ) {
			gdata(GDATA_FLAG_MAIN) = 30
			listmax = 0
			buff = lang("…そこの御人…頼みが…頼みがある…私はパルミアの斥候…王の命令でレシマスに潜んでいた者だ…", "You...you there, listen to me. *cough* *cough* I-I'm a scout of Palmia...I've been investigating on Zanan's strange activities in Lesimas.... *cough* I have a favor to ask...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("詳しく説明する体力は…私には…もう残っていない…ジャビ王にこの書簡を…届けて頂きたい…二つの大国の衝突を…シエラ・テールの…危機を…防ぐために…", "T-There's no time left for me to explain all....my wounds are... *cough* Listen, Sierre Terre is in danger...this letter..this letter...this is our only hope....")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…う、うぅ…貴方を…信じる以外にもう希望はない…私の所持品は…自由にして構わない…どうか、この知らせを…パルミアに…", "I have no choice but to trust you.... *cough* bring the letter to Palmia.... I-I beg you...You can take everything I have on me...so... plea..s..")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			repeat 4
				flt calcobjlv(gdata(GDATA_LEVEL)), calcfixlv(FIX_QUALITY_GOOD)
				flttypemajor = fltsetdungeon()
				itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 0
			loop
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 1000 + rnd(200)
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 3
			flt
			itemcreate -1, ITEM_ID_BEJEWELED_CHEST, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 0
			inv(INV_ITEM_PARAM2, ci) = 0
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャビ王への書簡を受け取った。ジャーナルが更新された。", "You recive a letter to the king Xabi. Your journal has been updated.")
			txtvalid = 0
			txt lang(cdatan(CDATAN_NAME, tc) + "は静かに息絶えた…", cdatan(CDATAN_NAME, tc) + " dies silently...")
			chara_vanquish tc
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 142 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) == 300 ) {
			listmax = 0
			buff = lang("えっ？ＬＦ計画の説明と参加手続きですか。…すみません、次回のアップデートを待ってくださいね。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 1010 ) {
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) < 300 ) {
				gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) = 300
				listmax = 0
				buff = lang("生還、できたのですね" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。本当によかった…。どう見ても限界を超えた力を行使していたので、ずっと心配していました。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…私のほうは、今はＬＦ計画関係の手続きや情報整理をしています。歴史の謎の多い部分に迫るチャンスですからね。大変だけどやりがいがあります。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("もし興味があったら、ぜひパルミア所属で参加してください。あなたならきっとＬＦ計画を成功に導いてくれるでしょう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 840 & gdata(GDATA_FLAG_MAIN) < 1010 ) {
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) >= 200 ) {
				listmax = 0
				buff = lang("私は私がやるべき仕事を続けます。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あなたもあなたの冒険を続けてください。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) < 200 ) {
				gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) = 200
				listmax = 0
				buff = lang("時々、脳裏に見たことのないはずの景色が浮かびます。あれが妖精の言っていたガイアス・ヴィスなのかもしれません。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("妖精から教えられたのですが…。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、混沌の神が蘇るというのは本当ですか？そしてあなたがそれと戦うというのも。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("信じて待っていてほしい", "I think not.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("神に挑むのも冒険だ", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("記録よりも記憶に残りたい", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("この戦いが終わったら結婚しよう", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("人類の歴史にはいくつかの神殺しの記録があります。どこまでが真実でどこからがフィクションかわかりませんが。もしも成し遂げたなら、あなたも記録に残るのでしょうか。", "Sorry, this is untranslated sentence.")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("ふふっ。新たな歴史の誕生を待っていますね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("なるほど…。これが冒険者の心理なのですね。ですが、油断はしないでください。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("わかりました。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("では" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。あなたのことは私が覚えておきます。たとえ、どんな結果になったとしても。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("それ、物語なら死ぬパターンですよ？縁起でもないから冗談でもやめてくださいね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 100 ) {
			listmax = 0
			buff = lang("生体兵器の殲滅、お疲れ様でした。こちらが報酬です。エユルにも後ほど報酬を渡しておきます。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_BRILLIANT_DIAMOND, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_ASTRAL_LIGHT_PEN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 20000
			flt
			itemcreate -1, ITEM_ID_SCROLL_GAIN_ATTRIBUTE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 1000
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 100 ) {
			listmax = 0
			buff = lang("メイルーンの悪魔討伐、お疲れ様でした。あら？マルカは一緒ではないのですか？", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("とりあえず、こちらがあなたの分の報酬です。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_ARGENT_SNOW, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_SCROLL_GROWTH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			flt
			itemcreate -1, ITEM_ID_BOOK_RESURRECTION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_BOTTLE_SPEED_UPPER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_POTION_SOMA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) = 1000
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) == 100 ) {
			listmax = 0
			buff = lang("エウダーナの悪魔討伐、お疲れ様でした。こちらが今回分の報酬ですよ。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_ROD_WISHING, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_SOUL_SPINDLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_POTION_NEKTAR, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			flt
			itemcreate -1, ITEM_ID_POTION_POTENTIAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) = 1000
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 100 ) {
			listmax = 0
			buff = lang("キッカスの悪魔討伐、お疲れ様でした。こちらが今回分の報酬ですよ。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_TOKEN_FRIENDSHIP, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_SPECIAL_STEAMED_MEAT_BUN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_PUFF_PUFF_BREAD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_MANDRAKE_SALAD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_PUTITORO, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 500000
			gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) = 1000
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) >= 100 ) {
				listmax = 0
				buff = lang("混沌の神が倒され、ロスリアに集まっていた混沌の精霊たちも各国の連合軍に鎮圧されました。事実上、混沌の軍勢は滅んだことになりますね。ロストイルヴァの情報も大体集まりましたし、ようやく私も落ち着けます。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) < 100 ) {
				listmax = 0
				buff = lang("ええっ？混沌の神を倒した…のですか？あなたが？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…あまり、そのことは喋らない方がいいかもしれませんよ。邪神を殺した英雄がそれ以上の危険因子として迫害されることがあるのは、歴史が証明しています。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) = 100
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) + gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) + gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) + gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) >= 4000 ) {
			listmax = 0
			buff = lang("私の方では、今のところ新たに依頼する討伐もありません。ロストイルヴァの調査をお願いします。何か新しい発見があれば、内容に応じて報酬を出しましょう。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_MAIN) <= 470 ) {
				listmax = 0
				buff = lang("ああ、そういえばイェルス軍からご指名がありましたよ。あなたの手が空いたら、メルガスへ来てほしいとのことでした。依頼の内容と指名の理由は明かしてくれませんでしたが…。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 404 & gdata(GDATA_FLAG_MAIN) < 440 ) {
			if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) < 100 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("生体兵器の殲滅（ザナン）", "I undertake.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) < 100 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("破壊の悪魔討伐（メイルーン）", "Not matter?")
				listmax++
			}
			if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) < 100 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("復讐の悪魔討伐（エウダーナ）", "Not matter.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) < 100 ) {
				list(0, listmax) = 3
				listn(0, listmax) = lang("疫病の悪魔討伐（キッカス）", "Not matter")
				listmax++
			}
			buff = lang("よく来てくれました。" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あなた向きの国家依頼を紹介します。ひとつ達成するごとに報酬を渡しましょう。", "Okay. Wait here.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("資料によると、ザナンのある機関で研究されていた生体兵器が暴走したとされています。なんでも、メシェーラの特性で強化した生物を薬物でコントロールしようとしていたとか。イェルスでメシェーラの無害化にも携わったというあなたは適任でしょう。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("なお、研究施設の元関係者を名乗る冒険者が名乗りをあげたので彼にも同行してもらいます。彼の名前はエユル。目的地への案内人も兼ねているので、準備が出来次第接触してください。現在は関所のノースティリス側宿屋に待機してもらっています。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 0 ) {
					gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 1
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("永世中立を保ち他国との関わりを避けていたメイルーンですが、突如として現れた悪魔と混沌の精霊たちの襲撃に対抗できる戦力が無く、今回救援を要請してきました。あなたの前に派遣した討伐隊は、メイルーン唯一の港ごと船を破壊され壊滅しています。冒険者は驚くことに泳いで帰ってきましたが、しばらくは再起不能でしょう。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("あなたにはメイルーンを熟知した銀熊…マルカとともにノイエルから陸路で現地へ向かってもらいます。彼女はノイエルの教会に待機させているので早く合流してください。元から人の話を殆ど聞かないうえに、襲撃を受けているのは彼女の故郷…時間が経つと独断先行してしまうかもしれません。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 0 ) {
					gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) = 1
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("ニーサ沿岸部に突如出現したとされる復讐の悪魔は、大陸を東に移動しつつエウダーナ王都に到達。王都は以前から混沌の精霊たちの襲撃を受けており、その度に魔術旅団が対応にあたっていました。しかし悪魔到達後に状況は一変。下級・中級の魔術士たちが悪魔にそそのかされ敵に回ったのです。元よりエリート主義のエウダーナにおいて才能に劣る者は辛く惨めな思いをするものだと聞きます。おそらくは悪魔はその心の隅間に付け込んだのでしょう。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ロストイルヴァ探索に出ていたエウダーナの調査部隊が敵勢力に２度挑んでいますが、老魔導士のライゼル以外に渡り合える者がおらず、撤退を繰り返しています。再攻撃を予定している魔導船に乗って最前線に赴き、王宮周辺の結界に籠城している魔術士たちと協力して敵勢力を殲滅…それが今回の国家依頼です。大混戦が予想されるので注意してください。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) == 0 ) {
					gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) = 1
				}
				goto *chat_end
			}
			if ( chatval == 3 ) {
				listmax = 0
				buff = lang("キッカス辺境の街が一夜にして地獄に変わりました。疫病の悪魔を名乗る怪物が瘴気で街を包み込んだのです。人々が次々に死亡し、アンデッドに変えられて街を徘徊しているのだとか。街から逃げ出してきた旅人はこの報告直後に全身から血を噴き出して死亡、さらに介抱した周囲の人々も２４時間以内に死亡したそうです。事態を重く見たキッカス政府は周辺を封鎖し、同盟に救援を要請してきました。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("今回は歴戦の傭兵部隊に同行することになります。現地へはロストイルヴァ北端に存在する神艦から小型艇で移動する手筈です。詳しくは神艦で準備を進めている傭兵のリーダー、シュライダと打ち合わせをお願いします。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 0 ) {
					gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) = 1
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 382 ) {
			if ( gdata(GDATA_FLAG_MAIN) < 404 ) {
				listmax = 0
				buff = lang("今後も何かあったら協力をお願いするかもしれませんね。" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、頼りにしていますよ。", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 380 ) {
			listmax = 0
			buff = lang("銀熊と遭遇した…？わかりました、一度順を追って報告をお願いします。", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("なるほど、そういうことでしたか。…メイルーンに送り込まれた化け物というのも気になりますね。この件に関してはこちらでメイルーンに連絡を取ってみます。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それと、今回の報酬を渡しておきます。今後も何かあったら協力をお願いするかもしれませんね。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_ASTEROID_BELT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			flt
			itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			flt
			itemcreate -1, ITEM_ID_CARD_PACK, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 500
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_MAIN) = 382
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 363 ) {
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) == 1 ) {
				listmax = 0
				buff = lang("メイルーンの銀熊の足取りと、浮遊大陸の調査を独自に進めてください。調査の報酬は再検討しましょう。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 363 ) {
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) == 0 ) {
				listmax = 0
				buff = lang("ポート・カプールの船長から、航海中にあなたが突如行方不明になったとの報告を受けています。一体、何があったのか話してください。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				redraw 0
				gosub *screen_draw
				gosub *screen_fade
				gsel BUFFER_BUF
				pos 0, 0
				picload exedir + "graphic\\face1" + devfile + ".bmp", 1
				gsel BUFFER_SCREEN
				listmax = 0
				buff = lang("そんなことが…。今まで行方不明になった者たちはあの浮遊大陸に…？わかりました。メイルーンの銀熊の足取りと、浮遊大陸の調査を独自に進めてください。調査の報酬は再検討しましょう。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_4) = 1
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 362 ) {
			listmax = 0
			buff = lang("メイルーンに到着したら忘れずに、情報屋を介して連絡をくださいね。", "Do not forget to arrive at Mayroon, please contact me through the informer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 361 ) {
			listmax = 0
			buff = lang("私達の雇った冒険者の一人、メイルーンの銀熊は消息を絶つ寸前、故郷周辺の調査を行っていました。手始めに、あなたにはメイルーンの銀熊の足取りを追ってもらいたいのです。", "One of the adventurers who hired us, the silver bear Mayroon was never heard from again on the verge of doing a survey around his hometown. For starters, I would like to have you in the footsteps of his chase.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("わかった", "Will do.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("船だけは嫌だ", "I just do not want to take the ship.")
			listmax++
			buff = lang("銀熊の故郷はノイエルの遥か東、大陸極東の城下町…雪と氷に覆われ道などはないので陸路は避け、ポート・カプールから船で東部へ回り込んでください。", "His hometown is far east of Noyel. There is no road is covered in snow and ice. Avoid Roads, please wrap to the east by boat from the Port Kapul.")
			gosub *chat_select
			if ( chatval == 1 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("わかった", "Will do.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("絶対に船に乗りたくない", "I absolutely do not want to take a boat.")
				listmax++
				buff = lang(cdatan(CDATAN_NAME, CHARA_PLAYER) + "、大丈夫ですよ。洋上でのエーテル風も最近は観測されていません。それに私達の用意する探査船ならエーテル風程度で沈むことはないでしょう。", "It's okay. Recently has not been observed ether wind at sea. Would not be if the ship sank at about winds ether to provide our exploration it.")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("そ、そんなに怯えなくても…。あ！逃げないでください！", "Oh! Please do not run away!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("ポート・カプールの船長にはすでに話をつけています。この紹介状を持って行ってください。", "The captain of the port Kapoor wears the story already. Please take this with a letter of introduction.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("[紹介状]を受け取った。ジャーナルが更新された。", "You receive a kletter of introduction. Your journal has been updated.")
				gdata(GDATA_FLAG_MAIN) = 362
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 360 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("引き受けよう", "I undertake.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("どうでもいい", "Not matter.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("いくら出す？", "How much do you pay?")
			listmax++
			buff = lang(cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "…！またあなたの力が必要なのです。あなたもうわさで耳にしたことがあるかもしれませんが、今このイルヴァの各地で行方不明者が続出しています。あなたに頼みたいことは２つ。行方不明者の居所を突き止めること、そして事件の元凶を断つことです。", "We need your help again.  You may have heard a rumor that you also, who have been missing one after another in different parts of this Irva now. The two do you want to ask you. It is to break the curse of the incident, and that determine the whereabouts of missing persons.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("あなたの協力に感謝します。早速、メイルーンへ調査に赴いて欲しいのです。準備が出来たらまた話しかけてください。", "I appreciate your help. Today, I want to go survey to Mayroon. Please also was ready to talk.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("それと…そうですね、報酬は成果に応じて適宜支払いましょう。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 361
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("…。", "....")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("少し、考えさせてください。", "A little, please Let me think about it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("ごめんなさい。あなたにも選ぶ権利があるのに。私、つい舞い上がってしまって…。", "I am sorry. But you also have the right to choose.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 200 & gdata(GDATA_FLAG_MAIN) < 360 ) {
			listmax = 0
			buff = lang("私達は…失敗してしまったのですね、サイモアの陰謀を止めることに。あなたが《常闇の眼》を入手するために冒した危険も、カラムの命も、今となっては何の意味があったのでしょう。", "We... have failed, haven't we, to stop Saimore's scheme. Everything, the <Origin of Vice> you obtained from your travels and Karam's death, what did they all mean?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ゆっくりと死に行く世界の中で、私達は無力感と罪を抱えて生きています。森を忌み嫌い、あるいは迫害を見ぬふりをしてきた私達が、サイモアだけを責めることなどできません。いっそのこと、あの時森とともに…", "In this slowly dying world we can only carry on with our sins and weakness. Hating the forest or ignoring those who did, how can we blame Saimore.  Should have just, along with the forest...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ごめんなさい、つまらないことを聞かせてしまって。あなたは…あなたの目には…まだ希望の光が宿っている。" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あなたはその運命の先に何を見ているの？", "I'm sorry, forcing you to hear my blathering. You... Those eyes of your... They still have the glimmer of hope. " + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + " What can you see beyond this fate?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 120 ) {
			listmax = 0
			buff = lang(cdatan(CDATAN_NAME, CHARA_PLAYER) + "、遂に三つの魔石を揃えたのですね。私達の調査によれば、魔石はレシマスの秘宝の間への扉を開く鍵となるはず。しかし、レシマスの奥深くには恐ろしい魔物が巣食い、その場所が何階にあるかまでは判明していません。", "It looks like you've finally managed to acquire all three of the stones, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". According to the information we've learned during the excavation, these stones are the key that will unlock the deepest reaches of Lesimas. However, be aware that terrifying monsters lurk in the abyss of Lesimas, and we have no idea how deep those caverns go.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あなたの任務…おそらくは最後の任務は、決して易しいものではなく、失敗も許されません。レシマスの探索を進め、秘宝の間を見つけてください。そして、《常闇の眼》を地上に持ち帰り、ザナンの陰謀を阻止するのです。", "This will be your last mission. This will be no easy task, but failure is not an option. It's up to you to complete the excavation of Lesimas and find the room where the codex is kept. If you can't retrieve the <Origin of Vice> we will never be able to uncover the conspiracy Zanan has set into motion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("カラムの言葉が正しければ、秘宝を守るのは今は亡きザシム王の従弟ゼーム。ゼームは名うての魔術士としても知られていました。彼が生きながらえているのも何らかの魔力によるものでしょう。心して望んでください。", "If what Karam told us is true, the codex will be guarded by the late king Zashim's cousin, Zeome. Zeome was known to have been a very powerful warlock. In his day, he was by far the most powerful sorcerer alive. I wouldn't dare confront him unless you are extremely well prepared.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あなたの働きには、感謝の言葉だけでは報いきれません。この指輪は、スターシャ様からの贈り物です。あなたの冒険の成功を祈っています。", ": Words alone are not enough to express our gratitude for all that you have done. This ring is a gift from Queen Stersha. We are praying for your success, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ".")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("レシマス25階の鍵を受け取った。ジャーナルが更新された。", "You receive a key of Lesimas 25th level. Your journal has been updated.")
			flt
			itemcreate -1, ITEM_ID_PALMIA_PRIDE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gdata(GDATA_FLAG_MAIN) = 125
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 105 ) {
			listmax = 0
			buff = lang(cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あなたの帰還を待っていました。カラムは無事に…いえ、その顔を見れば想像はつきます。私達の救助は遅すぎたのですね…。", "I've been waiting for you to return, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". I hope that Karam is... no, I can tell just by looking at you. Then our rescue attempt was too little too late.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("…カラムの言葉、確かに受け取りました。《常闇の眼》という古宝の伝承は、私も本で読んだことがあります。それは、イルヴァの大地の上に築かれた全ての歴史の真相を網羅していると伝えられています。もしカラムが危惧していたように、ザナンのサイモアが自らの説の誤りを葬らんとするためレシマスの秘宝を狙っているとしたら、私達はそれを阻止しなければなりません。", "Well take Karam's message to heart then... I remember reading about the <Origin of Vice> in one of these books. I believe it referred to the codex as an exhaustive account of the history of all things in Irva. If, as Karam suspected, Saimore is trying to acquire the codex in order cover up gaps in his story we can't allow him to succeed.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…王の悲報はもう耳にしているでしょう。都の土は涙で濡れていようとも、今の私達に喪に服している時間はありません。カラムと、そして王の意思を継ぐためにも。", "I'm sure you've already heard the tragic news about the king. All of Palmia is heartbroken by the king's demise, but we can't afford to spend our time mourning right now. We have to push on, for Karam... and for the king's sake.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あなたの次の任務は、三つの魔石の入手です。魔石には、レシマスの秘密を解く何らかの力が秘められていると伝えられています。魔石の所在は判明していますが、どこも危険な場所ばかり。まずは、各地を探索し、経験を積んでから臨んだほうがいいでしょう。", "Your next task is to retrieve the three magic stones. It's written in several of our historical accounts that the stones are the key to unlocking the secrets of Lesimas. We already know where the stones are located, but actually getting the stones is definitely easier said than done. First, you should scout the areas, gather info, and build your strength.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("探索に必要な情報がある時は、いつでも私に話しかけてくださいね。帰還したばかりなのに酷ですが、あなたに期待しています。", "Please come see me anytime you get information that would be useful to the expedition. I know you've just returned and we're asking an awful lot of you, but I don't have any other choice.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("レシマス17階の鍵を受け取った。ジャーナルが更新された。", "You receive a key of Lesimas 17th level. Your journal has been updated.")
			gdata(GDATA_FLAG_MAIN) = 110
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 60 ) {
			buff = lang("あら、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。レシマスの調査は進んでいるかしら？ 何か必要な情報があれば、何でも聞いてください。", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", how's your investigation going? Ask anything you'd like to know.")
			list(0, listmax) = 6
			listn(0, listmax) = lang("レシマスについて", "Tell me about Lesimas.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("任務について", "Tell me about the mission again.")
			listmax++
			if ( gdata(GDATA_FLAG_MAIN) >= 100 & gdata(GDATA_FLAG_MAIN) <= 120 ) {
				list(0, listmax) = 3
				listn(0, listmax) = lang("古城について", "Tell me about the ancient castle.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("灼熱の塔について", "Tell me about the tower of infernos.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("死者の洞窟について", "Tell me about the crypt of the damned.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 6 ) {
				listmax = 0
				buff = lang("ネフィアの迷宮群の一つレシマスは、パルミアの歴史と大きな関わりがあります。エウダーナがネフィアの秘宝の一つにより繁栄を約束されたように、第三代の王ザシムと従弟ゼームもレシマスの秘宝を求め、調査隊を繰り出していました。", "Lesimas is one of the numerous ruins of Nefia. It is a significant part of the history of Palmia. The Eulderna sought to bring themselves prosperity using one of the treasures of Nefia, so the third king, Zashim and his cousin Zeome organized an excavation of Lesimas.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そしてある日、ついに調査隊の朗報を受けた王と従弟は、レシマスの最下層に赴き、秘宝の眠る部屋へと足を踏み入れたのです…しかし数刻の後、部屋から出てきたのはザシム王一人だけでした。ザシム王は側近にこう言ったと伝えられています。", "Eventually the king and his cousin received word that the excavation had been completed, so they descended to the deepest reaches of Lesimas. Together they set foot into the darkened room where the codex was said to reside, yet after some number of hours, King Zashim emerged from the chamber alone. As the legend goes, King Zashim said the following words to his aides.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("「それは強き者の手の内では決して輝くことはない。弱き者の手の内では黄金のごとくまばゆいが、その輝きを他の者に見せることはできない。この秘宝は、常闇の中で永遠に眠っているべきものなのだ。直ちに部屋を封印するがよい。従弟はもはや帰っては来るまい」", "\"In the hands of the mighty it was dull and lifeless, devoid of magic... yet... in the hands of the meek it shone with a brilliant golden light that defies description. But no matter how I tried, I could not show that light to anyone else. The codex was meant to remain in these depths for all of eternity. We should seal this place off. My cousin will never return from these caves.\"")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("秘宝が眠る部屋は魔術により封印され、封印の鍵となる三つの魔石は、それぞれ最も強大で邪悪な存在の元に送られたといいます。魔石の真の価値は知らさずに。", "And so, they sealed the cavern with powerful magic and bound that seal to three magic stones. They gave each stone to a powerful guardian and sent them to three separate strongholds to watch over the stones. I don't believe the guardians knew the true value or purpose of the stones. ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("以来、パルミアの代々の王家は、ザシム王の意思を引き継ぎ、洞窟にはもう何もないかのように、他の国々の目を欺いてきたのです。", "Since that day, the passing generations of Palmia's royal family have preserved King Zashim's mandate and we've always deceived the other nations into believing there was nothing of value in Lesimas.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 5 ) {
				if ( gdata(GDATA_FLAG_MAIN) >= 125 ) {
					listmax = 0
					buff = lang("レシマスの探索を進め、秘宝の間を見つけてください。あなたの任務は、《常闇の眼》を地上に持ち帰ることです。", "Complete the excavation of Lesimas and find the room where the codex is kept. And retrieve the <Origin of Vice>.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("カラムの言葉が正しければ、秘宝を守るのは今は亡きザシム王の従弟ゼーム。ゼームは名うての魔術士としても知られていました。彼が生きながらえているのも何らかの魔力によるものでしょう。心して臨んでください。", "If what Karam told us is true, the codex will be guarded by the late king Zashim's cousin, Zeome. Zeome was known to have been a very powerful warlock. In his day, he was by far the most powerful sorcerer alive. I wouldn't dare confront him unless you are extremely well prepared.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 110 ) {
					listmax = 0
					buff = lang("あなたの任務は、三つの魔石の入手です。魔石には、レシマスの秘密を解く何らかの力が秘められていると伝えられています。魔石の所在は判明していますが、どこも危険な場所ばかり。まずは、各地を探索し、経験を積んでから臨んだほうがいいでしょう。", "Your next task is to retrieve the three magic stones. It's said that the stones are the key to unlocking the secrets of Lesimas. We already know where the stones are located, but all three locations are extremely dangerous. First, you should scout the areas, gather info, and build your strength.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				listmax = 0
				buff = lang("あなたの任務は、レシマスに赴き、冒険者カラムとの接触を試みることです。彼から最後に連絡があったのは、レシマスの16階。おそらくは、より深層に探索の手を広げているでしょう。彼の足取りをつかみ、私に報告してください。", "Your duty will be to penetrate into Lesimas and make contact with <Karam>. In the last report we received from him, he told us he was on the 16th floor. If you can find him, perhaps we can get this excavation back on track. Godspeed, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", find out what happened to <Karam>.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 3 ) {
				listmax = 0
				buff = lang("古城の宝物庫には、数々の財宝とともに《覇者の魔石》が眠っていると伝えられています。ヴェルニースを南に下った閑静な森の中に佇むこの城は、古の王族により建てられました。しかし王都がパルミアに移り、廃墟となった城は、長らくならず者の拠点として使われるように…", "The <King's Magic Stone>, along with numerous other treasures, sleeps deep in the recesses of the vaults of the Ancient Castle, which was built by a long forgotten generation of the royal family. However, the royal family eventually relocated to Palmia and the former throne of the kingdom fell into disrepair and decay. It has been ages since anyone even set foot in the old castle...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("一世紀前に《古城の主ワイナン》が占拠してからは、ダルフィの悪漢でさえ近づかない恐ろしい場所として知られるようになりました。危険度はレシマスの１７階相当です。", "After <Lord Wynan> took charge of the castle in the first century, even the thugs of Derphy became too scared to approach the castle. The danger level in the castle is roughly equivalent to the 17th floor of Lesimas.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("噂では、《ワイナン》がまだ生きて王座に君臨しているとか…彼は生粋の戦士で、その手に持つ槍斧《ランキス》は相対するものの生命を吸い取ると怖れられています。地獄への耐性か、間合いをとる手段なくして彼と対するのは賢明とはいえませんね。", "It is rumored that <Wynan> still rules the throne along with his personally trained army. Legends tell that the deadly halberd he carries, <Rankis>, can drain the life from his enemies with the greatest of ease. It would be wise to boost your resistance to nether... oh, and bear in mind that unless you can time your actions wisely he'll devour you like a dog.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("パルミアを出て北の未開の地を越えていくと、真紅に燃え盛る灼熱の塔が見えます。この塔の守護者、《赤き義眼のクルイツゥア》と呼ばれる魔女が、《賢者の魔石》を所持しているはずです。", "If you head north from Palmia, through the untamed lands, you will eventually see a tower blazing with an aura of flames. The guardian of the tower, a powerful witch named <Quruiza the Red-eyed Deceiver>, watches over the <Sage's magic stone>. ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("《クルイツゥア》の強力な魔法だけでなく、彼女の僕であり夫である《鋼鉄竜コルゴン》の力も恐るべきものです。", "Quruiza's magic is not the only thing you have to fear either, her husband, the <Steel Dragon Corgon> has pledged his strength to her defense. No doubt you'll have to face him as well.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("言うまでもないですが、この塔を探索するには、何らかの手段で火への耐性を得る必要があるでしょう。もし体力を失っていくようであれば、耐性を高めてから挑戦したほうが無難です。危険度はレシマスの１５階相当です。", "I probably don't need to tell you this, but if you plan to brave the tower, you'll need some sort of powerful resistance to heat and flames. If you find you can't hack it inside, I'd suggest you find some way to protect yourself from the heat before venturing back inside. The danger level in the tower is roughly equivalent to the 15th floor of Lesimas. ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("《愚者の魔石》が眠っているとされる死者の洞窟は、最も危険な場所です。多くの名のある冒険者が命を落とし、不死者となりこの洞窟の中をさ迷っています。", "The <Crypt of the Damned> is an ancient cave where the walking dead have amassed in droves. It is the most dangerous of the three strongholds. Countless famous adventurers have lost their lives in the crypt, doomed to wander its halls for all eternity as undead.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("魔石はこの洞窟を支配する《闇の奇形イスシズル》が身に着けているはずです。死霊術使い達に崇められ、その名は大陸中で畏怖されていますが、《イスシズル》の姿や力は、全くの謎に包まれています…", "I believe that the <Dark Abomination Issizzle> rules the hallowed crypts. He is a powerful necromancer and his name is feared all across the continent. Issizzle is shrouded in mystery so I'm afraid I can't tell you much more.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("死者の洞窟はヴェルニースとパルミアを結ぶ街道の中ほどにあり、危険度はレシマスの２５階相当です。万全に準備を整えていく必要があるでしょう。", "The crypt of the damned is along the road that runs between Vernis and Palmia. The danger level of the crypt is roughly equivalent to the 25th floor of Lesimas. You will need to be well prepared before venturing into the crypts.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 50 ) {
			listmax = 0
			buff = lang("あなたが" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "ですね。使いの者から聞いています。私はエリステア、ネフィア迷宮群の歴史の研究に携わって来ました。今は王の命によりレシマス調査隊の副長を務めていますが、洞窟に眠る多くの謎と危険な魔物の存在のために、調査は思わしく進んでいません。あなたのような冒険者に力になってもらえると心強いです。", "You must be " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". I've already heard some of the soldiers talking about you. My name is Erystia, and I came to Nefia in order to research the history behind the various labyrinths here. The King has placed me in charge of the expedition to investigate Lesimas. Unfortunately, due to numerous obstacles and an abundance of hostile monsters residing in the caverns, our investigation has made little, if any, progress. It would be very reassuring if we had the strength of adventurers like you backing us up.")
			list(0, listmax) = 1
			listn(0, listmax) = lang("力になる", "I will gladly pledge my strength to Palmia.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("断る", "I'm not interested.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("わかりました。考えが変わったら、また声をかけてください。", "Fair enough... but should you change your mind feel free to come back.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("よかった。こんなことを言っては悪いけれど、城の兵士達は機転に欠けていて、調査にはあまり役に立ってくれなかったの。", "Fantastic. I hate to admit it, but the castle's regular soldiers have been virtually useless so far...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("私達の目的は、レシマスを探索し、最下層にあるといわれる《秘宝》を持ち帰ることです。《秘宝》の正体とレシマスについての研究はある程度進んでいます。一度に言っても覚えられないでしょうから、必要があればその度に聞いてください。", "Our objective is to penetrate to the deepest reaches of Lesimas and retrieve the <codex> hidden there. Once we know the true nature of the <codex>, we can complete our research on the ancient dungeon. You probably won't remember all this after hearing it only once, so feel free to ask me about it again.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("最初の仕事として、あなたにはある人物の探索をお願いします。男の名は《カラム》。あなたと同じように、彼もレシマス探索の協力を買って出た屈指の冒険者です。調査隊の情報源として少なからぬ貢献をしていました。しかし、彼からの音沙汰がここ数週間まったくないのです。", "Now, on to the first order of business. We need you to investigate the fate of one of our agents. His name is <Karam>. Like you, he is one of the foremost adventurers in the land. He agreed to assist with the expedition into Lesimas. He had been contributing information to the expedition's limited intelligence on the caverns. However... we haven't heard anything from him in some number of weeks.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あなたの任務は、レシマスに赴き、《カラム》との接触を試みることです。彼から最後に連絡があったのは、レシマスの16階。おそらくは、より深層に探索の手を広げているでしょう。彼の足取りをつかみ、私に報告してください。", "Your duty will be to penetrate into Lesimas and make contact with <Karam>. In the last report we received from him, he told us he was on the 16th floor. If you can find him, perhaps we can get this excavation back on track. Godspeed, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", find out what happened to <Karam>.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("レシマス4階の鍵を受け取った。ジャーナルが更新された。", "You receive a key of Lesimas 4th level. Your journal has been updated.")
			gdata(GDATA_FLAG_MAIN) = 60
			goto *chat_end
		}
		_switch_sw++
	}
	if ( _switch_val == 146 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 90 ) {
			gdata(GDATA_FLAG_MAIN) = 100
			listmax = 0
			buff = lang("…貴公は？そうか、エリステアが私を探すために…見ての通り私は傷を負い、もう動くことすらできぬ。しかし、貴公のおかげで、今まで生きながらえたことに意味を見出せるようだ。", "Who's there? So... Erystia sent you to find me... as you can see I'm badly wounded. I'm pretty much screwed aren't I? But then... now that you're here maybe I won't have to die in vain after all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("貴公にお願いする。エリステアに、私の言葉を伝えてもらいたい。信じがたいことだが、レシマスの奥深くに眠る秘宝を守護しているのは、今は亡きザシム王の従弟ゼームだ。秘宝の、いやレシマスの魔に操られているのだろうか。もはや生身の人間ではあるまい。", "I have something to ask of you. Relay this message to Erystia for me... Tell her that she must believe me when I say that the one guarding the codex in the depths of Lesimas is none other than former King Zashim's brother, Zeome. He's the one controlling the magic of the codex... or... no, not just the codex, but the whole of Lesimas. He's no longer human... he's something... more.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それだけではない。ザナンのサイモアが秘宝を狙っていることは知っておろう。しかし、その理由は？", "But it doesn't end there... You need to know that Saimore from Zanan is also after the codex. But... why?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ゼームが持つ秘宝の名は《常闇の眼》。世界の真実を映すといわれている。レム・イドの終焉とシエラ・テールの始まりを説き、自身の説に莫大な支持を得ているサイモアが、今更秘宝を求めるのは何故だ？", "The codex Zeome possesses... it's called the <Origin of Vice>. They say it describes the darkness inherent in the world. But if Saimore's widely accepted story about the end of Rehm-Ido and the beginning of the era of Sierre Terre is true, then why would he be after the relic?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("サイモアの後押しにより、エレアとの戦が始まろうとしている。異形の森がレム・イドの災厄《メシェーラ》であるというのは、果たして確かなのだろうか。そして、もしサイモアの説が偽りであるとすれば、かの者の本当の目的は？…私には悪い予感がする。", "aimore has been pushing for war against the Eleas. The truth is that Saimore's claim that the irreverent forest destroyed the world of the era of Rehm-Ido is a carefully constructed lie and that the <Meshella> are real. But then, if Saimore's theory was just a deception meant to incite a war, then what is he really after? I have a bad feeling about all of this...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("私の役目はこれまでだ。この傷ではどうせ地上まで辿りつけまい。この報せを早く都に…貴公に運命の神の加護のあらんことを！", "My role in this saga is at an end... I'll never make it out with these injuries... Deliver my message to the capital quickly... and may the gods of fate watch over you...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			repeat 4
				flt calcobjlv(gdata(GDATA_LEVEL)), calcfixlv(FIX_QUALITY_GOOD)
				flttypemajor = fltsetdungeon()
				itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 0
			loop
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 1000 + rnd(200)
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 3
			flt
			itemcreate -1, ITEM_ID_BEJEWELED_CHEST, cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 0
			inv(INV_ITEM_PARAM2, ci) = 0
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			txtvalid = 0
			txt lang(cdatan(CDATAN_NAME, tc) + "は自ら命を絶った…", cdatan(CDATAN_NAME, tc) + " kills himself...")
			chara_vanquish tc
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 208 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 2
		listn(0, listmax) = lang("素材槌の素材変化", "Change material of material kit.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("アイテムの名前・外見変更", "Change item name and appearance. ")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("ジュークボックス作成", "Make a juke box.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("見て分からんか、わしゃ忙しいんじゃ。用があるなら手短にな。", "Don't you see I'm busy!")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("そんなことか…まぁよい。手間賃としてブロンズ硬貨を1枚もらう。", "What? ...Fine. I'll do it for 1 bronze coin.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				cigp = stat
				if ( inv(INV_ITEM_NUM, cigp) >= 1 ) {
					invsubroutine = 1
					invctrl = INVCTRL_MATERIAL_ENCHANT, 10
					snd SOUNDLIST_INV
					gosub *com_inventory
					if ( key == key_cancel ) {
						goto *chat_end
					}
					if ( stat == 1 ) {
						listmax = 0
						buff = lang("それで、何番の計画通りに作り変えればよいのじゃ。", "Okay. Enter the blueprint number that you want me to use.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						txt lang(" (user\\item内のプラン番号を1~33で入力。21~33は48*96サイズ用。0で元に戻す) ", " (Enter the number of the sprite inside the useritem folder. Sprites 21-33 are 48*96 in size. Enter 0 to set it to the default.) ")
						val = (windoww - 100) / 2 + inf_screenx, winposy(90), 5, 0, 0
						inputlog = ""
						gosub *prompt_word
						if ( inv(INV_ITEM_PIC, ci) < xy2pic(0, 27) | inv(INV_ITEM_PIC, ci) > xy2pic(32, 27) ) {
							inv(INV_ITEM_PROPERTY, ci) = inv(INV_ITEM_PIC, ci) + 10
						}
						if ( inputlog == "0" ) {
							if ( inv(INV_ITEM_PARAM4, ci) != 0 ) {
								inv(INV_ITEM_PARAM4, ci) = 0
							}
							if ( inv(INV_ITEM_PROPERTY, ci) > PROP_SPECIAL ) {
								inv(INV_ITEM_PIC, ci) = inv(INV_ITEM_PROPERTY, ci) - 10
								inv(INV_ITEM_PROPERTY, ci) = PROP_NONE
								txtef COLOR_GREEN
								txt lang("元に戻した。", "You returned it to its original appearance.")
							}
						}
						else {
							if ( inputlog == "1" ) {
								inv(INV_ITEM_PARAM4, ci) = 1
								inv(INV_ITEM_PIC, ci) = xy2pic(0, 27)
							}
							if ( inputlog == "2" ) {
								inv(INV_ITEM_PARAM4, ci) = 2
								inv(INV_ITEM_PIC, ci) = xy2pic(1, 27)
							}
							if ( inputlog == "3" ) {
								inv(INV_ITEM_PARAM4, ci) = 3
								inv(INV_ITEM_PIC, ci) = xy2pic(2, 27)
							}
							if ( inputlog == "4" ) {
								inv(INV_ITEM_PARAM4, ci) = 4
								inv(INV_ITEM_PIC, ci) = xy2pic(3, 27)
							}
							if ( inputlog == "5" ) {
								inv(INV_ITEM_PARAM4, ci) = 5
								inv(INV_ITEM_PIC, ci) = xy2pic(4, 27)
							}
							if ( inputlog == "6" ) {
								inv(INV_ITEM_PARAM4, ci) = 6
								inv(INV_ITEM_PIC, ci) = xy2pic(5, 27)
							}
							if ( inputlog == "7" ) {
								inv(INV_ITEM_PARAM4, ci) = 7
								inv(INV_ITEM_PIC, ci) = xy2pic(6, 27)
							}
							if ( inputlog == "8" ) {
								inv(INV_ITEM_PARAM4, ci) = 8
								inv(INV_ITEM_PIC, ci) = xy2pic(7, 27)
							}
							if ( inputlog == "9" ) {
								inv(INV_ITEM_PARAM4, ci) = 9
								inv(INV_ITEM_PIC, ci) = xy2pic(8, 27)
							}
							if ( inputlog == "10" ) {
								inv(INV_ITEM_PARAM4, ci) = 10
								inv(INV_ITEM_PIC, ci) = xy2pic(9, 27)
							}
							if ( inputlog == "11" ) {
								inv(INV_ITEM_PARAM4, ci) = 11
								inv(INV_ITEM_PIC, ci) = xy2pic(10, 27)
							}
							if ( inputlog == "12" ) {
								inv(INV_ITEM_PARAM4, ci) = 12
								inv(INV_ITEM_PIC, ci) = xy2pic(11, 27)
							}
							if ( inputlog == "13" ) {
								inv(INV_ITEM_PARAM4, ci) = 13
								inv(INV_ITEM_PIC, ci) = xy2pic(12, 27)
							}
							if ( inputlog == "14" ) {
								inv(INV_ITEM_PARAM4, ci) = 14
								inv(INV_ITEM_PIC, ci) = xy2pic(13, 27)
							}
							if ( inputlog == "15" ) {
								inv(INV_ITEM_PARAM4, ci) = 15
								inv(INV_ITEM_PIC, ci) = xy2pic(14, 27)
							}
							if ( inputlog == "16" ) {
								inv(INV_ITEM_PARAM4, ci) = 16
								inv(INV_ITEM_PIC, ci) = xy2pic(15, 27)
							}
							if ( inputlog == "17" ) {
								inv(INV_ITEM_PARAM4, ci) = 17
								inv(INV_ITEM_PIC, ci) = xy2pic(16, 27)
							}
							if ( inputlog == "18" ) {
								inv(INV_ITEM_PARAM4, ci) = 18
								inv(INV_ITEM_PIC, ci) = xy2pic(17, 27)
							}
							if ( inputlog == "19" ) {
								inv(INV_ITEM_PARAM4, ci) = 19
								inv(INV_ITEM_PIC, ci) = xy2pic(18, 27)
							}
							if ( inputlog == "20" ) {
								inv(INV_ITEM_PARAM4, ci) = 20
								inv(INV_ITEM_PIC, ci) = xy2pic(19, 27)
							}
							if ( inputlog == "21" ) {
								inv(INV_ITEM_PARAM4, ci) = 21
								inv(INV_ITEM_PIC, ci) = xy2pic(20, 27)
							}
							if ( inputlog == "22" ) {
								inv(INV_ITEM_PARAM4, ci) = 22
								inv(INV_ITEM_PIC, ci) = xy2pic(21, 27)
							}
							if ( inputlog == "23" ) {
								inv(INV_ITEM_PARAM4, ci) = 23
								inv(INV_ITEM_PIC, ci) = xy2pic(22, 27)
							}
							if ( inputlog == "24" ) {
								inv(INV_ITEM_PARAM4, ci) = 24
								inv(INV_ITEM_PIC, ci) = xy2pic(23, 27)
							}
							if ( inputlog == "25" ) {
								inv(INV_ITEM_PARAM4, ci) = 25
								inv(INV_ITEM_PIC, ci) = xy2pic(24, 27)
							}
							if ( inputlog == "26" ) {
								inv(INV_ITEM_PARAM4, ci) = 26
								inv(INV_ITEM_PIC, ci) = xy2pic(25, 27)
							}
							if ( inputlog == "27" ) {
								inv(INV_ITEM_PARAM4, ci) = 27
								inv(INV_ITEM_PIC, ci) = xy2pic(26, 27)
							}
							if ( inputlog == "28" ) {
								inv(INV_ITEM_PARAM4, ci) = 28
								inv(INV_ITEM_PIC, ci) = xy2pic(27, 27)
							}
							if ( inputlog == "29" ) {
								inv(INV_ITEM_PARAM4, ci) = 29
								inv(INV_ITEM_PIC, ci) = xy2pic(28, 27)
							}
							if ( inputlog == "30" ) {
								inv(INV_ITEM_PARAM4, ci) = 30
								inv(INV_ITEM_PIC, ci) = xy2pic(29, 27)
							}
							if ( inputlog == "31" ) {
								inv(INV_ITEM_PARAM4, ci) = 31
								inv(INV_ITEM_PIC, ci) = xy2pic(30, 27)
							}
							if ( inputlog == "32" ) {
								inv(INV_ITEM_PARAM4, ci) = 32
								inv(INV_ITEM_PIC, ci) = xy2pic(31, 27)
							}
							if ( inputlog == "33" ) {
								inv(INV_ITEM_PARAM4, ci) = 33
								inv(INV_ITEM_PIC, ci) = xy2pic(32, 27)
							}
							if ( inv(INV_ITEM_PIC, ci) >= xy2pic(0, 27) & inv(INV_ITEM_PIC, ci) <= xy2pic(32, 27) ) {
								gsel BUFFER_ITEM
								folder = exedir + "user\\item\\"
								dirlist buff, folder + "plan*.bmp"
								notesel buff
								repeat stat
									noteget file, cnt
									p = int(strmid(file, 4, instr(file, 4, "."))) + 890
									pos p \ 33 * inf_tiles, p / 33 * inf_tiles
									picload folder + file, 1
								loop
								gsel BUFFER_SCREEN
							}
						}
						listmax = 0
						buff = lang("ほれ。", "Done.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						snd SOUNDLIST_BUILD1
						inv(INV_ITEM_NUM, cigp)--
					}
					else {
						obvious = 0
					}
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…持っておらんのか！", "That is not enough.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
					goto *chat_end
				}
			}
			gosub *screen_draw
		}
		if ( chatval == 2 ) {
			medal = 0
			sozaituti = 0
			hennka = 0
			item_find ITEM_ID_SCROLL_SUPERIOR_MATERIAL, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
					hennka = 1
				}
			}
			item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
					medal = 1
				}
			}
			item_find ITEM_ID_MATERIAL_KIT, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
					sozaituti = 1
				}
			}
			if ( hennka != 1 | medal != 1 | sozaituti != 1 ) {
				listmax = 0
				buff = lang("じゃったら素材槌と*素材変化*の巻物を用意せい。それとブロンズ硬貨もじゃ。", "You'll need a material kit and a scroll of superior material before I can do that. And a bronze coin too.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				list(0, listmax) = 1
				listn(0, listmax) = lang("いい", "Yes.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("だめ", "No.")
				listmax++
				buff = lang("ほう。では*素材変化*の巻物を使って" + itemname(ci) + "を素材変化するがそれでよいな？手間賃としてブロンズ硬貨を1枚もらう。", "Oho. So you want me to use a scroll of superior material to change the material of your " + itemname(ci) + "? It'll cost you 1 bronze coin.")
				gosub *chat_select
				if ( chatval == 1 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("やっぱりやめる", "Cancel.")
					listmax++
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("革", "leather")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 2
						listn(0, listmax) = lang("宵晒", "dawn cloth")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 3
						listn(0, listmax) = lang("鱗", "scale")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 4
						listn(0, listmax) = lang("硝子", "glass")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 5
						listn(0, listmax) = lang("ミスリル", "mithril")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 6
						listn(0, listmax) = lang("シルバー", "silver")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 1 ) {
						list(0, listmax) = 7
						listn(0, listmax) = lang("竜鱗", "dragon scale")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 8
						listn(0, listmax) = lang("シルク", "silk")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 9
						listn(0, listmax) = lang("ザイロン", "zylon")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 10
						listn(0, listmax) = lang("生もの", "raw")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 11
						listn(0, listmax) = lang("水晶", "crystal")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 12
						listn(0, listmax) = lang("鉄", "metal")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 13
						listn(0, listmax) = lang("白銀", "platinum")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 2 ) {
						list(0, listmax) = 14
						listn(0, listmax) = lang("ダイヤ", "diamond")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 15
						listn(0, listmax) = lang("布", "cloth")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 36
						listn(0, listmax) = lang("木", "wood")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 16
						listn(0, listmax) = lang("ボーン", "bone")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 17
						listn(0, listmax) = lang("エメラルド", "emerald")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 18
						listn(0, listmax) = lang("スティール", "steel")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 19
						listn(0, listmax) = lang("ゴールデン", "gold")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 20
						listn(0, listmax) = lang("鉛", "iron")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 3 ) {
						list(0, listmax) = 21
						listn(0, listmax) = lang("エーテル", "ether")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 22
						listn(0, listmax) = lang("霊布", "spirit cloth")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 23
						listn(0, listmax) = lang("珊瑚", "coral")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 24
						listn(0, listmax) = lang("ミカ", "mica")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 25
						listn(0, listmax) = lang("オブシディアン", "obsidian")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 26
						listn(0, listmax) = lang("チタン", "titanium")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 27
						listn(0, listmax) = lang("ルビナス", "rubynus")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 4 ) {
						list(0, listmax) = 35
						listn(0, listmax) = lang("ナノマシン", "nanomachine")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 28
						listn(0, listmax) = lang("紙", "paper")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 37
						listn(0, listmax) = lang("竹", "bamboo")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 29
						listn(0, listmax) = lang("翼鳥鱗", "griffon scale")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 30
						listn(0, listmax) = lang("真珠", "perl")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 31
						listn(0, listmax) = lang("ブロンズ", "bronze")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 32
						listn(0, listmax) = lang("鉄鎖", "chain")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 33
						listn(0, listmax) = lang("クロム", "chrome")
						listmax++
					}
					if ( (gdata(GDATA_DAY) + 5) \ 5 == 0 ) {
						list(0, listmax) = 34
						listn(0, listmax) = lang("アダマンタイト", "adamantium")
						listmax++
					}
					buff = lang("今日の気分だとこんな感じゃ。どの素材にすればよいのか？", "Oho. So you want me to use a scroll of superior material to change the material of your " + itemname(ci) + "? It'll cost you 1 small medal.")
					gosub *chat_select
					if ( chatval > 0 ) {
						rndsozai = 1
						if ( chatval == 1 ) {
							rndsozai = 1
						}
						if ( chatval == 2 ) {
							rndsozai = 17
						}
						if ( chatval == 3 ) {
							rndsozai = 4
						}
						if ( chatval == 4 ) {
							rndsozai = 5
						}
						if ( chatval == 5 ) {
							rndsozai = 7
						}
						if ( chatval == 6 ) {
							rndsozai = 25
						}
						if ( chatval == 7 ) {
							rndsozai = 24
						}
						if ( chatval == 8 ) {
							rndsozai = 2
						}
						if ( chatval == 9 ) {
							rndsozai = 27
						}
						if ( chatval == 10 ) {
							rndsozai = 35
						}
						if ( chatval == 11 ) {
							rndsozai = 11
						}
						if ( chatval == 12 ) {
							rndsozai = 10
						}
						if ( chatval == 13 ) {
							rndsozai = 26
						}
						if ( chatval == 14 ) {
							rndsozai = 13
						}
						if ( chatval == 15 ) {
							rndsozai = 3
						}
						if ( chatval == 36 ) {
							rndsozai = 43
						}
						if ( chatval == 16 ) {
							rndsozai = 18
						}
						if ( chatval == 17 ) {
							rndsozai = 23
						}
						if ( chatval == 18 ) {
							rndsozai = 9
						}
						if ( chatval == 19 ) {
							rndsozai = 32
						}
						if ( chatval == 20 ) {
							rndsozai = 34
						}
						if ( chatval == 21 ) {
							rndsozai = 8
						}
						if ( chatval == 22 ) {
							rndsozai = 14
						}
						if ( chatval == 23 ) {
							rndsozai = 33
						}
						if ( chatval == 24 ) {
							rndsozai = 21
						}
						if ( chatval == 25 ) {
							rndsozai = 20
						}
						if ( chatval == 26 ) {
							rndsozai = 29
						}
						if ( chatval == 27 ) {
							rndsozai = 15
						}
						if ( chatval == 35 ) {
							rndsozai = 45
						}
						if ( chatval == 28 ) {
							rndsozai = 16
						}
						if ( chatval == 37 ) {
							rndsozai = 44
						}
						if ( chatval == 29 ) {
							rndsozai = 28
						}
						if ( chatval == 30 ) {
							rndsozai = 22
						}
						if ( chatval == 31 ) {
							rndsozai = 12
						}
						if ( chatval == 32 ) {
							rndsozai = 19
						}
						if ( chatval == 33 ) {
							rndsozai = 30
						}
						if ( chatval == 34 ) {
							rndsozai = 31
						}
						item_find ITEM_ID_MATERIAL_KIT, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
						if ( stat != (-1) ) {
							ci = stat
							if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
								inv(INV_ITEM_NUM, ci)--
							}
						}
						item_find ITEM_ID_SCROLL_SUPERIOR_MATERIAL, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
						if ( stat != (-1) ) {
							ci = stat
							if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
								inv(INV_ITEM_NUM, ci)--
							}
						}
						item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
						if ( stat != (-1) ) {
							ci = stat
							if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
								inv(INV_ITEM_NUM, ci)--
							}
						}
						redraw 0
						gosub *screen_draw
						gosub *screen_fade
						gsel BUFFER_BUF
						pos 0, 0
						picload exedir + "graphic\\face2" + devfile + ".bmp", 1
						gsel BUFFER_SCREEN
						snd SOUNDLIST_BUILD1
						flt
						nostack = 1
						itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						inv(INV_ITEM_MATERIAL, ci) = rndsozai
						listmax = 0
						buff = lang("ほれ、できたぞ。さっさと持っていかんか。", "There, all done.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
				}
				goto *chat_end
			}
		}
		if ( chatval == 3 ) {
			hennka = 0
			inv_getheader 0
			repeat invrange, invhead
				if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
					continue
				}
				if ( ibit(ITEM_BIT_NO_DROP, cnt) ) {
					continue
				}
				if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_DISC ) {
					if ( inv(INV_ITEM_NUM, cnt) >= 1 ) {
						hennka += inv(INV_ITEM_NUM, cnt)
					}
				}
			loop
			if ( hennka < 76 ) {
				listmax = 0
				buff = lang("そうか。番号が被っていてもよいから音楽用のディスクを76枚用意せい。", "That's what you want? Then get me 76 music discs. Doesn't matter if there are duplicates.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				list(0, listmax) = 1
				listn(0, listmax) = lang("いい", "Yes.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("だめ", "No.")
				listmax++
				buff = lang("ほう。よく収集したものじゃ。手間賃としてブロンズ硬貨を5枚もらうがよいな？", "Well, I'll be. You really do have those discs. It'll cost you 5 bronze coins. How about it?")
				gosub *chat_select
				if ( chatval == 1 ) {
					item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
					if ( stat != (-1) ) {
						cigp = stat
						if ( inv(INV_ITEM_NUM, cigp) >= 5 ) {
							inv(INV_ITEM_NUM, cigp) -= 5
							inv_getheader 0
							repeat invrange, invhead
								if ( hennka <= 0 ) {
									break
								}
								if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
									continue
								}
								if ( ibit(ITEM_BIT_NO_DROP, cnt) ) {
									continue
								}
								if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_DISC ) {
									if ( inv(INV_ITEM_NUM, cnt) >= 1 ) {
										hennka -= inv(INV_ITEM_NUM, cnt)
										inv(INV_ITEM_NUM, cnt) = 0
									}
								}
							loop
							hennka = 0
							snd SOUNDLIST_BUILD1
							flt
							nostack = 1
							itemcreate -1, ITEM_ID_JUKE_BOX, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							listmax = 0
							buff = lang("これでどうじゃ。", "Done. Take a look.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strbye
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("…持っておらんのか！", "That is not enough.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 209 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 2
		listn(0, listmax) = lang("小さなメダル", "I have some small medals.")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("荷車の改造", "Can you upgrade my cart?")
		listmax++
		list(0, listmax) = 4
		listn(0, listmax) = lang("ポーショマンの改造", "Can you upgrade my potioman?")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("ほほう、客人とはこれまた珍しい。暖かい飯でも馳走してさしあげたいが、あいにくと今日の飯番はガロクでの。あやつの作る料理は自らの作品に似て、なんとも無骨で味気ない。とても客人に楽しんでもらえる代物じゃないのう。まぁ、せっかくこんな辺境の地にまで、やって来なさったんだ。何か用件があれば、話だけは聞こうじゃないか。", "Oh oh, what we got, talk about rare! A traveler? I would have given you a feast if it were not Garok that handles today's dinner. You know, his cooking is as loutish as his creations. Oh well, you've come all the way here, what can I do for you?")
		gosub *chat_select
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("おお、小さなメダルを持っておるのか？メダルの収集は、わしの生きがいなんじゃよ！これまでに生産してきた自慢の品の数々と交換せぬか？", "Ho ho ho! You got small medals! Let me see, let me see! Would you like to trade them for one of my proud works?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			invctrl = INVCTRL_METAL, 0
			invfile = cdata(CDATA_ROLE_FILE_ID, tc)
			gosub *shop
			screenupdate = -1
			gosub *screen_draw
			cs = 0
			gosub *calcBurdenPc
			goto *chat_unique
		}
		if ( chatval == 3 ) {
			ccpc = limit(calccargoupdatecost(), 1, 10)
			buff = lang("荷車の改造とな。たいして気乗りのする仕事ではないが、そうじゃの、ブロンズ硬貨" + ccpc + "枚をくれるならやらんでもない。", "Upgrade your cart? It's not something I get excited about but if you can give me " + ccpc + " bronze coins, I'll think about it.")
			item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= ccpc ) {
					list(0, listmax) = 1
					listn(0, listmax) = lang("ブロンズ硬貨を渡す", "Here, take these bronze coins.")
					listmax++
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("断る", "I think not.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("ふぅん。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			txtnew
			txt lang("荷車の重量の限界が" + cnvweight(calccargoupdate()) + "上昇した。", "Your cart's weight limit has increased by " + cnvweight(calccargoupdate()) + ".")
			snd SOUNDLIST_BUILD1
			item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				inv(INV_ITEM_NUM, ci) -= ccpc
			}
			gdata(GDATA_CARGO_LIMIT) += calccargoupdate()
			gosub *calcBurdenPc
			listmax = 0
			buff = lang("ほい。", "Here you are.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 4 ) {
			txt lang("改造するポーショマンは？", "Which potioman?")
			invsubroutine = 1
			invctrl = INVCTRL_MATERIAL_ENCHANT, 22
			snd SOUNDLIST_INV
			gosub *com_inventory
			if ( stat == 1 ) {
				cibk = ci
				listmax = 0
				buff = lang("なんと！ポーショマンの改造とな！？", "Ho!? Upgrade this potioman?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
				if ( stat != (-1) ) {
					ci = stat
					if ( inv(INV_ITEM_NUM, ci) >= 2 ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("ブロンズ硬貨を渡す", "Here, take these bronze coins.")
						listmax++
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("やめておく", "I think not.")
				listmax++
				buff = lang("それならブロンズ硬貨2枚でよいぞ！いやぁ、腕が鳴るわい。", "2 bronze coins are enough for remodeling. I'm excited!")
				gosub *chat_select
				if ( chatval != 1 ) {
					listmax = 0
					buff = lang("ふぅん。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				ci = cibk
				list(0, listmax) = 0
				listn(0, listmax) = lang("やっぱりこのままでいい", "After all it is okay as it is.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("凄まじい回転をかけられるようにしたい", "I want this to be able to spin tremendously.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("基本威力を高めたい", "I want to increase the basic power of this.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("二発同時発射できるようにしたい", "I want to be able to fire two shots at the same time.")
				listmax++
				list(0, listmax) = 4
				listn(0, listmax) = lang("連射しやすいようにしたい", "I want to make this easier to shoot continuously.")
				listmax++
				list(0, listmax) = 5
				listn(0, listmax) = lang("精密な射撃ができるようにしたい", "I want this to be capable of precise shooting.")
				listmax++
				list(0, listmax) = 6
				listn(0, listmax) = lang("弾道を変えられるようにしたい", "I want this to be able to change trajectory.")
				listmax++
				buff = lang("それで、こいつをどんなポーショマンにしたいんじゃ？", "Hmmm, what kind of potioman do you want this to be?")
				chatesc = 1
				gosub *chat_select
				pot = chatval
				if ( chatval >= 1 & chatval <= 6 ) {
					if ( inv(INV_ITEM_COL, ci) == 0 | inv(INV_ITEM_COL, ci) == 1 ) {
						inv(INV_ITEM_COL, ci) = 12
					}
					if ( inv(INV_ITEM_COL, ci) == 29 ) {
						inv(INV_ITEM_COL, ci) = 28
					}
					if ( inv(INV_ITEM_COL, ci) == 12 | inv(INV_ITEM_COL, ci) == 11 | inv(INV_ITEM_COL, ci) == 28 | inv(INV_ITEM_COL, ci) == 10 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(29, 36)
					}
					if ( inv(INV_ITEM_COL, ci) == 9 | inv(INV_ITEM_COL, ci) == 4 | inv(INV_ITEM_COL, ci) == 18 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(29, 37)
					}
					if ( inv(INV_ITEM_COL, ci) == 15 | inv(INV_ITEM_COL, ci) == 6 | inv(INV_ITEM_COL, ci) == 5 | inv(INV_ITEM_COL, ci) == 27 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(30, 36)
					}
					if ( inv(INV_ITEM_COL, ci) == 26 | inv(INV_ITEM_COL, ci) == 23 | inv(INV_ITEM_COL, ci) == 30 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(30, 37)
					}
					if ( inv(INV_ITEM_COL, ci) == 24 | inv(INV_ITEM_COL, ci) == 21 | inv(INV_ITEM_COL, ci) == 19 | inv(INV_ITEM_COL, ci) == 22 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(31, 36)
					}
					if ( inv(INV_ITEM_COL, ci) == 2 | inv(INV_ITEM_COL, ci) == 8 | inv(INV_ITEM_COL, ci) == 7 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(31, 37)
					}
					if ( inv(INV_ITEM_COL, ci) == 13 | inv(INV_ITEM_COL, ci) == 14 | inv(INV_ITEM_COL, ci) == 17 | inv(INV_ITEM_COL, ci) == 25 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(32, 36)
					}
					if ( inv(INV_ITEM_COL, ci) == 3 | inv(INV_ITEM_COL, ci) == 16 | inv(INV_ITEM_COL, ci) == 20 ) {
						inv(INV_ITEM_PIC, ci) = xy2pic(32, 37)
					}
					inv(INV_ITEM_PARAM3, ci) = pot + inv(INV_ITEM_COL, ci) * 100
					if ( inv(INV_ITEM_PARAM3, ci) \ 100 == 1 ) {
						encadd ci, ENCHANT_POTIOMAN_ROTATION, 500, 0, 1, 0, 1
					}
					if ( inv(INV_ITEM_PARAM3, ci) \ 100 == 2 ) {
						encadd ci, ENCHANT_POTIOMAN_POWER, 500, 0, 1, 0, 1
					}
					if ( inv(INV_ITEM_PARAM3, ci) \ 100 == 3 ) {
						encadd ci, ENCHANT_POTIOMAN_TWO_SHOTS, 500, 0, 1, 0, 1
					}
					if ( inv(INV_ITEM_PARAM3, ci) \ 100 == 4 ) {
						encadd ci, ENCHANT_POTIOMAN_CONTINUOUS_SHOT, 500, 0, 1, 0, 1
					}
					if ( inv(INV_ITEM_PARAM3, ci) \ 100 == 5 ) {
						encadd ci, ENCHANT_POTIOMAN_PRECISE_SHOT, 500, 0, 1, 0, 1
					}
					if ( inv(INV_ITEM_PARAM3, ci) \ 100 == 6 ) {
						encadd ci, ENCHANT_POTIOMAN_CHANGING_TRAJECTORY, 500, 0, 1, 0, 1
					}
					inv(INV_ITEM_PARAM2, ci) = 4
					snd SOUNDLIST_BUILD1
					item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
					if ( stat != (-1) ) {
						ci = stat
						inv(INV_ITEM_NUM, ci) -= 2
					}
					gosub *calcBurdenPc
					redraw 0
					gosub *screen_draw
					gosub *screen_fade
					gsel BUFFER_BUF
					pos 0, 0
					picload exedir + "graphic\\face2" + devfile + ".bmp", 1
					gsel BUFFER_SCREEN
					snd SOUNDLIST_COMPLETE1
					listmax = 0
					buff = lang("よしよし、それがおぬしの新たなポーショマンじゃ。サービスで弾も込めておいたからの。", "Here you are...This is your new potioman!!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 221 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1000 | gdata(GDATA_FLAG_SUB_MOTHER) == 1005 ) {
			if ( gdata(GDATA_FLAG_MAIN) > 340 ) {
				listmax = 0
				buff = lang("奇跡が起きたってお医者さんが言ってたの。もう、おかあさんの病気は大丈夫だって…！きっと" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さんが助けてくれたおかげだよね、ありがとう" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん！大好き！", "Miracle happened, the doctor had said.Other diseases of mommy is okay ...!I'll bet you have helped my mommy, thanks. Thank you " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "!You're my hero!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_AREA) == AREA_NOYEL & adata(ADATA_FESTIVAL, gdata(GDATA_AREA)) ) {
				listmax = 0
				buff = lang("おかあさん、なんだか今日はいつもより元気そう！お祭りは人がたくさん来るから嫌いだけど、おかあさんにもジュア様の加護があるといいな。", "Mommy looks to be in a good condition today. I hate the festival because there are so many people out there but I hope St.Jure will watch over her!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("どうして、おかあさんはあんなに悲しそうなの？顔はかわっても、あたしのおかあさんだよ。お友達もきっとわかってくれる…わかってくれないんなら…お友達なんていらない！…あたしはお母さんとずっといっしょにいるんだもん。", "Why does mommy look so sad? Her face has changed but she's still my mommy. I know my friends will understand too... and if they don't then they're no friends of mine! I'll always be by her side. Always.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1001 | gdata(GDATA_FLAG_SUB_MOTHER) == 1006 & gdata(GDATA_FLAG_MAIN) > 340 ) {
			listmax = 0
			buff = lang("ひどいこと言ってごめんなさい、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん…おかあさんも苦しかったんだよね。でも…でもこんなのってないよ…おかあさん…。", "I'm sorry to say terrible things, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + " ... I would be mommy had suffered. But ... I do not like it ... What is the mommy ....")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1001 | gdata(GDATA_FLAG_SUB_MOTHER) == 1006 ) {
			listmax = 0
			buff = lang("消えて！二度と顔をみせないで！！ばかぁ！", "Go away! Never come back again! Bastard!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1002 & gdata(GDATA_FLAG_MAIN) > 340 ) {
			listmax = 0
			buff = lang("お金を貯めていつか、おかあさんを買い戻すの！それにしても誰がおかあさんを売ったんだろう…。", "Mommy... where have you gone mommy?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1002 ) {
			listmax = 0
			buff = lang("おかあさん…どこにいっちゃったのぉ…", "Mommy... where have you gone mommy?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 0 ) {
			buff = lang("おかあさんがひどい病気で…お医者さんはエーテル病だから手に負えないって…", "Mommy is really sick... but the doctor said she has the ether plague and he can't do anything...")
			gosub *sqMother_1
			if ( stat == 0 ) {
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MOTHER) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1 | gdata(GDATA_FLAG_SUB_MOTHER) == 3 ) {
			listmax = 0
			buff = lang("おかあさん、はやく良くなるといいな♪", "I hope mommy gets better soon!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 5 | gdata(GDATA_FLAG_SUB_MOTHER) == 7 ) {
			listmax = 0
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、大好き〜。おかあさん、どんどん元気になってるよ♪", "Thanks " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", you're the best! Mommy's getting better and better.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 2 | gdata(GDATA_FLAG_SUB_MOTHER) == 4 ) {
			buff = lang("あ…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん。おかげで、おかあさんの身体も良くなっています。でもまだ、もとどおりに元気になるには時間がかかるみたいなの…", "Ah, hi " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". Thanks to you Mommy's been getting better! But... it still looks like she needs more time before she's really healthy.")
			gosub *sqMother_1
			if ( stat == 0 ) {
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MOTHER)++
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 6 ) {
			buff = lang("おかあさん、最近すごく調子いいの！お医者さんも、もう大丈夫だろうって。最近は、近所の子供達もお見舞いに来てくれるんだよ。はやく、おかあさんやみんなと遊びたいな〜。", "Mommy looks a lot better these days! The doctor said she's gonna be okay. Even the other kids have come to visit her in the past few weeks. I want mommy to be able to play with us soon...")
			gosub *sqMother_1
			if ( stat == 0 ) {
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MOTHER)++
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 8 ) {
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん…おかあさんが…！おかあさんが…急に具合が悪くなって…身体がおかしくなって…あぁ…神様…", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "! It's mommy... something's wrong! Suddenly she started acting strange and... now her body is changing and... oh gods...")
			gosub *sqMother_1
			if ( stat == 0 ) {
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MOTHER)++
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 9 ) {
			listmax = 0
			buff = lang("おかあさん…大丈夫？…大丈夫？…あぁぁ…", "Mommy! Mommy are you okay? Mommy?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 10 ) {
			listmax = 0
			buff = lang("おかあさんが…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さんにお話があるって…", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", mommy has something to say to you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 222 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1006 & gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("本当にこれでよかったのか、ずっと悩んでいるんです。せめて、せめてあの子が友達と仲良く暮らせていますように…。", "Th-thank you... thank...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1002 & gdata(GDATA_FLAG_MAIN) > 340 ) {
			listmax = 0
			buff = lang("(見世物になることにすっかり慣れてしまったようだ。ぼんやりとどこか遠くを眺めている…。)", "(Lily had to be thoroughly familiar spectacle. She is looking at somewhere far away ...)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1002 ) {
			listmax = 0
			buff = lang("ひどいわ。", "You are cruel.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1005 | (gdata(GDATA_FLAG_SUB_MOTHER) == 1000 & gdata(GDATA_FLAG_MAIN) > 340) ) {
			listmax = 0
			buff = lang("最近、なんだか身体の調子が良いの。なぜかしら？薬を飲まなくても痛みがほとんどしなくって。…一時期は本当に苦しくて、死んでしまいたいと本気で考えていました。でもそうしたらあの子はまた心を閉ざしてしまう…。この姿はもう戻らないだろうけれど、強く生きていこうと思います。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、今まで本当にありがとう…。", "Recently, the good condition of the body somehow. I wonder why? Rarely is pain medication dosing... One time is really painful, and I really think I want to be dead. But Pael Then they will shut your heart again ... This figure will go back again though, I'll make a strong living. Thank you very much ...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_MOTHER) = 1005
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 10 ) {
			listmax = 0
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん…こんな姿でごめんなさい…どうか、怖がらないで…逃げないで…。私の病気は、もう手遅れだったようです。この醜い身体が元に戻ることは…もうないでしょう。", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", I'm sorry you have to see me this way... please don't turn away... don't run. It seems my disease has already progressed too far... my twisted and hideous body... I can never return to normal... the disease is too advanced now.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("病気は治りませんでしたが、あなたがして下さった全ての事に感謝します。ええ、無駄ではなかったの。だって、おかげでパエルに、あんないいお友達がたくさんできたんだもの。", "Even though you couldn't cure me, you have my thanks for all that you've done. It was... your efforts were not in vain. You helped Pael make a lot of friends... surely that counts for something.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_HAPPY_APPLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 20000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
			snd SOUNDLIST_COMPLETE1
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("これは少ないですが、私の感謝の気持ちです。受け取ってくださいな。", "This isn't much, I know, but it's the least I can do. Please accept these gifts.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_MOTHER) = 1000
			cbitmod CHARA_BIT_SHUT_UP, tc, TRUE
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1000 ) {
			if ( gdata(GDATA_AREA) == AREA_NOYEL & adata(ADATA_FESTIVAL, gdata(GDATA_AREA)) ) {
				buff = lang("ああ、もうこんな時期になったのね。パエルったら、私のことなんか気にせずお祭りを楽しんでくればいいのに。元気な頃は、よく二人でジュア様の像にお参りしていたのよ。", "Oh, it's already this time of the year. Poor Pael, she should stop worring about me and go to the festival. We used to visit the St.Jure's statue and pray together around this time.")
				inv_find ITEM_ID_POTION_CURE_CORRUPTION, CHARA_PLAYER
				if ( stat != (-1) ) {
					list(0, listmax) = 2
					listn(0, listmax) = lang("エーテル抗体を1本渡す", "(Give her a potion of cure corruption.)")
					listmax++
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("お元気で", "Take care.")
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( chatval == 2 ) {
					inv_find ITEM_ID_POTION_CURE_CORRUPTION, CHARA_PLAYER
					inv(INV_ITEM_NUM, stat)--
					txt lang("エーテル抗体を1本渡した。", "You hand her a potion.")
					snd SOUNDLIST_EQUIP1
					modkarma 0, 20
					listmax = 0
					buff = lang("ありがとうございます。あなたは本当に優しい心の持ち主ね。ジュア様のご加護があるのかしら、なんだか聖夜祭の間はお薬がよく効く気がするわ。", "Oh, thank you. You have a very kind heart, you know. The potion, I feel like the medicine is working better during the festival. Maybe it's the blessing of St.Jure?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				listmax = 0
				buff = lang("いつもお見舞いに来てくれてありがとう…どうぞ、聖夜祭を楽しんで行ってください。", "Thanks for coming to see me. Please enjoy yourself at the festival during your stay.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1000 ) {
			listmax = 0
			buff = lang("…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、あなたに最後のお願いがあります。この短剣で、私の胸を貫いてください。うふふ、だって、この姿がばれてしまったら、せっかくできたパエルの友達が逃げてしまうじゃない。あの子が幸せになれるなら、私は…", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", I have... I have one final request. Take this knife and... *weep*. If Pael's friends saw me like this, they'd run in fear and treat her with fear and suspicion. As long as she can be happy, I...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			buff = lang("こんなこと、あなたにだから頼めるのよ。どうか、この苦しみを終わらせてください…", "Please, I'm begging you. Please put me out of my misery...")
			inv_find ITEM_ID_POTION_CURE_CORRUPTION, CHARA_PLAYER
			if ( stat != (-1) ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("エーテル抗体を1本渡す", "(Give her a potion of cure corruption.)")
				listmax++
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("息の根を止める", "(End her life.)")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("希望はまだある", "There's still hope.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("あぁ…ありがとう。ありがとう…", "Th-thank you... thank...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				evadd EVENT_KILL_MOTHER, tc
				goto *chat_end
			}
			if ( chatval == 2 ) {
				inv_find ITEM_ID_POTION_CURE_CORRUPTION, CHARA_PLAYER
				inv(INV_ITEM_NUM, stat)--
				txt lang("エーテル抗体を1本渡した。", "You hand her a potion.")
				snd SOUNDLIST_EQUIP1
				modkarma 0, 20
				listmax = 0
				buff = lang("そんなものいくら飲んでも、元には戻らないのに…そんなもの…ありがとう…", "No matter how many I drink, my body won't return to normal... thank you though...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("あ、あなたは、私にこんな姿で生き続けろというの？世の中には、希望なんて便利な言葉で片付けられない苦しみがたくさんあることを、あなたはわかっているのかしら。…ごめんなさい、きついことをいって。あなたの考えが変わるのをまっているわ。", "You... you would force me to keep living like this? You'll find there's much suffering and pain in this world that you can't heal with such optimistic words. I'm sorry... I'm sure you know that well, but... I'll be waiting for you to change your mind.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) > 7 ) {
			listmax = 0
			buff = lang("う…けほっ、けほっ。ごめん…なさい…気分が悪いの…はぁ…はぁ…", "*cough* *cough* I'm... sorry... I don't... feel well... *huff* *huff*")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) > 5 ) {
			listmax = 0
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、いらっしゃい。え？嬉しそうな顔をしてる？…うふふ、この前ね、パエルが家の外に大きな雪だるまを作ってくれたのよ。「お友達と一緒に作ったの」って、あんな嬉しそうな顔は、あの子の父親が死んでしまって以来見ていないわ。あのパエルの作った雪だるまを窓から見てるとね、とても幸せな気分になるのよ。", "It's good to see you, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". Hmm? You say I look happy? Hehe, just before you arrived Pael made a snowman outside. She said she made it with her friends. I haven't seen her smile like that since her father died. When I see that snowman through the window, I feel a joy I cannot describe to you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MOTHER) > 3 ) {
			listmax = 0
			buff = lang("いつもありがとうございます。薬のおかげで、だいぶ痛みも引いてきました。身体が元気になったら、あの子と思う存分遊んであげたいわ…そうそう、最近では、近所の子供達が何人か見舞いに来てくれるの。パエルも、最初は恥ずかしがっていたけど、だんだんと話をするようになって。パエル、いいお友達ができるといいわね。", "Thanks for everything you've done for us. The medicine has mostly stopped the pain. If I can get better I promise to play with my daughter to her heart's content... thankfully the other kids in the neighborhood have been coming to visit these past few days. Pael seems quite shy, but even still she's started to talk to them little by little. I hope Pael can make a lot of friends.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("あら…こんな姿でごめんなさいね。パエルの父親は、パエルが5歳の時にプチに食べられてしまったの…そのせいで、あの子は家にこもりがち、いつも、ほかの子供が外で遊ぶのを眩しそうに眺めているわ。きっと、外の世界と自分との間に、見えない線を引いてしまっているのね。小さな胸の奥には、とても、とても優しい心をもった子なのに。ちょっとだけ甘えん坊さんだけどね、うふふ。", "Oh... I'm sorry you have to see me like this. Pael's father died when she was five... he was eaten by a swarm of putits. Ever since then, she just sits around the house. She's always watching the other kids play from the window, staring longingly like she'd love to join them. I can tell she's torn between watching over me and joining the outside world. She has a kind heart beating in that little chest of hers. She's a bit spoiled, but she's a good kid, he he.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 223 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_WIFE) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("こんな嫁でよければ", "Sure.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("ふざけるな", "Don't mock me you filthy wretch.")
			listmax++
			buff = lang("俺を誰だか知らないだと？この街で一番の色男、ラファエロ様だよ。俺が狙った女は、例えパルミアの王妃だろうとイチコロさ。だがな、最近はどうも恋の駆け引きみたいなのに、飽き飽きしちまった。本音をいや、無条件で俺に仕えてくれる女が欲しいんだよ。で、お前の嫁を俺によこさないか？礼はするぜ。", "Don't you know who I am? I am, by far, without question, doubtlessly and unabashedly, the sexiest man in this town -- if not the whole of North Tyris. I can have any woman I desire. I could even take the princess of Palmia if I wanted. Yet, strange as it sounds, lately I've grown tired of the game. Honestly, I want a woman who will serve me unconditionally and never complain. Why don't you let me keep one of your wives? You'd have my thanks. ")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("まあ、そんな上手い話はないよな。", "Pah, it's not that important anyhow.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("よしきた。じゃあ、お前さんのとびきりの嫁を連れてきてくれ！", "Great. Bring me a fine young girl!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_WIFE) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WIFE) == 1 | gdata(GDATA_FLAG_SUB_WIFE) == 1000 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("嫁を見せる", "How's this one?")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("ふざけるな", "Forget it.")
			listmax++
			buff = lang("どれどれ、お前さんの嫁を吟味させてもらおうか！", "Which one? Who have you brought for me to inspect?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("はやくしてくれよ。", "Bring me your wife!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			allyctrl = 1
			gosub *com_ally
			if ( stat == (-1) ) {
				listmax = 0
				buff = lang("じらすなよ。", "Huh?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			rc = stat
			if ( cbit(CHARA_BIT_MARRIED, rc) == FALSE ) {
				listmax = 0
				buff = lang("嫁だ。嫁じゃなきゃだめだ！", "I want your wife, it's pointless if you aren't married!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( cdata(CDATA_PRAY, rc) == 1 ) {
				snd SOUNDLIST_FAIL1
				txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
				goto *chat_end
			}
			listmax = 0
			buff = lang("ほう、これがお前さんの嫁か。" + cdatan(CDATAN_NAME, rc) + "ちゃんというのだな？恥ずかしがらずこっちに来なさい。", "So... this is your wife eh? " + cdatan(CDATAN_NAME, rc) + " is her name? Don't be embarassed, come along!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			tcbk = tc
			tc = rc
			if ( cdata(CDATA_IN_TAGTEAM, tc) == (-1) ) {
				ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
				cell_findspace cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 1
				if ( stat == 0 ) {
					txt lang("充分なスペースがない。", "There's no place.")
					goto *chat_end
				}
				cell_setchara ttc, rtval, rtval(1)
				tag_end tc
			}
			if ( cdata(CDATA_IN_TAGTEAM, tc) == 1 ) {
				ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
				cell_findspace cdata(CDATA_X, tc), cdata(CDATA_Y, tc), 1
				if ( stat == 0 ) {
					txt lang("充分なスペースがない。", "There's no place.")
					goto *chat_end
				}
				cell_setchara ttc, rtval, rtval(1)
				tag_end tc
			}
			s = refchara(cdata(CDATA_ID, tc), DBSPEC_CHARA_FILTER, 1)
			if ( instr(s, 0, "/man/") == (-1) ) {
				dmgcon tcbk, CONDITION_INSANE, 1000
				f = 1
				modkarma 0, 2
			}
			else {
				f = 0
				modkarma 0, -15
			}
			listmax = 0
			buff = lang("いやん、おじさまったら♪", "Ick, you're leaving me with him!?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			tc = tcbk
			if ( f == 1 ) {
				listmax = 0
				buff = lang("な、おま…ぎぎ…の嫁さ…ぎぎ…は人間じゃ…ぎぎ", "She's...she's not--hiii--She's not even hu--huuuuu--human..")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_WIFE) == 1 ) {
				flt
				itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5000
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				txtmore
				gdata(GDATA_FLAG_SUB_WIFE) = 1000
			}
			else {
			}
			chara_vanquish rc
			snd SOUNDLIST_COMPLETE1
			flt
			itemcreate -1, ITEM_ID_UNICORN_HORN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			listmax = 0
			buff = lang("これは…すごい嫁だ…！お前さんとは女の趣味が合いそうだな。また嫁を連れてきてくれれば、いいものを渡すぜ。", "She's a work of art! Looks like we have the same taste in women. Hey, if you bring me another one as good as this one, I will give you something incredible. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 224 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_YEEK) >= 1000 ) {
			listmax = 0
			buff = lang("イィッック！！イィーック！ハ…これは、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、その節はお世話になりました。", "Yeeeeck! Yeee-eeeck! Oh, it's you again.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_YEEK) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("まかせて", "I'll do it for you.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("イィッック！やめて！くるなぁー！イィーック！ハ…失礼しました。あなた…見たところ強そうですね。私に力を貸してくれませんか？騎士昇格のための試験で、イークの首領を討伐しなければならないんです。私には、荷が重い使命ですよ…イーク…うわあああ…ダメだ…思い出しただけでも膝が震える…お願いです。私の代わりに倒してきてください。", "Yeeeck! Nooo! Don't!! Get away from me!! Yeeeee-eeeck! Hump, oh....excuse me. You...you look like a skilled adventurer. Could you give me your hand? I need to slay the head of Yeek to be promoted to the rank of knight. The duty is too heavy for me....yeeee-eeck! No...! Hump...I don't want to recall anything anymore. Please do this duty for me.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("イィッック！", "Yeeeeck!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("助かります。こんな試験、私にはできっこないんです。奴らの住処は、この村を出てすぐ西の洞窟。頼みますよ。", "Fantastic! This duty...it's impossible for me. Yeeks live in a dungeon west from here. I'm counting on you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_YEEK) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_YEEK) == 1 ) {
			listmax = 0
			buff = lang("イィッック！やめて！くるなぁー！イィーック！ハ…あなたでしたか…", "Yeeeeck! Noo...! Don't..! That's my nose..! Yeee-eeeck! Hmup, oh, it's you...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_YEEK) == 2 ) {
			listmax = 0
			buff = lang("おお、無事にイークの首領を討伐してくれたのですね。これで晴れて立派な騎士になれます。本当にありがとうございました。", "Oh, you've brought me the head! I can pass the test now. Thank you very much.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_DAL_I_THALION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_YEEK) = 1000
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 225 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_DOG) == 1000 ) {
			listmax = 0
			buff = lang("くぅぅぅぅん！", "Ruff-ruff!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("連れて行く", "I'll take you")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("くぅうううん。", "Woof!")
		gosub *chat_select
		if ( chatval == 1 ) {
			if ( get_freeally() == 0 ) {
				txt lang("仲間の最大数に達しているため、これ以上連れていけない…。", "Your party is already full. You can't invite someone anymore.")
				goto *chat_end
			}
			txt lang(name(tc) + "を保護した。無事に連れて帰らなければ。", "You must return the puppy safely.")
			rc = tc
			gosub *add_ally
			cbitmod CHARA_BIT_GUARD_TEMP, rc, TRUE
			r1 = rc
			gosub *charaRefresh
			goto *chat_end
		}
		else {
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 226 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_DOG) == 1000 ) {
			listmax = 0
			buff = lang("あ、冒険者さんだ〜。こんにちわん！", "Hello there!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DOG) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("まかせて", "I'll get your puppy.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("はうっ。ポピーがいなくなっちゃった…また、あの洞窟に迷い込んじゃったのかな？どうしよう、お父さんは今いそがしくて相手にしてもらえないし…あ、冒険者さん、ポピーを探してきてよ〜。ポピー？ポピーは子犬だよ。私の一番のお友達なの。", "Oh no, Poppy has disappeared again... He probably got lost in that cave again. Now what do I do, Papa is too busy to deal with this... Oh, Adventurer, go and find Poppy for me. Poppy? He's my puppy, and my best friend.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("ポピー…。", "Poppy...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("よかったぁ！ポピーは、きっといつもの洞窟で迷っちゃったの。この街を出て、すぐ東にある洞窟だよ。冒険者さん、よろしくね。", "Great! I'm sure he's lost in that cave he always plays in. The cave is just outside the east side of town. Thanks in advance.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_DOG) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DOG) == 1 ) {
			if ( findallyguard(CREATURE_ID_POPPY_THE_LITTLE_DOG) == (-1) ) {
				listmax = 0
				buff = lang("ポピーまだ〜？", "Where's poppy?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("ポピー！！無事だったのね！冒険者さん、ありがとう。これ、あげる〜。", "Poppy you're safe！ Thank you adventurer, you can have this...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_COOLER_BOX, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_DOG) = 1000
			chara_vanquish findallyguard(CREATURE_ID_POPPY_THE_LITTLE_DOG)
			flt
			characreate -1, CREATURE_ID_POPPY_THE_LITTLE_DOG, 31, 4
			cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 227 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_CAT) >= 1000 ) {
			listmax = 0
			buff = lang("いや〜。おかげさまで家は猫から解放されたんですが、宿屋のほうが快適でね。つい、こっちで寝泊りしてしまいますよ。ははは。", "Well...actually...I found this inn more comfortable than my house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_CAT) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "No problem.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("面倒だ", "None of my business.")
			listmax++
			buff = lang("あなた、猫は大丈夫ですか？いえね、私は猫が大の苦手なんですが、こともあろうに、私の家に大量の猫が住み着いてしまったんですよ。そこで冒険者さん達に猫の退治を依頼しているものの、何故か誰も帰ってこない…いったい、家の中はどうなってるんでしょうか。あなた、もしよかったら見てきてくれませんか？", "Hey you, are you..uh...alright with cats? Well, those filthy...they creatures took over my house. I've been asking adventurers to clean up the cats from my house ever since but none returned. What's happening in my house? You...can you take a look inside my house?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("そうですか…残念です。", "Uh...fine...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("おお、ありがたい。家は南の畑の近くにあります。中にどんな猫がいるのかわからないので、十分気をつけてください。", "Thank God. My house is south from hear. Be careful, for it is unknown what inhabits my home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_CAT) = 1
			cell_featset 23, 22, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 3
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_CAT) == 1 ) {
			listmax = 0
			buff = lang("どうです？猫はもういなくなりましたか？", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_CAT) == 2 ) {
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_DRAGON
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 25500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("なんと、猫使いなる魔物が住み着いていたのですか。いやぁ、猫退治なんて軽い依頼ではなかったですね。ははは。私の家で亡くなった冒険者さん達も、あなたのおかげで浮かばれるでしょう。どうぞ、お礼を受け取ってください。", "What? An evil cat tamer? My God...I have no idea why such a creature took over my home. But uh...thanks anyway.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_CAT) = 1000
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 231 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_WAR) == 1000 ) {
			listmax = 0
			buff = lang("おお、勇者よ！酒でも飲み交わさぬか？フハハハア！", "Ah, the bravest of the brave. Care for a drink? Muwahaha!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( 0 == 0 ) {
			if ( cdata(CDATA_FAME, CHARA_PLAYER) < 5000 ) {
				listmax = 0
				buff = lang("なんだ、ぬしは。私は弱いものには興味がないのだ。フハハハハ！", "Be gone. I have no interest in weak soldiers. Muwahahaha!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txtef COLOR_RED
				txt lang("クエスト必要名声：5000　", "Required Fame: 5000 ")
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_WAR) == 0 ) {
			listmax = 0
			buff = lang("オパートスの名にかけて！貴殿はかの有名な" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "ではないか？貴殿の腕を見込んで頼みがあるのだ。", "By Opatos! You are the famous " + cdatan(CDATAN_AKA, CHARA_PLAYER) + ", are you not? I have a favor I'd like to ask of someone like you. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("話を聞こう", "(Hear him out.)")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("止めておこう", "(Don't get involved.)")
			listmax++
			buff = lang("私はジューアの解放軍の大佐、ギルバートと申す。軍といっても国軍ではないが、イェルスの犬に占拠された各地のジューア自治区を、解放してまわっておるのだ。その我が軍が、パルミア国境でイェルスの強力な防衛線の前に苦戦しておる。最新鋭の武装を持つ奴らに対し、我らは剣に頼るのみ。このままでは、全滅する恐れさえあるのだ。どうか、貴殿の力、貸してもらえぬか？", "I am colonel of the Juere Liberation Army, Gilbert. We're not the true national army, but even so we fully intend to free the Juere Territories from their occupation by those Yerles dogs. However, our army is having a hard time breaking through their strong front lines on the Palmia border. We only have our swords against their advanced weapons, after all. If things go on like they are, our army might be totally annihalated. So I ask you, will you lend us your power?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("フハハハハッ。腰抜けめ。", "Mwahahaha! You damn coward!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("フハハハァッ。そうこなくてはならぬ。貴殿の力、頼りにしておる。戦の準備ができ次第、声をかけてくれ。", "Mwahahaha! Good, good. We'll be counting on you. Once you've prepared, come speak to me again.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_WAR) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WAR) == 1 | gdata(GDATA_FLAG_SUB_WAR) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("いい", "They are.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("だめ", "Not yet.")
			listmax++
			buff = lang("準備はいいかな？", "Are your preparations complete, then?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("待っておるぞ。", "We're waiting on you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("フハァ。貴殿にオパートスの加護のあらんことを祈っておるぞ！", "Hahaha! I pray Opatos' favor be with you!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_WAR) = 2
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_YOWYN
			gdata(GDATA_TELEPORT_LEVEL) = 4
			levelexitby = MAP_EXIT_TELEPORT
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WAR) == 3 ) {
			flt
			itemcreate -1, ITEM_ID_HERO_CHEESE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("フアハァァァァハハハ。フハー！ファハハハ、フ、フハッ、ブ、ブバァッ…ゲホッ…ゲホ…す…すまん。震えるほど興奮したぞ、ぬしの雄姿に。解放軍も、これで無事にパルミアの国境を越えることができる。どうか、この感謝の気持ちを受け取ってくれ。", "Muwahahahah! Muwahahahahaha! Mu-mu...wa..! *cough* *cough* My...My apologies. Your heroic figure striding the battlefield was so amazing that even I was shaken. With this, the Liberation Army can pass the borders of Palmia. Please, accept this reward as my thanks.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_WAR) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 243 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_KAMIKAZE) == 1000 ) {
			listmax = 0
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、あなたとまた会話できるとは光栄です！", "I hope we can talk again sometime, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_KAMIKAZE) == 0 ) {
			buff = lang("パルミア軍第十連隊は、カミカゼ特攻隊の猛攻により大苦戦中です！敵は弾幕の雨の中を潜り抜け、戦死を前提とした自爆攻撃を繰り返しています。このままでは、我が部隊は全滅の恐れもあります。どうか、援軍に向かってください！", "Right now, the 10th regiment of the Palmian army is entrenched in battle with the suicide squadron! The enemy has broken through our suppression fire and now they're assaulting us with suicide bombers. The enemy know they're going to die and they're trying to take us with them. How can we fight an enemy with no regard for his own life! At this rate it looks like we'll be totally annihilated! We need you to lend us your hand and lead the reinforcements.")
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "I accept.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめておこう", "That sounds... dangerous. No thanks.")
			listmax++
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("無念です！", "I'm sorry to hear that.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("助かります。準備ができ次第、声をかけてください。必ず遠隔武器を持参してください！", "Thank the gods. call me when you're prepared. Be sure to bring a ranged weapon!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_KAMIKAZE) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_KAMIKAZE) == 1 | gdata(GDATA_FLAG_SUB_KAMIKAZE) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("いい", "Okay, let's roll.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("だめ", "I'm not ready yet.")
			listmax++
			buff = lang("準備はよろしいですか？あなたは、パルミア軍が撤退するまでの間、どうにかカミカゼ特攻隊の猛撃に耐え、時間稼ぎをして下さい。", "Are you ready to go? We need you to hold off the suicide squadron's onslaught and buy time for the Palmian army to retreat.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("早くしてください！", "Hurry up!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("伝令に現地まで案内させます。くれぐれも、奴らの接近を許さないように！パルミア軍の撤退が完了次第、連絡を入れます。", "I'll guide you to the battlefield then. Whatever you do, don't let them get close! I'll tell the Palmian army to begin a steady retreat.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_KAMIKAZE) = 2
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_PORT_KAPUL
			gdata(GDATA_TELEPORT_LEVEL) = 25
			levelexitby = MAP_EXIT_TELEPORT
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_KAMIKAZE) == 3 ) {
			flt
			itemcreate -1, ITEM_ID_MAGIC_FRUIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 7500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("お疲れ様であります。まさか、あの死地から戻って来ることができるなんて！戦友の無事も確認し、感謝の言葉もありません。どうかこの報酬を受け取ってください。", "Well done. I cannot believe you made it back alive! I've heard my comrades are safe. I can find no words to express my gratitude. Please take this reward.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_KAMIKAZE) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 247 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MIAS_DREAM) == 1000 ) {
			listmax = 0
			buff = lang("あ、冒険者さん！お仕事お疲れでありますぅ♪", "Mia's happy happy nyah!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MIAS_DREAM) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("捕ってくる", "I'll find one for you!")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("さ、さようなら", "Oh...God...")
			listmax++
			buff = lang("るんるんるん♪ねっこねっここっねこっ♪ふ〜んふ〜ん♪お〜冒険者さんだ〜。お仕事お疲れ様でありますぅ。冒険者さんは、シルバーキャット見たことありますかぁ？シルバーにゃんことお風呂できゃっきゃするのが、ミーアの夢なんですっ！う〜。ねっこねっここっねこ♪ふ〜んふ〜ん♪", "Nyobody knyows the touble Mia's seen, nyobody knyows Mia's Tru-bull. Nuah? Adventurer? Nyou find silver cat for me?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("はーい♪さよ〜なら〜♪", "Nyo? Mia can wait nyou. Mia have goood mannyah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("はわぁ。ほんとですか〜！楽しみにしてますぅ♪う〜。ねっこねっここっねこ♪は〜やくこないかな♪るんるんるん♪", "Nyowl! Mia trust nyou. Nyo hurting cats!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_MIAS_DREAM) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MIAS_DREAM) == 1 ) {
			if ( findally(CREATURE_ID_SILVER_CAT) == (-1) ) {
				listmax = 0
				buff = lang("ふ〜んふ〜ん♪ねっこねっこ〜ま〜だかな〜♪にししし！", "Nyoroon nyoroon. Nyou still look for Mia's cat?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			rc = findally(CREATURE_ID_SILVER_CAT)
			if ( cdata(CDATA_PRAY, rc) == 1 ) {
				snd SOUNDLIST_FAIL1
				txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
				goto *chat_end
			}
			if ( cdata(CDATA_IN_TAGTEAM, rc) == (-1) ) {
				ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
				cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
				if ( stat == 0 ) {
					txt lang("充分なスペースがない。", "There's no place.")
					goto *chat_end
				}
				cell_setchara ttc, rtval, rtval(1)
				tag_end rc
			}
			if ( cdata(CDATA_IN_TAGTEAM, rc) == 1 ) {
				ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
				cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
				if ( stat == 0 ) {
					txt lang("充分なスペースがない。", "There's no place.")
					goto *chat_end
				}
				cell_setchara ttc, rtval, rtval(1)
				tag_end rc
			}
			if ( jp ) {
				listmax = 0
				buff = "はわわ？それはっ！シルバーキャット！お〜♪ミーアうれしぃであります！こっちにおいで〜だっこだっこっ♪にしし！"
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("そうだ、お礼にこれあげますっ！モンスターハートっていって、所持しているだけで、支配の確率を高めてくれるアーティファクトなんですよ♪", "Nyanya! Nyou bring Mia silver cat! Come come, I nyo hurt nyou, nyo hurt nyou. Mia thank you very much. Nyou can have Mia's precious!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_MONSTER_HEART, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MIAS_DREAM) = 1000
			rc = findally(CREATURE_ID_SILVER_CAT)
			if ( cbit(CHARA_BIT_PCC, rc) == TRUE ) {
				cbitmod CHARA_BIT_PCC, rc, FALSE
			}
			relocate_chara findally(CREATURE_ID_SILVER_CAT), -1
			cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
			cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
			cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 252 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_VEIN) == 1000 ) {
			listmax = 0
			buff = lang("私は…生きている価値があるのかな？", "I wonder... does my life really have any value?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_VEIN) == 0 | gdata(GDATA_FLAG_SUB_VEIN) == 1 ) {
			listmax = 0
			buff = lang("才能のある者の努力とない者の努力は、果たして同じなのだろうか？私の妹は、このルミエストの都で絵描きを目指していた。彼女は美しいものを愛したが、画家としての才能には恵まれてなかった。自分の限界に気付いた彼女は精神を病み、周りの者に当たり散らかした。罵られ、蔑まれ、誰からも理解されないまま、冬のある日、湖に身を投げて死んでしまった。", "Talent or no talent... if you're a success it doesn't really matter now does it? My sister came here to Lumiest hoping to make it as an artist. She dearly loved the beauty of all things, but she had no artistic ability. She tried so hard and she pushed her limits to succeed, but in the end she was rejected and it drained all the life out of her. The people here mocked and insulted her; nobody understood her. Then, one winter day, despondent, she jumped into the lake and drowned herself. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("私は知っていたよ…身体を壊すほどに、妹が絵の勉強に励んでいたこと。常人離れした情熱と、名声への憧れ。だが、妹が死んで間も無く、一人の天才がこの都にやって来て、何の努力もなしに彼女が望んでいた全てを手に入れてしまった。名声、幸福、富…", "I was sure... her obsession with painting had wrecked her health. Her devotion and desire for fame alienated her from ordinary people. But, not long after my sister died, a prodigal artist came here to Lumiest. With hardly any effort, all the things she wanted came to her. Fame, happiness, fortune... ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("恵まれたもの、恵まれないもの、全ては運命の偶然に過ぎない…何がいいたいかよくわからない？そうだな、私自身、この感情をうまく説明できないんだ。ただ、私には人生の意味が分からなくなった…ただそれだけだ。", "To be privileged, or to be poor, it's all just a matter of fate and inescapable coincidence... Do you understand what it is I'm trying to say? That's right, it's tough for me to explain exactly how I feel. It's just that... I guess I no longer understand the meaning of life... that's all. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…レイチェルという童話作家が描いた絵本を、妹は愛読していた。もうすでに遺品は処理してしまったが、一度読んでみたいものだ。今ならば、あいつの気持ちを、少しはわかってやれそうな気がするよ。", "I remember my sister really loved a series of children's books illustrated by a woman named Rachel. I'm sure they're really rare, or even impossible to find, but... I'd like to read them just once. I feel that perhaps you can understand how I feel... ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_VEIN) == 0 ) {
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				gdata(GDATA_FLAG_SUB_VEIN) = 1
				goto *chat_end
			}
			p = 0, 0, 0, 0, 0, 0
			inv_getheader 0
			repeat invrange, invhead
				if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
					continue
				}
				if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_BOOK_RACHEL ) {
					p(inv(INV_ITEM_PARAM2, cnt)) = 1
				}
			loop
			repeat 4, 1
				p += p(cnt)
			loop
			if ( p != 0 ) {
				if ( p != 4 ) {
					listmax = 0
					buff = lang("私のために、絵本を" + p + "冊集めた…？感謝するよ。絵本は第4巻まであるみたいだ。もし全部集めて持ってきてくれれば、何か礼をしよう。", "You've brought " + p + " book" + _s2(p) + ", just for my sake? You have my thanks. If I recall correctly, there are four volumes in the series. If you can find all four for me I'll find some way to thank you. ")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				p = 0, 0, 0, 0, 0, 0
				inv_getheader 0
				repeat invrange, invhead
					if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
						continue
					}
					if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_BOOK_RACHEL ) {
						if ( p(inv(INV_ITEM_PARAM2, cnt)) == 0 ) {
							inv(INV_ITEM_NUM, cnt)--
							p(inv(INV_ITEM_PARAM2, cnt)) = 1
						}
					}
				loop
				listmax = 0
				buff = lang("おお…これがレイチェルの絵本か。本を読んでみたいと思う気になったのは、久しぶりだよ。さっそく読ませてもらおう。", "Oh! So these are Rachel's storybooks? I feel like I should read them soon... it's been such a long time. Give me a moment to look at them will you. These... these are! And this! And... *Renton bubbles at the mouth* ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( jp ) {
					listmax = 0
					buff = "…"
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = "… …"
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = "… … …"
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				listmax = 0
				buff = lang("ビーーー！バリリ！！ビリリ！バリリリリリ！！！こんなモノ！こんなモノ！こんなモノ…ビリビリビリビリ！ふう、ふう、はあ、はあ…。", "And this... *huff* *huff* ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txtef COLOR_YELLOW
				txt lang("エヘカトル「うみゃ〜？」", cnvtalk("Mew?"))
				listmax = 0
				buff = lang("…こんな絵は天才にしか描けない。努力じゃダメだ。妹には…どう頑張っても、描けなかっただろう。幸運の女神というものがいるのなら、私は彼女を恨もう。…すまない、報酬だ。しばらく一人にしておくれ。", "*A crazed look wells up in Renton's eyes* These books... only a true artistic genius could've painted like this. Effort alone is simply not enough... my sister... no matter how long or hard she tried, she never could've painted such beauty. If the goddess of luck truly exists, I should devote myself to her. I'm sorry... you do deserve a reward. I'd like to be alone for a while... if you don't mind. ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_STATUE_LULWY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				flt
				itemcreate -1, ITEM_ID_HERO_CHEESE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				flt
				itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 20000
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				gdata(GDATA_FLAG_SUB_VEIN) = 1000
			}
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 253 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_PYRAMID) != 0 ) {
			listmax = 0
			buff = lang("フッ。安心してください。あなたの家に侵入したりはしませんよ。", "Heh, don't worry, I won't break in your house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( 0 == 0 ) {
			if ( cdata(CDATA_FAME, CHARA_PLAYER) < 3000 ) {
				listmax = 0
				buff = lang("私に気安く話しかけないでもらえますか？", "Leave me alone, please.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txtef COLOR_RED
				txt lang("クエスト必要名声：3000　", "Required Fame: 3000 ")
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_PYRAMID) == 0 ) {
			listmax = 0
			buff = lang("ああ、あなたがかの有名な" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "ですね。いい所に来てくれました。カプールの北にある謎のピラミッドを、あなたはご存知ですか？ネフィアがまだ草の根も生えない砂漠だった頃から存在し、古代の秘宝が眠っているといわれている。しかし、入り口は奇妙な魔法に保護され、誰も手をつけることができない。いや…できなかった。", "Ah, you're the famous " + cdatan(CDATAN_AKA, CHARA_PLAYER) + ", aren't you? You came at a fine time. North of Kapul lies an enigmatic pyramid; you may have heard of it. The pyramid has existed since Nefia was a barren desert where even grass wouldn't grow and it's said that ancient treasure is buried within. However, it's sealed by some bizarre enchantments and is impenetrable... well, it WAS impenetrable.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 20000 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("購入する", "Alright, I'll pay.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("私宛にね、面白い手紙が届いたんですよ。送り主はピラミッドの主『ツェン』。そして、内容は私にピラミッドへの挑戦権を与えると。…しかし、私は危険な罠に自ら陥るほど愚かではない。あなたにこの挑戦状を譲りましょう。むろん、タダではない。金貨20000枚でいかがですか？", "I have received a very strange letter. The sender calls himself Tsen, lord of the Pyramid. He has sent me an invitation to enter his pyramid... However, I am not foolish enough to barge into such a trap. I'll turn this invitation over to you. Of course, I won't let you have it for nothing. 20,000 gold and it's yours.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("フッ。お金が用意できたら、いつでも取引に応じますよ。", "Heh, talk to me again when you have money.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 20000
			snd SOUNDLIST_PAYGOLD1
			flt
			itemcreate -1, ITEM_ID_RED_BOOK, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_PARAM1, ci) = 13
			listmax = 0
			buff = lang("ホホホッ。取引成立ですね。では、この招待状をあなたに渡しましょう。", "Deal then. You can keep this invitation.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_PYRAMID) = 1
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 259 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_NUKE) == 1000 ) {
			if ( gdata(GDATA_FLAG_MAIN) < 1000 ) {
				listmax = 0
				buff = lang("あなた、生きてたのね。ヒトゴロシくん。", "You're still alive I see. Good luck glorious murderer.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_MAIN) >= 1000 ) {
				listmax = 0
				buff = lang("よく来たわね。…カミゴロシくん。", "You're still alive I see. Good luck glorious god-killer.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_SUB_NUKE) == 1 ) {
			listmax = 0
			buff = lang("パルミアの街に、真っ赤な花が咲くのを楽しみにしているわ。", "Go, make the brilliant blood red roses bloom.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_SUB_NUKE) == 2 ) {
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_SECRET_TREASURE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_PARAM1, ci) = 162
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 6
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_NUKE) = 1000
			listmax = 0
			buff = lang("感じるわ、大勢の人の苦しみ…悲しみ…。あなたはたくさん殺したのね。小さな子供…女性…老人…生まれて間もない赤ん坊…あたし、今日は身体が火照って眠れそうにないわ。ありがとう。報酬を受け取りなさい。", "I can feel it already... the suffering... the misery... the pain... You've killed many people this day, you should be proud. Little kids, girls, women, old men, the sick, the feeble, even those neither born nor conceived yet. My body's hot, excited, I can hardly breathe... I won't be able to sleep tonight. My thanks, these are for you. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_SUB_NUKE) == 0 ) {
			listmax = 0
			buff = lang("ねえ、あなたはこの街の人じゃないでしょ？匂いで分かるのよ、外の人間は。外の…世界があたしに教えてくれたのは、力のない生物はただ利用され、弄ばれ、最後には食べられてしまうっていうことだけ。同情や慈悲さえ勝ち取れない人間が、この街には大勢いる。そんな者達がどうやって生活しているか、あなたは知りたいと思わない？", "Hey, you're not from around here are you? I can tell just by your smell. The world out there... it's taught me that the meek are inevitably used, abused, and then chewed up and swallowed kicking and screaming. The people here are a rank lot, you won't win them over with sympathy and compassion. Don't you ever wonder how people like that can survive? Don't you want to know? ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("もちろん", "Of course I will.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("あたし達は他人の血を吸って生きるのよ。憎しみや妬み、執着、悪意が、この街では力になる。外の世界では疎まれ軽蔑される弱き者達が、自分に与えられた能力をはじめて認めてもらえる、ダルフィはそんな街。ねえ、あなたもこっちの世界に来る気はない？", "It's so simple, we live by feeding off of others. Spite, hatred, wickedness, and unrelenting cutthroat vigor are the only strengths you can rely on in this town. The outside world looks down on our lot, so we will make the outside world recognize our might. Say, stranger, why don't you join me in my world?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("どっかにいきなさい、イクジナシくん。", "Then find somewhere else to stand, coward.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_NUKE) = 1
			listmax = 0
			buff = lang("そう、あなたもこちらの人間だったのね。歓迎の宴の代わりに、美味しい話をあげるわ。あたしね、たくさんの人が苦しむ姿を想像すると、とても興奮するの。パルミアの宿屋にぬいぐるみが置かれている部屋がある。そのぬいぐるみに、爆弾をしかけなさい。あのすました街を血で溢れさせてくれたら、ご褒美をあげる。", "Looks like you're one of us after all. I can't give you a welcome feast, but I can tell you a delicious little story. When I imagine others suffering, whimpering in pain and misery, I get goose bumps. How divine! At the inn in Palmia, there's a stuffed toy laying on the floor of the private room. Stick this bomb inside that toy for me. If you can drown those mindless ants in blood for me, I'll reward you well ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 12000 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("購入する", "I'll buy.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("ねえ、核爆弾買う？金貨12000枚よ。", "Here, you need a nuke. Only 12000 gold pices.")
		gosub *chat_select
		if ( chatval != 1 ) {
			listmax = 0
			buff = lang("あら、そう。", "Then go away.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		txtmore
		txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
		cdata(CDATA_GOLD, CHARA_PLAYER) -= 12000
		snd SOUNDLIST_PAYGOLD1
		flt
		itemcreate -1, ITEM_ID_NUCLEAR_BOMB, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
		listmax = 0
		buff = lang("さあ、真っ赤な花を咲かせなさい。", "Go, make the brilliant blood red roses bloom.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 279 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) >= 1000 ) {
			listmax = 0
			buff = lang("なんじゃ、話しかけるでにゃー。実験で忙しいのじゃ！", "I don't have time to talk, I'm busy conducting experiments. Out of the way!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 2
			listn(0, listmax) = lang("仲間に実験を手伝わせる", "My companions will help you.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("自分が実験を手伝う", "Alright, I'll see what I can do.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("(後天的身体部位の削除実験と書かれた紙が散らばっている…)", "(He seems to have an experiment to remove body parts...)")
			gosub *chat_select
			if ( chatval == 1 ) {
				list(0, listmax) = 1
				listn(0, listmax) = strbye
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("ああ", "Alright, I'll see what I can do.")
				listmax++
				buff = lang("おおそうか！！ならば早速、実験体になってもらおうかの！さっさと全部脱がんか！", "Then get out of my way you fool!")
				gosub *chat_select
				if ( chatval != 0 ) {
					goto *chat_end
				}
				listmax = 0
				buff = lang("これをこうして…さらに…ん？まぁよいか。そしてこうやってついでに…。", "Then get out of my way you fool!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				maebui = 0
				repeat 30, 100
					if ( cdata(cnt, CHARA_PLAYER) != 0 ) {
						maebui++
					}
				loop
				snd SOUNDLIST_BALL1
				await 300
				snd SOUNDLIST_ALERT1
				await 300
				snd SOUNDLIST_HEART1
				await 300
				snd SOUNDLIST_KILL2
				await 300
				snd SOUNDLIST_VOMIT
				await 300
				snd SOUNDLIST_AMMO
				await 300
				snd SOUNDLIST_PRAY2
				await 300
				snd SOUNDLIST_CRUSH1
				redraw 0
				gosub *screen_draw
				gosub *screen_fade
				gsel BUFFER_BUF
				pos 0, 0
				picload exedir + "graphic\\face1" + devfile + ".bmp", 1
				gsel BUFFER_SCREEN
				repeat 30, 100
					p = cdata(cnt, CHARA_PLAYER) \ EXT_EQUIP_SLOTS
					if ( p == 0 ) {
						continue
					}
					p--
					inv(INV_ITEM_EQUIP, p) = 0
					cdata(cnt, CHARA_PLAYER) = cdata(cnt, CHARA_PLAYER) / EXT_EQUIP_SLOTS * 10000
				loop
				repeat 30, 100
					if ( cdata(cnt, CHARA_PLAYER) != 0 ) {
						cdata(cnt, CHARA_PLAYER) = 0
					}
				loop
				r1 = 0
				rc = CHARA_PLAYER
				s(1) = ""
				if ( cdatan(CDATAN_RACE, r1) == "kobolt" ) {
					s(1) = "頭|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "orc" ) {
					s(1) = "頭|体|背|手|手|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "largeanimal" ) {
					s(1) = "頭|首|体|背|手|足|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "lizardman" ) {
					s(1) = "頭|体|背|手|手|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "minotaur" ) {
					s(1) = "頭|体|手|手|指|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "yerles" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "norland" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "eulderna" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "fairy" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "asura" ) {
					s(1) = "手|手|手|手|首|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "slime" ) {
					s(1) = "頭|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "doggod" ) {
					s(1) = "頭|首|体|手|指|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "dwarf" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "juere" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "zombie" ) {
					s(1) = "頭|首|体|背|手|手|指|腕|腰|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "elea" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "rabbit" ) {
					s(1) = "頭|首|体|背|腕|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "sheep" ) {
					s(1) = "頭|首|体|背|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "frog" ) {
					s(1) = "体|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "worm" ) {
					s(1) = "背|指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "snail" ) {
					s(1) = "背|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "mandrake" ) {
					s(1) = "頭|背|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "beetle" ) {
					s(1) = "首|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "mushroom" ) {
					s(1) = "頭|首|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "bat" ) {
					s(1) = "頭|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "ent" ) {
					s(1) = "手|指|指|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "lich" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "bacteria" ) {
					s(1) = "体|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "ghost" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腰|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "spirit" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "eye" ) {
					s(1) = "頭|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "mazin" ) {
					s(1) = "首|体|背|手|手|指|指|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "wasp" ) {
					s(1) = "頭|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "giant" ) {
					s(1) = "体|背|手|手|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "imp" ) {
					s(1) = "首|体|手|手|指|指|指|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "hand" ) {
					s(1) = "手|手|指|指|腕|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "snake" ) {
					s(1) = "体|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "drake" ) {
					s(1) = "首|体|指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "goblin" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "bear" ) {
					s(1) = "手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "armor" ) {
					s(1) = "頭|首|体|背|手|手|腕|腰|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "medusa" ) {
					s(1) = "体|手|手|指|指|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "cupid" ) {
					s(1) = "首|体|手|手|指|指|腕|腰|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "karune" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "harpy" ) {
					s(1) = "首|背|手|手|指|指|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "dragon" ) {
					s(1) = "首|体|指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "dinosaur" ) {
					s(1) = "首|体|指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "beast" ) {
					s(1) = "頭|首|体|背|手|足|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "spider" ) {
					s(1) = "指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "golem" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "rock" ) {
					s(1) = "頭|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "zanan" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "skeleton" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "piece" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "cat" ) {
					s(1) = "頭|首|体|背|手|足|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "dog" ) {
					s(1) = "頭|首|体|背|手|足|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "roran" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "rat" ) {
					s(1) = "背|手|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "shell" ) {
					s(1) = "背|手|手|指|指|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "catgod" ) {
					s(1) = "頭|首|体|手|指|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "machinegod" ) {
					s(1) = "頭|体|背|手|手|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "undeadgod" ) {
					s(1) = "首|体|背|手|手|指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "machine" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "wisp" ) {
					s(1) = "頭|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "bird" ) {
					s(1) = "頭|体|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "seamonster" ) {
					s(1) = "頭|首|体|背|手|手|腕|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "catsister" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "mutant" ) {
					s(1) = "体|手|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "yeek" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "yith" ) {
					s(1) = "手|手|手|手|指|指|指|指|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "servant" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "horse" ) {
					s(1) = "体|足|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "god" ) {
					s(1) = "手|手|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "quickling" ) {
					s(1) = "頭|体|背|手|手|指|指|腕|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "metal" ) {
					s(1) = "頭|体|背|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "vehicle" ) {
					s(1) = "頭|体|背|手|手|腰|足|足|"
					gosub *set_figure
				}
				if ( cdatan(CDATAN_RACE, r1) == "behicle" ) {
					s(1) = "頭|体|背|手|手|腰|足|足|"
					gosub *set_figure
				}
				if ( s(1) == "" ) {
					s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
					gosub *set_figure
				}
				r1 = 0
				gosub *calcBodyFix
				atobui = 0
				repeat 30, 100
					if ( cdata(cnt, CHARA_PLAYER) != 0 ) {
						atobui++
					}
				loop
				if ( maebui > atobui ) {
					txtef COLOR_RED
					txt lang("気がつくと、後天的な部位に装備が出来なくなっていた…。", "Then find somewhere else to stand, coward.")
				}
				r1 = 0
				gosub *charaRefresh
				dbidn = 0
				dbmode = DBMODE_NONE
				txt lang("「カガクの進歩に犠牲はつきものじゃー」", "Moyer yells, " + cnvtalk("You idiot!"))
				r1 = 0
				rc = CHARA_PLAYER
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("おおそうか！！それで、誰が手伝ってくれるんじゃ？", "Then get out of my way you fool!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				ikoru = 100
				allyctrl = 3
				gosub *com_ally
				ikoru = 0
				if ( stat != (-1) ) {
					c = stat
					snd SOUNDLIST_OK1
					txtnew
					listmax = 0
					buff = lang("ならば早速、実験体になってもらおうかの！さっさと全部脱がんか！", "Then get out of my way you fool!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("これをこうして…さらに…うむ？こっちじゃったか？とりあえずはこうして…。", "Then get out of my way you fool!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					maebui = 0
					repeat 30, 100
						if ( cdata(cnt, c) != 0 ) {
							maebui++
						}
					loop
					snd SOUNDLIST_BOLT1
					await 300
					snd SOUNDLIST_FIZZLE
					await 300
					snd SOUNDLIST_ATK_HELL
					await 300
					snd SOUNDLIST_EQUIP1
					await 300
					snd SOUNDLIST_POP3
					await 300
					snd SOUNDLIST_LOCKED1
					await 300
					snd SOUNDLIST_VOMIT
					redraw 0
					gosub *screen_draw
					gosub *screen_fade
					gsel BUFFER_BUF
					pos 0, 0
					picload exedir + "graphic\\face1" + devfile + ".bmp", 1
					gsel BUFFER_SCREEN
					r1 = c
					repeat 30, 100
						p = cdata(cnt, r1) \ EXT_EQUIP_SLOTS
						if ( p == 0 ) {
							continue
						}
						p--
						inv(INV_ITEM_EQUIP, p) = 0
						cdata(cnt, r1) = cdata(cnt, r1) / EXT_EQUIP_SLOTS * 10000
					loop
					repeat 30, 100
						if ( cdata(cnt, r1) != 0 ) {
							cdata(cnt, r1) = 0
						}
					loop
					r1 = c
					rc = c
					s(1) = ""
					if ( cdatan(CDATAN_RACE, r1) == "kobolt" ) {
						s(1) = "頭|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "orc" ) {
						s(1) = "頭|体|背|手|手|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "largeanimal" ) {
						s(1) = "頭|首|体|背|手|足|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "lizardman" ) {
						s(1) = "頭|体|背|手|手|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "minotaur" ) {
						s(1) = "頭|体|手|手|指|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "yerles" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "norland" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "eulderna" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "fairy" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "asura" ) {
						s(1) = "手|手|手|手|首|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "slime" ) {
						s(1) = "頭|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "doggod" ) {
						s(1) = "頭|首|体|手|指|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "dwarf" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "juere" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "zombie" ) {
						s(1) = "頭|首|体|背|手|手|指|腕|腰|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "elea" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "rabbit" ) {
						s(1) = "頭|首|体|背|腕|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "sheep" ) {
						s(1) = "頭|首|体|背|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "frog" ) {
						s(1) = "体|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "worm" ) {
						s(1) = "背|指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "snail" ) {
						s(1) = "背|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "mandrake" ) {
						s(1) = "頭|背|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "beetle" ) {
						s(1) = "首|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "mushroom" ) {
						s(1) = "頭|首|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "bat" ) {
						s(1) = "頭|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "ent" ) {
						s(1) = "手|指|指|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "lich" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "bacteria" ) {
						s(1) = "体|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "ghost" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腰|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "spirit" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "eye" ) {
						s(1) = "頭|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "mazin" ) {
						s(1) = "首|体|背|手|手|指|指|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "wasp" ) {
						s(1) = "頭|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "giant" ) {
						s(1) = "体|背|手|手|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "imp" ) {
						s(1) = "首|体|手|手|指|指|指|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "hand" ) {
						s(1) = "手|手|指|指|腕|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "snake" ) {
						s(1) = "体|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "drake" ) {
						s(1) = "首|体|指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "goblin" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "bear" ) {
						s(1) = "手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "armor" ) {
						s(1) = "頭|首|体|背|手|手|腕|腰|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "medusa" ) {
						s(1) = "体|手|手|指|指|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "cupid" ) {
						s(1) = "首|体|手|手|指|指|腕|腰|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "karune" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "harpy" ) {
						s(1) = "首|背|手|手|指|指|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "dragon" ) {
						s(1) = "首|体|指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "dinosaur" ) {
						s(1) = "首|体|指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "beast" ) {
						s(1) = "頭|首|体|背|手|足|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "spider" ) {
						s(1) = "指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "golem" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "rock" ) {
						s(1) = "頭|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "zanan" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "skeleton" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "piece" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "cat" ) {
						s(1) = "頭|首|体|背|手|足|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "dog" ) {
						s(1) = "頭|首|体|背|手|足|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "roran" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "rat" ) {
						s(1) = "背|手|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "shell" ) {
						s(1) = "背|手|手|指|指|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "catgod" ) {
						s(1) = "頭|首|体|手|指|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "machinegod" ) {
						s(1) = "頭|体|背|手|手|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "undeadgod" ) {
						s(1) = "首|体|背|手|手|指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "machine" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "wisp" ) {
						s(1) = "頭|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "bird" ) {
						s(1) = "頭|体|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "seamonster" ) {
						s(1) = "頭|首|体|背|手|手|腕|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "catsister" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "mutant" ) {
						s(1) = "体|手|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "yeek" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "yith" ) {
						s(1) = "手|手|手|手|指|指|指|指|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "servant" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "horse" ) {
						s(1) = "体|足|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "god" ) {
						s(1) = "手|手|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "quickling" ) {
						s(1) = "頭|体|背|手|手|指|指|腕|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "metal" ) {
						s(1) = "頭|体|背|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "vehicle" ) {
						s(1) = "頭|体|背|手|手|腰|足|足|"
						gosub *set_figure
					}
					if ( cdatan(CDATAN_RACE, r1) == "behicle" ) {
						s(1) = "頭|体|背|手|手|腰|足|足|"
						gosub *set_figure
					}
					if ( s(1) == "" ) {
						s(1) = "頭|首|体|背|手|手|指|指|腕|腰|足|"
						gosub *set_figure
					}
					r1 = c
					gosub *calcBodyFix
					r1 = c
					gosub *charaRefresh
					dbidn = 0
					dbmode = DBMODE_NONE
					atobui = 0
					repeat 30, 100
						if ( cdata(cnt, r1) != 0 ) {
							atobui++
						}
					loop
					if ( maebui > atobui ) {
						txtef COLOR_RED
						txt lang(name(r1) + "は後天的な部位を失った…。", "Then find somewhere else to stand, coward.")
					}
					txt lang("「カガクの進歩、発展のためには犠牲がつきものじゃ！」", "Moyer yells, " + cnvtalk("You idiot!"))
					r1 = 0
					rc = CHARA_PLAYER
					ikoru = 0
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("手伝う", "Alright, I'll see what I can do.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("おうおう、良いところに来た。猫の手も借りたいほど忙しいのじゃ。今わしは能力転移の研究をしておってな、成功すればなんと、生物の能力だけを取り出し他の生物に付加することができるのじゃ！最後の段階として、5体の実験体が必要じゃが、ちょっと取ってきてはくれまいか？報酬として、今使っている試作機をくれてやってもよいぞ。", "Oh oh oh! You've come at *just* the right time. I'm so busy I'd take help from a cat if I had to. I'm doing research in the most remarkable field of genetic engineering. You see, I can take two living creatures and combine them to produce an even stronger creature! Is that not the most amazing thing you've ever heard? I need five test subjects for the final stage of the experiment. Won't you go get some for me? I'll reward you with the gene splicing machine I'm building.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("とんだ愚か者じゃ！", "Then get out of my way you fool!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MONSTER_BALL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 6
			inv(INV_ITEM_PARAM2, ci) = 5
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			listmax = 0
			buff = lang("よしよし…このモンスターボールを持っていくがいい。使い方はわかっておるな？そう、瀕死のモンスターに投げつけるだけでよい。ボールのLVを超える相手には効かないから、気をつけるのじゃ。5個のボールにモンスターを捕獲して持ってきてくれ。いいか、「ボールに入れたまま」持ってくるのじゃぞ！", "Good good! Take these monster balls with you. Do you know how to use them? Yes yes, throw them at a monster when it is near death. The ball won't work if the monster is too high in level, so be careful. Please bring me five captured monsters. Oh... and... *please* bring them back to me IN the balls!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_BIOLOGIST) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) >= 1 & gdata(GDATA_FLAG_SUB_BIOLOGIST) <= 5 ) {
			inv_getheader 0
			f = 0
			repeat invrange, invhead
				if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) >= 6 ) {
					break
				}
				if ( inv(INV_ITEM_NUM, cnt) > 0 ) {
					if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_MONSTER_BALL ) {
						if ( inv(INV_ITEM_SUB_NAME, cnt) != 0 ) {
							inv(INV_ITEM_NUM, cnt)--
							gdata(GDATA_FLAG_SUB_BIOLOGIST)++
							f = 1
							txt itemname(cnt, 1) + "を納入した。"
							continue cnt
						}
					}
				}
			loop
			if ( f ) {
				listmax = 0
				buff = lang("おうおう、例の品を持ってきてくれたか。これで実験が進むわい。", "Oh oh! What a fine batch of samples you've brought me. I can finally proceed with the final stage.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( f == 0 ) {
				listmax = 0
				buff = lang("さあ、実験体を集めてくるのじゃ。", "Let's go over this one last time... I need five samples to proceed. FIVE.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gosub *calcBurdenPc
		}
		if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) >= 6 ) {
			listmax = 0
			buff = lang("成功じゃ！実験は大成功じゃ！おぬしの働きに感謝して、記念すべき能力複合機第一号をくれてやろう。大事に使うのじゃぞ！", "Success! The experiment is a huge success! You've done a great job! To express my thanks I'll let you use my gene splicing machine. Please take good care of it.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_GENE_MACHINE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_BIOLOGIST) = 1000
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 280 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_SEWER) == 1000 ) {
			listmax = 0
			buff = lang("ルミエストの街道は今日も清潔だぞ！", "Clean! The roads are clean in Lumiest today!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_SEWER) == 0 ) {
			listmax = 0
			buff = lang("待て待て待てーい！", "Wait there! Wait!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("いいだろう", "Alright.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("面倒だ", "I'm not interested.")
			listmax++
			buff = lang("俺はルミエストの清掃を生涯の使命とし、この道十年のベテラン掃除屋バルザックだ。街道に落ちているどんな小さなゴミも見逃さないプロ魂を持つが、そんなプライドも傷つけられちまった。下水道にとんでもない化け物が住みついて、もう俺の手には負えないぜ。お前なら、俺の気持ちを汲み取って、退治に出向いてくれるな？", "I'm Balzak, a well known pro garage man who has been cleaning the roads of Lumiest for 10 years. I've never missed a single garbage that messes the beautiful town of Lumiest. But my days are over. Foul creatures now nest in the sewer and sick smells spread everywhere. Adventurer, you are my only hope. Kill them, kill them for my sake!")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("そうか…冷たい奴だな…", "Ok. You are cruel.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("その言葉、待っていたぜ！下水道は宿屋の近くにある。気をつけろ、奴らは手強いぞ！", "That's the word I wanted to hear! You'll find the sewer entrance near the inn. Be aware, they are pretty tough.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_SEWER) = 1
			cell_featset 18, 45, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 20
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_SEWER) == 1 ) {
			listmax = 0
			buff = lang("おう、お前か。下水道の掃除は進んでいるか？", "Oh, it's you. What about the task I asked you?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_SEWER) == 2 ) {
			flt
			itemcreate -1, ITEM_ID_STATUE_JURE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MONSTER_BALL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_PARAM2, ci) = 30
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 15000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("なんだってー！あの下水道をほんとに浄化したというのか？さすがに、お前を選んだ俺の目は確かだったということか…ともかく礼をいうぞ！この報酬を受け取ってくれ。", "Whaaat! You've really done it? Amazing! Here, take it, you deserve this.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_SEWER) = 1000
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 288 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_MAGE) == 1 ) {
			if ( gdata(GDATA_RANK_GUILD) > 8000 ) {
				listmax = 0
				buff = lang("新しく入った方ですか。頑張ってください。", "One of the new arrivals? Do your best.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_RANK_GUILD) > 4000 ) {
				listmax = 0
				buff = lang("最近活躍しているらしいですね。あなたには期待していますよ。", "Lately seems you've been quite active. I look forward to your progress.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_RANK_GUILD) > 600 ) {
				listmax = 0
				buff = lang("以前は他の２ギルドに押され気味でしたが…あなたが来てからこのギルドもかなり活気が戻りました。…ありがとうございます。", "Some time ago it felt like we were being suppressed by two guilds... but since your arrival we've gained momentum. ...You have my thanks.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS1) < 1 ) {
				listmax = 0
				buff = lang("狡猾な盗賊ギルドと野蛮な戦士ギルドを制し、我々魔術士ギルドの力を世間に見せつけるのです！あなたにはどちらか片方のギルドのマスターを倒してきてほしいのです。もう片方は私が相手しましょう。", "By claiming rule over the dastardly Thieves guild and the Savage fighters our Mages guild will show the world our might! I want you to defeat the guild master of one of the guilds.  I will take care of the other.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_GUILD_WARS1) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS1) == 1 ) {
				listmax = 0
				buff = lang("さて、私はどちらのギルドを攻めましょうかね…。", "Now then, which guild shall I combat?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS1) == 2 ) {
				listmax = 0
				buff = lang("素晴らしい！あなたならやってくれると信じていました！これは私からの特別報酬です。どうか受け取ってください。", "Wonderful! I believed you would succeed! This is a special reward from me.  Please take it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_BOOK_BOKONON, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				flt
				itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				gdata(GDATA_FLAG_SUB_GUILD_WARS1) = 1000
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS1) >= 3 ) {
				listmax = 0
				buff = lang("次のギルドマスターはあなたしかいない。私だけではなく皆もそう考えているようです。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "…これからもよろしく頼みますね。", "There is none other than you worthy of guild master. Not just me but everyone feels the same. " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "...From now on we leave things in your hands.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_GUILD_WARS1) = 1000
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("どちらさまでしょう？見かけない顔ですね。", "Sorry...who are you?")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 291 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_FIGHTER) == 1 ) {
			if ( gdata(GDATA_RANK_GUILD) > 8000 ) {
				listmax = 0
				buff = lang("新人か。精進するように。", "A new recruit. With due diligence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_RANK_GUILD) > 4000 ) {
				listmax = 0
				buff = lang("最近活躍しているらしいじゃないか。君には期待しているよ。", "You've been quite active recently. I expect much from you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_RANK_GUILD) > 600 ) {
				listmax = 0
				buff = lang("他の者たちも君に負けじと切磋琢磨している。いい傾向だ、君がわがギルドに加入してくれて本当によかった…。", "Others have refined themselves to surpass you. It's a magnificent trend. I honestly appreciate you choosing our guild...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS3) < 1 ) {
				listmax = 0
				buff = lang("戦士の力を目にもの見せる時が来た！盗賊ギルドと魔術士ギルド、腑抜けた両ギルドのマスターを私と君で打倒するぞ！戦いやすい方のマスターを倒してきてくれ。もう片方は私が引き受けよう。", "The time has come to show the might of we warriors! We shall overthrow the cowardly guild masters both! Please defeat the lesser of the two masters. The other I shall.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_GUILD_WARS3) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS3) == 1 ) {
				listmax = 0
				buff = lang("うむ、明確な打倒目標があると素振りにも気合が入るな！", "Mm, having a discernable opponent has my blood flowing!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS3) == 2 ) {
				listmax = 0
				buff = lang("勝ってきたか！君ならばやってくれると信じていたよ！これは私からの特別報酬だ。受け取ってほしいな。", "You have been victorious! I believed you would! This is a special reward from me. Please accept it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_FLAME_EDGE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				flt
				itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				gdata(GDATA_FLAG_SUB_GUILD_WARS3) = 1000
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS3) >= 3 ) {
				listmax = 0
				buff = lang("次のギルドマスターは君しかいない。私だけではなく皆もそう考えているはずだ。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、これからもよろしく頼むぞ。", "There is no other than you for the next guild master. Not just me but everyone believes so. " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", we are looking forward to working with you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_GUILD_WARS3) = 1000
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("誰だ？見かけない顔だな。", "Who are you? I haven't seen your face before.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 292 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_THIEF) == 1 ) {
			if ( gdata(GDATA_RANK_GUILD) > 8000 ) {
				listmax = 0
				buff = lang("貴様が新入りか。ヘマはするなよ。", "Yer a newbie. Don't screw up.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_RANK_GUILD) > 4000 ) {
				listmax = 0
				buff = lang("貴様の働きぶりには正直感心している。だが油断はしないことだ。", "Yer stuff is pretty good. Don't be getting lazy.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_RANK_GUILD) > 600 ) {
				listmax = 0
				buff = lang("貴様ほどの冒険者ならば他のギルドでも活躍できたろうに。わざわざ闇に生きるとは変わったやつだ。…おかげで助かってはいるがな。", "Since yer such a great adventurer ya coulda gone to another guild. Yer a strange one fer picking ours. Big help for us ya been.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS2) < 1 ) {
				listmax = 0
				buff = lang("とっておきの仕事が入った。魔術士ギルドと戦士ギルドのギルドマスターを片づける。重要な仕事だが…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、貴様に片方のマスターの始末を任せる。しくじるなよ。", "Got a reserved mission. We're gonna ice the mage and warrior guild masters. Pretty important job ya see..." + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", Yer gonna take down one of them. Don't screw up.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_GUILD_WARS2) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS2) == 1 ) {
				listmax = 0
				buff = lang("貴様なら他のギルドマスターにもきっと勝てるさ。ふっ、他人をここまで信頼するとは俺も甘くなったものだ。", "Ya can probably take down one of the other guild masters. Heh, Can't believe I'm trustin someone else this much, I've gotten soft.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS2) == 2 ) {
				listmax = 0
				buff = lang("遅かったな…別に心配をしていたわけではないが。こいつは貴様の分け前だ。とっておけ。", "Too yer sweet time... Not like I was worried. Here's yer share. Take it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_WOODEN_HORSE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				flt
				itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				gdata(GDATA_FLAG_SUB_GUILD_WARS2) = 1000
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_GUILD_WARS2) >= 3 ) {
				listmax = 0
				buff = lang("次のギルドマスターを貴様に任せてやってもいいと思っている。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "…裏切らないでくれよ。", "I'm thinkin I can just leave it up to you for next guild master. " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", ya better not double cross us.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_GUILD_WARS2) = 1000
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("貴様…何者だ？", "Who the hell are you?")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 290 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_MAGE) == 0 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("魔術士ギルドとは？", "Tell me about the Mages Guild.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("魔術士ギルドに加入したい", "I want to join the Mages Guild.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("止まれ…この階段の先は魔術士ギルドの管轄だ。ギルドメンバー以外の者が入ることはできない…", "Halt...the stairs lead to the Mages Guild office. Only the members of the guild are allowed to enter.")
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("魔術士ギルドは、各地の魔術士を管轄する組合だ。組合員には規律の厳守と一定のノルマを求める代わりに、援助を施し特定の仕事を斡旋している。魔術士ギルドのメンバーには、魔法書の予約、全店舗での魔法書の割引、魔術スキルの習得と訓練などの特典がある。ギルドに加入するには、厳しい審査を通らなければならない。", "The Mages Guild is made up by experienced and confirmed experts in their field of magic. We offer a variety of services such as discounted spellbook prices, reservation of spellbooks, and training related skills. But joining the Mages Guild requires you to pass a difficult test that most common mages will fail.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_SUB_GUILD_MAGE) == 0 ) {
					listmax = 0
					buff = lang("ギルドに加入したいというのか…我々が求めているのは有能な魔術士だ。お前がギルドのメンバーにふさわしいか試させてもらおう。", "So you are interested in joining our guild and we only need elite mages. Let's see if you are worthy of becoming one of us.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("この世界には、特殊な古代文字で書かれた古書物が散らばっている。これらを収集し、解読した後に、私の横にある納入箱に収めたまえ。古書物の価値に応じてギルドポイントを与えよう。30ギルドポイント、これがお前に課せられた試験のノルマだ…", "In north Tyris, there are several ancient books encrypted with an unique rune language. Should you want to join the guild, put them into the delivery chest after you have decrypted them and we shall reward you with guild points.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_GUILD_MAGE) = 1
					gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 30
					snd SOUNDLIST_WRITE1
					txtef COLOR_GREEN
					txt lang("ジャーナルが更新された。", "Your journal has been updated.")
					listmax = 0
					buff = lang("ギルドポイントを規定の値まで集めたら、また私に話しかけてくれ。", "You will need to gather 30 guild points in order to enter the Mages Guild. Until then, we have no more to speak.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				if ( gdata(GDATA_FLAG_GUILD_MAGE_NORMA) > 0 ) {
					listmax = 0
					buff = lang("まだノルマが残っているじゃないか…ギルドに加入するには、審査を通らなければならないぞ。", "To pass the test, you need to gather 30 guild points.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				listmax = 0
				buff = lang("ほう…どうやらノルマを見事に達成したようだな。", "Quite impressive...you have accomplished the task.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_RANK_GUILD) = 10000
				gdata(GDATA_FLAG_GUILD_THIEF) = 0
				gdata(GDATA_FLAG_GUILD_FIGHTER) = 0
				gdata(GDATA_FLAG_GUILD_MAGE) = 1
				gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 0
				gdata(GDATA_FLAG_GUILD_THIEF_NORMA) = 0
				gdata(GDATA_FLAG_SUB_GUILD_MAGE) = 1000
				gdata(GDATA_FLAG_SUB_GUILD_THIEF) = 0
				gdata(GDATA_FLAG_SUB_GUILD_FIGHTER) = 0
				gdata(GDATA_FLAG_SUB_FIGHTER_1) = 0
				gdata(GDATA_FLAG_SUB_MAGE_1) = 0
				gdata(GDATA_FLAG_SUB_THIEF_1) = 0
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				txtmore
				txtef COLOR_YELLOW
				txt lang("魔術士ギルドに加入した！", "You have joined the Mages Guild!")
				listmax = 0
				buff = lang("おめでとう…合格だ。今日からお前は魔術士ギルドの一員となった。以後はギルドの規約を守り、ノルマをしっかりこなすように。", "Congratulations. You are a formal member of the Mages Guild now. You can now use this stairs to enter the office. Always conform to the guild's rule and perform your tasks.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cell_movechara tc, 4, 20
				cdata(CDATA_X_ORG, tc) = 4
				cdata(CDATA_Y_ORG, tc) = 20
			}
			goto *chat_end
		}
		cell_movechara tc, 4, 20
		cdata(CDATA_X_ORG, tc) = 4
		cdata(CDATA_Y_ORG, tc) = 20
		if ( gdata(GDATA_FLAG_SUB_MAGE_1) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("ノルマを貰う", "Do you have any tasks for me?")
			listmax++
		}
		else {
			list(0, listmax) = 2
			listn(0, listmax) = lang("ノルマを報告する", "About my task.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("ようこそ魔術士ギルドへ、" + ranktitle(8) + "の" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "…どうぞ中にお入り下さい。", "Welcome to the Mages Guild, " + ranktitle(8) + " " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "... Please come in.")
		gosub *chat_select
		if ( chatval == 1 ) {
			gdata(GDATA_FLAG_SUB_MAGE_1) = 1
			gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 75 - gdata(GDATA_RANK_GUILD) / 200
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("良い心がけです。あなたにランク昇格のためのノルマを与えましょう。世界に散らばった古書物を解読し、納入してください。" + gdata(GDATA_FLAG_GUILD_MAGE_NORMA) + "ギルドポイントが集まったら、昇進のための審査会に報告してあげましょう。", "Sure, there're plenty of tasks for you. For now, collect the ancient books and decrypt them. When you gather " + gdata(GDATA_FLAG_GUILD_MAGE_NORMA) + " guild points, we will think about your promotion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			if ( gdata(GDATA_FLAG_GUILD_MAGE_NORMA) > 0 ) {
				listmax = 0
				buff = lang("どうしました？まだノルマが残っていますよ。", "You have your task to complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			gdata(GDATA_FLAG_SUB_MAGE_1) = 0
			flt 51 - gdata(GDATA_RANK_GUILD) / 200
			flttypemajor = FILTER_ITEM_SPELLBOOK
			itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000 - gdata(GDATA_RANK_GUILD) + 1000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(4 - gdata(GDATA_RANK_GUILD) / 2500, 1, 4)
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			modrank 8, 500, 8
			listmax = 0
			buff = lang("確かにノルマの達成を確認しました。早速審査会を開いてあなたの功績を報告しましょう。", "You've done very well. I will give a report of your achievement to the council.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 294 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_THIEF) == 0 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("盗賊ギルドとは？", "Tell me about the Thieves Guild.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("盗賊ギルドに加入したい", "I want to join the Thieves Guild.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("止まれ…この階段の先は盗賊ギルドの管轄だ。ギルドメンバー以外の者が入ることはできない…", "Halt...the stairs lead to the Thieves Guild office. Only the members of the guild are allowed to enter.")
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("盗賊ギルドは、各地の盗賊を管轄する組合だ。組合員には規律の厳守と一定のノルマを求める代わりに、援助を施し特定の仕事を斡旋している。盗賊ギルドのメンバーには、盗品の高値での売却、ブラックマーケットでの割引、盗賊スキルの習得と訓練などの特典がある。ギルドに加入するには、厳しい審査を通らなければならない。", "The Thieves Guild is made up by experienced and confirmed experts in their field of the shadow arts. We offer a variety of services such as buying stolen goods, discount at black markets, and training related skills. But joining the Thieves Guild requires you to pass a difficult test that most common thieves will fail.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_SUB_GUILD_THIEF) == 0 ) {
					listmax = 0
					buff = lang("盗賊ギルドに加入したいというのか…我々は知ってのとおり闇の世界に身を置いている。まずは、お前が信頼に足る者かどうか試させてもらおう。", "So...you're interested in our guild? We hide in the shadows of the society. You must win our trust to enter the guild.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_GUILD_THIEF) = 1
					snd SOUNDLIST_WRITE1
					txtef COLOR_GREEN
					txt lang("ジャーナルが更新された。", "Your journal has been updated.")
					listmax = 0
					buff = lang("なに、簡単な試験だ。一定のレベルを越えた冒険者には、税金が毎月徴収されるのは知っているな？その税金を4ヶ月滞納するだけだ。無論、お前は犯罪者となり、国の犬に追われる事になるだろう。その覚悟がないならば、盗賊ギルドに入るなど諦めることだ。", "It is a simple task. We require you to not pay tax for 4 months. Take it granted the guards will try to hunt you down. If you aren't ready to face it, just forget it.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				if ( gdata(GDATA_BILL) <= 4 & 0 == 0 ) {
					listmax = 0
					buff = lang("どうした？試験の内容は伝えたはずだ。税金を4ヶ月以上滞納してからまた来るがいい。", "I've already told you what you must do. Don't pay tax for 4 months.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				listmax = 0
				buff = lang("ほう…どうやらノルマを見事に達成したようだな。", "Quite impressive...you have accomplished the task.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_RANK_GUILD) = 10000
				gdata(GDATA_FLAG_GUILD_THIEF) = 1
				gdata(GDATA_FLAG_GUILD_FIGHTER) = 0
				gdata(GDATA_FLAG_GUILD_MAGE) = 0
				gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 0
				gdata(GDATA_FLAG_GUILD_THIEF_NORMA) = 0
				gdata(GDATA_FLAG_SUB_GUILD_MAGE) = 0
				gdata(GDATA_FLAG_SUB_GUILD_THIEF) = 1000
				gdata(GDATA_FLAG_SUB_GUILD_FIGHTER) = 0
				gdata(GDATA_FLAG_SUB_FIGHTER_1) = 0
				gdata(GDATA_FLAG_SUB_MAGE_1) = 0
				gdata(GDATA_FLAG_SUB_THIEF_1) = 0
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				txtmore
				txtef COLOR_YELLOW
				txt lang("盗賊ギルドに加入した！", "You have joined the Thieves Guild!")
				listmax = 0
				buff = lang("おめでとう…合格だ。今日からお前は盗賊ギルドの一員となった。以後はギルドの規約を守り、ノルマをしっかりこなすように。", "Congratulations. You are a formal member of the Thieves Guild now. You can now use this stairs to enter the office. Always conform to the guild's rule and perform your tasks.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cell_movechara tc, 17, 17
				cdata(CDATA_X_ORG, tc) = 17
				cdata(CDATA_Y_ORG, tc) = 17
			}
			goto *chat_end
		}
		cell_movechara tc, 17, 17
		cdata(CDATA_X_ORG, tc) = 17
		cdata(CDATA_Y_ORG, tc) = 17
		if ( gdata(GDATA_FLAG_SUB_THIEF_1) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("ノルマを貰う", "Do you have any tasks for me?")
			listmax++
		}
		else {
			list(0, listmax) = 2
			listn(0, listmax) = lang("ノルマを報告する", "About my task.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("ようこそ盗賊ギルドへ、" + ranktitle(8) + "の" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "…どうぞ中にお入り下さい。", "Welcome to the Thieves Guild, " + ranktitle(8) + " " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "... Please come in.")
		gosub *chat_select
		if ( chatval == 1 ) {
			gdata(GDATA_FLAG_SUB_THIEF_1) = 1
			gdata(GDATA_FLAG_GUILD_THIEF_NORMA) = (10000 - gdata(GDATA_RANK_GUILD)) * 6 + 1000
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("良い心がけです。あなたにランク昇格のためのノルマを与えましょう。他人の所有物を奪い取り、盗品を売り払いなさい。金貨" + gdata(GDATA_FLAG_GUILD_THIEF_NORMA) + "枚相当の盗品を売ったら、昇進のための審査会に報告してあげましょう。", "Alright. The Thieves Guild wants you to put stolen goods into circulation. Steal some goods and sell them to any vendors. When the sum of your earning reachs " + gdata(GDATA_FLAG_GUILD_THIEF_NORMA) + " gold pieces, we will think about your promotion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			if ( gdata(GDATA_FLAG_GUILD_THIEF_NORMA) > 0 ) {
				listmax = 0
				buff = lang("どうしました？まだノルマが残っていますよ。", "You have your task to complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			gdata(GDATA_FLAG_SUB_THIEF_1) = 0
			flt 51 - gdata(GDATA_RANK_GUILD) / 200
			flttypemajor = FILTER_FURNITURE
			itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000 - gdata(GDATA_RANK_GUILD) + 1000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(3 - gdata(GDATA_RANK_GUILD) / 3000, 1, 3)
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			modrank 8, 500, 8
			listmax = 0
			buff = lang("確かにノルマの達成を確認しました。早速審査会を開いてあなたの功績を報告しましょう。", "You've done very well. I will give a report of your achievement to the council.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 297 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_FIGHTER) == 0 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("戦士ギルドとは？", "Tell me about the Fighters Guild.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("戦士ギルドに加入したい", "I want to join the Fighters Guild.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("止まれ…この階段の先は戦士ギルドの管轄だ。ギルドメンバー以外の者が入ることはできない…", "Halt...the stairs lead to the Fighters Guild office. Only the members of the guild are allowed to enter.")
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("戦士ギルドは、各地の戦士を管轄する組合だ。組合員には規律の厳守と一定のノルマを求める代わりに、援助を施し特定の仕事を斡旋している。戦士ギルドのメンバーには、能力復元・装備解呪・鑑定・武具強化の価格の割引、戦士スキルの習得と訓練などの特典がある。ギルドに加入するには、厳しい審査を通らなければならない。", "The Fighters Guild is made up by experienced and confirmed experts in their field of the fighting. We offer a variety of services such as reduced prices of identification and restoring, and training related skills. But joining the Fighters Guild requires you to pass a difficult test that most common fighters will fail.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_SUB_GUILD_FIGHTER) == 0 ) {
					listmax = 0
					buff = lang("戦士ギルドに加入したいというのか…我々が求めているのは、どんな強敵にも怯まず、与えられた指令を確実にこなす真のハンターだ。お前が信頼に足る者かどうか試させてもらおう。", "To join the Fighters Guild, you must first face a test to earn our trust. We only need skilled hunters who will obey our law and remain undaunted even in a desperate battle.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_GUILD_FIGHTER) = 1
					snd SOUNDLIST_WRITE1
					txtef COLOR_GREEN
					txt lang("ジャーナルが更新された。", "Your journal has been updated.")
					repeat
						flt 10
						characreate MAX_CHARA_NC, CREATURE_ID_BUG, -3, 0
						if ( refchara(cdata(CDATA_ID, MAX_CHARA_NC), DBSPEC_CHARA_RARE) < 70 ) {
							continue
						}
						if ( cdata(CDATA_RELATION, MAX_CHARA_NC) != RELATION_ENEMY ) {
							continue
						}
						if ( cdata(CDATA_QUALITY, MAX_CHARA_NC) >= FIX_QUALITY_MIRACLE ) {
							continue
						}
						if ( cdata(CDATA_LEVEL, MAX_CHARA_NC) > cdata(CDATA_LEVEL, CHARA_PLAYER) * 2 ) {
							continue
						}
						break
					loop
					gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) = 5
					gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF) = cdata(CDATA_ID, MAX_CHARA_NC)
					chara_vanquish MAX_CHARA_NC
					listmax = 0
					buff = lang("" + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + "を" + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + "匹討伐するのが、お前に課せられた試験だ。ノルマを達成したら、私に報告したまえ。", "The task given to you is this. Find and slay " + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + " " + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + ". When you complete this, talk to me again")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				if ( gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) > 0 & 0 == 0 ) {
					listmax = 0
					buff = lang("どうした？試験の内容は伝えたはずだ。" + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + "をあと" + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + "匹討伐してからまた来なさい。", "You still need to slay " + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + " more " + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + ".")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				listmax = 0
				buff = lang("ほう…どうやらノルマを見事に達成したようだな。", "Quite impressive...you have accomplished the task.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_RANK_GUILD) = 10000
				gdata(GDATA_FLAG_GUILD_THIEF) = 0
				gdata(GDATA_FLAG_GUILD_FIGHTER) = 1
				gdata(GDATA_FLAG_GUILD_MAGE) = 0
				gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 0
				gdata(GDATA_FLAG_GUILD_THIEF_NORMA) = 0
				gdata(GDATA_FLAG_SUB_GUILD_MAGE) = 0
				gdata(GDATA_FLAG_SUB_GUILD_THIEF) = 0
				gdata(GDATA_FLAG_SUB_GUILD_FIGHTER) = 1000
				gdata(GDATA_FLAG_SUB_FIGHTER_1) = 0
				gdata(GDATA_FLAG_SUB_MAGE_1) = 0
				gdata(GDATA_FLAG_SUB_THIEF_1) = 0
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				txtmore
				txtef COLOR_YELLOW
				txt lang("戦士ギルドに加入した！", "You have joined the Fighters Guild!")
				listmax = 0
				buff = lang("おめでとう…合格だ。今日からお前は戦士ギルドの一員となった。以後はギルドの規約を守り、ノルマをしっかりこなすように。", "Congratulations. You are a formal member of the Fighters Guild now. You can now use this stairs to enter the office. Always conform to the guild's rule and perform your tasks.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cell_movechara tc, 29, 2
				cdata(CDATA_X_ORG, tc) = 29
				cdata(CDATA_Y_ORG, tc) = 2
			}
			else {
			}
			goto *chat_end
		}
		cell_movechara tc, 29, 2
		cdata(CDATA_X_ORG, tc) = 29
		cdata(CDATA_Y_ORG, tc) = 2
		if ( gdata(GDATA_FLAG_SUB_FIGHTER_1) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("ノルマを貰う", "Do you have any tasks for me?")
			listmax++
		}
		else {
			list(0, listmax) = 2
			listn(0, listmax) = lang("ノルマを報告する", "About my task.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("ようこそ戦士ギルドへ、" + ranktitle(8) + "の" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "…どうぞ中にお入り下さい。", "Welcome to the Fighters Guild, " + ranktitle(8) + " " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "... Please come in.")
		gosub *chat_select
		if ( chatval == 1 ) {
			repeat
				flt cdata(CDATA_LEVEL, CHARA_PLAYER) + 10
				characreate MAX_CHARA_NC, CREATURE_ID_BUG, -3, 0
				if ( refchara(cdata(CDATA_ID, MAX_CHARA_NC), DBSPEC_CHARA_RARE) < 70 ) {
					continue
				}
				if ( cdata(CDATA_RELATION, MAX_CHARA_NC) != RELATION_ENEMY ) {
					continue
				}
				if ( cdata(CDATA_QUALITY, MAX_CHARA_NC) >= FIX_QUALITY_MIRACLE ) {
					continue
				}
				if ( cdata(CDATA_LEVEL, MAX_CHARA_NC) > cdata(CDATA_LEVEL, CHARA_PLAYER) * 2 ) {
					continue
				}
				break
			loop
			gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) = 3
			gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF) = cdata(CDATA_ID, MAX_CHARA_NC)
			chara_vanquish MAX_CHARA_NC
			gdata(GDATA_FLAG_SUB_FIGHTER_1) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("良い心がけです。あなたにランク昇格のためのノルマを与えましょう。" + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + "をあと" + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + "匹討伐してからまた来なさい。昇進のための審査会に報告してあげましょう。", "There's a task for you. Go find and slay " + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + " " + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + ". Then we will think about your promotion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			if ( gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) > 0 & 0 == 0 ) {
				listmax = 0
				buff = lang("どうしました？まだノルマが残っていますよ。", "You have your task to complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			gdata(GDATA_FLAG_SUB_FIGHTER_1) = 0
			flt 51 - gdata(GDATA_RANK_GUILD) / 200, calcfixlv(FIX_QUALITY_GREAT)
			flttypemajor = FILTER_WEAPON
			itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000 - gdata(GDATA_RANK_GUILD) + 1000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(4 - gdata(GDATA_RANK_GUILD) / 2500, 1, 4)
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			modrank 8, 500, 8
			listmax = 0
			buff = lang("確かにノルマの達成を確認しました。早速審査会を開いてあなたの功績を報告しましょう。", "You've done very well. I will give a report of your achievement to the council.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 301 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MINOTAUR) >= 1000 ) {
			listmax = 0
			buff = lang("おお、おぬしか。先の依頼の件、恩に着るぞ。どうだ、わしの部下になる気はないか？", "Ahh it's you. I owe you for what you've done for me. What do you say? Why don't you join my unit?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MINOTAUR) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("まかせて", "Leave it to me!")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang(strbye, "No way.")
			listmax++
			buff = lang("困った…困ったぞ。そこのおぬし、良いところに来た。実はヨウィンがミノタウロスの軍による侵攻の危機にさらされておるのだ。ヨウィンの村長とは親しくしておって、一軍を討伐に向かわせたいのだが、今は何やら不穏な空気がパルミアを包んでいる。城から離れるわけにはいかないのだ。そこで、冒険者のおぬしに依頼したい。ミノタウロスを退治してきてはくれぬか？報酬はもちろん用意しよう。", "What do I do about this... You, over there... You couldn't have gotten here a moment too soon. Yowyn is in great danger of being destroyed by the minotaur legion. The mayor of Yowyn is a close friend of mine and I don't want to leave him out in the cold. I want to send a detachment to reinforce him but I can't. The current political climate in Palmia is too volatile and the troops must remain in the castle. That's why I'm asking an adventurer like you. Will you attack the minotaur's camp for me? I will reward you handsomely if you do this.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("やはり、おぬしには無理か…", "I knew you'd refuse... and after all, why wouldn't you?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("なに、真か！では早々にミノタウロスの巣窟に向かい、首領を退治して来てもらいたい。場所は確かヨウィンの南と聞いておる。頼むぞ！", "What? You will? Great... Head to the Minotaur's Nest south of Yowyn and take the head of the minotaurs' king. Godspeed, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ".")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_MINOTAUR) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MINOTAUR) == 1 ) {
			listmax = 0
			buff = lang("どうした？おぬしが早くせんと、ヨウィンが危ないのだ。頼むぞ。", "What's wrong? You must make haste. Yowyn is in grave danger.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MINOTAUR) == 2 ) {
			listmax = 0
			buff = lang("おぬしか、待っておったぞ。報告はいい。すでに、ミノタウロスの王を倒した英雄のことは、この耳に伝わっておるわい。わしの感謝の印だ、受け取ってくれ。", "You've returned. I've been waiting for you. I've already heard the good word about the hero who crushed the minotaur king. Take this as a sign of my eternal gratitude.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_ADAMANTIUM
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 50000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MINOTAUR) = 1000
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 302 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) <= 10 ) {
			listmax = 0
			buff = lang("チッ、一文無しの乞食" + _ka() + "とっとといっちまい" + _na(), "Bah, a begger without a penny. What a waste of time! Just go away!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("返り討ちにする", "Try me.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("降参する", "I surrender.")
		listmax++
		buff = lang("おまえさん、ついてない" + _na() + _ore(3) + "達は泣く子も黙る冷血な盗賊団、その名も" + gdatan(GDATAN_TEAM2) + _da() + "命が惜しければ、おとなしく荷車の積荷と金貨" + cdata(CDATA_GOLD, CHARA_PLAYER) / 5 + "枚を渡すがいい" + _yo(), "Halt, halt, traveler. You're a quite fortunate one. Before you is the renowned band of legendary brigands \"" + gdatan(GDATAN_TEAM2) + "\" that the mere mention of its name is enough to silence a naughty child. Yet we will spare your life for only a toll of " + cdata(CDATA_GOLD, CHARA_PLAYER) / 5 + " gold pices and your cargos. Quite fortunate indeed.")
		chatesc = 0
		gosub *chat_select
		if ( chatval == 0 ) {
			snd SOUNDLIST_PAYGOLD1
			cdata(CDATA_GOLD, CHARA_PLAYER) -= cdata(CDATA_GOLD, CHARA_PLAYER) / 5
			inv_getheader 0
			repeat invrange, invhead
				if ( inv(INV_ITEM_NUM, cnt) > 0 ) {
					if ( refitem(inv(INV_ITEM_ID, cnt), DBSPEC_CARGO) == 1 ) {
						txtmore
						txt lang(itemname(cnt) + "を渡した。", "You hand over " + itemname(cnt) + ".")
						inv(INV_ITEM_NUM, cnt) = 0
					}
				}
			loop
			gosub *calcBurdenPc
			listmax = 0
			buff = lang("なかなか賢明な判断" + _da(), "A wise choice.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			levelexitby = MAP_EXIT_NORMAL
			chatteleport = 1
			snd SOUNDLIST_EXITMAP1
			goto *chat_end
		}
		listmax = 0
		buff = lang("いい度胸" + _da() + "…しかし、賢い選択とは言えない" + _na() + "ここがおまえさんの墓場" + _da(2), "You've got some guts. But your decision sure ain't a wise one. This will be your grave, kid.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		gdata(GDATA_QUEST) = QUEST_TYPE_THIEVES, 1, encounterref, 1
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 306 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_LULWY & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_LULWY) == 0 ) {
			listmax = 0
			buff = lang("こうして間近で見るとますます手放したくなくなってくるわね。ホラ、オマエにはこれがお似合いだわ。いいと言うまで外しちゃだめよ。", "You are mine. This collar suits you nicely.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_TEMPEST_CHOKER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_LULWY) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("なかなか面白そうなところじゃない。飽きるまではここにいてあげるわ。", "I place quite interesting. Until I get bored, I'll be here.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("アハハ！そうね、気が向いたら行ってあげるわよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 306
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 322 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_DADDY) == 0 ) {
			listmax = 0
			buff = lang("よく聞きなさい。もしあなたがいつかリトルシスターに出会ったら、あの子達に救いの手を差し伸べてあげて。見た目こそ化け物のように映るかもしれないけど、彼女たちがまた元の可愛らしい笑顔を取り戻せるように、私は研究を続けている。だから、お願い。この道具を使ってリトルたちを私の元に運んで。あなたへのお礼は、いつか必ず。", "Please, hear me out. If you should happen to run into any little sisters during your travels, please help them out. They seem to be monsters at first glance, but I'm doing important research that will return these girls to their gentle smiling nature. Please I beg of you... use these balls to bring the little sisters to me. I'd be very grateful, perhaps I'd even reward you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_DADDY) = 0
			flt
			itemcreate -1, ITEM_ID_LITTLE_BALL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_DADDY) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DADDY) > 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("約束のお礼は？", "What about the reward you promised me?")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("捕獲玉を補充したい", "I need to replenish my supply of little balls.")
			listmax++
			if ( findally(CREATURE_ID_LITTLE_SISTER) != (-1) ) {
				rc = findally(CREATURE_ID_LITTLE_SISTER)
				list(0, listmax) = 3
				listn(0, listmax) = lang(cdatan(CDATAN_NAME, rc) + "を引き渡す", "(Turn over the little sister.)")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("リトルたちを、終わりのない苦痛から解放するべきだという人もいる。でも、大抵の人間はあの子達の力が欲しくて殺すのよ。そう、確かにリトルの肉は人の肉体を進化させる。それでも、私はあの子たちを救う別の道があることを信じているの。…そして覚えておいて。もしあなたがリトルの命を奪うようなことがあれば、いつかその酬いを受ける時がくるから。", "Many people feel we need to free these girls from their never-ending oppression, but there are also scumbags that would murder them for their own personal gain. That's right; they say eating the girls' flesh will evolve the human body. I know there must be a better way to help them... don't forget that. Remember that if you take their lives, you will eventually get what's coming to you. ")
			gosub *chat_select
			if ( chatval == 1 ) {
				p = gdata(GDATA_SISTER_KILLED)
				repeat gdata(GDATA_SISTER_REWARD) + 1, 1
					p += limit(cnt, 1, 10)
				loop
				if ( gdata(GDATA_SISTER_SAVED) >= p | 0 ) {
					listmax = 0
					buff = lang("報酬、そうね。リトルたちが、あなたのために街で落し物を集めてきたのよ。好きなものを選んでちょうだい。あなたが貴重なものを無くした時のために、保留しておいてもいいわ。", "That's right...your reward. The little sisters have been picking things up for you that people have lost in town. Please, take whatever you like. If you like, you can wait until you've lost something valuable.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					begintempinv
					mode = MODELIST_SHOP
					flt
					itemcreate -1, ITEM_ID_SUITCASE, -1, -1, 0
					flt
					itemcreate -1, ITEM_ID_WALLET, -1, -1, 0
					if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) >= 1000 ) {
						flt
						itemcreate -1, ITEM_ID_GENE_MACHINE, -1, -1, 0
					}
					repeat 1499
						if ( cnt == 672 ) {
							continue
						}
						randomize gdata(GDATA_DAY) + cnt
						f = 0
						if ( itemmemory(0, cnt) ) {
							f = 1
						}
						if ( cnt == 931 ) {
							if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) >= 6 ) {
								f = 1
							}
						}
						if ( cnt == 662 ) {
							if ( gdata(GDATA_FLAG_SUB_KAMIKAZE) >= 1000 ) {
								f = 1
							}
						}
						if ( cnt == 655 ) {
							if ( gdata(GDATA_FLAG_SUB_VEIN) >= 1000 ) {
								f = 1
							}
						}
						if ( cnt == 639 ) {
							if ( gdata(GDATA_FLAG_SUB_MOTHER) >= 1000 ) {
								f = 1
							}
						}
						if ( f ) {
							flt cdata(CDATA_LEVEL, CHARA_PLAYER) * 3 / 2, calcfixlv(FIX_QUALITY_GREAT)
							itemcreate -1, cnt, -1, -1, 0
							if ( stat == 1 ) {
								if ( inv(INV_ITEM_QUALITY, ci) < FIX_QUALITY_MIRACLE ) {
									inv(INV_ITEM_NUM, ci) = 0
								}
							}
						}
						randomize
					loop
					txt lang("落し物をひとつ貰える。", "You can choose 1 item.")
					invsubroutine = 1
					invctrl = INVCTRL_INV_FILE_GET, 4
					gosub *com_inventory
					exittempinv
					mode = MODELIST_MAIN
					listmax = 0
					buff = lang("お目当てのものはあったかしら？", "Did you find what you were looking for?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				else {
					listmax = 0
					buff = lang("あなたにまだその資格はない。リトルたちを連れてきて。報酬はそのあと考えるわ。", "You haven't brought me enough sisters yet. Bring me a few more and then we'll discuss your reward.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("さあ、これを持っていきなさい。重いから注意して。", "Well then, take this with you. Be careful though, it's heavy.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_LITTLE_BALL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				goto *chat_end
			}
			if ( chatval == 3 ) {
				if ( cdata(CDATA_PRAY, rc) == 1 ) {
					snd SOUNDLIST_FAIL1
					txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
					goto *chat_end
				}
				if ( cdata(CDATA_IN_TAGTEAM, rc) == (-1) ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
					cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end rc
				}
				if ( cdata(CDATA_IN_TAGTEAM, rc) == 1 ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
					cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end rc
				}
				txt lang("リトルシスターを謎の科学者に手渡した。", "You turn over the little sister.")
				gdata(GDATA_SISTER_SAVED)++
				txtef COLOR_GREEN
				txt lang("リトルシスター殺害" + gdata(GDATA_SISTER_KILLED) + "回、救出" + gdata(GDATA_SISTER_SAVED) + "回。", "You have saved little sister " + gdata(GDATA_SISTER_SAVED) + " times and killed " + gdata(GDATA_SISTER_KILLED) + " times.")
				chara_vanquish findally(CREATURE_ID_LITTLE_SISTER)
				snd SOUNDLIST_COMPLETE1
				listmax = 0
				buff = lang("ありがとう。あなたの行いに感謝するわ。あなたへのお礼は近いうちに必ず。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 331 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_EHEKATL & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_EHEKATL) == 0 ) {
			listmax = 0
			buff = lang("うみゃー♪ころころ♪ころころ♪…うみゅ？これほしいの？ほしいの？みゅーん…君のこと好きだからあげるぅ！だいじにしてね！してね！", "Mew? Are you by any chance want this ball? Want? Meow...okay, I give this ball because I like you!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_WHEEL_OF_FORTUNE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_EHEKATL) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("前のエヘカトルも大変だったみたいなの！あ…。前のエヘカトルなんていないよ！よ！今の聞かなかったことにしてね？してね？", "There's no God inside!There's no God inside!")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("イルヴァの楽園…？じゃあたらばがに！たらばがにあるよね！！行く！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 331
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 338 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_OPATOS & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_OPATOS) == 0 ) {
			listmax = 0
			buff = lang("フッハハハハハー！！！これでもつけて脚力を鍛えろォ！", "Muwahahahaha! Train the leg strength to wear it!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_FETTER_OF_EARTH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_OPATOS) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("フハハハ！ファハハハハハー！冒険者も体が資本だろう！いっしょにトレーニングだ！フハハハッハァー！", "Mwahaha haha! Body would be an adventure capital! I'm going to train together!")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("フワッハハハ！面白い！待っていろ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 338
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 339 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_KUMIROMI & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_KUMIROMI) == 0 ) {
			listmax = 0
			buff = lang("(正気を失ったような目であなたを見つめ微笑んでいる)", "(He is smiling stare you in the eye, such as lost sanity)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ああ…もう君がどこにも行かないよう…これで縛りつけてしまいたい…。でもきっと君なら…そんなことしなくても僕を置いていかないよね…？", "Oh .... I want tie up you so that you do not go anywhere ....But, you will not going to leave behind me?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_GREEN_GOD_THORN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_KUMIROMI) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("僕の大切な信者を奪うのなら…誰だって許さない…。…どんな手を使ってでも…取り戻してみせる…。", "If someone take away my precious followers, I will not forgive...and I will sure to get back their....")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("いいよ…。でも…どうなっても知らないから…ね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 339
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 342 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_MANI & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_MANI) == 0 ) {
			listmax = 0
			buff = lang("これは装甲板ではない、拘束具だ。いつか己の力を制御できなくなったときはこれを身に着け抑え込むがいい。", "This is not a plate armor, it's restraint. Wear this harness is good when you can no longer control your power someday.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_MECHANICAL_BINDER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_MANI) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("どうしてルルウィは私をあんなに毛嫌いしているのだ？理解不能だ。", "Why would Luluwy dislike me so much? It is incomprehensible.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("いいだろう。定命の生活を確認するのも悪くない。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 342
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 345 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_KANEDA) >= 3 ) {
			buff = lang("どうだい、俺に乗ってみたいかい？", "So, you wanted to ride me right?")
			list(0, listmax) = 1
			listn(0, listmax) = lang("乗りたい", "Yes!")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("別に乗りたくない", "Not really.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				f = get_freeally()
				if ( f == 0 ) {
					listmax = 0
					buff = lang("おっと、これ以上仲間を連れて行けないようだぜ。", "Oops, it seems your party is already full.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				listmax = 0
				buff = lang("そうこなくっちゃ！", "That's the spirit!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				rc = tc
				gosub *add_ally
				gdata(GDATA_FLAG_SUB_KANEDA) = 0
				goto *chat_end
			}
			listmax = 0
			buff = lang("まあ、いいさ。", "If you say so.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		inv_find ITEM_ID_BLUE_CAPSULE_DRAG, CHARA_PLAYER
		if ( stat != (-1) ) {
			buff = lang("それはカプセル・ドラッグ！…なあ、それ譲ってくれよ。", "That-that's a capsule drag! ...hey, you want to give it to me?")
			list(0, listmax) = 1
			listn(0, listmax) = lang("譲る", "Take it.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("断る", "Nope.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				inv_find ITEM_ID_BLUE_CAPSULE_DRAG, CHARA_PLAYER
				inv(INV_ITEM_NUM, stat)--
				txt lang("カプセルドラッグを1錠渡した。", "You hand him a capsule drag.")
				snd SOUNDLIST_EQUIP1
				listmax = 0
				buff = lang("いいねえ。", "Great, you are great.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_KANEDA)++
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("気安く話しかけんな。", "Stay away from me, will ya?")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 348 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) != AREA_NOYEL | adata(ADATA_FESTIVAL, gdata(GDATA_AREA)) == 0 ) {
			goto *chat_unique_SWEND1
		}
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_JURE ) {
			listmax = 0
			buff = lang("ああ、あなた既に信者さんでしょ？悪いけど、新規契約しか受け付けてないからさ。", "Ah, you already believe in Jure right? Sorry bro, but we are only looking for new contracts.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("ジュア教に改宗する", "I will become a faithful servant of Jure.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("興味ない", "Not interested.")
		listmax++
		buff = lang("ジュア教団では、ただ今他の神からの乗り換えキャンペーン実施中だ！聖夜祭の期間中なら、改宗によるペナルティは払わなくていいよ。さらになんと、今なら無料で数量限定のジュア様の抱き枕も付けちゃうよ！お客さん、このチャンスを見逃す手はないよ。", "We are running a special campagin to help you join our religion! Be a brother of Jure today and win free body pillow of Jure! During the festival, you won't have to pay penalty to convert your religion neither. So what are you waiting? Sign up now!")
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("はい", "Yes.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("いいえ", "No.")
			listmax++
			buff = lang("よし、本当に改宗していいね？", "Okie, just making sure. Do you really want to convert your religion?")
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("あざーっす！やっと今日のノルマが達成できたよ。", "Thanks, bro! I kiraojust filled my daily quota.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				flt
				itemcreate -1, ITEM_ID_JURES_BODY_PILLOW, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				cdata(CDATA_GOD, CHARA_PLAYER) = GOD_JURE
				gosub *change_god
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("ちっ！", "Shit!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 351 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) < 1000 ) {
			listmax = 0
			buff = lang("昔…シルヴィアは色事師に連れ去られたことがあってな。監禁されていたところを助け出しはしたものの、彼女は精神を完全に病んでしまっていた。", "Silvia is such a long time ago ... it may have been taken to master gallantry. Although the rescue was the place that had been in captivity, she had gone completely mental suffering.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そして長い遠征から俺が帰ってきたときには、彼女は町で乱交を繰り返すようになっていたのだ…。", "When I came home from a long road trip, she had started to repeat promiscuity in the town ....")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("俺は、それでもシルヴィアを…。", "I still, ...still love Silvia.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) == 1000 ) {
			listmax = 0
			buff = lang("最近、シルヴィアの錯乱ぶりがますます酷くなっている。いったい何が原因なのだ…？", "Recently, confusion of Silvia has become more and more terrible. What is a cause that?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 352 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) == 1000 ) {
			listmax = 0
			buff = lang("ああ…エウリュピデス、私を置いてどこに行ってたの！体が疼いてしょうがないわ。もう、我慢できないのよ！", "Oh..Euryupids, Where have you been put me! I can not be helped body was aching. Anymore, I can not stand!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あなたと合体したい…。", "I want to unite with ... you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめて！", "Stop!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gosub *sexsex
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) == 0 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("仰せのままに", "Sure")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("私ではダメですか", "How bad is me?")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("お気を確かに", "Keep composed!")
			listmax++
			buff = lang("もうこの辺の男どもじゃ満足できないの！見惚れるほどの容姿を持った男を私の前に連れてきなさい！", "I can not be satisfied with the man around here anymore! Bring before me a charming man! ")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("あんたなんか最初から期待してなかったわよ！", "I did not expect something from scratch you!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("…悪いけどタイプじゃないわ。", "I'm sorry to you, You're not my favorite.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("いい返事ね。じゃあすぐに連れてきなさい、私は待たされるのが嫌いなの！", "Great. Bring charming man soon. I hate to be kept waiting!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("もちろん", "How's this one?")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("えっ", "Forget it.")
			listmax++
			buff = lang("ちゃんと連れてきたんでしょうね？", "Which one? Who have you brought for me to inspect?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("えっ。", "Forget it?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			allyctrl = 1
			gosub *com_ally
			if ( stat == (-1) ) {
				listmax = 0
				buff = lang("何もたもたしてるのよ。使えないわね。", "What are you slow. You are useless.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			rc = stat
			if ( sdata(SKILL_ATTR_CHA, rc) < 100 ) {
				listmax = 0
				buff = lang("嫌よ、こんな不細工！", "I hate, this clumsy!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( cdata(CDATA_PRAY, rc) == 1 ) {
				snd SOUNDLIST_FAIL1
				txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
				goto *chat_end
			}
			listmax = 0
			buff = lang("なかなか分かってるじゃない！また頼んでやってもいいわよ。", "You know the favorite of me! I can do I'll ask again.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			modkarma 0, -2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) = 2
			chara_vanquish rc
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("連れてまいりました", "How's this one?")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("えっ", "Forget it.")
			listmax++
			buff = lang("エウリュピデスはまだなの？早く連れてきて！", "Bring Euryupids soon!")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("えっ。", "Forget it...?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			allyctrl = 1
			gosub *com_ally
			if ( stat == (-1) ) {
				listmax = 0
				buff = lang("早くしなさいよこの馬鹿！", "Please fast!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			rc = stat
			if ( sdata(SKILL_ATTR_CHA, rc) < 150 ) {
				listmax = 0
				buff = lang("会いたかったわ…ってエウリュピデスはこんな不細工じゃないわよ！", "Euryupids is not ugly like this!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( cdata(CDATA_PRAY, rc) == 1 ) {
				snd SOUNDLIST_FAIL1
				txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
				goto *chat_end
			}
			listmax = 0
			buff = lang("ふふ…エウリュピデス…愛してるわ…。", "Euryupids...I love you....")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			modkarma 0, -2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) = 3
			chara_vanquish rc
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) == 3 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("わたしです", "I am Euryupids")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("なにそれこわい", "Huh?")
			listmax++
			buff = lang("あん…エウリュピデス…エウリュピデスはどこ？私には貴方しかいないの…。", "Where...? Where is Euryupids...?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("えっ。", "Huh?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( sdata(SKILL_ATTR_CHA, CHARA_PLAYER) < 200 ) {
				listmax = 0
				buff = lang("愛してるわエウリュピデス…って誰よあんた！", "I love only you...who are you!?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("ふふ…エウリュピデス…いっしょに気持ち良くなりましょう…。", "So...Euryupids...Would be comfortable with me!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			modkarma 0, -4
			flt
			itemcreate -1, ITEM_ID_HAPPY_APPLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1000
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) = 1000
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gosub *sexsex
			goto *chat_end
		}
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 359 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_JURE & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_JURE) == 0 ) {
			listmax = 0
			buff = lang("ホラ！これ、いらないからあげるわよ。か、勘違いしないでよね？別にアンタに渡すためにわざわざ持ってきたわけじゃないんだから！", "Hey! I raise this Item because it does not have. D-do not misunderstand? Because I'm not necessarily brought it all the way to pass to you separately!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_SASUMATA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_JURE) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("(あなたが話しかけると、女神は顔を真っ赤にしてそっぽを向いた)", "(When you talk, the goddess was turned away by red in the face)")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("そ、そこまで言うなら行ってあげてもいいわよ？本当は予定があるんだけど、仕方がないわね！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 359
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 364 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_H_SISTER) >= 1000 ) {
			listmax = 0
			buff = lang("冒険もいいけれど…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、体には気をつけてね。", "Watch out for the body but also good adventure.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_H_SISTER) == 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("面倒だ", "None of my business.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "No problem.")
			listmax++
			buff = lang("た、助けて" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！最近地下室にＨな妹が住み着いて…あたし狙われてるみたいなの。お願い、退治してきて！", "Help! Rogue sister was settled in the basement aimed at me. Please, come to get rid of!")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("お姉ちゃんを見捨てるの！？は、はくじょうものぉーー！", "Are you forsake older sister!? You're a cold-hearted!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("ありがと。じゃあさっそく行ってきて！地下室はうちの裏からいけるわ。", "Thanks. Please come to kill the sister immediately then. You can go to basement from the back of my house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_H_SISTER) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			cell_featset 57, 10, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 3
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_H_SISTER) == 1 ) {
			listmax = 0
			buff = lang("早く！早くＨな妹を退治して！", "Soon! To kill the sister soon!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_H_SISTER) == 2 ) {
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 300
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_ETHER
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("どうしてあたしがヒットマンに狙われてたのかしら…。とにかくありがとね！これ、少ないけどご褒美だよ。", " I thank you anyway! Let me give you a little reward though.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_H_SISTER) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 365 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("ＨはＨでもhellだかheavenだかわからんがなぁーっ！", "It is really good! I was about to just want someone chop to pieces!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("好き", "No.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("嫌い", "Yes.")
		listmax++
		buff = lang("私を退治しにきた…？えっ？そんなにＨな妹が嫌いなの？", "Huh? You came to kill me ...?")
		chatesc = 0
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("そんな…ひどい…。", "Such a terrible ...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("はい", "Kill you.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("Yes", "Yes.")
			listmax++
			buff = lang("正直に言って！Ｈな妹は好き？", "Are you really going to fight with me?")
			chatesc = 0
			gosub *chat_select
		}
		listmax = 0
		buff = lang("本当！？嬉しい…", "Really? It is ...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("ＨはＨでもhitmanの方だがなぁーっ！", "It is really good! I was about to just want someone chop to pieces!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 366 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 450 & gdata(GDATA_FLAG_MAIN) < 470 ) {
			listmax = 0
			buff = lang("依頼の説明はガベラに任せている。あいつから聞いてくれ。用意した報酬は私が選んだものだからな。痺れるぞ？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 390 & gdata(GDATA_FLAG_MAIN) < 450 ) {
			listmax = 0
			buff = lang("水を割って決戦兵器が出てきたときは盛り上がったが、発進する度にあれでは手間がかかる。いつまた襲撃されるかもわからない以上、地上で待機させておくしかないだろう。本国の方にはさらに上位のモンスターが多数出現しているらしい。エウダーナやジューアと争う余裕はなさそうだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 290 & gdata(GDATA_FLAG_MAIN) < 330 ) {
			listmax = 0
			buff = lang("汚染・同化される可能性を考えると、やはり奴に生身で挑むのは得策ではない。イェルスード２号機の完成を急がせるか…。", "Given the possibility of pollution and are assimilated, the challenge to the target in the flesh is not a good idea. I must hasten the completion of the new weapon.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 240 & gdata(GDATA_FLAG_MAIN) < 290 ) {
			listmax = 0
			buff = lang("調査部隊は遺跡最深部へ向かっている。追いついて護衛に加わってくれ。私たちの部隊も後から行く予定だ。", "Survey troops are heading to the deepest part remains. Join to escort me to catch up. It also plans to go after our troops.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) > 210 & gdata(GDATA_FLAG_MAIN) < 220 ) {
			listmax = 0
			buff = lang("何？レミード遺跡に入りたいだと。…ふむ、腕はたちそうだな。調査部隊の護衛を引き受けてくれるのなら考えてやらんでもない。", "What? And I would want to enter the ruins Remido. Hmm ..., you're quite so strong. I will act accordingly if you undertake the escort of troops who underwriting investigation.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("酒場の隣のラボにガベラという白衣を着た男がいる。護衛をする気があるなら通行用のカードキーをそいつに作ってもらいたまえ。ただあいつは変わり者だ…上手く交渉するんだな。", "There is a man wearing a white robe that development room next to the bar.  If you undertake an escort, ask him to make a card key for the passage. However, there is a need to negotiate because he is a crazy guy.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 220
			goto *chat_end
		}
		listmax = 0
		buff = lang("見ろ、このパワーアームを。痺れるだろう？イェルスの科学力は世界一だ。", "Look, this power arms. Science developed in Yerles.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 367 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) >= 1000 ) {
			listmax = 0
			buff = lang("船酔いでダウンするなんてまだまだ俺様も修行が足りねぇな…。", "You have the money, now get lost.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) == 0 ) {
			listmax = 0
			buff = lang("お、" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "じゃねぇか。お前最近この辺で有名になってきてるな。", "Hey! " + cdatan(CDATAN_AKA, CHARA_PLAYER) + "! You have become famous in this area recently.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("その話乗った！", "Fine, let's do it.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("船はトラウマだ", "I do not have good memories of the ship.")
			listmax++
			buff = lang("そうだ、俺様と海賊狩りに行かねぇか？この辺の海には海賊がうじゃうじゃ出るってんで、船を借りて海賊のお宝強奪に行こうと思ってよ。船代は俺様が出すからお宝の取り分は俺様が7、お前が3だ。", "Would you like to go hunting pirates with me? The sea of pirates around here seems to haunt so well, I am going to go to snatch the treasure of pirate. I have already borrowed a ship.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("なんだ？前に乗ってた船が沈没でもしたのか？", "What? Did you also experience the sinking of the ship?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("じゃあ早速行くか！俺様とお前で海賊どもを蹴散らしてやろうぜ。", "Then I will go at once! I think you can easily get rid of the pirates would we.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) = 1
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_KURUALM
			gdata(GDATA_TELEPORT_LEVEL) = 3
			levelexitby = MAP_EXIT_TELEPORT
			evadd EVENT_BOSS_MUSIC
			gdata(GDATA_FLAG_BOSS_MUSIC) = 1
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) == 1 | gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("狩りに戻ろう", "Let's return to hunt pirates.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("大丈夫？", "Are you okay?")
			listmax++
			buff = lang("う、うっぷ…。船、揺れすぎだろ…。", "I feel the ship shaking is bad too.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("も、問題ない。でも、もう少し休んでいこうぜ…。", "P-Please let me rest a little more...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("あ…あ…？そ、そうだな…。", "Ah...T-That's right...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) = 2
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_KURUALM
			gdata(GDATA_TELEPORT_LEVEL) = 3
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 4
			mapstarty = 11
			evadd EVENT_BOSS_MUSIC
			gdata(GDATA_FLAG_BOSS_MUSIC) = 1
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) == 3 ) {
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("う…何、海賊のボス討ち取ってきたって？そりゃすげぇ…うっぷ。お前は船酔いしなかったのか？俺様はずっと甲板でダウンしてたぜ…。あ？宝？そんなもんどうでもいいから背中さすってくれよ…。", "Do you have been getting rid of pirate boss? It is incredible.... Are you did not get seasick? I had to limp on the deck for a long time. What? Treasure? Because such a thing not matter, please rub my back.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 369 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_LOST_KITTY) == 0 ) {
			listmax = 0
			buff = lang("近年、迷子になる子猫が急増しているのであります！もし迷子の子猫を見かけましたら、本官のところまで送り届けて欲しいであります！何も報酬は出せませんが…子猫たちとそのご家族のためにも、どうかよろしくお願いなのです！", "In recent years, the cat to get lost is increasing rapidly. If you see a stray cat, I would want you to deliver stray cat. I do not put out a reward, it is also somehow thank you for kittens and their families.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_LOST_KITTY) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_LOST_KITTY) == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("頭をなでる", "Pat on his head.")
			listmax++
			if ( findally(CREATURE_ID_STRAY_CAT) != (-1) ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("迷子の子猫を引き渡す", "(Turn over the stray kitten.)")
				listmax++
			}
			buff = lang("子猫のご両親も心配しておられます！早く見つけ出して安心させてあげたいでありますよ…。", "Parents of stray cat are concerned! I want to let them relieved to find out soon.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("わふっ？", "...?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				rc = findally(CREATURE_ID_STRAY_CAT)
				if ( cdata(CDATA_PRAY, rc) == 1 ) {
					snd SOUNDLIST_FAIL1
					txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
					goto *chat_end
				}
				if ( cdata(CDATA_IN_TAGTEAM, rc) == (-1) ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
					cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end rc
				}
				if ( cdata(CDATA_IN_TAGTEAM, rc) == 1 ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
					cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end rc
				}
				txt lang("迷子の子猫を犬のおまわりさんに引き渡した。「おうちかえれう？」", "You turn over the stray cat.")
				chara_vanquish findally(CREATURE_ID_STRAY_CAT)
				snd SOUNDLIST_COMPLETE1
				modkarma 0, 10
				p = sdata(19 + STARTING_SKILL_SPACT, CHARA_PLAYER) \ 1000
				if ( p < 300 ) {
					modgrowth CHARA_PLAYER, SKILL_ATTR_LUC, 80 + rnd(21)
				}
				else {
					skillexp SKILL_ATTR_LUC, CHARA_PLAYER, 1000
				}
				listmax = 0
				buff = lang("この子は…捜索願の出ていた子猫であります！本官が責任を持って親元に帰しましょう。ご協力感謝であります！他にも迷子は大勢いますので、見つけたらまた連れてきてください！", "This cat had been hoping to search! I will ascribed to parents. I thank you for your cooperation. So are numerous other lost, please bring also found!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 370 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) == 1000 ) {
			listmax = 0
			buff = lang("貴方のように優秀な戦士は嫌いではありません。今度わが軍の入隊試験を受けてみませんか？貴方と共に戦える日を楽しみにしています。", "Excellent warrior like you, I do not hate. Do not you want to take a test of our military enlistment time? I'm looking forward to the day that fight with you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) == 0 ) {
			listmax = 0
			buff = lang("自分はイェルス軍第７特殊部隊の隊長、ミーリスです！冒険者である貴方に我々の作戦に参加していただきたいのです。", "My name is Milis,the captain of the Yerles 7th Special Forces troops. I'd like to have you participate in our operations.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("了解です", "Fine, let's do it.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめておきます", "I'll pass.")
			listmax++
			buff = lang("現在、我々が調査している遺跡をエウダーナも狙っています。任務遂行のためにエウダーナの補給ルートを潰したいのですが、この近くに駐留しているエウダーナの部隊が邪魔なのです。そちらに割くだけの戦力はありません。敵部隊を攻撃して注意を惹きつけていただけないでしょうか？当然、報酬は支払います。", "Eulderna also aim the ruins that Yerles are investigating now. For mission, it is necessary to cut off the supply route of Eudana, but   Eulderna troops are stationed close to this town is in the way. There is no force of just taking there. Could you attract attention by attacking the enemy forces? Of course, we will pay a reward.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("残念です…もしお気持ちが変わりましたら、またおいでください。", "Please come and also your feelings, if Should you instead.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("引き受けていただいたこと、感謝します。準備ができたら、声をかけてください。部下が作戦領域まで案内します。", "I am grateful that you have accepted the request. Please talking to me if you are ready. My men will escort you to the mission area.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) == 1 | gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("問題ありません", "Yes.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("用事を思い出した", "I remembered an errand.")
			listmax++
			buff = lang("しばらくの間、彼らの注意を惹きつけておいてください。準備はよろしいですか？", "For a while, please keep attracting their attention. Are you ready?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("急いでくださいね。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("敵部隊の中には魔法で支配されたわが軍の兵士もいるようです。一思いに楽にしてあげてください。…生きて、またここで会いましょう。では…作戦開始！", "There are some of Yerles soldiers was ruled by magic in the enemy. Please give the settlement without hesitation. I'll see you in here again, alive. Then,...operation start!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) = 2
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_EIREL
			gdata(GDATA_TELEPORT_LEVEL) = 3
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 9
			mapstarty = 10
			evadd EVENT_BOSS_MUSIC
			gdata(GDATA_FLAG_BOSS_MUSIC) = 1
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) == 3 ) {
			flt
			itemcreate -1, ITEM_ID_STATUE_KUMIROMI, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 600
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			flt
			itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("貴方のおかげでこちらの作戦も無事完了しました…え？陽動するだけでなく殲滅まで？こちらで処理をするつもりでしたが手間が省けました。ではその分、報酬の上乗せをしておきますね。", "I have also successfully completed thanks to your strategy here. What? Do you went to destroy not only diversion? Post-processing effort was saved. That amount, plus keep the reward in.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 371 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) == 1000 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("がんばれ", "I want on behalf of the town management.")
			listmax++
			if ( mdata(MDATA_CITY_COMPLAINTS) == 0 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("管理代行するよ", "I want on behalf of the town management.")
				listmax++
			}
			if ( mdata(MDATA_CITY_COMPLAINTS) > 0 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("管理コマンド", "I want to run the town management.")
				listmax++
			}
			buff = lang("うああー。施設の点検めんどくせー。適当にやっとけばいいかー。", "Ah... Inspection of underground passage is troublesome. I'll do it to sloppy."), strbye
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("へへっ…。", "Hum...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				snd SOUNDLIST_COMPLETE1
				listmax = 0
				buff = lang("マジで！？いやぁ、助かるわー！じゃあ頼んだ！", "By all means, please!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				mdata(MDATA_CITY_COMPLAINTS) = limit(mdata(MDATA_CITY_WATER_POLLUTION) - 15 + mdata(MDATA_CITY_ETHER_CORRUPTION) - 15 + mdata(MDATA_CITY_TAXES), 1, 99)
				goto *chat_end
			}
			if ( chatval == 0 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("キャンセル", "Cancel")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("政策の実施", "Policy implementation")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("法律整備", "Law development")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("周辺施設の建設", "Construction of the facility")
				listmax++
				buff = lang("おっ。どういう管理をしてくれるんだ？", "What the management are you going to do ?")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("まだできないぜ。", "Sorry, this is undeveloped.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("未実装らしいな。", "Sorry, this is undeveloped.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("開発中だってさ！", "Sorry, this is undeveloped.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "No problem.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("面倒だ", "None of my business.")
			listmax++
			buff = lang("どどどどどうしよう！やっべえ、コレやっべえぞ…？ハッ…！冒険者か、ちょうどいいところに！実は防衛システムの電源切りっぱなしにしててさ…さっき見るまで地下通路がジューアの連中に占拠されてることに気付かなかったんだよ。みんなにバレる前にこっそりなんとかしてくれ！", "W-W-What I'm trying to... I was a bad idea. Oh!  You came at just the right time. Because I have to cut the power to leave the defense system, it did not notice that the underground passage is occupied by Juere guys actually just to make sure. Secretly solves this thing before everyone finds out!")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("断るのはしょうがないけど、今聞いたことはバラすなよ！", "Do not tell everyone that you have heard today!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("た、助かったァー！！地下通路への階段出しとくからすぐに行ってきてくれ！", "Thank God!! I keep out stairs to the underground passage. Please come visit soon!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			cell_featset 8, 21, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 3
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) == 1 ) {
			listmax = 0
			buff = lang("なんか地下がうるさい、とか言うやつ出てきちゃったよ！早く殲滅してきてくれ！", "People are saying what underground is noisy! Please come to destroy the intruder soon!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) == 2 ) {
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_ADAMANTIUM
			flt
			itemcreate -1, ITEM_ID_AWESOME_SWIMWEAR, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 500
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("うへぇ、ギリギリセーフだ。危ないところだった…。こいつはお礼さ、受け取ってくれ。なんか地下倉庫の隅で埃かぶってた欠陥装備だけど、そこそこ高性能なはずだ。多分。", "It was just ... dangerous. Please take this please. This equipment was defected in the corner of the basement. It'd be fairly sophisticated. Maybe.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 374 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_UNPAID_COMPENSATION) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("わかった", "Leave it to me.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("お断りします", "I refuse it.")
			listmax++
			buff = lang("おお…旅の方よ、私の愚痴に付き合ってはくれないか。この街は以前、大量発生したネズミによって大きな被害を受けていたのだ。そこでネズミの駆除依頼を受けたのが私だ。この笛による素晴らしい演奏で小汚いネズミどもを片づけたのだが…この街の連中ときたらその報酬を全く払わないのだ！ああ、話していたら腹が立ってきた。報復としてちょっとこの街の子供をさらってきてくれ。", "I get rid of dirty rats by my wonderful performance with the flute. But, the people of this city should not pay compensation to me at all. Oh, I've been frustrated by talking. Please come kidnap the children of this town as a little retaliation.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("そっかー。死ね！", "Go to hell!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("わかってくれたか！そうそう。さっきはこの街の子供と言ったが、もうこの際どこの街の子供でもかまわんよ。よろしく頼む。", "Did you know! Oh yeah. I just said the children of this town. It does not matter where in the children of the city, but in this case the other. I ask you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_UNPAID_COMPENSATION) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_UNPAID_COMPENSATION) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("聞き流す", "Yeah.")
			listmax++
			if ( findally(CREATURE_ID_TOWN_CHILD) != (-1) ) {
				rc = findally(CREATURE_ID_TOWN_CHILD)
				list(0, listmax) = 2
				listn(0, listmax) = lang(cdatan(CDATAN_NAME, rc) + "を引き渡す", "(Turn over " + cdatan(CDATAN_NAME, rc) + ".)")
				listmax++
			}
			buff = lang("どんどんさらってきてくれ。私を馬鹿にしたことを世間に後悔させてやる。", "On me getting kidnapped. I let the public regret that they have to fool me.")
			gosub *chat_select
			if ( chatval == 2 ) {
				if ( cdata(CDATA_PRAY, rc) == 1 ) {
					snd SOUNDLIST_FAIL1
					txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
					goto *chat_end
				}
				if ( cdata(CDATA_IN_TAGTEAM, rc) == (-1) ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
					cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end rc
				}
				if ( cdata(CDATA_IN_TAGTEAM, rc) == 1 ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, rc)
					cell_findspace cdata(CDATA_X, rc), cdata(CDATA_Y, rc), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end rc
				}
				listmax = 0
				buff = lang("いい子だ…さぁこっちへおいで。これから毎日素晴らしい演奏を聴かせてあげよう。…ん？まだいたのか、これは報酬だ。受け取っておきたまえ。", "Come over here ... Come on good boy. I'll have to listen to great music every day from now on. ... What? Are you still were there? This is a reward. I bestow you receive.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 300
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				chara_vanquish findally(CREATURE_ID_TOWN_CHILD)
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 373 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) >= 1000 ) {
			listmax = 0
			buff = lang("おかしいなぁ…全身にユニコーンの角突き刺したのに全然効果がないや。まだ足りないのかなぁ…？", "That's odd... I stabbed him with the unicorn horns all over but didn't work.  Just not enough of them...?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			inv_find ITEM_ID_UNICORN_HORN, CHARA_PLAYER
			if ( stat != (-1) ) {
				buff = lang("え？またユニコーンの角をもってきてくれたの？", "Huh?  You'll bring me more horns?")
				list(0, listmax) = 1
				listn(0, listmax) = lang("そうだよ", "Take it.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("ちがうよ", "Nope.")
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( chatval == 1 ) {
					inv_find ITEM_ID_UNICORN_HORN, CHARA_PLAYER
					inv(INV_ITEM_NUM, stat)--
					txt lang("ユニコーンの角を1本渡した。", "You hand him a unicornHorn.")
					snd SOUNDLIST_EQUIP1
					modkarma 0, 10
					listmax = 0
					buff = lang("ありがとう！今度こそ治るといいなぁ…。", "Thanks! This time we'll cure him...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				listmax = 0
				buff = lang("冒険者さんも狂気攻撃には気をつけてね。", "Mr. Adventurer should watch out for sanity attacks too.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("まかせて", "I'll get your puppy.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("うう…父さまが…父さまがおかしくなっちゃった…。ユニコーンの角とかいうの使えば治るって癒し手さんに教えてもらったけど、そんなの持ってないよぉ。お願い、冒険者さん。ユニコーンの角を持ってきて！", "Huu... Daddy.... Daddy's gotten all weird...A nurse taught me we could cure him if we had unicorn horns but I don't have any. Please Mr. Adventurer. Bring me some unicorn horns!")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("ちゃんとお礼はするもん。だからお願い！", "I'll reward you. So please!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("よかったぁ…でも父さまの狂気はすごくひどいんだ。念のため、いっぱい持ってきてほしいな。", "Thank goodness... But his insanity is really bad.  Just to be safe, I want you to bring a lot of them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) >= 1 & gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) <= 10 ) {
			inv_getheader 0
			f = 0
			repeat invrange, invhead
				if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) >= 10 ) {
					break
				}
				if ( inv(INV_ITEM_NUM, cnt) > 0 ) {
					if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_UNICORN_HORN ) {
						inv(INV_ITEM_NUM, cnt)--
						gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY)++
						f = 1
						txt itemname(cnt, 1) + "を納入した。"
						continue cnt
					}
				}
			loop
			if ( f ) {
				listmax = 0
				buff = lang("ユニコーンの角もってきてくれたの？ありがとー！", "You brought some unicorn horns? Thanks!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( f == 0 ) {
				listmax = 0
				buff = lang("もしユニコーンの角を見つけたら持ってきて。", "If you find any unicorn horns bring them to me.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gosub *calcBurdenPc
		}
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) >= 10 & gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) < 1000 ) {
			listmax = 0
			buff = lang("これだけあれば父さまもきっと良くなる…冒険者さんありがとう。後でさっそく父さまに使ってみるね。これ、お礼だよ！", "This should be enough to make daddy better... Thanks Mr. Adventurer.  I'll use them on him later. Here, it's your reward!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_HERO_CHEESE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 380 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("おお、ここまで辿り着いてしまったのか！ 悲しいかな、ここから先に行かせるわけにはいかないのだ。お前の冒険はレシマスから真実の書を持ち出したところで終わり、本来この地を訪れることは未来永劫なかったはず。", "Ah, you've finally arrived! How unfortunate, I cannot allow you to go any farther. Your adventure ended when you had taken the book of genuine truth from Lesimas. You were never to come to this land.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang(" (青年はやれやれといった様子で首を振った) 予測を外れた行動ばかりとるお前には、我々も最初は楽しませてもらったが…。お前はどうやらイレギュラーのようだ。異形の森の魔女…あれは森とともに燃えつきて死ぬはずだったのだが、お前と接触したことにより運命が変わってしまった。このままお前を放っておけば永遠の盟約にすら干渉しかねない。", " (The youth shakes his head dejectedly) You whom constantly surpassed my expectations brought me entertainment in the beginning...But you seem to be just an irregularity.  The witch of the irreverent forest... She was to be burned along with the forest itself but from coming in contact with you fate had been changed. If I do not deal with you now the eternal covenant will be all for naught.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("我々の王も大層不機嫌でいらっしゃる。ここでお前は混沌に呑まれ消えることになるだろう。", "Our lord is as well utmostly distraught. You shall be swallowed by chaos and vanish.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 381 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) > 1000 ) {
			listmax = 0
			buff = lang("大丈夫、私は正気に戻りました！", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			atama = rnd(7)
			if ( atama == 0 ) {
				listmax = 0
				buff = lang("なぜならばこれもひとえにポストが赤いからであり、宇宙から降り注ぐインフィニティどんどこ光線の影響によって私の体は隅々までこっぱみじんだって言ってるじゃないですか。困っちゃうなぁ、どうしてそこで鉄と鉄が違うものであるという当たり前のことに気づかないで、植木鉢のことを知っているなんて言い張れるんだ？しかし、ここで戦争について考えるのは素敵という意見もちらほら耳にしたり鼻にしたり軍隊アリ。どちらにしてもここではロマンチックだとは到底思えませんがもう終わったことです、仕方がないことなのです…。ですから今日も元気に！逆立ちパラダイスと洒落込もうじゃないかあ！", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( atama == 1 ) {
				listmax = 0
				buff = lang("それもそのはず。まぁ、さっきまでは花瓶がささっているように見えていましたが、しばらく見ていると布団がねじれているのです。あのルドルフも泣いていました。ルドルフの身に何があったのでしょうか？ルドルフって誰でしょうか？あれはまだ、ハリガネ派とシイタケ派の仲がよかった頃…ある日、頭を地面に埋葬することが決定し、そろそろトマトも芽を出すと思われた頃…その事件は起きました。爆散したサザンカが宙を舞い、赤・青・しじみがそろったことにより、カブトムシ界に破滅が巻き起こったのです。あのカブトムシ界に。ああ、もう誰にも止められない。もう髪をむしりつくすことさえできない…。全部うそでーす、めへー。", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( atama == 2 ) {
				listmax = 0
				buff = lang("ここで言うのもなんですが…私の大切なツチノコ第二形態が危篤なんです、髪をむしらせろ。なるほど…そうですか。時計には触れない、か…その気持ちわからなくもありませんが、ちぎれた触角を天井に植えるのはどうかと思います。確かにドライヤーならあっちで日光浴してますよ！さっき私が燃やしたらしいけどさ！です・が　きが２　たでしょうかあれは　わた４　かんしして１　のです。こんなこと…ただ虚しいだけなんです。そうです、もう一度考え直すのです。ここを逃したら、もう考えをまとめるチャンスなどありません！よく考えろ、そこまでする必要はどこにもないっ！あー。話は変わるけど、駐車場って男臭いですよね。杏仁豆腐を思い出します。", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( atama == 3 ) {
				listmax = 0
				buff = lang("ええ、波動は全てを飲み込みます。そして現れるキャベツ、ニンジン、ナス、羊飼いといった色とりどりの魚たち…このバトルの結末はどうなってしまうのでしょうか？私の予想では一昨日の空模様は豪雨だけど最終的にはやっぱり投石器かと思われれれれ！…ここで気になるのはやはりダンゴムシ派です。では今日はダンゴムシ料理を試食してみましょう。みたらしだんごです、どうぞ。えっ…それともまさか叫ばせる気ですか？何度でも叫ぶって言ってたじゃないか、うそつき！あなたも知っているでしょう！こんなこと誰にも言えないけれどここだけの話、扉と和解しなくちゃいけません。ごめんなさい、全てはクロロホルムとの調和のためなんです…わくわくするなぁ！", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( atama == 4 ) {
				listmax = 0
				buff = lang("前にも同じようなことがありました。…あれはまだ海が干上がっていたころの話ですが星々が永劫の彼方へ収束していくなか、二匹の黄金ミミズが灼熱の大気に身を躍らせていました。これでこの話はおしまいです。もうおしまいだぁ…やめにしませんか？こんなことはよくありません！因果律をすっとこどっこいさせるだけなんです！あ、でもそれもいいかもしれませんね。私はあなたを取り込もうとしただけですしね。さて…茹で上がる爆弾たちと「痛いじゃないですか」と連呼した後、空に向かってツバを吐く遊びはみなさんしたことがありますよね？しかしこの遊びの重大な問題が発見されたのです。本日はありがとうございました。", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( atama == 5 ) {
				listmax = 0
				buff = lang("有名な話ですが、解放点を最初に発見したコルチェーン＝トルバッソ男爵は覆面を何十枚もかぶっていたのが原因で呼吸困難に陥り死亡しました。享年57歳とされていますが…俺があのくらいで死んだと思ったのか？まぁそんなことは乾電池の眼球くらいにどうでもいいことです。今重要なのは本の144ページですからね。キーワードは宇宙イノシシの鳴き声だけど中身を気にしてはいけません。あれ、どこまで話しましたっけ？そうそう、崖から落ちていく最中、私は彼らと感動的な再会を果たしました。そして仲良く地面に叩きつけられ、私だけが生き残りました。私の勝ちです。やったぁ。", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( atama == 6 ) {
				listmax = 0
				buff = lang("私がこうなったのも、全ては一枚の蛹から始まった感動物語。呼吸をしなければ人間は生きてはいけない。だがそれは本当なのだろうか。世界を取り巻く何か大きなものにそう思い込まされているだけではないだろうか。悩みの末に、入賞できたはずなのに結局回り道をしてサボテンになってしまった物語。あれは割と高価だったのに！人間はなんと愚かなのだろう…。あのルドルフも最後には「おいおい、どうしてみんな殺人ミミズの皮膚をかぶっているのかしら」と言った。そして死んだ。娘も言った。そして死んだ。お前は？言った？何を？騙されているぞ、早く反論するんだ！そうとも！ここには、なにもいないんだ…。", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) < 1000 ) {
			listmax = 0
			buff = lang("実は今まで秘密にしていたのですが、塩コショウってなんなんだよまったくもうふざけるのもいい加減にしろよ！そもそもこの場合ポイントとなるのは３箇所、まず世界のため私のため昼寝をすることはナンセンスだという気持ちでしょう？素人はそんな不毛な争いの果てに一体何が残るって言うんだと思うかもしれませんが、それとこれとは別の事象を表していますよね。何を隠そう、事象は私の大好物なんですよ！それでも不満があるのなら天上天下唯我独尊の精神のもとに解脱や世界統一に向けて、負けるな！闘え！武器を捨てて投降しろ！貴様らは完全に包囲されているが、それでもウサギってかわいいぴょんぴょん♪", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) == 1000 ) {
			listmax = 0
			buff = lang("(なぜかはわからないが、全身に大怪我をしている…)", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("何かがカインの手から落ちた。", "You hand him a capsule drag.")
			snd SOUNDLIST_EQUIP1
			flt
			itemcreate -1, ITEM_ID_INSANE_BOOK, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) = 1001
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 382 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 400 ) {
			listmax = 0
			buff = lang("イルヴァ中の国家がまとまったのは単に脅威からそれぞれの身を守るためだ。国々が真に和解する日まで、僕たちの戦いは続くんだ。", "Yerles and Juere might want to be reconciled.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) < 400 ) {
			listmax = 0
			buff = lang("イェルスとジューアはそろそろ和解したほうがいいと思うんだ。", "Yerles and Juere might want to be reconciled.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 383 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 400 ) {
			listmax = 0
			buff = lang("ブースター使ったアンドロイドさんにも付いていけるよう、速度を鍛えなきゃ。足を引っ張るのは嫌だから。", "Why does race or sex relate to love?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) < 400 ) {
			listmax = 0
			buff = lang("愛に性別も種族も関係ないよね？", "Why does race or sex relate to love?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 388 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) >= 1003 ) {
			listmax = 0
			buff = lang("圧倒的な戦闘能力を持つコアラとの戦い…今回は厳しい生存競争だった。コアラの残党…きのこたちとの因縁…里の復興…問題はまだまだ山積みだ。だが今は、とりあえず束の間の平和を享受するとしよう…。", "Well...actually...I found this inn more comfortable than my house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 1002 ) {
			listmax = 0
			buff = lang("あの数のコアラを撃退するとは、さすがだ！誇りに思うべきだよ。お前はこの地に満ちる光と、難民たちを守ったんだ。そして絶望の闇に負けない強さを我々に示してくれた。これはお礼だ…どうか受け取ってほしい。", "This fear turned into reality soon enough for she cried out as she woke up.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_SCROLL_GAIN_ATTRIBUTE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1500
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10
			flt
			itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			listmax = 0
			buff = lang("なに、遠慮はしなくていい。どうせ全部すぎのこの村の跡地から漁ってきたものだからな。", "Well...actually...I found this inn more comfortable than my house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) = 1003
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 1001 ) {
			listmax = 0
			buff = lang("きのこたちとは違い、コアラには言葉すらも届かない。やつらは暗黒の炎によって土地を焼き払い、暗黒焦土を作り出す。焦土に蒔かれた暗黒種子は周囲の光を塗りつぶし暗黒樹へと成長する。そうして闇に包まれた地域は通常の動植物の生育ができない暗黒の大地と化すのだ。暗黒樹がつける果実や葉は豊富な暗黒物質を含み、闇喰コアラたちの餌になる。世代を重ねるごとにコアラの個体数と体内の闇濃度は増していき、さらなる暗黒餌場を求めて別の土地へと暗黒行進を始めるのだ…！！お先真っ暗とはまさにこのことだぜ。", "Well...actually...I found this inn more comfortable than my house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 1000 ) {
			if ( gdata(GDATA_FLAG_MAIN) > 355 ) {
				listmax = 0
				buff = lang("まずいぞ。コアラを決死に食い止めていた、同盟のすぎのこの村からの連絡が途絶えた…。ここが襲われるのももはや時間の問題だろう。", "This fear turned into reality soon enough for she cried out as she woke up.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("引き受ける", "No problem.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("無理", "None of my business.")
				listmax++
				buff = lang("コアラはきのことは比較にならないほどの強敵だが、お前の力ならやつらを止められるはずだ！頼む…！このままでは周囲一帯が暗黒焼畑農業によって暗黒物質農場にされてしまう…。", "Hey you, are you..uh...alright with cats? Well, those filthy...they creatures took over my house. I've been asking adventurers to clean up the cats from my house ever since but none returned. What's happening in my house? You...can you take a look inside my house?")
				gosub *chat_select
				if ( chatval != 1 ) {
					listmax = 0
					buff = lang("せめて難民たちは逃がした方がいいんじゃないか？受け入れ先が見つかるとは思えないが…。", "Uh...fine...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				listmax = 0
				buff = lang("お馴染みの魔法の階段を出しておく…そこから乗り込んでくれ。頼むぞ、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！お前が最後の希望だ！！", "Thank God. My house is south from hear. Be careful, for it is unknown what inhabits my home.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) = 1001
				cell_featset 17, 19, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 10
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 1000 ) {
			if ( gdata(GDATA_FLAG_MAIN) <= 355 ) {
				listmax = 0
				buff = lang("風のうわさによるとこの地に向かってコアラの大群が行進してきているそうだ。", "This fear turned into reality soon enough for she cried out as she woke up.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("やつらは危険だ。実力もさることながら、かわいい外見に反して腹の中は真っ黒だからな…。このままコアラに蹂躙されるのを待つしかないのか…？", "Well...actually...I found this inn more comfortable than my house.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("引き受ける", "No problem.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("ざまぁみろ", "None of my business.")
			listmax++
			buff = lang("かつてこの地には我々たけのこが平和に暮らす里が広がっていた…だが突如攻めてきたきのこ達に里を焼かれてしまい、今では難民の溜まり場だ。仲間はみんな炎に飲み込まれてしまって、生き残ったのはおれだけさ。…お願いだ、きのこ達が住む山を滅ぼしてきてくれ！仲間達の仇をとってやってくれよ！", "Hey you, are you..uh...alright with cats? Well, those filthy...they creatures took over my house. I've been asking adventurers to clean up the cats from my house ever since but none returned. What's happening in my house? You...can you take a look inside my house?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("消えろ、ぶっとばされんうちにな。", "Uh...fine...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("ありがとう。やっぱりたけのこ派は親切な人ばかりだ。きのこはアホだから侵攻に使った魔法の階段をまだ残してやがる。そこから乗り込んでくれ！", "Thank God. My house is south from hear. Be careful, for it is unknown what inhabits my home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) = 1
			cell_featset 23, 22, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 3
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 1 ) {
			listmax = 0
			buff = lang("え？きのこやたけのこのくせにどうやって移動してるのかって？歩いてるとこ見たことないの？そんなことより早く山のきのこを殲滅してきてくれよ。", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) == 2 ) {
			flt
			itemcreate -1, ITEM_ID_OBJET_HEART, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 600
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
			flt
			itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("山のきのこどもを滅ぼしてきたんだな！ついに…ついに有史以前から続いていたきのことの戦いに勝利したのか。しかし…なんだこの虚しさは…？", "This fear turned into reality soon enough for she cried out as she woke up.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("我々は…ずっと相手方を憎み嫌い、戦うことしか考えていなかった。だがそれはきっと間違いだったのだ。滅ぼす前にちゃんと歩み寄れば、もしかしたら和解することもできたのかもしれない…。", "This fear turned into reality soon enough for she cried out as she woke up.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("まぁでも滅ぼしちゃったものはしょうがないよねー。", "Well...actually...I found this inn more comfortable than my house.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 408 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 1003 ) {
			if ( gdata(GDATA_AREA) == AREA_MAID_MANSION ) {
				listmax = 0
				buff = lang("ご主人様、ご無事で！この屋敷に集まりつつあった増援の悪魔たちは、どうやら親玉を失ったことでみんな逃げて行ったみたいですよ！", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そうそう、地下室への階段前にこんなものが落ちていたんです。清掃業者さんの物でしょうか？とりあえずご主人様に渡しておきますね。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_CLEANING_DOLL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) = 1004
				snd SOUNDLIST_COMPLETE1
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) >= 1000 ) {
			if ( gdata(GDATA_AREA) == AREA_MAID_MANSION ) {
				if ( gdata(GDATA_FLAG_PICNIC_BASKET_MAID_MANSION) == 100 ) {
					listmax = 0
					buff = lang("え〜っと…これ、メイドの皆で作ったんです。よかったら食べてください。お口に合うといいんですけれど…。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
					flt
					itemcreate -1, ITEM_ID_LARGE_PICNIC_BASKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_LEVEL, ci) = 7
					gdata(GDATA_FLAG_PICNIC_BASKET_MAID_MANSION) = 0
				}
				if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) >= 1004 ) {
					list(0, listmax) = 5
					listn(0, listmax) = lang("地下に行く", "Turn over the little sister.")
					listmax++
				}
				if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 1002 ) {
					list(0, listmax) = 4
					listn(0, listmax) = lang("地下に害虫駆除に行く", "Turn over the little sister.")
					listmax++
				}
				if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 1001 ) {
					list(0, listmax) = 3
					listn(0, listmax) = lang("キッチンに虫がいたんだけど", "Turn over the little sister.")
					listmax++
				}
				list(0, listmax) = 2
				listn(0, listmax) = lang("気持ちいいことしない？", "Turn over the little sister.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("ここ、こんなに埃が残ってるよ", "I need to replenish my supply of little balls.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("頑張ってるね", "Turn over the little sister.")
				listmax++
				buff = lang("何か御用でしょうか？", "Nyanya! Nyou bring Mia silver cat! Come come, I nyo hurt nyou, nyo hurt nyou. Mia thank you very much. Nyou can have Mia's precious!")
				gosub *chat_select
				if ( chatval == 1 ) {
					if ( maidsikari > 3 ) {
						listmax = 0
						buff = lang("そ、その…あまりいじめないでください…。", "Well then, take this with you. Be careful though, it's heavy.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( maidsikari <= 3 ) {
						listmax = 0
						buff = lang("す、すみませんすみません！すぐに掃除しなおします！", "Well then, take this with you. Be careful though, it's heavy.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					maidsikari++
					goto *chat_end
				}
				if ( chatval == 2 ) {
					if ( maidkimoti > 4 ) {
						listmax = 0
						buff = lang("も、もういいじゃないですかその話は！", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( maidkimoti == 4 ) {
						listmax = 0
						buff = lang("え、えっちなのはいけないと思います！", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("…え？あれ？気持ちいいことって、そういうことじゃないんですか…？", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( maidkimoti <= 3 ) {
						listmax = 0
						buff = lang("ぶっ！？だ、ダメです！ダメですそんなの！し、仕事に戻らせていただきます！", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					maidkimoti++
					goto *chat_end
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("！ご主人様も遭遇しましたか…。あのおぞましき悪魔達は地下から侵入してきているようで、何度駆除してもきりがないんです。先日、地下に清掃業者を送り込んだのですけど誰一人として帰ってきませんでした…。いったいどうすれば…。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) = 1002
					goto *chat_end
				}
				if ( chatval == 4 ) {
					list(0, listmax) = 1
					listn(0, listmax) = lang("危険は承知の上だ", "I need to replenish my supply of little balls.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("じゃあやめる", "Turn over the little sister.")
					listmax++
					buff = lang("ご主人様自ら駆除に向かわれるのですか！？そんな！危険です！", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
					gosub *chat_select
					if ( chatval != 1 ) {
						listmax = 0
						buff = lang("それがいいですよ！また今度業者に頼みましょう。", "Well then, take this with you. Be careful though, it's heavy.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
						snd SOUNDLIST_FAIL1
						txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
						goto *chat_end
					}
					listmax = 0
					buff = lang("…止めても無駄の様ですね。わかりました、地下へは私が案内いたします。どうぞこちらへ…。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
					gdata(GDATA_TELEPORT_AREA) = AREA_MAID_MANSION
					gdata(GDATA_TELEPORT_LEVEL) = 3
					levelexitby = MAP_EXIT_TELEPORT
					gdata(GDATA_LEVEL_START_ON) = 7
					mapstartx = 16
					mapstarty = 28
					chatteleport = 1
					goto *chat_end
				}
				if ( chatval == 5 ) {
					if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
						snd SOUNDLIST_FAIL1
						txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
						goto *chat_end
					}
					listmax = 0
					buff = lang("え…。また地下室へ行かれるのですか？アイテム整理ですか？ま、まさかそれとも悪魔たちの残党がまだ…！？と、とにかくご案内しますね。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
					gdata(GDATA_TELEPORT_AREA) = AREA_MAID_MANSION
					gdata(GDATA_TELEPORT_LEVEL) = 3
					levelexitby = MAP_EXIT_TELEPORT
					gdata(GDATA_LEVEL_START_ON) = 7
					mapstartx = 3
					mapstarty = 16
					chatteleport = 1
					goto *chat_end
				}
				if ( chatval == 0 ) {
					if ( maidhome > 3 ) {
						listmax = 0
						buff = lang("もう…そんなに褒めても何も出ませんよ♪", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( maidhome <= 3 ) {
						listmax = 0
						buff = lang("はい、ありがとうございます。…えへへ、ほめられちゃった。", "Thanks, I appreciate what you've done. It won't be long before I can reward you")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					maidhome++
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ご主人様、お仕事お疲れ様です！", "You're still alive I see. Good luck glorious murderer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("屋敷ごと買ってやる", "Of course I will.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("はぁー…。え、どうしたのかって？実はご主人様が事業に失敗して借金まみれになってしまって。それで屋敷を売りに出すと同時に私たちメイドも全員解雇。ああ…新しいお仕事先探さないと。でもこの辺りで募集してるとこ、ないんですよねー…。", "Hey, you're not from around here are you? I can tell just by your smell. The world out there... it's taught me that the meek are inevitably used, abused, and then chewed up and swallowed kicking and screaming. The people here are a rank lot, you won't win them over with sympathy and compassion. Don't you ever wonder how people like that can survive? Don't you want to know? ")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("さよならー…故郷のお母さんへの仕送りどうしよう…。臓器でも売るか！", "Then find somewhere else to stand, coward.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("お気持ちは嬉しいですけど…今つけられてる屋敷の値段、金貨200万枚ですよ。そんなもの大富豪でもないと買えるわけが…。", "Thank God. My house is south from hear. Be careful, for it is unknown what inhabits my home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 1 ) {
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 2000000 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("この金で屋敷買える？", "I'll buy.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("新しいお仕事見つからない…。", "Here, you need a nuke. Only 200000 gold pices.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("これは本格的に臓器売らなきゃー…。", "Then go away.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("安いね", "Of course I will.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("考え直す", "Of course I will.")
			listmax++
			buff = lang("うひゃあ！？す、すみません。変な声出ちゃいました。…こんな大金、見るの初めてです！これなら…。あ、でもでも！これとは別に維持費が人件費込みで毎月金貨6000枚ほどかかりますよ。それでも大丈夫ですか？", "I can feel it already... the suffering... the misery... the pain... You've killed many people this day, you should be proud. Little kids, girls, women, old men, the sick, the feeble, even those neither born nor conceived yet. My body's hot, excited, I can hardly breathe... I won't be able to sleep tonight. My thanks, these are for you. ")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("やっぱり高いですよねぇ。", "Then go away.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("わ、私とは金銭感覚が違いすぎる…っ！…えっと。屋敷は元のご主人様が売っているものなのでそちらに交渉しにいってください。今はルードゥスに住んでいるはずです。", "Looks like you're one of us after all. I can't give you a welcome feast, but I can tell you a delicious little story. When I imagine others suffering, whimpering in pain and misery, I get goose bumps. How divine! At the inn in Palmia, there's a stuffed toy laying on the floor of the private room. Stick this bomb inside that toy for me. If you can drown those mindless ants in blood for me, I'll reward you well ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) = 2
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 2 ) {
			listmax = 0
			buff = lang("一緒に解雇されたメイド仲間に、雇ってくれるかもしれない人がいるって連絡しないとなぁ。あ、ルードゥスはここからずっと東にある農村ですよ。", "You're still alive I see. Good luck glorious murderer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 413 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) >= 1004 ) {
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 300000 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("物件を買う", "I'll buy.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("おお、オマエか。ここだけの話、いい物件があるんだが買わんか？散らかってはいるが、大量のペットを放し飼いにすることだって可能な広さだ。しかも税金がかからんぞ！これでなんと金貨30万枚だ！どうだ？もちろん買うだろう？", "You're still alive I see. Good luck glorious murderer.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("シッシッ、貧乏がうつるわ。", "Then go away.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 300000
			snd SOUNDLIST_PAYGOLD1
			listmax = 0
			buff = lang("ほら権利書だ、好きなところに移すとよいぞ。他にも同じような物件があるから、足りなくなったらまた買いに来るのだぞ！", "Go, make the brilliant blood red roses bloom.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_DEED_DISCARDED_RANCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) >= 1000 ) {
			listmax = 0
			buff = lang("ククク…貧乏人どもめ。わしを馬鹿にしたことを後悔させてくれるわ。", "You're still alive I see. Good luck glorious murderer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("くそっ、くそっ！どいつもこいつも…。没落したわしを憐れんでいるが、あれは絶対裏で馬鹿にしておるわ！今に見ておれ貧乏人ども…屋敷が売れてまとまった金さえ入ってくれば…。", "You're still alive I see. Good luck glorious murderer.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) == 2 ) {
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 2000000 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("屋敷を買う", "I'll buy.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			buff = lang("おおおお！？金！金だ！屋敷を買ってくれるのか！？…ふむ…本来は金貨240万枚のところを200万枚にまけておいてやろう。ありがたく思えよ。", "Here, you need a nuke. Only 12000 gold pices.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("シッシッ、わしはオマエのような貧乏人とは本質的に違うのじゃ。", "Then go away.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 2000000
			snd SOUNDLIST_PAYGOLD1
			listmax = 0
			buff = lang("金は確かに受け取った。あの屋敷はもうオマエのものじゃ。好きにするがよいぞ。", "Go, make the brilliant blood red roses bloom.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) = 1000
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 435 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("(不気味に胎動している…。)", "(Ethers were discharged to crystallize some.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("(あなたはフロアの中央に巨大な肉塊が鎮座しているのを見つけた)", "You found a huge piece of meat enshrined in the middle of the floor.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("(それこそが、かつてこの地で産み落とされたメシェーラのオリジナル。生命を無差別に取り込み、星をも食らいつくさんと今も進化を続ける災厄の苗床であった)", "Okay. Wait here.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) < 320 ) {
			cbitmod CHARA_BIT_CRADLE_15TH, tc, TRUE
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 437 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 835 ) {
			evname = lang("忘れられない世界の『ミロス』", "<Miros> the unforgettable singularity")
			evold = lang("忘れられた世界の『ミロス』", "<Miros> hail flom Erusia")
			if ( strmid(cdatan(CDATAN_NAME, tc), 0, strlen(evold)) == evold ) {
				cdatan(CDATAN_NAME, tc) = evname + strmid(cdatan(CDATAN_NAME, tc), strlen(evold), strlen(cdatan(CDATAN_NAME, tc)) - strlen(evold))
			}
			listmax = 0
			buff = lang("私は後悔していない。あの日、生魂の儀を選択したことを。それが、運命の筋書きに定められたものだったかなんて関係なく、だ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("エルシアの首飾りは由緒あるお守りだ。旅の安全を祈り、出会った人々の想いを繋げるとされている。…当時はただの気休めだと思っていたが、意外とそうでもなかったようだな。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_ENCOUNTER_HALION_IN_RUOZA) > 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("伝説って？", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("光に包まれてこちらの世界に飛ばされた際、何者かが私たちの前に現れた。声も姿もおぼろげでよくわからなかったが、運命がどうとか言っていた。もしかすると伝説のノルンだったのかもしれないな。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("ああ、すまない。こちら側の住人にガイアス・ヴィスの伝説はわからないか。ノルンとは、運命の女神とその末裔…巨人族のアールン、妖精族のエルン、小人族のドヴァルン。その総称だ。古神戦争によって大多数が死滅したが、生き残りが運命を見通し人を導くとされている。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		else {
			listmax = 0
			buff = lang("パルミア、エウダーナ、エーテルの風…", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("この世界…イルヴァといったか。どうやら我々の世界ガイアス・ヴィスとまったく無関係というわけではなさそうだな。使命さえなければじっくり調査をしたいところだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それにしてもこの世界はどこかおかしい。死人や瀕死の怪我人が、生魂の儀もなしに自然回復するのだから。誰も疑問に思わないのか？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 438 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 835 ) {
			evname = lang("忘れられない世界の『カルラ』", "<Karura> the unforgettable singularity")
			evold = lang("忘れられた世界の『カルラ』", "<Karura> hail flom Erusia")
			if ( strmid(cdatan(CDATAN_NAME, tc), 0, strlen(evold)) == evold ) {
				cdatan(CDATAN_NAME, tc) = evname + strmid(cdatan(CDATAN_NAME, tc), strlen(evold), strlen(cdatan(CDATAN_NAME, tc)) - strlen(evold))
			}
			if ( gdata(GDATA_FLAG_SUB_KARURA) == 1 ) {
				listmax = 0
				buff = lang("私…？ええ、少し考え事をしていたの。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("カルラに聞いた話だ", "I have some small medals.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("ラーネイレに聞いた童話だ", "I have some small medals.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("よくわからなかった話だ", "I have some small medals.")
				listmax++
				buff = lang("あなたは、獣に変えられた王子の話を覚えているかしら。", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("たとえ姿が…いえ、全てが変わったとしても。あなたが、あなたであったことには変わりない。今の私にはそう思えるの。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("こちら側にも同じ話があるのね。いえ、彼女だからこそ、なのかしら。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("…ごめんなさい。あなたには、まだ、この意味はわからないわね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_KARURA) == 0 ) {
				gdata(GDATA_FLAG_SUB_KARURA) = 1
				listmax = 0
				buff = lang("その首飾りは…。ちょっと見せてもらえるかしら。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("この傷、この彫刻…。間違いないわ、これはあの日助けた旅人に…いえ、「旅人たち」に渡してきた物。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そう、あなたがそうだったのね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_ENCOUNTER_HALION_IN_RUOZA) > 0 ) {
			listmax = 0
			buff = lang("ミロスと合流できたのはいいけれど、こちら側に来てから相当年月が過ぎているわ。もし時間の流れが同じだとしたら…今頃、森の異変はどうなっているのかしら。考えるだけで恐ろしいわ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		else {
			listmax = 0
			buff = lang("あら、あなたは…！？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…ごめんなさい、人違いだわ。昔助けた冒険者と雰囲気が似ていたから間違えてしまったの。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("この土地に流れ着いてから色々な人に話を聞いたけれど、どうやらこの世界は私のいた世界ではないようね。早くガイアス・ヴィスに帰ってザビ王に謁見しなければならないのだけれど、どうやって帰ればいいのかしら？ミロスともはぐれてしまったし…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 445 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 1
		listn(0, listmax) = lang("音楽チケット", "I have some small medals.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("やれやれ、昔はここも美しく静かな街だったのに。観光客が増えたおかげで騒がしくて仕方がありません。賑やかになったと孫は喜んでますが。", "Oh oh, what we got, talk about rare! A traveler? I would have given you a feast if it were not Garok that handles today's dinner. You know, his cooking is as loutish as his creations. Oh well, you've come all the way here, what can I do for you?")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("音楽チケットを持っているのですか？パーティを開催しようにも切らして困っていたのです。わたしの特別なコレクションと交換してくれませんか？", "Ho ho ho! You got small medals! Let me see, let me see! Would you like to trade them for one of my proud works?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			invctrl = INVCTRL_METAL, 1
			invfile = cdata(CDATA_ROLE_FILE_ID, tc)
			gosub *shop
			screenupdate = -1
			gosub *screen_draw
			cs = 0
			gosub *calcBurdenPc
			goto *chat_unique
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 449 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 630 ) {
				listmax = 0
				buff = lang("今思うと、イトコのフロンとリアにも来てもらうべきでしたね。下見ついでに混沌の神も倒せて一石二鳥だったかもしれません。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				listmax = 0
				buff = lang("妹のエールと一緒にツアーの下見に来たんですが、ここで貧弱なお客様を守りながら戦うのは至難の業ですね…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("というか、妹がどんどん先に行くのではぐれてしまいました。か弱い姉を置きざりにするなんてどんな判断だ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 630
				goto *chat_end
			}
		}
		else {
			listmax = 0
			buff = lang("ツアーガイドたるもの、どんなところだろうと観光案内できなくてはなりません。あらゆる環境を案内できるように己を鍛え上げ、襲いくる野盗や怪物との戦闘をむしろお客様向けのショーにできるような者だけが、このイルヴァでツアーガイドを務められるのです。", "Tour guide must be able to guide everywhere.In Irva, only the person can be a tourist guide, who has trained oneself in order to guide everyone around everyplace and can make battles with brutal monsters or bandits as a dramatic show for clients.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 534 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 650 ) {
				listmax = 0
				buff = lang("ネフィア探索は添乗員の専門外なんですけどねぇ…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				listmax = 0
				buff = lang("うーん。気が付いたらお姉ちゃんがいない…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("お姉ちゃんは昔から私達の中でもどんくさい感じでしたので。ちんたらしてて迷子になったんでしょうね。やれやれ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 650
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 459 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 480 ) {
			listmax = 0
			buff = lang("さすがだ" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、やはりお前を推薦してよかったな。ああ、量産型は一度回収した。今回のような敵が出てくれば対応できない…設定や装備の変更が必要そうだ。", "I called you because I think when fighting data was collected.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("依頼報酬を預かっている。受け取ってくれ。", "Okay...this is reward for data.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_MIGHTY_ARMS, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_SCROLL_GAIN_ATTRIBUTE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			flt
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_SPIRIT
			flt
			itemcreate -1, ITEM_ID_MIND_DISC, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gdata(GDATA_FLAG_MAIN) = 490
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("依頼のため、腕試しのため、好奇心のため、世界のため…理由こそ様々だが、精鋭の冒険者たちが巨城の最深部を目指していると聞く。俺は、お前なら一番乗りも充分狙えると思っている。まぁその、なんだ。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、無茶しない程度に頑張れよ。", "Okay...this is reward for data.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 1010 ) {
			if ( gdata(GDATA_FLAG_DIALOG_LEOLD) < 300 ) {
				gdata(GDATA_FLAG_DIALOG_LEOLD) = 300
				listmax = 0
				buff = lang("…っ！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("遅いぞ" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。無事だったなら、もっと早く顔を見せてくれ。こっちはお前が灰になるところまでしか見ていないんだからな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("あんなボロボロになっても諦めず神に挑むなんて、お前は馬鹿だ。最高の馬鹿だよ全く。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("俺も諦めずにやってみるさ。AO-Iの有用性を示し、再生産の予算を勝ち取る！その暁にはお前に見せたいものもあるからな、気長に待っていてくれ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 840 & gdata(GDATA_FLAG_MAIN) < 1010 ) {
			if ( gdata(GDATA_FLAG_DIALOG_LEOLD) < 200 ) {
				gdata(GDATA_FLAG_DIALOG_LEOLD) = 200
				listmax = 0
				buff = lang("すまん。お前に見せてやりたいものが…いや、見てもらいたいものがあったんだがな。おじゃんになってしまった。はぁ…思った以上にヘコむな、これは。戻し作業にどれだけの月日と予算がかかることやら。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…なぁ、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。神の間に行くのはやめておけ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("お前が混沌と戦うことを、ノルンってやつから聞いたよ。だが、神の間は得体の知れない奴に掌握されてしまった。AO-Iがデータを収集する間もなくスクラップにされるとは、正直俺も思わなかった。危険度は計り知れない。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("心配いらない", "I think not.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("危険を冒してこそ冒険だ", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("ちょっとAO-Iの仇を討ってくる", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("この戦いが終わったら結婚しよう", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("頼む。行かないでくれ。", "Sorry, this is untranslated sentence.")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("信じていいんだな？そうだよな、お前ならきっと…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("やはり無理か。そりゃそうだ。冒険者が冒険をやめるわけないよな。まったく…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("そういうノリでいくのか。わかったわかった。もういい。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("じゃあお前が駄目だった時は、俺がお前の分もまとめて仇を討ってやるからな。どんな手を使ってでもだ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("嫌だ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("そういう約束をした奴は大抵、帰ってこないらしいからな…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 760 & gdata(GDATA_FLAG_MAIN) < 840 ) {
			if ( gdata(GDATA_FLAG_DIALOG_LEOLD) == 100 ) {
				listmax = 0
				buff = lang("実はAO-Iには隠された機能があったんだ。本当は時間がある時にでも見せたかったんだが…。ブツを神の間に置いておくことになってしまったから無理だ。悪いな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_DIALOG_LEOLD) < 100 ) {
				listmax = 0
				buff = lang("…うん。お前に気の利いたことでも言おうと考えていたんだが、特に思いつかなかったぞ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_DIALOG_LEOLD) = 100
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 455 & gdata(GDATA_FLAG_MAIN) < 760 ) {
			listmax = 0
			buff = lang("しかしこの量産型が受けた空間固定、データを見る限りとても興味深い…次元歪曲航法を応用すれば理論的には再現できそうだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("…", "Here, take these medals.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("次元歪曲航法って？", "I think not.")
			listmax++
			buff = lang("追加武装という形でなんとか搭載を…。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("ああ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("次元歪曲航法について聞きたいか！よし…長くなるから端折って説明してやろう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("専門外だから魔法の詳しい原理はわからないが、ショートテレポートと帰還、この二つの魔法に似た現象を科学的に引き起こせると思えばいい。ここではそれぞれ空間歪曲方式、次元扉形成方式と分類させてもらう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そもそも普通に空間を歪曲させて移動しようとすると、エーテル波の影響で空間均衡が崩れ、移動距離の２乗に比例した大きさの揺らぎが生じる。結果として、予定と異なる方向・距離の地点に出てしまう。ズレが大きくなるだけじゃないぞ、歪曲に使うエネルギーも大きくなる。結論を言えば、空間歪曲方式で正確に長距離を移動することは非常に困難だ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("次元歪曲航法ではこれを逆手に取っている。揺らぎが小さくなるよう限りなく短い距離を、移動座標をコンピュータで矯正しながら瞬間的にテレポートし続けることで、物体や力場等を擬似的に「すり抜けて移動」できる。ロストイルヴァ周囲に張り巡らされている結界を突破するのに使っているのもこの機能だ。システム負荷の関係上、連続使用は数１０ｍが限度のため地中を潜らせたりはできないが。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("さて、話はまだ半分だ。続いて次元の扉を形成する方式について話そう。こちらは正確に長距離を移動できる反面、移動先まで続く安定したゲートを形成できる環境が必要になる。つまり、特定地点への移動にしか使えないということだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("帰還の魔法等の移動先がどのような理によって定められているのかは知らん。気まぐれな時空の管理者とやらが関係しているのかもしれないな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("次元歪曲航法では設置した転移拠点へと移動できるようになっているが、これには膨大なエネルギーが必要でな。移動者側からでなく拠点側からエネルギーを供給することで次元の扉を形成している。それでも現時点ではエネルギー供給装置がかなり大型化してしまうため、基地ぐらいにしか設置していない。小型の供給装置が開発できれば、転移拠点を森にでも隠しておいて伏兵を出すのに使えるかもしれないが。それでも移動したい先にあらかじめ装置を設置しなければならないので、攻めるのには向いていない。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("以上、空間歪曲方式と次元扉形成方式を併せ持つ航法のため、次元歪曲航法と命名したところだ。これは現時点でイェルスの専売特許…と言いたいところだが、成果品の一部が他国に流出しているという噂がある。本国の研究所はスパイ探しに躍起になってるって話だ。原理が理解できなくても「機械を使う」ことはできるからな、他国に利用される可能性はあるだろう。使いこなせるかは別としてな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 2
				listn(0, listmax) = lang("ああ！", "Here, take these medals.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("いや？", "Here, take these medals.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("ＺＺＺ…", "I think not.")
				listmax++
				buff = lang("…すまん。かなり色々省いて、手短に説明したつもりなんだが。やっぱり長かったか…？", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("うむ。傷つくな…これは。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("はは、口頭で説明するのは難しいものだな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("おお、そうか！もっと長くしても大丈夫ということだな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 450 ) {
			listmax = 0
			buff = lang("おっと、来てくれたか" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。お前が一番メルガストに慣れてるってことで推薦させてもらったぞ。まぁ…その、あれだ。他に知り合いで頼れる冒険者がいなかったというのもある。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("他の知り合いの冒険者たちは…あの日、レミード遺跡最下層で全滅してしまったからな…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 2
			listn(0, listmax) = lang("仕方ないね", "Here, take these medals.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("内容による", "Here, take these medals.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("頼まれてくれない", "I think not.")
			listmax++
			buff = lang("というわけで、ひとつ頼まれてくれないか？個人的に報酬は出すし、軍から報酬が出るぞ。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval != 1 & chatval != 2 ) {
				listmax = 0
				buff = lang("そうか？駄目か…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("じゃあ説明するぞ。…混沌の巨城の最深部へ向かっていた量産型メルガストが、全機まとめて１０層で行動不能に陥った。移動が不可能なだけで、観測データ受信に問題は生じていないのだが、現在は空間に完全固定されたような状態でな。量産型には、階層と結界の突破時のみ次元歪曲航法を使用するようプログラムしてあるため、どうにもならない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("同階層に魔物の姿があるのだが、こいつの攻撃を受けたのが原因だ。お前にはその魔物を倒してきてもらいたい…ああ、安心しろ。すでに対策を用意してあるからな。お前が動けなくなる心配はない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("奴の空間干渉パターンは量産型から得られたデータで把握済みだ。この装置は奴の干渉を検知すると、近くのメルガストのシステムを外部から強制的に発動させる。周囲の空間を一時的に歪ませて、干渉の94％を逸らしてくれる上、近づけば量産型メルガストたちも行動可能になるだろう。なに、ちょっとした次元歪曲航法の応用だ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("[Ｄジャマー]を持たされた。", "You obtain the Distortion jammers!")
			listmax = 0
			buff = lang("パルミアから貰った資料によると、混沌の巨城７層の階段が混沌の力で塞がれているとの話だったが。先ほど試作型を偵察に行かせたところ、そのような状況は見られなかった。もしお前が行った時に塞がれているようであれば、その時は試作型に乗って突破してくれ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ああ、これも言っておかないとな。パルミアのエリステア女史にこそ確認していないが、特徴から察するに相手は各地に現れた悪魔と同種で間違いないと思う。なぜそいつだけ街や軍事施設ではなく巨城の中にいるのかよくわからんが、番人のような存在なのだろうか？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 455
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 390 & gdata(GDATA_FLAG_MAIN) < 450 ) {
			listmax = 0
			buff = lang("何十体ものモンスターに襲撃されたが、俺のイェルスードは伊達じゃない。大群をバッサバッサ斬り捨てていく勇姿をお前にも見せてやりたかった。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 366 ) {
			listmax = 0
			buff = lang("どうやら上手くいったようだな。突入実験を早めた甲斐があったというものだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…テスカトリポカ、だったな。浮遊大陸へ飛ばされた者は大勢いるが、直前にメッセージがあったケースは今回が初めてだ。浮遊大陸への召喚を行っているのが奴なのかは定かでないが、シエラ・テールの滅亡に関しては防がなくてはならん。俺の研究開発に支障が出るからな。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("イェルス本国はメルガストを量産し、神の間とやらに攻め込むつもりだ。俺も対応で忙しくなるだろう。お前の乗ってきた試作型だが、現在は風の山道頂上に隠してある。あれで浮遊大陸とを行き来し、独自に調査を進めてくれ。試作型とはいえ、本国の作戦に関わる以上堂々と一介の冒険者に使わせるわけにはいかないんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 367
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 320 ) {
			item_find ITEM_ID_AWESOME_SWIMWEAR, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
					listmax = 0
					buff = lang("ん？それは開発中止になった特殊スーツ…地下に保管してあったはずだが、何故お前が持っているんだ？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("いや、そんなことはどうでもいいか。今は重要じゃないな。…その装備が未完成であることは知っているな？設計した以上はいつか完成させてみたいと思っていたんだよ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= 30 ) {
						list(0, listmax) = 3
						listn(0, listmax) = lang("プラチナ硬貨30枚を渡す", "Here, take these medals.")
						listmax++
					}
					if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= 20 ) {
						list(0, listmax) = 2
						listn(0, listmax) = lang("プラチナ硬貨20枚を渡す", "Here, take these medals.")
						listmax++
					}
					if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= 3 ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("プラチナ硬貨3枚を渡す", "Here, take these medals.")
						listmax++
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("考えておく", "I think not.")
					listmax++
					buff = lang("仕上げの特殊加工にはプラチナを大量に必要とする。完成品をお前にやるからプラチナを譲ってくれないか？そうだな、プラチナ硬貨に換算して30枚くらいあれば間違いないだろう。", "Upgrade your cart? It's not something I get excited about but if you can give me " + calccargoupdatecost() + " small medals, I'll think about it.")
					gosub *chat_select
					if ( chatval == 3 ) {
						if ( ibit(ITEM_BIT_NO_DROP, ci) ) {
							snd SOUNDLIST_FAIL1
							txt lang("それはあなたの大事なものだ。<調べる>メニューから解除できる。", "It's set as no-drop. You can reset it from the <examine> menu.")
							goto *chat_end
						}
						if ( inv(INV_ITEM_EQUIP, ci) != 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("装備から外す必要がある。", "You will need to remove it from the equipment.")
							goto *chat_end
						}
						snd SOUNDLIST_PAYGOLD1
						inv(INV_ITEM_NUM, ci) -= 1
						cdata(CDATA_PLATINUM, CHARA_PLAYER) -= 30
						listmax = 0
						buff = lang("よし、すぐに完成させるからちょっと待ってろ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						redraw 0
						gosub *screen_draw
						gosub *screen_fade
						gsel BUFFER_BUF
						pos 0, 0
						picload exedir + "graphic\\face1" + devfile + ".bmp", 1
						gsel BUFFER_SCREEN
						listmax = 0
						buff = lang("いやぁ、自分で言うのもなんだがいい出来だ！ほら、持って行け。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						flt
						itemcreate -1, ITEM_ID_WAR_SUIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
						goto *chat_end
					}
					if ( chatval == 2 ) {
						if ( ibit(ITEM_BIT_NO_DROP, ci) ) {
							snd SOUNDLIST_FAIL1
							txt lang("それはあなたの大事なものだ。<調べる>メニューから解除できる。", "It's set as no-drop. You can reset it from the <examine> menu.")
							goto *chat_end
						}
						if ( inv(INV_ITEM_EQUIP, ci) != 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("装備から外す必要がある。", "You will need to remove it from the equipment.")
							goto *chat_end
						}
						snd SOUNDLIST_PAYGOLD1
						inv(INV_ITEM_NUM, ci) -= 1
						cdata(CDATA_PLATINUM, CHARA_PLAYER) -= 20
						listmax = 0
						buff = lang("少し足りないな…だがこれ以上は払えないというのなら仕方あるまい。やれるだけやってみよう。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						redraw 0
						gosub *screen_draw
						gosub *screen_fade
						gsel BUFFER_BUF
						pos 0, 0
						picload exedir + "graphic\\face1" + devfile + ".bmp", 1
						gsel BUFFER_SCREEN
						listmax = 0
						buff = lang("すまん、やはり失敗した。劣化したうえに少し縮んでしまったな…。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						flt
						itemcreate -1, ITEM_ID_DANGER_SWIMWEAR, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
						goto *chat_end
					}
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("…？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("あ。ゼロがひとつ足りないぞ、とツッコんでもらいたかったのか？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("なんかその…悪かったな。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 315 ) {
			listmax = 0
			buff = lang("なるほど、これなら充分すぎるくらいだ。よくやった、ちょっと待ってろよ。あとはこいつを組み込むだけなんだ。", "Well done, wait a little time. After that I just built the damn thing.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("…。", ".....")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…できたぞ！さぁ持って行け。で、こっちの残りは軍用に、っと。軍は強力な機械兵士を開発して怪物と戦わせるつもりでな、しばらくは動けそうにないから当てにしないことだ。", "...Okay! Bring this item.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_GREEN
			txt lang("[エーテルブラスター]を手に入れた！", "You obtain Ether Blaster !")
			snd SOUNDLIST_COMPLETE1
			gdata(GDATA_FLAG_MAIN) = 320
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 290 ) {
			listmax = 0
			buff = lang("…無事だったか" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。先行していた調査部隊が全滅したと聞いて、正直心配していた。有能な助手を失うのは勘弁だからな。", "I was worried about you because I hear news that investigation troops had been wiped out.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("助手になった覚えはない？まぁいいじゃないか。調査部隊が遺したデータを解析してわかったんだが…最下層に巣くう怪物を完全に倒すには、弱ったところに超高純度のエーテルをぶち込むしかないようだ。", "I found weakness by analyzing the survey data troops left behind. There are only way in order to completely defeat the monster of the Rehmido lowest layer, It is flinging the very high concentrations ether when the monster is weak .")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ここから北東…エイレルの南東といったほうがわかりやすいか？そこの山岳地帯に強力なエーテル反応が確認されている。エーテルの根源を発見し、可能であれば回収してきてくれ。周囲に気化したエーテルが充満しているから抗体は多めに持っていくべきだぞ。", "Ether powerful reaction has been confirmed in the mountains of southeast Eirel. Discover the source of the ether if it is possible. You should take many cure corruption because there are  filled with vaporized ether.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_GREEN
			txt lang("[回収用カプセル]を手に入れた！", "You obtain Capsule for collect!")
			snd SOUNDLIST_COMPLETE1
			gdata(GDATA_FLAG_MAIN) = 295
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 281 ) {
			listmax = 0
			buff = lang("そろそろ戦闘データも集まった頃だと思ってな。…よし、ちゃんと記録されているな。データを吸いだしてっと…また頼むぞ。", "I called you because I think when fighting data was collected.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そうそう、こいつは超兵士を作るために細胞を培養していたら偶然できた代物でな。興味もないし廃棄する予定だったんだが、もしかしたらお前が欲しがるかと思ってとっておいたのさ。遠慮せずに持って行け。", "Okay...this is reward for data.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_FORBIDDEN_MEAT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			gdata(GDATA_FLAG_MAIN) = 282
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 266 ) {
			listmax = 0
			buff = lang("ご苦労。…うむ、派手にやったなお前。データが欠損してやがる。こうなったら、代わりにお前にデータ収集をしてきてもらうぞ。", "Oh...data was lost. Please collect new data.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("新しい[データレジスタ]を無理矢理持たされた…。", "You obtain the data register!")
			gdata(GDATA_FLAG_MAIN) = 267
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 240 ) {
			listmax = 0
			buff = lang("確かにブツは受け取った。ほら、カードキーだ。調査部隊が使っているカードキーはまた別に指紋や声紋も照合するんだが、こいつは特別製だからその辺を省略できるぞ。冒険者はよく変異して体質が変わるからな、こっちのほうが都合がいいだろう。そうだ…おまけでこの像もやろう。", "Okay. I received items indeed. You know, it's a card key. Yeah, I'll give this statue a bonus as well.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 250
			gdata(GDATA_FLAG_MAIN_DATA_CHIP) = 2
			gdata(GDATA_FLAG_MAIN_CHAOS_WINGS) = 2
			gdata(GDATA_FLAG_MAIN_ANKH) = 2
			txtef COLOR_GREEN
			txt lang("[カードキー]を手に入れた！", "You obtain the Card key!")
			snd SOUNDLIST_COMPLETE1
			flt
			itemcreate -1, ITEM_ID_STATUE_MANI, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 220 ) {
			listmax = 0
			buff = lang("将軍からさっき通信で聞いたよ。お前がレミードに潜りたがってる馬鹿だな。いいぞ、そういう馬鹿は嫌いじゃない。調査部隊に加わるかどうかに関係なく、個人的に協力してやろう。ただしちょっとしたおつかいをやってくれたら、だがな。その気になったらまた来い。", "If you have done little errands, I would zapped to enter Rehmido.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 223
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 223 & gdata(GDATA_FLAG_MAIN) <= 330 ) {
			buff = lang("よう、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。お前は面白そうだからな。何か用があるなら俺の貴重な時間を割いてやるぞ。", "" + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", how's your investigation going? Ask anything you'd like to know.")
			list(0, listmax) = 7
			listn(0, listmax) = lang("装備部位を増やしたい", "Tell me about Rehmido.")
			listmax++
			list(0, listmax) = 6
			listn(0, listmax) = lang("レミードについて", "Tell me about Rehmido.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("任務について", "Tell me about the mission again.")
			listmax++
			if ( gdata(GDATA_FLAG_MAIN) >= 223 & gdata(GDATA_FLAG_MAIN) < 240 ) {
				list(0, listmax) = 3
				listn(0, listmax) = lang("機械要塞について", "Tell me about the machinery fort.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("異形の神殿について", "Tell me about the valley of hereafter.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("黄泉の谷について", "Tell me about the chaos shrine.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 7 ) {
				goto *chat_unique_leold_limb
				goto *chat_end
			}
			if ( chatval == 6 ) {
				listmax = 0
				buff = lang("レミードは一般にはネフィアと混同されているが、オリジナル部分はネフィアではなく旧文明の施設だ。…オリジナルというのはだ、レミードは数百年単位で周囲の土地を取り込んでいてな。地上からスキャンしたところ、最下層以外はすべて周囲から吸収し再構築したものだと判明した。そして数千年後にはこの大陸全土が飲み込まれる計算だ。", "Lesimas is one of the numerous ruins of Nefia. It is a significant part of the history of Palmia. The Eulderna sought to bring themselves prosperity using one of the treasures of Nefia, so the third king, Zashim and his cousin Zeome organized an excavation of Lesimas.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("名前からも推測はつくだろうが、あれはレム・イド文明時代に建造された研究施設だ。レム・イドは生化学の発達した文明…おそらく研究していた何かが暴走した結果だろう。", "Eventually the king and his cousin received word that the excavation had been completed, so they descended to the deepest reaches of Lesimas. Together they set foot into the darkened room where the codex was said to reside, yet after some number of hours, King Zashim emerged from the chamber alone. As the legend goes, King Zashim said the following words to his aides.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("軍の上層部はそいつを制御し、あわよくば兵器転用するつもりだ。そこまでして一体何と戦うのかって？戦うためだけじゃない。利益になりそうなものがあれば取り入れたくなる。うちの民族の気質だな。", "And so, they sealed the cavern with powerful magic and bound that seal to three magic stones. They gave each stone to a powerful guardian and sent them to three separate strongholds to watch over the stones. I don't believe the guardians knew the true value or purpose of the stones. ")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("護衛の件で活躍しようものならお前も軍にスカウトされるだろうな。自由気ままに冒険を続けたいなら、遺跡にも軍にも取り込まれないよう注意することだ。", "Since that day, the passing generations of Palmia's royal family have preserved King Zashim's mandate and we've always deceived the other nations into believing there was nothing of value in Lesimas.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 5 ) {
				if ( gdata(GDATA_FLAG_MAIN) >= 290 ) {
					listmax = 0
					buff = lang("最下層にいる怪物はメシェーラの塊だ。貴重なサンプルだから滅ぼすのは少し惜しいが…。", "Monster in the bottom layer is a mass of meshera. It is little regrettable to destroy because target is a precious sample, but there is no way....")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 250 ) {
					listmax = 0
					buff = lang("一応お前の任務は調査部隊の護衛ってことになってるからな。早く行って追い付いたほうがいいと思うぞ。そうそう、遺跡で何か面白そうなものを見つけたら俺に教えてくれ。", "You should catch up with the troops to go to Rehmido early investigation.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_unique
				}
				listmax = 0
				buff = lang("任務ってほどじゃないが…手伝いをしてもらおうか。俺が指定したデータやサンプルを回収してきてくれ。軍が危険防止のために設けた電子ロック、そいつを解除できるカードキーを発行してやるからさ。", "Bring items which I specified. If it does so, I'll issue Card key of Rehmido.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 3 ) {
				listmax = 0
				buff = lang("湖の近辺にうちの軍が建造した機械要塞があるんだが、だいぶ前に地殻変動によってネフィアと融合してしまった。そして融合時のショックで制御システムが暴走したみたいでな、連絡もとれやしない。", "There is Machinery fort in the other side of the lake to the west side.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("こちらも何度か兵士を送り込んだのだが、要塞の防衛システムに阻まれことごとく失敗してな。軍は要塞を破棄することにしたようだ。だが俺はどうしてもシステムに蓄積されたデータを回収したいのだ。もったいないからな。", "Go to there and take out Data chip from <Metal Vesda> the fire machine.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そこでだ。要塞中枢に投げ出されていると思われる大型飛行兵器の頭部から、データチップを抜き取り持ってきてもらいたい。それ以外のパーツは好きにしてかまわんぞ。ちょっと加工すれば装備品にできるパーツもあるはずだしな。", "Wings of <Metal Vesda> will Just good to equip.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("北東の山岳地帯手前に、異形の者が支配する神殿がある。最深部には外法で強化されたモンスターどもが待ち受けているらしい。", "There is Chaos shrine in front of the mountains of the northeast.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そこの親玉、《エグゾシズル》の翼を研究したくてな。ちょっと翼をもいできてくれ。６枚全部だぞ。", "Go to there and take out Chaos wings from <Exossil> the chaos wing.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("魔法のことはよくわからんが、色々と厄介な攻撃を仕掛けてくることが予想される。十分に注意しろ。", "However, I'm expecting a nasty attack comes in various ways. Caution.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("サウスティリス極東の農村…ルードゥスのそのさらに東、ザナンとの国境近くの砂漠に、死者の眠る谷がある。階層こそ少ないが、表層には侵入者を惑わす罠が満載らしい。", "There is Valley of hereafter in desert which in the east of the Ludus.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("その奥にいると言われる《アヌビス》は太陽の光を操るアンクを所持しているらしい。そいつを研究したいんでな、回収してきてくれ。", "Go to there and take out Ankh of sun from <Anubis> the master of death.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("砂漠は道を迷いやすい上に日中はスタミナを大きく消耗する。遭難したくなければ日中の移動は避け、迷ったら慌てずに混乱が解けるまで待て。シェルターを用意していくといいかもしれんな。谷の内部はアンデッドまみれらしいから火炎攻撃が重宝するだろう。ただ、呪いをかけてくる連中も多いはずだ。対策を練ってからのほうが賢明だろうな。", "By the way, there internal is  covered by undeads.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) < 220 ) {
			listmax = 0
			buff = lang("誰だか知らんが邪魔しないでくれ。", "Do not disturb me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
			goto *chat_unique_SWEND1
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("別に…", "No problem.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("装備部位を増やしたい", "No problem.")
		listmax++
		buff = lang("どうした" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "？何か用か？", "Sorry, I'm busy right now."), strbye
		gosub *chat_select
		if ( chatval == 0 ) {
			goto *chat_unique_leold_limb
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 474 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("いいよ", "No problem.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("だめ", "None of my business.")
			listmax++
			buff = lang("ねぇ" + _onii(cdata(CDATA_SEX, CHARA_PLAYER)) + "さんって冒険者だよね？この前お友達がノースティリスのパルミアってところに引っ越したんだけど、その友達に手紙を届けて欲しいんだ！", "Hey you, are you..uh...alright with cats? Well, those filthy...they creatures took over my house. I've been asking adventurers to clean up the cats from my house ever since but none returned. What's happening in my house? You...can you take a look inside my house?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("えー…じゃあどうしよう。", "Uh...fine...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("わーい。ありがとう" + _onii(cdata(CDATA_SEX, CHARA_PLAYER)) + "さん♪", "Thank God. My house is south from hear. Be careful, for it is unknown what inhabits my home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("手紙を預かった。", "You give her a potion.")
			snd SOUNDLIST_EQUIP1
			gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) = 1
			gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) = 2
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) == 1 ) {
			listmax = 0
			buff = lang("え？ぼくに手紙？", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("わぁい、ちょうどおなかすいてたんだ！ムシャムシャ…。", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ハッ！？ど、どうしよう。クロンちゃんからのお手紙食べちゃった…。", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("さっきの手紙のご用事なぁに、っと…。この手紙をクロンちゃんに渡して。もちろんタダじゃないよ。プラチナ硬貨あげるからお願い！", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("手紙を預かった…。", "You give her a potion.")
			snd SOUNDLIST_EQUIP1
			gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) = 2
			gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) = 2
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) == 1 | gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) == 2 ) {
			listmax = 0
			buff = lang("ちゃんと友達にお手紙届けてね。友達はパルミアに住んでるクロンって子だよ。", "This fear turned into reality soon enough for she cried out as she woke up.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 475 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) == 2 | gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) == 2 ) {
			listmax = 0
			buff = lang("え？あたしに手紙？", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("わぁい、ちょうどおなかすいてたの！ムシャムシャ…。", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("うメェー！ハッ！？ど、どうしよう。シラハくんからのお手紙食べちゃった…。", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("さっきの手紙のご用事なぁに、っと…。この手紙をシラハくんに渡して。もちろんタダじゃないよ。プラチナ硬貨あげるからお願い！", "Is my home safe from the cats?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("手紙を預かった…。", "You give her a potion.")
			snd SOUNDLIST_EQUIP1
			gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) = 1
			gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE) = 3
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) == 1 ) {
			listmax = 0
			buff = lang("ちゃんと友達にお手紙届けてね。友達はルードゥスに住んでるシラハって子だよ。", "This fear turned into reality soon enough for she cried out as she woke up.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 476 | _switch_sw ) {
		_switch_sw = 0
		item_find ITEM_ID_PUFF_PUFF_BREAD, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
				listmax = 0
				buff = lang("ひっ！？そ、それは…ふかふかパ…い、イヤぁあああああ！お、お願いだから早くしまって！！！", "Okay. Wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				dmgcon tc, CONDITION_FEAR, 1000
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_DOGS_TAIL) >= 1000 ) {
			listmax = 0
			buff = lang("今度ネフィアに素材集めに行く時ボディガードとして雇わせてよ。時々、私の錬成したアイテムとかサービスするから、ね？", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DOGS_TAIL) == 3 ) {
			listmax = 0
			buff = lang("それはエストークの尻尾！これで私は師匠を超えることができる…！", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("…。", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あ、お礼がまだだったね。これは私が前に錬成したアイテムだけど、よかったら使ってみて！", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_SMALL_MEDAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_PHILOSOPHERS_STONE_MASK, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 800
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_DOGS_TAIL) = 1000
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DOGS_TAIL) == 1 ) {
			listmax = 0
			buff = lang("無理にとは言わないけれど、錬金術の発展のためにもぜひ協力して！", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_DOGS_TAIL) == 0 ) {
			listmax = 0
			buff = lang("ナプラスのアトリエにようこそ♪…ちょっとお願い聞いてくれる？", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("私が錬成したいアイテムにどうしても必要な素材があって…それを取ってきてほしいな。素材はエストークの尻尾、サウスティリス南西端にある犬神の森の主を倒さないといけないの。", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("本当は私が調達しに行くべきなんだけど…他の依頼で忙しいし、そもそもエストークは私が敵う相手じゃないんだ。とぉぉぉぉっても危険な依頼だけど、よかったら引き受けてほしいなーって。", "Okay. Wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_DOGS_TAIL) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 477 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			listmax = 0
			buff = lang("才能はあるのに修行を嫌がって何度も家出するもんだから、わざわざ死亡工作までして忍を抜けさせてやったというのに。まさか遊びながら我流で俺より強くなってしまうとはな…。お兄ちゃんは泣きたいぞ。立つ瀬がないぞ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…まぁ。反抗期の酷かった妹が、こうして俺を頼ってくれたのはすごく嬉しいがな！お兄ちゃん頑張っちゃうぞ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 842 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 540 ) {
				listmax = 0
				buff = lang("表向きは、ファッションやおしゃべりを嗜む普通の女の子。しかし裏では、闇に紛れて人々のために忍術を振るう…。くぅ〜！これだよこれ！僕の理想のニンジャ・ライフスタイルは！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 535 ) {
				listmax = 0
				buff = lang("あれれ…君もここに来てたの？そうか、冒険者だもんね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("その節はありがとう。いつか、こっちから話しかけてお礼言わなきゃとは思ってたんだけど…。ほら、人前だと市民として振る舞わなきゃいけないからさ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("実家の流派はあの時やめたんだけど、今はなんかいろいろ物騒だからさ。我流忍術で混沌と戦ってるんだ！我流は良いよ。どんな格好しても、規律だのなんだのと怒られないし最高だよ！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 540
				goto *chat_end
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 478 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_FAME, CHARA_PLAYER) < 40000 ) {
			listmax = 0
			buff = lang("えーと…どちらさま？", "Who the hell are you?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_RED
			txt lang("クエスト必要名声：40000　", "Required Fame: 40000 ")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) == 0 ) {
			listmax = 0
			buff = lang("ふーん…君、強そうだね。君になら頼めるかも。", "Hey you! Hold up! I've seen your face before punk... " + cdatan(CDATAN_AKA, CHARA_PLAYER) + "... Ah yes... I remember now. You've been making quite a name for yourself throughout Tyris, haven't you?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("御意", "Fine, let's do it.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("無理でござる", "Not a chance, you worm.")
			listmax++
			buff = lang("僕の兄さん、忍者軍団の頭領やってるんだけどさ。もう融通がきかないのなんのって！僕が忍者やめたいっていくら言っても、機密がどうのこうのの一点張りなんだ…。もうあんな頑固者、兄だとも思わない！ちょっとうちの忍者軍団ごとぶっとばしてきてよ！", "You wanna make some quick cash? I've got a request from the organization of Zanan. They need a test subject in order to get some experimental data. Or perhaps \"sacrifice\" would be a more appropriate term. *If* you survive, I'll pay you enough to party until you waste away. Make your decision quickly.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("そんなこと言われても困るでござる。", "Fine then, piss off you spineless coward.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("やったぁ！準備ができたら僕に声をかけてね。屋敷まで案内するからさ。", "Excellent. Let me know when you've finished your preparations for death and I'll take you to the testing grounds.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) == 1 | gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) == 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("目をつぶる", "Yes.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("何をする気だ！", "No.")
			listmax++
			buff = lang("準備はできた？案内するからちょっと目をつぶっててね…。", "So, are you ready?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("何って…案内するだけだよ！？", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) = 2
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_KURUALM
			gdata(GDATA_TELEPORT_LEVEL) = 5
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 23
			mapstarty = 21
			evadd EVENT_BOSS_MUSIC
			gdata(GDATA_FLAG_BOSS_MUSIC) = 3
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) == 3 ) {
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("うん、兄さんを倒すなんて君はやっぱり強いや。僕の目に狂いはなかったみたいだね。", "Th-thank you... thank...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("えへへ♪ともかく、これで僕もやっと忍者をやめることができるよ。僕、実は可愛い服とか着て街を堂々と歩いてみたかったんだ。何にも縛られず自由きままに…。君が戻ったら似合うかどうか見てもらおうと、アクセサリも買ってあったりして。", "Th-thank you... thank...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…ッ！この気配は…！？", "Th-thank you... thank...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) = 1000
			evadd EVENT_KUNOICHI, tc
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 482 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_YACATECT & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_YACATECT) == 0 ) {
			listmax = 0
			buff = lang("お得意様には特別にこのアクセサリーをプレザント！どや？ふふん、金ぴかでキレイやろ？値打ちもんなんやで。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_AUREATE_CONSTRICTER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_YACATECT) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("ふーむ。エヘカトルの食べ残しやクミロミの抜け羽なんかを拾ってきたら売れるやろか？どうやろ？", "Hmm...I wonder if I could sell Ehekatl's leftovers or Kumiromi's shed feathers for cash. What do you think?")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("よっしゃ、お宅拝見と行こか！ふっふっふ、今から片づけようとしても遅いでー？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 482
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 484 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOD, CHARA_PLAYER) == GOD_ITZPALT & cdata(CDATA_PIETY, CHARA_PLAYER) >= 4000 & gdata(GDATA_FLAG_SUMMONED_ITZPALT) == 0 ) {
			listmax = 0
			buff = lang("我が賢き信者よ、受け取るがいい。汝ならばこのアーティファクトをもきっと使いこなせるであろう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_MAGIC_CANCELER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			gdata(GDATA_FLAG_SUMMONED_ITZPALT) = 1000
			goto *chat_end
		}
		else {
			list(0, listmax) = 0
			listn(0, listmax) = lang("立ち去る", "Bye.")
			listmax++
			if ( gdata(GDATA_FLAG_INVITED_GOD) == 0 ) {
				if ( gdata(GDATA_RANK_HOME) < 200 ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("わが家に招待する", "Invite to home.")
						listmax++
					}
				}
			}
			buff = lang("先代のエヘカトルとテスカトリポカは大層仲が悪く、よく我が仲裁に入ったものだ。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("ふむ。汝の築き上げし楽園、我が双眸に刻むのもよいだろう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_INVITED_GOD) = 484
				cdata(CDATA_CHARACTER_TIER, tc) = CHARACTER_TIER_SUMMONED_GOD_HOME
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}

	if ( _switch_val == 490 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) == 2 ) {
			listmax = 0
			buff = lang("ご先祖様はまた石になって眠っているのう。もう数万年は目覚めることはないじゃろう。わしは…わしは平気じゃよ。じゃから、心配せんでよい。こうして地上から話に来てくれる者がおるだけで…充分じゃ。", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("おぬしには礼をしたかったんじゃが、この城には地上人が喜びそうなものがなくてのう。その…情けない話じゃが、お弁当くらいしか用意できんかったんじゃ。あれだけの大仕事をしてもらっておいて弁当はどうかと思うが、よければ受け取ってくれんか。", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_ABYSS_LUNCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			listmax = 0
			buff = lang("さぁて！辛気臭い話はやめて、切り替えてゆくぞ！", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) = 3
		}
		if ( gdata(GDATA_FLAG_MAIN) == 400 ) {
			if ( gdata(GDATA_LEVEL) == 30 ) {
				listmax = 0
				buff = lang("よくぞ止めてくれた…おぬしには本当に感謝しておる。この島の人々には本当に悪いことをしたが、わしの力では元通りにできぬ…。せめて今は少しでも早く地上を離れ、城と共に海の底に戻るとしよう。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、城には何も面白いものはないがよかったら遊びに来とくれ。…待っておるぞ。何万年でも、のう。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
				gdata(GDATA_TELEPORT_AREA) = AREA_VALM
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_TELEPORT
				chatteleport = 1
				goto *chat_end
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 399 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("一度帰りたい", "No.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("任せておいて", "No.")
			listmax++
			buff = lang("おぬしには苦労をかけるのう…。", "So, are you ready?")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				listmax = 0
				buff = lang("確かに色々と準備もいるじゃろうな。じゃが時間があまり残っておらんことは忘れんでくれ。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
				gdata(GDATA_TELEPORT_AREA) = AREA_VALM
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_TELEPORT
				chatteleport = 1
				goto *chat_end
				goto *chat_end
			}
			listmax = 0
			buff = lang("すまぬ…すまぬ…。", "Be quick.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 398 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("助けを呼ぶ声が聞こえた", "Yes.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("…今すぐ帰りたい", "No.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("亀に無理矢理連れてこられた", "No.")
			listmax++
			buff = lang("お、おぬしは！？", "So, are you ready?")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("な、何をやっとるんじゃレイキは！？うちの亀が申し訳ないことをしたのう…。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				listmax = 0
				buff = lang("…そうじゃろうな。この状況では当然じゃの…。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
				gdata(GDATA_TELEPORT_AREA) = AREA_VALM
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_TELEPORT
				chatteleport = 1
				goto *chat_end
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("わしの呼び声はなんとか届いたのじゃな…。", "Follow me.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("のう、見とくれこの惨状を。昔浮上した時は辺りに島なんてなかったんじゃがのう。地殻変動の影響じゃろうな。…何人かは逃がせたんじゃが、ほぼ全ての島民が精神崩壊してしもうた。わしらは…地上の人々に害をなす種族だったんじゃな。そんなことも知らずにわしはずっと地上に憧れておった。本当に…馬鹿じゃな…。", "Follow me.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…泣き言を言っている場合ではないのう。城は島の向こう側にある。浅瀬を渡っていけば辿り着くじゃろう。ご先祖様はその奥じゃ。どうも物理的にはそこまで強くない上に寝起きで魔術も上手く使えないようじゃ。おぬしの力ならきっと止められる。", "Follow me.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ただしご先祖様は断続的に精神波をテレパシーで飛ばしておる。今までは大量の海水がそれを遮っていたんじゃろうが、今は遮るものもない。もしも耐えきれず精神をある程度病んだ場合は即刻連れて帰るように、とレイキには命じておる。精神状態には常に気をつけとくれ。", "Follow me.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 399
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_PRINCESS_COMPANION) < 1000 ) {
			listmax = 0
			buff = lang("苦しゅうない。さぁさ、はやく地上の世界の話を聞かせるのじゃ！", "Ah you're here. So, now tell me all about how things are on the surface!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("（あなたが今までしてきた冒険の話や世界情勢について聞かせた…）", " (I was asked about the state of the world and story of adventure...) ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) == 0 ) {
				listmax = 0
				buff = lang("うう…あちこち冒険できるお主が羨ましいぞ。わしは役目があってこの城から離れられないからのう…。最後にこの城が浮上したのは何万年も前だと聞く。", "ghh...So jealous you can adventure out to different places. I can't even leave this castle...How many thousands of years has it been since someone last floated up here...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("ともかく大義であったぞ。また遊びに来てくれい。これは礼じゃ！", "Anyways, Let me thank you.  Please come visit again. Here, this is for you!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_AGING_LUNCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_PRINCESS_COMPANION) = 1000
			goto *chat_end
		}
		list(0, listmax) = 2
		listn(0, listmax) = lang("遊びに来た", "I came to play.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("自分の身の上話をする", "I speak my career.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("仲間の身の上話をする", "I speak the career of fellow.")
		listmax++
		buff = lang("何か面白い話でもあるのかのう？", "Oh? You have some interesting stories to tell?")
		gosub *chat_select
		if ( chatval == 1 | chatval == 0 ) {
			if ( chatval == 1 ) {
				c = 0
			}
			if ( chatval == 0 ) {
				ikoru = 10
				allyctrl = 3
				gosub *com_ally
				ikoru = 0
				if ( stat != (-1) ) {
					c = stat
				}
				else {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("おお、それは楽しみじゃのう！早く話すのじゃ！", "Ah, been waiting for this! Quickly now, I want to hear!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			ohanasi1 = cdata(CDATA_BACKGROUND_PART_A1, c)
			ohanasi2 = cdata(CDATA_BACKGROUND_PART_A2, c)
			ohanasi3 = cdata(CDATA_BACKGROUND_PART_B1, c)
			ohanasi4 = cdata(CDATA_BACKGROUND_PART_B2, c)
			ohanasi5 = cdata(CDATA_BACKGROUND_PART_B3, c)
			goto *chat_unique_mizuki
			goto *chat_end
		}
		listmax = 0
		buff = lang("それはそれでうれしいのう、何もないところじゃがゆっくりしていくとよいぞ。", "That still does make me happy, there may not be anything here but take a load off.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		item_find ITEM_ID_SMALL_MEDAL, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("メダルを渡す", "Here, take these medals.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("あげないよ", "I think not.")
				listmax++
				buff = lang("ところでそのきらきらしたやつ…小さなメダルというものじゃったか？それ綺麗じゃのう…たのむ、ゆずってくれんか？", "That shiney thing you've got there... It isn't a small medal is it? ...Please, can you give it to me?")
				gosub *chat_select
				if ( chatval != 1 ) {
					listmax = 0
					buff = lang("う…無理を言ってすまんかったの。", "ngh... Sorry for asking the impossible.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				snd SOUNDLIST_PAYGOLD1
				inv(INV_ITEM_NUM, ci) -= 1
				listmax = 0
				buff = lang("おお…感謝感激じゃ！ふむ、ただで貰うのも悪いしのう。こんなものしかないんじゃが受け取ってくれるかの？", "Oh...Thank you so much! Hm, wouldn't be right to take it for nothing. I only have this but will you accept it?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 400 & rnd(4) == 0 ) {
					flt
					itemcreate -1, ITEM_ID_ABYSS_LUNCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
				}
				else {
					if ( rnd(2) == 0 ) {
						flt
						itemcreate -1, ITEM_ID_AGING_LUNCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
					}
					else {
						flt
						itemcreate -1, ITEM_ID_AGE_RETURN_LUNCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
*chat_unique_mizuki
		ohanasi6 = 100
		if ( ohanasi1 != 0 ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang("前半リロール", "Here, take these medals.")
			listmax++
		}
		if ( ohanasi1 != 0 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("後半リロール", "Here, take these medals.")
			listmax++
		}
		list(0, listmax) = 3
		listn(0, listmax) = lang("全リロール", "Here, take these medals.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("上記に沿って話す", "Here, take these medals.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("内緒にする", "I think not.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("話すのをやめる", "I think not.")
		listmax++
		buff = lang("（内容決定）", "Upgrade your cart? It's not something I get excited about but if you can give me " + calccargoupdatecost() + " small medals, I'll think about it.")
		gosub *chat_select
		ohanasi6 = 0
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("そうじゃったのか…改めて思うが、人それぞれに物語があるんじゃな…。", "Is that so... Thinking about it, each person does have their own story...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( cdata(CDATA_BACKGROUND_PART_A1, c) != 0 ) {
				if ( ohanasi1 == cdata(CDATA_BACKGROUND_PART_A1, c) & ohanasi2 == cdata(CDATA_BACKGROUND_PART_A2, c) & ohanasi3 == cdata(CDATA_BACKGROUND_PART_B1, c) & ohanasi4 == cdata(CDATA_BACKGROUND_PART_B2, c) & ohanasi5 == cdata(CDATA_BACKGROUND_PART_B3, c) ) {
					listmax = 0
					buff = lang("それにしても前に聞いた話と全く同じような…。な、なんじゃ？わしはまだボケておらんぞ！イケイケのピチピチじゃ！", "It does feel like I've heard the exact same story before... W-what? I'm not going senile!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
			}
			cdata(CDATA_BACKGROUND_PART_A1, c) = ohanasi1
			cdata(CDATA_BACKGROUND_PART_A2, c) = ohanasi2
			cdata(CDATA_BACKGROUND_PART_B1, c) = ohanasi3
			cdata(CDATA_BACKGROUND_PART_B2, c) = ohanasi4
			cdata(CDATA_BACKGROUND_PART_B3, c) = ohanasi5
			goto *chat_end
		}
		if ( chatval == 5 ) {
			ohanasi1 = rnd(32) + 1
			ohanasi2 = rnd(28) + 1
			goto *chat_unique_mizuki
		}
		if ( chatval == 4 ) {
			ohanasi3 = rnd(13) + 1
			ohanasi4 = rnd(41) + 1
			ohanasi5 = rnd(36) + 1
			goto *chat_unique_mizuki
		}
		if ( chatval == 3 ) {
			ohanasi1 = rnd(32) + 1
			ohanasi2 = rnd(28) + 1
			ohanasi3 = rnd(13) + 1
			ohanasi4 = rnd(41) + 1
			ohanasi5 = rnd(36) + 1
			goto *chat_unique_mizuki
		}
		ohanasi1 = 0
		ohanasi2 = 0
		ohanasi3 = 0
		ohanasi4 = 0
		ohanasi5 = 0
		if ( chatval == 1 ) {
			cdata(CDATA_BACKGROUND_PART_A1, c) = 0
			cdata(CDATA_BACKGROUND_PART_A2, c) = 0
			cdata(CDATA_BACKGROUND_PART_B1, c) = 0
			cdata(CDATA_BACKGROUND_PART_B2, c) = 0
			cdata(CDATA_BACKGROUND_PART_B3, c) = 0
		}
		if ( chatval == 0 | chatval == 1 | key == key_cancel ) {
			listmax = 0
			buff = lang("な…気になるじゃろうが！", "Wha... Now that is going to bother me!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique
		_switch_sw++
	}
	if ( _switch_val == 491 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) == 1 ) {
			listmax = 0
			buff = lang("その節はどうもありがとうございました！…姫様とも話していただけると嬉しいです。実はあれからずっと塞ぎ込んだままなんです。", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) = 2
		}
		if ( gdata(GDATA_FLAG_MAIN) == 398 | gdata(GDATA_FLAG_MAIN) == 399 ) {
			listmax = 0
			buff = lang("冒険者様、どうか力を貸してください！姫様が精神波を抑え込めるのにも限界が来ています！", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 2
			listn(0, listmax) = lang("断る", "No.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("無言で亀に乗る", "Yes.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("誰かの代わりは嫌だ", "No.")
			listmax++
			buff = lang("この街にいた強い人たちに協力をお願いしようと来てみたのですが、「戦争中の国を説得してやろうと思う」とかで旅に出た後だったんですよ！姫様はずっと地上の世界に憧れていて…ようやく願いが叶ったと思えば地上の人々を苦しめることになってしまって…。せめて、せめて冒険者様を連れて行くことで姫様のお役に立ちたいのです！", "So, are you ready?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("お願いします！お願いします…！", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			listmax = 0
			buff = lang("…！ありがとうございます…。ではお連れします！", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_VALM
			gdata(GDATA_TELEPORT_LEVEL) = 30
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 18
			mapstarty = 65
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_LEVEL) == 3 ) {
			listmax = 0
			buff = lang("姫様は奥の部屋にいますよ。くれぐれも機嫌をそこねないようにお願いします。それと、正面の階段は魔法によって地上とつながっています。お帰りの際はそちらからどうぞ。", "The princess resides in the far room.  I ask you to not upset her. As well, the stairwell before you connects to the surface through magic.  Feel free to use it as you wish to leave.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_PRINCESS_COMPANION) == 0 ) {
			if ( gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) == 0 ) {
				listmax = 0
				buff = lang("おや、冒険者の方ですか？ちょうどいいところに来てくれました。", "Ah, If it isn't an adventurer? You've come at the right time.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("いいよ", "Fine, let's do it.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("やだ", "Not a chance, you worm.")
			listmax++
			buff = lang("うちの姫様は城から出られず毎日退屈しているのです。よければ一緒に来て、地上世界の話を聞かせてあげてください。", "Our Princess is quite bored with these days of not being able to leave the castle. If you would please come with me, and could you speak of the surface world with her.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("このまま帰ったら姫様に怒られてしまう！どうか亀助けだと思ってお願いします！", "The princess will be quite displeased if you leave! Please think of it as being rewarded for your virtue!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("おお、感激です。準備ができたら、声をかけてください。", "Excellent. Let me know when you've finished your preparations for death and I'll take you to the testing grounds.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_PRINCESS_COMPANION) = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_PRINCESS_COMPANION) >= 1 & gdata(GDATA_LEVEL) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("いく", "Yes.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("いかない", "No.")
			listmax++
			buff = lang("姫様のところに行くのですね？", "So, are you ready?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("ええー。", "Be quick.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			listmax = 0
			buff = lang("ではお連れしましょう。しばらく息を止めててください。", "Follow me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_VALM
			gdata(GDATA_TELEPORT_LEVEL) = 3
			levelexitby = MAP_EXIT_TELEPORT
			evadd EVENT_BOSS_MUSIC
			gdata(GDATA_FLAG_BOSS_MUSIC) = 2
			chatteleport = 1
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 495 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("僕になにか…こほん。私に何か用でしょうか。", "Okay. Wait here.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("え…忍者？な、なにそれ？", "Okay. Wait here.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 508 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 265 & gdata(GDATA_FLAG_MAIN) < 266 & gdata(GDATA_LEVEL) == 106 ) {
			listmax = 0
			buff = lang("気をつけろ。どうやら遺跡内の機械たちが怪電波で暴走したみたいだ。余裕がなければすぐに階段を上がって逃げろ。ただし、今お前と会話してるこのモニターつきのやつ、こいつはデータ収集兼通信用に配備しておいたメカでな。データ格納部分を傷つけずに駆動系を破壊して、メルガスの俺のラボまで持ってきてくれ。", "I will be careful. Machines in ruins seems runaway by radio special. fled up the stairs as soon as there is no margin. However, the machine with this monitor conversation with you right now, this is such a mechanism that had been deployed for data collection and communication. Destroy the drive system without damaging the data storage portion,and bring me to my lab of Melugas.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 280 & gdata(GDATA_FLAG_MAIN) < 281 & gdata(GDATA_LEVEL) == 122 ) {
			listmax = 0
			buff = lang("探索中悪いが、至急メルガスの俺のラボまで来てくれ。…と言ったところで冒険者は無視しかねないからな。次の階層への扉を遠隔操作で施錠しておいた。それ以上進めないぞ。", "Not live in during the search, come to the lab immediately Melugas. There is a possibility to ignore, I had been locked the door to the next level by remote control. You will not proceed further.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 281
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 516 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("(もうエーテルを散布している様子はないが、今までに結晶化したエーテルが相変わらず表面を覆っている。)", "(Ethers were discharged to crystallize some.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("(エーテルを生成しこの辺り一帯に散布している生命体だ。排出したエーテルが一部結晶化し表面を覆っている。)", "(Covering the body's surface life that is sprayed all around this generates the ether. Ethers were discharged to crystallize some.)")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 300 & gdata(GDATA_FLAG_MAIN) < 310 ) {
			listmax = 0
			buff = lang("(あなたは手ごろな結晶体に狙いを定め、叩き割った。)", "(You strike and divide with aim at affordable crystal)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_GREEN
			txt lang("回収用カプセルに高純度のエーテル塊を入れた。", "You obtain the high purity ingot of ether!")
			snd SOUNDLIST_COMPLETE1
			gdata(GDATA_FLAG_MAIN) = 310
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 519 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) != AREA_DOJO ) {
			listmax = 0
			buff = lang("貴様！ここは道場じゃないぞ！", "Go, make the brilliant blood red roses bloom.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_PETS_IN_DOJO) < 0 ) {
			gdata(GDATA_PETS_IN_DOJO) = 1
		}
		if ( gdata(GDATA_FLAG_DIALOG_NAZUNA) == 1000 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("なんでもないです", "I think not.")
			listmax++
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) > 10000 ) {
				if ( gdata(GDATA_PETS_IN_DOJO) < 150 ) {
					list(0, listmax) = 1
					listn(0, listmax) = lang("仲間の入門（10000gp）", "Entrust ally(10000gp)")
					listmax++
				}
			}
			if ( gdata(GDATA_PETS_IN_DOJO) > 0 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("特別訓練", "Special train allys")
				listmax++
			}
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) > 200 ) {
				list(0, listmax) = 3
				listn(0, listmax) = lang("魔法スタイルの変更（200gp）", "CastStyle Chage(200gp)")
				listmax++
			}
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) > 200 ) {
				list(0, listmax) = 4
				listn(0, listmax) = lang("格闘スタイルの変更（200gp）", "MeleeStyle Change(200gp)")
				listmax++
			}
			buff = lang("はああああっ！喝ッ！！…ん？何の用だ！", "Gah!! Hm? State your business!")
			gosub *chat_select
			if ( chatval == 1 ) {
				allyctrl = 3
				gosub *com_ally
				if ( stat != (-1) ) {
					c = stat
					snd SOUNDLIST_OK1
					txtnew
					if ( c == 0 ) {
						goto *chat_end
					}
					if ( cbit(CHARA_BIT_BODYGUARD, c) != FALSE | cbit(CHARA_BIT_GUARD_TEMP, c) != FALSE ) {
						listmax = 0
						buff = lang("おい！ここは護衛対象の面倒を見る場所ではないぞ！", "You've no desire to improve! You wuss!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( cdata(CDATA_IN_TAGTEAM, c) == (-1) ) {
						ttc = cdata(CDATA_TAGTEAM_PARTNER, c)
						cell_findspace cdata(CDATA_X, c), cdata(CDATA_Y, c), 1
						if ( stat == 0 ) {
							txt lang("充分なスペースがない。", "There's no place.")
							goto *chat_end
						}
						cell_setchara ttc, rtval, rtval(1)
						tag_end c
					}
					if ( cdata(CDATA_IN_TAGTEAM, c) == 1 ) {
						ttc = cdata(CDATA_TAGTEAM_PARTNER, c)
						cell_findspace cdata(CDATA_X, c), cdata(CDATA_Y, c), 1
						if ( stat == 0 ) {
							txt lang("充分なスペースがない。", "There's no place.")
							goto *chat_end
						}
						cell_setchara ttc, rtval, rtval(1)
						tag_end c
					}
					cdata(CDATA_X_ORG, c) = cdata(CDATA_X, c)
					cdata(CDATA_Y_ORG, c) = cdata(CDATA_Y, c)
					txt lang(cdatan(CDATAN_NAME, c) + "を入門させた。", cdatan(CDATAN_NAME, c) + " stay" + _s(c) + " at here.")
					if ( cbit(CHARA_BIT_PCC, c) == TRUE ) {
						txtef COLOR_RED
						txtmore
						txt lang("ナズナ「修行にお洒落など不要！さぁ、着替えろ！」", "Nazuna yells, " + cnvtalk("You've no need of fancy wear in training! Now, go change!"))
						cbitmod CHARA_BIT_PCC, c, FALSE
					}
					gdata(GDATA_PETS_IN_DOJO)++
					if ( cdata(CDATA_EXIST, c) != CHAR_STATE_DEAD ) {
						if ( cdatan(CDATAN_RACE, c) == "servant" ) {
							gdata(GDATA_ACTIVE_GOD_PETS)++
						}
					}
					cdata(CDATA_AREA, c) = gdata(GDATA_AREA)
					cbitmod CHARA_BIT_LIVESTOCK, c, TRUE
					relocate_chara c, -1
					cdata(CDATA_RELATION, c) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, c) = RELATION_DISLIKE
					cdata(CDATA_ROLE, c) = ROLE_SPECIAL
					listmax = 0
					buff = lang("ほう、鍛えがいがありそうだ。しばらく預かるぞ。また連れて行きたくなったら勝手に連れ出せばいい。", "Ohh, they seem worth my time. I'll take care of them. If you want them back then feel free to take them.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					snd SOUNDLIST_PAYGOLD1
					cdata(CDATA_GOLD, CHARA_PLAYER) -= 10000
					goto *chat_end
				}
			}
			if ( chatval == 3 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("仲間", "ally")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("自分", "myself")
				listmax++
				buff = lang("魔法使いは普通、詠唱音声を媒体として魔法を行使するのは知っているな？しかし私の指導でマナの込め方を変えれば、視線だろうが触手だろうがあとは本人の根性次第で媒体にできる。さぁ、指導を受けたいのはどいつだ！", "Sorry, this is untranslated sentence.")
				chatesc = 1
				gosub *chat_select
				if ( chatval == 0 ) {
					allyctrl = 3
					gosub *com_ally
					if ( stat != (-1) ) {
						c = stat
						snd SOUNDLIST_OK1
						txtnew
					}
					if ( stat == (-1) ) {
						goto *chat_end
					}
				}
				if ( chatval == 1 ) {
					c = 0
				}
				if ( chatval != 0 & chatval != 1 ) {
					goto *chat_end
				}
				listmax = 0
				buff = lang("そうか…指導についてこられるだろうな？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("音声", "spell")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("糸", "cobweb")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("体液", "fluid")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("触手", "tentacle")
				listmax++
				list(0, listmax) = 4
				listn(0, listmax) = lang("視線", "gaze")
				listmax++
				list(0, listmax) = 5
				listn(0, listmax) = lang("胞子", "spores")
				listmax++
				list(0, listmax) = 6
				listn(0, listmax) = lang("振動", "vibration")
				listmax++
				list(0, listmax) = 7
				listn(0, listmax) = lang("魔法陣", "magical circle")
				listmax++
				list(0, listmax) = 8
				listn(0, listmax) = lang("粉", "powder")
				listmax++
				list(0, listmax) = 9
				listn(0, listmax) = lang("思念", "vicious mind")
				listmax++
				list(0, listmax) = 10
				listn(0, listmax) = lang("やっぱり今のままでいい", "do not change")
				listmax++
				buff = lang("媒体にしたいのはどれだ？どれも魔法としての性能は変わらんぞ！", "Select new castStyle!!")
				chatesc = 1
				gosub *chat_select
				if ( chatval >= 0 & chatval <= 9 ) {
					cdata(CDATA_CAST_STYLE, c) = chatval
					listmax = 0
					buff = lang("いいだろう…手短に行くぞ！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					snd SOUNDLIST_PAYGOLD1
					cdata(CDATA_GOLD, CHARA_PLAYER) -= 200
					redraw 0
					gosub *screen_draw
					gosub *screen_fade
					gsel BUFFER_BUF
					pos 0, 0
					picload exedir + "graphic\\face1" + devfile + ".bmp", 1
					gsel BUFFER_SCREEN
					snd SOUNDLIST_DING3
					listmax = 0
					buff = lang("こんなものだな。後はその辺りで試してこい！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				goto *chat_end
			}
			if ( chatval == 4 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("仲間", "ally")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("自分", "myself")
				listmax++
				buff = lang("格闘はつまるところ気合だ。私の指導で気合を込めれば全身が武器となりうる！足が無かろうと気合次第で蹴りが可能！闘気を飛ばせば気合次第で睨み殺すことも可能！さぁ、指導を受けたいのはどいつだ！", "Sorry, this is untranslated sentence.")
				chatesc = 1
				gosub *chat_select
				if ( chatval == 0 ) {
					allyctrl = 3
					gosub *com_ally
					if ( stat != (-1) ) {
						c = stat
						snd SOUNDLIST_OK1
						txtnew
					}
					if ( stat == (-1) ) {
						goto *chat_end
					}
				}
				if ( chatval == 1 ) {
					c = 0
				}
				listmax = 0
				buff = lang("そうか…指導についてこられるだろうな？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( chatval == 0 | chatval == 1 ) {
					buff = lang("身に着けたい格闘法を選べ。どれも性能は変わらん！", "Select new meleeStyle!!")
					list(0, listmax) = 0
					listn(0, listmax) = lang("殴打", "punch")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("引っ掻き", "claw")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("蹴り", "kick")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("噛みつき", "bite")
					listmax++
					list(0, listmax) = 4
					listn(0, listmax) = lang("睨み", "gaze")
					listmax++
					list(0, listmax) = 5
					listn(0, listmax) = lang("刺突", "sting")
					listmax++
					list(0, listmax) = 6
					listn(0, listmax) = lang("接触", "touch")
					listmax++
					list(0, listmax) = 7
					listn(0, listmax) = lang("胞子", "spore")
					listmax++
					list(0, listmax) = 8
					listn(0, listmax) = lang("枝", "branch")
					listmax++
					list(0, listmax) = 9
					listn(0, listmax) = lang("投げ", "strike")
					listmax++
					list(0, listmax) = 10
					listn(0, listmax) = lang("体当たり", "rush")
					listmax++
					list(0, listmax) = 11
					listn(0, listmax) = lang("ハサミ", "scissors")
					listmax++
					list(0, listmax) = 12
					listn(0, listmax) = lang("殴打と蹴りの混合", "punch and kick")
					listmax++
					list(0, listmax) = 13
					listn(0, listmax) = lang("やっぱり今のままでいい", "do not change")
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( chatval >= 0 & chatval <= 12 ) {
						cdata(CDATA_MELEE_STYLE, c) = chatval
						listmax = 0
						buff = lang("いいだろう…手短に行くぞ！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						snd SOUNDLIST_PAYGOLD1
						cdata(CDATA_GOLD, CHARA_PLAYER) -= 200
						redraw 0
						gosub *screen_draw
						gosub *screen_fade
						gsel BUFFER_BUF
						pos 0, 0
						picload exedir + "graphic\\face1" + devfile + ".bmp", 1
						gsel BUFFER_SCREEN
						snd SOUNDLIST_DING3
						listmax = 0
						buff = lang("こんなものだな。後はその辺りで試してこい！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				monka = 0
				repeat 245
					if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
						continue
					}
					if ( cbit(CHARA_BIT_LIVESTOCK, cnt) != TRUE ) {
						continue
					}
					if ( cnt == 0 ) {
						continue
					}
					monka++
				loop
				trcost = monka * 30000 + 10000 - monka * monka * 30
				if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= trcost ) {
					list(0, listmax) = 1
					listn(0, listmax) = lang("訓練する", "Too expensive, but okay.")
					listmax++
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("やめる", "No way!")
				listmax++
				buff = lang("預けている者たちに特別訓練を受けさせたいと言うか…その心意気はよし！" + trcost + "goldかかるが問題なかろう？", "You want the ones I'm watching to take special training... That's the spirit! That'll cost " + trcost + ", not a problem?")
				chatesc = 1
				gosub *chat_select
				if ( chatval != 1 ) {
					listmax = 0
					buff = lang("こちらも道場の存続がかかっているのだ。費用を捻出してこい！", "This is for the sake of continuing this dojo. Go get and get me the money!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= trcost
				repeat 245
					if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
						continue
					}
					if ( cbit(CHARA_BIT_LIVESTOCK, cnt) != TRUE ) {
						continue
					}
					if ( cnt == 0 ) {
						continue
					}
					tc = cnt
					modgrowth tc, SKILL_ATTR_STR, 25
					modgrowth tc, SKILL_ATTR_CON, 25
					modgrowth tc, SKILL_ATTR_DEX, 25
					modgrowth tc, SKILL_ATTR_PER, 25
					modgrowth tc, SKILL_ATTR_LER, 25
					modgrowth tc, SKILL_ATTR_WIL, 25
					modgrowth tc, SKILL_ATTR_MAG, 25
					modgrowth tc, SKILL_ATTR_CHA, 25
					repeat 112 - 100, 100
						if ( sorg(cnt, tc) == 0 ) {
							continue
						}
						modgrowth tc, cnt, 15
					loop
					repeat 191 - 150, 150
						if ( sorg(cnt, tc) == 0 ) {
							continue
						}
						modgrowth tc, cnt, 15
					loop
					repeat 302 - 300, 300
						if ( sorg(cnt, tc) == 0 ) {
							continue
						}
						modgrowth tc, cnt, 15
					loop
					r1 = tc
					gosub *charaRefresh
				loop
				txtef COLOR_YELLOW
				txtmore
				txt lang("ナズナ「全員の主能力とスキルの潜在能力を鍛えてやったぞ！」", "Nazuna yells, " + cnvtalk("I trained all of their skill potentials!"))
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_DIALOG_NAZUNA) == 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("断る", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("身代わりを差し出す", "I think not.")
			listmax++
			buff = lang("チェストオオオオオオ！！なぜ門下生が集まらない！？そこのお前、鍛え直してやるから今すぐ入門しろ！", "Why aren't any pupils joining!? Hey you, I'm going to retrain you so enroll now!")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("向上心がないのか！このフヌケめ！", "You've no desire to improve! You wuss!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			allyctrl = 3
			gosub *com_ally
			if ( stat != (-1) ) {
				c = stat
				snd SOUNDLIST_OK1
				txtnew
				if ( c == 0 ) {
					goto *chat_end
				}
				if ( cbit(CHARA_BIT_BODYGUARD, c) != FALSE | cbit(CHARA_BIT_GUARD_TEMP, c) != FALSE ) {
					listmax = 0
					buff = lang("おい！ここは護衛対象の面倒を見る場所ではないぞ！", "You've no desire to improve! You wuss!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( cdata(CDATA_IN_TAGTEAM, c) == (-1) ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, c)
					cell_findspace cdata(CDATA_X, c), cdata(CDATA_Y, c), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end c
				}
				if ( cdata(CDATA_IN_TAGTEAM, c) == 1 ) {
					ttc = cdata(CDATA_TAGTEAM_PARTNER, c)
					cell_findspace cdata(CDATA_X, c), cdata(CDATA_Y, c), 1
					if ( stat == 0 ) {
						txt lang("充分なスペースがない。", "There's no place.")
						goto *chat_end
					}
					cell_setchara ttc, rtval, rtval(1)
					tag_end c
				}
				cdata(CDATA_X_ORG, c) = cdata(CDATA_X, c)
				cdata(CDATA_Y_ORG, c) = cdata(CDATA_Y, c)
				txt lang(cdatan(CDATAN_NAME, c) + "を入門させた。", cdatan(CDATAN_NAME, c) + " stay" + _s(c) + " at here.")
				if ( cbit(CHARA_BIT_PCC, c) == TRUE ) {
					txtef COLOR_RED
					txtmore
					txt lang("ナズナ「修行にお洒落など不要！さぁ、着替えろ！」", "Nazuna yells, " + cnvtalk("You've no need of fancy wear in training! Now, go change!"))
					cbitmod CHARA_BIT_PCC, c, FALSE
				}
				gdata(GDATA_PETS_IN_DOJO)++
				if ( cdata(CDATA_EXIST, c) != CHAR_STATE_DEAD ) {
					if ( cdatan(CDATAN_RACE, c) == "servant" ) {
						gdata(GDATA_ACTIVE_GOD_PETS)++
					}
				}
				cdata(CDATA_AREA, c) = gdata(GDATA_AREA)
				cbitmod CHARA_BIT_LIVESTOCK, c, TRUE
				relocate_chara c, -1
				cdata(CDATA_RELATION, c) = RELATION_DISLIKE
				cdata(CDATA_RELATION_ORG, c) = RELATION_DISLIKE
				cdata(CDATA_ROLE, c) = ROLE_SPECIAL
				listmax = 0
				buff = lang("なんだ、このなっていないペットは！こいつは預かるぞ。みっちりと鍛えてやろう！", "What's the meaning of this half-hearted pet! I'll take them. I'll train them from the ground up!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_DIALOG_NAZUNA) = 1000
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 522 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("うちのおじいちゃん、よくパーティーを開いてるんだけどいつも音楽チケットが足りなくて困ってるんだ。", "My Grandpa, he always opens up parties and always runs out of music tickets.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		item_find ITEM_ID_MUSIC_TICKET, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 8000 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("チケット8000枚を交換する", "Here, take 8000 tickets.")
				listmax++
			}
		}
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 500 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("チケット500枚を交換する", "Here, take 500 tickets.")
				listmax++
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("交換できるほど持ってない", "I think not.")
		listmax++
		buff = lang("ねぇ、音楽チケットとなにかアイテムを交換しない？", "Hey, want to trade some tickets for stuff?")
		gosub *chat_select
		if ( chatval == 2 ) {
			snd SOUNDLIST_PAYGOLD1
			inv(INV_ITEM_NUM, ci) -= 8000
			flt
			itemcreate -1, ITEM_ID_BOTTLE_SPEED_UPPER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			listmax = 0
			buff = lang("そんなにくれるの？じゃあこれをどうぞ！", "You'll give this many? Then here you go!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			snd SOUNDLIST_PAYGOLD1
			inv(INV_ITEM_NUM, ci) -= 500
			flt
			itemcreate -1, ITEM_ID_CARD_PACK, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			listmax = 0
			buff = lang("この交渉、成立だね！このカードパックをどうぞ！", "This was a successful negotiation! Here's a card pack!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("そうなの？じゃあいいや。", "Really? Then fine.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 577 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_MAGE) == 1 ) {
			if ( gdata(GDATA_FLAG_SUB_MAGE_1) == 0 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("ノルマを貰う", "Do you have any tasks for me?")
				listmax++
			}
			else {
				list(0, listmax) = 2
				listn(0, listmax) = lang("ノルマを報告する", "About my task.")
				listmax++
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("私は魔術士ギルドの調査員。ギルドメンバーのサウスティリスでの功績を調査しているのです。", "Welcome to the Mages Guild, " + ranktitle(8) + " " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "... Please come in.")
		gosub *chat_select
		if ( chatval == 1 ) {
			gdata(GDATA_FLAG_SUB_MAGE_1) = 1
			gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 75 - gdata(GDATA_RANK_GUILD) / 200
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("良い心がけです。あなたにランク昇格のためのノルマを与えましょう。世界に散らばった古書物を解読し、納入してください。" + gdata(GDATA_FLAG_GUILD_MAGE_NORMA) + "ギルドポイントが集まったら、昇進のための審査会に報告してあげましょう。", "Sure, there're plenty of tasks for you. For now, collect the ancient books and decrypt them. When you gather " + gdata(GDATA_FLAG_GUILD_MAGE_NORMA) + " guild points, we will think about your promotion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			if ( gdata(GDATA_FLAG_GUILD_MAGE_NORMA) > 0 ) {
				listmax = 0
				buff = lang("どうしました？まだノルマが残っていますよ。", "You have your task to complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			gdata(GDATA_FLAG_SUB_MAGE_1) = 0
			flt 51 - gdata(GDATA_RANK_GUILD) / 200
			flttypemajor = FILTER_ITEM_SPELLBOOK
			itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000 - gdata(GDATA_RANK_GUILD) + 1000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(4 - gdata(GDATA_RANK_GUILD) / 2500, 1, 4)
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			modrank 8, 500, 8
			listmax = 0
			buff = lang("確かにノルマの達成を確認しました。早速本部にあなたの功績を報告しましょう。", "You've done very well. I will give a report of your achievement to the council.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 576 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_THIEF) == 1 ) {
			if ( gdata(GDATA_FLAG_SUB_THIEF_1) == 0 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("ノルマを貰う", "Do you have any tasks for me?")
				listmax++
			}
			else {
				list(0, listmax) = 2
				listn(0, listmax) = lang("ノルマを報告する", "About my task.")
				listmax++
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("あたしは盗賊ギルドの調査員…ギルドメンバーのサウスティリスでの功績を調査しているのよ…。", "Welcome to the Thieves Guild, " + ranktitle(8) + " " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "... Please come in.")
		gosub *chat_select
		if ( chatval == 1 ) {
			gdata(GDATA_FLAG_SUB_THIEF_1) = 1
			gdata(GDATA_FLAG_GUILD_THIEF_NORMA) = (10000 - gdata(GDATA_RANK_GUILD)) * 6 + 1000
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("良い心がけね。あなたにランク昇格のためのノルマを与えるわ。他人の所有物を奪い取り、盗品を売り払いなさい。金貨" + gdata(GDATA_FLAG_GUILD_THIEF_NORMA) + "枚相当の盗品を売ったら、昇進のための審査会に報告してあげる…。", "Alright. The Thieves Guild wants you to put stolen goods into circulation. Steal some goods and sell them to any vendors. When the sum of your earning reachs " + gdata(GDATA_FLAG_GUILD_THIEF_NORMA) + " gold pieces, we will think about your promotion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			if ( gdata(GDATA_FLAG_GUILD_THIEF_NORMA) > 0 ) {
				listmax = 0
				buff = lang("どうしたの？まだノルマが残っているわ…。", "You have your task to complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			gdata(GDATA_FLAG_SUB_THIEF_1) = 0
			flt 51 - gdata(GDATA_RANK_GUILD) / 200
			flttypemajor = FILTER_FURNITURE
			itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000 - gdata(GDATA_RANK_GUILD) + 1000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(3 - gdata(GDATA_RANK_GUILD) / 3000, 1, 3)
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			modrank 8, 500, 8
			listmax = 0
			buff = lang("確かにノルマの達成を確認したわ。早速本部にあなたの功績を報告するわね…。", "You've done very well. I will give a report of your achievement to the council.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 575 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_GUILD_FIGHTER) == 1 ) {
			if ( gdata(GDATA_FLAG_SUB_FIGHTER_1) == 0 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("ノルマを貰う", "Do you have any tasks for me?")
				listmax++
			}
			else {
				list(0, listmax) = 2
				listn(0, listmax) = lang("ノルマを報告する", "About my task.")
				listmax++
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		buff = lang("私は戦士ギルドの調査員。ギルドメンバーのサウスティリスでの功績を調査しているのだ。", "Welcome to the Fighters Guild, " + ranktitle(8) + " " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "... Please come in.")
		gosub *chat_select
		if ( chatval == 1 ) {
			repeat
				flt cdata(CDATA_LEVEL, CHARA_PLAYER) + 10
				characreate MAX_CHARA_NC, CREATURE_ID_BUG, -3, 0
				if ( refchara(cdata(CDATA_ID, MAX_CHARA_NC), DBSPEC_CHARA_RARE) < 70 ) {
					continue
				}
				if ( cdata(CDATA_RELATION, MAX_CHARA_NC) != RELATION_ENEMY ) {
					continue
				}
				if ( cdata(CDATA_QUALITY, MAX_CHARA_NC) >= FIX_QUALITY_MIRACLE ) {
					continue
				}
				if ( cdata(CDATA_LEVEL, MAX_CHARA_NC) > cdata(CDATA_LEVEL, CHARA_PLAYER) * 3 ) {
					continue
				}
				break
			loop
			gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) = 3
			gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF) = cdata(CDATA_ID, MAX_CHARA_NC)
			chara_vanquish MAX_CHARA_NC
			gdata(GDATA_FLAG_SUB_FIGHTER_1) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("良い心がけだ。お前にランク昇格のためのノルマを与えよう。" + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + "をあと" + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + "匹討伐してからまた来なさい。本部に報告してあげよう。", "There's a task for you. Go find and slay " + gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) + " " + refchara(gdata(GDATA_FLAG_GUILD_FIGHTER_THIEF), DBSPEC_CHARA_NAME_ORG, 1) + ". Then we will think about your promotion.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_unique
		}
		if ( chatval == 2 ) {
			if ( gdata(GDATA_FLAG_GUILD_FIGHTER_NORMA) > 0 & 0 == 0 ) {
				listmax = 0
				buff = lang("どうした？まだノルマが残っているぞ。", "You have your task to complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_unique
			}
			gdata(GDATA_FLAG_SUB_FIGHTER_1) = 0
			flt 51 - gdata(GDATA_RANK_GUILD) / 200, calcfixlv(FIX_QUALITY_GREAT)
			flttypemajor = FILTER_WEAPON
			itemcreate -1, ITEM_ID_DUMMY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10000 - gdata(GDATA_RANK_GUILD) + 1000
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(4 - gdata(GDATA_RANK_GUILD) / 2500, 1, 4)
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			modrank 8, 500, 8
			listmax = 0
			buff = lang("確かにノルマの達成を確認した。早速本部にお前の功績を報告しよう。", "You've done very well. I will give a report of your achievement to the council.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1014 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 820 ) {
			listmax = 0
			buff = lang("この分身体の役目は終わったね。君が望むのなら手合わせをしてもいい。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("腕試しには丁度いいんじゃないかな。なにせ君が見ているこの私達は、ソピアーよりやや強い程度でしかないからね。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 825
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 825 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("まだ見ていないところがある", "Yes.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("外に出して", "No.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("ソピアーをどう思う？", "No.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("君を知りたい", "No.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("聞き流したから何をすればいいかわからない", "No.")
			listmax++
			buff = lang("ラスキリスの谷間から出たいなら私達が谷の外に飛ばしてあげてもいいよ。もちろん、谷の景色を見ながらかつての旅人の道を辿って出ていってもいい。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘から逃げるわけにはいかない！", "You can not run away from the duel!")
					goto *chat_end
				}
				listmax = 0
				buff = lang("さようなら。君のことは忘れないよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_TELEPORT_AREA) = gdata(GDATA_WORLD), 1
				msg_halt
				gosub *screen_draw
				levelexitby = MAP_EXIT_TELEPORT
				goto *map_exit
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("昔のあの子は特に知識欲が強くてね。知られざる父のことを知ろうとしすぎて大失敗したのは聞いたかな？あの時はもう皆大慌てで天から落っこちたソピアーを探して、見つかったと思ったら歪んだ情熱で自分を見失ってたせいで天に戻れなくなってたりしていて大変だったんだからね。結局、歪んだ情熱をちぎり捨てて連れ帰ったんだけど、そしたら今度は捨てた部分が人型になって活動し始めてね。妹から生まれた分身を滅するのも忍びないから自由に生きさせてたんだけど、それがエンテュメイシスになっちゃって。戦ったから知ってるよね、あの神の間にいた混沌の女神だよ。本当に、手がかかる子。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("まぁ、お馬鹿な末っ子は可愛い、というのが私達の総意だよ。おっと、私達から聞いたことはソピアーに言わないでおいてね、機嫌が悪くなるから。別の世界だし黙っておけば常闇の杖でもわからないよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 3 ) {
				listmax = 0
				buff = lang("私達は28柱が結束して生まれた合体神だよ。交合のミクシスと相互混合のシュンクラーシスと統合のシュネシスの3柱が中心となっているね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("その他の内訳は、思念のエンノイア、理法のヌース、真理のアレーテイア、言葉のロゴス、生命のゾーエー、人間のアントローポス、教会のエクレーシア。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("深みのビュテイオス、不壊のアゲーラトス、配慮のヘンノーシス、成長のアウトピュエース、快楽のヘードネー、不動のアキネートス、独り子のモノゲーネス、浄福のマカリア。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("仲介のパラクレートス、信仰のピスティス、父性のパトリコス、希望のエルピス、母性のメートリコス、愛のアガペー、永遠のアエイヌース、伝道のエクレーシアスティコス、幸福のマカリオーテス、意欲のテレートス。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("別に覚えてくれなくていいよ。名前が長くて多くて似ていて大変だろう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 4 ) {
				listmax = 0
				buff = lang("…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("この世界が忘れられないように、記憶を共有する処置をした。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("君には混沌の巨城の最深層で戦うべき相手がいる。いいね？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 605 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 820 | gdata(GDATA_FLAG_MAIN) == 825 ) {
			listmax = 0
			buff = lang("君に渡しておきたいものがあるんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_GREEN
			txt lang("[エルシアの首飾り]と[クイーン・セドナの乗船券]を手に入れた！", "You obtain Elsian Necklace and Boarding ticket of Queen Sedona!")
			snd SOUNDLIST_COMPLETE1
			listmax = 0
			buff = lang("これらは、ボクがこの地でガイドした最後の旅人の持ち物だ。最後のループで、最期にボクに託された。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("何の役にも立たないだろう。それでも君に持っていてほしい。これらを持つにふさわしいのは、君しかいないと思うから。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("頑張って", "Yes.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("なんか恥ずかしいな", "No.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("おい馬鹿やめろ", "No.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("無言で腹を殴る", "No.")
			listmax++
			buff = lang("…じゃあボクはイルヴァの民達にガイアス・ヴィスの話をしてくるよ。ああ、そうだ！ついでに君が混沌と戦うことも君の知り合いたちに教えてあげよう！", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 3 ) {
				snd SOUNDLIST_BASH1
				listmax = 0
				buff = lang("ぶぎぇっ！？何するのさ" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_MAIN) = 830
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_AIMWELL ) {
			if ( gdata(GDATA_FLAG_MAIN) == 780 ) {
				listmax = 0
				buff = lang("君は今！ガイアス・ヴィスへの第一歩を踏み出した！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ここまで長かった…この世界ではまだ1年程度しか経っていないけれど、ボクはループ込みで10年以上対応にあたっていたんだ。ようやく君という因子を見つけて連れてくることができたよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("うーん。でもまだ君の認識が次元を超えて追いついていないみたいだね。おそらく君の目には、多くのものがイルヴァのものに置き換わって見えるだろう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("さて…見る限り、依然として廃忘獣がはびこっているようだね…。あのお方は大丈夫だろうか。一緒に探しに行こう！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("えっ。あのお方が誰かって？ああ、まだ説明してなかったっけ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("叡智を司る中級神であるソピアーは知っているよね。あのお方はソピアーの兄姉たち28柱が合神した、超越神だよ。幾多の神々を吸収した混沌の神に対抗するための措置だったらしいね。ちなみにソピアーは一回堕天している影響でうまく合神できなかったんだって。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("あのお方はガイアス・ヴィスの実情を知って、色々と便宜を図ってくれた。今は直接、忘却の女神と戦っているはずだ。こうしてボクが覚えている以上、間違いなく無事なはずなんだけど…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("一度引き返したい場合は、こちら側のロイターを経由してね。そこの炭鉱街…ザイールにいるはずだから。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 790
				goto *chat_end
			}
		}
		if ( gdata(GDATA_AREA) == AREA_VERNIS ) {
			if ( gdata(GDATA_FLAG_MAIN) == 770 ) {
				listmax = 0
				buff = lang("そうそう、君に渡しておくものがあった。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txtef COLOR_GREEN
				txt lang("[記憶の灯]を手に入れた！", "You obtain Light of memory !")
				snd SOUNDLIST_COMPLETE1
				listmax = 0
				buff = lang("それは生命情報を記録するアストラル光の塊。記憶をバックアップしてくれるから、記憶消去攻撃を受けてもかなり軽減できるらしいよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("さらに、世界を行き来するための道具でもあるんだ。ロイターに向かって使ってみて。現時点で、彼は誰よりもガイアス・ヴィスとの結びつきが強い。あちら側にも極めて近い存在がいるんだ。記憶の灯はその繋がりを照らして導いてくれるよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 780
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 765 ) {
			listmax = 0
			buff = lang("さぁ、時は来た！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 770
			list(0, listmax) = 0
			listn(0, listmax) = lang("ああ！", "Yes.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("忘れたなぁ？", "No.")
			listmax++
			buff = lang("ボクに協力してほしい。前に話したことは憶えているよね？", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("忘却の女神の力がここまで及んでいるなんて！もう、時間がないみたいだね…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("ヴェルニースの酒場に来てほしい。今残っている場所では、あそこが一番つながりの強いところなんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 525 ) {
				listmax = 0
				buff = lang("その時が来たら、君にも協力してもらいたいと思っているんだ。そしてガイアス・ヴィスの話も憶えておいてほしい。人も世界も、完全に忘れられた時に本当の死を迎えるのだから。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_MAIN) == 520 ) {
				listmax = 0
				buff = lang("いやぁ、君が宇宙人の相手をしてくれてようやく解放されたよ。それじゃあ本題について話すよ。…君には真の脅威について知っておいて欲しいんだ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("実は、ボクはこの世界の出身じゃない。ガイアス・ヴィスという別世界から来たんだ。妖精族の中でもエルンと呼ばれる特別な種族で、あちら側では訛ってエルフとも呼ばれていたよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ガイアス・ヴィスはこのイルヴァの姉妹世界。同じようにエーテルの風が吹き、人々は異形の森を排除しようとしていた。けれどその矢先、突如として現れた忘却の化身に侵略され、世界のおよそ95％が記憶ごと存在消滅しガイアス・ヴィスは滅んだ。消滅した部分には何があったのか、どんな人々が暮らしていたのか、ボクにももう思い出せない。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そんな時、絶望するボクの前に現れたのがあのお方だったんだ！ボクはあのお方の力を借りて時間を巻き戻し、運命を変えようと努力した。イレギュラーと成り得る旅人を何人も導き、何度も共に戦った。戦いの中で、ボクの先祖である運命の女神ノルンの力も覚醒した。同じ女神の血統を持つ巨人族・小人族の末裔とも協力した。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("でも何千回繰り返してもダメだった！なんとか街ひとつとその周囲を含むごく一部の地域の存在は守れた程度で、消滅する運命にある世界の大半には干渉すらできなかった。導いた旅人たちはどこへも行けず、疲弊し、考えるのをやめ、最後はいつも忘却の海へと消えていった。君にわかるかい、この絶望が。この怒りが。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そしてボクはあのお方から聞いた。イルヴァという姉妹世界も忘却の化身に狙われていると。ガイアス・ヴィスの悲劇を繰り返してはいけない。そう思ったボクらはこちら側に移り住み、迎撃準備を始めた。忘却の化身に滅ぼされたいくつもの世界から生存者をかき集め、運命変革因子として揃えた。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そうして奴らの尖兵を撃退することには成功したんだ。一匹は完全に抹殺し、一匹はエウダーナの老魔導士が支配して制御に成功した。残り一匹、死にぞこないがサウスティリスに潜伏しているみたいだけど、あれはもう本来の力を取り戻せないから大丈夫だよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("けれどまだ事態は解決していない。わずかに運命が更新されたに過ぎない。…いつか、必ず奴らの本隊がやってきて世界は滅びる運命にある。でもそんなことは承知の上。ボクは最後まで足掻いてみせる。そう、どんな手を使ってもね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 525
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) < 520 ) {
				listmax = 0
				buff = lang("あー！いいところに来てくれたね。ちょっとここに潜る途中で見知らぬ宇宙人に絡まれちゃってさ。ボクがガイドだからってしつこく聞いてくるんだけど、宇宙人が探してる人間なんて知るわけないよ。誰だよ黒髪の少年で名前がクロヤンって。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("君に話があるんだけどね…先にあのしつこい宇宙人をここから連れ出してよ。迷惑なんだよね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_GUARDIAN_SHRINE ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("する", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("しない", "I think not.")
			listmax++
			buff = lang("ノースティリスへ移動する？", "Huh.")
			gosub *chat_select
			if ( chatval == 0 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				listmax = 0
				buff = lang("はい、ノースティリスへご案内〜。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				animeid = ANIMATION_GENE
				anic = 0
				gosub *anime
				gdata(GDATA_TELEPORT_AREA) = AREA_NORTH_TYRIS
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_NORMAL
				prevsekai = gdata(GDATA_WORLD)
				gdata(GDATA_WORLD_RENEW) = TRUE
				gdata(GDATA_WORLD) = AREA_NORTH_TYRIS
				gdata(GDATA_WORLD_X) = adata(ADATA_X, 25), adata(ADATA_Y, 25)
				fixtransfermap = 1
				chatteleport = 1
				goto *chat_end
			}
			listmax = 0
			buff = lang("冷やかしは迷惑だよ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_LARNA ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("する", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("しない", "I think not.")
			listmax++
			buff = lang("ロストイルヴァへ移動する？", "Huh.")
			gosub *chat_select
			if ( chatval == 0 ) {
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				listmax = 0
				buff = lang("はい、ロストイルヴァへご案内〜。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				animeid = ANIMATION_GENE
				anic = 0
				gosub *anime
				gdata(GDATA_TELEPORT_AREA) = AREA_LOST_YLVA
				gdata(GDATA_TELEPORT_LEVEL) = 1
				levelexitby = MAP_EXIT_NORMAL
				prevsekai = gdata(GDATA_WORLD)
				gdata(GDATA_WORLD_RENEW) = TRUE
				gdata(GDATA_WORLD) = AREA_LOST_YLVA
				gdata(GDATA_WORLD_X) = adata(ADATA_X, 79), adata(ADATA_Y, 79)
				fixtransfermap = 1
				chatteleport = 1
				goto *chat_end
			}
			listmax = 0
			buff = lang("冷やかしは迷惑だよ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 390 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("わかった", "Alright.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("逃げ出さずに待っていろ", "I think not.")
			listmax++
			buff = lang("ああ、こんなところにいたんだね！ちょうどいい。神々の力でノースティリスへ続く道が開通したんだ。君も地上へ降りたいなら中央神殿までおいでよ。待ってるよ。", "Long time no see... I guess? You became quite an adventurer since the last time I saw you.  There was merit in me showing you the way.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("はぁ。君は何を言っているんだい？", "Hm? I've too much to do to go along with your foolery.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("それじゃ、またね。", "I'll be off.  There are lots of other adventurers I need to guide. Man, so busy.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 394
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 363 ) {
			listmax = 0
			buff = lang("久しぶり…になるのかな？しばらく見ないうちに立派な冒険者になったみたいだね、ボクも案内をした甲斐があったよ。", "Long time no see... I guess? You became quite an adventurer since the last time I saw you.  There was merit in me showing you the way.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("わかった", "Alright.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("どちらさまですか？", "I think not.")
			listmax++
			buff = lang("突然召喚されて戸惑っているのかな？ここはエウダーナ上空を浮遊している大陸、ロストイルヴァさ。君より先に呼ばれた人たちがそこの遺跡に大勢いるし、詳しい話は彼らから訊いてみるといいよ。", "I guess you're confused as to why you were summoned? This is the floating continent above Eulderna, LostIrva. There are plenty of people in those ruins whom were called before you so you can ask them about the details.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("うん？悪いけど君の冗談に付き合っているほど暇じゃないんだ。", "Hm? I've too much to do to go along with your foolery.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("それじゃあボクはこれで。他にも案内しなきゃいけない冒険者がいるんだ。ああ、忙しい。", "I'll be off.  There are lots of other adventurers I need to guide. Man, so busy.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 364
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 606 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 365 ) {
			listmax = 0
			buff = lang("よう、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。こちらはガベラだ。あー、このメッセージはエウダーナ上空の浮遊大陸でお前を発見した場合に、自動再生されるよう設定してある。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("驚いたぞ、突然お前の現在座標がエウダーナの上空に移動したんでな。現在、浮遊大陸は周囲を特殊な結界で囲まれており、人間はおろか機械も魔物も通行ができない状態だ。もっとも、このメルガストは次元歪曲航法により結界自体を無視して行き来が可能なわけだが…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("なお、このメッセージの30秒後に本機は現空域を離脱する。探索の最中かもしれんが、取り残されたくなければさっさと乗るべきだぞ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			snd SOUNDLIST_BOLT1
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			gdata(GDATA_TELEPORT_AREA) = AREA_SOUTH_TYRIS
			gdata(GDATA_TELEPORT_LEVEL) = 1
			levelexitby = MAP_EXIT_TELEPORT
			prevsekai = gdata(GDATA_WORLD)
			gdata(GDATA_WORLD_RENEW) = TRUE
			gdata(GDATA_WORLD) = AREA_SOUTH_TYRIS
			gdata(GDATA_RUN_DIR) = 1
			gdata(GDATA_LAST_EDGE_X) = -1
			fixtransfermap = 0
			gdata(GDATA_FLAG_MAIN) = 366
			dbg_skipevent = 1
			gdata(GDATA_HOUR) += 2
			gosub *advance_hour
			dbg_skipevent = 0
			chatteleport = 1
			goto *chat_end
		}
		else {
			if ( gdata(GDATA_FLAG_MAIN) >= 368 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("する", "I think not.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("しない", "I think not.")
				listmax++
				buff = lang("登録データとの照合完了。次元歪曲シーケンスを開始するか？", "Huh.")
				gosub *chat_select
				if ( chatval == 0 ) {
					if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
						snd SOUNDLIST_FAIL1
						txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
						goto *chat_end
					}
					listmax = 0
					buff = lang("了解。搭乗を確認したら発進する。なお、到着まで約2時間を要する。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					redraw 0
					gosub *screen_draw
					gosub *screen_fade
					snd SOUNDLIST_BOLT1
					gsel BUFFER_BUF
					pos 0, 0
					picload exedir + "graphic\\face1" + devfile + ".bmp", 1
					gsel BUFFER_SCREEN
					if ( gdata(GDATA_AREA) == AREA_ULM_LESON ) {
						gdata(GDATA_TELEPORT_AREA) = AREA_SOUTH_TYRIS
					}
					if ( gdata(GDATA_AREA) == AREA_ANCIENT_GARDEN ) {
						gdata(GDATA_TELEPORT_AREA) = AREA_LOST_YLVA
					}
					gdata(GDATA_TELEPORT_LEVEL) = 1
					levelexitby = MAP_EXIT_NORMAL
					prevsekai = gdata(GDATA_WORLD)
					if ( gdata(GDATA_AREA) == AREA_ULM_LESON ) {
						gdata(GDATA_WORLD_RENEW) = TRUE
						gdata(GDATA_WORLD) = AREA_SOUTH_TYRIS
					}
					if ( gdata(GDATA_AREA) == AREA_ANCIENT_GARDEN ) {
						gdata(GDATA_WORLD_RENEW) = TRUE
						gdata(GDATA_WORLD) = AREA_LOST_YLVA
					}
					if ( gdata(GDATA_AREA) == AREA_ULM_LESON ) {
						gdata(GDATA_WORLD_X) = adata(ADATA_X, 55), adata(ADATA_Y, 55)
					}
					if ( gdata(GDATA_AREA) == AREA_ANCIENT_GARDEN ) {
						gdata(GDATA_WORLD_X) = adata(ADATA_X, 71), adata(ADATA_Y, 71)
					}
					fixtransfermap = 1
					dbg_skipevent = 1
					gdata(GDATA_HOUR) += 2
					gosub *advance_hour
					dbg_skipevent = 0
					chatteleport = 1
					goto *chat_end
				}
				listmax = 0
				buff = lang("冷やかしか…？休止状態に移行する。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 841 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 530 ) {
				listmax = 0
				buff = lang("ヨロテオトル…神の心臓を失っても、神力がある限り身体は維持できます。心配なんかしなくていいんですよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 525 ) {
				listmax = 0
				buff = lang("あれ、君は…また会いましたね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("覚えていませんか？ノイエル・メイルーン間で君たちを運んだ犬神です。大氷河を駆け抜けるのはさすがに疲れるので悪魔戦には参加できませんでしたが、君たちの戦いを見守っていましたよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("今ここにいる理由ですか？自分も何かできないかと思い、神の間を目指してこの大陸に来たのですが…最深層に神が入ると、神力を吸われてしまうと警告されましてね。ええ、途中で会った妖精の方から聞いた話です。それで仕方がなく、後続のためにここで露払いをしているというわけです。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ここで会ったのも縁…まぁ、あの時君と一緒にいた銀髪の子にも会ったのですが、話を聞いてもらえなかったので。自分は、地元のメイルーンを救ってくれた君に賭けてみたい。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("(突然、" + name(tc) + "は自らの胸を抉りぬいた！)", "(" + he(tc) + " ignores you...)")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				snd SOUNDLIST_STAB
				cdata(CDATA_CONDITION_BLEED, tc) = 30
				listmax = 0
				buff = lang("あぐっ…がああ…！び、微力ですがこれを役立ててください。どうか、混沌の神を…！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_EVITEM, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				if ( stat != 0 ) {
					inv(INV_ITEM_PARAM1, ci) = 13
					inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
				}
				snd SOUNDLIST_COMPLETE1
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				gdata(GDATA_FLAG_MAIN) = 530
				autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 612 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 500 ) {
				listmax = 0
				buff = lang("ドラゴン種族殺すべし。慈悲はないわ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 495 ) {
				listmax = 0
				buff = lang("ドラゴンの強い魔力を複数感知したから探索に来てみたんだけど…いやー、きっついわねこの城。やたらとモンスターが強くて、進むのに一苦労よ。これってネフィアの守護者と同じ原理で強化されてるのかしらね？", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("はぁ…そろそろ一度戻って、鍛えなおしてこようかしら。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 500
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) >= 1000 ) {
			listmax = 0
			buff = lang("今回は違ったけれど、もし黒い体に銀の瞳のドラゴンを見つけたら私に教えて。そいつが、私がドラゴンハンターになった理由のひとつなのよ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) == 2 ) {
			listmax = 0
			buff = lang("火山から強大なドラゴンの魔力を感じなくなったわ。あなたが討伐してくれたんでしょ？話を聞かせてもらえる？", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("なるほどね、身体に火を纏った竜だったんだ。うーん…火の鳥だったら私も何羽か見かけたんだけど、擬態してその中に混ざっていたのかしらね？", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あ、そうだ。ドラゴンハンターとして頑張った君にこれを進呈するわ。これからもじゃんじゃん竜を狩っていって！", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) = 1000
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_DRAGON
			flt
			itemcreate -1, ITEM_ID_DRAGON_SLAYER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 15
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) == 1 ) {
			listmax = 0
			buff = lang("ドラゴンの魔力はいまだ健在…一体どこに隠れているのかしら。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) == 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("いるみたい？", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("興味ない", "I think not.")
			listmax++
			buff = lang("ねぇねぇ、ドラゴン狩りに興味はない？この遺跡の南に火山があるんだけど、どうもその最奥に強力なドラゴンがいるみたいなの。", "Huh.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("あ、いるみたいってのはね。私、子供の頃にたちの悪いドラゴンに呪われちゃって体が半竜になってるのよ。で、その代わりと言ったらあれだけど、ドラゴンの発する魔力を感じ取ることができるの。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("火山内部から感じた竜の魔力は尋常じゃなかった。あれは災厄クラスよ。ただ、火山には私も何度か討伐に行ってるんだけど竜の子一匹見つけられないのよね。魔力は確かに感じるから、もしかすると姿を隠す能力を持っているのかもしれないわ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("もしあなたがそれらしいドラゴンを見つけたら、私に代わって討伐してほしいの。これは私からのドラゴンサブクエストよ！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			else {
				listmax = 0
				buff = lang("えーと。そこまではっきり拒否されると、話続けられないっていうかなんというか…。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 621 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) >= 1001 ) {
			listmax = 0
			buff = lang("アリーヴェデルチ！", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbyea
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 1000 ) {
			listmax = 0
			buff = lang("私カラモ、礼ヲ言ワセテクダサイ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("元ノ姿ニ戻シテクレテ…", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("本当ニ、本当ニ…", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("サンキューベリマッチ！！", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) = 1001
			snd SOUNDLIST_DING1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) >= 6 & gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) < 1000 ) {
			listmax = 0
			buff = lang("アリッス。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) < 6 ) {
			listmax = 0
			buff = lang("ギャオオオオオ！", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 5 ) {
				list(0, listmax) = 6
				listn(0, listmax) = lang("薬をぶつける", "Here, take these medals.")
				listmax++
				list(0, listmax) = 5
				listn(0, listmax) = lang("薬を浴びせる", "Here, take these medals.")
				listmax++
				list(0, listmax) = 4
				listn(0, listmax) = lang("薬を塗る", "Here, take these medals.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("薬を注射する", "Here, take these medals.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("薬を飲ませる", "Here, take these medals.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("薬を渡す", "Here, take these medals.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("薬を上にあげる", "I think not.")
				listmax++
				buff = lang("(" + cdatan(CDATAN_NAME, tc) + "は不思議そうにこちらを見ている)", "That shiney thing you've got there... It isn't a small medal is it? ...Please, can you give it to me?")
				gosub *chat_select
				if ( chatval < 1 | chatval > 6 ) {
					if ( chatval == 0 ) {
						txt lang("ハイ、上にあげたー。", "ngh... Sorry for asking the impossible.")
					}
					listmax = 0
					buff = lang("…？？？", "ngh... Sorry for asking the impossible.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 6 ) {
					snd SOUNDLIST_CRUSH2
				}
				if ( chatval == 5 | chatval == 4 ) {
					snd SOUNDLIST_WATER2
				}
				if ( chatval <= 3 & chatval >= 1 ) {
					snd SOUNDLIST_DRINK1
				}
				txt lang("おや？" + cdatan(CDATAN_NAME, tc) + "の様子が…！？", "Oh? " + cdatan(CDATAN_NAME, tc) + "'s act strange ...")
				msg_halt
				animeid = ANIMATION_GENE
				anic = tc
				gosub *anime
				cdata(CDATA_PIC, tc) = 642 + 19 * 1000
				gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) = 6
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 622 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 570 ) {
				listmax = 0
				buff = lang("ああ、アリスは私が置いてきたわ。ハッキリ言ってこのネフィアにはついていけない…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 565 ) {
				listmax = 0
				buff = lang("森の王様の力も借りてここまで来たけれど、ちょっと…いや、かなり厳しくなってきたわね。あなたはまだ余裕がありそうね…すごいわ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("蟲の研究ができなくなるのは困るから、私たちは神の間を真面目に目指しているのよ。これでも一応はね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 570
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) >= 1000 ) {
			listmax = 0
			buff = lang("娘には内緒よ？", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 6 ) {
			listmax = 0
			buff = lang("薬をあげてきてくれたのね！さて、アリスの様子は…。", "Oh...Thank you so much! Hm, wouldn't be right to take it for nothing. I only have this but will you accept it?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			p = findcharala(CREATURE_ID_ALICE_THE_BIG_ANT)
			if ( p == 0 ) {
				listmax = 0
				buff = lang("し、死んどる…。", "Kill them all.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("！？アリス…その姿は…？", "Kill them all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("完全に元通りになれたのね！一時はどうなることかと思ったのよ。…人語もちゃんと喋れるみたいね。本当によかった。", "Kill them all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん、ありがとう！これで娘に嫌われずに済むわ。預かってる最中にあんな姿に変異させたことが知れたら…おおぅ。", "Kill them all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("え？アリスは娘じゃないわよ。そんなこと一度も言ってないけど…？アリスは森で迷いアリになっていたところを私の娘に拾われたの。今では娘の大事なペット…いや子分かしら？そりゃあ元に戻ったと言っても、結局はアリのままの姿よ。", "Kill them all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("とにかく、これは報酬＆口止め料よ。本当に助かったわ！", "Kill them all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_SCROLL_GAIN_ATTRIBUTE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5000
			flt
			itemcreate -1, ITEM_ID_POTION_DESCENT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			flt
			nostack = 1
			itemcreate -1, ITEM_ID_MATERIAL_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			inv(INV_ITEM_MATERIAL, ci) = ITEM_MATERIAL_GRIFFON
			gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) = 1000
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 5 ) {
			listmax = 0
			buff = lang("悪いけど、アリスに薬をあげるところまでお願い。", "Oh...Thank you so much! Hm, wouldn't be right to take it for nothing. I only have this but will you accept it?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) >= 1 & gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) < 5 ) {
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 1 ) {
				listmax = 0
				buff = lang("それじゃあ。持ってきてほしいアイテムなんだけど、いくつかあるから簡単そうなものからお願いしようかしら。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) <= 2 ) {
				gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) = 2
				listmax = 0
				buff = lang("そうね。今回持ってきてほしいアイテムは変化の杖よ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 3 ) {
				listmax = 0
				buff = lang("そうね。今回持ってきてほしいアイテムは復活の書よ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 4 ) {
				listmax = 0
				buff = lang("今回で最後ね！持ってきてほしいアイテムは変異治療のポーションよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 2 ) {
				item_find ITEM_ID_ROD_CHANGE_CREATURE, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 3 ) {
				item_find ITEM_ID_BOOK_RESURRECTION, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			}
			if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 4 ) {
				item_find ITEM_ID_POTION_CURE_MUTATION, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			}
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
					list(0, listmax) = 1
					listn(0, listmax) = lang("渡す", "Here, take these medals.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("渡さない", "I think not.")
					listmax++
					buff = lang("あら？もしかして今持っていたりするんじゃない？", "That shiney thing you've got there... It isn't a small medal is it? ...Please, can you give it to me?")
					gosub *chat_select
					if ( chatval != 1 ) {
						listmax = 0
						buff = lang("そう…？急いでもらえると助かるわ。", "ngh... Sorry for asking the impossible.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					snd SOUNDLIST_EQUIP1
					inv(INV_ITEM_NUM, ci)--
					gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE)++
					listmax = 0
					buff = lang("ありがとうねぇ、アリだけに。", "Thank you so much!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) < 5 ) {
						goto *chat_end
					}
					listmax = 0
					buff = lang("えーと…持ってきてもらったアイテムたちを、館長さんに教えてもらった通りの配分で合成して…さらに私の調達してきたゴッドロイヤルゼリーと混ぜれば…。", "Oh...Thank you so much! Hm, wouldn't be right to take it for nothing. I only have this but will you accept it?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					redraw 0
					gosub *screen_draw
					gosub *screen_fade
					gsel BUFFER_BUF
					pos 0, 0
					picload exedir + "graphic\\face1" + devfile + ".bmp", 1
					gsel BUFFER_SCREEN
					snd SOUNDLIST_COMPLETE1
					listmax = 0
					buff = lang("よし、特殊変異治療薬の完成よ！ちょっと後片付けがあるから、アリスに薬をあげるところまでお願いするわ。", "Oh...Thank you so much! Hm, wouldn't be right to take it for nothing. I only have this but will you accept it?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					txtef COLOR_GREEN
					txt lang("[特殊変異治療薬]を手に入れた。", "You obtain EX mutation treatment!")
					snd SOUNDLIST_WRITE1
					txtef COLOR_GREEN
					txt lang("ジャーナルが更新された。", "Your journal has been updated.")
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あの子のためにも、可及的速やかに頼むわ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) == 0 ) {
			listmax = 0
			buff = lang("大変なのよ！ある朝、目が覚めたらうちのアリスがあんなおぞましい姿に変身していたの！すっかり人語も喋れなくなって…！", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("手伝わない", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("手伝う", "I think not.")
			listmax++
			buff = lang("ここの館長さんに話を聞いたんだけど、なんでも元の姿に戻すには複数のアイテムが必要らしいのよ。報酬は出すから、アイテム集めを手伝ってくれないかしら？", "Huh.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("ありがとうね！これで蟲の研究に専念できるわぁ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			listmax = 0
			buff = lang("そんな！このままじゃ私、娘に顔向けできないわ…。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 796 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			listmax = 0
			buff = lang("逆境上等！覚悟は上々！お父さんと拳で語り合えるくらい強くなるためにも、このネフィアで修行だっ！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) >= 1003 ) {
			listmax = 0
			buff = lang("武者修行から戻ってきてみても、お母さんまだ虫図鑑読み漁ってるんだから。はぁ…過去の文明のまで合わせたら、何億種書かれているかわかったもんじゃないよ。暇つぶしにその辺のネフィアにでも潜ってこようかな…。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("おかしい。久しぶりに会ったと思ったら、お母さんが挙動不審すぎる。アリスに訊いても口ごもるし、何か隠してるんじゃ…？", "Huh.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("知らない", "I think not.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("ちょっと口止めされてて…", "I think not.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("巨大蟻の管理不行届き", "I think not.")
		listmax++
		buff = lang("あの、すみません。うちの母が何か隠してないか、知りませんか？", "Huh.")
		gosub *chat_select
		if ( chatval >= 0 & chatval <= 2 ) {
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("そう…。ここはやはり、拳で語ってみるしかないか！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("おっけー、おっけー、なるほどね。家族会議でとことん問い詰めてやる！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("また！？今回はちゃんとアリスの面倒見るから大丈夫、ってお母さん言ってたのに！もう、ずぼらなんだから！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) = 1003
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 855 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			listmax = 0
			buff = lang("神には神の思惑があって動いている。盲目的にそれに従うのではなく、我ら定命は自身の考えを持って行動しなければならない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 624 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE) \ 2 == 0 ) {
			if ( gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_YEAR) == gdata(GDATA_YEAR) & gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_DAY) == gdata(GDATA_DAY) & gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_MONTH) == gdata(GDATA_MONTH) & gdata(GDATA_HOUR) == 14 & gdata(GDATA_MIN) >= 30 ) {
				listmax = 0
				buff = lang("時間通りに来たということは…手伝ってくれるということだね。では早速始めようか。なに、ただの単純作業だよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				redraw 0
				gosub *screen_draw
				gosub *screen_fade
				gsel BUFFER_BUF
				pos 0, 0
				picload exedir + "graphic\\face1" + devfile + ".bmp", 1
				gsel BUFFER_SCREEN
				gdata(GDATA_HOUR) = 17
				listmax = 0
				buff = lang("…こんなところだね、お疲れさま。これはお礼だよ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				txtmore
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				if ( gdata(GDATA_FLAG_SUB_VICE_STAFF_RECEIVED) == 0 ) {
					flt
					itemcreate -1, ITEM_ID_VICE_STAFF, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				}
				if ( gdata(GDATA_FLAG_SUB_VICE_STAFF_RECEIVED) != 0 ) {
					if ( gdata(GDATA_MIN) >= 30 & gdata(GDATA_MIN) <= 39 ) {
						flt
						itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						inv(INV_ITEM_PARAM1, ci) = 18
						encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_DARKNESS_BOLT, 200, 0, 1, 0, 1
					}
					if ( gdata(GDATA_MIN) >= 40 & gdata(GDATA_MIN) <= 49 ) {
						flt
						itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						inv(INV_ITEM_PARAM1, ci) = 17
						encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_ELEMENT_SCAR, 200, 0, 1, 0, 1
					}
					if ( gdata(GDATA_MIN) >= 50 & gdata(GDATA_MIN) <= 60 ) {
						flt
						itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						inv(INV_ITEM_PARAM1, ci) = 16
						encadd ci, ENCHANT_ENHANCE_SPELL, 200, 0, 1, 0, 1
					}
				}
				gdata(GDATA_FLAG_SUB_VICE_STAFF_RECEIVED)++
				gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE)++
				snd SOUNDLIST_COMPLETE1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 1010 ) {
			if ( gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) < 400 ) {
				gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) = 400
				listmax = 0
				buff = lang("よく来たね" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。身体の具合はどうかな。限りなく以前と同じはずだけどね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("報酬については…あれから色々と考えたんだけどね。選ばせておいてなんだけど、他のが良かったとか後で言われても面倒だから、もう全部渡しておくよ。当初の想定以上に働いてくれたこともあるしね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_G_FINGER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
				flt
				itemcreate -1, ITEM_ID_PHOTON_FAIRY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
				flt
				itemcreate -1, ITEM_ID_CONTRADCTION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
				flt
				itemcreate -1, ITEM_ID_BIGAMY_PERMIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				snd SOUNDLIST_COMPLETE1
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 840 & gdata(GDATA_FLAG_MAIN) < 1010 ) {
			if ( gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) < 200 ) {
				gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) = 200
				listmax = 0
				buff = lang("まさか次元の狭間から帰還したうえに、ピンポイントで巨城に現れるとは…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ああ、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。ノルンから報告は受けているよ。おねにい様…ミクラネシスはこのことを予知していたのかな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("拘束が解けても、混沌の超児は全てを忘れていて指の一本すら動かせないはずだ。それでも神の間に混沌勢力が侵入している以上、安心はできないね。たとえばオルフェが全神力を吸収しようものなら、どうなるかわからない。弱体化しているとはいえ、未だに神数十柱分の力が残っているはずだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("倒してくるからそんなの必要ない", "I think not.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("ちょっとした冒険だな", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("オルフェと決着をつけてくる", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("この戦いが終わったら結婚しよう", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("念のために最終対抗手段は用意しておいた…けれど内容は捨て身同然だし、どこまで通用するかも疑問だからあまり使いたくはないね。", "Sorry, this is untranslated sentence.")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("君の力は評価しているよ。でも、少しうぬぼれ過ぎだね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("これだから冒険者は…。神の頭を悩ませるレベルの案件だと理解してほしいね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("確かに君もオルフェと因縁があったね。止めはしないけれど、邪魔だけはしないでほしいね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("えっっ！？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("な、何を馬鹿なことを言っているのかな…。いいかい。私は生まれる前から双子の兄であるテレートスを伴侶としてデザインされている。確かにテレートスは合神していなくなってしまったし結婚という儀式も行ってはいない。しかし私と結婚しようとするのはいかがなものかと思うね。いや、もちろん私だって異種族婚そのものを否定するわけではないのだけれど、特に私は一度堕天した関係で他の神ほど魂が綺麗ではないから、同じ神なら別の…ああもう！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("…まぁ、そういうわけで。まずはお友達から始めるべきだと思うね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
			if ( gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) < 100 ) {
				listmax = 0
				buff = lang("君はよくやってくれたね。けれど大きすぎる力は人の恐怖を煽ることを忘れてはいけない。そして、情報が思わぬスピードで伝わることも。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("混沌の神が君に討伐されたという情報が、一部の勢力にもう伝わってしまったようだね。特にロスリアという国の上層部は、君を人類共通の新たな脅威ということにしようと画策している。気をつけたほうがいいね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("民衆には伝わっていないようだけど、ほとぼりが冷めるまではあまり派手に動かず、一時的に身を隠したほうが得策かもしれないね。その頃には、例の報酬も用意できていると思う。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) += 100
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 690 ) {
			listmax = 0
			buff = lang("決戦因子は君に決まったんだね。テスカトリポカが認めたなら大丈夫だろう。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("仕方がない", "I think not.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("はい喜んで", "I think not.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("報酬は出るの？", "I think not.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("嫌です", "I think not.")
			listmax++
			buff = lang("もし神の間が崩壊して混沌の神が出てきたら、神々も定命ももれなく混沌に取り込まれ、世界は蹂躙され全ての秩序は失われてしまうだろう。もちろん討伐を引き受けてくれるね？", "Huh.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 4 ) {
				listmax = 0
				buff = lang("これは混沌から今の世界を守るためだからね。考え直してほしい。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			gdata(GDATA_FLAG_MAIN) = 691
		}
		if ( gdata(GDATA_FLAG_MAIN) == 691 ) {
			listmax = 0
			buff = lang("さて…報酬はどうしようか。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 1
			listn(0, listmax) = lang("富", "I think not.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("名声", "I think not.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("強さ", "I think not.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("ハーレム", "I think not.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("神の座", "I think not.")
			listmax++
			list(0, listmax) = 6
			listn(0, listmax) = lang("おまえ", "I think not.")
			listmax++
			buff = lang("とりあえずアーティファクトはつけるとして…他に欲しいものがあるかな？", "Huh.")
			chatesc = 0
			gosub *chat_select
			gdata(GDATA_FLAG_MAIN_SOPHIA_THE_WISE) = chatval
			if ( chatval == 5 ) {
				listmax = 0
				buff = lang("神には神の仕事ルールがあってそれに縛られることになるよ。個神には大した権限もない。自由と引き換えてでも秩序維持のために働きたいのなら話は別だけど、それでも君の一生を全うしてからの方がいいだろうね。とにかくおすすめしないね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 6 ) {
				listmax = 0
				buff = lang("そんなことを言って、相手の反応が知りたいだけだろう？そのくらいは本で読んで知っているよ。神をからかってはいけないね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("なるほど…覚えておくね。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("話は変わるけれど、混沌の神を倒すための道具を渡しておこう。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txtef COLOR_GREEN
			txt lang("[来光の牙]を手に入れた！", "You obtain Origin of Light !")
			snd SOUNDLIST_COMPLETE1
			gdata(GDATA_FLAG_MAIN) = 695
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("いくら弱っているとはいえ、相手は私達より圧倒的に高い存在級位を有している。物理的にいくら殺しても倒したことにならない。あれほどの存在をアカシックレコードから切り離すには、膨大なアストラル光が必要になるんだ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そこで、混沌の神を殺す瞬間…すなわち、相手の存在が最も揺らぐ瞬間に神々全員の神力をマナに逆変換して来光の牙に送る。来光の牙はマナをアストラル光に変換するための装置なんだ。発生したアストラル光によって、奴のアカシックレコードに完全干渉する。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…簡単に説明すると、トドメの際に突き刺せば、完全に消滅させるなり、無力化したうえで闇檻に放り込むなりで処理できるということだね。この突破法の準備に3000年かかったから失敗しないでほしいね。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…そうだ。あと、これも必要なら持っていくといい。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_HEART_OF_TWILIGHT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			listmax = 0
			buff = lang("神の間の物理崩壊は時間の問題だし、混沌の軍勢も勢力を増してきている。君も準備を整えてほしい。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 370 ) {
			if ( gdata(GDATA_FLAG_MAIN) < 695 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("神の間とは？", "I think not.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_MAIN) < 695 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("神々の大戦とは？", "I think not.")
				listmax++
			}
			list(0, listmax) = 3
			listn(0, listmax) = lang("何か手伝えることは？", "I think not.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("スリーサイズは？", "I think not.")
			listmax++
			if ( gdata(GDATA_FLAG_MAIN) >= 695 & gdata(GDATA_FLAG_MAIN) < 760 ) {
				list(0, listmax) = 4
				listn(0, listmax) = lang("混沌の神の正体は？", "I think not.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_MAIN) >= 695 ) {
				list(0, listmax) = 5
				listn(0, listmax) = lang("これまでの経緯をまとめて知りたい", "I think not.")
				listmax++
			}
			if ( gdata(GDATA_FLAG_MAIN) >= 695 & gdata(GDATA_FLAG_MAIN) < 760 ) {
				list(0, listmax) = 6
				listn(0, listmax) = lang("混沌の神を倒したらネフィアはどうなる？", "I think not.")
				listmax++
			}
			buff = lang("ネフィアの永遠の盟約により、全てを話すことはできないね。しかし、制限されていない情報に関しては君にも教えよう。", "Huh.")
			gosub *chat_select
			if ( chatval == 4 ) {
				listmax = 0
				buff = lang("…そうだね。あまり言いたくないけれど、あれと戦う君には知る権利があるね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("混沌の神…エンテュメイシス。その正体は…もう一人の私なんだ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("わが父、《深淵のプロパトル》は至高の超越者だった。神としても次元が違うため知覚できないほどのね。末娘だった私は兄や姉に尋ねたが一切の情報が不明。「誰にも知ることができない神」と言っても過言じゃない…。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("それでも…当時の私はどうしても父のことを知りたかった。知識欲の強い娘が、行方不明の父のことを調べようと思うのは仕方がないことだったと思う。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("知りたいのに知ることができないジレンマが頂点に達した時、暴走した想いが私の中にエンテュメイシスという穢れた人格を作り出した。魂の穢れた私は堕天して地上を彷徨っていたが、兄姉の精神浄化によってエンテュメイシスを分離して帰還することができた。今では父のことを知ろうなんて気持ちは微塵もないね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("だがエンテュメイシスは私から分離した後も実は消えずに活動していたんだ。人間たちには不死で知りたがりの娼婦として扱われていたようだね。とりあえずは無害だと判断されていたのだけれど、神々の大戦中、エンテュメイシスは牙を剥いた。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("あれはまるで本から知識を取り込むかのように、大戦で傷ついた神々を次々と吸収していった。父についての情報を得るため、そして力を蓄えて同じ高次元に立とうとしたんだね。熱いパトスのおもむくまま、むやみやたらと神の力を混ぜ合わせていった結果、両性具有の混沌神となってしまった。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("エンテュメイシスは封印される間際でも父を求めていたよ。混沌の眷属が人間を取り込もうとするのも、根底にあるのは知識欲なのかもしれないね…。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 5 ) {
				listmax = 0
				buff = lang("じゃあ簡単に、かいつまんで。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("1。長い歴史の中で、人類は神は徐々に必要としなくなり、神は力を失いつつあった。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("2。そんな時、人類の大半が宇宙に旅立ってしまった。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("3。存在の危うくなった神々が殺気立つ中、テスカトリポカが他の神を倒して信者を奪った。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("4。神々の生存競争が勃発。多くの神が消滅したり魔物に堕ちた。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("5。大戦の裏で混沌の神が力をつけていたことが発覚。混沌対神々の構図に変化。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("6。激戦の末混沌の神は封印できたがこの時混沌の精霊の種が蒔かれていた。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("7。ひとつの大陸がイルヴァから隔離され、ロストイルヴァとなった。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("8。神の間が老朽化したのを狙って混沌の軍勢が巨城に侵攻。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("9。テスカトリポカが混沌の軍勢を撃退するも右腕を失う。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("10。オルフェをはじめとする決死隊が役目を果たし、神の間の増築に成功。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("11。唯一生還したオルフェが常闇の眼を盗んで行方不明に。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("12。混沌の神から奪った力を還元し、ネフィア誕生。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("13。魔法大戦によりナーク・ドマーラ滅亡。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("14。神の間が再度老朽化。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("15。人工細菌メサイアによって作られた秩序の一角獣が役目を果たす。後にサーペントに取り込まれてカオス・ユニコーンへ変化したことが判明。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("16。エーテルが発見され、はみ出し者たちによって盛んに採取された。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("17。メサイアが事故で変異・流出しメシェーラと呼ばれるように。エーテル減少で弱っていた環境は激変。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("18。環境変化に耐え切れずレム・イド滅亡。人類はシェルターで細々と暮らす程度に。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("19。メシェーラに侵食された超生物やアンデッド兵器が荒野を徘徊する、永い暗黒期の到来。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("20。年月経過で回復したエーテルが風を生みヴィンデールの森が発達。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("21。メシェーラ環境に適応できる人類が生まれ世界に拡散。シエラ・テール誕生。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("22。常闇の杖が完成しオルフェ発見。監視を開始。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("23。さらに回復したエーテルがヴィンデールの森を拡大。カルーン滅亡。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("24。ヴィンデールの森が焼き払われる。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("25。神の間が再度老朽化。潜伏していた混沌の軍勢も活発に。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そして、強さと抵抗力を兼ね備えた君が決戦因子として選ばれた…というわけだね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 6 ) {
				listmax = 0
				buff = lang("いい質問だね。現在のネフィアは混沌の神から奪った力を魔力に変換後、陽牢の塔を経由して龍脈に流し込んで生成している。つまり混沌の神がいなくなったらネフィアは維持できなくなるね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ただし、すぐにネフィアが消えるわけじゃないよ。ネフィアのベースとなる遺跡はまた8億程ストックがあるし、魔力も現在対流している分が枯渇するのに最低2000年かかる。それまでに定命も神もネフィアに依存しないシステムを構築していく必要があるね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("神の間は混沌の神が封じられている場所だね。神の間の役割は張り巡らされた結界によって混沌の神の力を奪うことだ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("やがて奪った力はネフィアを生む魔力へと変換してイルヴァ全土に拡散されることになった。こうして世界に混沌と魔法の力がもたらされることになるわけだが…。それはまた別のお話だね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("そもそもの始まりは、神を必要としないまでに人の技術が発達したことだった。その極限にしてエイス・テールの前身…アース・テールと呼ばれる文明。もはやこの星に納まる存在でなくなった当時の人類は、わずかばかりの民を残して星の海に旅立っていった。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("それまでに信仰が絶えかけ力が衰えていた神々にとって、人類の旅立ちは存在を危うくするほどの一大事。残された定命たちに力を誇示し信仰を確保せねば、神としての存在を維持することができないと誰もが考えていたね…。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そしてテスカトリポカによる虐殺が引き金となり、神々の生存戦争が始まったんだ。あの時のイルヴァには、秩序などなかったね…。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…これから先はまだ話すわけにはいかない。すまないね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				sophia = rnd(21)
				if ( sophia >= 16 & sophia < 21 ) {
					listmax = 0
					buff = lang("セクハラとは感心しないね。いつの時代も定命はやんちゃで困る。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				if ( sophia >= 11 & sophia < 16 ) {
					listmax = 0
					buff = lang("そんなことはどうだっていい。重要じゃない。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				if ( sophia >= 6 & sophia < 11 ) {
					listmax = 0
					buff = lang("そうか…君はそんなやつなんだな。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				if ( sophia >= 1 & sophia < 6 ) {
					listmax = 0
					buff = lang("ヘンタイ。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
				if ( sophia == 0 ) {
					listmax = 0
					buff = lang("上から、はちじゅうろ…な、何を言わせる気！？", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
							goto *chat_end
						}
					}
				}
			}
			if ( chatval == 3 ) {
				if ( gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE) == 0 ) {
					gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE) = 1
				}
				if ( gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE) \ 2 != 0 ) {
					gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_YEAR) = gdata(GDATA_YEAR)
					gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_MONTH) = gdata(GDATA_MONTH) + 1
					if ( gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_MONTH) == 13 ) {
						gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_MONTH) = 1
						gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_YEAR)++
					}
					gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_DAY) = gdata(GDATA_DAY) + rnd(3)
					if ( gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_DAY) >= 30 ) {
						gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_DAY) -= rnd(3) + 3
					}
					soukop = gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE) / 2 + 1
					if ( gdata(GDATA_FLAG_MAIN) < 695 ) {
						listmax = 0
						buff = lang("君には神の間を目指して欲しいのだけどね。", "Huh.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					listmax = 0
					buff = lang("ふむ…そうだね。この後、地下の第" + soukop + "倉庫を整理する予定があって人手を探しているんだ。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("もしも君にその気があるのなら、" + gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_YEAR) + "年" + gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_MONTH) + "月" + gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_DAY) + "日の午後14時半から15時までの間に話しかけてもらえるかな。", "Huh.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE)++
					snd SOUNDLIST_WRITE1
					txtef COLOR_GREEN
					txt lang("ジャーナルが更新された。", "Your journal has been updated.")
					goto *chat_end
				}
				listmax = 0
				buff = lang("もしも君にその気があるのなら、" + gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_YEAR) + "年" + gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_MONTH) + "月" + gdata(GDATA_FLAG_SUB_SOPHIA_THE_WISE_DAY) + "日の午後14時半から15時までの間に話しかけてもらえるかな。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 368 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("返却する", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("ただではやれないな", "I think not.")
			listmax++
			buff = lang("ようやく来たね。唐突ですまないけれど、君の所有している常闇の眼を返却してほしい。", "Huh.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("君は常闇の眼を混沌の眷属から解放してくれた。その駄賃を払うのもいいだろう。けれど人界の通貨はあまり持っていなくてね。とりあえず持ち合わせを全部出すからそれで勘弁してほしい。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cdata(CDATA_GOLD, CHARA_PLAYER) += cdata(CDATA_GOLD, tc)
				cdata(CDATA_GOLD, tc) = 0
				snd SOUNDLIST_PAYGOLD1
			}
			txt lang("あなたは常闇の眼を返却した。", "Huh.")
			msg_halt
			listmax = 0
			buff = lang("おかえり…。叡智の結晶であるこの書物は、大戦の末期に台座ごと人界に持ち去られ、ずっと行方がわからなくなっていた。すっかり魔力を失ってしまったようだけど…それでもこの上なく貴重な資料であることに変わりはないね。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…さて。常闇の眼を解読しているなら気付いているかもしれないが…これに記されているのは人界の出来事のみ。かつてこの地で起きたこと、そして今起きていることを知りたければ始まりのネフィア…混沌の巨城地下にある、神の間を目指しなさい。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 370
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 628 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) == 745 ) {
				listmax = 0
				buff = lang("お若いの、頑張ったのう。よくぞ、わしが到着するまで持ちこたえてくれた。後はこの老いぼれが引き受けよう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("思えば、魔術に捧げた人生じゃった。才を活かすため師匠に背いて軍に入り、無我夢中で戦い続け…気づけば老いていた。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("死なないで！", "Do not die!")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("美味しいところを持っていく気？", "Are you going to success than me?")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("化身？凶獣？", "What is oblivion beast?")
				listmax++
				buff = lang("今こそ使おう…邪悪なる化身、凶獣の力を！", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("死なんよ。死ぬよりもっと酷い目に遭うだけじゃ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("はは、すまんのう…。最期くらい恰好をつけさせとくれ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("世界が消える前兆として、忘却の女神の化身が現れる。奴らが人々から記憶を抹消することで、人も世界も完全に忘れ去られ本当の死を迎える…。その内の一匹が魔術で我が手の内にあるのじゃ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				listmax = 0
				buff = lang("さぁ行け、ナゴレウ！わしの記憶を燃やし、奴を蝕め！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 755 ) {
				listmax = 0
				buff = lang("…。", "....")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
			listmax = 0
			buff = lang("…(もはや言葉すらわからないようだ)", "(" + he(tc) + " seems to not know the language...)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) >= 1000 ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("下級魔法使いたちの扱いを見直すよう国王陛下に進言したが、人の価値観というものはそう容易く変わらんからのう。少しずつでも良い方向に変わっていくといいのじゃがな。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) == 100 ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("おまえさんの協力で王都を守ることができたよ、ありがとう。もしこれだけの戦力で攻めて倒せなければ、その時は最後の手段を使わなけらばならなかったのじゃ。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("おかげで混沌の神を相手にするまで温存することができるわい。邪悪な化身である凶獣の力を、のう…。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("むむ。今、わしは何を話しておった…？", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_AREA) == AREA_BLIMP ) {
			if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) == 3 ) {
				if ( gdata(GDATA_LEVEL) == 100 ) {
					listmax = 0
					buff = lang("どうじゃ、わがエウダーナの魔導船は。よい乗り心地じゃったろう？…ところでわしらは何をしに来たんじゃったか。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("おお、あの悪魔を倒さねばならん。敵の数は多い、危なくなったらすぐに船に退避するんじゃぞ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) = 4
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_AREA) == AREA_BLIMP ) {
			if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) > 0 ) {
				if ( gdata(GDATA_LEVEL) == 1 ) {
					if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) <= 2 ) {
						listmax = 0
						buff = lang("うむ…おまえさんは…？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("おお、" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "か。パルミアが寄越した協力者じゃな。話は聞いておるよ。" + gdata(GDATA_DEEPEST_LEVEL) + "階相当のネフィアを攻略している強者とな。他の者はおまえさんの力を認めないかもしれんが、わしは決定的な戦力になると予感しておるぞ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("ちょっと待って", "Good luck.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("出撃！", "I was looking for you.")
					listmax++
					if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) <= 4 ) {
						buff = lang("さて、何をするんじゃったか…？おお、そうじゃ。さっそく王都を奪還しに行こうぞ。皆の者！戦の準備じゃ！", "Sorry, this is untranslated sentence.")
					}
					gosub *chat_select
					if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) <= 2 ) {
						gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) = 3
					}
					if ( chatval == 1 ) {
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						mapidou = 100
						mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
						gdata(GDATA_TELEPORT_AREA) = AREA_BLIMP
						gdata(GDATA_TELEPORT_LEVEL) = 100
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 5
						mapstarty = 13
						dbg_skipevent = 1
						gdata(GDATA_HOUR) += 2
						gosub *advance_hour
						dbg_skipevent = 0
						chatteleport = 1
						goto *chat_end
					}
					else {
						listmax = 0
						buff = lang("そうかい？ところでおまえさんは誰じゃったかのう？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) == 0 ) {
			listmax = 0
			buff = lang("おお、冒険者か。よいところに来たのぅ。少し頼みたいことがあるのじゃ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("頼みたいことは…んむ？忘れてしもうた…。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 629 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) == 1002 ) {
			listmax = 0
			buff = lang("…負けないわ。あたしは王宮魔導士長になってベルム家を栄えさせて、行方不明のミネア姉さまにも戻ってきてもらうんだ！こんなところで挫けるわけにはいかないのよ！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) == 1001 ) {
			listmax = 0
			buff = lang("魔術書を見ながらでもほとんど解読できないなんて…王宮や魔術研の専門家は一体どんな脳みそを持ってるのよ…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("あっち行く", "Good luck.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("読んでみる（読書130相当）", "I was looking for you.")
			listmax++
			buff = lang("なに見てるのよ！あっち行きなさいよ！", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( sdata(SKILL_NORMAL_LITERACY, CHARA_PLAYER) < 130 ) {
					txt lang("難解だ！", "It's too difficult!")
					dmgcon CHARA_PLAYER, CONDITION_CONFUSE, 100
					listmax = 0
					buff = lang("（ネインはこれ以上にないくらいに呆れている）", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
			if ( chatval == 1 ) {
				if ( sdata(SKILL_NORMAL_LITERACY, CHARA_PLAYER) >= 130 ) {
					listmax = 0
					buff = lang("はぁ？身の程知らずってレベルじゃ…え？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ちょ、ちょっと嘘でしょ！？デタラメに言ってるだけじゃ…？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ストップ！それ以上言わないで！言わないでってば！ぶつわよ！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) = 1002
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) == 1000 ) {
			listmax = 0
			buff = lang("それじゃ早速解読を始めましょうか。…なに見てるのよ、暇なの？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ふむふむ、ベースとなっている式は現在使われている魔力電池とほぼ共通ね。あ、ここは施術ゼミでやったところだわ。なんだ、意外と単純ね。さてメインの魔術構造については…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("……。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("な、なによこれは…既存のものと概念からして異なるわ。近年発表されたものとも違う。く、組むのは無理でも読むくらいなら…ええと、この式がこちらに繋がって…あっうう…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あ、あたしは！ベルム家の才女で！飛び級で！首席なのよぉ！？こ、このくらい…くっ！！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("なっ、なに見てるのよ！あっち行きなさいよ！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) = 1001
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) == 2 ) {
			if ( findally(CREATURE_ID_MANA_MONOLITH) == (-1) ) {
				listmax = 0
				buff = lang("なに話しかけてきてるわけ？さっさと探してきなさいよ。無理だと思うけれど。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("そ、それは！？信じられないけどこの魔力回路の封印パターンは間違いない、軍で使われてるものだわ！まさか本当に持ってくるなんて。まぐれ…いえ、運だけではこんな…。冒険者は大した魔法も使えないのに好き勝手やってる無法者と教わったのだけれど。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…いいわ。どんな手を使ったのかは知らない。だけどここにある以上、認めてあげる。未来の王宮魔導士…いえ、魔導士長も夢じゃない。そんなあたしに認められるのだから光栄に思いなさい。これは報酬よ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_SONIC_BROOM, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_SCROLL_GAIN_ATTRIBUTE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) = 1000
			listmax = 0
			buff = lang("転写魔法でメモしたからモンスターは返すわ。あたしがいつまでも持ってるとマズいもの。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) == 1 ) {
			listmax = 0
			buff = lang("…は？本当に来たの？凡人のくせに恐れ多いと思わないの？仕方ないわね…じゃああんたの実力を見せてもらおうかしら。あたしの貴重な時間を使うに相応しいかテストしてあげるわ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("わがエウダーナの軍部は、魔力を蓄える擬似生命体の開発を近年進めているわ。機密だから詳しい仕様は伏せられているけれど、構成がどうしても気になるのよ。だから捕獲してあたしの前に連れてきなさい。あたしが直接捕獲するのは立場上問題があっても…冒険者が「偶然」捕まえたものを天才であるあたしに「調査依頼」するのは自然なことでしょう？", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("肝心の擬似生命体は、野外で試験運用されていると聞くわ。魔力のアウトプットを考えると、魔力回路が少なからず表面に露出しているはず。学がないと歪な魔法陣に見えるかもしれないけれど。名前に関しては情報が出回ってないから不明よ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あまり期待はしてないけど、上手くいったら報酬をあげる。そうね、あたしの学友たち…と言ってもあんな低レベルのと同列に扱われたくないんだけど。まぁそいつらにもまだ見せていない、あたし特製のアーティファクトもつけてあげるわ。上手くいったらの話よ？", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) = 2
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) == 0 ) {
			listmax = 0
			buff = lang("何よあんたは。…ははーん。さてはあたしの魔法の才能に目をつけたんでしょ！目的はスカウト？それとも弟子入り？ふふ。パッとしない感じだけど、人を見る目はあるみたいじゃない。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ほんとは凡人に付き合う義理も暇もないんだけど…いいわ、あたしは心が広いからあんたにもチャンスをあげるわ。また来なさいよ。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1037 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			if ( gdata(GDATA_LEVEL) == 11 | gdata(GDATA_LEVEL) == 12 ) {
				if ( soulstage == 4 ) {
					cdata(CDATA_PIC, tc) = xy2pic(21, 31)
					cdata(CDATA_RELATION, tc) = RELATION_ENEMY
					cdata(CDATA_AI_AGGRO, tc) = 80
					cdata(CDATA_TARGET, tc) = 0
					listmax = 0
					buff = lang("早ク！コッチヨ！早ク…コッチヨ…ハヤクハヤク！ハヤクコッチ…アハハハハハハハ！！！！", "Hurry up...here....hurry...hurry hurry! here!!! Hahahahahaha!!!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					soulstage = 5
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			if ( gdata(GDATA_LEVEL) == 11 | gdata(GDATA_LEVEL) == 12 ) {
				if ( soulstage == 3 ) {
					listmax = 0
					buff = lang("早く！こっちよ！", "Hurry up! Here!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					txt lang("「おかあ…さん？」", cnvtalk("Mom...?"))
					soulstage = 4
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1034 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("ここまでよく頑張った！", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("もう安全だよ…", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("えっ、ここ暖かいの？", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("楽しく暮らせよクソガキ", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("ここは、あたたかいね…。", "Thank you...")
				chatesc = 0
				gosub *chat_select
				txtef COLOR_GREEN
				txt lang(name(tc) + "の姿は薄れ、見えなくなった…。", name(tc) + " faded out and became invisible...")
				soulimit = 0
				soulstage = 0
				gdata(GDATA_FLAG_SUB_AMURDAD)++
				chara_vanquish findallyguard(CREATURE_ID_STRAY_SOUL)
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
		}
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			if ( gdata(GDATA_LEVEL) == 31 | gdata(GDATA_LEVEL) == 32 ) {
				if ( soulstage == 2 ) {
					listmax = 0
					buff = lang("もうイヤ…！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("必ず守ると約束する！", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("もう少しだから頑張ろう？", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("泣きたいのはこっちだよ〜！", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("黙って走れクソガキ", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("(" + name(tc) + "は泣いてうずくまってしまった…)", "(" + name(tc) + " cried and crouched...)")
					chatesc = 0
					gosub *chat_select
					listmax = 0
					buff = lang("が、がんばる…。", "O..OK...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					soulstage = 3
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			if ( gdata(GDATA_LEVEL) == 60 ) {
				if ( soulstage < 2 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("君を助けに来た！", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("一緒に探してあげるよ", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("お父さんのことも探してあげよう？", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("いいから着いてこいクソガキ", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("お母さん…どこ…。こわいよぉ…！", "Sorry, this is untranslated sentence.")
					chatesc = 0
					gosub *chat_select
					listmax = 0
					buff = lang("う、うん…。", "O..OK...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( rnd(2) == 0 | 0 ) {
						kaidan = 1
					}
					else {
						kaidan = 0
					}
					if ( kaidan == 0 ) {
						cell_featset 5, 22, tile_upstairs, CELL_FEATURE_UPSTAIRS, 51
					}
					else {
						cell_featset 5, 22, tile_upstairs, CELL_FEATURE_UPSTAIRS, 52
					}
					rc = tc
					gosub *add_ally
					cbitmod CHARA_BIT_GUARD_TEMP, rc, TRUE
					r1 = rc
					gosub *charaRefresh
					soulstage = 2
					txt lang(name(tc) + "を保護した。無事に連れて帰らなければ。", "You must return the child safely.")
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 637 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_AMURDAD) \ 10 == 2 ) {
			listmax = 0
			buff = lang("お疲れさま…これは報酬…。他の子を見つけたらまた連絡する…。今は魂を休めて…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			if ( gdata(GDATA_FLAG_SUB_AMURDAD) == 2 ) {
				flt
				itemcreate -1, ITEM_ID_ASTRAL_SICKLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				gdata(GDATA_FLAG_SUB_AMURDAD) = 10
			}
			else {
				if ( gdata(GDATA_MIN) >= 0 & gdata(GDATA_MIN) < 15 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 12
					encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_NIGHTMARE, 200, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 15 & gdata(GDATA_MIN) < 30 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 13
					encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_NETHER_BREATH, 200, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 30 & gdata(GDATA_MIN) < 45 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 14
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_NETHER, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 45 & gdata(GDATA_MIN) < 60 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 15
					encadd ci, ENCHANT_TIME_STOP, 150, 0, 1, 0, 1
				}
				gdata(GDATA_FLAG_SUB_AMURDAD) += 8
			}
			snd SOUNDLIST_COMPLETE1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_AMURDAD) \ 10 == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("わかった", "I'll try to do.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("わからん", "I'm busy now.")
			listmax++
			buff = lang("悪鬼たちに囚われた無垢な魂を見つけた…。ここを離れられないから…君に救ってきてほしい。道は作る…。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval != 0 ) {
				listmax = 0
				buff = lang("わかって…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			if ( get_freeally() == 0 ) {
				txt lang("仲間の最大数に達しているため、行っても連れてこられない…。", "Your party is already full. You can't invite someone anymore.")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("侵入したら、気づかれる前に…急いで…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			soulimit = 4444
			soulstage = 1
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_AMUR_CAGE
			gdata(GDATA_TELEPORT_LEVEL) = 10
			if ( 0 ) {
				soulimit = 2000
			}
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 17
			mapstarty = 3
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_AMURDAD) == 0 ) {
			listmax = 0
			buff = lang("定命の君に、頼みたいことがある…。気が向いたら…また話しかけて…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_AMURDAD) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_AMURDAD) \ 10 == 0 ) {
			listmax = 0
			buff = lang("アストラル光で描かれたこの世界の生命の記録…記録通りの死を迎えた時、アーカーシャへの画廊へと導かれ…生命は光になる。光になって星に還る…。そしていつか、また光の海から生まれ出る…。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			cell_featset 27, 20, tile_downstairs, CELL_FEATURE_DOWNSTAIRS, 2
			list(0, listmax) = 0
			listn(0, listmax) = lang("意味が分からない", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("だいたい分かった", "I think not.")
			listmax++
			buff = lang("輪廻から外れた者たち…見える…？そう、彼らは世界の定めによって星の光となれずに…ただ、ただそこに存在する…。闇の中で、魂のキャンパスの上で永遠に…。生者の眼に映るのは一部だけ…魂の因縁を持つもの、だけ…。", "Huh.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("分からない…君の記録…無い…。君が光に還るのか…分からない。アーカーシャの影はどこ…？", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("永遠の闇と、星の光の祝福を…。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 639 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) == 0 ) {
			if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE) == 0 ) {
				repeat
					flt 170
					characreate MAX_CHARA_NC, CREATURE_ID_BUG, -3, 0
					if ( cdata(CDATA_RELATION, MAX_CHARA_NC) != RELATION_ENEMY ) {
						continue
					}
					if ( cdata(CDATA_QUALITY, MAX_CHARA_NC) >= FIX_QUALITY_MIRACLE ) {
						continue
					}
					if ( cdata(CDATA_LEVEL, MAX_CHARA_NC) < 100 ) {
						continue
					}
					break
				loop
				gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE) = cdata(CDATA_ID, MAX_CHARA_NC)
				chara_vanquish MAX_CHARA_NC
				listmax = 0
				buff = lang("いつもの奴隷くんたちをいじめるのにも飽きてきたわ…。もっと新鮮な反応が欲しいの。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) = 1
				if ( dbg_noautosave == 0 & gdata(GDATA_WIZARD) == 0 ) {
					gosub *game_save
					txtmore
					txtef COLOR_BLUE
					txt lang(" *保存* ", "*saving*")
				}
				listmax = 0
				buff = lang("あなた、" + refchara(gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE), DBSPEC_CHARA_NAME_ORG, 1) + "を連れてきてくれない？…ご褒美？いいわよ、気が向いたらね。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
		}
		kaiwa = rnd(4)
		if ( kaiwa == 0 ) {
			listmax = 0
			buff = lang("日差しは強いし砂はくっついて不愉快だし本当に酷いところ…。アンテナだかなんだか知らないけれど、ルルウィお姉さまったら忠誠を誓った私にこんな砂漠の塔の管理を任せたのよ。そういう容赦のないところがまたステキなんだけどね…。", "I declare, these rays are just too strong and the sand just sticks everywhere and how awful is this dreadful place. I care not if this is an antenna but having sworn loyalty to Miss lulwy I have been given governance of this desert tower. That cruel part of her is just quite wonderful as well...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 1 ) {
			listmax = 0
			buff = lang("この浜辺は黒天使ちゃんとその下僕たちが作ってくれたわ。汚い砂漠の砂を使わずに他所から取り寄せたから色々と大変だったみたいね。その分ご褒美をたっぷりとあげたけれど。", "This beach was build by those little Dark Angels and her minions. Was quite a trial I hear, not using a single bit of that dirty sand and bringing it in from somewhere else.  She was well rewarded for it. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 2 ) {
			listmax = 0
			buff = lang("ルルウィお姉さまもここにはバカンスに来るのよ。気ままにお茶したり浜辺で戯れたり一緒に囚人君にお仕置きをしたり。でもメインイベントは最後の嵐ね。塔の中で小規模な嵐を起こして全てをめちゃくちゃにして去っていくの。風であちこちに叩きつけられながら思うの…ああ、やっぱり私はお姉さまには敵わないんだなぁって。すごくゾクゾクするのよ…。", "Miss Lulwy does come here on vacation.  Tea as she feels in the mood, frolicing together on the beach and punishing the prisoners. The best part are those hurricanes.  Inside this tower small hurricanes swirl around and jumble up everything. That wind slapping around here and there... Oh my, I really can't compete with her.  Just makes me tremble...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 3 ) {
			listmax = 0
			buff = lang("なんだか知らないけれど塔の周りに不吉な黒猫が住み着いてるのよね。黒天使ちゃんが何度か襲われて翼をもがれそうになってるし、ボス猫女神にはいずれお仕置きしなきゃいけないわ。", "I don't know why but these unlucky black cats have taken up residence around the tower. The Dark Angels have been attacked and those wings have been plucked near clean.  That empress cat god must be taught a lesson.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE) == 0 ) {
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE) != 0 ) {
			if ( findally(gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE)) == (-1) ) {
				goto *chat_end
			}
		}
		rc = findally(gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE))
		if ( cdata(CDATA_PRAY, rc) == 1 ) {
			snd SOUNDLIST_FAIL1
			txt lang("それはあなたの大事な仲間だ。会話メニューから解除できる。", "It's set as no-drop ally.")
			goto *chat_end
		}
		if ( cdata(CDATA_IN_TAGTEAM, rc) != 0 ) {
			txt lang("タッグを解除する必要がある。", " It is necessary to release the tag.")
			goto *chat_end
		}
		chara_vanquish rc
		listmax = 0
		buff = lang("あら…♪言いつけどおり、新しい玩具を持ってきてくれたのね。コレが壊れたらまた頼むからよろしく。", "Huh.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM)++
		gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM_CREATURE) = 0
		if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) == 4 ) {
			gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) = 100
		}
		if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) \ 5 == 0 ) {
			listmax = 0
			buff = lang("そうね、気分がいいからご褒美をあげるわ。とっておきなさい。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) == 100 ) {
				flt
				itemcreate -1, ITEM_ID_RAINBOW, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			}
			if ( gdata(GDATA_FLAG_SUB_ARASIEL_OF_THE_SANDSTORM) > 100 ) {
				if ( gdata(GDATA_MIN) >= 0 & gdata(GDATA_MIN) < 8 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 1
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_FIRE, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 8 & gdata(GDATA_MIN) < 16 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 2
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_COLD, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 16 & gdata(GDATA_MIN) < 24 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 3
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_LIGHTNING, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 24 & gdata(GDATA_MIN) < 33 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 4
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_POISON, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 33 & gdata(GDATA_MIN) < 42 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 5
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_CHAOS, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 42 & gdata(GDATA_MIN) < 51 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 6
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_SOUND, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 51 & gdata(GDATA_MIN) < 60 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 7
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_DARKNESS, 400, 0, 1, 0, 1
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 640 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("不幸度を見てほしい", "Please tell me my misfortune.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("ごめんなさい", "Sorry.")
		listmax++
		buff = lang("むにゃあ…。アタシのごろごろを邪魔して、いったい何のつもりなんだニャ？", "Mreow... You daring to disturb my laze meow, what's the meaing of this meow?")
		gosub *chat_select
		if ( chatval == 0 ) {
			hukou = 100 - sdata(SKILL_ATTR_LUC, CHARA_PLAYER)
			listmax = 0
			buff = lang("仕方がないにゃあ！そこまで言うなら特別に教えてやるニャ。どれどれぇ？", "Ah well, meow! If you're that desperate then I'll teach you meow.  Let's see?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…不幸度は" + hukou + "！！", "Your misfortune is " + hukou + "!!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( sdata(SKILL_ATTR_LUC, CHARA_PLAYER) <= 1 ) {
				listmax = 0
				buff = lang("ニャハハ！可哀想なくらいツイてないやつだニャ。", "meowhaha! You're quite an unfortunately unlucky one meow.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( sdata(SKILL_ATTR_LUC, CHARA_PLAYER) > 1 & sdata(SKILL_ATTR_LUC, CHARA_PLAYER) < 50 ) {
				listmax = 0
				buff = lang("ツイてないやつだニャ。アタシ的には面白いけどニャ。", "You're an unlucky one meow.  It's quite fun for me though meow.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( sdata(SKILL_ATTR_LUC, CHARA_PLAYER) >= 50 & sdata(SKILL_ATTR_LUC, CHARA_PLAYER) < 100 ) {
				listmax = 0
				buff = lang("…まぁ、ふっつうなのニャ。", "...well, that's about average meow.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( sdata(SKILL_ATTR_LUC, CHARA_PLAYER) >= 100 & sdata(SKILL_ATTR_LUC, CHARA_PLAYER) < 200 ) {
				listmax = 0
				buff = lang("ずいぶんとツイてるみたいニャ。気に入らないニャア…。", "You're pretty lucky meow.  Bothersome meow.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( sdata(SKILL_ATTR_LUC, CHARA_PLAYER) >= 200 ) {
				listmax = 0
				buff = lang("気に入らないニャア…。すっごく、気に入らないのニャア…。", "Bothersome meow... Very, bothersome meow....")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("分かればいいのニャ。許してやるからさっさとあっち行くのニャ。", "Good if you understand meow.  I'll forgive you so hurry up and get out of here meow.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 641 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 1002 ) {
			if ( gdata(GDATA_AREA) == AREA_HARVEST_SHIP ) {
				listmax = 0
				buff = lang("我々は８０７１番目の資源回収部隊だ。別働隊が同じようにこの星にやってくる可能性はゼロではない。私はこの区画で機能休止…廃品に埋もれ眠ることにしたが、もしも別働隊があの遥かなる星の海からやってきたら教えてくれないか。上手くいくかはわからないが、その時は再び説得にあたってみよう。", "Thanks! Please wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) = 1003
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 1001 ) {
			if ( gdata(GDATA_AREA) == AREA_HARVEST_SHIP ) {
				listmax = 0
				buff = lang("君か…また…会ったな。あれから私はアクセス端末に細工して仲間の大半を行動不能にし、その隙に船の動力部と中枢を修復不能なまでに破壊した。無我夢中だったもので、少し…いや、かなりやりすぎてしまった。…まいったな、どうやら私には資源回収より破壊工作のほうが合っていたようだ。", "Thanks! Please wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("もう星には帰れない、そもそも帰る文明が存在しているのかさえわからない。残った仲間たちは相変わらず説得を聞いてくれず、私を処分しようとする。もう疲れた。", "Thanks! Please wait here.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) = 1002
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 3 ) {
			listmax = 0
			buff = lang("突破口を開いてくれたか、感謝する！これはささやかだがお礼だ。この星でどれだけの価値があるかはわからないが…不要でなければ受け取ってほしい。", "Thanks! Please wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			flt
			itemcreate -1, ITEM_ID_USAMIMI, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			flt
			itemcreate -1, ITEM_ID_CHIP, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			flt
			itemcreate -1, ITEM_ID_ACCUMULATOR, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
			flt
			itemcreate -1, ITEM_ID_BOTTLE_WATER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			listmax = 0
			buff = lang("私はこれから船に乗り込んで始末をつけてくる。仲間たちの妨害があるだろうが、宇宙生物に比べればなんとでもなるレベルだ。そこまで君に頼むわけにはいかない。", "Thanks! Please wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("さらばだ、異星の友人よ。本当に迷惑をかけたな。", "Thanks! Please wait here.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) = 1000
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			if ( cdata(CDATA_ID, tc) == CREATURE_ID_JIN_THE_MACHINE_REBEL ) {
				chara_vanquish tc
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 1 | gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 2 ) {
			listmax = 0
			buff = lang("我々の乗ってきた船はこの塔から南東に存在するが、船のシステムを止めるには中枢へ辿りつかなくてはならない。しかしそのためには、戦闘用に調整された宇宙生物が防衛している区画をどうしても通らなければならないのだ。宇宙生物を排除してもらえれば、後は私がこの全機能を賭して始末をつけてみせよう。", "Our base is located southeast of the tower but to enter the base's system lockdown you must pass through the central system. Except to do so you must pass through the division of reprogramed space creatures.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そうそう、正面の入り口は当然封鎖されている。私は廃品から作り上げた爆発物で廃棄処理区画の壁と天井に穴を開けて出てきた。場所を教える、そこを入り口として侵入してくれ。", "If you can expunge the space creatures I will then enter a total system shutdown.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 1 ) {
				gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) = 2
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				txtvalid = 0
				txt lang("星渡の収穫船の入り口について教えてもらった。", "This mansion is for sale. There was no one.")
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) == 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("ああ", "Yes.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("いや…", "No...")
			listmax++
			buff = lang("おお…君は私の話を聞いてくれるのか？", "Oh...You will help me?")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("単刀直入に言おう。この星は狙われている。", "Put bluntly. This planet is being targeted.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("私は資源回収用に造られた機械生命体。メモリーによれば造物主たちに代わって遥かなる星の海を渡り、異星から資源を持ち帰るのが使命なのだが…", "I am a mechanical lifeform designed for resource extraction. If my data is without error my creator has sent me in place to cross galaxies and bring resources back from alien planets...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("私が再起動したとき、万年単位で計測可能な計器が振り切れていた。通信装置も故障はしていないはずなのだが応答が無く、いったいどれだけの年月が過ぎたのか、造物主たちが今存命しているのかさえ不明な状況だ。", "Except in my reactivation I percieved my measurements have surpassed the 10,000 year mark. My transmission equipment is without error yet no response has been given, How much time has passed is unknown, The living status of my creator is unknown.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("知的生命体の暮らす星だった場合は友好関係のためにもまず造物主たちの判断をあおぐことになっている。だが私の仲間たちは独断で宇宙生物を用いてこの星を制圧し様々な資源を持ち帰ろうと暗躍しているのだ。私も説得を試みたのだが、思考プログラムの欠陥と判断され廃棄処理区画に送られてしまった。", "My creator has determined that planets of intelligent life should be met with friendly contact but my companions have acted on their own accord to utilize space creatures for suppressing this planet and returning resources to our planet. I have tried persaude them but my thought program was deemed defective and I was sent to the disposal division.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("この星の人々に頼むのも非常に筋違いな話だが、どうか仲間たちの暴挙を止めるのに協力してほしいのだ…。", "I conclude it illogical to rely on the humans of this planet except I ask you to assist in stopping my companions violations.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("都合が悪くなければ、また話しかけてほしい。詳細を話そう。", "I want you to talk if convenient. I will talk more.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("君も彼らと同類なのか…。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 650 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) >= 8 ) {
			listmax = 0
			buff = lang("（" + cdatan(CDATAN_NAME, tc) + "はニタニタと笑っている…）", cdatan(CDATAN_NAME, tc) + " kills himself...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ついによそ者の小娘が出ていきおった。あれだけ夢を語っておきながら諦めるとは実に情けない！所詮その程度の覚悟しかなかったのだろう。まぁおれはそのことを見抜いていたから早く本性を現すように色々と仕向けたのだがな。まったく、無駄な時間を費やせおって。おれの貴重な時間を返してもらいたいものだ。", "...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("考えておく", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("前金をよこせ", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("めちゃくちゃになるのはお前だ", "Sorry.")
			listmax++
			buff = lang("だが孫娘のサリムまでいなくなったのは問題だ。おそらく小娘がサリムをたぶらかして連れ出したに違いないからな、拠点を探し出してめちゃくちゃにしてこい。なに、相手は誘拐犯だからどんなことをしようが構わん、悪いのは全て奴だ。ああ、サリムにはまだ利用価値があるから傷つけずに連れてこい。わかったな？", "Mreow... You daring to disturb my laze meow, what's the meaing of this meow?")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("はっ。信用ならないよそ者に前金なんて出せるとでも？自分の立場を少しはわきまえたらどうだ。", "...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("謝罪する", "Please tell me my misfortune.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("立ち去る", "Please tell me my misfortune.")
				listmax++
				buff = lang("金を貰って仕事を引き受ける以上、お前は信用を大事にしなければならない。それなのに依頼者に対して、なんだその口のきき方は？お前には他人から依頼されるだけの資格がない。", "Mreow... You daring to disturb my laze meow, what's the meaing of this meow?")
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("お前の述べる謝罪なんて信用できんな。誠意を見せろ、誠意を。", "...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval != 0 ) {
					listmax = 0
					buff = lang("ふん。反論できないようだな。", "...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) >= 5 & gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) < 8 ) {
			listmax = 0
			buff = lang("なんだ？他にも武具を「評価」してもらいたいのか？だが断る。これ以上不快な思いをさせられるのはごめんでな！", "...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 4 ) {
			listmax = 0
			buff = lang("おい、ちょっと待て。お前の持っているその短剣…よく見せてくれ。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 1 ) {
				listmax = 0
				buff = lang("ほう、これはよくできている。握りやすく滑らず振りも軽い。見た目こそ華が無いが、刀身も丈夫で実用に特化した…", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 2 ) {
				listmax = 0
				buff = lang("これは美しい…。武器として使うには適さないがこの造形は美術品として通用するだろう。刀身の装飾もさることながら煌めき具合も…", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 3 ) {
				listmax = 0
				buff = lang("新しい！一見、奇抜にしか見えない形状だがこの刀身が切れ味を大きく向上させている。異なる進化を遂げた斬新な…", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 4 ) {
				listmax = 0
				buff = lang("これだけ特徴の異なるものを一人で作り分けたのは見事。あえて言うならば、この武骨な短剣が特に完成度が高いと見た。実用性重視の作品を得意としているのかもしれん。しかし残りの2本もよくできて…", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("おや…？こんなところに小さく、製作者の名前が入れてあるな…。…イルマ？", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("(ダインは短剣を溶解炉に放り込んだ) ", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_ATK_POISON
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 1 ) {
				listmax = 0
				buff = lang("持ちやすく丈夫な短剣など、この世にいくらでもあるわ。短剣の装飾品としての面を捨て去り、新しい発想もないときた。まさに陳腐としか言いようがない。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 2 ) {
				listmax = 0
				buff = lang("外見だけ取り繕った中身のない代物だ。武具職人など辞めて彫刻家にでもなったらどうか。それでもこの程度の装飾では食っていくこともままならないだろうがなぁ。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 3 ) {
				listmax = 0
				buff = lang("やはり奇をてらっただけの出来損ないだな。それどころかあの小娘のことだ、従来の短剣の形状を否定し喧嘩を売っているに違いない。実におこがましい。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) == 4 ) {
				listmax = 0
				buff = lang("職人たるもの自らの適正を見極め己だけの持ち味を作り出すべき。評価者に媚びて他の作風に手を出すなど言語道断よ。もっとも、あの小娘がどのような持ち味を作り出したところでわしは決して認めんのだがな。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("言い方ってものがある", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("滅相もない", "Sorry.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("短剣を返せ", "Sorry.")
			listmax++
			buff = lang("なんだ？まさか文句でも？", "So you are also of the same genus as the others...")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("ふん。おれは思ったことを述べただけ。理屈も基準も、決めるのは評価者であるおれだ。何を言おうがおれの勝手だろう？", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("おう、持って帰れ。溶解炉に腕を突っ込めばまだ残骸が見つかるかもしれんぞ。いや、あんな質の作品ではとっくに溶けているか？わはは。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("ちっ、長々と喋ったら疲れたぞ。まったく、無駄な時間だったな。さっさと帰れ。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_IRMA_DAGGER) = 10
			gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 5
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) < 4 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("武器や防具を買いたい", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("そうしよう", "Sorry.")
			listmax++
			buff = lang("旅人か、こんなところまでよく来たもんだ。店数は少ないが、丘の民が作ったアイテムの数々を買っていくといい。", "Mreow... You daring to disturb my laze meow, what's the meaing of this meow?")
			gosub *chat_select
			if ( chatval == 0 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("残念だ", "Please tell me my misfortune.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("後継者になりたい", "Sorry.")
				listmax++
				buff = lang("それは残念だな、この村に武具店は無いぞ。後継者不足でだいぶ昔に廃業してなぁ。家具や小屋を作る者はいくらか残っとるが、武具職人はもう誰もおらん。", "So you are also of the same genus as the others...")
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("ああ、うちの孫がおれの跡を継いでくれたらなぁ。しかし強要するのもイカンし…。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("は…？", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("おい、伝統職人芸を馬鹿にしてるのか！！よそ者が後継者だと？お前といい、あの赤髪の小娘といい…ふざけているのか？ああ？もういい。出ていけ。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 651 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) >= 11 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("聞かなかったことにする", "Sorry.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("アーティファクトの見極め", "my misfortune.")
			listmax++
			buff = lang("猫 イズ フリ〜ダ〜ム♪ 猫 イズ フリ〜ダ〜ム♪", "So you are also of the same genus as the others...")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("任せておきな。そうだな…アーティファクトが秘めたエンチャントの強度を数値化して教えてやるぜ。どれを見ればいい？", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				invsubroutine = 1
				invctrl = INVCTRL_MATERIAL_ENCHANT, 8
				snd SOUNDLIST_INV
				gosub *com_inventory
				if ( stat == 1 ) {
					p = 0
					if ( ibit(ITEM_BIT_ALIVE, ci) ) {
						p += 10000
					}
					repeat 15
						if ( inv(40 + cnt * 2 + 1, ci) > 0 ) {
							if ( inv(40 + cnt * 2, ci) >= 10000 ) {
								if ( inv(40 + cnt * 2, ci) / 10000 == 6 ) {
									p += 100
									continue
								}
								if ( inv(40 + cnt * 2, ci) / 10000 == 1 ) {
									p -= inv(40 + cnt * 2 + 1, ci) * 6 / 10
								}
								if ( inv(40 + cnt * 2, ci) / 10000 == 3 ) {
									p -= inv(40 + cnt * 2 + 1, ci) / 2
								}
							}
							p += inv(40 + cnt * 2 + 1, ci)
						}
					loop
					listmax = 0
					buff = lang("ふむふむ…。こいつのエンチャントは" + p + "パワーってところだぜ。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					p = 0
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 1001 | gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 9 ) {
			if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) <= 10 ) {
				listmax = 0
				buff = lang("ようこそ。歓迎するぜ、心の友よ。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…ここはうちのジジイのライバルが昔やっていた工房跡地だ。その人は変わり者だったけど根は優しくてさ。あたしはジジイに内緒でよく遊びに来ていたんだ。あの日までは…。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("魔物に工房を襲撃されたんだ、と皆は言っていた。だけどあたしは気付いちまったんだ。一見、めちゃくちゃに荒らされたように見えるが、価値のある作品が特に念入りに破壊されていることに。そして恐ろしい仮説が脳裏に浮かんできた…でも証拠もなかったし、何よりそれを信じたくなかった。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ここを訪れたのは何十年ぶりだろう。とりあえず墓と掘っ立て小屋の作成はイルマが一晩でやってくれたよ。故郷を捨てるのはつらいが、もう戻るわけにはいかない。ここでイルマと暮らしていくつもりだ…。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) <= 10 ) {
					gdata(GDATA_FLAG_SUB_IRMA_DAGGER) = 11
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 7 ) {
			listmax = 0
			buff = lang("…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あんたには世話になった。この洞窟を出て、真北の山のふもとに廃屋がある。今度はそこで会おう。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 5 | gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 6 ) {
			listmax = 0
			buff = lang("どうだった？流石の偏屈ジジイも少しは見直していただろう？", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			listmax = 0
			buff = lang("なっ…。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ジジイっ…！腹立たしいぜ！もはや我慢ならん！", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あんたには世話になった。この洞窟を出て、真北の山のふもとに廃屋がある。今度はそこで会おう。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 7
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) >= 3 & gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) < 5 ) {
			listmax = 0
			buff = lang("えへへ…。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("いや、やっぱり外の人たちと話すのは新鮮でいいもんだなって。新しい風がこの集落を変える、なんだかそんなふうに思えてくるのさ。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 2 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("また今度", "Please tell me my misfortune.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("何をすればいい？", "Sorry.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("心の友になったつもりはない", "Please tell me my misfortune.")
				listmax++
				buff = lang("やぁ、心の友よ。ちょっと手伝ってくれないか。", "Mreow... You daring to disturb my laze meow, what's the meaing of this meow?")
				gosub *chat_select
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("なっ…。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ま、まぁ。あたしにはイルマがいるからな…。寂しくなんかないぜ。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("いいね…頼りになるじゃないか。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ちょっと残ってた素材と道具でイルマにサンプルを作ってもらったんだが、イルマの腕はかつての職人たちと比べても遜色ないと思うのさ。あの偏屈ジジイに少しでも職人魂が残っているなら、正攻法で認めさせてやれるかもしれない。ジジイに見せる前に、あんたも冒険者の視点で見て何かアドバイスをしてやってくれ。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 3
					snd SOUNDLIST_WRITE1
					txtef COLOR_GREEN
					txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				}
				goto *chat_end
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("武具職人にならないか", "Please tell me my misfortune.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("よそ者について", "Sorry.")
		listmax++
		buff = lang("この集落はドン詰まりさ。今の子供たちが大人になる頃にはもうにっちもさっちもいかなくなってるだろうな。それでも土地への愛着で誰も離れようとしないんだろうけど。", "Mreow... You daring to disturb my laze meow, what's the meaing of this meow?")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("…ジジイの差し金かい？言っとくけど、あたしはあいつのことが大嫌いだ。昔からダサいことばかりしやがって…あたしを職人にしたいってのも、結局は名声のためでさ。あいつの喜ぶようなことはしたくないね。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("ここの連中はよそ者が住み着くことを嫌っている。イルマが定住を申し出たときは特に酷かったな。「大昔にこの丘を捨てて出て行った民の子孫だから」とか理由をつけて、殺してでも追い出そうとしていたくらいだ。丘長の孫娘であるあたしが空き部屋を貸すと言ったら途端に静かになりやがったけど。今では表向きではいない扱い、裏では陰湿な嫌がらせってとこみたいさ。本当にダサいぜ。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 1 ) {
				listmax = 0
				buff = lang("…この丘に伝わる武具作成の技法だって？子供の頃からジジイに嫌というほど聞かされたから、知識としては持っているぜ。だけど、あたしはあんたやイルマに心を許したわけじゃない。伝統技法を教えてもいいような人物か、見極めさせてもらう。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 2
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 652 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) >= 1001 ) {
			if ( gdata(GDATA_THALIA_LIMIT) < 1500 ) {
				gdata(GDATA_THALIA_LIMIT) = 1500
			}
			if ( gdata(GDATA_THALIA_LIMIT) > 2000 ) {
				gdata(GDATA_THALIA_LIMIT) = 2000
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("どうだろう", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("合成用アイテムを持ってきた", "Sorry.")
			listmax++
			buff = lang("よく夜中に猫やサリムがベッドに潜り込んでくるのじゃ。そんなに寒いのかのう？単にわしが寒さに慣れておるのかもな…ロストイルヴァに来る前はメイルーンに住んでいたからのう。", "So you are also of the same genus as the others...")
			gosub *chat_select
			if ( chatval == 1 ) {
				kiseki = gdata(GDATA_THALIA_LIMIT) + 500
				kiseki2 = gdata(GDATA_THALIA_LIMIT) + 1000
				listmax = 0
				buff = lang("おお、練習に付き合ってくれるのか。それで、エンチャントを付与する武具はどれじゃ？エンチャントが強力な武具は、魔術抵抗があって付与できないかもしれん。そうじゃな…サリムの言うエンチャント強度とやらで言うと、今は固定アーティファクトなら" + gdata(GDATA_THALIA_LIMIT) + "パワー、奇跡品なら" + kiseki + "パワー、神器品なら" + kiseki2 + "パワー、辺りが限界じゃ。それとネフィアティア以外を合成する場合は能力獲得の巻き物を一枚使うのう。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				invsubroutine = 1
				invctrl = INVCTRL_MATERIAL_ENCHANT, 8
				snd SOUNDLIST_INV
				gosub *com_inventory
				if ( stat == 1 ) {
					p = 0
					if ( ibit(ITEM_BIT_ALIVE, ci) ) {
						p += 10000
					}
					repeat 15
						if ( inv(40 + cnt * 2 + 1, ci) > 0 ) {
							if ( inv(40 + cnt * 2, ci) >= 10000 ) {
								if ( inv(40 + cnt * 2, ci) / 10000 == 6 ) {
									p += 100
									continue
								}
								if ( inv(40 + cnt * 2, ci) / 10000 == 1 ) {
									p -= inv(40 + cnt * 2 + 1, ci) * 6 / 10
								}
								if ( inv(40 + cnt * 2, ci) / 10000 == 3 ) {
									p -= inv(40 + cnt * 2 + 1, ci) / 2
								}
							}
							p += inv(40 + cnt * 2 + 1, ci)
						}
					loop
					if ( inv(INV_ITEM_QUALITY, ci) == FIX_QUALITY_MIRACLE ) {
						p -= 500
					}
					if ( inv(INV_ITEM_QUALITY, ci) == FIX_QUALITY_GODLY ) {
						p -= 1000
					}
					if ( p > gdata(GDATA_THALIA_LIMIT) ) {
						snd SOUNDLIST_ATK_ELEC
						listmax = 0
						buff = lang("くっ！こいつはちょっと無理じゃ。付与されているエンチャントが強く反発しておる！", "So you are also of the same genus as the others...")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						p = 0
						goto *chat_end
					}
					p = 0
					cibk = ci
				}
				else {
					goto *chat_end
				}
				ii = -1
				repeat 15
					if ( ii == (-1) ) {
						if ( inv(40 + cnt * 2, ci) == 0 ) {
							ii = cnt
						}
					}
				loop
				if ( ii == (-1) ) {
					listmax = 0
					buff = lang("むむむ。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("出直す", "Please tell me my misfortune.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("問題ない", "Sorry.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("エンチャントひとつ消去", "Sorry.")
					listmax++
					buff = lang("エンチャントが既に15個埋まっておるな。このまま組み込んでも同系エンチャント以外は乗らないんじゃが、大丈夫かの？", "So you are also of the same genus as the others...")
					gosub *chat_select
					if ( chatval != 2 & chatval != 1 ) {
						goto *chat_end
					}
					if ( chatval == 2 ) {
						if ( inv(40 + (15 - 1) * 2, cibk) == 37 | inv(40 + (15 - 1) * 2, cibk) == 45 ) {
							listmax = 0
							buff = lang("消去とな？ふむ、このエンチャントだと…ちょっと無理じゃな…すまんのう。", "So you are also of the same genus as the others...")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							goto *chat_end
						}
						listmax = 0
						buff = lang("消去とな？ふむ、このエンチャントなら結びつきを削り取ればどうにか…。", "So you are also of the same genus as the others...")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						snd SOUNDLIST_BUILD1
						await 300
						snd SOUNDLIST_BUILD1
						inv(40 + (15 - 1) * 2 + 1, cibk) = 0
						inv(40 + (15 - 1) * 2, cibk) = 0
						txt lang(itemname(cibk) + "のエンチャントがひとつ消えた。", itemname(cibk) + " removes an enchantment.")
					}
				}
				listmax = 0
				buff = lang("それで、どのアイテムの力をアーティファクトに組み込めばよいのじゃ？", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				invsubroutine = 1
				invctrl = INVCTRL_MATERIAL_ENCHANT, 9
				snd SOUNDLIST_INV
				gosub *com_inventory
				if ( stat == 1 ) {
					if ( inv(INV_ITEM_ID, ci) != ITEM_ID_CORE_OF_NEFIA ) {
						nefiat = 100
						item_find ITEM_ID_SCROLL_GAIN_ATTRIBUTE, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
						if ( stat == (-1) ) {
							listmax = 0
							buff = lang("ふむふむ…って、能力獲得の巻き物を持っておらんではないか。", "So you are also of the same genus as the others...")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							nefiat = 0
							goto *chat_end
						}
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_CORE_OF_NEFIA ) {
						if ( inv(INV_ITEM_PARAM1, ci) == 0 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_SHIELD, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 1 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_EVASION, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 2 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_DUAL_WIELD, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 3 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_TWO_HAND, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 4 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_TACTICS, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 5 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_MARKSMAN, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 6 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_HEALING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 7 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_DISARM_TRAP, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 8 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_HEAVY_ARMOR, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 9 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_MEDIUM_ARMOR, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 10 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_LIGHT_ARMOR, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 11 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_STEALTH, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 12 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_GREATER_EVASION, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 13 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_MEDITATION, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 14 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_MAGIC_DEVICE, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 15 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_CONTROL_MAGIC, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 16 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_CASTING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 17 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_WEIGHT_LIFTING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 18 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_CARPENTRY, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 19 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_TAILORING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 20 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_JEWELER, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 21 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_ACTIVE_PICKPOCKET, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 22 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_SWIMMING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 23 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_FISHING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 24 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_ANATOMY, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 25 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_LITERACY, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 26 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_GENE_ENGINEER, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 27 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_MEMORIZATION, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 28 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_FAITH, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 29 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_NEGOTIATION, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 30 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_INVESTING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 31 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_NORMAL_PERFORMER, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 32 ) {
							encadd cibk, ENCHANT_SKILL * EXT_ENCHANTMENT + SKILL_ACTIVE_RIDING, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 33 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_STR, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 34 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_CON, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 35 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_DEX, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 36 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_PER, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 37 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_LER, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 38 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_WIL, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 39 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_MAG, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 40 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_CHA, 500, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 41 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_SPD, 400, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 42 ) {
							encadd cibk, ENCHANT_ATTRIBUTE * EXT_ENCHANTMENT + SKILL_ATTR_LUC, 400, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 43 ) {
							encadd cibk, ENCHANT_RES_PARALYZE, 100, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 44 ) {
							encadd cibk, ENCHANT_RES_CONFUSE, 100, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 45 ) {
							encadd cibk, ENCHANT_RES_FEAR, 100, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 46 ) {
							encadd cibk, ENCHANT_RES_SLEEP, 100, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 47 ) {
							encadd cibk, ENCHANT_RES_POISON, 100, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 48 ) {
							encadd cibk, ENCHANT_RES_BLIND, 100, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 49 ) {
							encadd cibk, ENCHANT_SEE_INVISIBLE, 150, 0, 1, 0, 1
						}
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_GLIA_CRYSTAL ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_FIRE, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_HIBACHI ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_COLD, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_ACCUMULATOR ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_LIGHTNING, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_LAMP_PLANT ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_DARKNESS, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_MIND_DISC ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_MIND, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_BLACK_DRIP ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_POISON, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_TROLL_CELL ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_NETHER, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_MURDER_CD ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_SOUND, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_MAGIC_ICE ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_NERVE, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_CHAOS_JEWEL ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_CHAOS, 210, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_ETHER_MIRROR ) {
						encadd cibk, ENCHANT_RESISTANCE * EXT_ENCHANTMENT + SKILL_RES_MAGIC, 160, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_IKAROS_WING ) {
						encadd cibk, ENCHANT_FLOAT, 100, 0, 1
					}
					if ( inv(INV_ITEM_ID, ci) == ITEM_ID_TEAR_OF_GOD ) {
						if ( inv(INV_ITEM_PARAM1, ci) == 1 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_FIRE, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 2 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_COLD, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 3 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_LIGHTNING, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 4 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_POISON, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 5 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_CHAOS, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 6 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_SOUND, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 7 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_DARKNESS, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 8 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_GRAVITY_SPHERE, 150, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 9 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_MAGIC, 300, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 10 ) {
							encadd cibk, ENCHANT_PIERCING_CHANCE, 200, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 11 ) {
							encadd cibk, ENCHANT_REMOTE_BLOW, 250, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 12 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_NIGHTMARE, 200, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 13 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_NETHER_BREATH, 200, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 14 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_NETHER, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 15 ) {
							encadd cibk, ENCHANT_TIME_STOP, 150, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 16 ) {
							encadd cibk, ENCHANT_ENHANCE_SPELL, 200, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 17 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_ELEMENT_SCAR, 200, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 18 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_DARKNESS_BOLT, 200, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 19 ) {
							encadd cibk, ENCHANT_FIRE_AND_COLD, 150, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 20 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_GRENADE, 51, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 21 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_NERVE, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 22 ) {
							encadd cibk, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_MIND, 400, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 23 ) {
							encadd cibk, ENCHANT_MIND_AND_SOUND, 150, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 25 ) {
							encadd cibk, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_LIGHTNING_BOLT, 210, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 29 ) {
							encadd cibk, ENCHANT_SHOOTING_MECHANISM, 150, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 30 ) {
							encadd cibk, ENCHANT_DEC_ENEMY_CRIT_CHANCE, 300, 0, 1, 0, 1
						}
						if ( inv(INV_ITEM_PARAM1, ci) == 31 ) {
							encadd cibk, ENCHANT_EXTRA_RANGED, 150, 0, 1, 0, 1
						}
					}
					inv(INV_ITEM_NUM, ci)--
					snd SOUNDLIST_BUILD1
					await 300
					snd SOUNDLIST_BUILD1
					await 300
					snd SOUNDLIST_BUILD1
					await 300
					snd SOUNDLIST_DING2
					listmax = 0
					buff = lang("…エンチャント融合完了！いい練習になったのう。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( nefiat == 100 ) {
						item_find ITEM_ID_SCROLL_GAIN_ATTRIBUTE, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
						if ( stat != (-1) ) {
							ci = stat
							if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
								inv(INV_ITEM_NUM, ci)--
							}
						}
						nefiat = 0
					}
					if ( gdata(GDATA_THALIA_LIMIT) < 2000 ) {
						gdata(GDATA_THALIA_LIMIT) += 50
					}
				}
				else {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 9 ) {
			listmax = 0
			buff = lang("ご先祖様ゆかりの地で堂々と制作できればよかったんじゃが、こうなってしまっては仕方あるまい。静かなところでゆっくり作業するのも悪くないからのう。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("サリムから教わった伝統技法は、理論はわかるんじゃが実践が難しくてなぁ。丘の内と外に分離した技術で元は一つだったとは言え、独自進化を遂げてきたからのう。幾百もの世を越えて、再び一つの形に練り上げるにはもう少し時間が必要じゃな。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("よかったら、時々遊びに来とくれ。合成の練習台にしていいようなアイテムを持ってきてくれると助かるのう。", "So you are also of the same genus as the others...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 9 ) {
				gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 1001
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 5 | gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 6 | gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 7 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("訊かない方がいい", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("ボロクソだった", "Sorry.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("なぜ名前を入れた？", "Sorry.")
			listmax++
			buff = lang("結果はどうじゃった？", "So you are also of the same genus as the others...")
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("入れておかんと仮に良い評価を貰っても自作と証明できんと思ってのう。どうもよく思われていないようじゃからな…入れてなければ入れてないで難癖をつけられる可能性もあるかな、と。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			else {
				listmax = 0
				buff = lang("…なるほど。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 3 | gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 4 ) {
			if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 4 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("楽しみだ", "Please tell me my misfortune.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("持っていく短剣を変える", "Sorry.")
				listmax++
				buff = lang("さて、どうなるかの。もし評判が良かったら同じ形状のアーティファクトを作ろうかのう。", "So you are also of the same genus as the others...")
				gosub *chat_select
				if ( chatval != 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 3 ) {
				listmax = 0
				buff = lang("おお、見とくれ" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "殿！サリムの提案で、趣向の違う短剣を三本作ってみたんじゃ。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("実用性重視の短剣", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("美しい装飾の短剣", "Sorry.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("斬新な形状の短剣", "Please tell me my misfortune.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("三本とも見せればいい", "Sorry.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("エンチャントによる", "Please tell me my misfortune.")
			listmax++
			buff = lang("どれが一番受けがよさそうかの？まぁ、何を重視するのかは好みの問題とも思うのじゃが。", "So you are also of the same genus as the others...")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("うむ。やはり実際の使い心地は重要じゃろう。こいつは可変グリップが手に馴染み、刀身強度も抜群の一品じゃ。武骨なデザインじゃが、考えるのに一番悩んだかもしれん。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_IRMA_DAGGER) = 1
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("いやぁ。この小さな短剣にここまで細かい装飾を施すのには骨が折れたわい。精密さも腕前の指標じゃからな、集中して頑張ったんじゃよ。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_IRMA_DAGGER) = 2
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("ふふ。奇抜に見えるかもしれんが、これはこれで理に適った形状での。この独自構造によって、軽く押し当てるだけでも通常とは比べ物にならん切れ味が出るのじゃ。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_IRMA_DAGGER) = 3
			}
			if ( chatval == 3 ) {
				listmax = 0
				buff = lang("なるほど…確かに、作風の幅を見せておくのもよいかもしれん。何かに特化したものは好みが分かれるからのう。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_IRMA_DAGGER) = 4
			}
			if ( chatval == 4 ) {
				listmax = 0
				buff = lang("いや、あの…。造形の話であって、そういう趣旨ではないんじゃが…。", "So you are also of the same genus as the others...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 3 ) {
				if ( gdata(GDATA_FLAG_SUB_IRMA_DAGGER) != 0 ) {
					listmax = 0
					buff = lang("ふむ…。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("すまぬ。その短剣、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "殿が丘長さんに見せてやってくれんかのう。わしが行っても怒鳴られて門前払いされる気しかしないのじゃ…。", "So you are also of the same genus as the others...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					txt lang("イルマが作った短剣を預かった。", "I was entrusted with dagger from Irma.")
					gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 4
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("うむむ、参ったのう…。光に包まれてこの大陸に辿りついて…そして彷徨った先で、ご先祖様たちの故郷を見つけた。その時はまさに天の導きだと感激したのじゃが…。", "So you are also of the same genus as the others...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("どうも歓迎されてないようじゃな、とほほ…。出かけている間に仕事道具を壊されたり材料を隠されたり、散々じゃ。誰がやったのか問い正しても管理が悪いとなじられる始末での…。", "So you are also of the same genus as the others...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("しかし！ここであきらめるわけにはいかん！この始まりの丘に伝わる技法を習得し、わしの技法と統合して最高の武具を作り出すのが夢なのじゃ！とは言っても丘長さんは何も教えてくれぬし、他の民は技法を知らぬようじゃし…八方ふさがりじゃ。もし" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "殿が耳寄りな情報を知ったら教えてくれい…。", "So you are also of the same genus as the others...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) == 0 ) {
			gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 654 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 510 ) {
			if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
				listmax = 0
				buff = lang("ちょっと体が大きくて力が強くて毛深いくらいで熊呼ばわりってみんな酷すぎるよ。傷つくんだよね…。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) == 505 ) {
			if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
				listmax = 0
				buff = lang("このネフィアの一番下層に混沌の神的なものが眠っていて、それが異変の原因になっている！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("って私の冒険者としての勘が言うからここに来ているんだけど。他にも複数の冒険者が下層に向かっているみたい。うーん。みんな勘がいいんだなぁ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 510
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) >= 1000 ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("この街も年中雪が降ってるけど、メイルーンに比べれば全然暖かいほうだよ。このくらいなら裸で走りまわっても平気だね。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 100 ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("協力ありがとう、助かったよ。後の掃除は私一人で出来る…というかやらせてよ。私を熊呼ばわりしてからかっていた連中に活躍を見せつけておかなきゃいけない。…なんてね。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("これだけの大仕事、国が報酬とか出してくれてもいいと思うんだけどなぁ。まぁいいや、勝手にやったことだし。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 4 ) {
			if ( gdata(GDATA_LEVEL) == 100 ) {
				listmax = 0
				buff = lang("あんまり知られてないけど、実はここって観光名所みたいな扱いで、実際に城として使われてはいないんだよね。それを知って驚く観光客がとても多いんだけど、あれはなんなの？メイルーンの人間は氷の建物やカマクラで生活しているとでも思っているの？", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("まぁ…私もメイルーンから出てみるまで、各国に勝手なイメージを抱いていたから偉そうなことは言えないんだけど。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) = 5
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 3 ) {
			if ( gdata(GDATA_LEVEL) == 100 ) {
				listmax = 0
				buff = lang("やれやれ、吹雪の中をものすごい速さで駆け回るから振り落とされるかと思ったよ。…なんかガタガタ震えてるけど大丈夫かい？", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("それはともかく、ここがメイルーンの氷の城さ。前回で、街々は解放してきたんだけど城は守りが固くて突破できなかったんだ。今回狙うのは最奥に陣取ってる悪魔の首。あいつさえ仕留めれば、残りの雑魚は私一人でも片づけられる。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…それにしても酷い有様だ、前来た時よりもボロボロになってるよ。床にも屋根にも大穴が開いている、奥まで辿り着けるか…？", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) = 4
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) > 0 ) {
			if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) < 100 ) {
				if ( gdata(GDATA_LEVEL) == 1 ) {
					if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 2 ) {
						listmax = 0
						buff = lang("あれ？君どこかで会ったっけ？まぁいいや。君、強そうだね。ちょっと一緒に来てほしいんだ。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 2 ) {
						listmax = 0
						buff = lang("化け物を倒そうと慌てて故郷に戻ったんだけど、予想以上に敵が多くてあと一歩のところで撤退せざるをえなくてさ。ホント参っちゃうよ、わざわざ徒歩で雪原と氷河渡ったのに。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 2 ) {
						listmax = 0
						buff = lang("大丈夫、大丈夫。誰が用意したのか知らないけどさっき犬ぞりが置いてあるの見つけたから、それを使わせてもらおう。歩いていく必要はないよ。めちゃくちゃでかい犬だからクレバスにも落ちないんじゃないかな。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("その場を離れる", "No problem.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("わかった", "Sorry.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("行きたくない", "It was very painful.")
					listmax++
					buff = lang("いつまでも準備に時間かけてるわけにはいかない。さぁ、出発しようか。", "S-sorry. Not hurt are you?")
					gosub *chat_select
					if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) < 3 ) {
						gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) = 3
					}
					if ( chatval == 1 | chatval == 2 ) {
						listmax = 0
						buff = lang("なにボサっとしてるのさ。ほら、行くよ。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						mapidou = 100
						gdata(GDATA_TELEPORT_AREA) = AREA_NOYEL
						gdata(GDATA_TELEPORT_LEVEL) = 100
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 20
						mapstarty = 55
						dbg_skipevent = 1
						gdata(GDATA_HOUR) += 2
						gosub *advance_hour
						dbg_skipevent = 0
						chatteleport = 1
						goto *chat_end
					}
					else {
						listmax = 0
						buff = lang("ちょっと！私の話を聞いてくれよ！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) <= 378 ) {
			if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) == 0 ) {
				listmax = 0
				buff = lang("うわっ！？", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("大丈夫", "No problem.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("骨が折れた", "Sorry.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("あなたが銀熊か", "It was very painful.")
				listmax++
				buff = lang("わ、悪いね。怪我はないかい？", "S-sorry. Not hurt are you?")
				gosub *chat_select
				listmax = 0
				buff = lang("そんなことより大変なんだよ！下の階で変な三人組を見かけてさ。不審に思って物陰から様子を窺っていたんだ。すると三人組の一人が突然…何て言ったらいいのかな、空間を捻じ曲げて別の空間と繋いだというか…とにかく見間違えるはずがない、繋げた先、あれは確かに私の故郷の風景だった。", "Ah but more importantly! I saw some weird trio on the lower level.  Thought they were intruders so I spied on them from the shadows. Then one of the trio studdenly... not sure how to say it, distorted space connecting to another place...Well anyways it was something like that I'm sure of it.  It connect to somewhere that resembled where I'm from. Then they released a monster into my home town and were talking about all kinds of crazy stuff as they headed down farther....")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("恐ろしいこととは？", "What terrible?")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("どうでもいい", "It does not matter.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("それは知っている", "I know.")
				listmax++
				buff = lang("そしてあいつらは巨大な化け物を私の故郷に送り込むと、恐ろしいことを話しながら下へと降りて行ったんだ…。", "So you are also of the same genus as the others...")
				gosub *chat_select
				list(0, listmax) = 0
				listn(0, listmax) = lang("あなたを探していた", "I was looking for you.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("話を聞いてほしい", "I want you to listen to my talk.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("メルガストのことを教える", "Good luck.")
				listmax++
				buff = lang("じゃあ私は故郷が心配だからもう行くよ。なんとしてでも地上に降りてやるつもりさ！", "I'm heading back home because I'm worried. I will make it back outside!")
				gosub *chat_select
				snd SOUNDLIST_EXITMAP1
				txt lang("あなたの話を聞かず、冒険者は階段を駆けあがっていった…。", "Will not listen to you, adventurer went up the stairs....")
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 665 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) < 15000 ) {
			kaiwa = rnd(10)
			if ( kaiwa == 0 ) {
				listmax = 0
				buff = lang("ここは神々が永遠の盟約を結んだ場所でもあります。あの時の緊張しているジュア様も素敵でした…。はぁ…ジュア様…。", "I declare, these rays are just too strong and the sand just sticks everywhere and how awful is this dreadful place. I care not if this is an antenna but having sworn loyalty to Miss lulwy I have been given governance of this desert tower. That cruel part of her is just quite wonderful as well...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa >= 5 ) {
				listmax = 0
				buff = lang("混沌の本格的な侵攻が始まったというので、やむなく神殿を開放し神力の一部で地上と繋げましたが…まさか人が集まるのをいいことにこの神聖な場所で商売を始める者が出てくるとは。", "Miss Lulwy does come here on vacation.  Tea as she feels in the mood, frolicing together on the beach and punishing the prisoners. The best part are those hurricanes.  Inside this tower small hurricanes swirl around and jumble up everything. That wind slapping around here and there... Oh my, I really can't compete with her.  Just makes me tremble...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa < 5 ) {
				if ( kaiwa != 0 ) {
					listmax = 0
					buff = lang("本当はジュア様に付き従いたいのですが…神殿の守護をそのジュア様に任されたので、投げ出すわけにもいかないのです。", "Miss Lulwy does come here on vacation.  Tea as she feels in the mood, frolicing together on the beach and punishing the prisoners. The best part are those hurricanes.  Inside this tower small hurricanes swirl around and jumble up everything. That wind slapping around here and there... Oh my, I really can't compete with her.  Just makes me tremble...")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 668 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) >= 10 ) {
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 == 0 ) {
				listmax = 0
				buff = lang("おかげで助かっている。再び貴君の助力が必要となった時は、また連絡するのでよろしく頼む。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 == 6 ) {
			listmax = 0
			buff = lang("貴君に感謝する。検査の結果、数機が電子ウイルスに感染して狂っていたことが判明。他の者も電子頭脳が熱暴走しており、いつ爆発してもおかしくない状態であった。これは私からの礼だ。受け取ってくれ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) == 6 ) {
				flt
				itemcreate -1, ITEM_ID_N97_CANNON, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			}
			else {
				if ( gdata(GDATA_MIN) >= 0 & gdata(GDATA_MIN) < 15 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 25
					encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_LIGHTNING_BOLT, 210, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 15 & gdata(GDATA_MIN) < 30 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 29
					encadd ci, ENCHANT_SHOOTING_MECHANISM, 150, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 30 & gdata(GDATA_MIN) < 45 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 30
					encadd ci, ENCHANT_DEC_ENEMY_CRIT_CHANCE, 300, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 45 & gdata(GDATA_MIN) < 60 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 31
					encadd ci, ENCHANT_EXTRA_RANGED, 150, 0, 1, 0, 1
				}
			}
			gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) += 4
			snd SOUNDLIST_COMPLETE1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) >= 10 ) {
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 == 1 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("もうこりごりだよ〜", "I'm busy now.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("次の区画はもっとうまくやれるでしょう", "I'll try to do.")
				listmax++
				buff = lang("ログを見るに、他の区画でも暴走している作業機械がいるようなのだが…潜入して彼らにも精密検査を受けさせてくれないだろうか？各区画は独立していて、外部からでは指導はできても強制ができないのだ。", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval != 1 ) {
					listmax = 0
					buff = lang("そうか…すまない。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				listmax = 0
				buff = lang("なんと頼もしい。貴君に託すぞ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM)++
				gdata(GDATA_FLAG_WORK_A_REMAINING) = 1000 + rnd(300)
				gdata(GDATA_FLAG_WORK_B_REMAINING) = 2000 + rnd(400)
				gdata(GDATA_FLAG_WORK_C_REMAINING) = 3000 + rnd(500)
				mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
				gdata(GDATA_TELEPORT_AREA) = AREA_DOCK
				gdata(GDATA_TELEPORT_LEVEL) = 70
				levelexitby = MAP_EXIT_TELEPORT
				gdata(GDATA_LEVEL_START_ON) = 7
				mapstartx = 15
				mapstarty = 12
				chatteleport = 1
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 >= 2 & gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 < 6 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("しばらく行きたくない…", "I'm busy now.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("もうちょっとやってみる", "I'll try to do.")
			listmax++
			buff = lang("潜入の方は上手くやれそうかね？", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("…無理せず休んでくれたまえ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			listmax = 0
			buff = lang("了解した。無理せずじっくり取り組んでもらって構わない。健闘を祈っている。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			kurikaesitaisaku = 1
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_DOCK
			gdata(GDATA_TELEPORT_LEVEL) = 70
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 15
			mapstarty = 12
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("今忙しいんだ", "I'm busy now.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("暇だしやるよ", "I'll try to do.")
			listmax++
			buff = lang("長期戦になるかもしれないが…やってくれるかね？", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("そうか。すまない。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			listmax = 0
			buff = lang("関係者以外は立ち入り禁止なのだが、わが権限で特別にＩＤと転送経路を用意した。怪しまれずに潜入できるだろう。健闘を祈っている！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) = 2
			gdata(GDATA_FLAG_WORK_A_REMAINING) = 1000 + rnd(300)
			gdata(GDATA_FLAG_WORK_B_REMAINING) = 2000 + rnd(400)
			gdata(GDATA_FLAG_WORK_C_REMAINING) = 3000 + rnd(500)
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_DOCK
			gdata(GDATA_TELEPORT_LEVEL) = 70
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 15
			mapstarty = 12
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) == 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("興味がない", "Good luck.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("内容を知りたい", "I was looking for you.")
			listmax++
			buff = lang("よくぞ来てくれた。貴君をわが艦に迎え入れたのは、協力を要請したいことがあるためだ。もちろん報酬は用意しているが、どうかね？", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("…とある区画で暴走している作業機械がいる。こちらからの指令も一切受け付けない状態だ。送り込んだ者たちは囚われ、1機たりとも帰ってこなかった。艦長である私は持ち場を離れるわけにはいかない…そこで、だ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("貴君には該当区画に潜入し、作業機械の暴走を止めてもらいたいのだ。その気になったらまた話しかけてくれたまえ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) = 1
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 675 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) == 0 ) {
			gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) = 2
		}
		if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) \ 3 == 0 ) {
			listmax = 0
			buff = lang("限定コンサートは大成功ー♪キミが守ってくれたおかげだねっ！これはお礼！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("クエストを達成した！", "You have completed the quest!")
			snd SOUNDLIST_COMPLETE1
			txtmore
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) == 3 ) {
				flt
				itemcreate -1, ITEM_ID_MELODYUS, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			}
			else {
				if ( gdata(GDATA_MIN) >= 0 & gdata(GDATA_MIN) < 15 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 20
					encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_GRENADE, 51, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 15 & gdata(GDATA_MIN) < 30 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 21
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_NERVE, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 30 & gdata(GDATA_MIN) < 45 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 22
					encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_MIND, 400, 0, 1, 0, 1
				}
				if ( gdata(GDATA_MIN) >= 45 & gdata(GDATA_MIN) < 60 ) {
					flt
					itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_PARAM1, ci) = 23
					encadd ci, ENCHANT_MIND_AND_SOUND, 150, 0, 1, 0, 1
				}
			}
			if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) >= 30 ) {
				gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) = 3
			}
			gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE)++
			snd SOUNDLIST_COMPLETE1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) \ 3 == 2 ) {
			if ( gdata(GDATA_DAY) == 30 ) {
				if ( gdata(GDATA_LEVEL) == 1 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("断る", "No.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("引き受ける", "OK.")
					listmax++
					if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) == 2 ) {
						buff = lang("あっ、キミキミ！月末の限定コンサートなんだけど、会場内の警備に回す人員がいなくてさ。報酬は出すから引き受けてくれないかなー？ホント緊急なの！", "Sorry, this is untranslated sentence.")
					}
					if ( gdata(GDATA_FLAG_SUB_KARAVIKA_OF_SONG_AND_DANCE) != 2 ) {
						buff = lang("あっ、いいところに！またキミに会場警備をお願いしたいんだけど、いいかな？", "Sorry, this is untranslated sentence.")
					}
					gosub *chat_select
					if ( chatval == 1 ) {
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						listmax = 0
						buff = lang("やった！そうと決まれば急いで会場に向かうよ！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("着いたらすぐに始めるから！私の演奏が中断されたりお客さんが死んだりしたら失敗だよ。外の警備を突破して会場にストーカーが押し入ってくると思うから、コンサートを守り抜いてね。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						snd SOUNDLIST_WRITE1
						txtef COLOR_GREEN
						txt lang("ジャーナルが更新された。", "Your journal has been updated.")
						mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
						gdata(GDATA_TELEPORT_AREA) = AREA_OL_DRAN
						gdata(GDATA_TELEPORT_LEVEL) = 5
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 14
						mapstarty = 9
						chatteleport = 1
						goto *chat_end
					}
					else {
						listmax = 0
						buff = lang("う〜ん。ファンには悪いけど安全対策ができないとやっぱり中止だよねぇ…。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
				}
				if ( gdata(GDATA_LEVEL) == 5 ) {
					listmax = 0
					buff = lang("みんなお待たせ〜！さぁ、今日も歌って踊って奏でるよーっ！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					snd SOUNDLIST_CHEER
					snd SOUNDLIST_CHEER
					snd SOUNDLIST_CHEER
					listmax = 0
					buff = lang("よーし、テンション上がってきた！！！愛してるぜベイベー！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbaby
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					cdata(CDATA_ROW_ACT, tc) = ACTION_PERFORM
					cdata(CDATA_ACTION_PERIOD, tc) = 9999
					ens = 1
					cdata(CDATA_RELATION, tc) = RELATION_DISLIKE
					gdata(GDATA_FLAG_KAMIKAZE) = 0
					txtef COLOR_YELLOW
					txt lang("限定コンサートを守り抜こう！", "You must protect the concert from the stalker!")
					repeat 245
						if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
							continue
						}
						if ( cnt < 16 ) {
							continue
						}
						if ( cbit(CHARA_BIT_NECRO, cnt) == TRUE ) {
							continue
						}
						if ( cdata(CDATA_ROLE, cnt) == ROLE_ADVENTURER ) {
							continue
						}
						if ( cbit(CHARA_BIT_GUARD_TEMP, cnt) == TRUE ) {
							continue
						}
						cbitmod CHARA_BIT_BODYGUARD, cnt, TRUE
					loop
					flt
					characreate -1, CREATURE_ID_GOOSE, 14, 1
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 8, 6
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 14, 6
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 20, 6
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 2, 17
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 26, 17
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 2, 27
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					flt
					initlv = 125
					characreate -1, CREATURE_ID_GOLDEN_KNIGHT, 26, 27
					cdata(CDATA_AI_CALM, rc) = 3
					cdata(CDATA_RELATION, rc) = RELATION_DISLIKE
					cdata(CDATA_RELATION_ORG, rc) = RELATION_DISLIKE
					cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
					goto *chat_end
				}
				goto *chat_end
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("ごめんなさい", "I was looking for you.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("サインください", "Good luck.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("付き合ってください", "Good luck.")
		listmax++
		if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) \ 2 == 1 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("【カラヴィカちゃん握手権を使う】", "<Handshake rights of Karavika>")
			listmax++
		}
		buff = lang("えっと…集中したいからあまり話しかけないでね？", "Sorry, this is untranslated sentence.")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("わかってくれればいいの。あたしのコンサート楽しんでいってね！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…あと、おひねりもよろしくっ！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("握手権？ちょっとまってね…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("はい、オッケー！じゃあ手を出して！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) == 1 ) {
				listmax = 0
				buff = lang("うーん、初めての人と握手するのは緊張するなぁ。…これからも応援してね！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strthankyou
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) == 3 ) {
				listmax = 0
				buff = lang("また握手に来てくれたんだ？ふふん。これでも、握手したファンの顔はちゃんと覚えているんだよっ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strthankyou
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) == 5 ) {
				listmax = 0
				buff = lang("まいどごひいきに！…ふふっ、ヤカ姐の真似しちゃった。無理には信仰しなくていいけど、富の女神もよろしくね！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strthankyou
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) == 7 ) {
				listmax = 0
				buff = lang("よーし、今日は一段と張り切っちゃうぞ。あたしの歌と踊りと演奏を楽しんでいってね！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strthankyou
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) == 9 ) {
				listmax = 0
				buff = lang("ここまで応援してくれる人がいると思うと、なんか感激しちゃうな。うん…素直に嬉しいよ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strthankyou
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) == 11 ) {
				listmax = 0
				buff = lang("キミは握手が好きだねー。…あたしも好きだけどね！なんだか、元気を貰える気がするの。捧げものから神力を貰うのもこんな感じなのかな？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strthankyou
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			p = gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) / 2
			if ( gdata(GDATA_FLAG_HANDSHAKE_RIGHTS) >= 13 ) {
				listmax = 0
				buff = lang("これで握手に来てくれるの" + p + "回目だよね？今日も盛り上がっていくよベイベー！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbaby
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			modimp tc, 10
			gdata(GDATA_FLAG_HANDSHAKE_RIGHTS)++
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("あー。サインについてはヤカ姐も教えてくれなかったっけ…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("練習しておくからまた今度、ね？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 2 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("ごめんなさい", "I was looking for you.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("付き合ってください！", "Good luck.")
			listmax++
			buff = lang("そ、そういうのはちょっと。事務所を通してもらわないと…。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("ごめんなさい", "I was looking for you.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("付き合ってくだぁさい！！", "Good luck.")
				listmax++
				buff = lang("ごめんね。そういうのって困るんだ。まだ曲も残ってるし…。", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("ひっ…！？ス、ストーカー？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ガード！ガード！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					repeat 4
						await 50
						snd SOUNDLIST_FOOT1A
						await 50
						snd SOUNDLIST_FOOT1B
						await 50
					loop
					if ( jp ) {
						txt lang("黄金の騎士たちが駆け付けてきた。", "Nazuna yells, " + cnvtalk("You've no need of fancy wear in training! Now, go change!"))
						txtef COLOR_YELLOW
						txtmore
						txt lang("「なんのつもりだ！」", "Golden knight yells, " + cnvtalk("Stop there criminal!"))
						txtef COLOR_YELLOW
						txtmore
						txt lang("「変質者め、おとなしくしろ」", "Golden knight yells, " + cnvtalk("Be quiet!"))
						txtef COLOR_YELLOW
						txtmore
						txt lang("「のこのこ現れるとはな！」", "Golden knight yells, " + cnvtalk("You scum! Stay there!"))
						txtef COLOR_YELLOW
						txtmore
						txt lang("「観念しろ！」", "Golden knight yells, " + cnvtalk("You will pay for your sin!"))
					}
					listmax = 0
					buff = lang("(騎士に引きずられていくあなたを見ながら、怯えている…)", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					evadd EVENT_QUEST_TIMEOUT
					goto *chat_end
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 676 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) >= 32 ) {
			listmax = 0
			buff = lang("おかしい…少し目を離した隙にまた靴下が全部無くなっているぞ。", "If you find any unicorn horns bring them to me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("備品調達係、仕事だよ。また靴下30枚集めてきな。…そう嫌な顔をするなって。また褒美はやるよ。", "If you find any unicorn horns bring them to me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) = 2
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) >= 2 ) {
			if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) <= 32 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("ちょっと待って", "I was looking for you.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("所持靴下全納入", "Good luck.")
				listmax++
				buff = lang("どうだい？靴下は持ってきたのかい？", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 1 ) {
					inv_getheader 0
					f = 0
					repeat invrange, invhead
						if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) >= 32 ) {
							break
						}
						if ( inv(INV_ITEM_NUM, cnt) > 0 ) {
							if ( inv(INV_ITEM_ID, cnt) == ITEM_ID_SOCKS ) {
								if ( inv(INV_ITEM_EQUIP, cnt) == 0 ) {
									if ( ibit(ITEM_BIT_NO_DROP, cnt) != TRUE ) {
										inv(INV_ITEM_NUM, cnt)--
										gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM)++
										f = 1
										txt itemname(cnt, 1) + "を納入した。"
										continue cnt
									}
								}
							}
						}
					loop
					if ( f ) {
						listmax = 0
						buff = lang("よぉし…確かに受け取ったよ。", "You brought some unicorn horns? Thanks!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( f == 0 ) {
						listmax = 0
						buff = lang("しっかりしな。アタシに渡せる靴下がないじゃないか。", "If you find any unicorn horns bring them to me.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					gosub *calcBurdenPc
					if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) >= 32 ) {
						listmax = 0
						buff = lang("根性あるじゃないか…アンタは立派な騎士団備品調達係さ！褒美をやるよ、取っておきな。", "If you find any unicorn horns bring them to me.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
						if ( gdata(GDATA_FLAG_SUB_SOCKS_THIEF) == 12 ) {
							gdata(GDATA_FLAG_SUB_SOCKS_THIEF) = 100
							flt
							itemcreate -1, ITEM_ID_GRANDEL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						}
						if ( gdata(GDATA_HOUR) >= 0 & gdata(GDATA_HOUR) < 6 ) {
							flt
							itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							inv(INV_ITEM_PARAM1, ci) = 8
							encadd ci, ENCHANT_PROC * EXT_ENCHANTMENT + ENCHANT_PROC_GRAVITY_SPHERE, 150, 0, 1, 0, 1
						}
						if ( gdata(GDATA_HOUR) >= 6 & gdata(GDATA_HOUR) < 12 ) {
							flt
							itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							inv(INV_ITEM_PARAM1, ci) = 9
							encadd ci, ENCHANT_ELEMENTAL_DMG * EXT_ENCHANTMENT + SKILL_RES_MAGIC, 300, 0, 1, 0, 1
						}
						if ( gdata(GDATA_HOUR) >= 12 & gdata(GDATA_HOUR) < 18 ) {
							flt
							itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							inv(INV_ITEM_PARAM1, ci) = 10
							encadd ci, ENCHANT_PIERCING_CHANCE, 200, 0, 1, 0, 1
						}
						if ( gdata(GDATA_HOUR) >= 18 & gdata(GDATA_HOUR) < 24 ) {
							flt
							itemcreate -1, ITEM_ID_TEAR_OF_GOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							inv(INV_ITEM_PARAM1, ci) = 11
							encadd ci, ENCHANT_REMOTE_BLOW, 250, 0, 1, 0, 1
						}
						goto *chat_end
					}
					goto *chat_end
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_SOCKS_THIEF) == 12 ) {
			if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) == 1 ) {
				listmax = 0
				buff = lang("靴下泥棒をぶっ飛ばしてきたって？フハハハ！アンタやるじゃないか、見直したよ！その調子で靴下の調達も頼むよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) = 2
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_SOCKS_THIEF) < 12 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("騎士団に入りたい", "I was looking for you.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("口説きに来た", "I was looking for you.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("特にない", "Good luck.")
			listmax++
			buff = lang("おう、アタシになんか用か？", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 0 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("残念だ", "I was looking for you.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("雑用で構わない", "I was looking for you.")
				listmax++
				buff = lang("あー、悪い。そう言って盗みに入った不届き者がいてなぁ。今は身内以外の入団を認めてないんだよ。", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("雑用？うむむ…そういうことなら、試しに少し働いてもらおうか。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("とりあえず今言った泥棒は北東の雪原へ逃げていってね。まだ雪原を越えた辺りに潜伏しているかもしれない、見かけたら成敗しときな。もし真人間のふりをしていても、靴下を見せれば正体を現すだろうよ。後はそいつに盗まれた靴下30枚分を調達してきてくれ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) == 0 ) {
						gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) = 1
						snd SOUNDLIST_WRITE1
						txtef COLOR_GREEN
						txt lang("ジャーナルが更新された。", "Your journal has been updated.")
					}
					goto *chat_end
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("フハハ！面白い冗談を言うじゃないか、照れちまうよ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("おうちかえぅ", "I was looking for you.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("冗談じゃない", "I was looking for you.")
				listmax++
				buff = lang("さ、お子ちゃまはおうちに帰りな。", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("はいはい。いい子でちゅねー。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("フッハハハハハ！フハハハァー！…いや、悪いねぇ。アタシにとっちゃ冗談の域を出てないのさ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 817 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) >= 1000 ) {
				if ( gdata(GDATA_FLAG_MAIN) >= 575 ) {
					listmax = 0
					buff = lang("クロやんも腕を上げたものだ。昔ちょっと見せただけの宇宙剣術をしっかりものにしたうえで、独自のアレンジを加えているとは。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) == 1000 ) {
			kaiwa = rnd(8)
			if ( kaiwa == 0 ) {
				listmax = 0
				buff = lang("まじかよ、クロやん…うちの家宝の剣のイミテーション作ってるじゃないか。おそろいのつもりなんだろうけど…気まずいなぁ。前回勝手に家宝持ち出したこと親父に叱られたから、今回は持ってきてないんだよな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 1 ) {
				listmax = 0
				buff = lang("惑星ユゴスを訪れた時、スペースハンターのボッヘリト星人が外来種であるミ=ゴの駆除をしていてね。抵抗するミ=ゴもいたが、訓練された犬の３倍の能力を持つボッヘリト星人には次々と狩られていった。その手伝いの後のミ=ゴ鍋は旨かったなぁ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 2 ) {
				listmax = 0
				buff = lang("バイアクヘー星間旅行サービスも利用したことがあるんだが、あれは精神しか旅行できないから食べ歩きに不向きなんだよなぁ。事前準備も色々と面倒だし。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 3 ) {
				listmax = 0
				buff = lang("惑星サイクラノーシュを訪れた時は大変だった。現地のキノコ料理をご馳走になったまではいいんだが、勝手に彼らのボスの配偶者に選ばれてしまって。慌てて逃げ出したよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 4 ) {
				listmax = 0
				buff = lang("惑星トゥンツァに行った時、ちょうどそこの神の生誕祭をやっていてな。ラッキーだと思ったら、振る舞われたのが亜硫酸スープ。一応飲んだけど、舌がピリピリして味わうどころじゃなかった。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 5 ) {
				listmax = 0
				buff = lang("物も人も星すらも食い尽くして宇宙を渡り歩く宇宙最凶生物…デベロンダッタ星人とよく間違われて攻撃されるんだ。宇宙食べ歩きには危険がいっぱいだな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 6 ) {
				listmax = 0
				buff = lang("お土産にジェラミス星人のたこやきも用意していたんだが、あんまり暴れるものだから来る途中で全部食べてしまったよ。仕方がないから代わりにイス焼きを大量に買ってきた。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 7 ) {
				listmax = 0
				buff = lang("猫とかいう、ニャリン星人に似た生物がこの星には大勢いるな。前に来た時はすごく驚いた。スンバラリア星を侵略した強豪と聞いていたからな…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 515 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("知らん", "I think not.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("案内するよ", "Here, take these medals.")
				listmax++
				buff = lang("クロやん…いったい今どこにいるんだ…。", "It isn't a sox is it? ...Please, can you give it to me?")
				gosub *chat_select
				if ( chatval != 1 ) {
					goto *chat_end
				}
				if ( get_freeally() == 0 ) {
					txt lang("仲間の最大数に達しているため、これ以上連れていけない…。", "Your party is already full. You can't invite someone anymore.")
					goto *chat_end
				}
				listmax = 0
				buff = lang("なんだって？それは助かる！ではちゃんと案内してくれたら礼をしよう。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				rc = tc
				gosub *add_ally
				cbitmod CHARA_BIT_GUARD_TEMP, rc, TRUE
				r1 = rc
				gosub *charaRefresh
				if ( gdata(GDATA_FLAG_MAIN) < 520 ) {
					gdata(GDATA_FLAG_MAIN) = 520
				}
				gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) = 1
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) < 515 ) {
				listmax = 0
				buff = lang("わが名はベルファート。どこにでもいる、普通の宇宙剣客だ。この星には以前ふらっと立ち寄ったぐらいで場所もわからなかったのだが、宇宙ガイドとかいう人物に案内されて再び訪れることができた。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そうしてこの地でかつての友人を探している途中、ガイドを名乗る妖精に出会ったので尋ねてみたのだが。…知らないと一蹴されてしまってね。何でもいいから知らないか、とすがるうちに気が付いた。こいつからは隠し事をしている臭いがする。ボロを出すまで徹底的に問い詰めてやるつもりだ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 515
				goto *chat_end
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 684 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 580 ) {
				listmax = 0
				buff = lang("あー…腕に自信がないなら、次の階層はあんまり近寄らないほうがいいかもしれないね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 575 ) {
				listmax = 0
				buff = lang("いやー。駄目元でも交渉してみるものだね。おっと、こっちの話さ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 580
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) == 1 ) {
			if ( findallyguard(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) != (-1) ) {
				chara_vanquish findallyguard(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD)
				flt
				characreate -1, CREATURE_ID_BELPHAT_THE_COSMIC_SWORD, 6, 12
				cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
				listmax = 0
				buff = lang("少年の頃、僕には異星の友人がいてね。彼が母星に帰ってから数十年…別れ際に再会を誓ったけれど、僕はもうこんなおじさんになってしまった。生きている間にもう一度会いたいものだね…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("DNAスキャン…外見は変わっているが間違いない。クロやん！", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) == 0) + (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) != 0) * findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("え、誰！？", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) == 0) + (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) != 0) * findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…いや、待てよ。その呼び方は…わが友、ベルっちではないか？", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) == 0) + (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) != 0) * findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("いかにも。…しかしクロやんも変わったな。体毛が白くなり皮膚が収縮している。まさか病気か？", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) == 0) + (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) != 0) * findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("お前がなかなか戻ってこないから老化したんだよ！そう言うベルっちも変わりすぎだ！昔はもっと丸っこくて小さくてボーッっとした感じだったろ！一皮剥けたとかそういうレベルじゃないぞ。", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) == 0) + (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) != 0) * findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ああ。82回脱皮した。", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) == 0) + (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) != 0) * findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("剥けすぎだろ…。", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) == 0) + (findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE) != 0) * findchara(CREATURE_ID_KUROYA_THE_LOOKING_UNIVERSE)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("いかんいかん。話が長くなってしまう。先に、ここまで案内してくれた恩人に礼をしなきゃな。お土産の一部を受け取って…おっと。この星では足元に転がすのだったな。", "Sorry, this is untranslated sentence.")
				tc = tc * (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) == 0) + (findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD) != 0) * findchara(CREATURE_ID_BELPHAT_THE_COSMIC_SWORD)
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_AMULET_OF_YEKUB, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				flt
				itemcreate -1, ITEM_ID_GIFT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				inv(INV_ITEM_PARAM4, ci) = 4
				flt
				itemcreate -1, ITEM_ID_YITH_YAKI, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) = 1000
				txt lang("クエストを達成した！", "You have completed the quest!")
				snd SOUNDLIST_COMPLETE1
				goto *chat_end
			}
		}
		kaiwa = 0
		item_find ITEM_ID_SOCKS, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
				if ( ibit(ITEM_BIT_NO_DROP, ci) != TRUE ) {
					kaiwa = 1
				}
			}
		}
		if ( kaiwa == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("あげないよ", "I think not.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang(itemname(ci) + "を交換", "Here, take these medals.")
			listmax++
			buff = lang("おや…？君、靴下を持っているみたいだね。提案なんだけれど、その靴下１枚と信仰の巻物３枚を交換してくれないかな？", "It isn't a sox is it? ...Please, can you give it to me?")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("それは…残念だね。", "ngh... Sorry for asking the impossible.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				inv(INV_ITEM_NUM, ci) -= 1
				gosub *calcBurdenPc
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				flt
				itemcreate -1, ITEM_ID_SCROLL_FAITH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				if ( gdata(GDATA_FLAG_SUB_URCAGUARY_THE_TENACIOUS_GEM) >= 1 ) {
					if ( gdata(GDATA_FLAG_SUB_SOCKS_THIEF) <= 10 ) {
						list(0, listmax) = 0
						listn(0, listmax) = lang("靴下泥棒か？", "I think not.")
						listmax++
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("ほどほどに", "Here, take these medals.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("同志よ", "Here, take these medals.")
				listmax++
				buff = lang("いやぁ、最近はすっかりくつしたの虜で…。クライアントに引き渡すのが惜しいくらいだね。", "That shiney thing you've got there... It isn't a small medal is it? ...Please, can you give it to me?")
				gosub *chat_select
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("気が遠くなるほどの快楽…いいよね。焦りも苛立ちも、くつしたを嗅いでいる間は忘れられる。", "ngh... Sorry for asking the impossible.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("！！", "ngh... Sorry for asking the impossible.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					list(0, listmax) = 1
					listn(0, listmax) = lang("それならよし", "Here, take these medals.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("もはや問答無用", "Here, take these medals.")
					listmax++
					buff = lang("まさか雪原を越えて追手が来ていたとは…。ただ訂正をさせてもらうと、僕は靴下たちを盗んだわけじゃないんだ。床に無惨に脱ぎ散らかされていた可哀想な靴下たちを保護したに過ぎないんだ。", "ngh... Sorry for asking the impossible.")
					gosub *chat_select
					if ( chatval == 2 ) {
						listmax = 0
						buff = lang("…実力行使か！仕方がない。ソックスソードマン、いざ参る！", "ngh... Sorry for asking the impossible.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						cdata(CDATA_RELATION, tc) = RELATION_ENEMY
						cdata(CDATA_AI_AGGRO, tc) = 80
						cdata(CDATA_TARGET, tc) = 0
						gdata(GDATA_FLAG_SUB_SOCKS_THIEF) = 11
						goto *chat_end
					}
					if ( chatval != 2 ) {
						listmax = 0
						buff = lang("そう。靴下の匂いが、僕を呼んでいるのさ…。", "ngh... Sorry for asking the impossible.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					goto *chat_end
				}
				goto *chat_end
			}
			goto *chat_end
		}
		if ( kaiwa == 0 ) {
			if ( gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) >= 1000 ) {
				listmax = 0
				buff = lang("ベルっちの旅行話は、相変わらず内容がＢ級でゾクゾクするねぇ。本当に…また会えたんだなって実感が湧いてくるよ。子供の頃も、よくわからない固有名詞に戸惑いながら聞いていたっけ。ああ、懐かしいなぁ…。涙が出てきそうだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			listmax = 0
			buff = lang("この神の艦は空を飛び、イルヴァの外…宇宙をも航行できるのだとか。宇宙…行ってみたいものだね。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("少年の頃、僕には異星の友人がいてね。彼が母星に帰ってから数十年…別れ際に再会を誓ったけれど、僕はもうこんなおじさんになってしまった。生きている間にもう一度会いたいものだね…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("宇宙を航行できるというこの艦を見ていると切なさを感じるとともに、友人の話を聞いて宇宙に思いを馳せていた頃に戻ったような…そんな気分になれるんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 685 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("より良いツアーのプランを組み立てるには、常に各地の現状を把握しておく必要があります。それがたとえ火の中、水の中、草の中、森の中、土の中や雲の中だろうと。ツアープランナーはお客様の護衛がないぶんガイドよりも身軽ですが、未開拓地域の調査や力ずくでの交渉といった業務も多いため、高い戦闘力が要求されます。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 805 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) >= 1004 ) {
			listmax = 0
			buff = lang("兄さんは昔から過保護で…わたしのためによく無茶もしていました。わたしの存在が足枷になっているんじゃないか、と最近思うんです。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 1003 ) {
			listmax = 0
			buff = lang("あの、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さん？兄さんの知り合いだったんですね。その節は兄がご迷惑をお掛けしたみたいですみません…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("今でこそ人型を保ってますけど、メシェーラの侵食が無くなったわけではないので、勝手に触手が出たり突然腕が膨張したりわたしが弱ると身体を乗っ取られたりするんです。兄さんが毎晩神経を接続して調整はしてくれてるんですけど…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("兄と毎晩接続…な、なんか卑猥ですね。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 1004
			goto *chat_end
		}
		listmax = 0
		buff = lang("…わたしに近寄らない方がいいですよ。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 686 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) >= 1003 ) {
			listmax = 0
			buff = lang("こんな身体じゃ故郷にももう戻れん。幸い身体能力は向上しているから、このまま二人で冒険者としてやっていこうと思っているんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 1002 ) {
			listmax = 0
			buff = lang("お、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！レナイが！ついにオレの妹が見つかったんだ！苦労したよ本当に…。見つけた時にはもう身体が異形化しててさ、意識もほぼ無くて襲い掛かってきたんだぜ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("結論から言うと、ギリギリまで同化されることで神経を接続し、コントロールを菌から乗っ取ることでなんとか奪還できた。二人分同時に制御できるという保証はなかったから、完全にイチかバチかだったけどな。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ああ、オレがメシェーラを制御できるのは薬物と人体改造で神経系を強化されているかららしい。あんたと研究施設に行った時見つけた手帳に、そう書いていた…そうだ、この手帳やるよ。何かの手がかりにと思って持っていたけど、もう必要なさそうだしな。燃やすなり売り払うなり、ザナンの軍部をゆする材料にするなり好きにしてくれ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_RESEARCHERS_DIARY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 1003
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) >= 1000 ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("あれからあちこちの地域に行って聞き込みをしているんだけど、今のところ何も手がかりないんだよな。…早く探し出してやらないと。あいつは昔から寂しがり屋なんだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 100 ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("１６番独房には内側から破られた形跡があった。レナイはどこかできっと生きている。まだ希望はあるはずだ…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 4 ) {
			if ( gdata(GDATA_LEVEL) == 100 ) {
				listmax = 0
				buff = lang("オレが囚われていたのは１４番独房。妹の隣の部屋だった。すぐ隣にいるっていうのに、オレには根拠のない励ましの言葉をかけるくらいしかできなかった…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 5
			}
		}
		if ( gdata(GDATA_AREA) == AREA_NT_SOUTH_BORDER ) {
			if ( gdata(GDATA_LEVEL) == 100 ) {
				if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) == 3 ) {
					listmax = 0
					buff = lang("酷い目にあった…殺人的な加速だったな…。道理で冒険者にしか使わせないはずだ、市民なんかが乗ったら負荷で死にかねない。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("…とは言え実験場には着いたな。ここでは旧型メシェーラの生体強化能力に着目した兵器の開発・実験が行われていた。素体にされていたのは人間…オレもその一人だ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("オレは体内のメシェーラを制御するための改造手術が終了した後に隙を見て逃げ出せたんだが、最奥の１６番独房にはオレの妹の『レナイ』がまだ囚われているはずだ。…逃げるのに必死で、まさか迎えに来るのがこんなに遅れるとはな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("軍がさじを投げたほどの状況では望みが薄いが…オレのように適合している可能性もある。他の連中もだ。もし適合できずに苦しんでいたのなら…その時は、楽にしてやりたい。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("撤退のタイミングはあんたに任せる。またアレに乗ることになるけど、それはもう仕方ない。後は…ん？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("…厄介だな。被験者の動きを止めるための強力な神経ガスが散布されているみたいだ。大方、研究員が収容区画に撒いたんだろうが、入口付近にまで漏れてきているとは。注意したほうがいいだろう。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 4
				}
			}
		}
		if ( gdata(GDATA_AREA) == AREA_NT_SOUTH_BORDER ) {
			if ( gdata(GDATA_LEVEL) == 1 ) {
				if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) <= 2 ) {
					listmax = 0
					buff = lang("あんたが" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "だな。オレがエユルだ。よろしくな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("渡された資料を読んだんだが、神から借りてきた核駆動の超音速の乗り物であっと言う間に目的地まで移動できるらしい。なんだか凄そうだな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("ちょっと待って", "Good luck.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("出発", "I was looking for you.")
				listmax++
				if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) <= 3 ) {
					buff = lang("そろそろ出発しようか？", "Sorry, this is untranslated sentence.")
				}
				if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) > 3 ) {
					buff = lang("またアレに乗らなきゃいけないのか…。", "Sorry, this is untranslated sentence.")
				}
				gosub *chat_select
				if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) <= 2 ) {
					gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) = 3
				}
				if ( chatval == 1 ) {
					if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
						snd SOUNDLIST_FAIL1
						txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
						goto *chat_end
					}
					mapidou = 100
					mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
					gdata(GDATA_TELEPORT_AREA) = AREA_NT_SOUTH_BORDER
					gdata(GDATA_TELEPORT_LEVEL) = 100
					levelexitby = MAP_EXIT_TELEPORT
					gdata(GDATA_LEVEL_START_ON) = 7
					mapstartx = 4
					mapstarty = 4
					dbg_skipevent = 1
					gdata(GDATA_HOUR) += 2
					gosub *advance_hour
					dbg_skipevent = 0
					chatteleport = 1
					goto *chat_end
				}
				else {
					listmax = 0
					buff = lang("オーケー。ここで待ってるからな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 687 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_WAR_GROUND ) {
			listmax = 0
			buff = lang("リューツェ君を守って何かと戦っていたのは憶えている。その後光に包まれてこのわけのわからない場所に…。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("クソッ。獣が散々食い散らかした跡みたいに、記憶が断片しか残っていないなんて。それも、ぐちゃぐちゃでほとんど繋がらないときたわ…！", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( neres == 100 ) {
				listmax = 0
				buff = lang("チッ…置いてきたリューツェ君が心配になってきたわ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( neres == 0 ) {
				listmax = 0
				buff = lang("この世界のこと教えてあげるって言うからついてきたけど、何なのよここは。私、都合よく護衛に使われてる気がするんだけど。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				neres = 100
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 688 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_WAR_GROUND ) {
			if ( gdata(GDATA_FLAG_TALK_RYUTYE) == 0 ) {
				listmax = 0
				buff = lang("は、初めまして。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("僕はリューツェ…っていう名前らしいです。…ごめんなさい、自分の名前もよく憶えていないんです。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("あなたは…ええと、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "さんっていうんですね。あちこちを冒険されているんですか？羨ましいです、僕なんて屋敷から全然…あれ？なんだっけ…。僕は、今まで、「どういうふうに生きてきた」んだっけ…？", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TALK_RYUTYE) = 1
				goto *chat_end
			}
			listmax = 0
			buff = lang("ネレスちゃんがイライラしている気持ちもわかるんです。大切なものをいくつも失っていて…それが何なのか気が付くことができなくて…こうなった原因もわからなくて…とても、もどかしいんです。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 711 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 600 ) {
				listmax = 0
				buff = lang("さぁて、神の間にはどんな秘密が隠されてるのかなー？ふふふ、秘密を暴くのって楽しいもんね！", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 595 ) {
				listmax = 0
				buff = lang("へぇー。ロスリアの連中になかなか出くわさないと思ったら、本隊はまだ上の階層にいるんだ。やっぱり少数精鋭が身軽だよね！あまりに大所帯だと思うように進めないんじゃないかなぁ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 600
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_DAY) == 1 | gdata(GDATA_DAY) == 15 ) {
			if ( gdata(GDATA_FLAG_MELGET_SCORE) != (-1) ) {
				listmax = 0
				buff = lang("問題数ももっと増やさなきゃなぁ…。あ、次回のクイズは次の1日と15日にやるからね。本職のほうは明日からやるよ。", "I will perform next quiz on next 1th and 15th.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_DAY) == 1 | gdata(GDATA_DAY) == 15 ) {
			listmax = 0
			buff = lang("イエーイ！冒険者4択クイズ〜！ルールは簡単、10問連続の4択クイズに素早く正解しようっ！かかった時間と正解数で得点が決まるけど、制限時間は60秒。それを過ぎるといくら正解していても0点になるから気をつけて！ええっとキミなら…ティリス大陸にまつわる問題でいこうか。", "Yeah! I will put 10 quiz to you...the time limit is 60 seconds!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やらないよ", "Not ready.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("ＯＫ！", "All right!")
			listmax++
			buff = lang("それじゃあ準備はいいかな？問題文は下の方に出すからね！", "Ready? I will put out the question text statement at the bottom.")
			gosub *chat_select
			if ( chatval != 1 ) {
				listmax = 0
				buff = lang("なんてこったい。", "Sorry!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			snd SOUNDLIST_WHISTLE
			txtef COLOR_RED
			txt lang("スタート！", "Start!")
			music = MUSICLIST_MCARENA
			gosub *music_play
			startsec = timeGetTime() / 1000
			repeat 35
				qi = rnd(cnt + 1)
				qp(cnt) = qp(qi)
				qp(qi) = cnt
			loop
			repeat 10
				if ( qp(cnt) == 0 ) {
					txtsetquiz0 4
				}
				if ( qp(cnt) == 1 ) {
					txtsetquiz1 4
				}
				if ( qp(cnt) == 2 ) {
					txtsetquiz2 4
				}
				if ( qp(cnt) == 3 ) {
					txtsetquiz3 4
				}
				if ( qp(cnt) == 4 ) {
					txtsetquiz4 4
				}
				if ( qp(cnt) == 5 ) {
					txtsetquiz5 4
				}
				if ( qp(cnt) == 6 ) {
					txtsetquiz6 4
				}
				if ( qp(cnt) == 7 ) {
					txtsetquiz7 4
				}
				if ( qp(cnt) == 8 ) {
					txtsetquiz8 4
				}
				if ( qp(cnt) == 9 ) {
					txtsetquiz9 4
				}
				if ( qp(cnt) == 10 ) {
					txtsetquiz10 4
				}
				if ( qp(cnt) == 11 ) {
					txtsetquiz11 4
				}
				if ( qp(cnt) == 12 ) {
					txtsetquiz12 4
				}
				if ( qp(cnt) == 13 ) {
					txtsetquiz13 4
				}
				if ( qp(cnt) == 14 ) {
					txtsetquiz14 4
				}
				if ( qp(cnt) == 15 ) {
					txtsetquiz15 4
				}
				if ( qp(cnt) == 16 ) {
					txtsetquiz16 4
				}
				if ( qp(cnt) == 17 ) {
					txtsetquiz17 4
				}
				if ( qp(cnt) == 18 ) {
					txtsetquiz18 4
				}
				if ( qp(cnt) == 19 ) {
					txtsetquiz19 4
				}
				if ( qp(cnt) == 20 ) {
					txtsetquiz20 4
				}
				if ( qp(cnt) == 21 ) {
					txtsetquiz21 4
				}
				if ( qp(cnt) == 22 ) {
					txtsetquiz22 4
				}
				if ( qp(cnt) == 23 ) {
					txtsetquiz23 4
				}
				if ( qp(cnt) == 24 ) {
					txtsetquiz24 4
				}
				if ( qp(cnt) == 25 ) {
					txtsetquiz25 4
				}
				if ( qp(cnt) == 26 ) {
					txtsetquiz26 4
				}
				if ( qp(cnt) == 27 ) {
					txtsetquiz27 4
				}
				if ( qp(cnt) == 28 ) {
					txtsetquiz28 4
				}
				if ( qp(cnt) == 29 ) {
					txtsetquiz29 4
				}
				if ( qp(cnt) == 30 ) {
					txtsetquiz30 4
				}
				if ( qp(cnt) == 31 ) {
					txtsetquiz31 4
				}
				if ( qp(cnt) == 32 ) {
					txtsetquiz32 4
				}
				if ( qp(cnt) == 33 ) {
					txtsetquiz33 4
				}
				if ( qp(cnt) == 34 ) {
					txtsetquiz34 4
				}
				repeat 4
					promptl(0, promptmax) = s(cnt), key_select(cnt), "" + promptmax
					promptmax++
				loop
				val = promptx, prompty, 310, 0
				gosub *prompt_key
				if ( rtval == seitou ) {
					snd SOUNDLIST_ENC
					seikai++
				}
				else {
					snd SOUNDLIST_QUIZBAD
				}
				msg_newline
			loop
			endsec = timeGetTime() / 1000 - startsec
			snd SOUNDLIST_GONG
			gosub *music_play
			gdata(GDATA_FLAG_MELGET_SCORE) = limit(60 - endsec, 0, 60) * seikai
			listmax = 0
			buff = lang("お疲れ！経過時間は" + endsec + "秒、正解数は" + seikai + "だね。", "Cheers for good work! Elapsed time " + endsec + " seconds, the number of correct answers was " + seikai + ".")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			seikai = 0
			endsec = 0
			startsec = 0
			if ( gdata(GDATA_FLAG_MELGET_SCORE) > gdata(GDATA_FLAG_MELGET_HISCORE) ) {
				listmax = 0
				buff = lang("最終スコアは" + gdata(GDATA_FLAG_MELGET_SCORE) + "点だよ！今までの最高記録は" + gdata(GDATA_FLAG_MELGET_HISCORE) + "点だから…新記録だね！おめでとう！", "Your score is " + gdata(GDATA_FLAG_MELGET_SCORE) + " points! It's new record!!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MELGET_HISCORE) = gdata(GDATA_FLAG_MELGET_SCORE)
			}
			else {
				listmax = 0
				buff = lang("最終スコアは" + gdata(GDATA_FLAG_MELGET_SCORE) + "点！…うーん、最高記録である" + gdata(GDATA_FLAG_MELGET_HISCORE) + "点は更新できなかったか。また挑戦してね〜。", "Your score is " + gdata(GDATA_FLAG_MELGET_SCORE) + " points...The next, charange to update the best record " + gdata(GDATA_FLAG_MELGET_HISCORE) + " points!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_MELGET_SCORE) >= 200 ) {
				listmax = 0
				buff = lang("200点超えたから、一応努力賞出しておくね。一応、ね。", "Since such more than 200 points, I send an Effort Award.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			}
			gosub *music_play
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
			goto *chat_end
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("さようなら", "Good luck.")
		listmax++
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 10000 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("他冒険者の所持品(10000gp)", "Good luck.")
			listmax++
		}
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 500 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("今月の豆知識(500gp)", "I was looking for you.")
			listmax++
		}
		buff = lang("あー、色々しゃべっちゃいたくてウズウズするなぁ！どうしようかなー。何を教えちゃおうかなー？", "Sorry, this is untranslated sentence.")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("うーん…全部教えるのもつまらないし、キミ以外の冒険者が現在持っている固定アーティファクトや貴重品を「ひとつまで」教えてあげる。メモの準備はいい？一度しか言わないよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 10000
			snd SOUNDLIST_PAYGOLD1
			repeat 40 - 1
				rc = cnt
				if ( cdata(CDATA_ROLE, rc) != ROLE_ADVENTURER ) {
					continue
				}
				f = 0
				inv_getheader rc
				repeat 20
					ci = invhead + cnt
					if ( inv(INV_ITEM_NUM, ci) == 0 ) {
						continue
					}
					if ( inv(INV_ITEM_QUALITY, ci) != FIX_QUALITY_UNIQUE & ibit(ITEM_BIT_PRECIOUS, ci) != TRUE ) {
						continue
					}
					if ( inv(INV_ITEM_NUM, ci) != 0 ) {
						f = 1
						valn = itemname(ci, 1)
						break
					}
				loop
				if ( f == 0 ) {
					listmax = 0
					buff = lang(cdatan(CDATAN_AKA, rc) + " " + cdatan(CDATAN_NAME, rc) + " は貴重品を持っていないよ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( f == 1 ) {
					listmax = 0
					buff = lang(cdatan(CDATAN_AKA, rc) + " " + cdatan(CDATAN_NAME, rc) + " は" + valn + "を持っているよ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
			loop
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
			goto *chat_end
		}
		if ( chatval == 2 ) {
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 500
			snd SOUNDLIST_PAYGOLD1
			if ( gdata(GDATA_MONTH) == 1 ) {
				listmax = 0
				buff = lang("私、チョコレートとか嫌いだから。だからバレンタインの中止を願ったんだけど、そしたら願いの女神が手作りチョコをよこしてきたの。もったいないから仕方なく食べたけど、願いの女神も色々と大変ね…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 2 ) {
				listmax = 0
				buff = lang("とあるドラゴンハンターの攻撃は、ドラゴン類に対して１割ほど与えるダメージが多いの。ドラゴンにはドラゴンの攻撃が効果抜群…というわけではなくて、単純に強い殺意で力が入ってるんでしょうね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 3 ) {
				listmax = 0
				buff = lang("ザナンの実験施設の収容者リストを見ていて気がついたんだけど、全員の名前をつなげて読むと文章になるの。すごい偶然だと思わない？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 4 ) {
				listmax = 0
				buff = lang("中央神殿の神様はどれだけジュア狂なのかしら。目の前で癒しの女神に色々セクハラしようとしたけど、絶対に防衛されちゃうの。壁の向こうからでも察知して阻止するのよ？神様って怖い。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 5 ) {
				listmax = 0
				buff = lang("天使たちの中には輝く羽ペンを所持している者がいるけれど、特定の街や施設に滞在している天使は絶対に羽ペンを持っていないのよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 6 ) {
				listmax = 0
				buff = lang("カードが欲しくて砂漠の塔にいた神様に喧嘩売ったら、戦闘中に不幸の日が訪れたの。慌てて逃げ出したけど、それからしばらく大変だったなぁ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 7 ) {
				listmax = 0
				buff = lang("宇宙ミミズクは、撫でると気持ち悪いくらい喜ぶの。それも、仲が良くなれば良くなるほど激しくね…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 8 ) {
				listmax = 0
				buff = lang("栗を粗末にした者の前に現れる巨人は、栗をぶつけると大怪我を負うみたい。私もぶつけてみたけどすっごく痛がってたよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 9 ) {
				listmax = 0
				buff = lang("錬金術士を名乗る人々は、栗を投げる際に必ず「うに」だと言い張るの。まぁだからと言って特に何か変わるわけでもないんだけど。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 10 ) {
				listmax = 0
				buff = lang("宇宙に思いを馳せている誰かさんは、実は裏で老若男女問わず靴下収集を行っているの。せっかくだから私も靴下をアイテムと交換してもらったわ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 11 ) {
				listmax = 0
				buff = lang("丘の民の集落長は、あることないこと言いふらして気に入らない相手を社会的に抹殺しているの。４月馬鹿でもないのに不正確な情報をわざと流すなんて、情報屋として見過ごせないよ…！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 12 ) {
				listmax = 0
				buff = lang("この前ツアープランナーのお姉さんを見かけたんだけど、数日で街から去って行ったわ。ツアーの下準備で絶えずあちこちを回っているみたいだから、運がいいとキミも会えるかもね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 936 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) == 735 ) {
				listmax = 0
				buff = lang("どうだ、次元アンカーの威力は？物理的な力では絶対に抜け出せないぞ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("こちらガベラだ。通信状況は良好のようだな。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、あの怪物がテスカトリポカなのか？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("えっ、違う？？まぁいい。ロスリアでの戦闘直後、急に心配になってな。こいつを飛ばして正解だった。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("これなら勝てる！", "I appreciate it!")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("無理だ、下がれ！", "This place is dangerous!")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("A0-Iって何？", "What is A0-I?")
				listmax++
				buff = lang("奴の移動は封じた。畳みかけるぞ！", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("ああ勝てるさ、俺たちなら！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("無理だのなんだの言ってたら科学は進歩しないんだよ！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("よくぞ聞いてくれた。次元歪曲ユニットは勿論、重射撃装備、電子戦装備、増加装甲、追加推進装置、さらに自律戦闘通信機の特殊電波システム、イェルス超重力砲の小型改良版…束縛の悪魔との交戦データを参考に理論化した次元アンカー！その全てのオプションを実装したメルガストがA0-Iだ。これだけの同時装備を可能にしたのは生体ハードポイント理論からのデータ逆輸入によるところが大きく、俺の技術の集大成とも言える存在で特筆すべきは…おっと、解説している場合ではないな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 745 ) {
				listmax = 0
				buff = lang("しぶといやつだな…。完全に無力化するのは難しいかもしれん。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 755 ) {
				listmax = 0
				buff = lang("終わったようだな…なにもかも。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 790 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
			listmax = 0
			buff = lang("ラーネイレ…。", "Larnneire...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) == 725 ) {
				listmax = 0
				buff = lang("ふと胸騒ぎがして降りてみたが、まさか目と鼻の先にあったとは。60層はあるものだと思っていた。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("助かる！", "I appreciate it!")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("危ないよ！", "This place is dangerous!")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("ラーネイレたちが探していたよ", "Larnneire were looking for you.")
				listmax++
				buff = lang("…あれが噂に聞く混沌の神か？" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "、加勢するぞ。", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("猫の手ならぬ、鷹の翼も借りたい、といったところか。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("ふっ…危険は承知の上だ。私も今や冒険者の端くれだからな。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("ラーネイレが？彼女には悪いが、こいつを片づけるまで辛抱してもらう！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 735 ) {
				listmax = 0
				buff = lang("今、奴が何かに拘束されたように見えたが…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 745 ) {
				listmax = 0
				buff = lang("いくら移動を封じたとはいえ、倒せないのでは…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 755 ) {
				listmax = 0
				buff = lang("あれが凶獣の能力か…。人間が受けたら即座に廃人だな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) >= 660 ) {
				listmax = 0
				buff = lang("神の間、決戦因子、か。どうにも胡散臭いものを感じる。進むのをためらうほどにな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				listmax = 0
				buff = lang("君は…ひょっとして" + cdatan(CDATAN_AKA, CHARA_PLAYER) + "か？こんなところで知り合いに会うとは奇遇だな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("せめて昔の勘を取り戻すまで、私はこの階層で鍛錬するつもりだ。今でも力不足を感じるのに、あと何層あるのかわかったものじゃないからな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 660
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 822 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("そんなものはない", "No problem.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("最深攻略ネフィアの記憶", "Memory of the deepest Nefia")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("所属ギルドのこと", "Memory of belonging Guild")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("旅に出る前のこと", "Memory of before leaving on a journey")
		listmax++
		list(0, listmax) = 4
		listn(0, listmax) = lang("善の心・悪の心", "Conscience and malice")
		listmax++
		list(0, listmax) = 5
		listn(0, listmax) = lang("仲間との関係", "Relationship with ally")
		listmax++
		list(0, listmax) = 6
		listn(0, listmax) = lang("自分の技術と経験", "My skills and experience")
		listmax++
		list(0, listmax) = 7
		listn(0, listmax) = lang("仲間の技術と経験", "Fellow skills and experience")
		listmax++
		list(0, listmax) = 8
		listn(0, listmax) = lang("仲間の営業技術", "Fellow shop feats")
		listmax++
		list(0, listmax) = 9
		listn(0, listmax) = lang("生きている武器の能力と経験", "Memory of alive weapon")
		listmax++
		buff = lang("さぁ、消したい記憶のことだけ念じるのです。貴方の頭からも、この世界からも、痕跡を残さず綺麗に消してあげましょう。", "Think the only thing of memory you want to erase. Also from your head, even from this world, I will clean off without leaving a trace.")
		gosub *chat_select
		if ( chatval >= 1 & chatval <= 4 ) {
			snd SOUNDLIST_CURSE2
		}
		if ( chatval == 1 ) {
			snd SOUNDLIST_WRITE1
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata189の値が初期化されました。再計算を行います…。", "[System]By unknown error, globaldata189 has been initialized.")
			gdata(GDATA_DEEPEST_LEVEL) = gdata(GDATA_FLAG_MAIN) * 1 / 4 + 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			dmgsan CHARA_PLAYER, 100
			listmax = 0
			buff = lang("これで、またひとつ…。", "I was approached a little.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
		}
		if ( chatval == 2 ) {
			snd SOUNDLIST_WRITE1
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata263の値が初期化されました。", "[System]By unknown error, globaldata263 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata264の値が初期化されました。", "[System]By unknown error, globaldata264 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata265の値が初期化されました。", "[System]By unknown error, globaldata265 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata266の値が初期化されました。", "[System]By unknown error, globaldata266 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata269の値が初期化されました。", "[System]By unknown error, globaldata269 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata469の値が初期化されました。", "[System]By unknown error, globaldata469 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata470の値が初期化されました。", "[System]By unknown error, globaldata470 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaldata128の値が初期化されました。", "[System]By unknown error, globaldata128 has been initialized.")
			gdata(GDATA_RANK_GUILD) = 1000000
			gdata(GDATA_FLAG_GUILD_THIEF) = 0
			gdata(GDATA_FLAG_GUILD_FIGHTER) = 0
			gdata(GDATA_FLAG_GUILD_MAGE) = 0
			gdata(GDATA_FLAG_GUILD_MAGE_NORMA) = 0
			gdata(GDATA_FLAG_GUILD_THIEF_NORMA) = 0
			gdata(GDATA_FLAG_SUB_GUILD_MAGE) = 0
			gdata(GDATA_FLAG_SUB_GUILD_THIEF) = 0
			gdata(GDATA_FLAG_SUB_GUILD_FIGHTER) = 0
			gdata(GDATA_FLAG_SUB_FIGHTER_1) = 0
			gdata(GDATA_FLAG_SUB_MAGE_1) = 0
			gdata(GDATA_FLAG_SUB_THIEF_1) = 0
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			dmgsan CHARA_PLAYER, 100
			listmax = 0
			buff = lang("これで、またひとつ…。", "I was approached a little.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
		}
		if ( chatval == 3 ) {
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobalpcdata161の値が初期化されました。", "[System]By unknown error, globalpcdata161 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobalpcdata162の値が初期化されました。", "[System]By unknown error, globalpcdata162 has been initialized.")
			cdata(CDATA_BACKGROUND_PART_A1, CHARA_PLAYER) = 5
			cdata(CDATA_BACKGROUND_PART_A2, CHARA_PLAYER) = 29
			dmgsan CHARA_PLAYER, 100
			listmax = 0
			buff = lang("これで、またひとつ…。", "I was approached a little.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
		}
		if ( chatval == 4 ) {
			snd SOUNDLIST_WRITE1
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaltrait162の値が初期化されました。", "[System]By unknown error, globaltdata162 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobaltrait169の値が初期化されました。", "[System]By unknown error, globaltdata169 has been initialized.")
			trait(TRAIT_OTHER_WICKED) = 0
			trait(TRAIT_OTHER_SAINT) = 0
			dmgsan CHARA_PLAYER, 100
			listmax = 0
			buff = lang("これで、またひとつ…。", "I was approached a little.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
		}
		if ( chatval == 5 ) {
			allyctrl = 7
			gosub *com_ally
			if ( stat != (-1) ) {
				rc = stat
				snd SOUNDLIST_CURSE2
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalrcdata17の値が初期化されました。", "[System]By unknown error, globalrdata17 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalrcdata169の値が初期化されました。", "[System]By unknown error, globalrdata169 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalrcbit961が削除されました。", "[System]By unknown error, globalbdata961 has been initialized.")
				cdata(CDATA_IMPRESSION, rc) = 50
				cdata(CDATA_MASTER_SERVANT, rc) = 50
				cbitmod CHARA_BIT_MARRIED, rc, FALSE
				if ( cdata(CDATA_EXIST, rc) == CHAR_STATE_ALIVE ) {
					txtef COLOR_PURPLE
					txt lang(name(rc) + "は、初めて会ったような顔であなたを見ている…。", name(rc) + " is looking at you for the first time met such a face...")
				}
				dmgsan CHARA_PLAYER, 100
				listmax = 0
				buff = lang("これで、またひとつ…。", "I was approached a little.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
			}
		}
		if ( chatval == 6 ) {
			snd SOUNDLIST_CURSE2
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobalpcdata198の値が再計算されました。", "[System]By unknown error, globaltdata162 has been initialized.")
			txtef COLOR_RED
			txt lang("[システム]原因不明のエラーによりglobalpcdata199の値が再計算されました。", "[System]By unknown error, globaltdata169 has been initialized.")
			r1 = 0
			gosub *calcMaileINITService
			cdata(CDATA_INIT_ATTR, CHARA_PLAYER) = sorg(SKILL_ATTR_STR, CHARA_PLAYER) + sorg(SKILL_ATTR_CON, CHARA_PLAYER) + sorg(SKILL_ATTR_DEX, CHARA_PLAYER) + sorg(SKILL_ATTR_PER, CHARA_PLAYER) + sorg(SKILL_ATTR_LER, CHARA_PLAYER) + sorg(SKILL_ATTR_WIL, CHARA_PLAYER) + sorg(SKILL_ATTR_MAG, CHARA_PLAYER) + sorg(SKILL_ATTR_CHA, CHARA_PLAYER)
			cdata(CDATA_INIT_SPEED, CHARA_PLAYER) = sorg(SKILL_ATTR_SPD, CHARA_PLAYER)
			r1 = 0
			gosub *charaRefresh
			dmgsan CHARA_PLAYER, 100
			listmax = 0
			buff = lang("これで、またひとつ…。", "I was approached a little.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 7 ) {
			allyctrl = 7
			gosub *com_ally
			if ( stat != (-1) ) {
				rc = stat
				snd SOUNDLIST_CURSE2
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata198の値が再計算されました。", "[System]By unknown error, globaltdata162 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata199の値が再計算されました。", "[System]By unknown error, globaltdata169 has been initialized.")
				r1 = rc
				gosub *calcMaileINITService
				cdata(CDATA_INIT_ATTR, rc) = sorg(SKILL_ATTR_STR, rc) + sorg(SKILL_ATTR_CON, rc) + sorg(SKILL_ATTR_DEX, rc) + sorg(SKILL_ATTR_PER, rc) + sorg(SKILL_ATTR_LER, rc) + sorg(SKILL_ATTR_WIL, rc) + sorg(SKILL_ATTR_MAG, rc) + sorg(SKILL_ATTR_CHA, rc)
				cdata(CDATA_INIT_SPEED, rc) = sorg(SKILL_ATTR_SPD, rc)
				r1 = rc
				gosub *charaRefresh
				dmgsan rc, 100
				listmax = 0
				buff = lang("これで、またひとつ…。", "I was approached a little.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		if ( chatval == 8 ) {
			allyctrl = 7
			gosub *com_ally
			if ( stat != (-1) ) {
				rc = stat
				snd SOUNDLIST_CURSE2
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata989の値が初期化されました。", "[System]By unknown error, globalrdata989 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata990の値が初期化されました。", "[System]By unknown error, globalrdata990 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata991の値が初期化されました。", "[System]By unknown error, globalrdata991 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata992の値が初期化されました。", "[System]By unknown error, globalrdata993 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalcdata994の値が初期化されました。", "[System]By unknown error, globalrdata994 has been initialized.")
				if ( cbit(CHARA_BIT_SHOP_ELEGANCE, rc) ) {
					skillmod SKILL_ATTR_CHA, rc, -30000
				}
				cbitmod CHARA_BIT_SHOP_ELEGANCE, rc, FALSE
				cbitmod CHARA_BIT_SHOP_AESTHETIC_SENSE, rc, FALSE
				cbitmod CHARA_BIT_SHOP_SALES_ROUTE, rc, FALSE
				cbitmod CHARA_BIT_SHOP_STRONG_ALLY, rc, FALSE
				cbitmod CHARA_BIT_SHOP_BUSINESS_SMILE, rc, FALSE
				cdata(CDATA_SALES_FEATS, rc) = 0
				r1 = rc
				gosub *charaRefresh
				dmgsan rc, 100
				listmax = 0
				buff = lang("これで、またひとつ…。", "I was approached a little.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		if ( chatval == 9 ) {
			invsubroutine = 1
			invctrl = INVCTRL_MATERIAL_ENCHANT, 20
			snd SOUNDLIST_INV
			gosub *com_inventory
			f = stat
			if ( ibit(ITEM_BIT_NO_DROP, ci) ) {
				snd SOUNDLIST_FAIL1
				txt lang("それはあなたの大事なものだ。<調べる>メニューから解除できる。", "It's set as no-drop. You can reset it from the <examine> menu.")
				f = 0
			}
			if ( f == 1 ) {
				snd SOUNDLIST_CURSE2
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalinvdata25の値が初期化されました。", "[System]By unknown error, globalinvdata25 has been initialized.")
				txtef COLOR_RED
				txt lang("[システム]原因不明のエラーによりglobalinvdata26の値が初期化されました。", "[System]By unknown error, globalinvdata26 has been initialized.")
				inv(INV_ITEM_PARAM2, ci) = 0
				inv(INV_ITEM_PARAM1, ci) = 1
				if ( inv(INV_ITEM_LEVEL, ci) > 0 ) {
					inv(INV_ITEM_LEVEL, ci) = 0
				}
				repeat 15
					inv(40 + cnt * 2 + 1, ci) = 0
					inv(40 + cnt * 2, ci) = 0
				loop
				r1 = 0
				gosub *charaRefresh
				listmax = 0
				buff = lang("これで、またひとつ…。", "I was approached a little.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 879 | _switch_sw ) {
		_switch_sw = 0
		kaiwa = rnd(5)
		if ( kaiwa == 0 ) {
			listmax = 0
			buff = lang("旅の途中、「憎しみの連鎖を断つために、何をされてもやり返さずに許しなさい」とかしつこく説教してくる善人がいてな。頭にきて小石を投げつけたら、罵倒されまくった挙句ガードを呼ばれたんだが、これは一体どういうことなんだ。全然許されなかったんだが。", "Huh.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 1 ) {
			listmax = 0
			buff = lang("実を言えば、俺は森が災厄の根源という説を最初は信じていた。エーテルの風が病を招くのも事実だったからだ。ならば森に住む我々が迫害されるのも仕方がないと思い、理不尽な仕打ちにも耐えてきた。ずっと、ずっとな。だからこそ真相を知った時…エレアを悪者にするために流されたデマだと知った時、俺ははらわたが煮えくり返る思いだった…！", "Woah!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 2 ) {
			listmax = 0
			buff = lang("「来る者は拒まず」と言うが、何も考えずに受け入れるというのも考え物だな…。規模が大きくなりすぎて、構成員ひとりひとりの実態が把握できたもんじゃない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 3 ) {
			listmax = 0
			buff = lang("決起を始めた頃、罪悪感や同情から支援を申し出てくる者はそれなりにいたんだ。世間では我関せずといった態度の者が多かったが、支援者も一定数いるということを知って俺は感動した！まぁ、台無しになったけどな…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( kaiwa == 4 ) {
			listmax = 0
			buff = lang("このルオザは、戦争で火をつけられて滅んだ土地らしいな。親近感がないとは言わないが、エレアは別にザナンと戦争をしていたわけではないからな…。殴り合って怪我するのと、一方的に殴られて怪我するくらいの違いがあるぞ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		_switch_sw++
	}
	if ( _switch_val == 756 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 610 ) {
				listmax = 0
				buff = lang("これ以上の階層は正直厳しい。もう少し探索したら俺たちは撤退する予定だ。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 605 ) {
				listmax = 0
				buff = lang("お前は…よくここまで来れたな。こちらは特に腕の立つメンバーを揃えてきたが、火力が全く足りない。ボランティアで、報酬も出ないので金も足りない。", "Huh.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 610
				goto *chat_end
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) >= 1000 ) {
			if ( gdata(GDATA_LEVEL) == 60 ) {
				listmax = 0
				buff = lang("うちの傭兵たちは戦争に加担しないことにしている。だが傭兵団の維持にはそれなりに金がかかる…モンスター討伐依頼は稼ぎどころってわけだ。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 100 ) {
			if ( gdata(GDATA_LEVEL) == 60 ) {
				listmax = 0
				buff = lang("お疲れ様、と言っておこうか。冒険者をよこしてもらったのは正解だった。俺たちだけでは戦力が足りなかっただろう。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("俺たち傭兵は、残党の処理と地域一帯の消毒までが任務だ。お前はもう戻って報酬を貰うといい。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 4 ) {
			if ( gdata(GDATA_LEVEL) == 100 ) {
				listmax = 0
				buff = lang("悪魔一体一体がこの程度の力を持つとすると…別働隊が心配だな。上手くやってくれているといいが。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) = 5
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 3 ) {
			if ( gdata(GDATA_LEVEL) == 100 ) {
				listmax = 0
				buff = lang("…ひどい有様だ。首都が近く、小さいながらも賑やかな街だったと聞くが。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("物音から察するに相当数のアンデッドが徘徊しているようだ。物陰には気をつけろ。細菌の影響は…薬が効いているようだ。今のところ粘膜からの出血は最低限に抑えられているが、ダメージが蓄積すればどうなるかわからん。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("地の利だけじゃない。敵の物量・耐久性からも考えて持久戦は不利だ。お前の制圧能力にもよるが、ある程度片づけたら切り込んだほうが得策だろう。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("行くぞ、作戦開始だ。", "Woah!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) = 4
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) > 0 ) {
			if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) < 100 ) {
				if ( gdata(GDATA_LEVEL) == 60 ) {
					if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 2 ) {
						listmax = 0
						buff = lang("お前がパルミアから寄越された冒険者だな。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 2 ) {
						listmax = 0
						buff = lang("今回のターゲットは魔力を用いて細菌を制御する悪魔だ。制御範囲外では毒性が大きく落ちるようだな。とは言え感染力は侮れない。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 2 ) {
						listmax = 0
						buff = lang("我々が使う小型艇に消毒装置を用意してもらった。つまり、単なる移動手段ではなく感染拡大を防止する役割も持つわけだ。敵陣に直接乗りつけて破壊されるわけにはいかない。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 2 ) {
						listmax = 0
						buff = lang("ツアーコンダクターの情報によると、付近の鍾乳洞が街の井戸に繋がっているらしい。そこから突入し、ターゲットを討ち取る算段だ。俺の仲間は全員小型艇に乗り込んで待機している。いつでも出撃可能だ。", "Woah!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("ちょっと待って", "No problem.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("大丈夫", "Sorry.")
					listmax++
					buff = lang("準備はいいか？", "S-sorry. Not hurt are you?")
					gosub *chat_select
					if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) < 3 ) {
						gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) = 3
					}
					if ( chatval == 1 ) {
						if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 3 ) {
							listmax = 0
							buff = lang("報告を見る限り、毒や出血といった症状が予想される。これを飲んでおけ、ザナンから提供してもらった薬だ。あくまで症状を緩和するためのものだがな。", "Woah!")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						snd SOUNDLIST_DRINK1
						if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) == 3 ) {
							listmax = 0
							buff = lang("…よし！着いてこい。", "Woah!")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						mapidou = 100
						gdata(GDATA_TELEPORT_AREA) = AREA_DOCK
						gdata(GDATA_TELEPORT_LEVEL) = 100
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 5
						mapstarty = 52
						dbg_skipevent = 1
						gdata(GDATA_HOUR) += 2
						gosub *advance_hour
						dbg_skipevent = 0
						chatteleport = 1
						goto *chat_end
					}
					else {
						listmax = 0
						buff = lang("急げよ。ターゲットが移動してからでは遅いからな。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 857 | _switch_sw ) {
		_switch_sw = 0
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) <= 99999 ) {
			listmax = 0
			buff = lang("10万ぽっちも持っていないんですかぁ…。特に喋ることはないです。", "You do not even have 100,000 gp .... I do not particularly speak.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_WAR_GROUND ) {
			if ( gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) == 3 ) {
				listmax = 0
				buff = lang("「昔々、ネフィアが生まれるよりも昔のお話。神々は戦争でマナが乱れた大陸を、危険なのでイルヴァから隔離し結界を張ることにしました」", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("「神々は大陸に住んでいた人々に移住を勧め、ほとんどの人がそれに従いました。しかし、丘の民は違いました。愛着のある丘に断固として残ろうとする者と、惜しみながらも丘を離れようとする者にわかれたのです」", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("「争いと迷いの末に、私達のご先祖様は新たな技術と素材を求め別大陸に渡ることを選びました。別大陸に渡ったご先祖様が後ろを振り返ると、大陸が光に包まれて空へと昇っていくところでした」", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("「あなたが空を見上げたなら思い出しなさい。あの空のどこかに始まりの丘…ルストールがあり、今もなお丘の民がそこで暮らしているかもしれないことを」", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…伝説どおりだったとは思いませんでしたぁ。ロストイルヴァは本当にあったんだ！ルストールもまだ残っているんですかねぇ？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) = 4
				goto *chat_end
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("別に…", "No problem.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("機械のマニ", "Mani of Machine")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("風のルルウィ", "Lulwy of Wind")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("元素のイツパロトル", "Itzpalt of Element")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("幸運のエヘカトル", "Ehekatl of Luck")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("地のオパートス", "Opatos of Earth")
			listmax++
			list(0, listmax) = 6
			listn(0, listmax) = lang("癒しのジュア", "Jure of Healing")
			listmax++
			list(0, listmax) = 7
			listn(0, listmax) = lang("収穫のクミロミ", "Kumiromi of Harvest")
			listmax++
			list(0, listmax) = 8
			listn(0, listmax) = lang("富のヤカテクト", "Yacatect of Wealth")
			listmax++
			buff = lang("各神のグッズはいかがですかぁ？どれも１０万ポッキリ！お得ですよぉ。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("またまたぁ…そんなつれないこと言っちゃってぇ。余ってるでしょう？お金。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval > 0 ) {
				dbid = 0
				if ( chatval == 1 ) {
					dbid = 1094
				}
				if ( chatval == 2 ) {
					dbid = 1098
				}
				if ( chatval == 3 ) {
					dbid = 1096
				}
				if ( chatval == 4 ) {
					dbid = 1095
				}
				if ( chatval == 5 ) {
					dbid = 1100
				}
				if ( chatval == 6 ) {
					dbid = 767
				}
				if ( chatval == 7 ) {
					dbid = 1099
				}
				if ( chatval == 8 ) {
					dbid = 1097
				}
				if ( dbid > 1 ) {
					snd SOUNDLIST_PAYGOLD1
					cdata(CDATA_GOLD, CHARA_PLAYER) -= 100000
					flt
					itemcreate -1, dbid, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
					inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
					txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
					listmax = 0
					buff = lang("お客さんもお目が高いですねぇ。うへへ…毎度ありですぅ。", "Thank you for your purchase!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					dbid = 0
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) >= 560 ) {
				if ( gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) >= 3 ) {
					listmax = 0
					buff = lang("本当はもっと売りたかったんですけど…。カギだけじゃなく私の靴下もつけるって条件で、前のお客さんが１ダースずつ買っていっちゃってですねぇ。もう在庫がないんです。こんなに捌けるなんて大誤算ですよぉ。", "Actually I wanted more to sell goods. However, since the previous customers bought one dozen, I'm no longer in stock.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_MAIN) >= 560 ) {
				if ( gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) < 3 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("別に…", "No problem.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("もう片方も買いたい(99999gp)", "I want the other goods(99999gp)")
					listmax++
					buff = lang("うん？まだ何か御用ですかぁ？", "Yeah! It is for something?")
					gosub *chat_select
					if ( chatval == 1 ) {
						if ( gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) == 2 ) {
							flt
							itemcreate -1, ITEM_ID_POTION_SACRED_HEALER_JURE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_SCROLL_HEALING_RAIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_SCROLL_MANA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_FIRST_AID_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						}
						if ( gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) == 1 ) {
							flt
							itemcreate -1, ITEM_ID_SCROLL_VANISH_CURSE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_SCROLL_RECHARGE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_POTION_RESTORE_SPIRIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_POTION_RESTORE_BODY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_ACIDPROOF_LIQUID, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_FIREPROOF_LIQUID, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
							flt
							itemcreate -1, ITEM_ID_SPECIAL_AMMO_BOX, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						}
						snd SOUNDLIST_PAYGOLD1
						cdata(CDATA_GOLD, CHARA_PLAYER) -= 99999
						listmax = 0
						buff = lang("へへへ…毎度ありですぅ。お客さん羽振りがいいですねぇ。", "Thank you for your purchase!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) = 3
						snd SOUNDLIST_COMPLETE1
						txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
						goto *chat_end
					}
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_MAIN) <= 545 ) {
				listmax = 0
				buff = lang("お客さん、この先に進みたいんですか？ここから先は危ないので封鎖してしまったんですよ。うちの商品を買ってくれるのなら、カギを渡してもいいですねぇ。悪い話じゃないと思うんですけど。", "Do you want to proceed to this destination? Ahead it was blocked so dangerous from here. If you willing to purchase the goods, I will pass the key.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN) = 550
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 550 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("いらん", "I do not need.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("回復アイテム詰め合わせ(99999gp)", "Recovery item assortment(99999gp)")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("メンテナンス用品詰め合わせ(99999gp)", "Maintenance assortment(99999gp)")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("殺してでもカギを奪い取る", "I wrest a key even kill you.")
				listmax++
				buff = lang("さぁ、カギが欲しければ商品を買っていくしかないですよぉ。", "Now. If you would like a key, there is no choice but to buy the goods.")
				gosub *chat_select
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("殺されてでもカギは落とさないですねぇ。ちゃんと四次元ポケットに入れてるので。", "Even if you kill me, I will not drop the key. The key is put in a 4-Dimensional Pocket.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 | chatval == 2 ) {
					if ( chatval == 1 ) {
						flt
						itemcreate -1, ITEM_ID_POTION_SACRED_HEALER_JURE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_SCROLL_HEALING_RAIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_SCROLL_MANA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_FIRST_AID_KIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) = 1
					}
					if ( chatval == 2 ) {
						flt
						itemcreate -1, ITEM_ID_SCROLL_VANISH_CURSE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_SCROLL_RECHARGE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_POTION_RESTORE_SPIRIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_POTION_RESTORE_BODY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_ACIDPROOF_LIQUID, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_FIREPROOF_LIQUID, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						flt
						itemcreate -1, ITEM_ID_SPECIAL_AMMO_BOX, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
						inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
						gdata(GDATA_FLAG_SUB_MANYTIA_THE_ADVENTURE_MERCHANT) = 2
					}
					snd SOUNDLIST_PAYGOLD1
					cdata(CDATA_GOLD, CHARA_PLAYER) -= 99999
					listmax = 0
					buff = lang("はぁい、毎度ありです！じゃあカギも渡しておくですよ。", "Thank you for your purchase! The key is also passed to you.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_MAIN) = 560
					snd SOUNDLIST_COMPLETE1
					txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 860 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_GUARDIAN_SHRINE ) {
			listmax = 0
			buff = lang("父上は救助できましたが、身体を張って私を先に行かせた部下たちは、きっともう…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_34) == 0 ) {
				listmax = 0
				buff = lang("父上…どこですか父上…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_34) != 0 ) {
				listmax = 0
				buff = lang("私は…何ひとつ守れなかった…。任務も、父も、部下も、そして自分の命すらも。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_FLAG_MAIN) == 575 | gdata(GDATA_FLAG_MAIN) == 580 | gdata(GDATA_FLAG_MAIN) == 585 ) {
				listmax = 0
				buff = lang("そこの冒険者、止まってください。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 0
				listn(0, listmax) = lang("はく製を置いてこの世を去りなさい", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("嫌でも通してもらおう", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("靴下くれないと暴れるよ", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("話せばわかる！", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("見てのとおり、ここは現在我々が占拠しています。速やかに武器を置いてこの階を去りなさい。", "Sorry, this is untranslated sentence.")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 2 ) {
					listmax = 0
					buff = lang("！？貴方もですか…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval >= 0 & chatval <= 2 ) {
					listmax = 0
					buff = lang("我々が消耗しているとでも思っているのでしょうが…我々は物資を獲得したことにより、戦闘可能な状態に回復しています。一介の冒険者など敵ではありません。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("警告はしました。これ以上の問答は無用です。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				gdata(GDATA_FLAG_MAIN) = 590
				txt lang(cdatan(CDATAN_NAME, tc) + "は自らにも言い聞かせるように叫んだ。", cdatan(CDATAN_NAME, tc) + " shouted as remind even herself.")
				txtvalid = 0
				txt lang("「総員、防衛を開始せよ！我々の使命を果たすため、その者を通すな。身体の動く限り戦いなさい！」", "Sorry, this is untranslated sentence.")
				snd SOUNDLIST_CHEER
				repeat 245
					if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
						continue
					}
					if ( cdata(CDATA_RELATION, cnt) != RELATION_ENEMY ) {
						continue
					}
					addbuff cnt, BUFF_SPEED, 271 * 3 + 50, 75
					addbuff cnt, BUFF_HERO, 271 * 3 + 100, 75
					if ( cdata(CDATA_POWER_GAUGE, cnt) < 50 ) {
						cdata(CDATA_POWER_GAUGE, cnt) += 10
					}
				loop
				goto *chat_end
			}
			if ( gdata(GDATA_LEVEL) == 173 ) {
				listmax = 0
				buff = lang("間に…合わなかった…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_30) < 4
				listmax = 0
				buff = lang("アンデッド兵器の突破に時間をかけすぎた…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 893 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_ZISILION_SIDEQUEST) == 1 ) {
			listmax = 0
			buff = lang("君、働いてるねぇ。僕は労働って素晴らしいと思うんだ。生活費を得られ、社会にも貢献できる…社会的生物として、「適度な」労働は喜ばしいものなんだ。僕はすでに一財産稼いでしまって、今さら労働する気になれないけどね…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そんな僕にもささやかな楽しみがあってね。ここに立って関所を行き来する人々を眺め、その中でも頑張ってる感じの働き者にプラチナ硬貨をあげることなんだ。君にも何枚かあげよう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
			gdata(GDATA_FLAG_ZISILION_SIDEQUEST) = 200
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_ZISILION_SIDEQUEST) == 200 ) {
			listmax = 0
			buff = lang("そう、がっつかないでくれよ…。明日また来てくれ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_ZISILION_SIDEQUEST) == 100 ) {
			listmax = 0
			buff = lang("君、まだ今日は街の依頼を達成していないようだな。達成したらまたおいで。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("元気に働いているようだね。ううん…素晴らしい汗だ。頑張っている君にプラチナをあげよう。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		flt
		itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
		gdata(GDATA_FLAG_ZISILION_SIDEQUEST) = 200
		txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
		listmax = 0
		buff = lang("また、明日おいで。", "Sorry, this is untranslated sentence.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 886 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_GUARDIAN_SHRINE ) {
			listmax = 0
			buff = lang("任務に失敗したうえ、後続隊を犠牲にしてまで生き延びてしまいましたか…。ヴァリウス様に合わせる顔がありませんね…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = "(" + name(tc) + lang("は見えない誰かに話しかけている…", " is stunned.") + ")"
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_25) >= 1000 ) {
				listmax = 0
				buff = lang("すみません。私が巻き込まなければランカータはまだこちらに来なかったでしょう。本当にすみませんでした。機嫌を直してください。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			listmax = 0
			buff = lang("うぐがぁぁっ！いいですか…もし、辿り着いても…混沌の神は、まだ、倒しては…！あれは世界を…結束させるため必要なっ…！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あっ…がっ…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			animeload 7, tc
			snd SOUNDLIST_BALL1
			txt lang(name(tc) + "の身体が弾け飛んだ。", name(tc) + " shatter" + _s(tc) + ".")
			dmghp tc, 99999, DAMAGE_FROM_SUICIDE
			gdata(GDATA_FLAG_MAIN) = 640
			gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_34) = 1
			chara_vanquish tc
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 896 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) == 100 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("生活講座担当、アジタリオ・ラーンクリスです。長いのでアジラーって呼んでください。みなさん、これからよろしく。", "Hi everyone, I'm Ajetalio Rancris, the lecturer for the survival seminars. It's quite a mouthful so you can call me Ajilla if you want.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("まずは冒険の基本中の基本、食料の調達方法について話しますね。", "To begin with I'd like to talk about where you can get food. After all, an army marches on its stomach.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("手軽なのは街の中にもある果物の木ですかね。これにbキー…bash（ぶつかる）することで果物が落ちてきます。種類にもよりますが、結構重いし半日前後で腐るので、早めに処理しましょう。腐らせるくらいなら、地面に置いて仲間に食べさせたほうがいいですね。", "The fruit trees in town are one of the most readily available sources - literally low-hanging fruits. 'b'ash them (b key) to make some fruits fall off. There are different kinds of fruits, but they are all pretty heavy in general and rot in about half a day, so be sure to finish them soon. If you can't, leave them on the ground for a pet to eat.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("慣れてきたら街の外で野原にEnterして、落ちているアピの実、栗、薬草を拾いましょう。これらは腐らない上に軽いので携帯食としても保存食としても優秀です。山菜も落ちていますが、これは２日で腐るので注意してください。咲いている花も食べられますが、いい値段なので売った方がいいでしょう。", "Once you feel you're ready, press Enter outside of town to enter the wilderness. There, you can pick up api nuts, chestnuts, and healthy leaves. These are lightweight and never rot, so they are excellent for bringing around or storing away for emergencies. You will also find edible wild plants, but remember that these will rot within two days. Wildflowers are also edible, but they fetch for quite a lot so you may want to sell these instead.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("おっと。外で食料を集める時は野生動物に気をつけてください。強そうな動物や、実際強いと感じた相手からは無理せずエリア外まで逃げましょう。入りなおせば他にいくらでも拾えるので、目先の食料に眼が眩まないように。", "However, always keep an eye out for wild animals. If you see a wild animal that looks really strong, or if you've already exchanged a few blows and think it's out of your league, run. Get out of the area and you will be safe. You can always find more food by reentering the area, so don't be too hung up about grabbing the chestnut that has a lion standing on it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("長時間の冒険用に、宿屋で旅糧を買いこんでもいいですね。お腹が膨れるだけで強くはなれませんが、飢え死にするよりはいいでしょう。旅糧は緊急用にして、腐らず・腹持ちもよく・能力も鍛えられる食品…パン系を常備するのもありですね。どちらも結構お金がかかるので、余裕ができてから一考を。", "If you're planning on taking a long trip, you may want to stock up on traveler's food, available from innkeepers. These will only make you full and won't train any of your stats, but you'll likely find these preferable to starving to death. With these as your emergency food supply to fall back on, you'll want something else as a regular go-to food for long trips. Something that doesn't rot, fills you up, and trains your stats - something like bread. Neither traveler's food nor bread are cheap, but you should consider having some with you at all times if you can afford it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("街によっては街中にパンが置いてあったり野菜が植えてあったります。行儀の悪いことですが、どうしてもお腹がすいている時はこっそり食べてしまうことも可能です。ただし、呪われていた場合は吐いて最悪死ぬので、最後の手段にしてください。何もない街でも宿屋があれば食事はとれます。有料なうえに強くもなれませんが安全ですよ。", "Some towns have sticks of bread lying around and vegetables on the fields. It's bad manners to do that, but just eat those in secret and no one will complain. If it's a town that has no food around, then get the innkeeper to bring you something to eat. That's going to cost you though, and it won't train your stats either.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("ああ、仲間は主人から必要最低限の生命エネルギーを分けられているので、吐きすぎない限り餓死しませんよ。と言っても聴診器で診た時に青緑色なら、お腹が空いている証拠です。食料に結構余裕がある場合は与えたほうがいいでしょう。食べさせた方が強くなりますよ。", "Pets, by the way, will never starve to death unless they throw up too much. They get enough energy to sustain them through their connection with their master. But if you auscultate them with a stethoscope and you see that their HP bar is blue-green, that means they're hungry. If you have food to spare, feed them to make them grow stronger.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("今日はネフィアや税金について学びましょうか。", "Let's talk about Nefia and taxes today.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("ネフィアは多くの魔物や罠がひしめいている迷宮です。採掘スキルや魔法の地図、物質感知があると探索がはかどりますよ。不自然な行き止まりがある場合は、足踏みやsキー…searchを続けることで隠し通路を発見できます。", "Nefia are dungeons that contain lots of monsters and traps. Exploring these will be much easier with the Mining skill and Magic Map and Sense Object spells. If you see a suspicious dead end, you can stand for some turns or 's'earch a bit to reveal hidden passages.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("ネフィアの危険度は階層で表現されます。一概には言えませんが、その階層値程度のレベルの敵が出現すると思ってください。レベル１０の敵に苦戦している段階で１０階相当のネフィアに挑むのは危険、ということですね。仮に最初の階層はなんとかなったとしても、次の階層には１１レベル、次の次には１２レベルの敵が存在しうるわけで。倒せた敵のレベルはcキー…キャラシートの適正欄に記録されているので、参考にしましょう。", "The danger level (DL) of Nefia is a rough indicator of the level of enemies on that floor. If you find level 10 enemies too tough, then DL 10 Nefia are going to be too dangerous for you. Even if you manage to clear it, one floor down will be DL 11, and then DL 12, and so on. The highest level enemy you've defeated is recorded on the 'c'haracter sheet, so use that number as a reference.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("給料と税金についても話しておきます。毎月１日と１５日に、家の給料箱へ給料や補給品が振り込まれます。税金を滞納していないと補給品の質がよくなるみたいですね。そして給料の額は冒険者としての各種ランキングによって決まります。ランキングについてはjキー…journal（日誌）で確認しましょう。", "Now, about taxes and salary. On the 1st and 15th of every month, gold pieces and supplies will be delivered to the salary chest in your home. If you don't owe any taxes, the supplies that you receive will be of a better quality. The amount of gold pieces, on the other hand, depends on your placing in the various rankings. You can check you rankings with the 'j' key - 'j' for journal.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("さて、レベル１０以上になると毎月１日に給料と一緒に税金の請求書も届くはずです。これはこのゼミから東にあるパルミア大使館に持っていくと支払えます。税金は所持金と所有物件、雇用から算出されます。もし悪人として生きていきたくても、最初のうちは毎月ちゃんと払ってください。４か月税金を滞納すると、カルマが減って一発アウトですから。", "Taxes will be billed to you on the 1st of every month if you are level 10 or above. Bills are delivered together with your salary. To pay these, take the bill, and head east from this building to the Palmian Embassy, and put it into the tax box there. How much you get billed for depends on how much money you currently have and what buildings you currently own. Pay it every month even if you plan to become a criminal until you're sure you know what you're doing. If you have 4 months of unpaid taxes, your karma will plummet and you will be immediately wanted by guards everywhere.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("今回は信仰について説明しますよ。神々の干渉がほとんどなかった時代には、いるのかもよくわからない存在に頼らないと自分を律することができない人や、架空の神をでっち上げて人々から金をむしり取る罰当たりもいたそうですが、現在のイルヴァは確実に存在する八柱の神によって統治されています。", "Let's talk about faith and worship today. There was a period of time in the past when the gods rarely bothered Themselves with the affairs of the land. During this period, there were agnostics who claimed that they would not depend on entities that they weren't sure if existed. There were even people who made up gods and religions to siphon money off of the gullible, only to receive divine punishment for that. Today, however, we know for sure that there are eight gods who rule over Irva.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("神を信じ捧げものをすることで、能力の強化、特殊能力、神の化身、秘宝、武器といった具体的な恩恵を確実に授かることができます。俺は神に頼らないぞ！みたいな信条が特にないのなら、とりあえず誰かを信仰しておくべきでしょう。", "Worshipping a god will grant bonuses to some of your skills or attributes, give you new abilities, secret treasures, and weapons. Unless you're a stubborn atheist, then it's best to just pick a god for worship. Any god will do, if you can't decide on one.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("神を信仰するなら、パルミアの北…つまりこのゼミから北東にある、神々の休戦地で行うといいですよ。祭壇の上でpキー…pray（祈る）すればその神の情報を見たうえで信仰するか否か選べます。信仰してその神が好むものを祭壇でEnterして捧げていけば、やがてご褒美のお告げがあるでしょう。", "If you want to start worshipping a god, then head north from Palmia to the Truce Grounds. Or if you want to get there directly from here, it's in the north-east. Press the 'p' key ('p' for pray) while standing over the altar of a god to read about Him. When you're worshipping a god, make offerings by pressing Enter while standing over his altar. Make enough offerings, and your god will eventually tell you that you're eligible to receive a reward.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("お告げがあったら、pray（祈る）すれば空からご褒美が降ってきます。もっとも、神の化身は信仰スキル１５、秘宝は信仰スキル２５、武器は信仰スキル４０が最低でも必要になりますけどね。", "To receive that reward, simply 'p'ray and it will fall from the skies. That said, in order to get enough favour with your god to get a servant as a reward, you'll need 15 levels of Faith skill. For the secret treasure and weapon, 25 and 40 Faith are needed respectively.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("さて、最後となる第４回では、本格的な生計の立て方について勉強しましょう。", "Welcome to the fourth and final lecture. Today, I'm going to talk about how you can actually make a living for yourself.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("迷宮に潜って金貨を取ってくるだけでは生計は成り立ちません。今時そんな、迷宮一本で生計を立てられるのは、レベル数百の迷宮を難なく攻略できる化け物くらいです。その化け物だって、強くなるまでにこつこつと稼いできたんですよ。そして、そんな強さになる頃には既に金には困らなくなっている。", "You'll find that gold pieces from Nefia aren't going to be enough. The only people who can do that are the freakishly strong Nefia delvers who breeze through places with a danger level of several hundred. But even these people have had to slowly work their way up when they were just starting out. And by the time you're that strong, you won't be needing any of that money.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("強くなるにしても稼ぐにしても、兼業冒険者のほうがやれることが多いぶん有利ですよ。最初は街の依頼で元手を作り、牧場の権利書を手に入れることを目標にしましょう。食糧や資金の調達がはかどりますよ。詳しくは物件に付属してくる赤い本を読んでください。", "Whatever your ultimate goal is - be it getting powerful or becoming filthy rich - you'll find that having additional sources of income goes a long way in helping you reach it. Start off with doing jobs in town and save up enough for a shop deed. Just by selling mediocre pieces of equipment that don't sell for much elsewhere, the shop can and will become your main source of income.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("店を構える際には、店主を任せるために交渉スキルを持つ仲間も用意しましょう。店物件は１店舗でも充分なので、早めに欲しい物件ですね。微妙な性能で売値も大したことのないような装備でも、自分の店で売るといい収入に化けます。", "When you have a shop, be sure to have an ally with Negotiation skill that you can assign as the shopkeeper. Just one shop is all you need, so it's something you should try to get as soon as possible.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("それと並行して、サブ収入として生産系のスキルや採掘、釣り、演奏などから好みのものを冒険の合間に上達させていきましょう。こだわりがなければ全部やったほうが効率がいいくらいです。迷宮でアイテムを集めるほどの戦力や気力が無いような時でも、これらの手段は使えますからね。", "For secondary sources of income, you can craft items, mine, fish, and perform when not adventuring. In fact it's actually more efficient to do all of those I've mentioned, unless you're trying to focus on one of them for some reason. They're something to try when you're too weak or lazy to go item hunting in Nefia.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE) = 101
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("食べちゃダメなものは？", "What shouldn't I eat?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("のどが渇いたら？", "What do I do when I'm thirsty?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("料理ってどうやるの？", "How do I cook?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("アジラーの好きな食べ物は？", "What's your favourite food, Ajilla?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("ネフィアにはどんな種類があるの？", "Are there different types of Nefia?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang("カルマが減るとどうなるの？", "What do I do when my karma is low?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 6
			listn(0, listmax) = lang("発言力って？", "What's authority?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 7
			listn(0, listmax) = lang("街が大変なことになったら？", "What do I do if something really bad happens in town?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 8
			listn(0, listmax) = lang("祭壇がなくなったら？", "What do I do if I lose or destroy an altar?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 9
			listn(0, listmax) = lang("信仰スキルってどうやって上げるの？", "How do I level up my Faith skill?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 10
			listn(0, listmax) = lang("おすすめの神様は？", "Which god do you recommend worshipping?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 11
			listn(0, listmax) = lang("アジラーの前髪は腐ってるの？", "Is that a rotting sliver of meat hanging on your forehead, Ajilla?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 12
			listn(0, listmax) = lang("他におすすめ物件は？", "Are there any other deeds I should buy?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 13
			listn(0, listmax) = lang("交易について教えて？", "Can you teach me about trading?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 14
			listn(0, listmax) = lang("お金に余裕が出てきたら？", "What do I do when I have some money saved up?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 15
			listn(0, listmax) = lang("お金があり余るようになったら？", "What do I do when I have too much money?")
			listmax++
		}
		buff = lang("何か質問はありますか？", "That's about it. Any questions?")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("いい質問ですね！呪われているものは食べると吐いてしまい、逆にお腹が減ったり場合によっては即餓死したりします。呪われているかは鑑定することでわかりますが、空腹の仲間の足元に置いてみて食いつくかでもわかります。どんなにお腹が空いていても、仲間は足元の呪われている食べ物には手を出しません。", "Good question! Cursed food will make you throw up and get even hungrier, and that can leave you starving. Of course, you can identify it to determine if the food is cursed, but another way of finding out is to place it at the feet of a hungry ally. Your allies will never eat cursed food off the ground no matter how hungry they are.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あと、腐ったものを食べると能力が永続的に下がります。腐っていても消化できるようになる装備もありますが、クーラーボックスや四次元にしまって腐らないように対策しましょう。", "Rotten food will permanently decrease your attributes. There's equipment that you can wear to let you digest rotten food, but in the meantime you should just stick food into a cooler box or 4-dimensional storage to stop it from rotting.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ただし、特定のアンデッドの死体は見た目上腐っていなくても腐っているので気をつけて。骨のやつなんかは大丈夫ですが、動く死体系のやつらは腐っていますよ。", "That said, you must be very careful of the corpses from some undead. They are rotten even if they don't look like it. The bony kind of undead are fine, but the ones that are basically walking corpses are all rotten.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("他にはエイリアンなどの寄生生物の死体を食べると寄生、輝くハリねずみやウィスプなんかはエーテル病を進行させます。発狂したり毒になる死体もあります。怖いですか？でもプチ系や馬系みたいに能力に経験値が入るものや、サソリや妖精みたいに属性耐性を得られる特殊なものもあるので、対策を用意したうえで色々食べて覚えていきましょう。", "Other corpses you should watch out for are aliens and other parasitic creatures that you make you pregnant. Shining hedgehogs and wisps will hasten the progression of ether disease. And there are always those that will make you mad, poison you, and so on. It's not all bad, though, because some of the latter are like putits and horses in that they will train your attributes. Scorpions and fairies will even grant resistances to some elements, so you should try eating them. After you've made the necessary preparations, of course.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("のどが渇いて脱水症までいくと疲労します。死にはしませんがね。疲労すると技能は使えなくなり動作も鈍くなるので、戦闘が難しくなります。早めに何かを飲みましょう。雨や水場で濡れれば、少しずつ回復することも覚えておいてください。", "When you're thirsty to the point of dehydration, you'll become tired. It won't kill you though. But being tired means you're slower and won't be able to use special actions, so combat will be harder, so drink something soon. Also, if you get wet in the rain or in a puddle, you'll slowly quench your thirst.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("遠出する際には、食べ物だけでなく飲み物も用意しておきましょう。ジュースミキサーがあれば果物や野菜からジュースを作ってのどを潤すことができます。呪われていたり腐っている物でも無害化して有効活用できます。街中であればバーテンダーに飲み物を注文してもいいですね。", "If you plan on travelling, pack both food and drinks.If you have a juice mixer, you can make juice from fruits and vegetables for drinking. It will also make normal juice from cursed or rotten produce, so that way you never waste anything. If you're in town, you can get something to drink from a bartender.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("料理スキルがあるなら、調理器具を使うことで料理ができます。料理すると、腐るまでのタイムリミットと心身を鍛える効果が大幅に伸びるので、できるようになっておきたいですね。料理の達人になれば、ひとつの素材から複数の料理を作ることができます。腐ったものも料理の練習台にするくらいの気持ちで頑張りましょう。調理器具によって、どこまでの料理を作れるか限界があるので一度道具説明欄を見て下さい。", "If you have the Cooking skill, then just use cooking tools to make cooked food. Food lasts for a lot longer and trains attributes by a much greater amount when cooked, so it's an important skill to have. Expert cooks are able to make several dishes from one ingredient, so you may want to practise cooking whenever you can, even on rotten food.  The rank of cooked food is limited by the type of cooking tool, so have a look at the tool descriptions.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("牧場を持っていれば、料理スキルがなくても干し肉を作ることができます。栄養と価値はそこそこですが、保存性がバツグンです。あとは、素材が燃えて料理になることもありますが…ろくな品にならないので、下手でも自分で料理したほうがマシだと思います。むしろ、勝手に燃やされないように注意してください。", "If you have a ranch, you can make jerky even without the Cooking skill. It's not that great in terms of nutrition and selling price, but it keeps for a long time. You could also set ingredients on fire to cook them... but they're never going to turn out well, so you should try to keep your food from burning up. Cook yourself if you can even if you're not good at it.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("えぇぇ…僕の好物ですか？そ、そうですね。ハーブでしょうか。腐らないし軽いし、心身も鍛えられるんですよ。合成の壺で料理に混ぜると最高ですね。", "My favourite kind of food...? Uh. Um, herbs, I guess. They never rot, they're light, and train both your body and mind. They're even better when you infuse cooked food with them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 4 ) {
			listmax = 0
			buff = lang("大別して、地殻変動で出たり消えたりするランダムネフィアと、地殻変動で消えない固定ネフィアがあります。", "There are two types in general - random Nefia that appear and disappear with tectonic diastrophisms, and unique ones that stay put.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ランダムネフィアは３〜６層で構成され、最下層のボスを倒せば攻略完了となり宝が手に入ります。ボスを倒す前に最下層から逃げると、失敗となり攻略不能になります。あと、ランダムネフィアの表層以外の階層には、金貨が集められている箇所が必ず存在します。なんだかネフィアが冒険者を誘っているみたいで不気味ですが…ありがたく拾っておきましょう。", "Random Nefia have 3 to 6 floors and a boss on the bottom floor that drops treasure when defeated. If you go out of the last floor before defeating the boss, you'll fail to clear the Nefia and won't be able to try it again. Random Nefia also have gold pieces scattered throughout them - but never on their first floor. They're like bait to attract adventurers... creepy if you ask me, but hey it's free money.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("固定ネフィアは位置も固定されており動きません。外見や名前が固有のものが多いので、ランダムネフィアとはそこで判別してください。２層程度の浅いものから４０層以上あるものまで様々です。独自のルールや環境を持っている固定ネフィアもあります。", "Unique Nefia will always be on the same spot. Most of them also have a unique appearance that allows you to tell them apart from random Nefia. Inside, they can range anywhere from 2 to 40 or more floors. Some of them even impose special conditions or have unique environments.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ランダムネフィアは元となった遺跡に応じて８タイプあります。それぞれ住み着いている種族が違うので、目当ての魔物を探す時は対応したタイプのネフィアを探すといいでしょう。固定ネフィアは基本的に種族のごった煮ですが、固定のものもあったりと一概には言えませんね。", "Random Nefia can be classified into 8 types, depending on the ruins that they are based on. Nefia type dictates the races of monsters that inhabit it, so if you're searching for certain types of monsters, you can narrow down the Nefia you need to search. In general, unique Nefia are inhabited by every monster race, but there are some exceptions.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 5 ) {
			listmax = 0
			buff = lang("カルマが−３１まで下がると、犯罪者として指名手配されます。街中のガードが一斉に殺しに来て、店の人も物を売ってくれなくなります。ガードを一蹴できる強さと、変装手段を確保するまではカルマを下げすぎないよう慎重に行動してください。", "When your karma falls below -30, you will be wanted as a criminal. All guards in towns will make a beeline for you and shopkeepers will no longer sell anything to you. Unless you're strong enough to handle the guards with ease or can disguise yourself, then manage your karma carefully.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("もし、思いがけず犯罪者になってしまったら。このゼミから南西にある、ダルフィという街を拠点にして依頼などでカルマを上げてください。ダルフィにはガードがおらず、犯罪者でも店も使えますからね。", "If you find yourself a criminal, then you should head over to the town of Derphy south-west from here. Over there, you can complete some jobs to bring your karma back up. There aren't any guards there and their shopkeepers also don't mind dealing with criminals.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 6 ) {
			listmax = 0
			buff = lang("発言力は街への貢献度の証。そこの管理者や長に話しかけることで、発言力を使って街の管理代行を行うことができます。…と言ってもまだできたばかりの制度で、やれることは少ないんですけどね。現段階では、依頼のついでで貯まっていくものだと認識してください。", "Authority is a measure of how much you've contributed to a town. By talking to the person in charge there like their elder, you can use that authority as a proxy to enact laws in town. Or should be able to, anyway. It's a new system, so there's not much you can do with it at this point. For now, it's just something that accumulates as you do jobs for the people in town.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 7 ) {
			listmax = 0
			buff = lang("街が大火事になったり、モンスターが湧いて虐殺が始まることがよくあります。よくないことだけど、よくあることなんです。…ごほん。火の消し方とモンスターの出現を止める方法を教えておきましょう。", "Is a town burning to the ground? Are there monsters going around killing everyone? Yeah that sucks, but that's life. I mean- I'll give you some tips on putting out fires and stopping monsters from spawning.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("まず火の消し方。火が一つ二つなら、手持ちのポーション類をそこにＴキー（shift+t）…Throwすることで鎮火できます。水たまりを誰かが踏むと怒られるので、自分で踏んで消しておきましょう。ポーションを上手く投げられなかったり、火が四つ五つに増えてきたら、蜘蛛の巣や闇の霧、水系の魔法を使いましょう。杖版でもいいですけど。これらは対象範囲の火を消してくれます。そんなものは持っていなかったり、火が燃え広がりすぎている時は、雨の日までその街には近寄らないことをオススメします。", "So, about fires. If there are only one or two, then you can 'T'hrow (shift + 't' key) potions to put them out. People get mad if they step on puddles made by you, so be sure to step on them yourself to get rid of them after you're done. If you can't seem to hit with your fires and the fire spreads to 4 or 5 tiles, you can use spells of web or mist of darkness, or any of the water spells. These can put out fires in a large area. If you don't have any of those, or if the fire is already burning out of control, then you might want to stay out of town until a rainy day.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("続いてモンスターが湧く場合。井戸を飲んだ時に出てきたものは、倒してしまえば終わりです。しかしモンスターを無限に召喚する装備を付けた冒険者が徘徊していることがあります。これは、犯人を見つけてその装備を没収しましょう。", "Monsters that spawn from drinking from a well aren't much of a problem - kill them to resolve the problem. But sometimes adventurers can have equipment that will keep spawning monsters. In this case, you will need to track down the source of the monsters and take that piece of equipment off their hands.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("エイリアンなどの寄生生物が腹から出てくることもあります。腹にいる間なら毒や染料、硫酸を飲むことで完全駆除できますよ。寄生されている人には渡せば素直に飲んでくれます。むしろ飲ませないと宿主が生きている限り無限に産まれてくるんです。", "Aliens will spawn from impregnated people. Just hand poisons, dyes, or acids to the pregnant to cure them of it. They'll drink it quite happily if they're pregnant. If they don't get rid of the pregnancy, they'll just keep giving birth over and over again.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("寄生生物がすでに産まれすぎていたり、モンスターがモンスターを召喚したり、分裂して増殖しているケースもありますね。相手が倒せる強さなら、壁生成で囲って各個撃破しましょう。倒せない場合、街から脱出してください。５日ほどして様子を見に行くと、いなくなっているはずです。もし少し残っていたら、また日を改めましょう。", "Sometimes you'll find too many people giving birth, or maybe you might find a monster that keeps summoning more monsters. Or there could be a monster that keeps splitting indefinitely. If these monsters aren't too strong for for you to handle, you can just create some walls to fight them individually. If you can't handle them, then hightail it out of there. Waiting 5 days should be enough to get rid of them. If they're still there, then try coming back another day.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 8 ) {
			listmax = 0
			buff = lang("違う神の祭壇に捧げものをすることで、その祭壇を乗っ取って上書きすることができます。何も考えていないと、次に信仰したい神の祭壇が無くなることもあるでしょう。神の祭壇は特定の場所に置いてある分か、ネフィアでごく稀に生成されるものしかありません。後者が目当ての神のものである確率は単純に考えて八分の一です。代わりを確保していない状態で無計画に乗っ取るのは止めておきましょう。", "If you try to make offerings at the altar of a god that's not the one you're currently worshipping, your god will take over that altar. If you're not careful about this, you might not have an altar of a god that you want to switch to later on. Aside from the few altars that are at a couple of locations, they will only appear very rarely in Nefia. Even if you did manage to find an altar in a Nefia, there's only a 1/8 chance of it being one of the god you want, on top of the chance that it might be unclaimed. So be careful to leave one altar for every god.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 9 ) {
			listmax = 0
			buff = lang("信仰スキルは、夢を通じて神の偉大さを感じることで受動的に上昇します。また、信仰スキル１０以上の時にレベルアップすれば、広域技能の説法を習得できます。説法で聴衆を満足させることができれば、信仰スキルを能動的に上昇させられますよ。", "Getting visited by saints in your dream will deepen your faith. When you level up with 10 Faith or more, you will learn how to Preach, a 'W'ide area special action. When you preach and your audience is satisfied, this will also increase your Faith.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 10 ) {
			listmax = 0
			buff = lang("最初は、捧げものが簡単で生存能力を強化してくれるオパートスやジュア、生計を助けてくれるヤカテクトがいいでしょうね。アイテムを大人買いできる資金ができてきたらマニやルルウィ、イツパロトルも簡単です。エヘカトルは釣りができるようになってから、クミロミは農業ができるようになってからが無難です。", "For your first god, you could pick Opatos or Jure whose offerings are easy to acquire and will also increase your survivability. Or you could go with Yacatect, who will make it easier for you to earn money. With money, worshipping Lulwy, Itzpalt, and Mani will be easy. You may want to worship Ehekatl and Kumiromi only after acquiring the Fishing and Gardening skills respectively.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("多くの冒険者は、神々を何柱か信仰してご褒美をもらった後に最終信仰を決めていますね。全ての神を信仰コンプリートする者も少なくありません。神々は能力も性格も個性的なので、最終的には自分のスタイルに合っている神を選ぶといいでしょう。もちろん、面白さとか可愛さとかで決めてもいいんですけどね。", "Many adventurers change the god they worship several times before finally settling on one for good. Some of them even go through every single one of them in that process. Worshipping different gods will be different due to their unique boons offered and personalities, so you will have to decide which one is the best fit for you. Of course, you can always just go with the cutest or the most interesting one.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 11 ) {
			listmax = 0
			buff = lang("…これはファッションです。腐って変色しているわけではないのです。", "...It's hair. It's supposed to be fashionable. This lock of hair is not of a different colour because it's gone bad or anything.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 12 ) {
			listmax = 0
			buff = lang("だいたいどの物件にも使い道はあります。所持金と相談して、苦にならない範囲で買ってみましょう。博物館ははく製やカードを並べていくと、そこそこのお金やプラチナ硬貨が入手できます。畑は栽培スキルが必要ですが、軌道に乗れば様々なアイテムが手に入ります。充分にスキルがあればハーブも増産できますしね。", "Most deeds have a purpose, so buy one when you're sure it won't break your bank. Museums display figures and cards and yield gold and platinum pieces in income. Farms need Gardening skill to be useful, but yield all sorts of items once they're up and running. You can even mass-produce herbs with enough skill.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("建ててからやっぱりいらないな、と思ったらワールドマップ上で拾うことで撤去できます。権利書は戻ってきませんがね。場所を変えたい時は物件移設です。移設申請書があれば、内装そのままで移設ができるので安心ですね。", "If you want to get rid of a building after constructing it, 'g'rab it on the world map to get rid of it. You don't get the deed back though. If you just want to move it, use a deed of property transfer to keep the interior the way it was. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 13 ) {
			listmax = 0
			buff = lang("交易には、各地の交易店でのみ売れる交易品が必要です。交易品は旅糧以外の（荷車）アイテムのことで、荷車に積まれます。街ごとに安いもの高いものが違うので、荷車の積載限界まで積んで別の街まで運び、差額を利用して稼ぐことになります。交易品を工具で自作したり、盗賊団から強奪すれば効率が上がります。", "To trade, you will need trade goods that can be bought and sold only at traders in towns. Trade goods are those cargo items that go into your cart except for traveler's food. Different towns have will have different prices for each type of trade goods, so the idea here is fill your cart in one town, sell it off in another, and reap the price difference as profit. You can also craft trade goods yourself or get them from dispatching rogue attacks.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ただし、「交易だけ」で大儲けするのは大変です。生活費分くらいは余裕で稼げますが、基本は街から街へ移動するついでに併用して行うものですよ。なお、他所からの交易品を売却すると、額に応じて街の経済が活性化し、一時的に他店の品物数も増えます。そちら目当てで交易するのもいいでしょう。", "However, making a living from only trading is going to be difficult. It will bring in enough profits to pay for most of your expenses, but it's something that you should do on the side while moving from town to town. There's one more benefit to trading. Selling trade goods from other towns will stimulate the economy by an amount dependent on how much you've sold and temporarily increase the number of items that shopkeepers in that town will sell.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 14 ) {
			listmax = 0
			buff = lang("所持金に余裕があるなら、地面に落ちている金貨は拾わずに仲間に拾わせるといいでしょう。勝手に訓練に行ってくれるようになります。…これは育成講座の分野ですかね？", "If you have enough money, then consider leaving gold pieces on the floor there for your allies to pick up. They'll use it to train their potentials in town. ...I think you'll learn more about that in the Training seminars.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("後は、装備を強化して性能を底上げしていきましょう。＋６までは対応する巻物で強化できますが、武具店で＋１０まで強化できます。現行でいい感じの防具などは、ケチらずに強化しておくと後で仲間のお下がりとして重宝しますよ。また、装備している物の平均＋値は力の解放…ゲージ技の威力にも影響します。平均＋１０でダメージが５倍以上まで伸びるので、覚えておきましょう。", "You can also use that money to improve your equipment. Enchant scrolls will get them up to +6, and a blacksmith can bring that up to +10. If you have a decent piece of equipment, it's probably best not to be stingy and enchant it right away, then pass it to your allies when you find something better. One thing to remember is that the average +enhance value of all your equipment will determine the power of your gauge attacks, done by using Gauge Release. At an average of +10, the multiplier on gauge attacks reaches a whopping x5.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…っと。これは道具講座の分野ですね。つまり、お金は生計を立てるだけでなく、様々な戦力強化にも絡んでくる材料なんです。惜しまずにちゃんと使っていきましょう。先に安定した収入基盤さえ作っておけば、また稼げるんですから。", "...That was material that's supposed to be covered by the Item seminars. But anyway, money is useful for more than letting you live a cushy life - it can enable you to fight better. Don't be stingy, because you can always earn more as long as you've already made the necessary investments. ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 15 ) {
			listmax = 0
			buff = lang("お金があり余ってきたら、よく使う店に投資しましょう。自宅に招いてる店に投資するようにすれば、特定の街に縛られずに引っ越しができますよ。投資すると扱う品物の質や数が向上します。規模１５０もあれば十分でしょう。", "If you have money to burn, then invest in shops. If you invest in the shopkeepers hired in your home, that investment will be there even if you move your house elsewhere. By investing, the quality and number of items that they sell will increase. About 150 ranks should suffice.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それでも余ったら？うーん…仲間を増やしてみるとか、複数の武具店やブラックマーケットにさらに投資するとか？特に何もなければ、貯金箱に入れておいたほうがいいですよ。現在の所持金に応じて税金が増えるだけですからね。", "If you still have money after that... Hmm. Maybe invest in multiple blacksmiths and blackmarkets vendors? If you don't really have anything to spend it on, you should just put it in a kitty bank. After all, taxes increase based on the money that you have on you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) <= 102 ) {
			listmax = 0
			buff = lang("じゃあ質問のある人は直接聞きに来てください。", "Alright, if anyone has anymore questions you can come see me at the front.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( 0 ) {
			txt lang("ゼミフラグ＝" + gdata(GDATA_FLAG_TRAINING_COURSE_STATE) + " ", " ")
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 897 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) == 100 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("道具講座担当のクレッシェ・ルカンよ。れっしぇーって呼んでね！", "Hey everybody, I'm Cresce Lucan, your lecturer for the Item Seminars. You can just call me Ressie.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("冒険者を始めたばかりだと、どの道具が重要でどの道具がさほど重要でないのか、わからないと思うの。だから用意するべき道具を簡単に説明しておくわね。", "If you're just starting off, you might not know which items are useful and which aren't, so that's what I'm going to cover today.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("まずは１つずつでいいから持っておきたいものとして、紐、聴診器、コマンドフラッグ、ホイッスル。その辺で普通に拾えたり買えたりするけれど、後から手に入れようとするとなかなか見つからないのよね。", "I'm going to start with the ones that are must haves - the leash, stethoscope, command flag, and whistle. You can buy or find these just about everywhere, but they're surprisingly hard to find when you need them.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("音楽のディスク、カード、はく製などは、戦闘には関係ないけど集めておくと使う日が来るかも。後から揃えるのは大変なものだし、売るのはダブったものだけにしようね。", "You should collect music discs and figures. They don't do anything in combat, but the time may come when you need them so start collecting. If you neglect doing that, it's going to be really tiresome to start your collection from scratch later. If you have any duplicates, you can just sell them off.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("道具の効果は、「鑑定」することでわかるようになるの。鑑定の巻物や杖を使うか、街の魔術士さんに鑑定してもらいましょう。初めて見た道具は使う前にちゃんと鑑定してね。未鑑定だと売却価格も大幅に下がったりするわ。分析スキルが高いなら、持ち歩いている内に鑑定できたりもするけど…時間はかかるわね。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("今日は、普段持ち歩くべき道具についてお勉強しましょうね。", "Okay, today I'm gonna cover the items that you should always have with you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("使わないものは置いていくとして回復とテレポート、食料とシェルターは忘れずに持っていって。杖は微妙に重いから気持ち少なめにね。アイテムを守るために耐火・耐冷ブランケットを１枚ずつ忘れずに。強い敵や未知の土地に挑む場合は、強化用のアイテムをいつもより多く。", "The must-haves are healing and teleporting items, as well as food and a shelter. Rods are rather heavy, so don't carry too many of them. You should also have one each of fireproof and coldproof blankets to protect your items. When you know you're going to go up against something strong or headed into the unknown, stock up on buffing items.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("駆け出しの間は、１０ｓ以上あるような重いものは、四次元ミラーにでもしまっておいて。重量挙げスキルが育てばそのうち素で持てるようにもなるわ。戦利品を多く持ち帰るためにも、重量・枠に余裕を持ってね。", "When you're starting off, put items that are 10s or more in a 4-dimensional mirror. Later on, you'll become able to carry them around normally when your Weight Lifting skill is high enough. But always be sure to leave enough inventory space and weight capacity to carry the spoils out of a Nefia.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("いらない物を置くのはdキー…dropだけど、この時＊キーを押すと連続で物を置けるわ。調節する際に便利だから覚えておきましょう。", "To drop things that you don't need, press the 'd' key. If you want to drop multiple items, then press the '*' key after that. This is invaluable when reorganizing your backpack.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("第３回は、道具の状態について詳しく教えるわね。", "For part 3, I'll be talking about two possible states that items can have - blessed and cursed.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("信仰している神の祭壇に水を置くと、水が祝福されるの。神を信仰していない場合は、神の支配していない祭壇に水を置けばいいわ。祝福された水を混ぜることでどんな道具でも祝福できるのよ。１スタックまとめて効果があるから、巻物なんかには数十枚貯めてから混ぜましょう。", "If you drop water onto the altar of a god you are currently worshipping, it becomes blessed water. If you aren't worshipping any god, then just drop it onto an altar that isn't currently claimed by any god. Any item that gets mixed with this blessed water becomes blessed itself. You can bless an entire stack at a go, so you can wait until you've accumulated dozens of a scroll before blessing the whole stack.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("一方、悪夢や敵の呪いで装備が呪われることもあるわ。呪われた装備は外せないうえに、身に着けていると突然テレポートさせられたりダメージを受けたりするの。早めに解呪の魔法や道具を使うか、街の癒し手に解呪してもらいましょう。", "Sometimes your equipment might get cursed during a nightmare or by an enemy. You can't take off cursed equipment, and while wearing it you'll keep getting hurt or teleported at random. When that happens, you'll want to uncurse it with a spell or a magic tool. Or you can ask the healers in towns to do it for you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("呪われている食べ物は吐いてしまうし、ポーションや巻物、杖は本来の効果が出なかったり弱まったりしてしまうわ。売値も下がってしまうから、特に理由がなければ祝福された解呪の巻物を使っておきましょう。祝福された解呪の巻物は所持品全部に解呪効果が出る優れものよ。", "Cursed food make you vomit when you eat. Cursed potions, scrolls, and rods will either have no effect or have their effect weakened. Their prices will also be reduced. To fix that, you can use a blessed scroll of uncurse on them. When blessed, scrolls of uncurse will uncurse each and every single item in your backback. They're great like that.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("最後となる第４回では、装備についてもう少し説明するわよ。", "For the last part, I'm going to talk a little about equipment.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("分析スキルで自然鑑定が発動するとわかるんだけど、装備には粗悪、良質、高品質、奇跡、神器、特別の６種があるわ。完全に鑑定すると、高品質には接頭語、奇跡には☆と『』、神器には☆と《》、特別には★と《》がつくの。", "Equipment has 6 ranks - Bad, Good, Great, Miracle, Godly, and Special. Its rank becomes evident when your Analysis skill does its job. When completely identified, then you'll also be able to see the prefix for Great equipment, the bracketed <name> for Miracle, {name} for Godly, and bracketed <name> again for Special equipment.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("高品質、奇跡、神器には特別な力…エンチャントがランダムで付与されるわ。奇跡と神器はランダムアーティファクトと呼ばれていて、高品質以下よりも本体性能もエンチャント面も上限が上なの。特別は固定アーティファクトと呼ばれ、性能が固定されているわ。", "Great, Miracle, and Godly equipment are unusually powerful. This is due to the random equipment attributes that they come with. Miracle and Godly equipment are also referred to as 'random artifacts' by some people, and these have better base stats and equipment attributes than Great equipment. Special equipment are also called 'unique artifacts'; their equipment attributes and stats are predetermined and will be the same every time you come across them.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("最終的には奇跡以降で装備欄を埋めることになるわね。高品質は奇跡にランクアップさせる方法もあるから、いいエンチャントがついていたら取っておいてね。", "Every piece of equipment that you wear will eventually be of Miracle quality or greater. There's also a method of promoting Great equipment into Miracle quality, so you find a really nice Great piece of equipment you can save it for later.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE) = 101
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("集めるべき巻物は？", "What kind of scrolls should I stock up on?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("集めるべきポーションは？", "What kind of potions should I stock up on?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("寝具と調理器具はどれがいいの？", "Which sleeping and cooking tools should I use?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("れっしぇー歳いくつ？", "How old are you Ressie?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("杖ってどうなの？", "But what about rods?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang("重すぎるとどうなるの？", "What happens when I'm overweight?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 6
			listn(0, listmax) = lang("持ち歩かないものはどうするの？", "What do I do with stuff that's too heavy?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 7
			listn(0, listmax) = lang("保持指定って何？", "What's 'Tag No-Drop'?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 8
			listn(0, listmax) = lang("他に祝福したほうがいい道具は？", "What other items should I bless?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 9
			listn(0, listmax) = lang("呪った方がいい道具は？", "What items should I curse before using?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 10
			listn(0, listmax) = lang("＋１とかって何？", "What's that +1 thing mean?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 11
			listn(0, listmax) = lang("れっしぇー恋人いないの？", "Are you single, Ressie?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 12
			listn(0, listmax) = lang("いい装備の入手法は？", "Where do I find good equipment?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 13
			listn(0, listmax) = lang("装備の素材って？", "What does the material of equipment mean?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 14
			listn(0, listmax) = lang("マテリアルって何？", "What are materials?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 15
			listn(0, listmax) = lang("合成の壺って？", "What's the pot for fusion?")
			listmax++
		}
		buff = lang("何か質問はあるかしら？", "So. Got any questions?")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("巻物はとても軽いうえに、スキルがなくても使えるから駆け出し時代に重宝するの。特に鑑定、素材変化、魔法の地図、物質感知、帰還、解呪、武器防具強化、不思議、マテリアル生成あたりをこつこつ集めておくと後で便利よ。よく出る割に単価の高い神託は、交換や売却にも使えるわ。", "Scrolls are great because they're light and you need no skill to use them. Scrolls of identify, change material, magical map, detect objects, return, uncurse, enchant armor and weapon, wonder, and gain materials are the ones that you should start stockpiling for later. Oh, and scrolls of oracle too, which are common but surprisingly expensive, so you can sell them or use them for trading items.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("大抵は代替手段があるけれど、巻物は軽いから落ちてるのは全回収でオッケー。あと成長、能力獲得、羽の生えた巻物はレアよ。最初はなるべく使わずに、後で必要になる時まで温存しておきたいところね。家具の本棚は家に置くと本や巻物を収納できるから整理整頓に便利よ。", "Most scrolls have their spell, rod, or potion equivalents, but there's no harm in picking up every single one that you find because they're just so light. Scrolls of growth, gain attribute, and flying scrolls don't, however. And they are very rare finds. You probably shouldn't use them right away either - save them up for when you need them. You can also store scrolls and books in bookshelves that you have at home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("ポーションは軽くていっぱい持てるの。一部のポーションは魔道具スキルで効果が伸びるから取っておくといいわ。回復系とバフ系、デバフ系、状態異常系、空き瓶、耐酸性コーティング液、耐熱コーティング液…とにかく数を用意しておきたいわね。軽いから落ちてるのは全回収でオッケーよ。", "Potions are light as well, so you can carry a lot of these too. Some potions have their effects increased by the Magic Device skill, so that might be something you want to get. The ones that you want to stock up on are those for healing, buffing, debuffing, status ailments, empty bottles, and acidproof and fireproof liquids. Or you could just pick up every single one that you find since they're so light.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あと水、エーテル抗体、潜在能力、下落、進化、ヘルメスの血、スピードリンクはレアだから取りこぼしのないようにね。家具の「ポーションの棚」は家に置くとポーションを収納できるから手に入れておきたいわ。", "Don't overlook bottles of water, speed upper, and hermes blood, as well as potions of cure corruption, potential, descent, and evolution. These are rare. You can store potions in the rack of potions furniture item when you have it placed at home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("寝具と調理器具にはランクがあって、それぞれ効果に差があるわ。最初は軽い低ランク品でもいいけれど、多少無理してでも高ランク品に替えていきたいわね。街中にある高ランク品を使うことも、視野に入れておいてね。", "Sleeping and cooking tools are ranked and hence differ in their effects. Early on, you can just stick with lower ranked once since they tend to be lighter, but you should switch over to higher ranks when you can even though they're heavy. When using the beds and cooking tools that are in town, try to find the highest ranked ones.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("…。", "...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そんな時に便利な道具がインサイトレンズよ！相手の年齢どころか、身長体重種族、そして能力値や弱点耐性まで丸わかり！道具の進歩って素晴らしいと思わない？", "When you want to know something like that, try using the insight lens! It tells you the target's age, height, race, attributes, and even their elemental weaknesses. Tool are really advanced these days, dont'cha think?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 4 ) {
			listmax = 0
			buff = lang("杖の場合、魔道具スキルが効果だけでなく成功率にも影響するわ。微妙に重いし残り回数が違うとまとまらないから少な目にね。杖の残り回数は充填の巻物や技能の魔力の充填で増やせるわ。魔力の充填は魔道具スキルレベル２０以上の時レベルアップで習得よ。", "Aha, so you want to know about rods. The Magic Device skill not only affects the effectiveness of rods - it also increases the chance that you'll successfully zap them. They're kinda heavy and don't stack well in your backpack though. The remaining charges on a rod can usually be topped up with scrolls of recharge or the Fill Charge special action. You'll learn the latter when you level up with 20 levels in Magic Device or more.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("回復系と壁生成、テレポート、変化、鑑定の杖は便利ね。それでも残り回数を充填できるようになったら４、５本で充分かな。杖主体で戦う気がないのなら、攻撃系の杖は魔道具上げや残り回数の抽出、元素の神への捧げものに使うといいわ。", "Rods of healing, wall creation, change creature, and identify are the useful ones. If you're able to recharge them, then 4-5 of each rod should be enough. If you don't rely on rods for their attack spells, then you can draw the charges from those offensive rods or offer them to the god of Elements.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("支配の杖は敵を仲間に引き込むレア杖だけど、魔道具スキルのレベルが支配できる相手のレベルに関わってくるわ。スキルが低いと絶対成功しないから、使うつもりならスキルを上げておきましょう。", "Rods of domination are rare ones that will recruit enemies as your allies, and the level of enemies they are dominate will depend on your Magic Device skill level. The chance of successfully dominating a target is pretty much negligible with a low skill level, so level it up before zapping these rods.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 5 ) {
			listmax = 0
			buff = lang("バックパックの重量に応じて重荷、圧迫、超過、潰れと変化していくの。重荷くらいなら問題ないけれど、圧迫以降は速度が大きく下がって戦闘が不利になるわ。超過になると歩いているだけでたまに大ダメージを受けるようになるから、そのまま行動してると事故死に一直線よ。どうしても超過で移動しなきゃいけない場合は、時々荷物を降ろして休憩しましょうね。", "You get Burdened, Burdened!, Overweight, and Overweight! as the total weight of things in your backpack increases. Being Burdened is okay, but any more than that and it will gimp your speed too much, which is dangerous in combat. When at Overweight or worse, you will periodically take damage when moving around, so if you keep pushing yourself like that you can die. If there's something that you absolutely must carry but makes you Overweight, drop it on the ground once in a while and take a break to recover your health.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ちなみに重量挙げスキルは、重荷以上の時にしか成長しないの。うまく荷物を調整して、普段は重荷で活動・強敵と戦う時は重荷解除、ってできるようにしておきたいわね。", "And remember, your Weight Lifting skill trains only when you are Burdened or above. You'll have to manage what you carry to keep yourself Burdened, then when in a pinch you can drop enough to remove all encumbrance.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 6 ) {
			listmax = 0
			buff = lang("すぐに使わない道具は、わが家や倉庫に置いておきましょう。死んで落としたり、燃やされたりするわ。軽い巻物やポーションも半分くらいは保管しておいた方がいいわ。ただし場所によってものを置ける数に限界があるの。必要に応じて倉庫を建てるか、掃除されない場所に仮置きするといいわ。", "Items you don't need often can be left at home or in a storage house. Or else you might end up dropping them when you die or lose them to a fire. That's why it's a good idea to leave about half of your scrolls and potions somewhere safe even though they're light. However, every location has a maximum number of items that you can drop on the ground. You can buy more storage houses when you need them, or just leave them for a bit on maps that don't get periodically cleaned.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("掃除されない場所かどうかは、不要なものを一週間ほど置いて確認してみましょう？大抵の街は置いたものが片づけられてしまうけれど、地下は対象外だったりするのよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そうそう！置ける道具の種類数は、場所によって決まっているの。限界ギリギリのところに新しい物が生成されると、古い物がいくつか破壊されてしまうわ。これはマップだけじゃなくて、給料箱とかお店の金庫の中も同じなの。定期的に整理整頓して、余裕をもたせておきましょうね。", "Speaking of storage, the number of different types of items - that is, stacks of items - that you can store is different for different areas and containers. When a new item is created but there's no more space, then a few older items will get destroyed. This applies not only to maps, but also to containers like salary and shop chests. You'll have to clean out their items periodically to make sure they have room to spare.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 7 ) {
			listmax = 0
			buff = lang("保持指定は、調べる…ｘキーで一覧を表示している最中に＊キーでできるわよ。もう一度＊キーで解除されるわ。", "To tag items as no-drop, just e'x'amine items - 'x' key - then press '*' on an item. Pressing '*' again will unmark it.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("保持指定をした道具は、「大事なもの」として扱われるわ。置いたり売ったりできなくなるから、紛失防止になるの。いくらでも指定できるから、貴重な道具や良い装備は忘れずに指定しておきましょうね？…漫然と道具整理していると、たまに手が滑るのよねぇ。", "Items that you've tagged as no-drop means that they are important to you. As the name suggests, while tagged, you won't be able to drop those items on the ground or sell them, so you can avoid accidentally losing them. You can tag as many items as you want, so be sure to tag important items and good pieces of equipment. After all, accidents do happen when you're not paying attention.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 8 ) {
			listmax = 0
			buff = lang("杖や巻物は祝福すると効果が強化されるのよ。特に成長、素材変化、羽の生えた巻物はまとめて祝福してから使いたいわ。", "Rods and scrolls are more effective when blessed. Especially for scrolls of growth, change material, and flying. These you are the ones that you really should bless before using.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("祝福された食品・飲み物は、たまに幸運の日が訪れることがあるの。でもそれ狙いでわざわざ祝福するほどではないわ。祝福されたポーションは効果が上昇するけれど、特に肉体・精神復活と潜在能力、ヘルメスの血は祝福してから使った方がいいわね。", "When eating and drinking blessed food and drinks, you may sometimes experience a Lucky Day. It's not often enough to warrant deliberately blessing your food though. Blessed potions are more efficacious, but the ones that you really need to bless before using are potions of potential, restore body, restore spirit, potential, and bottles of hermes blood.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("祝福された武器は攻撃力と命中力が、防具は回避力と防御力がわずかにアップするわ。呪いで上書きされちゃうしまとめて祝福できないから、祝福された水が余っていない限り、他の道具に回した方が無難ね。", "When blessed, the attack power and accuracy of weapons increase. For armour, it's evasion and defence. Unlike other items, however, you can't stack up equipment and bless them all at one go, so you should do this only when you have a lot of blessed water to spare and there are no other items that you need to bless.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 9 ) {
			listmax = 0
			buff = lang("使えそうなのは装備を重くできる呪われた羽の巻物や、体重を重くできる呪われた鈍足とかかしら。呪縛の巻物で呪われた水を作っておくと、呪いたい時に便利よ。", "The practical ones are cursed flying scrolls that can be used to make equipment heavier and cursed rods or potions of slow to make you or your allies heavier. You can make cursed water with scrolls of curse; cursed water are a lot easier to use than the scrolls for cursing things.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ちなみに呪縛の巻物が呪うのは所持品からランダム。と、いうことは…？もし所持品が水だけなら…？", "That's because scrolls of curse will curse a random item that you have on you. So guess what happens when the only thing you're carrying is a bottle of water?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 10 ) {
			listmax = 0
			buff = lang("＋値ね。基本的に、数値が高いと価格が跳ね上がるわ。食品なら料理、装備や家具なら高級な素材に素材変化するとさらに価格が上がるの。そして、栄養のある食品の場合は元々の能力鍛え効果も強化されるわよ。", "The +enhance value? For food, it increases the amount of nutrition that they have if they had any in the first place. That means they'll train your attributes more when eaten. The price of items also increases as their +enhance value increases. And by the way, you can increase the price of food even further if they are cooked into a high-ranked dish. For equipment and furniture, you can reroll their material into an expensive one with scrolls of change material to make them more expensive.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("装備の場合は＋の数値分、性能が強化されているってことでもあるわ。武器なら攻撃力と命中力が、防具なら回避力と防御力が少しアップしているの。強化の巻物を使うか、武具店で強化してもらえるわよ。この場合の最大値は＋１０だけど、スターダストを使えば＋１５まで可能よ。。", "+Enhance value makes equipment more effective. Weapons become more accurate and do more damage, while armour have their evasion and defence increased. The maximum enhance is +10 if you enhance them with scrolls of enchant weapon and armor or have a blacksmith do it for you. If you use stardust, it can go up to +15.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あと、酸攻撃を受けると装備の＋値が減ったり−になったりするの。当然だけど性能も低下。素材変化で一度でも耐酸能力のある素材に変えれば、別の素材に変えても持続するわよ。もしくは耐酸性コーティング液を混ぜておきましょう。", "When equipment gets damaged by acid, their +enhance value and thus effectiveness will get decreased and may even go negative. If you change a piece of equipment's material into something acidproof once, it'll remain acidproof forever even if you change it again. You could also mix an acidproof liquid on them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 11 ) {
			listmax = 0
			buff = lang("喧嘩売ってんのか殺すぞ。", "Watch your mouth or I'll kill you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 12 ) {
			listmax = 0
			buff = lang("エンチャントはランダムだから、とにかく数が重要ね。店や依頼で手に入れるだけじゃ足りないわ。こつこつネフィアで拾ったり、誰かから奪ったりしないと。その場合は、高品質以上を狙いましょう。鑑定できる道具を持っていれば、その場で確かめてもいいわね。", "Since equipment come with random attributes, you'll want to have as many sources as possible to maximize your chances of finding something good. Relying on shops and job rewards isn't going to be enough. Keep an eye out for equipment that's Great or better in Nefia or on other people. If you have a rod or scroll that you can use to identify, then you can determine if they're worth bringing back from a Nefia.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("軽い装備品なら、粗悪や良質でも分析スキル上げに使えるかもしれないわね。でも重い装備はよっぽど重量と枠に余裕がない限り、高品質未満だとわかった瞬間捨てたほうがいいわ。その分他のものを拾いましょう。", "Light pieces of equipment can be useful even if they are only of Bad or Good quality - carry them around to train your Analysis. As for heavier equipment, it's best to toss them away the moment you find out that they're only Bad or Good unless you have a lot of carrying capacity to spare. Use your spare capacity for something more valuable.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("結婚指輪・首輪、羽、ソックス、スピードの指輪、パンティー、オーロラリング、ヴィンデールクローク、セブンリーグブーツ、フェアリーハットは軽くて固有効果も強いから、粗悪や良質でも拾い集めておきたいわね。特に結婚指輪・首輪は友好を深めるのにも使えるわ。", "Engagement rings, engagement amulets, wings, sox, speed rings, panties, aurora rings, vindale cloaks, seven league boots, and fairy hats are light and are guaranteed to have certain equipment attributes. These you should pick up even if they're only Bad or Good. Engagement rings and amulets can be used to increase the Impress that someone has for you too.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 13 ) {
			listmax = 0
			buff = lang("素材によって色々な効果があるわ。それに装備の能力も素材に応じて倍率がかかるの。倍率だから、元の性能が低いカテゴリ…指輪や首輪なんかは足りない耐性を補う素材を、元の性能が高いカテゴリは装備能力の高くなる素材を選ぶといいわ。", "Each material has its own properties and its multiplier for equipment stats. And the fact that it's a multiplier means that you should try to get materials that give resistances for types of equipment that have bad base stats like rings and amulets, and materials that give high multipliers for equipment with high base stats.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("素材変化の巻物は変化する素材が決まっているけれど、祝福すると上位の素材になったりもするわ。他には素材槌っていう対応した素材に確定で変えるものもあるけれど…レアなのよねぇ。神器の装備は素材槌じゃないと素材変化できないから注意よ。", "Scrolls of change materials change materials to the next one in a predetermined list, with the possibility of changing into the best materials if the scroll is blessed. Material kits which will give you the material that they're for right off the bat, but are rare. And remember: Godly equipment can only be rerolled with material kits.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 14 ) {
			listmax = 0
			buff = lang("マテリアルは重さのない特殊な材料よ。そのままでは使えないけれど、錬金術の道具や工具、宝石加工ツール、裁縫セットで道具を自作する際に必要になるわ。今持っているマテリアルは、mキー…materialで確認できるの。", "Materials - as in those listed in the 'm'aterial list - are ingredients and are weightless. They're not useful as they are; they're used to create items with the alchemy kit, carpenter's tool, gem cutter, and sewing kit.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("マテリアルはネフィア内にある採取スポットでsキー…search（探す）を行うと手に入るわ。採取スポットの種類に応じて釣り、採掘、栽培、生化学、探知のスキルが必要よ。浅い階層では上位のマテリアルは取れないことと、ネフィアのタイプによって取れる種類が変わることに気をつけて。あと、採取に成功しても失敗しても対応スキルに経験値が入るから、取っておきましょう。", "Materials are obtained by searching the gathering spots inside Nefia. To 's'earch, either use the 's' key, Space, or Enter. Different types of gathering spots will need different skills for searching - these skills are Fishing, Mining, Gene Engineer, and Detection. Spots on dungeon floors with a low danger level will yield no high-level materials, and the types of materials you can find will also vary with Nefia type. It's good to have these skills, as you will receive experience for them even if you fail while searching.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("工作、錬金術、裁縫、宝石細工のスキルが上がれば、マテリアルで自作できるものも増えていくわ。自作することでも経験値が入るから、積極的に採取スポットで材料を集めていきましょう。仲良くなった仲間がスキルを持っていれば、マテリアルを集めてきてくれることもあるのよ。", "The number of types of items that you can create with Carpentry, Alchemy, Tailoring, and Jeweler increase as those skills level up. The skills will also level up when you craft items, so be sure to search every spot you come across for materials to be able to craft more items. If an ally has one or more of those skills required for gathering materials and their Impress is high, they'll gather materials for you from enemies too.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 15 ) {
			listmax = 0
			buff = lang("合成の壺なんかを使うと、道具同士を合成して新しいものを自作したり、特殊効果を付与することができたりするわ。特定のスキルや道具が必要になるけれど、本当に色々なことが可能よ。一度確認してみるといいわ。", "Pot for fusion allows you to fuse items together or imbue them with special effects. There are skill and item requirements to fulfill, but there are many things that you can do with this, so give it a go.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) <= 102 ) {
			listmax = 0
			buff = lang("質問のある人はいるかしら？いたら聞きに来てね。", "Come up to the front if you have any more burning questions.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( 0 ) {
			txt lang("ゼミフラグ＝" + gdata(GDATA_FLAG_TRAINING_COURSE_STATE) + " ", " ")
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 898 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) == 100 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("育成講座担当のイヅル・グンバイだ、よろしくな。師範とでも呼んでくれよ。さて、初回はcキー…characterで確認できる能力値について簡単に教えておくぞ。", "I'm your instructor for the training seminars. You may call me 'Instructor'. To begin with, I'm going to explain the stats on the 'c'haracter sheet, accessible via the 'c' key.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("筋力や耐久といった値が主能力。潜在能力は、その能力がどのくらい伸びやすいかを示している。superb（素晴らしい）が最高で、hopeless（絶望的）が最低の状態だ。主能力は対応したスキルや能力、行動を行うことで自然に成長していくんだ。", "Strength, Constitution and all that are what we call the main attributes. Potentials are how easy they are to raise. The highest is 'Superb', and the lowest 'Hopeless'. Whenever you use a skill or special action, you will train the attribute that they depend on.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("主能力の潜在を回復させるには、ランクの高い寝具で睡眠を取ることだ。寝る前に得た経験値が多いほど潜在も多く回復するけれど、睡眠を遅らせてまで経験値を稼ぐくらいなら早く寝たほうがいいぞ！規模の大きい魔法店が稀に入荷する、潜在能力のポーションを飲むのもいいけれど、レアゆえに供給がなかなか追いつかないだろうな。", "To recover the potentials of your attributes, you need to sleep in a high-ranked bed. The more experience you gain before sleeping, the more potentials you recover while you sleep. Don't stay up just to earn more experience though. It's not worth it. It's better to just go to bed when you need to. High-ranked magic shops will stock potions of potential occasionally. But not often enough that you can depend entirely on just these to raise your potentials.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("主能力に*マークがついているものは、装備によって能力維持…能力が低下しない状態になっている。上昇は普通にするから安心してくれ。能力維持されていないと、敵の弱体化技能や身体を痛めた影響で一時的に下がったりする。下がった主能力は肉体・精神復活の道具か魔法、街の癒し手で戻すといいぞ。", "Attributes that have a * mark beside them means that they are being maintained by your equipment. That just means that they cannot be temporarily weakened. Don't worry, you can train them and they'll increase just fine. When not maintained, they can get weakened by certain special actions that enemies have, or it can also happen when you don't take care of yourself. When they are weakened, you can restore them with potions of restore body or spirit, or pay a healer in town to do that for you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("スキルは、トレイナーにプラチナ硬貨を払うことで、習得したり潜在を回復させたりできるぞ。成長させるには経験値の入る行動が必要なんだ。どのスキルに経験値が入っているのかは、スキルトラック…スキルを*キーで登録すればわかりやすいな。", "Now. Skill potentials. You can pay skill trainers in towns platinum pieces to increase them. To increase their levels, you will need to earn experience for them. You can easily check if a skill is receiving experience by tracking it, which will display it on the left at all times. Track a skill by pressing the '*' key while it's selected.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("今日は仲間…ペットと呼ぶ人もいるが…について軽く教えようか。", "The topic for today is allies. Also known as pets.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("仲間にできる数は主人の魅力が高まれば少しずつ増えていく。最大１５人まで増えるけれど、枠がないと依頼人やクエスト護衛対象を連れ歩けないから、最低１人分は空けておきたいな。", "The number of allies you can have increases with your Charisma, up to a maximum of 15. This limit includes people you are currently escorting for a job or quest, so keep at least one slot free at all times.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("仲間は専門の店で買ったり、支配の魔法や杖で支配下に置いたり、モンスターボールで捕まえたりすることで増やせる。倒すことで仲間になる者や、信仰を深めると神からもらえる者もいるぞ。", "To get allies, you can buy them from some shopkeepers, dominate enemies with the spell or rod, or capture them with a monster ball. Some enemies will also join you when they are defeated, and a few will join you when you have enough favour with your god.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("支配は魔法レベルや魔道具レベルが相手のレベルに比べて低いと成功しない。乱用は難しいけれど即、効果があるんだ。モンスターボールは特にスキルがなくても使えるけれど相手を瀕死にしないと成功しない。上手く使い分けよう。", "If the level of your Dominate spell - your Magic Device level for the rod of domination - is lower than the target's level, then you will fail to recruit it. These aren't things that you can use haphazardly due to their cost, but their effects are immediate. Monster balls require no skill, but can only be used when targets are nearly dead. Use whichever suits your needs best.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("前回に引き続き、仲間について説明しよう。応用編だぞ。", "Last seminar, we talked about allies. Today, I'm going to talk about how you can apply what you've learned last time.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("仲間は能力を上げるだけではデコイにすらならないぞ。大抵は能力が同等以上の相手と戦うことになるわけだしな。ちゃんとした装備を与えてやろう。逆に言うと、装備が確保できていない状態で無理に仲間を戦闘に連れていくのはわざわざ殺しにいくようなものだ。", "Your allies are not going to be useful if all you do is increase their attributes. Not even as decoys. After all, most of the time they're going to be up against something that has higher attributes than they do. That's why equipment is important. Making them fight while they're not properly equipped is no different from leading them to slaughter.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("育てるのは早期から並行してやっておきたいから、仲間をどんどん増やすのは構わない。ただ、装備が揃うまではお留守番させておこう。死なせると友好度も下がって嫌われちゃうぞ。生存率を上げるために、紐と聴診器は必ず使っておこう。仲間を回復するのに手いっぱいになるようなら、装備を見直したほうがいいぞ。", "Training allies is something you should start early and do in parallel with everything else, so you can have as many of them with you as you want. But keep them out of combat until they have their equipment, or else they'll just die and their Impress for you will fall. To further increase their survivability, use a leash and stethoscope on them. If you find yourself healing them all the time, then you should get them better equipment.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("育て上げ、装備を固めた仲間は安心感が違うぞ。最終的には放っておいても神クラスを余裕で鎮圧するくらいになる。単純に攻撃手段も増えるしターゲットも分散するから、孤独を愛していない限り是非育成してみてくれ！", "You will find allies that have been trained and properly equipped far more dependable. Eventually, they might even be able to easily go toe-to-toe with a god. Allies are also a source of damage and act as additional targets for enemy attacks. Unless you love being alone, then try to train some allies.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("もう第４回か…早いものだな。今までの話から、育成において潜在がどれだけ重要なのはわかってくれたかな？", "We're at the fourth part already! How time flies. You probably already know how important potentials are in training.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("最終経験値は、元の経験値量をベースに、潜在と現在能力値によって増減するんだ。能力が低い間は少ない経験値と潜在でももりもり上がっていく。", "But the skill and attribute levels themselves also matter; these, together with potentials, will determine the final amount of experience received by the skill or attribute. For example, while your attributes are low, they'll grow at a very fast pace even if your potentials for them are terrible.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("だけどある程度能力が上がった後は、潜在や元の経験値量を上げないと最終経験値が微々たるものになってしまうんだ。切り捨てでゼロにすら成り得る。初回で教えたスキルトラックも使って、経験値の入り方を確認するのもいいだろうな。", "But once your attributes are higher, you will have to increase your potentials or else they won't grow at all. You might even end up with zero growth after rounding occurs. It's times like these when skill tracking comes in handy in making sure that your skills aren't stagnating.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("元の経験値量については…主能力の「習得」が高いと、経験値が稀に増えることがある。あと、召喚された魔物や分裂体からは一切経験値が入らないので気をつけてくれ。こんなところかな？", "One thing to note is that there is a low chance that your Learning attribute can increase amount of experience gained, and this chance increases with Learning. Oh, and monsters that were summoned or created by splitting give no experience whatsoever.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE) = 101
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("おすすめのスキルは？", "Which skills are useful?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("技能って？", "What are special actions?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("プラチナの入手法は？", "How do I get platinum pieces?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("師範って何？", "Why 'Instructor'?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("仲間の能力を上げるには？", "How do I increase my allies' attributes?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang("仲間のスキルを上げるには？", "How do I level up my allies' skills?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 6
			listn(0, listmax) = lang("仲間の潜在能力を高めるには？", "How do I increase my allies' potentials?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 7
			listn(0, listmax) = lang("仲間が死んだら？", "What do I do when my allies die?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 8
			listn(0, listmax) = lang("友好度と主従度って？", "What's are the Impress and Servant/Master values?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 9
			listn(0, listmax) = lang("タッグって何？", "What's tag-teaming?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 10
			listn(0, listmax) = lang("仲間の進化って？", "What's pet evolution?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 11
			listn(0, listmax) = lang("仲間にスキルを覚えさせるには？", "How do I teach my allies new skills?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 12
			listn(0, listmax) = lang("魔法を覚えるには？", "How do I learn spells?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 13
			listn(0, listmax) = lang("魔法の鍛え方は？", "How do I train spells?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 14
			listn(0, listmax) = lang("ＡＰとＩＮＩＴの関係は？", "What does INI have to do with AP?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 15
			listn(0, listmax) = lang("いづるんの実家って？", "What does your family do, I-dude?")
			listmax++
		}
		buff = lang("さぁ、質問はあるかい？", "Questions?")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("むうー…どのスキルも使い道があるからなぁ。優先的に取るべきなのは、生存力・成長力に関わる料理、持てる重量を増やす重量挙げ、出費を抑える交渉、他スキルの成長を助ける旅歩き、かな。", "Hm. That's a difficult one to answer, because every skill has their use. I would say that the first ones you should get are Cooking, which is necessary for survival and training your attributes, Weight Lifting so you can carry more, Negotiation to reduce your expenses, and Traveling to train every one of your skills.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そこまで揃ったら、探索に役立つ分析、鍵開け、採掘、魔道具などのスキルを、そして目指すスタイルが決まったらそれに合わせたスキルを取ろうか。…まぁ、難しいことは考えず、全部取っても構わないぞ。早期から持っておけば他のスタイルに切り換えるのも楽だろうな。", "Once you have those, then go for skills that come in handy for Nefia-diving, like Analysis, Lock Picking, Mining, and Magic Device. Once you have those, you should get ones that match the way you do things. It's also okay to just get every skill. If you get every skill early on, you'll be able to switch between fighting styles and whatnot with relative ease later.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("街ごとにトレイナーの教えてくれるスキルは違うんだ。今挙げたスキルならここからすぐ北にあるヴェルニース、東南東にあるヨウィン、西北西にあるポート・カプールで全部揃うけれど、他の街のトレイナーにも確認してみるといいぞ。", "Trainers in different towns will teach you different skills. The skills I just listed can all be learned from either Vernis just north of here, east-south-east in Yowyn, or west-north-west in Port Kapul. That's just for the ones I listed, so you should also have a look at trainers in other towns.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("例外もあるけれど、技能は主能力やスキルが一定値以上の時にレベルアップすると習得できるんだ。aキー…abilityから発動できる通常技能と、Ｗ(Shift+w)キー…Wideで発動できる広域技能がある。技能が増えてきたら、使わないものは*キーで非表示にしておくといいだろう。", "Special actions are abilities that you learn upon level up when certain attributes or skills are at certain levels. Most of them, anyway. Regular special 'a'ctions can be used with the 'a' key, and 'W'ide-area special actions with shift+w. When you have too many of them, you can temporarily hide some of them with the '*' key.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("うん！プラチナ硬貨は、主に依頼を達成した時に手に入るぞ。貰える枚数は依頼によって違うが、芸人依頼以外は名声１万とか２万とかごとに増えるからね。他にはネフィアを攻略したり、プラチナベルを倒すことでも手に入るけど、まぁ依頼報酬が基本かな。", "The main source of platinum pieces is from completing jobs. The number awarded depends on the type of job and on fame; you'll receive 1 more for every 10,000 or 20,000 fame depending on the job. Not for the performing jobs, though. Those only ever give 1 platinum piece. You can also get platinum pieces from clearing Nefia or killing platina bells, but not that much. It's mostly going to come from jobs.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("あー…それか。最初、イヅル・グンバイだからって「いづるん」というあだ名をつけられそうになってな。いい歳してそれは恥ずかしかったから、実家で呼ばれ慣れてる「師範」がいいと駄々をこねさせてもらった。", "Oh, uh, that. Some people were calling me I-dude, which is kind of strange given my age, so I started insisting on 'Instructor' because I'm used to getting called that at my parents' home.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 4 ) {
			listmax = 0
			buff = lang("仲間の能力を上げたいなら、ランクの高い料理をこまめに与えるといいな。ハーブもいいぞ！潜在能力を高めに保っていれば、食事で能力値２００くらいまではスクスク成長するだろう。", "Give your allies high-ranked cooked food to increase their attributes. That or herbs. If you keep their potentials high, then their attributes will skyrocket to 200 or so in no time at all.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("聴診器を使えばわかるんだが、青緑色の時は食事可能、桃色の時は満腹だ。腹がすいたらすぐに食わせることができるように、料理は多く持っておくんだ。足りなきゃ果物の木やパン屋から調達しような。", "When you've used a stethoscope on them, then you'll be able to tell when they're hungry. Blue-green for hungry, pink for full. Bring lots of cooked food so that you can feed them when hungry. When you run out, get more from fruit trees or bakers.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 5 ) {
			listmax = 0
			buff = lang("スキルの潜在能力を高く維持して、対応した行動をとらせよう。特に、戦闘スキルは交戦しないとなかなか伸びないからな。育てたい仲間が交戦する前に、敵を片っ端から先制攻撃で倒したりしないように。経験を積む機会をちゃんと与えてやるのも育成のうちだぞ。", "You'll have to keep their skill potentials high and make sure that they do things that will train the skills. To train combat skills, they'll have to fight. If you're mincing every single enemy before your allies even have a chance to attack, then they're not going to get any better at it. Part of training them is giving them the chance to earn experience.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("一応、旅歩きスキルを覚えさせていれば旅の中で見識を深め、少しずつ他のスキルも成長していくな。だけどあくまで補助的な経験でしかないから、旅歩きだけで上げようとするのは逆にしんどいぞ。色々な経験をさせてやれるよう、心がけてれば自然と育つもんさ。", "They do level skills with the Traveling skill, but the amount that they get from just this isn't going to be enough. If you're trying to level them up with only travelling, then it's going to be even harder than if you trained them the proper way. Just give them the relevant experience, pay them some attention, and they'll get better with time.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("仲間に新しいスキルを覚えさせる方法はまたの機会に説明するよ。それまでに、誰がどのスキルを持っているかをちゃんと把握しておこう。街の情報屋で教えてもらえるぞ。", "I'll explain how to teach your allies new skills some other time. In the meantime, make sure you know the skill sets of every one of your allies. To find out, you can go to the informers in towns.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 6 ) {
			listmax = 0
			buff = lang("潜在能力が低いままだと、頑張っても経験値が少ないから効率が悪いんだ。お金や宝石類を持たせれば、街で勝手に訓練して主能力とスキルの潜在を高めるぞ。余ってる場合は拾わせてあげよう。", "Potentials work the same way for your allies. They're not going to receive much experience if they're low no matter how hard they try. This is when you give them money or ores if you have any to spare. They'll use it to visit trainers in towns to increase their attribute and skill potentials.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("訓練に必要な費用は、当人の初期能力…ＩＮＩＴに応じて変わるんだ。ＩＮＩＴが低ければ低いほど、訓練費も少なくて済む。強く成長した後も初期能力依存だから増えない。むしろ魅力が高まればそこからさらに減るのさ。", "How much they need to train depends on their respective initial stats, the INI. The lower their INI, the cheaper the cost. This cost will never increase even after they get stronger, because the INI doesn't change. In fact, it will even decrease, as there's a discount based on their Charisma.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 7 ) {
			listmax = 0
			buff = lang("死んでしまった仲間は、街のバーテンダーが有料で蘇生してくれるぞ。レベルが高いほど蘇生費用も高くなるので、装備調整と回復はサボらないように。すぐ死ぬ仲間を蘇生させても、対策しなければまたすぐ死ぬだけだ。お金の無駄だぞ。", "Allies who have died can be revived for a fee at bartenders in towns. It'll get more expensive as their level goes up, so keep them equipped properly and healed up. If they're always quick to die and you don't do anything about it, you're just going to waste money resurrecting them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("復活の書か、復活の魔法があれば自分でも蘇生できるけど、まぁ前者はレアだし後者は使いこなせるようになるまでが大変だな。", "You can also do that yourself with a book of resurrection or the Resurrection spell, but the former is too rare and it's difficult to get the latter up to a level that makes it practical to use.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 8 ) {
			listmax = 0
			buff = lang("仲間に活躍させたり、褒めたり、特定の贈り物をするなどで友好度が上がる。友好度が高いと色々と判定が有利になるし、料理スキルがあれば朝食を、裁縫・大工スキルがあれば防具を作ってくれるようになったり、魂の友以上になると結婚もできるようになるんだ。媚薬をぶつけることでも上がるけれど、媚薬だけではお友達までにしかなれないから注意だぞ。", "Impress of your allies for you increases when they fight, with compliments, and by giving them certain gifts. High Impress is useful for many things. Also, in the morning, they'll make breakfast for you if they have the Cooking skill, and they'll make armour if they have the Tailoring or Carpentry skills. You can even marry them when at Soul Mate or closer. I suppose you could pelt them with love potions, but that will only get them up to Friend status.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("地面にあるものを我慢させ、もし手を出したら押しのけることで、主従度が上がる。いわばシツケだな。色々と従順になるが、友好度でほぼ代用できる。主人として接するのか、対等な立場として接するのか。そこをハッキリさせる、くらいの感覚でいいんじゃないかな。", "As for Servant/Master value, this increases when they refrain from items on the ground or if you push them off of it when they've started eating it. It basically measures how well they are disciplined. They'll become submissive and obedient as it goes higher, but there's nothing that you can do with it that you can't with Impress. In the end, it's really about whether you want to be their master, or to be their equal.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 9 ) {
			listmax = 0
			buff = lang("タッグを組ませると、ベースにした仲間が移動権を握って行動を共にする。もし二人の攻撃間合いが一致していれば、追随側は移動を省略して攻撃に専念できるわけだ。一致していないと足を引っ張り合うことになるけれどな。強制的に速度が遅いほうに合わせられるから、なるべく速度の近い組み合わせにするといい。", "When your allies form a tag a team, the ally you had interacted with will be in charge of movement. When both allies in the tag have the same preferred distance from a target, the other ally - the one on the right - can simply leave all the moving to the one on the left and focus on attacking. If they don't have the same preferred distance, they might end up getting in each other's way. They will also both move at the speed of the slowest ally in the tag, so try to team up the ones who have similar speeds.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("似たような戦闘タイプ…特に近接型を組ませて戦闘効率を上げるのは基本だな。庇いあうことで生存力が上がり、取得物が分配されたりするので弱い仲間を強い仲間と組ませて保護してもいいぞ。", "It's generally best to team up allies who fight in similar ways. By that I mean melee fighters with melee, and ranged with ranged. They will also take damage for each other and share their earnings, which means you can protect weaker allies by tagging them with stronger ones.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 10 ) {
			listmax = 0
			buff = lang("進化には特定のアイテムが必要なんだ。その仲間に対応しているかは使ってみないとわからないし、そもそも対応する進化アイテムがないやつも多い。進化アイテムは特定の敵が落とすぞ。友好度が高くないと進化を拒否されるから仲良くなっておこうな。", "You need certain items to evolve your allies. Pets. Whichever. Anyway, you won't know if you can use one on your ally until you actually try it out. Some allies also don't ever evolve. If their Impress isn't high enough they'll refuse it. These evolution items are dropped by certain enemies.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("進化すると単純に能力が上がったり、新たな技能を習得するケースもある。姿や名前が変わるけれど、扱い的には元のままで変わっていないから誤解しちゃだめだぞ。", "When they evolve, allies generally get their attributes increased. In some cases, they learn new special actions too. Their appearance and names will also change, but they other than that they're still the same.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 11 ) {
			listmax = 0
			buff = lang("あまり浸透していないけれど、仲間にスキルを覚えさせる方法としては能力合成っていうのがあるんだ。他の仲間を分解して能力を移植する、生化学文明時代の技術だな。仲間のレベル以上の生化学スキルが必要だ。確か、ポート・カプールの学者が個人レベルでも使えるように研究していたはずだぞ。", "Not many people know this, but there's a way of teaching your allies new skills called the gene machine. It's a biotechnology relic that breaks down an ally and transfers its abilities into another. I've heard that there's a biologist in Port Kapul who's been researching a method of gene engineering that's easy and simple enough for the average person to use, with the only prerequisite being a Gene Engineer level higher than their allies' character levels.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("能力を合成することで、素体からスキルや装備部位を移植できる。さらに、素体の主能力が上回っていれば最大で３種類＆１０上昇する。料理で上がらなくなる前に、生化学を鍛えてこちらに切り替えていこう。間違って強くしたい方を分解しないように、話しかけて大事な仲間に指定しておくのを忘れずに。あと、視界内にいないと能力が上昇したかどうかわからないから気をつけてくれよ。", "Through gene engineering, you can transfer new skills and body parts to another ally. If attributes are also greater, then up to 3 attributes will get increased by at most 10. Don't forget to designate allies you want as precious from the conversation window with them. After all, you don't want to accidentally break down someone. Another thing to note is that if you can't see an ally, you won't be notified when an attribute of theirs increases.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 12 ) {
			listmax = 0
			buff = lang("魔法は、夢で覚えるか、不思議な巻物で覚えるか、魔法書を解読して覚えるかだ。前者ふたつはランダムなのでおまけ程度に考えて、魔法書をメインにしたいな。いずれにしても、手に入れたストックを使い切るとその魔法を再補充する必要があるぞ。", "Spells are gained from dreams, scrolls of wonder, and reading spellbooks. The first two are completely random so don't depend on them. When you run out of stock for a spell, you'll have to get more before you can cast it again.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("魔法書には種類ごとに難易度があって、相応の読書スキルが必要なんだ。解読に失敗するとＭＰに大ダメージを受けたり魔物が召喚されたりすることもある。何度も失敗するレベルのものを強行したり、倒せない魔物が湧くと困る場所で読んだりしないように。", "Different spellbooks have different difficulties, and the Literacy skill required to read a spellbook increases with its difficulty. If you fail to read a spellbook, it can do nasty things like drain a whole bunch of your MP or summon monsters. Don't keep trying to read a spellbook that you've failed multiple times in a row already or read in a place where you really don't want monsters to spawn.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("解読で得られる魔法ストックの量は、暗記スキルで増えるから取っておこう。収穫と願いの魔法書以外は充填である程度使いまわすことができるし、一度解読したものは魔術士ギルドに入れば作家から予約して買えるぞ。", "Memorization skill increases the amount of stock from reading spellbooks, so you'll want to get it. Spellbooks aside from wish and harvest can also be recharged and hence reused for a bit. If you join the Mages' Guild, you can reserve spellbooks that you've successfully read before from their spell writer.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 13 ) {
			listmax = 0
			buff = lang("魔法は使うことで経験値が入り、レベルアップする。詠唱スキルが低いと詠唱成功率が微妙だから、積極的に唱えていこう。魔法にも潜在能力があって、cキーのリストから確認できるぞ。魔法の潜在能力は、スキルなどとは違い睡眠をとることで少しずつ回復していく。", "Spells receive experience and level up when you use them. Their casting success rate is going to be low while their level is low, so keep using them. Spells also have their own potentials, which you can see from the 'c'haracter sheet pages or from the spell list ('v'). Unlike skill and attribute potentials, you need to recover these by sleeping.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("街の魔術士にプラチナ硬貨を払えば、魔法実習ができる。実習ではストックを消費せずに複数の魔法に経験値が入るぞ。珍しい魔法や普段使っていない魔法も鍛えられるんだ。レベルアップすると詠唱成功率と効果が上昇するから最初に何回か受けておくのもいいだろう。ただし消費ＭＰもちょっと増えるから注意だ。", "You can attend magic practice sessions if you pay the wizard in towns some platinum pieces. Magic practice will give experience to a set of spells all at once at no stock cost. This will train even the rare spells and those spells that you normally never use. As spell levels increase, their casting success rate also increases, so you should definitely attend at least a few sessions of it. Be careful though about MP costs though. They increase together with the spell levels.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ちなみに、仲間の魔法は種類に関係なく一括管理されているぞ。魔法レベル＝詠唱スキルレベルに簡略化されているから、その仲間の詠唱スキルを上げていこう。範囲魔法を使う場合は、自分も仲間も魔力制御スキルが必須レベルなので忘れずに。", "By the way, spells for your allies don't work that way. The level of every spell they use is their Casting skill level, so you'll have to level up that skill for your magic-using allies. Note that if either you or your allies use area of effect spells, then Control Magic is a must for everyone of you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 14 ) {
			listmax = 0
			buff = lang("ＡＰっていうのは能力の覚醒に必要なポイントだ。一定の強さ以上の敵を倒すことで入手できるぞ。倒した時に何かがひらめくような感覚があるからわかるはずだ。", "AP is short for Awakening Points, points that you need to awaken to new powers. You get them by defeating enemies that are strong enough. You'll know when you receive AP if you experience something like a flash of inspiration after defeating something.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("入手には色々と細かい条件があるけれど、だいたいＩＮＩＴや速度が自分より高い相手が対象になるんだ。つまり、ＩＮＩＴが低い…最初は弱い者ほどＡＰが貯まりやすいってことだな。", "The actual requirements for gaining AP are a little complicated, but to put it simply what you need to do is defeat an enemy that has a higher INI and speed than you. So people who have low INI - those who were weak to start with - will gain AP a lot easier.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("噂ではＡＰを使って能力強化やスキル成長、特殊なフィート取得ができるって話だけれど…。少なくともノースティリスでそんなことができるという者は知らないぞ。もし本当なら、育成のために積極的に強い敵を倒してＡＰを入手していきたいな。", "I've heard that this AP can be used for improving attributes, skills and obtaining special feats... but there's no such person in North Tyris capable of doing that. If it's true, then it would make it worthwhile to seek out strong enemies to earn AP.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 15 ) {
			listmax = 0
			buff = lang("恥ずかしいから師範と呼んでほしい…！！", "Would you please call me Instructor? That name's kind of embarrassing...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あー…実家の道場はずっと南の方にあってね。有料だけど仲間を枠から外して預けることができる。冒険している間に少しずつ鍛えてもらえるし、有料で全員まとめて潜在を回復させたりもできる。宣伝じゃないけど、まぁ活用してみてくれ。", "...my family runs a doujou in the south. It's not free, but you can leave your allies there to free up a slot. They'll also train while you're off adventuring, and for another fee you can recover all the potentials of all allies there at once. I'm not trying to advertise here, but go have a look at it sometime.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) <= 102 ) {
			listmax = 0
			buff = lang("何か質問があったら聞きに来てくれ。", "If you have any more questions come see me.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( 0 ) {
			txt lang("ゼミフラグ＝" + gdata(GDATA_FLAG_TRAINING_COURSE_STATE) + " ", " ")
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 899 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) == 100 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("こんにちは！ミトリンこと、戦闘講座担当ミト・リディアンだよ。よろしくね。", "Hi guys, I'm Mitolyn a.k.a. Mito Lydian, the combat lecturer.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("戦闘は、生計を整えて道具をそろえて育成した先にあるものだから、他の先生たちの講座を先に体験してからのほうがスムーズかもね。", "I'll start of by saying that combat is something that you should be doing after getting all the items you need and trained yourself a little. So you're better off taking the other seminars first if you haven't already.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("そもそも、ある程度準備が整うまでは、街の討伐依頼すら危険だよ！無理な戦闘はしないでね。体力を一度に数割削ってくるような相手に出会ったら、返しで倒す自信がない限りすぐに逃げよう。手こずるような相手を複数相手にするのも避けようね。敵がうじゃうじゃいるところに突撃なんてもってのほかだよ。", "In fact, it's dangerous to even take on hunting jobs in towns if you aren't prepared well enough! Picking fights you can't win is a no-no. If you come across an enemy that takes a whole chunk out of your health in an instant, then you should run away immediately if you don't think you can match up to that. And don't try to take on multiple enemies if you already have trouble with just one of them. Also don't go charging into places filled with enemies.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				listmax = 0
				buff = lang("初心者だと、攻撃が全然当たらない相手や、ダメージをほとんど与えられない相手に、ムキになって殴りかかっている間に敵に囲まれたり体力を削られていくパターンもあるよ。少しずつでいいから相手の特性を覚えていって、優先順位を考えて動けるようになろうね。", "Some enemies keep evading your attacks. Or they might be really tough and your attacks do next to no damage. If you let your temper get to you after missing so many attacks and keep trying to hit it, you might find yourself surrounded before you know it. You'll always need to know which enemies to take out first, but to know that you'll need to remember the characteristics of every type of enemy, so start working on that.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("戦闘講座始めるよー。今日もがんばろうね！", "Alright, let's get started!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("んー…。まず、強い冒険者っていうのは能力値が高いって意味ではないの。能力値が１００あっても１０００あっても、能力値の劣る相手にしか勝てないんじゃ強いとは言えないよね。そんなの野生動物と変わらないよ。", "You know, strong adventurers don't just have high attributes. If they are only able to win against enemies that are weaker than them, then you can't really call that strong, even if their attributes are in the 100s or in the 1000s. That's be no better than what a wild animal is capable of.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("本当に強い冒険者は、あらゆる物の使い方を知っているわ。個々の要素の力を最大限に引き出して、自分の数倍の能力を持つ相手を安定して殺すの。勝てないほどの差があっても、相手の戦い方を見極めたうえで生還して次につなげ、最後には勝つんだよ。", "The truly strong adventurers are able to use all the resources available to them to their fullest potential to kill enemies that have several times their own attributes. Even if they aren't able to win, they would be able to fully understand how their opponent fights, survive, and then win the next time they meet.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				listmax = 0
				buff = lang("事前準備の段階からもう戦闘は始まっているんだよ。防御は固めず回復手段も逃走手段も用意せずに戦いに行くのは、冒険じゃなくて自殺行為だってことを覚えておいてね。", "Combat begins the moment preparations for it start. To go into a fight without proper equipment, healing methods, and some means of escaping is not adventuring. It's suicide, plain and simple.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("戦闘講座第３回…ともなると、そろそろみんなも自分の得意武器や戦闘スタイルが固まってきた頃じゃない？今日はメインウェポン以上に大切な、サブウェポンについて教えるね。", "This is our 3rd session... which means that you all should have more or less settled on a combat style and a weapon of choice by now. I'm going to talk about something that's even more important than your main weapon - your sub weapon.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("今はまだ単純に武器で殴ったり魔法の矢を撃ったりするだけでいいかもしれない。でも冒険を進めていくうちに、物理攻撃に強い敵や魔法に強い敵、属性攻撃を無効化する敵なんかと戦うことになるの。そうした時にサブウェポンとして別系統の攻撃手段がないと、有効打を与えられずに死ぬほど苦労することになるよ。なんだかこの敵全然削れないな？と思ったらサブウェポンを試してみよう。", "You might be doing well now just by clubbing enemies on the head with your weapon or firing Magic Darts. In your adventures, however, you will encounter enemies that are resistant to physical attacks or to spells, or immune to elemental attacks. It's times like these when you will need have a sub weapon as a different source of damage, or else you'll find it really hard to break the stalemate. When you find an enemy who isn't affected by your attacks, it's time to try your sub weapon.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("物理型なら武器のエンチャントで属性攻撃ができるから、得意スキルに依存する無属性攻撃技能をひとつくらいでいいよ。魔法使いなら攻撃魔法を魔法属性とそれ以外に好きな属性からひとつ選ぶ感じで。範囲攻撃魔法も使えるようになっておこう。", "Physical attackers can have weapons that come with equipment attributes that deal elemental damage, so they have that covered. What they do need is to find a special action that is based off a skill that they are good at. Spellcasters should know a magic-element attack spell and another one of an element of their choice, and also be able to use area of effect spells.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				listmax = 0
				buff = lang("攻撃手段が異なる仲間を複数そろえて、仲間自体をサブウェポンみたいに使うって手もあるね。相性の悪い相手の時は回復に専念することになっちゃうけど。とにかく、勝ち筋を増やしておいて損はないよ。", "You can also have allies who use different means of attacking and regard them as your sub weapons. Against enemies that they're bad against, however, you might find yourself having to heal them all the time. Anyway, just try to get more ways of securing victory, as you can never can have too many of those.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("みんなはこの先、冒険の中で本当に色々な敵と戦うことになるよ。単純なステータスや防御力や回避力だけじゃない、特殊な能力を持っていたりする敵も多いから、対策を知らないと苦戦するかも。", "You are all going to be fighting a very vast range of enemies in the future. They won't just have higher attributes, defence, or evasion; you'll also have to deal with those with special abilities. If you don't know how to counter these abilities, you're going to be in for a tough time.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				listmax = 0
				buff = lang("相手が『』《》つきじゃないなら、状態異常や変化の杖でどうにでも処理できるんだけど、ね。最後となる、第４回では具体的にそういう厄介な敵の対処法を教えるね。", "That said, you can always use rods of change creature or status ailments against enemies that don't have <> or {} brackets around their names to get rid of them. I'll be talking about how to fight these enemies in our last seminar next time.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE) = 101
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("上手く逃げるには？", "How do I run away?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("敵の強さを知るには？", "How do I know how strong an enemy is?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("どのくらいの難易度に挑めばいいの？", "What difficulty should I tackle?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("相手が速すぎる場合は？", "What if my opponent is too fast?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("防御を固めるって？", "How do I increase my defence?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang("回復手段って？", "How do I heal?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 6
			listn(0, listmax) = lang("状態異常の対策方法は？", "How do I protect against status ailments?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			list(0, listmax) = 7
			listn(0, listmax) = lang("ミトリンは好きな人いるの？", "Do you have someone you like Mitolyn?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 8
			listn(0, listmax) = lang("回避力が高い相手はどうすればいい？", "What do I do against opponents with high evasion?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 9
			listn(0, listmax) = lang("防御力が高い相手はどうすればいい？", "What do I do against opponents with high defence?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 10
			listn(0, listmax) = lang("回復力が高い相手はどうすればいい？", "What do I do against opponents who keep healing?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			list(0, listmax) = 11
			listn(0, listmax) = lang("アイテムを上手く投げるには？", "How can I get better at throwing items?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 12
			listn(0, listmax) = lang("カウンターしてくる敵の対策は？", "How do I deal with opponents who counterattack?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 13
			listn(0, listmax) = lang("自爆する敵の対策は？", "What do I do against suicide attacks?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 14
			listn(0, listmax) = lang("分裂する敵の対策は？", "How do I deal with enemies that split?")
			listmax++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			list(0, listmax) = 15
			listn(0, listmax) = lang("透明な敵の対策は？", "How do I deal with invisible enemies?")
			listmax++
		}
		buff = lang("質問があれば、何でも言ってね？", "Questions? Feel free to ask anything.")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("テレポートが基本だけど、巻物は盲目の時には読めないし、魔法は混乱の時や沈黙を受けている時には使えないよ。両方を用意しておくか、確実に杖を使えるようにしておこう。", "Mostly teleport, but you can't read scrolls while blind, nor use spells while confused or silenced. Make sure to have both of them or be able to use rods with a 100% success rate.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ただ、テレポートはどこに飛ぶかランダムだから死んじゃう寸前じゃなくて、余裕を持って使おう。ショートテレポートや空間歪曲はすぐ近くに移動する可能性が特に高いから、過信しないようにね。", "That said, you'll want to teleport with health to spare and not when you're almost dead. Teleporting takes you to a random location which could be near an enemy, especially for Short Teleport and Dimensional Move that have a limited range.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("敵との距離が離れている場合は、壁生成や扉生成でさえぎって追撃を防ぐのも有効だよ。モグラの魔物や、名前に『』や《》がつく変異種には破壊されちゃうけど。煙玉を投げて煙幕を張れば、遠距離攻撃のほとんどを無効化できるね。", "If an enemy is a distance away, you can use Wall Creation or Door Creation block their line of sight and attacks for a bit. Mole monsters and monster variants with <> and {} brackets around their names will break right through it though.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("＊キーで「見る」と、相手とのおおよそのレベル差がわかるよ。でも装備や戦い方で体感的な強さが大きく変わるから、本当に目安でしかないの。インサイトレンズっていう道具を使えば相手の能力値は確認できるけど、レアだから…。", "You can look at enemies with the '*' targeting key to get an estimate of the difference in level. This is just a rough estimate, though, since the actual difficulty of the fight can drastically change with your equipment and the way you fight. I suppose you could use an insight lens on the enemy to look at all their stats, but those are rare.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("攻撃方法とかは、実際に戦って覚えるしかないの。名前や見た目からある程度推測できる部分もあるよ。行動を把握する前に力任せに倒しちゃうと、後でパワーアップして出てきた時に痛い目を見るかも…？相手の力を見極めて知識として蓄積していくのも、戦闘では大事なことだよ。", "You're not going to know how they attack until you actually fight them. You could make a guess based on their name and their appearance though. If you don't get a good grasp of their abilities before defeating them, you might get a nasty surprise later on when a stronger version of them appears. It's important to learn and remember the different enemies and their strengths.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("最初の最初は、街の外…野外にEnterして、食料を拾いつつ野生動物と戦ってみよう。囲まれないように気をつけてね、死んじゃうよ。やどかりやかぶと虫を安定して倒せるようになってきたら、このゼミのすぐ北西にある「子犬の洞窟」に挑戦してみよう。底まで潜るのに必要な食料や補助アイテムの量を、感覚でつかんでおこうね。", "When you're starting off, press Enter in the wilderness outside of towns to pick up food and fight wild animals. Don't get surrounded, or you'll get dead. When you are able to easily handle hermit crabs and beetles, try out the Puppy Cave just north-east of this place. Get a grasp of the amount of food and other supplies that you'll need to get to the bottom floor there.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("子犬の洞窟を楽々探索できるようになったら、その辺の手ごろなネフィアに挑戦してみて。冒険者として、自分がどの辺りまで潜れる力があるのかは定期的に把握しておこう。準備万端ならそうそう死にはしないし、きつかったら撤退して、挑む階層を少し下げればいいからね。", "When you are able to breeze through it, then you can go for one of the Nefia around here that aren't of too high a level. As an adventurer, you should always have a grasp on the danger level that you are able to handle. You shouldn't die too easily if you're fully prepared. If you find it too difficult, then get it out of there and try one with a lower danger level.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("覚えておいてほしいのは、ネフィアの入口の階層レベルは挑戦推奨レベルとは違うってこと。今まで倒せた敵のレベルがｃキーのキャラシートに適正値として記録されているから、そっちを目安にしよう。", "One thing you should know is that the danger level of the first floor of Nefia is not the level at which you should challenge the Nefia. What you should use as a reference is the Rating on the character sheet which has the highest level enemy you've defeated so far.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("一定以上の階層じゃないと出ないアイテムやマテリアルもあるから…積極的に深層を目指して冒険者として成長していこうね！", "Some items and materials only appear in floors with a certain danger level or greater, so you should always aim to dive deeper and grow as an adventurer!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("主能力の速度が行動回数に影響しているの。この差が大きいと敵に連続で行動されちゃう。攻撃が激しくなるのは当然として、行動ターンが多く回ってくるから体力の自然回復も増えるんだよ。加速や鈍足、乗馬を使って差を埋めようね。", "The Speed attribute affects how many times you can move. If the difference in speed between you and the enemy is too great, you might find them moving several times between every turn of yours. When that happens, you're going to take a lot more hits. On top of that, their HP is also going to regenerate a lot more because they get more turns to do so. You'll have to bridge that difference with Speed or Slow potions, spells, or rods, or by riding an ally.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ただ、行動ターンが多く回ってくるってことは毒や出血、地面の酸でもりもり削られるってことでもあるの。速いけど体力の少ない敵は、染料や毒薬、栗なんかをぶつければあっけなく倒せたりするから試してみてね！", "However, being able to take more turns means that they'll get damaged more often by poison, bleeding, or by puddles of acid on the ground. Enemies that are fast but don't have much health tend to die quickly when you throw a dye, poison, or chestnut at them so be sure to try that out!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 4 ) {
			listmax = 0
			buff = lang("物理攻撃に対する強さとして、回避力のＤＶと防御力のＰＶがあるね。回避力を捨てて防御力だけ上げるか、回避力を上げて最低限の防御力は確保するか選ぼう。全ての物理攻撃を完全回避するのはむずかしいから、防御力を捨てる選択肢はないかなぁ。聖なる盾やフェザーで補ってもいいけどね。", "Evasion - DV - and defence - PV - will protect against physical attacks. You'll need to pick between neglecting evasion to focus on defence, or have a bare minimum amount of defence while focusing on evasion. Evading every single attack is nigh impossible, so having absolutely no defence is not an option. You can buff yourself with Holy Shield or Feather too to increase these.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あとは、各属性の耐性も少しずつ上げていこう。全く耐性のない属性攻撃は致命傷になるよ。火炎と冷気に至っては、耐性が低いと属性攻撃で持ち物を失うことがあるの。ダメージは減らせないけど、耐火ブランケットと耐冷ブランケットを持っておけば回数分持ち物を守ってくれるよ。余裕があれば仲間にも持たせてあげてね。", "You should also increase your resistances against every elements. Not having resistances against an element can be fatal. Without decent fire and cold resistances, you'll also lose your items sometimes. If you don't have those resistances yet, fireproof and coldproof blankets can protect your items for a few times even if they don't reduce the damage done to you. Oh, and give some blankets to your allies too if you have any to spare.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("最終的には全部の耐性を素晴らしい耐性まで上げたいけれど、揃うまでは属性耐性のポーションや魔法で補ったり、複数の装備を付け替えて対応することも考えようね。", "You'll eventually want to have Superb resistance to every element, but until then you should buff yourself with spells or potions of resistance, and keep swapping your equipment as needed.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 5 ) {
			listmax = 0
			buff = lang("実用的な回復手段をどれだけ用意できるか…これも重要だね。最低でも一度にＨＰを半分以上回復させる手段がないと、思わぬダメージを受けた時に立て直すのが難しいよ。杖、魔法、ポーション、特定の技能。それぞれ一長一短があるからこのうち２つ以上は使いこなせるようにしておこう。巻物は…回復量が伸びないから最初のうちだけかな。", "It's important to have multiple ways of healing your health by a practical amount. What I mean by practical is that it should recover at least half of your maximum HP per use. If it doesn't, then you'll be less likely to be able to out-heal damage and become unable to go on the offensive. There are several methods of healing yourself - rods, spells, potions, and certain special actions to name a few. Each of them have their own pros and cons so you'll want to have at least 2 to you can rely on. As for scrolls... the amount that they heal never increases, so they're only going to be of use early on.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("残りＨＰの管理は死なないための基本だよ。仲間にはちゃんと聴診器を使っておこうね。コールファミリアという技能を使えば仲間を退避させることもできるよ。", "Keeping an eye on the amount of HP you have left is critical for survival. For allies, use a stethoscope on them. If you have the Call Familiar special action, you can pull them out of the fray when they're in a pinch.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ＭＰの回復は魔力の杖でいいとして…ＳＰの急激な回復は難しいから消耗に気をつけてね。リジェネレーションで自動回復量を上げることも考えよう。最後の手段として、祈りがチャージされていればpキー…pray(祈る)でＨＰ・ＭＰ・ＳＰを全回復できるから神への捧げものはしておこうね。", "For recovering MP, you can use rods of mana. Recovering SP is not easy though. For that you'll have to use Regeneration to increase the rate at which it regenerates. As a last resort, you can 'p'ray if your prayer charge is filled to fully recover HP, MP, and SP, so keep offering stuff to your god.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 6 ) {
			listmax = 0
			buff = lang("状態異常は色々あるけれど、戦闘においては属性攻撃で発症したりするね。耐性が低かったり何度も受けると、長引いたり効果が増したりするの。だいたいはターン経過で消えるけど、体力回復手段を使うと副次効果で短くなるよ。", "There are various status ailments, some of them are caused by elemental damage received from enemies. The lower your resistance against that element and the more you get hit, the longer the duration of the status ailment. Most of them will go away after some turns have passed, but you can also shorten the duration by healing yourself.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("無効化するエンチャントのついた装備を身に着けるのが理想的だけど、それまでは無効効果のあるバフをあらかじめかけておくくらいかなぁ。麻痺や睡眠は行動不能になるし、盲目は何も見えなくなるし、混乱時に魔法を使うと自滅するし、毒や出血は放っておくと死にかけるし、どれも危険だよ。", "It's best to have equipment that negates status ailments, but if you don't have those you can make yourself more resistant to them with buffs. That's because all status ailments are dangerous. Paralysis and sleep prevent you from moving, you won't be able to see anything while blinded, trying to cast spells while confused will kill you, and poison and bleeding can bring you down to critical health if you're not careful.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("逆に、相手に麻痺や盲目をかけることができればかなり有利に戦えるってことだよね。『』や《》のついている相手にはほぼ効かないけど、効く相手には高確率で効くから、無力化したい相手に状態異常ポーションをぶつけたりしてみよう。", "They're effective against enemies too. Inflicting paralysis or blind on enemies can turn many battles in your favour. That said, enemies with <> or {} brackets around their names are highly resistant to status ailments. For all other enemies, if they're not resistant a status ailment they'll get afflicted by it easily, so toss some status ailment potions their way.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 7 ) {
			listmax = 0
			buff = lang("へっ！？", "Huh?!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…。", "...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ひ、秘密ぅ…。", "That's... a secret...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 8 ) {
			listmax = 0
			buff = lang("種族特性で回避力が元から高いモンスターがいるんだよね。わかりやすいのだと、コウモリとか。魔法は必中だから魔法使いだとあんまり問題ないんだけどね。", "Some races of monsters inherently have high evasion. Bats, for example, as I'm sure you would know from now. Spells always hit so this isn't a problem that spellcasters face.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("会心の一撃が出れば物理攻撃も必中だけど、出るまで殴り続けるのも大変だよ。通常攻撃を介さないタイプの攻撃技能は必中だから試してみてね。他には…睡眠状態にすれば避けられないよ。打たれ弱い敵なら、下手に殴るより重い物をぶつけたほうが早いかもよ？", "When a physical attack is a critical hit, it's guaranteed to hit, but that would mean you would have to keep trying to hit it to get a critical. What you should use instead are special actions independent of your regular attacks, as these are guaranteed to hit. Or you could also put them to sleep, because they won't be able to evade that way. Throwing heavy objects at fragile enemies might also be more efficient than trying to hit them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 9 ) {
			listmax = 0
			buff = lang("種族特性で防御力が元から高いモンスターがいるんだよね。わかりやすいのだと、やどかりとか。魔法は防御無視だから魔法使いだとあんまり問題ないんだけどね。", "Some monster races inherently have high defence. Like the hermit crab, for example. Once again, the mages here will be happy to know that spells completely ignore defence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("完全貫通が出れば防御力の大半を無視できるけれど、装備に完全貫通エンチャントがないとまず出ないよ。通常攻撃を介さないタイプの攻撃技能の中には、防御無視のもあるから試してみてね。防御力を下げる魔法やアイテムも使おう。", "Absolute piercing attacks will ignore most of the target's defence, but you'll need to have equipment with an attribute that gives it. Some special actions that don't rely on regular attack power ignore defence, so you'll need to rely on these instead. You could also lower their defence with spells or items.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 10 ) {
			listmax = 0
			buff = lang("回復行動を連打する、倒すのが困難な相手とも戦うことがあるかもね。ちょっとやそっと火力を上げたくらいじゃ突破できないけれど、だったら瞬間火力を大幅に上げればいいの。腐ったトマトをぶつけたり、一部の技能を使うことで激怒状態になるよ。激怒状態では与えるダメージも受けるダメージも２倍…あとはわかるかな？", "When you fight a strong enemy, you might find yourself healing all the time and unable to defeat it. For situations like these, just increasing your attack by a little isn't going to make a difference. What you should do is to increase your spike damage. Like by inflicting Fury status by throwing a rotten tomato or with a special action. While under Fury status, damage dealt and received is doubled. Get what I mean?")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あとは相手にターンを回さないのも有効だね。時を止めている間に準備をしたり、麻痺が入った瞬間を狙って畳みかけよう。『』《》つきにも鈍足は低確率で入るから、速度差をつけて行動回数を増やしてもいいね。", "You could also make sure that your opponent doesn't get a turn. You could stop time to buy time for preparations, or attack them while they are Paralyzed. Slow still has a low chance of working on enemies with <> or {} brackets around their names, so if you can manage to make it work on them you can put yourself ahead in Speed, giving you more chances to move.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ＨＰ下のパワーゲージが貯まると、技能の【力の解放】で必中・防御無視のゲージ技が出せるのは知ってるかな？そして、突撃体勢の仲間はゲージ技を使うけれど、迎撃体勢にすると使わない。…つまり、迎撃で仲間にゲージを貯めさせてタイミングよくコマンドフラッグで切り替えれば、全員でゲージ技を連続して叩き込めるんだよ。付与効果がないけど【合体攻撃発動】でもいいかな。", "When that power gauge under your HP bar fills up, you can use the <Gauge Release> special action to pull off the gauge attack that always hits and ignores defence. Allies who are set to Onslaught will use their gauge attacks, but those set to Intercept won't. So if you set all of them to Intercept and fill up their gauge bars, you can switch them all to Onslaught at once with a command flag to have them all unleash it at once. Or you can use <Unison Impact> if all you need is pure damage and nothing else.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("最後に、持久戦になるけどマナを吸収する武器で攻撃し続けて、マナの反動で大ダメージを与えるって手もあるね。今挙げたのはどれも同時に行えるから、ギリギリまで削ってから重複させれば突破できると思うよ。ただし、事前に準備していないとできない戦法だってことは忘れないでね？", "Lastly, you can use weapons that absorb the enemy's MP for long battles. With these, you can deal a large amount of damage through magic reaction. If you try all of those tactics I've outlined at the right time, you should be able to break an impasse. Keep in mind that they all require preparation beforehand though.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 11 ) {
			listmax = 0
			buff = lang("バックパック内の物をＴキー（shift+t）…Throwで投げつける場合は、投擲スキルを鍛えておこうね。ポーションやモンスターボール、投擲アイテムを外さずにぶつけられるよ。距離が遠いほど外れやすいから、投擲が低い間はなるべく至近距離から投げよう。", "If you want to 'T'hrow items - that's shift+T - you should train your Throwing skill. If you don't, everything from potions to monster balls will miss the opponent. The further you are from the target, the more you will miss, so stand as close as you can to the enemy when throwing.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("重量物をぶつける攻撃は、必中＆防御無視だから意外なところで役に立つかもね。基本は重いものほどダメージが大きいけれど、それとは別に出血させる栗や属性ダメージを与えるアイテムなんかもあるよ。", "Throwing heavy objects at an enemy is considered as an attack that always hits and ignores defence, so you might find it surprisingly useful. In general, the heavier the thrown object, the greater the damage, but there are certain objects like chestnuts that deal additional bleed or elemental damage.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 12 ) {
			listmax = 0
			buff = lang("物理攻撃した時に棘が刺さるタイプの敵は、もちろん遠距離から攻撃すればオーケーだよ。通常攻撃を介さない攻撃技能でも大丈夫。魔法を反射してくる敵は魔法の属性まで反射してくるから、自分の耐性が低かったり状態異常を無効にできない属性の魔法で攻撃するのはやめておこう。", "Enemies that cause you to get struck by thorns when you melee attack them should be attacked from range, of course. You could also use special actions that don't rely on your attack power, as those won't get countered either. As for enemies that reflect spells, they will bounce that exact same element back at you, so don't use elements that you don't have a high resistance for. Also don't use it if you aren't immune to the status ailment the element causes.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それから、射撃に対して先制射撃でカウンターしてくる敵もいるよ。これは状態異常で射撃できないようにしちゃえば大丈夫！…どれも１発１発のカウンターダメージは大したものじゃないけれど、連続攻撃するのはやめておこうね。", "There are also enemies that will preemptively counter ranged attacks with one of their own. These you can stop by making them unable to shoot, like with status ailments. These counter attacks don't deal much damage, but you still really shouldn't keep trying to shoot them.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 13 ) {
			listmax = 0
			buff = lang("隣接した時に自爆する敵と、ダメージを受けた時に自爆する敵がいるよ。爆弾岩はその両方を兼ね備えているね。遠距離から先制攻撃で仕留めるか、それができないならポーションを投げたり水魔法を使って濡らそう。濡れていると自爆できないからね。", "There are two different types of suicide attackers. There are those that blow themselves up when standing beside their target, and those that do so when they take damage. Bomb rocks have both of these traits. You'll need to dispatch them from a distance, or make them wet by throwing potions or with water spells. They can't blow up when wet.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 14 ) {
			listmax = 0
			buff = lang("状態異常の時でも分裂する敵と、状態異常の時は分裂しない敵がいるよ。どちらにしてもある程度大きいダメージを与えると分裂するから、一撃で削り取れない場合は、先にスリップダメージで削ろう。それが面倒なら、その敵の周囲を壁生成で囲っちゃおう。スペースがないと分裂できないからね。", "Of the enemies that split, some of them are able to do so even when afflicted with a status ailment, but others can't. What they all have in common is that they split when they receive a large amount of damage. So if you can't kill them with one hit, then you should try to lower their health with damage-over-time. If that's too much trouble for you, you can just use Wall Creation to box up that enemy. They can't split if there's no space for them to do so.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("分裂体は何も装備していないし何もドロップしない、そして経験値も得られないから、わざわざ分裂させる価値はほぼないかな。さっさと本体を片づけちゃおう。", "Enemies resulting from splitting have nothing equipped and will drop nothing. They also give no experience, so there's no merit to letting it split. Just get rid of them quickly.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( chatval == 15 ) {
			listmax = 0
			buff = lang("最終的には透明視を可能にするエンチャントのついた装備が欲しいね。それまでは、何かいると感じたら物質感知の巻物・魔法を使って見つけよう。後手に回っちゃうけどね。", "You will eventually want to have equipment that allows you to see invisible creatures. If you don't have that yet, then you can use scrolls of detect objects or the spell version of it when you think there's something there. You'll lose a turn by using those though.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("他には、濡れ状態にすれば透明が解除されるよ。水魔法とかで辺りに水を撒き散らせるなら、やってみよう。", "Invisible enemies also become visible when they are wet, so you can also use water spells or just create puddles everywhere.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_TRAINING_COURSE_STATE)++
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) <= 102 ) {
			listmax = 0
			buff = lang("質問がある人は聞きに来てねー！", "Come on up if you have any more questions!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( 0 ) {
			txt lang("ゼミフラグ＝" + gdata(GDATA_FLAG_TRAINING_COURSE_STATE) + " ", " ")
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 900 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_1) + gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_2) + gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_3) + gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_4) + gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_1) + gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_2) + gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_3) + gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_4) + gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_1) + gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_2) + gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_3) + gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_4) + gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_1) + gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_2) + gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_3) + gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_4) == 1600 ) {
			listmax = 0
			buff = lang("！！", "!!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("全ての講座を修了したキミには、赤剣先生の問題を…昔なら受けてもらうところなんだけどね。", "I see you've finished all the seminars! This is when I'd give students a little something extra to work on... but we don't do that any more.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("はい、これ努力賞。これからも頑張ってね！", "Anyway, here's a participation award. Good luck out there!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_CROWN_POINT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_COMPLETE1
			gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_4) = 1000
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) != 0 ) {
			listmax = 0
			buff = lang("一度にたくさん覚えるのは大変だよね？だから同じ人が受講できるのは一日に一回までなんだよ。今日教わったことをさっそく活かしてみよう！", "It's going to be really tough to remember all that new stuff, you know. So we limit participation to 1 seminar per day. You should try applying what you've learnt before attending the next one!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("冒険ゼミで、キミも最高の冒険生活を始めてみない？", "How 'bout it? Want to learn how to become a high-rolling adventurer?")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("結構です", "No thanks.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("冒険ゼミ・生活講座", "Survival seminars")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("冒険ゼミ・道具講座", "Tool seminars")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("冒険ゼミ・育成講座", "Training seminars")
		listmax++
		list(0, listmax) = 4
		listn(0, listmax) = lang("冒険ゼミ・戦闘講座", "Combat seminars")
		listmax++
		buff = lang("キミに合った講座を選んで、華麗にスタートダッシュを決めよう！", "Pick a seminar to give yourself a leg up on adventuring today!")
		gosub *chat_select
		if ( chatval > 0 & chatval <= 4 ) {
			gdata(GDATA_FLAG_TRAINING_COURSE_ID) = chatval
			gp1 = 200
			gp2 = 800
			gp3 = 1800
			gp4 = 3200
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 1 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_1) >= 100 ) {
					gp1 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_2) >= 100 ) {
					gp2 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_3) >= 100 ) {
					gp3 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_4) >= 100 ) {
					gp4 = 50
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 2 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_1) >= 100 ) {
					gp1 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_2) >= 100 ) {
					gp2 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_3) >= 100 ) {
					gp3 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_4) >= 100 ) {
					gp4 = 50
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 3 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_1) >= 100 ) {
					gp1 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_2) >= 100 ) {
					gp2 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_3) >= 100 ) {
					gp3 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_4) >= 100 ) {
					gp4 = 50
				}
			}
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 4 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_1) >= 100 ) {
					gp1 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_2) >= 100 ) {
					gp2 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_3) >= 100 ) {
					gp3 = 50
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_4) >= 100 ) {
					gp4 = 50
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やっぱり受けない", "I've changed my mind.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("初回 （" + gp1 + "gold）", "Part.1 / " + gp1 + "gold")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("第2回（" + gp2 + "gold）必要レベル2", "Part.2 / " + gp2 + "gold")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("第3回（" + gp3 + "gold）必要レベル4", "Part.3 / " + gp3 + "gold")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("第4回（" + gp4 + "gold）必要レベル6", "Part.4 / " + gp4 + "gold")
			listmax++
			buff = lang("キミの受けたい講座はどれかな？順番に受けるのがオススメ！", "Which part do you want? It's best to go through all of them one by one, yah?")
			gosub *chat_select
			if ( chatval > 0 & chatval <= 4 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) = chatval
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
					gp0 = gp1
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
					gp0 = gp2
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
					gp0 = gp3
				}
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
					gp0 = gp4
				}
				if ( cdata(CDATA_LEVEL, CHARA_PLAYER) < gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) * 2 - 2 | cdata(CDATA_GOLD, CHARA_PLAYER) < gp0 ) {
					listmax = 0
					buff = lang("あれ？条件を満たしていないね。よく確認しよう！", "Looks like you don't got the prerequisite for that yet!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
					snd SOUNDLIST_FAIL1
					txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
					goto *chat_end
				}
				listmax = 0
				buff = lang("しっかり勉強してきてね！聞き流したらお金がムダになっちゃうぞ！", "Work hard, mmkay? If you doze off in there it'll be a waste of your money, ya?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_TRAINING_COURSE_STATE) = 100
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= gp0
				mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
				gdata(GDATA_TELEPORT_AREA) = AREA_SEMINAR
				gdata(GDATA_TELEPORT_LEVEL) = gdata(GDATA_FLAG_TRAINING_COURSE_ID) + 1
				levelexitby = MAP_EXIT_TELEPORT
				gdata(GDATA_LEVEL_START_ON) = 7
				mapstartx = 11
				mapstarty = 6
				autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
				chatteleport = 1
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 902 | _switch_sw ) {
		_switch_sw = 0
		karata = 0
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 1 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_1) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 1
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 1 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_2) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 2
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 1 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_3) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 3
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 1 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_4) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 4
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 2 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_1) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 5
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 2 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_2) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 6
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 2 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_3) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 7
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 2 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_4) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 8
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 3 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_1) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 9
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 3 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_2) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 10
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 3 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_3) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 11
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 3 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_4) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 12
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 4 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_1) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 13
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 4 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_2) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 104 ) {
						karata = 14
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 4 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_3) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 15
					}
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_ID) == 4 ) {
			if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
				if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_4) == 0 ) {
					if ( gdata(GDATA_FLAG_TRAINING_COURSE_STATE) >= 105 ) {
						karata = 16
					}
				}
			}
		}
		if ( karata > 0 ) {
			listmax = 0
			buff = lang("キミはとても勉強熱心だから！ささやかながら努力賞をあげちゃうね！", "Wowie! A diligent student! I can't help but give you a little something for that...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			if ( karata == 1 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_1) = 100
				flt
				itemcreate -1, ITEM_ID_PORTABLE_COOKING_TOOL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
				flt
				itemcreate -1, ITEM_ID_HEALTHY_LEAF, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 2 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_2) = 100
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			}
			if ( karata == 3 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_3) = 100
				flt
				itemcreate -1, ITEM_ID_SCROLL_FAITH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
			}
			if ( karata == 4 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_DAILY_LIFE_4) = 100
				flt
				itemcreate -1, ITEM_ID_CURARIA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 5 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_1) = 100
				flt
				itemcreate -1, ITEM_ID_SCROLL_IDENTIFY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 6 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_2) = 100
				flt
				itemcreate -1, ITEM_ID_POTION_VITALITY, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
			}
			if ( karata == 7 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_3) = 100
				flt
				itemcreate -1, ITEM_ID_FIREPROOF_LIQUID, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 8 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_ITEM_4) = 100
				flt
				itemcreate -1, ITEM_ID_SCROLL_GAIN_MATERIAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 9 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_1) = 100
				flt
				itemcreate -1, ITEM_ID_PLATINUM_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			}
			if ( karata == 10 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_2) = 100
				flt
				itemcreate -1, ITEM_ID_RAW_RUBYNUS_ORE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
			}
			if ( karata == 11 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_3) = 100
				flt
				itemcreate -1, ITEM_ID_LOVE_POTION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
			}
			if ( karata == 12 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_TRAINING_4) = 100
				flt
				itemcreate -1, ITEM_ID_SCROLL_WONDER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
			}
			if ( karata == 13 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_1) = 100
				flt
				itemcreate -1, ITEM_ID_SCROLL_TELEPORTATION, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 14 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_2) = 100
				flt
				itemcreate -1, ITEM_ID_POTION_RESISTANCE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 15 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_3) = 100
				flt
				itemcreate -1, ITEM_ID_MAGISTUS, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_PARAM2, ci) = 15
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( karata == 16 ) {
				gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_4) = 100
				flt
				itemcreate -1, ITEM_ID_ROD_CHANGE_CREATURE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_BLESSED
			}
			item_identify ci, ITEM_KNOWN_FULL
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			goto *chat_end
		}
		listmax = 0
		buff = lang("う〜ん…。勉強熱心な子には、つい努力賞をあげたくなっちゃうカラ…。", "Mmm... can't help myself but give a little something to a diligent student...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("それはさておき。", "But anyways.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 1 ) {
			listmax = 0
			buff = lang("カラ太は宝箱のツクモガミなのだから！宝箱に擬態しているミミックとは、似て非なるモノなの！", "I'm not actually a mimic, ya know! I'm an object come to life. Com. Pleeeetely different from those mimics that pretend to be a chest!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("昔はタヌキとかライオンとか、マスコットの先輩もいたんだけど、みんな退職しちゃった…。", "There used to other mascots here like the raccoon and the lion, but they've all retired, so it's just me now...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 2 ) {
			listmax = 0
			buff = lang("冒険ゼミはバネッサコーポレーションという会社の一部なのだから！ちなみに、カラ太をスカウトしたのは創始者のバネッサなんだー。", "This adventurer seminar is part of the Vanessa Corporation! In fact, the one who scouted me was Vanessa the founder herself... ")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 3 ) {
			listmax = 0
			buff = lang("ゼミ創始者のバネッサはさらなる冒険を求め、友達のガイドを連れて宇宙に旅立ったのだから！いつ帰ってくるのかな？寂しい…。", "But she's gone off together with the guide friend of hers in search of more adventurers in outer space! I wonder when she'll come back. I miss her...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( gdata(GDATA_FLAG_TRAINING_COURSE_LEVEL) == 4 ) {
			listmax = 0
			buff = lang("バネッサコーポレーションは冒険者にお給料が払われるように国と交渉したり、ギルドのスポンサーもやっていたりするから！冒険者の地位を上げるために色々がんばっているんだー。", "The Vanessa Corporation is the one that negotiates with countries so that adventurers get paid, and they're also a sponsor for the guild! They're always working in the interest of adventurers to improve their standing...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 207 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) > 10 ) {
			listmax = 0
			buff = lang("これ以降はまだ準備中ですじゃ。今のうちに集めておいてほしいですのう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) >= 1 & gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) <= 10 ) {
			item_find ITEM_ID_DECK, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat == (-1) ) {
				listmax = 0
				buff = lang("おや、デッキを持っていないようですのう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			syurui = 0
			repeat 1500
				if ( card(0, cnt) == 0 ) {
					continue
				}
				syurui++
			loop
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 1 ) {
				listmax = 0
				buff = lang("10年20年などあっという間に過ぎるものですじゃ。この家も、越してきたばかりの頃は店だったのですよ。棚の奥に50年以上前のお菓子も入っておるでしょうなぁ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 2 ) {
				listmax = 0
				buff = lang("赤き義眼の魔女を知っておりますか。歳は大きく違えど、あれはわたしの知人なのですじゃ。愛する夫と末永く過ごすため、力を代償とする義眼まで埋め込むとはいやはや。若いとはいいものですのう。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 3 ) {
				listmax = 0
				buff = lang("こうしていると思い出しますのう。昔、ブラッドランナーという方がよく来てくださったのですじゃ。騎士見習いとして王宮に勤めることになったと言っておりましたが、その後どうなったか…おっと、あれは以前の文明の出来事でしたなぁ。時が過ぎるのは早いものですじゃ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 4 ) {
				listmax = 0
				buff = lang("ついこの間まではエウダーナという国に住んでおりました。弟子もおったのですよ。ええ、森の中で行き倒れていた孤児でしたのう。研究方面はからっきしでしたが戦闘魔法の才能は抜群でした。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 5 ) {
				listmax = 0
				buff = lang("わたしの弟子ですがねぇ、成長後は軍に入ってしまって。戦争の道具にされてしまったのですじゃ。他国の兵士を魔法で次々に蹂躙し新たな孤児を生む弟子の姿を見て、わたしは幻滅し国を去りました。弟子がその後どうなったのかは知りませぬ。生きていれば今頃わたしのような老いぼれでしょうなぁ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 6 ) {
				listmax = 0
				buff = lang("わたしが研究をしているのは下らない戦争のためではなく、ましてや誰かを助けるためでもありませんのじゃ。全ては可能性の追求のため。ひひひ…そのためなら他人の定めた倫理や禁忌など邪魔なだけですじゃ。そうは思いませんかのう？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 7 ) {
				listmax = 0
				buff = lang("その昔、海辺で暮らしていた頃の話ですがね。色々あって子亀を助けたら、海のお姫様に会うことになったのですじゃ。数万年ごとに子供の姿へ戻ることを繰り返し、悠久の時を生きてきたと言っておりましたなぁ。本当だとしたらまるでクラゲの一種ですのう。今も海の底で暮らしておるのでしょうか。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 8 ) {
				listmax = 0
				buff = lang("生化学文明の滅亡後は酷かったですのう。世界中が、変異したモンスターで溢れ、無法地帯となっておりました。わずかに生き残った普通の生物も、汚染された環境に適応できず次々と死んでいきましたねぇ。てっきり世界は終わってしまったものと考えておりました。まさか長い年月をかけて、様々な種族が汚染に適応した超生命となり、新たな文明まで築き上げるとは…驚きですじゃ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 9 ) {
				listmax = 0
				buff = lang("滅亡時期に活動していたネクロマンサーたちも今ではすっかり影を潜めておりますねぇ。それなりの数が生き残っているはずですがのう。いえ、彼らの偵察用アンデッドがちょくちょく研究を邪魔しにきて迷惑でしたがねぇ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 10 ) {
				listmax = 0
				buff = lang("シエラ・テールは過去の技術が一堂に会した奇妙な文明ですじゃ。神によって魔法が再び世界に広まり体系化され、錬金術が発達し、遺跡では機械群が大量に発掘され再起動までしております。そしてレム・イドがもたらした生化学技術。何かの意図を感じますのう。ひひっ…大きなことが起きる前触れかもしれませんなぁ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( syurui >= gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) * 50 ) {
				listmax = 0
				buff = lang("おや、カードを持ってきてくれたのですか？こんな婆のために、有難いことですなぁ。では少し拝見させて頂きますのじゃ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				redraw 0
				gosub *screen_draw
				gosub *screen_fade
				gsel BUFFER_BUF
				pos 0, 0
				picload exedir + "graphic\\face2" + devfile + ".bmp", 1
				gsel BUFFER_SCREEN
				listmax = 0
				buff = lang("ひぃーっひゃっひゃっ…！これはお礼ですじゃ。受け取ってくだされ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				flt
				itemcreate -1, ITEM_ID_KNEADED_SWEETS_OF_WITCH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD)++
				txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 2 ) {
					flt
					itemcreate -1, ITEM_ID_INSIGHT_LENS, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 10
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 3 ) {
					flt
					itemcreate -1, ITEM_ID_POTION_POTENTIAL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 7
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 4 ) {
					flt
					itemcreate -1, ITEM_ID_ASTRAL_LIGHT_PEN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 5 ) {
					flt
					itemcreate -1, ITEM_ID_POTION_SOMA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 6 ) {
					flt
					itemcreate -1, ITEM_ID_POTION_NEKTAR, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 7 ) {
					flt
					itemcreate -1, ITEM_ID_POTION_DISASTER, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 8 ) {
					flt
					itemcreate -1, ITEM_ID_POTION_AQUA_SANCTIO, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 8
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 9 ) {
					flt
					itemcreate -1, ITEM_ID_FLYING_SCROLL, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 10 ) {
					flt
					itemcreate -1, ITEM_ID_BOTTLE_HERMES_BLOOD, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 11 ) {
					flt
					itemcreate -1, ITEM_ID_TREASURE_MAP, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 5
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 12 ) {
					flt
					itemcreate -1, ITEM_ID_SCROLL_GROWTH, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 4
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 13 ) {
					flt
					itemcreate -1, ITEM_ID_MAGIC_FRUIT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 14 ) {
					flt
					itemcreate -1, ITEM_ID_HERO_CHEESE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 15 ) {
					flt
					itemcreate -1, ITEM_ID_POTION_DESCENT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 3
				}
				if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 16 ) {
					flt
					itemcreate -1, ITEM_ID_HAPPY_APPLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 2
				}
				listmax = 0
				buff = lang("いひひ…次は" + gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) * 50 + "種類まで集めたらまた見せてくだされ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				listmax = 0
				buff = lang("ところで、今デッキに入っているカードは" + syurui + "種類のようですのう。ひひ…" + gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) * 50 + "種類まで集めたらまた見せてくだされ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) == 0 ) {
			listmax = 0
			buff = lang("おやおや、訪問者とは珍しいこともあるものですじゃ。皆薄気味悪がって近寄らんものですがのう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ひひっ、まぁゆっくりしていってくだされ。わたしはアンデッド化しておりますが、旅人を取って食ったりはしませんですじゃ。襲われたら抵抗くらいはするかもしれませんがな…ひひひ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そうじゃ…この死にぞこないの婆のために、一つ頼まれてくれませぬか。カード…というものは、今の世にもありますじゃろう？そうですじゃ、生物の情報が記された…デッキに入れるものですじゃ。こう、森奥で死んだように長く暮らしておりますとなぁ。不意に、現代にはどんなものが生きているのか気になるのですよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("なぁに、悪いことには使いませぬ…まずは50種類以上のカードをデッキに入れて、見せていただけたならお礼をいたしますじゃ。もちろん、気の向いた時で構いませぬ。いひひ…なにせもうかなりの歳ですからなぁ。月日の流れが大層早く感じられまして、ねぇ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 663 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 605 | gdata(GDATA_FLAG_MAIN) == 610 | gdata(GDATA_FLAG_MAIN) == 615 ) {
			listmax = 0
			buff = lang("冒険者か…！いいところに来てくれた。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("気が向いたら", "See you later.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("逃げればいいんじゃ？", "See you later.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("お前は7層で倒したはず", "See you later.")
			listmax++
			buff = lang("俺たちは混沌の手の者に魔術で操られ、露払いをさせられていたのだが…。術者が死んだことにより、同じく操られていたアンデッド兵器が暴れ始めたのだ。鎮圧に手を貸してくれ！", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("頼むぞ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("操られている時に横で見ていてわかったが、こいつらは危険だ。後から来る者たちのためにも排除しておきたい。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("残念だったな、分身の術だよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			gdata(GDATA_FLAG_MAIN) = 620
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 923 | _switch_sw ) {
		_switch_sw = 0
		listmax = 0
		buff = lang("お父さん…お父さん…。", "Daddy..Daddy...!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) < 710 ) {
			goto *chat_unique_enthumesis
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 925 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("死んだことで両親と再会できるなんて。これで、あいつらを何度でも殺せるんだ…。", "I met my parents here again. Thanks, I can kill them many times.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("冒険者発見。第一陣と第二陣を突破してこんなところまで来ているなんて。", "I found you. I am surprised that you are already here.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("そこに冒険があるから", "Because there is an adventure here.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("もちろん報酬目当てだよ", "Of course I want a reward.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("レシマスの秘宝をわが物とするためだ", "Because I want a treasure of Lesimas.")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("君に会うため", "It was to meet you.")
		listmax++
		buff = lang("ただでさえ危険なネフィアで、刺客にも狙われているのに。どうして進み続けるの？パルミアの命令？", "You are targeted by assassins. Why do you keep going? Is Palmia's order?")
		chatesc = 0
		gosub *chat_select
		if ( chatval >= 0 & chatval <= 2 ) {
			listmax = 0
			buff = lang("…そう。やっぱり、あなたにはここで消えてもらう。", "So...This floor will be your graveyard.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		else {
			listmax = 0
			buff = lang("…？？？とりあえず、あなたにはここで消えてもらう。", "???...This floor will be your graveyard.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 926 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_AMUR_CAGE ) {
			listmax = 0
			buff = lang("ゼーム様トイッショ。嬉シイ。ゼーム様タノシソウ。嬉シイ。", "<Zeome> seems to be having fun. I'm glad.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("我ハ、ゼーム様ニヨッテ作ラレタガーディアン。ゼーム様ノ邪魔ヲスル者ハ排除スル。", "I am a guardian of <Zeome>. I will eliminate you.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 947 | _switch_sw ) {
		_switch_sw = 0
		if ( burt == 1 ) {
			listmax = 0
			buff = lang("街の外は危険だ。だがもしお前が冒険したいのなら、俺に止める権利はない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( burt == 0 ) {
			listmax = 0
			buff = lang("…ここから見える海の向こうの景色が好きだったんだ。でも今ではどんな景色だったのか思い出せない。海の向こうも奴らに喰われて黒く塗りつぶされてしまった。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("俺たちもかろうじて生き残っているだけて、やがて忘却に呑まれてしまうだろう。それがおそらく運命なんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("だが作物はこの街の中でだって栽培できるし、水もまだ井戸から汲むことができる。俺は最後の瞬間まで足掻いてみせるさ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			burt = 1
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 948 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_SUB_RESEARCH_DESTRUCTION_CRISIS) >= 900 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("調査隊のこと？", "Don't want it.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("きっと気のせいだ", "Taloned male.")
			listmax++
			buff = lang("何か…頼み事があったような気がするけど思い出せない…。", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("そういえば…調査隊なのになんで俺一人なんだ！？おかしいだろ。確かに仲間はいたはずだ…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("駄目だ！存在はぼんやりと思い出せるけど、名前も人数も思い出せない！みんな同じ格好だったから…いや、隊長はなんか紫っぽい格好をしていた気がするな…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("クソ、なんでこんな中途半端な忘れ方なんだ？？あの獣にただ喰われただけなら、もっと綺麗に忘れているはずなのに。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("思い出せ…そうだ。俺たちは元々、エーテルの風が谷の生態系にどんな影響を与えているかを調査しにきていた。だけどある日を境に、故郷のことがだんだん思い出せなくなっていって。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ただ事じゃないってアイツが…誰だっけ？とにかく騒ぎになって。そうこうしているうちに谷に記憶を喰らう獣が現れたって聞いて。この街の住民のことも思い出せなくなってきて…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("それから…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("調査隊？は…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ええと…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				list(0, listmax) = 1
				listn(0, listmax) = lang("怖っ", "Taloned male.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("たいしたことじゃない", "Don't want it.")
				listmax++
				buff = lang("すまん。何の話をしてたんだっけ？", "Sorry, this is untranslated sentence.")
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("だったらいいけどさ…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				else {
					listmax = 0
					buff = lang("？？？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strbye
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("そうか？重要なことだったと思うんだけどな…。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_RESEARCH_DESTRUCTION_CRISIS) == 1 ) {
			listmax = 0
			buff = lang("みんなが無事に戻ってきたらバーの酒で一杯やりたいよ。恐怖は旨い酒を飲んで忘れるに限る。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_SUB_RESEARCH_DESTRUCTION_CRISIS) == 0 ) {
			listmax = 0
			buff = lang("おや、旅人かい？…ひとつ頼まれてくれないか。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("負傷した俺を置いて、仲間がラスキリスに再調査に行ってしまったんだ。あそこはヤバイ。状況を打開する手がかりを探すとか言っていたけど、もう俺たちでどうにかなるものじゃないことは分かっているんだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("考えさせて", "Don't want it.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("わかった", "Taloned male.")
			listmax++
			buff = lang("隊長はこの事態に焦っている。なんとか説得してみんなを連れ戻してきてくれ…！", "Sorry, this is untranslated sentence.")
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("調査隊長の名前はラエスだ。君は強そうだけど、黒い獣にはくれぐれも気をつけてくれ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_SUB_RESEARCH_DESTRUCTION_CRISIS) = 1
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			}
			else {
				listmax = 0
				buff = lang("お願いだ…。一部、過激なのもいるけどみんな気のいいやつなんだ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 953 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("今のままで大丈夫", "It is OK as it is now.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("楽がしたい…", "I wish I could ease.")
		listmax++
		if ( gdata(GDATA_FLAG_GAME_MODE) == 2 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("経験値倍率を変えたい", "I wish change magnification of exp.")
			listmax++
		}
		if ( gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("残機数を変えたい", "I wish change number of remaining lives.")
			listmax++
		}
		if ( gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("最低経験値を変えたい", "I wish change lowest experience.")
			listmax++
		}
		buff = lang("この宇宙は苦難で満ちているのです。私と一緒に…少しでも楽な方へと堕ちていきませんか？", "Hey..you can change to an easier game mode than now...")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("ふふふ…♪もうこの宇宙には戻ってこれませんけど、構いませんよね？さぁ、一度そっと目を閉じて。…そして開いて、私の瞳孔を見つめるのです。", "You can not return to the original game mode. Now, please look at my eyes.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *label_6425
			goto *chat_end
		}
		if ( chatval == 2 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("3倍にする（デフォルト）", "Change to 3-fold.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("5倍にする", "Change to 5-fold.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("10倍にする", "Change to 10-fold.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("20倍にする", "Change to 20-fold.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("30倍にする", "Change to 30-fold.")
			listmax++
			buff = lang("アハァ♪わかりますよ、ええわかりますとも。物足りないのでしょう？さぁ、倍率を迷う必要なんてありません。どうせ、遅かれ早かれ最終的には最大倍率を選ぶのですから。", "Aha.. I understand you. I know that you are unsatisfactory! Well, there is no need to get lost. Anyway, sooner or later you will ultimately choose the maximum magnification.")
			gosub *chat_select
			if ( chatval == 0 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("ん〜？これで本当にいいんですかぁ？", "What...? Is this really OK?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_OVERDOSE_MODE_EXP_MULT) = 0
			}
			if ( chatval == 1 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("欲望の解放にブレーキをかけているのですね。かわいい〜♪", "Oh...You are braking the release of desire. Cute!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_OVERDOSE_MODE_EXP_MULT) = 2
			}
			if ( chatval == 2 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("中途半端が一番体によくないものです…。選び直さなくていいんですか…？", "The halfway is the most bad thing. Do not you have to select again...?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_OVERDOSE_MODE_EXP_MULT) = 7
			}
			if ( chatval == 3 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("「昔はそうだったから」セーフなんですよね？でも今は更に上があるんですよ？", "It is safe, because it used to be so, is not it? So are you really satisfied?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_OVERDOSE_MODE_EXP_MULT) = 17
			}
			if ( chatval == 4 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("いいんですよ…自分自身で選んだ快楽で、頭おかしくなっちゃいましょうねぇ♪", "It's ok...Please be crazy with head pleasure you chose...!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_OVERDOSE_MODE_EXP_MULT) = 27
			}
			goto *chat_end
		}
		if ( chatval == 3 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("残機0にする（デフォルト）", "I do not need it.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("残機3にする", "Get 3 remaining lives.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("残機9にする", "Get 9 remaining lives.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("残機20にする", "Get 20 remaining lives.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("残機99にする", "Get 99 remaining lives.")
			listmax++
			buff = lang("ほら、いくつ欲しいんですか…言ってくれなきゃわかりませんよ？足りなくなっても、いくらでも補充してさしあげますからね…。", "Here, how many do you want...? I do not know unless you tell me. When that is no longer enough, I will refill and give you...")
			gosub *chat_select
			if ( chatval == 0 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("あらあら。いまさらプライドを見せたところで無意味でしょう？", "Oh dear... Even if you show pride, it's too late.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_REMAINING_LIVES) = 0
			}
			if ( chatval == 1 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("欲望の解放にブレーキをかけているのですね。かわいい〜♪", "Oh...You are braking the release of desire. Cute!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_REMAINING_LIVES) = 3
			}
			if ( chatval == 2 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("中途半端が一番体によくないものです…。選び直さなくていいんですか…？", "The halfway is the most bad thing. Do not you have to select again...?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_REMAINING_LIVES) = 9
			}
			if ( chatval == 3 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("これっぽっちで不安になりませんか？もっと増やさなくていいんですか…？", "Will not you be anxious only by this? Do not you have to increase more...?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_REMAINING_LIVES) = 20
			}
			if ( chatval == 4 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("悪魔もびっくりですよ？本当に…本当に欲張りさぁん♪", "What hell! Really, really greedy..!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_REMAINING_LIVES) = 99
			}
			goto *chat_end
		}
		if ( chatval == 4 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("最低経験値0.001保障（デフォルト）", "Guarantee 0.001 experience at least.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("最低経験値0.01保障", "Guarantee 0.01 experience at least.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("最低経験値0.1保障", "Guarantee 0.1 experience at least.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("最低経験値1保障", "Guarantee 1 experience at least.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("最低経験値10保障", "Guarantee 10 experience at least.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("最低経験値100保障", "Guarantee 100 experience at least.")
			listmax++
			buff = lang("さぁ、どうぞ？ちなみに最低経験値1なら、何か経験値が入るたびにそのステータスが最低1レベルアップすることになります。潜在が足りなくても関係ありませんから、思う存分ステータスを上げられますよ。うふふふふ！良かったですねぇ。", "Well, please? By the way, if guarantee 1 experience, your status will increase by at least one level each time you gain experience. It doesn't matter if you don't have enough potential, so you can increase your status as much as you want. Aha...I am happy for you!")
			gosub *chat_select
			if ( chatval == 0 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("ん〜？これで本当にいいんですかぁ？", "What...? Is this really OK?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_GUARANTEED_EXP_PERCENTAGE) = 1
			}
			if ( chatval == 1 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("欲望の解放にブレーキをかけているのですね。かわいい〜♪", "Oh...You are braking the release of desire. Cute!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_GUARANTEED_EXP_PERCENTAGE) = 10
			}
			if ( chatval == 2 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("中途半端が一番体によくないものです…。選び直さなくていいんですか…？", "The halfway is the most bad thing. Do not you have to select again...?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_GUARANTEED_EXP_PERCENTAGE) = 100
			}
			if ( chatval == 3 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("1ずつ上がる程度ならセーフ？どこまでがセーフかなんて、気持ち次第じゃありませんか？", "Is it OK if you go up one by one? The criteria change depending on your feelings.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_GUARANTEED_EXP_PERCENTAGE) = 1000
			}
			if ( chatval == 4 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("怖がってはダ・メ♪時間がかかるかどうかだけで、どうせ行きつく先は一緒じゃないですかぁ？", "Don't be scared...! Only the time is different, and the destination is the same...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_GUARANTEED_EXP_PERCENTAGE) = 10000
			}
			if ( chatval == 5 ) {
				snd SOUNDLIST_LOAD
				listmax = 0
				buff = lang("いいんですよ…自分自身で選んだ快楽で、頭おかしくなっちゃいましょうねぇ♪", "It's ok...Please be crazy with head pleasure you chose...!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_GUARANTEED_EXP_PERCENTAGE) = 100000
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("適度な苦難こそが人を成長させる…そして、苦難を乗り越えなければ得られない悦びもある…。ええ、わかっていますとも。頭ではわかっていても…私の身体がわかってくれないのです。", "I already know. Just right difficulties will make people grow. And there are pleasures that can not be gained unless you overcome difficulties...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 980 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("ここで暮らしているの？", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("谷の奥はどうなっているの？", "Sorry, this is untranslated sentence.")
		listmax++
		buff = lang("ラスキリスは古くから旅人の通り道でな。こんなことになる前は、道端にちらほらと商人もおったものじゃ。", "Sorry, this is untranslated sentence.")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("ああ。誰かが製作中だったキャンプ地を利用させてもらっておる。自給自足生活も悪くない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("道を外れた奥地はまだまだ未開じゃ。軽い気持ちで入っていった冒険者は、大体帰ってこなかったな。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 981 | _switch_sw ) {
		_switch_sw = 0
		if ( nan == 1 ) {
			listmax = 0
			buff = lang("実は次の移住先を探しているの。ここも、いつまで安全かわからないでしょう？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( nan == 0 ) {
			listmax = 0
			buff = lang("私達の大切なものを奪っていく悪魔の獣…", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("と言っても、何が消えたのかすらもう思い出せないのですが。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("西にあるザイールも被害を受け、街として機能しなくなったので、仕方がなくここに移住したの。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			nan = 1
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 982 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("調子はどう？", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("どこの水辺でも釣れるのでは？", "Sorry, this is untranslated sentence.")
		listmax++
		buff = lang("魚の釣れる場所には目印にウキを浮かべているよ。参考にしてくれ。", "I'm floating on the place where fish can catch. Us to reference.")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("いつも通りさ。変わりゆく世界の中でも、釣れる魚は変わらない。いいことだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("ハハ、いいね。キミは水辺ならどこでも魚が釣れる世界から来たのかい？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 990 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("何が起きているの？", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("ちょっと様子見てくる", "Sorry, this is untranslated sentence.")
		listmax++
		buff = lang("谷の奥地は今、人間ではどうしようもないほどの超常現象に見舞われている…。長年の冒険で培った私の勘が、そう告げておりますぞ。", "I'm floating on the place where fish can catch. Us to reference.")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("まず、時間感覚がおかしくなっていますな。獣の発生からそれほど経っていないはずなのに、もうここに10年以上いる気がしてなりませんぞ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それに谷にいた皆さんが聞いたという謎の声。続く空間の隔離。何者かの手によって時空が捻じ曲げられていることは間違いないでしょう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("あなたが？ふむ…。どうにも不思議な雰囲気を感じますな。まるで…そう。この世界の人間ではないようだ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("失礼。ともかく並の冒険家ではないとお見受けします。それでも谷に向かわれるのならお気をつけて。携帯旅糧はお持ちですかな？きっと長く険しい道のりになるでしょう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 991 | _switch_sw ) {
		_switch_sw = 0
		if ( cray == 1 ) {
			listmax = 0
			buff = lang("冒険ってのは相応の準備をしたうえでそれでも危険を冒すこと。何も考えず、何も用意せずに危険に飛び込んでいくのは、冒険じゃなく自殺行為だ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…ってマンソンによく言われるんだ。そんなに危なっかしく思われてるのかな。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( cray == 0 ) {
			listmax = 0
			buff = lang("くっそー。谷の奥地が今どうなってるのか気になってしょうがないよ。でもマンソンがあれだけ止めるってことは、タイタン退治とか比較にならないくらいに危険なんだろう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("俺たちが止めるのを無視して、東にある裂け目に飛び込んでいった連中がいた気がする…。あいつらも冒険家だったのかな？もうよく思い出せないが。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			cray = 1
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 992 | _switch_sw ) {
		_switch_sw = 0
		if ( mary == 1 ) {
			listmax = 0
			buff = lang("こんな状況でも…ううん。こんな状況だからこそ、綺麗なお花に囲まれているだけで幸せだと思えるの。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( mary == 0 ) {
			listmax = 0
			buff = lang("まさかボニャックさんがお店を離れないなんて。お店、獣の群れに囲まれていたけど大丈夫かな…？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("谷にいた他の人たちも無事に脱出できているといいな。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			mary = 1
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 993 | _switch_sw ) {
		_switch_sw = 0
		if ( jenna == 1 ) {
			listmax = 0
			buff = lang("聞こえてきた声は神の啓示に違いないわ！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( jenna == 0 ) {
			listmax = 0
			buff = lang("いつものように谷で過ごしていたある日、それはどこからともなく聞こえてきたの。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("すぐに谷の入口まで退避しろという声。動物たちが次々に逃げ出すのを見て、さすがに危険を感じたわ。動物たちを追って移動する途中、ふと振り返ると、谷の奥から黒い獣が湧いてくるのが見えたの。恐ろしかったわ…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ここまで辿り着いた途端、空間が閉じて小さな裂け目だけが残ったわ。裂け目を覗くとね、谷の奥が見えるの。奥はもう、黒い獣でいっぱいよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ああ、こんな…！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("こんな神秘を体験できるなんて！！私は今、猛烈に感動しているわ！！！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			jenna = 1
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 995 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("お邪魔しました", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("何が言いたい？", "Sorry, this is untranslated sentence.")
		listmax++
		buff = lang("…メシェラ……星を食らう巨人…風…おお儚き風……森の免疫…醜き森…醜きカラス………ありのままの姿…死…再生…破滅……終わりなき闇………白い鷹…白子……絶望…虚無…その先に何を見るのか………厄災…壊れ行く世界……ありのままの存在…", "I'm floating on the place where fish can catch. Us to reference.")
		gosub *chat_select
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("…永遠の盟約……混沌…亡国の王子……忘却の女神……途絶えた運命…時空の管理者……忘れられない世界……妖精の伝える物語は………混沌の息子…世界記憶……アーカーシャの光…絆……最後の希望……続く開拓地……創出される世界……さいはてのうみ…", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…………(これ以上理解できない)", "(" + he(tc) + " ignores you...)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1001 | _switch_sw ) {
		_switch_sw = 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("避難したほうがいい", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("何を売っている？", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("焼き立てのパンが食べたい", "Sorry, this is untranslated sentence.")
		listmax++
		buff = lang("ラスキリスいらっしゃいませ。", "Wellcome to Raskilis.")
		gosub *chat_select
		if ( chatval == 0 ) {
			listmax = 0
			buff = lang("ご冗談を。この店を捨てて逃げるなんてとんでもない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("殺風景な店ですがね…。ここには色んなお客さんとの思い出が、いっぱい詰まっているんですよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("何も。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それでもここはボニャックの店なんです。たとえ売れる物が無くなろうと、たとえ世界から忘れ去られようと。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("あ、それ在庫切れ。", "It's sold out.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("残念ですよ。こんな状況でなければ、とびっきり美味しいパンを焼いて差し上げたのに。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1024 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) < 1000 ) {
			listmax = 0
			buff = lang("あはは。もし" + _onii(cdata(CDATA_SEX, CHARA_PLAYER)) + "さんが自分の物語を終わらせたら、アイメが楽しいお話をきかせてあげるねー。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("気が向いたらね", "Sorry, this is untranslated sentence.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("教えてほしいな", "Sorry, this is untranslated sentence.")
		listmax++
		buff = lang("アイメはねぇ、いろんなお話を知ってるんだよ。スターダストを５個くれたらおしえてあげるー。", "Sorry, this is untranslated sentence.")
		gosub *chat_select
		if ( chatval == 1 ) {
			item_find ITEM_ID_STARDUST, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
			if ( stat != (-1) ) {
				ci = stat
				if ( inv(INV_ITEM_NUM, ci) >= 5 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("《アイメの日記》（Lv???）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("仮面の下の笑顔（Lv200）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("夢を忘れた少年（Lv250）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("泥まみれの手（Lv300）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 4
					listn(0, listmax) = lang("銀涙（Lv400）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 5
					listn(0, listmax) = lang("巨神迷宮探検記（Lv500）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 6
					listn(0, listmax) = lang("カオスエンド（Lv600）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 7
					listn(0, listmax) = lang("イルヴァ学園七不思議（Lv700）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 8
					listn(0, listmax) = lang("トンファー怒りの大乱闘（Lv800）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 9
					listn(0, listmax) = lang("禁忌・天屍魔統（Lv900）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 10
					listn(0, listmax) = lang("イェルスード対ゼイレンオー（Lv1000）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 11
					listn(0, listmax) = lang("破壊神話（Lv1100）", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 12
					listn(0, listmax) = lang("永遠の英雄（Lv1200）", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("まだぜんぶ未実装だけどね。", "Sorry, this is untranslated sentence.")
					gosub *chat_select
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ひやかし？スターダストを５個もってきてねぇ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 1017 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_FLAG_MAIN) == 990 ) {
			listmax = 0
			buff = lang("馬鹿な…力では完全にこちらが上だったはず…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("だが…奇跡の代償は大きい…お前はあと数秒もせずに反動で崩壊し、自我も粉々になる…！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ぐっ…お前の憎たらしい顔も…もう見飽きた。私は先に虚無へと還る…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("まったく…こんな結末があるとは…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_MAIN) = 995
			gdata(GDATA_FLAG_COLLECTED_WISHES) = 0
			gdata(GDATA_FLAG_CUT_IN_MESSAGE_1) = 0
			gdata(GDATA_FLAG_CUT_IN_MESSAGE_2) = 0
			mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
			gdata(GDATA_TELEPORT_AREA) = AREA_CHAOS_CRADLE
			gdata(GDATA_TELEPORT_LEVEL) = 179
			levelexitby = MAP_EXIT_TELEPORT
			gdata(GDATA_LEVEL_START_ON) = 7
			mapstartx = 10
			mapstarty = 10
			chatteleport = 1
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_MAIN) == 890 ) {
			listmax = 0
			buff = lang("まだ動けるのか…もういいだろう？お前は必要ないくらいに耐えてみせた。その執念だけは認めてやる。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("だからどうした！", "What do I have to do next?")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("諦めるわけにもいかなくてね", "Where is Orphe?")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("それでもお前を殺す", "Can't you return to Tezcatlipoca?")
			listmax++
			buff = lang("大人しくそのまま散っておけ。無駄に苦しみ、頑張ったところでお前に勝ち目はない。", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			listmax = 0
			buff = lang("まだ減らず口を叩けるのか。いいだろう。敬意を表して、最期は念入りに焼き散らしてやろう。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			msg_clear
			txt lang("炎が一点に収束していく…！", "Flame converge to one point...!")
			txtef COLOR_YELLOW
			txt lang("*滅火の神槍*", "<HOROBINOYARI>")
			animeid = ANIMATION_HOROBINOYARI_2
			animode = 100 + 0
			gosub *anime
			snd SOUNDLIST_ATK_FIRE
			redraw 0
			gosub *screen_draw
			gosub *screen_fadeIn2
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face2" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			gdata(GDATA_FLAG_CUT_IN_MESSAGE_1) = 850
			animeid = ANIMATION_HOROBINOYARI_1
			anic = 0
			gosub *anime
			animeid = ANIMATION_GENE
			anic = 0
			gosub *anime
			gdata(GDATA_FLAG_MAIN) = 900
			cdata(CDATA_CONDITION_MPOISON, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_POISON, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_SLEEP, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_CONFUSE, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_BLIND, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_PARALYZE, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_SUFFOCATION, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_DIM, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_FEAR, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_DRUNK, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_BLEED, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_ATROPHY, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_BIND, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_OIL, CHARA_PLAYER) = 0
			cdata(CDATA_CONDITION_BRAINWASH, CHARA_PLAYER) = 0
			gdata(GDATA_SLEEP) = 0
			cdata(CDATA_HP, CHARA_PLAYER) = gdata(GDATA_FLAG_COLLECTED_WISHES)
			if ( cdata(CDATA_MP, CHARA_PLAYER) < gdata(GDATA_FLAG_COLLECTED_WISHES) ) {
				cdata(CDATA_MP, CHARA_PLAYER) = gdata(GDATA_FLAG_COLLECTED_WISHES)
			}
			if ( cdata(CDATA_SP, CHARA_PLAYER) < gdata(GDATA_FLAG_COLLECTED_WISHES) ) {
				cdata(CDATA_SP, CHARA_PLAYER) = gdata(GDATA_FLAG_COLLECTED_WISHES)
			}
			cdata(CDATA_POWER_GAUGE, CHARA_PLAYER) = 200
			if ( cdata(CDATA_HUNGER, CHARA_PLAYER) < 9000 ) {
				cdata(CDATA_HUNGER, CHARA_PLAYER) = 9000
			}
			if ( cdata(CDATA_THIRST, CHARA_PLAYER) < 9000 ) {
				cdata(CDATA_THIRST, CHARA_PLAYER) = 9000
			}
			p = 10, 11, 12, 17, 18, 14, 13, 16, 15, 19, -1
			repeat
				if ( p(cnt) == (-1) ) {
					break
				}
				i = p(cnt) + 240 - 10
				if ( cdata(i, CHARA_PLAYER) < 0 ) {
					cdata(i, CHARA_PLAYER) = 0
				}
				cdata(i, CHARA_PLAYER) = sorg(p(cnt), CHARA_PLAYER) / 7 + 5
			loop
			if ( tc == CHARA_PLAYER ) {
				gdata(GDATA_SLEEP) = 0
			}
			r1 = 0
			gosub *charaRefresh
			gosub *chat_refresh
			music = MUSICLIST_MCLASTBOSS5
			gosub *music_play
			msg_clear
			txt lang("立ちなさい！皆の願いを、力に換えて！", "Stand up! Convert the collected " + gdata(GDATA_FLAG_COLLECTED_WISHES) + " wishes into power!")
			listmax = 0
			buff = lang("何が起きた…！？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("主神級でも余裕で消滅するほどの火焔で貫いたはずだ。なぜ一介の定命が、これほどの奇跡に守られている？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("ただの冒険者だ", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("これは願いだ！つながりが生んだ光だ！", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("冒険者の恐ろしさを味わわせてやる…", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("何だか知らんがお前を倒す！", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("わからん…何だその光は。お前は一体…。", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			listmax = 0
			buff = lang("く…はははははは！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("お前は本当に番狂わせだよ！だが、そう。所詮は死に損ないが一時の奇跡を掴んだに過ぎない！そんなもの、圧倒的な力で真っ向から打ち砕いてくれる！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("さぁ来い…イレギュラァァァァッ！！", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			music = MUSICLIST_MCLASTBOSS5
			gosub *music_play
			goto *chat_end
			goto *chat_unique_SWEND1
		}
		if ( gdata(GDATA_FLAG_MAIN) == 880 ) {
			listmax = 0
			buff = lang("…？おや。灰が舞っているのかと思えば、焼け残りがいたのか。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("だからどうした", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("まだ勝負はついていない", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("仇は討つ", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("燃えカスになってもまだ抗う気か？戦力の差は歴然だぞ。", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			listmax = 0
			buff = lang("いいや。終わりだ、イレギュラー。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			msg_clear
			txtef COLOR_RED2
			txt lang("*滅火*", "<HOROBI>")
			redraw 0
			gosub *screen_draw
			gosub *screen_fadeIn2
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face2" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			snd SOUNDLIST_ATK_FIRE
			tcbk = tc
			if ( cdata(CDATA_IN_TAGTEAM, CHARA_PLAYER) != 0 ) {
				tag_end 0
			}
			if ( gdata(GDATA_RIDER) != 0 ) {
				ride_end
			}
			repeat 245
				if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
					continue
				}
				if ( 0 == cnt ) {
					continue
				}
				if ( tcbk == cnt ) {
					continue
				}
				map(cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt), MAP_TILE_ID) = xy2pic(3, 8)
				snd SOUNDLIST_ATK_FIRE
				if ( cdata(CDATA_ALLIED, cnt) != 0 ) {
					txt lang(cdatan(CDATAN_NAME, cnt) + "はあなたを庇って灰になった。", name(cnt) + " shielded you and turned to ashes.")
					cdata(CDATA_EXIST, cnt) = CHAR_STATE_SUSPEND
				}
				else {
					cdata(CDATA_HP, cnt) = 0
					if ( cdata(CDATA_RELATION, cnt) == RELATION_ALLY ) {
						dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH_SHIELDED
						dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH_SHIELDED
						dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH_SHIELDED
					}
					else {
						dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH
						dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH
						dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH
					}
				}
				map(cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt), MAP_CHARA_INDEX_PLUS_ONE) = 0
				cdata(CDATA_AREA, cnt) = AREA_NONE
			loop
			tc = CHARA_PLAYER
			repeat 200
				p = -1, 1
				cdata(CDATA_X_NEW, tc) = cdata(CDATA_X, tc) + (3 - cnt / 70 + rnd(5)) * p(rnd(2))
				cdata(CDATA_Y_NEW, tc) = cdata(CDATA_Y, tc) + (3 - cnt / 70 + rnd(5)) * p(rnd(2))
				cell_check cdata(CDATA_X_NEW, tc), cdata(CDATA_Y_NEW, tc)
				if ( cellaccess == 1 ) {
					rowactend tc
					cc = CHARA_PLAYER
					ccprev = tc
					gosub *procMove
					tc = ccprev
					snd SOUNDLIST_TAORE08
					break
				}
			loop
			gosub *screen_draw
			animeid = ANIMATION_JALDABAOTH
			gosub *anime
			txtef COLOR_YELLOW
			txt lang("テスカトリポカの遺した来光の牙が蒸発し、アカシックネットワークに溶けていった…。", "Sorry, this is untranslated sentence.")
			cdata(CDATA_HP, CHARA_PLAYER) = (-36456) + rnd(2189)
			cdata(CDATA_MP, CHARA_PLAYER) = 0
			if ( sdata(SKILL_ATTR_STR, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_STR, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_CON, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_CON, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_DEX, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_DEX, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_LER, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_LER, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_PER, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_PER, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_WIL, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_WIL, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_MAG, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_MAG, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_CHA, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_CHA, CHARA_PLAYER) = 10
			}
			if ( sdata(SKILL_ATTR_SPD, CHARA_PLAYER) > 10 ) {
				sdata(SKILL_ATTR_SPD, CHARA_PLAYER) = 10
			}
			gdata(GDATA_FLAG_MAIN) = 885
			txt lang("声が聞こえる…。", "You heard someone's voice...")
			gdata(GDATA_FLAG_CUT_IN_MESSAGE_1) = 500
			animeid = ANIMATION_LASTBOSS_2
			anic = 0
			gosub *anime
		}
		goto *chat_end
		goto *chat_unique_SWEND1
		_switch_sw++
	}
	if ( _switch_val == 526 | _switch_sw ) {
		_switch_sw = 0
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_LEVEL) == 179 ) {
				if ( gdata(GDATA_FLAG_MAIN) == 715 ) {
					listmax = 0
					buff = lang("おかしいな…やはり弱すぎるぞ。最初は誤差だと思っていたが、いくらなんでも予測と違いすぎる。", "That was too easy. Way too easy. I thought it would be close to what we expected at the beginning, but this is just... not what we thought it would be.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("死体に残存する神力量も少な…なんだこれは！？離れろ！", "There's also very little divine power left in the bod- what's going on?! Everyone get away!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 720 ) {
					listmax = 0
					buff = lang("まさか…エンテュメイシスが吸収していた神の一柱が、分離・復活したのか！？", "Is the a god that Enthumesis absorbed splitting away and reviving itself?!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("…いや、違う。寄生していたんだ。こいつが、エンテュメイシスのため込んだ神力を吸収していたんだッ！", "No, that's not it. It's a parasite. It's absorbing all the divine power that was stored in Enthumesis!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("来光の牙を使うにしても、枷をかけて閉じ込めるにしても、どうにか弱らせる必要がある。産まれたばかりでヨタヨタしているが、奴から感じる力は超神級だ。迂闊に近づくなよ" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！距離を取りながら突破口を見つけるんだ！", "We'll need to weaken it before using the Origin of Light or to trap it with a fetter. It's still clumsy because it's still a newborn, but the power coming off of it is definitely beyond that of a regular god. " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "! Don't get too close to it! Find a solution while keeping the distance!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					cdata(CDATA_AI_DISTANCE, tc) = 3
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 725 ) {
					listmax = 0
					buff = lang("ちっ…俺たちだけでは手に負えないな。", "Damn, this thing is out of our league.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 735 ) {
					listmax = 0
					buff = lang("なんだ今の攻撃は！？", "What was that attack just now?!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 745 ) {
					listmax = 0
					buff = lang("駄目だ、決定打が足りない！", "This is no good! Our attacks aren't doing enough!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 755 ) {
					listmax = 0
					buff = lang("止まったのか…？", "Did... did we stop it?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
					listmax = 0
					buff = lang("再生能力まで消してくれれば、殺して来光の牙を使えたんだが…もう爺さんが限界のようだな。凶獣とやらも崩壊しかかっている。術者が行動不能になった時に自壊するよう、安全装置が組み込まれていたようだ。とりあえず爺さんについては、仲間のところまで俺の化身が送り届けるぞ。", "We might've been able to use the Origin of Light if we get rid of that regeneration, but it looks like that old man's reached his limit. That beast is also not going to last much longer. It was probably made to self destruct when its user is incapacitated. For now, I'll just send that old man back with a servant of mine.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ノルンから連絡がいっているはずだが、俺からも他の神々に報告をしておきたい。もう動かなくなったとはいえ、こいつが何者なのか調べたり、今後どうするのか相談もしなきゃいけないしな。一度、神の間から出るぞ。", "Norne's going to report to the gods, but I'll have to do that too. We need to find out what that was even if it's not moving anymore, and we also need to decide what to do from now on. Come on, let's get out of here.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
						gdata(GDATA_FLAG_DUEL_TARGET) = 0
					}
					mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
					gdata(GDATA_TELEPORT_AREA) = AREA_CHAOS_CRADLE
					gdata(GDATA_TELEPORT_LEVEL) = 178
					levelexitby = MAP_EXIT_TELEPORT
					gdata(GDATA_LEVEL_START_ON) = 7
					mapstartx = 10
					mapstarty = 13
					chatteleport = 1
					goto *chat_end
					goto *chat_end
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_AREA) == AREA_CHAOS_CRADLE ) {
			if ( gdata(GDATA_LEVEL) == 178 ) {
				if ( gdata(GDATA_FLAG_MAIN) >= 1000 ) {
					if ( gdata(GDATA_FLAG_MAIN) == 1010 ) {
						listmax = 0
						buff = lang("ようやくお目覚めか。本当に…本当に待たせやがって…。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("俺はここで修行しながら番をしていた。お前の復活に邪魔が入ってもいけないからな。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("次は何をすればいい？", "What do I have to do next?")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("オルフェはどうしてる？", "Where is Orphe?")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("テスカトリポカに戻らないの？", "Can't you return to Tezcatlipoca?")
					listmax++
					if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 8 ) {
						list(0, listmax) = 3
						listn(0, listmax) = lang("まだ貰っていないご褒美がある", "I have not yet got the reward.")
						listmax++
					}
					buff = lang("ああ、そうそう。他の連中のクリア後会話はまだ開発中だ。気長に待ってくれ。", "Sorry, this is untranslated sentence.")
					if ( gdata(GDATA_FLAG_MAIN) == 1010 ) {
						gdata(GDATA_FLAG_MAIN) = 1015
					}
					gosub *chat_select
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("ノルンが言っていたが…お前は役割を全て果たしたらしい。もう運命に縛られることはないんだ…良かったじゃないか。この広い世界を飽きるまで冒険してくれ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("オルフェのやつは保護されて精密検査に回された。心身に細工されていないことが確認された後、監視付きでノースティリスに降ろされたよ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("俺はロストイルヴァで共に働くことを提案したんだがな、会わせる顔がないとか言って断られてしまった。そういえば、冒険者になるつもりだとか言っていたな。おそらく、お前に触発されたんだろう。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 2 ) {
						listmax = 0
						buff = lang("力は取り戻したんだが、今更あの八柱と勢力争いをする気も起きなくてな。元は神の間で活動するためだったとはいえ、この姿にも随分と愛着が湧いた。これからも神であることを隠し、強者を鍛えていこうと思っている。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("そうそう。力を取り戻した影響で、ＡＰで速度や生命力をさらに上げられるようになったぞ。暇があったらウールム・レゾンに寄ってみてくれ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 3 ) {
						listmax = 0
						buff = lang("褒美か。そうだな。", "A reward? Sure, I guess.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 6 ) {
							gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 6
							txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
							flt
							itemcreate -1, ITEM_ID_WAKE_UP_OF_NEFIA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							listmax = 0
							buff = lang("…宝玉を渡しておこう。近辺のネフィアを活性化させ、内部のモンスターの力を引き出すことができる。役に立つかはわからんが、副次効果としてネフィアの核も手に入れることができるだろう。", "You can have this gem. It awakens nearby Nefia and brings out the potential of the monsters within them. It also enables you to find the core of Nefia - though I don't know if you'll have any use for those.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						list(0, listmax) = 0
						listn(0, listmax) = lang("いらん", "Don't want it.")
						listmax++
						list(0, listmax) = 1
						listn(0, listmax) = lang("爪の♂", "Taloned male.")
						listmax++
						list(0, listmax) = 2
						listn(0, listmax) = lang("爪の♀", "Taloned female.")
						listmax++
						list(0, listmax) = 3
						listn(0, listmax) = lang("牙の♂", "Fanged male.")
						listmax++
						list(0, listmax) = 4
						listn(0, listmax) = lang("牙の♀", "Fanged female.")
						listmax++
						buff = lang("合計2体までなら化身をくれてやってもいいがどうする？爪の鋭い奴と牙がすごい奴がいるんだが。", "I can give you up to two servants. How 'bout it? There are ones with sharp talons and some with really impressive teeth.")
						gosub *chat_select
						if ( get_freeally() == 0 ) {
							listmax = 0
							buff = lang("む。そもそも仲間の最大数に達しているから連れていけないのか。", "Hmm. It looks like your party is already full and you can't invite someone anymore.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							goto *chat_end
						}
						if ( chatval == 0 ) {
							listmax = 0
							buff = lang("そうか…。", "I see.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						if ( chatval >= 1 & chatval <= 4 ) {
							gdata(GDATA_FLAG_LEOLD_LOCATION_STATE)++
							if ( chatval == 1 ) {
								p = 909
								sex = 0
							}
							if ( chatval == 2 ) {
								p = 909
								sex = 1
							}
							if ( chatval == 3 ) {
								p = 910
								sex = 0
							}
							if ( chatval == 4 ) {
								p = 910
								sex = 1
							}
							if ( p == 909 ) {
								txt lang("この獣爪兵はとても好戦的なうえ、煙の中でも視野を確保できるようだ。", "This soldier is very aggressive and seems to be able to see its targets perfectly clearly through smoke.")
							}
							if ( p == 910 ) {
								txt lang("このワニは泳ぎがとても得意なようだ。", "This crocodile seems to be very good at swimming.")
							}
							flt
							novoidlv = 1
							characreate -1, p, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
							cdata(CDATA_SEX, rc) = sex
							wishally = 100
							gosub *add_ally
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 840 & gdata(GDATA_FLAG_MAIN) < 1000 ) {
					if ( gdata(GDATA_FLAG_MAIN) == 840 ) {
						listmax = 0
						buff = lang("オルフェめ、やってくれたな。だが今度こそ止めてみせる…。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("おっと。待っていたぞ、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。必要なんでな、来光の牙を返してもらうぞ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						txtef COLOR_BLUE
						txt lang("[来光の牙]を失った。", "You have lost Origin of Light...")
						gdata(GDATA_FLAG_MAIN) = 845
						autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
						goto *chat_end
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("一緒に戦わせて", "I think not.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("締めくくりに相応しい冒険だ", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("相手の情報がほしい", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("【神の間へ降りる】", "<Head down to God's prison>")
					listmax++
					if ( leokaiwa == 0 ) {
						buff = lang("後は俺が何とかする…いや、俺が何とかしなきゃいけない。神の間へ降りたら、もう何があっても逃げ帰れないからな。これ以上お前を巻き込みたくないんだ。", "Sorry, this is untranslated sentence.")
					}
					if ( leokaiwa == 1 ) {
						buff = lang("…もしお前が一緒に決戦に挑むつもりでも、だ。今まで世話になった連中に一度声をかけてきたらどうだ？心残りを確かめ、戦う覚悟を…いや、すまん。余計なお節介だったな。", "Sorry, this is untranslated sentence.")
					}
					gosub *chat_select
					leokaiwa = 1
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("…なぜだろうな、心強く思えてしまうのは。お前に頼ってはいけないと、分かっているのだが。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("本当は、こんなところまで来させたくなかった。お前には、広い世界で普通に冒険をしていて欲しかった…。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 2 ) {
						listmax = 0
						buff = lang("俺がこのフロアで交戦した時点では、オルフェはレベル350程度。混沌の渦や衝撃波動を使ってきたから、混沌耐性やPVとDVが低いと致命的だろう。だがこれは最新の情報ではない。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("もし動けない混沌の超児からオルフェが全神力を吸収していた場合…とんでもないことになっているはず。そう、レベルは1200をゆうに超え、混沌のみならず他の神々から吸収したあらゆる属性で攻撃を仕掛けてくるだろう。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("もはや弱点は神に威力を発揮するエンチャントくらいのものだろう。それにしたって、物理攻撃は重力や束縛で回避率を落とさないとまず当たるまい。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("今の俺なら強化前のオルフェには余裕で勝てるだろう。だが強化されていた場合は…正直勝てる見込みは薄い。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strbye
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 3 ) {
						listmax = 0
						buff = lang("止めても無駄みたいだな。俺も行く。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("…なぁ、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。最後の戦いに挑む前に、どうしてもお前に言っておかなきゃいけないことがある。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("じゃあ…言うぞ。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("実はな。俺はオルフェを戦友のように思っていた。あいつが混沌に魅入られた後でも、本心ではなんとか救ってやりたかったんだ。…だが、もはやそんな悠長なことを言ってる場合ではなくなってしまった。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("もしも俺が戦いの中で迷いを見せたら。その時はお前が俺の頬をひっぱたいてくれ。…それだけだ。行くぞ！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
						gdata(GDATA_TELEPORT_AREA) = AREA_CHAOS_CRADLE
						gdata(GDATA_TELEPORT_LEVEL) = 179
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 10
						mapstarty = 13
						chatteleport = 1
						goto *chat_end
						goto *chat_end
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 715 & gdata(GDATA_FLAG_MAIN) < 840 ) {
					if ( gdata(GDATA_FLAG_MAIN) >= 760 ) {
						list(0, listmax) = 0
						listn(0, listmax) = lang("ちゃんと警備してよ", "Keep a close eye on that thing, will you?")
						listmax++
						if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 8 ) {
							list(0, listmax) = 1
							listn(0, listmax) = lang("ご褒美は？", "Where's my reward?")
							listmax++
						}
						buff = lang("アクシデントもあったが、エンテュメイシスは倒せた。お前は決戦因子としてよくやってくれたよ。後は俺たちに任せてくれ。", "It didn't go without a hitch, but what matters is that Enthumesis has been defeated. You can leave the rest to me.")
						gosub *chat_select
						if ( chatval == 0 ) {
							listmax = 0
							buff = lang("お前は心配性だな…。大丈夫だ、やつは身体の動かし方すら概念レベルで忘れている。出てこられるわけがない。仮に、外側から出そうとする者が来ても、神の間と物理的につながっているのはこの扉のみ。そしてこの階層は神力が使える。つまりソピアーが常闇の杖で事前に感知してくれるのさ。", "You don't have to worry about that. It can't even move its body properly - there's no chance that it'll get out from here. Even if someone from the outside tried to bring it out, Sophia will know. The door here is the sole entryway into God's Prison, and divine power can be use here perfectly well.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							listmax = 0
							buff = lang("警備は完璧だ。お前はいつもの生活に戻ってくれ。", "You can leave the guarding to me. You go back to your stuff now.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						if ( chatval == 1 ) {
							listmax = 0
							buff = lang("褒美か。そうだな。", "A reward? Sure, I guess.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 6 ) {
								gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 6
								txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
								flt
								itemcreate -1, ITEM_ID_WAKE_UP_OF_NEFIA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
								listmax = 0
								buff = lang("…宝玉を渡しておこう。近辺のネフィアを活性化させ、内部のモンスターの力を引き出すことができる。役に立つかはわからんが、副次効果としてネフィアの核も手に入れることができるだろう。", "You can have this gem. It awakens nearby Nefia and brings out the potential of the monsters within them. It also enables you to find the core of Nefia - though I don't know if you'll have any use for those.")
								tc = tc * (0 == 0) + (0 != 0) * 0
								list(0, listmax) = 0
								listn(0, listmax) = strmore
								listmax++
								chatesc = 1
								gosub *chat_select
								if ( scenemode ) {
									if ( scene_cut == 1 ) {
										goto *chat_end
									}
								}
							}
							list(0, listmax) = 0
							listn(0, listmax) = lang("いらん", "Don't want it.")
							listmax++
							list(0, listmax) = 1
							listn(0, listmax) = lang("爪の♂", "Taloned male.")
							listmax++
							list(0, listmax) = 2
							listn(0, listmax) = lang("爪の♀", "Taloned female.")
							listmax++
							list(0, listmax) = 3
							listn(0, listmax) = lang("牙の♂", "Fanged male.")
							listmax++
							list(0, listmax) = 4
							listn(0, listmax) = lang("牙の♀", "Fanged female.")
							listmax++
							buff = lang("合計2体までなら化身をくれてやってもいいがどうする？爪の鋭い奴と牙がすごい奴がいるんだが。", "I can give you up to two servants. How 'bout it? There are ones with sharp talons and some with really impressive teeth.")
							gosub *chat_select
							if ( get_freeally() == 0 ) {
								listmax = 0
								buff = lang("む。そもそも仲間の最大数に達しているから連れていけないのか。", "Hmm. It looks like your party is already full and you can't invite someone anymore.")
								tc = tc * (0 == 0) + (0 != 0) * 0
								list(0, listmax) = 0
								listn(0, listmax) = strmore
								listmax++
								chatesc = 1
								gosub *chat_select
								if ( scenemode ) {
									if ( scene_cut == 1 ) {
										goto *chat_end
									}
								}
								goto *chat_end
							}
							if ( chatval == 0 ) {
								listmax = 0
								buff = lang("そうか…。", "I see.")
								tc = tc * (0 == 0) + (0 != 0) * 0
								list(0, listmax) = 0
								listn(0, listmax) = strmore
								listmax++
								chatesc = 1
								gosub *chat_select
								if ( scenemode ) {
									if ( scene_cut == 1 ) {
										goto *chat_end
									}
								}
							}
							if ( chatval >= 1 & chatval <= 4 ) {
								gdata(GDATA_FLAG_LEOLD_LOCATION_STATE)++
								if ( chatval == 1 ) {
									p = 909
									sex = 0
								}
								if ( chatval == 2 ) {
									p = 909
									sex = 1
								}
								if ( chatval == 3 ) {
									p = 910
									sex = 0
								}
								if ( chatval == 4 ) {
									p = 910
									sex = 1
								}
								if ( p == 909 ) {
									txt lang("この獣爪兵はとても好戦的なうえ、煙の中でも視野を確保できるようだ。", "This soldier is very aggressive and seems to be able to see its targets perfectly clearly through smoke.")
								}
								if ( p == 910 ) {
									txt lang("このワニは泳ぎがとても得意なようだ。", "This crocodile seems to be very good at swimming.")
								}
								flt
								novoidlv = 1
								characreate -1, p, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
								cdata(CDATA_SEX, rc) = sex
								wishally = 100
								gosub *add_ally
							}
						}
						goto *chat_end
					}
					list(0, listmax) = 0
					listn(0, listmax) = lang("ああ", "Yes.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("いや…", "No...")
					listmax++
					if ( gdata(GDATA_FLAG_MAIN) == 715 | gdata(GDATA_FLAG_MAIN) == 720 ) {
						buff = lang("来たな。イベントの続きをやるぞ。本来はエンテュメイシスを倒した直後にそのまま起きるものだからな。", "You're back. Let's get on with the rest of the event. This was supposed to happen immediately after defeating Enthumesis.")
					}
					if ( gdata(GDATA_FLAG_MAIN) > 720 ) {
						buff = lang("神の間に行くか？", "Head down to God's Prison?")
					}
					gosub *chat_select
					if ( chatval == 0 ) {
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
						gdata(GDATA_TELEPORT_AREA) = AREA_CHAOS_CRADLE
						gdata(GDATA_TELEPORT_LEVEL) = 179
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 10
						mapstarty = 13
						chatteleport = 1
						goto *chat_end
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) >= 680 & gdata(GDATA_FLAG_MAIN) < 715 ) {
					if ( gdata(GDATA_FLAG_MAIN) < 700 ) {
						list(0, listmax) = 0
						listn(0, listmax) = lang("次は何をすればいい？", "What do I have to do next?")
						listmax++
					}
					if ( gdata(GDATA_FLAG_MAIN) <= 685 ) {
						list(0, listmax) = 1
						listn(0, listmax) = lang("それ知ってた", "I knew that already.")
						listmax++
					}
					if ( gdata(GDATA_FLAG_MAIN) <= 685 ) {
						list(0, listmax) = 2
						listn(0, listmax) = lang("テスカトリポカが混沌の神じゃないの？", "Isn't Tezcatlipoca the God of Chaos?")
						listmax++
					}
					if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 7 ) {
						list(0, listmax) = 3
						listn(0, listmax) = lang("勝ったご褒美は？", "What do I get if we win?")
						listmax++
					}
					if ( gdata(GDATA_FLAG_MAIN) >= 700 & gdata(GDATA_FLAG_MAIN) < 720 ) {
						list(0, listmax) = 5
						listn(0, listmax) = lang("もうちょっと待ってて", "Hang on a sec.")
						listmax++
					}
					if ( gdata(GDATA_FLAG_MAIN) >= 700 & gdata(GDATA_FLAG_MAIN) < 715 ) {
						list(0, listmax) = 4
						listn(0, listmax) = lang("【神の間へ降りる】", "<Head down to God's prison>")
						listmax++
					}
					if ( gdata(GDATA_FLAG_MAIN) == 680 ) {
						buff = lang("それにしても我…いや俺がテスカトリポカだとは思わなかっただろう。騙されたな？", "You didn't think I was Tezcatlipoca, did you? Gotcha.")
					}
					if ( gdata(GDATA_FLAG_MAIN) > 680 ) {
						buff = lang("神の間で決戦因子と共に戦えるように、受肉して神力を使わず純粋に鍛え上げた…それがこのレオルドとしての姿だ。混沌の神にどこまで通用するかはわからんがな。", "What you see here - the person you call Leold - is a physical form that I had created and trained up without any use of divine power. All so that I could fight alongside the Factors in God's Prison. I don't know how much of that effort will pay off against the God of Chaos though.")
					}
					if ( gdata(GDATA_FLAG_MAIN) == 680 ) {
						gdata(GDATA_FLAG_MAIN) = 685
					}
					gosub *chat_select
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("ソピアーが中心になって決戦準備を進めている。準備ができ次第、神の間に突入してもらうことになるだろう。気になるなら進捗を確認しに行ってくれ。", "Sophia and the others are preparing for the final confrontation. You're probably going to have to fight in God's Prison when the time comes. You can go check to see if how they're doing with the preparations if you want.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("う、嘘だろ？", "You... you're kidding...")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("ネタバレしたのは誰だ。…シナアか！？", "Who let the cat out of the bag?! Was it Sinaha?!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						txtef COLOR_YELLOW
						txt lang("シナア「うみゃっ！？」", cnvtalk("Mew!?"))
					}
					if ( chatval == 2 ) {
						listmax = 0
						buff = lang("誰がそんなこと言ったんだ。混沌の神は混沌の神、テスカトリポカはテスカトリポカで全くの別神だぞ。誰かの推測でも鵜呑みにしたか？", "Who told you that? The God of Chaos and Tezcatlipoca are completely separate and different. You shouldn't believe every rumour that you hear.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("俺は滅亡が迫っていることを決戦因子候補たちに警告し、神の間を目指して必要な力を示すように指示したが…俺自身が滅ぼすなんて一言も言っていないだろう。そもそも神の間に囚われていたら神力で外界に呼びかけること自体が不可能だ。", "When danger drew close, I did warn all the people who were candidates as Factors and tell them to head to God's Prison. I did not say anything about destroying anything myself, did I? Besides, it would've been impossible to summon anybody in the outside world if I were trapped in the Prison.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("確定情報と不確定情報は混同せず、ちゃんと分けて整理したほうがいいぞ。勘違いを招くからな。", "You should always be careful about separating facts from assumptions, or else you'll get things wrong again.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( chatval == 4 ) {
						listmax = 0
						buff = lang("神の間では記録ができないらしい。回復アイテム、補給用アイテム、装備の見直…いや、前にも言ったからもう分かっているか。", "It looks like you're not going to be able to save while you're inside there. Do you have all your healing and buffing items? What about your equipment? ...No, I think I've told you all that already, haven't I?")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("混沌の神エンテュメイシス討伐。ついにこの時が来た。よし…俺も覚悟を決めたぞ。行こう、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！", "The time to defeat Enthumesis has finally come. Alright. I think I'm ready too. Let's go, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
							snd SOUNDLIST_FAIL1
							txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
							goto *chat_end
						}
						autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
						mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
						gdata(GDATA_TELEPORT_AREA) = AREA_CHAOS_CRADLE
						gdata(GDATA_TELEPORT_LEVEL) = 179
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 10
						mapstarty = 13
						chatteleport = 1
						goto *chat_end
					}
					if ( chatval == 3 ) {
						listmax = 0
						buff = lang("褒美か。アーティファクトなら戦闘で落としたものを持っていけ。", "A reward? You can have this artifact that it dropped.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) == 6 ) {
							listmax = 0
							buff = lang("宝玉も渡しただろう？覚醒の宝玉だ。", "I gave you a gem, right? The gemstone of awakening.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 6 ) {
							gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 6
							txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
							flt
							itemcreate -1, ITEM_ID_WAKE_UP_OF_NEFIA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
							listmax = 0
							buff = lang("…宝玉を渡しておこう。近辺のネフィアを活性化させ、内部のモンスターの力を引き出すことができる。役に立つかはわからんが、副次効果としてネフィアの核も手に入れることができるだろう。", "You can have this gem. It awakens nearby Nefia and brings out the potential of the monsters within them. It also enables you to find the core of Nefia - though I don't know if you'll have any use for those.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						list(0, listmax) = 0
						listn(0, listmax) = lang("保留", "Maybe later.")
						listmax++
						list(0, listmax) = 1
						listn(0, listmax) = lang("爪の♂", "Taloned male.")
						listmax++
						list(0, listmax) = 2
						listn(0, listmax) = lang("爪の♀", "Taloned female.")
						listmax++
						list(0, listmax) = 3
						listn(0, listmax) = lang("牙の♂", "Fanged male.")
						listmax++
						list(0, listmax) = 4
						listn(0, listmax) = lang("牙の♀", "Fanged female.")
						listmax++
						list(0, listmax) = 5
						listn(0, listmax) = lang("全部だ！", "I want all of them!")
						listmax++
						buff = lang("あとは化身の1体くらいならくれてやってもいいがどうする？爪の鋭い奴と牙がすごい奴がいるんだが。", "You can have one more servant. How 'bout it? There are ones with sharp talons and some with really impressive teeth.")
						gosub *chat_select
						if ( get_freeally() == 0 ) {
							listmax = 0
							buff = lang("む。そもそも仲間の最大数に達しているから連れていけないのか。", "Hmm. It looks like your party is already full and you can't invite someone anymore.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							goto *chat_end
						}
						if ( chatval == 0 ) {
							listmax = 0
							buff = lang("そうか…。あと言っておくが、俺は光玉は持ってないからな。", "Okay then. And just so that you know, I don't have any god jewels.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						if ( chatval == 5 ) {
							listmax = 0
							buff = lang("欲張りだな。混沌の神を討伐したらもう1体くれてやるから勘弁してくれ。", "Greedy, aren't you? I'll give you just one more after we defeat the God of Chaos, okay?")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
						}
						if ( chatval >= 1 & chatval <= 4 ) {
							gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 7
							if ( chatval == 1 ) {
								p = 909
								sex = 0
							}
							if ( chatval == 2 ) {
								p = 909
								sex = 1
							}
							if ( chatval == 3 ) {
								p = 910
								sex = 0
							}
							if ( chatval == 4 ) {
								p = 910
								sex = 1
							}
							if ( p == 909 ) {
								txt lang("この獣爪兵はとても好戦的なうえ、煙の中でも視野を確保できるようだ。", "This soldier is very aggressive and seems to be able to see its targets perfectly clearly through smoke.")
							}
							if ( p == 910 ) {
								txt lang("このワニは泳ぎがとても得意なようだ。", "This crocodile seems to be very good at swimming.")
							}
							flt
							novoidlv = 1
							characreate -1, p, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
							cdata(CDATA_SEX, rc) = sex
							wishally = 100
							gosub *add_ally
						}
					}
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 665 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("ここで何してるの？", "What are you doing here?")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("二番乗りじゃなくて？", "First? I'm not the second?")
					listmax++
					buff = lang("よくここまで辿り着いたな。" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、お前が一番乗りだ。", "Good to see you here, " + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". You're the first.")
					chatesc = 1
					gosub *chat_select
					listmax = 0
					buff = lang("俺は決戦因子の選定役だ。ずっと伏せてきたが、これが八柱神から与えられた使命でな。そのために強者の育成に力を尽くしてきた。", "I'm been hiding and watching to choose the Factors. It's the job delegated to me by the Eight Gods. That's why I've been training and helping the strong all the time.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) == 1 ) {
						gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 2
						listmax = 0
						buff = lang("しかし、まさか初対面の冒険者が来るとはな。俺の立場がないぞ…。", "But to think that an adventurer that I've never met before would be here. Impressive. You put my efforts to shame.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					listmax = 0
					buff = lang("すぐ下が神の間に続いている。神の間では記録ができないらしい。回復アイテム、補給用アイテム、装備の見直し、メンバーの入れ替え…入念に準備をするんだ。用意ができたら話しかけてくれ、案内する。", "God's Prison is right ahead. I've heard that you won't be able to save in there. Make sure that you have all your healing and buffing items, and give your equipment and party a lookover. You can't be over-prepared for what's ahead. When you're ready, come talk to me and I'll take you there.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 5 ) {
						gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 5
					}
					gdata(GDATA_FLAG_MAIN) = 670
					goto *chat_end
				}
				if ( gdata(GDATA_FLAG_MAIN) == 670 | gdata(GDATA_FLAG_MAIN) == 675 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("用意してくる", "I'm ready.")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("力の覚醒", "Awaken powers.")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("【テスカトリポカと戦う】", "<Fight Tezcatlipoca>")
					listmax++
					buff = lang("どうした？", "Yeah?")
					gosub *chat_select
					if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) < 5 ) {
						gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 5
					}
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("ここでやると色々と処理が面倒くさくてな。悪いがウールム・レゾンまで来てくれ。", "That's going to be too much trouble here. If you want that, you'll have to come with me to Ulm-Leson.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( chatval == 2 ) {
						listmax = 0
						buff = lang("ん？…まぁ、いいだろう。先にテスカトリポカについて話をしておこう。", "Huh? Well, sure, I guess. But I should tell you about Tezcatlipoca before that.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("テスカトリポカは争いを好む軍神だ。元々はエヘカトルやイツパロトルと同じ地域で生まれた神だったらしいが。エヘカトルとは長年の好敵手で、代が変わっても殺し合ったり作ったものを壊し合ったり時に怪物討伐で共闘したりを繰り返してきたとされる。", "Tezcatlipoca is the conflict-loving God of War. It's said that he was born in the same domain as Ehekatl and Itzpalt. Forever the rival of Ehekatl, they would fight and kill each other generation after generation, but also fight alongside each other to slay monsters.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("特に初代は狡猾な奴だったらしい。具体的には、初代の男のエヘカトルを騙して特殊な呪われた酒を飲ませ、エヘカトルが悪酔いして実の妹と気持ちいいこと漬けになったところで暴露して追放に追い込んだりな。", "It's said that the first generation of Tezcatlipoca was extremely cunning. He tricked the first Ehekatl into drinking a special cursed wine and made him have a little tail with his own little sister, then exposed the scandal to exile him.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("とにかく油断をするなということだ。単純な戦闘力だけではなく、騙されるという危険があるのだから。では、いくぞ。", "Anyway, you just have to be careful of him. Tezcatlipoca isn't just about combat prowess - you also have to be careful not to get tricked. Now, let's go.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						evadd EVENT_TEZCATLIPOCA
					}
					goto *chat_end
				}
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) == 5 ) {
			listmax = 0
			buff = lang("いいか" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、お前は強い。その辺りの雑魚では相手にならず、戦いに飢え、取り返しのつかないことをしてしまう日がいつか来るかもしれない。…こいつを渡しておく。", "You're strong, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ". The time may come when monsters are no longer any match for you and you lust to fight something stronger. When that time comes, use this.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			flt
			itemcreate -1, ITEM_ID_WAKE_UP_OF_NEFIA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			listmax = 0
			buff = lang("その宝玉は近辺のネフィアを活性化させ、内部のモンスターの力を引き出すことができる。役に立つかはわからんが、副次効果としてネフィアの核も手に入れることができるだろう。", "This gem awakens nearby Nefia and brings out the potential of the monsters within them. It also enables you to find the core of Nefia - though I don't know if you'll have any use for those.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…言っておくが、お前だけにこれを渡しているわけではないからな。勘違いするなよ。", "Don't think you're the only one I've given this to, though. Because you're not.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 6
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 365 ) {
			if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) <= 1 ) {
				listmax = 0
				buff = lang("…！なかなかいい面構えをしている。こんな強者と今まで巡り会えなかったとは、俺もついていなかったな。", "You! You've got the look of someone strong. Just my luck that it would take me this long to meet someone as strong as you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("名前は…そうか、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "というのか。お前がこの地で生き延び強くなれるよう、少し助力をしてやろう。", "Your name is...? " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "? I see. Well then, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", I can lend you a hand if you want so that you can survive in these lands.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そうそう。強者といえば、銀熊という異名の冒険者は特に見込みがあったんだが、止めるのも聞かずに大陸中央の巨城に乗り込んでしまった。いくら強くても人の話を聞かないやつは駄目だな。", "Speaking of strong, I saw a particularly promising adventurer called the Silver Bear. But she completely ignored me and headed straight into the Cradle of Chaos at the centre of the continent. Strength isn't going to make up for that lack of attention to others.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 5
			}
			if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) >= 2 & gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) <= 4 ) {
				listmax = 0
				buff = lang("こっちに来てから、お前にまた会える日を待っていたぞ。他にも見込みのある冒険者が数名いたが…どうもな。", "I've been waiting for you ever since I got here. There are a few other promising adventurers, but well...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("銀熊という異名の冒険者は特に見込みがあったんだが、俺の話も聞かずに大陸中央の巨城に乗り込んでしまった。いくら強くても人の話を聞かないやつは駄目だな。", "There was one called the Silver Bear, but she completely ignored me and headed straight into the Cradle of Chaos at the centre of the continent. Strength isn't going to make up for that lack of attention to others.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 5
			}
		}
		else {
			if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) == 1 ) {
				listmax = 0
				buff = lang("…！なかなかいい面構えをしている。俺の見込み違いでなければ、お前は相当強くなれそうだ。", "You! You've got the look of someone strong. And if I'm not wrong about you, you're going to get really strong.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("名前は…そうか、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "というのか。お前がこの地で生き延び強くなれるよう、少し助力をしてやろう。", "Your name is...? " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "? I see. Well then, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", I can lend you a hand if you want so that you can survive in these lands.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 2
			}
			if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) == 3 & gdata(GDATA_AREA) == AREA_RUOZA ) {
				listmax = 0
				buff = lang("おお" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、お前もこっちに来てたのか。修行したいなら大陸中央にある遺跡がお勧めだぞ。メルガスに駐屯しているイェルス軍が管理していて入れないかもしれないがな。", "Ah, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + ", I see you're here too. If you want to train yourself, you should try the Nefia in the centre of the continent. But you might not be able to get in because the Yerles army stationed at Melugas have it locked down.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) = 4
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("今はいい", "No, I'm good.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("冒険の知恵", "Wisdom for adventurers.")
		listmax++
		if ( gdata(GDATA_FLAG_LEOLD_LOCATION_STATE) >= 4 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("力の覚醒", "Awaken powers.")
			listmax++
		}
		if ( gdata(GDATA_FLAG_LEOLD_JOB_STAGE) <= 0 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("目標が欲しい", "I want a ordeal.")
			listmax++
		}
		if ( gdata(GDATA_FLAG_LEOLD_JOB_STAGE) == 2 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("目標を達成した", "I have achieved the ordeal.")
			listmax++
		}
		buff = lang("現状に甘んじることなく、心と体を鍛え更なる高みを目指すのだ。何か俺に手伝えることはあるか？", "Never be complacent. Keep training your body and mind and reach new heights. What can I do for you?")
		gosub *chat_select
		if ( chatval == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("食料について", "About food.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("仲間について", "About allies.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("アイテムについて", "About items.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("生活について", "About surviving.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("その他", "Other advice.")
			listmax++
			buff = lang("聞きたいことはどれだ？", "What do you want to know more about?")
			gosub *chat_select
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("成長にいいのはハーブや出来のいい料理だ。だがどうしても食料に困ったら、ワールドマップ上の平原でマップに入り野草や実を拾うといいだろう。ちなみに宿屋での食事は安価で腹いっぱい食えるが栄養はないからな。", "You should always eat herbs and high-quality cooked food to strengthen yourself. But if you don't have any food at all, you can just pick up the leaves and nuts in the wilderness on the world map. Having a meal at an inn is cheap and fills you right up, but it has no nutrition.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("ただしパンや旅糧、アピの実や芋などの一部を除いて食べ物は大体腐る。無理に腐ったものや呪われたものを食べても、吐いて余計に腹が減るし体も弱るぞ。", "Take note that most food will rot, with the exception being bread, rations, nuts, and imo. If you eat rotten or cursed food, you'll just throw up and get even hungrier. You'll get weaker, too.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("そして何度も吐いていると拒食症になってしまう。拒食症の間は何を食べても吐いてしまい、飲み物しか喉を通らない。こうなると充分な睡眠をとって自然治療するか、死ぬしかないから気を付けろ。", "Throw up too many times and you'll get anorexia. If you get it, you'll throw up everything you eat, so you'll have to sate your hunger with drinks like potions. It'll only go away with enough sleep or when you're dead.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("最初は弱い仲間も鍛えればかなり戦力になるぞ。お前の魅力が高くなればより多くの仲間を連れ歩けるだろう。", "If you train your allies, they'll become a force to be reckoned with even if they were weak to begin with. And the higher your Charisma, the more pets you can have with you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("仲間を戦闘に参加させるなら、いい食べ物やいいアイテムを渡して強化することは必須だ。ただし鉱石や結婚指輪など、一部のアイテムは渡すと返してもらえないぞ。", "If you want your allies to be able to fight, then you have to give them good food and good equipment to make them stronger. They're not going to give back some items like ores and engagement rings though, so be careful.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("あと、浮遊していない仲間は水を飲もうとして井戸に落ちて死んでしまうことがあるから気をつけろ。死んだり待機している仲間は酒場で復帰させることが出来るから覚えておけ。", "One more thing. Allies who don't float will sometimes fall into wells and die when they try to draw water from it. If they're dead, you can get them resurrected at the bar.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("鑑定は大事だぞ。知らずに呪われたアイテムを使用するとろくなことにならないからな。特に、呪われた装備を着けると突然テレポートしたり、魔物を呼び寄せてしまったりする。他人が呪われた装備をしている場合は解呪の巻物を渡したり解呪の杖を振ったりするといいだろう。", "Identifying items is important. Nothing good ever comes out of using a cursed item. If you wear cursed equipment, you will get teleported around and attract monsters. If someone else has cursed equipment, you can give them a scroll of uncurse or zap a rod of uncurse at them.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("火炎属性・冷気属性の攻撃を受けると、持っているアイテムの一部が破壊されることがある。それと耐酸処理がされていない装備は酸属性のダメージで劣化することがある。防ぐには対応したアイテムが必要だ。前者は耐性を整えることでも防げるがな。", "There's a chance that some items will get destroyed whenever you take fire or cold damage. Equipment that hasn't been treated with acidproof liquid will get degraded by acid attacks. You'll need to the appropriate items to prevent these - though the former can also be prevented if your resistances are high enough..")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("祝福された水を調合（Bキー）でアイテムに使用すると、使用されたアイテムが呪われていても祝福されたアイテムに変化するぞ。祝福されたアイテムは大抵強化されるが、効果が変わらないものもある。装備アイテムならば素材変化させるアイテムを使うと基本性能が大きく変わるぞ。冒険するならば、アイテムもいいものを揃えたいところだな。", "When you Mix(B key) blessed water it turns an item into a blessed one even if it's cursed. Most of the time blessed items are stronger, but some things don't change. If you use a change material item on an piece of equipment the base stats change. If you're going to travel, best to find good stuff to use.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 3 ) {
				listmax = 0
				buff = lang("外に出て＊キーで辺りを見回してみろ。大体の地形は感じ取れるはずだ。大抵の街や施設では定期的に掃除と維持修復が行われている。長くアイテムを置いておくと片づけられてしまうから注意しろ。逆に、破壊するともう元に戻らない施設もあるから慎重にな。", "Press the * key on the outer world map.  You can get a feel for your surroundings. Towns that have government boards are cleaned and repaired at intervals.  You'll lose items you leave laying around for too long. Other places, like this one, on the other hand...they won't be fixed if you destroy them.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("街や演奏会場にはカジノ系アイテムが置いてあることがある。これを使うとブラックジャックをプレイ出来るぞ。景品に「エーテル抗体のポーション」など貴重なアイテムがあるが、ディーラーは手強い。相当な運かイカサマ技術がなければ苦戦するだろうな。", "There are Casino related items in towns and party halls.  If you use this you can play some blackjack. You can win prizes like potions of cure corruption but the dealer is tough.  It'll be a tough going without a lot of luck or cheating.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("街では依頼を受けられるが、アイテムが揃うまでは討伐依頼は避け、他の簡単な依頼を受けるのが賢明だろう。ちなみに配達依頼は簡単だが失敗すると大きな罪に問われる。期限に気をつけることだ。罪を負うと負のカルマが蓄積していき、-31になる頃には指名手配されているだろう。", "You can take up quests in towns, but smart to pass on the extermination quests until you have the gear and just do the simple quests. Delivery quests are easy by the way, but failing incurs big penalties.  Be careful of the time limit. The more crimes you commit the lower your Karma goes, you'll be a wanted criminal if it goes below -31.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 4 ) {
				listmax = 0
				buff = lang("基本的に状態異常は時間経過や回復魔法・アイテムで少しずつ治っていく。しかし寄生の場合は毒薬や染料、硫酸を飲めば治療することが出来る。他人が寄生されている場合でも、それらを渡せば自分から飲んでくれるはずだ。", "Basically, status effects can be restored by either waiting for time to pass or using restorative magic and items to recover bit by bit. Except being impregnated you need to drink poison, dyes or acids to cure it. If others are infect as well, if you give it to them they'll drink it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("それと、他の冒険者は俺と違って執念深い。うっかり怒らせると、倒してもマップを変えても出会うたびに襲い掛かってくるだろう。そうなるとアイテムや魔法で変装するしかないぞ。仲が悪いと自宅に放火されたりするらしいな。", "Also, unlike me, other Adventurers are vicious.  If you somehow make them angry, they'll still come after you even after defeating them or changing maps. If that happens the only thing you can do is disguise yourself with magic or objects.  They'll set fire to your house if they aren't your friend.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("成長には休息も重要だ。冒険が一段落したらなるべく質のいいベッドで寝るといいだろう。ただしベッドは「使わなければ」意味がないぞ！寝てる間に装備が呪われたりしたら、すぐに癒し手のところへ行き解呪してもらうんだな。", "Resting is important to growth.  After you complete an adventure you shoud sleep in the best you can find.  But it's meaningless unless you use it! If your equipment is cursed while you sleep, you should quickly get over to a healer to uncurse it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			listmax = 0
			buff = lang("まぁ、冒険者ギルドに所属しているならお前もマニュアルを持っているだろう。他にも色々と参考になることが書いてあるから、Shiftキーを押しながら？で読み返しておけ。", "Well, if you're a member of the Adventurer's guild then you probably already have a manual. There's a bunch of other stuff to check out written there, so hold down the shift key and press ? to read over it.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 3 ) {
			if ( gdata(GDATA_FLAG_LEOLD_JOB_TIER) < 15 ) {
				gdata(GDATA_FLAG_LEOLD_JOB_TIER) = 15
			}
			listmax = 0
			buff = lang("そうだな…。ひとまずレベル" + gdata(GDATA_FLAG_LEOLD_JOB_TIER) + "以上の敵モンスターを倒すことを目標にしてみてくれ。敵以外はノーカウントだからな。", "All right. Try to beat the enemy monster above the level " + gdata(GDATA_FLAG_LEOLD_JOB_TIER) + ". Non-enemies are no-count.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_LEOLD_JOB_STAGE) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( chatval == 4 ) {
			gdata(GDATA_FLAG_LEOLD_JOB_TIER) += 5
			listmax = 0
			buff = lang("よしよし…よくやったじゃないか！祝いにこれをやろう。", "Hmm...you did well! I give items to celebration.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			flt
			itemcreate -1, ITEM_ID_MUSIC_TICKET, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), limit(gdata(GDATA_FLAG_LEOLD_JOB_TIER) * 5 + 100, 100, 3000)
			flt
			itemcreate -1, ITEM_ID_BRONZE_COIN, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			snd SOUNDLIST_COMPLETE1
			if ( gdata(GDATA_FLAG_LEOLD_JOB_TIER) >= 2005 ) {
				listmax = 0
				buff = lang("もうお前はレベルがどうとかいう域には収まらない。これ以上の目標は、お前が自分で見つけていくんだ。", "You were beyond the area to judge by level. I hope that you will find more ordeals yourself.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				gdata(GDATA_FLAG_LEOLD_JOB_STAGE) = 1000
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
				goto *chat_end
			}
			listmax = 0
			buff = lang("次はレベル" + gdata(GDATA_FLAG_LEOLD_JOB_TIER) + "以上の敵モンスターを倒すことを目標にしてみてくれ。急がなくてもいいからな。", "Now try to beat the enemy monster above the level " + gdata(GDATA_FLAG_LEOLD_JOB_TIER) + ". Non-enemies are no -count.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_LEOLD_JOB_STAGE) = 1
			snd SOUNDLIST_WRITE1
			txtef COLOR_GREEN
			txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			goto *chat_end
		}
		if ( chatval == 2 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("自分の力を覚醒", "I want to awaken my own powers.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("仲間の力を覚醒", "I want to awaken an ally's power.")
			listmax++
			buff = lang("強敵との死闘の中でこそ得られる特別な経験もある。しかし大抵そのままでは形にならず、記憶の海に埋もれてしまうものだ。そんな経験を組み合わせて俺が新たな力として覚醒させてやろう。", "There's a special kind of experience that you gain only through life-and-death battles with the strong. Most of it never takes shape and gets lost in the sea of memories. If you want, I can put that together for you to let you awaken to another power.")
			gosub *chat_select
			if ( chatval == 0 ) {
				goto *chat_unique_leold_awaken
				goto *chat_end
			}
			if ( chatval == 1 ) {
				list(0, listmax) = 0
				listn(0, listmax) = lang("速度上昇", "Increase speed.")
				listmax++
				list(0, listmax) = 5
				listn(0, listmax) = lang("生命力上昇", "Increase life.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("フィート取得", "Acquire feats.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("魔法や技能の会得", "Learn spells and special abilities.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("戦闘スタイル変更・品質上昇", "Change battle style/increase character quality.")
				listmax++
				list(0, listmax) = 4
				listn(0, listmax) = lang("スキルボーナスの獲得", "Convert into bonus points.")
				listmax++
				buff = lang("どういった力に変えるんだ？", "What form do you want your powers to take?")
				gosub *chat_select
				if ( chatval == 5 ) {
					listmax = 0
					buff = lang("よし、初期生命力・マナや資質に応じたAPを消費して、生命力を10上げるぞ。誰の生命力を上昇させるんだ？", "Alright, I can raise an ally's life by 10 by consuming AP depending on initial life and bit. Who's it gonna be?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					leoval = -2
					goto *chat_unique_leold_done
					goto *chat_end
				}
				if ( chatval == 4 ) {
					listmax = 0
					buff = lang("AP500をスキルボーナス100に変換する。誰を対象にするんだ？", "100 bonus points will cost 500AP. Who's it gonna be?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					leoval = -1
					leoap = 500
					goto *chat_unique_leold_done
					goto *chat_end
				}
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("よし、初期速度に応じたAPを消費して速度を30上げるぞ。誰の速度を上昇させるんだ？", "Alright, I can raise an ally's speed by 30 by consuming AP depending on initial speed. Who's it gonna be?")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					leoval = 0
					goto *chat_unique_leold_done
					goto *chat_end
				}
				if ( chatval == 1 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("魅惑(消費AP1500)[直接攻撃してきた敵が朦朧]", "Charm (1500AP) [Dim enemies attacking at close range]")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("底力(消費AP1500)[HP減るほど最終与ダメージ上昇]", "Brave (1500AP) [Increase damage dealt as HP decreases]")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("意地(消費AP1800)[HP減るほど回避・軽減率上昇]", "Obstinate (1800AP) [Increase evasion, damage mitigation as HP decreases]")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("障壁(消費AP2000)[ダメージ分MP消費し攻撃無効化]", "Barrier (2000AP) [Negate attacks with equal amount of MP]")
					listmax++
					buff = lang("どのフィートを取得するんだ？", "Which feat do you want?")
					gosub *chat_select
					if ( chatval == 0 ) {
						leoap = 1500
						leoval = 101
						goto *chat_unique_leold_done
					}
					if ( chatval == 1 ) {
						leoap = 1800
						leoval = 102
						goto *chat_unique_leold_done
					}
					if ( chatval == 2 ) {
						leoap = 1500
						leoval = 103
						goto *chat_unique_leold_done
					}
					if ( chatval == 3 ) {
						leoap = 2000
						leoval = 104
						goto *chat_unique_leold_done
					}
					goto *chat_end
				}
				if ( chatval == 2 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("罵倒(消費AP100)", "Insult(100AP)")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("魔力の集積(消費AP600)", "Crystal Spear(600AP)")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("空間歪曲(消費AP100)", "Dimensional Move(100AP)")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("挑発(消費AP200)", "Provoke(200AP)")
					listmax++
					list(0, listmax) = 4
					listn(0, listmax) = lang("チャージ(消費AP1000)", "Charge Attack(1000AP)")
					listmax++
					list(0, listmax) = 5
					listn(0, listmax) = lang("タクティカルアーツ(消費AP600)", "Tactical Martial Arts(600AP)")
					listmax++
					list(0, listmax) = 6
					listn(0, listmax) = lang("タクティカルアタック(消費AP400)", "Tactical Attack(400AP)")
					listmax++
					list(0, listmax) = 7
					listn(0, listmax) = lang("タクティカルヒール(消費AP500)", "Tactical Heal(500AP)")
					listmax++
					list(0, listmax) = 8
					listn(0, listmax) = lang("タクティカルカース(消費AP500)", "Tactical Curse(500AP)")
					listmax++
					list(0, listmax) = 10
					listn(0, listmax) = lang("タクティカルスロー(消費AP500)", "Tactical Throw(500AP)")
					listmax++
					list(0, listmax) = 9
					listn(0, listmax) = lang("零の間撃(消費AP500)", "Zero shoot(500AP)")
					listmax++
					list(0, listmax) = 11
					listn(0, listmax) = lang("可変放射(消費AP400)", "Variable Breath(300AP)")
					listmax++
					list(0, listmax) = 12
					listn(0, listmax) = lang("範囲魔法可変術式(消費AP800)", "Variable Storm(800AP)")
					listmax++
					buff = lang("どの魔法や技能を覚えさせるんだ？", "Which spells and abilities do you want your ally to learn?")
					gosub *chat_select
					if ( chatval == 0 ) {
						leoval = 1
						leoap = 100
						goto *chat_unique_leold_done
					}
					if ( chatval == 1 ) {
						leoval = 2
						leoap = 600
						goto *chat_unique_leold_done
					}
					if ( chatval == 2 ) {
						leoval = 4
						leoap = 100
						goto *chat_unique_leold_done
					}
					if ( chatval == 3 ) {
						leoval = 14
						leoap = 200
						goto *chat_unique_leold_done
					}
					if ( chatval == 4 ) {
						leoval = 7
						leoap = 1000
						goto *chat_unique_leold_done
					}
					if ( chatval == 5 ) {
						leoval = 6
						leoap = 600
						goto *chat_unique_leold_done
					}
					if ( chatval == 6 ) {
						leoval = 5
						leoap = 400
						goto *chat_unique_leold_done
					}
					if ( chatval == 7 ) {
						leoval = 3
						leoap = 500
						goto *chat_unique_leold_done
					}
					if ( chatval == 8 ) {
						leoval = 15
						leoap = 500
						goto *chat_unique_leold_done
					}
					if ( chatval == 9 ) {
						leoval = 20
						leoap = 500
						goto *chat_unique_leold_done
					}
					if ( chatval == 10 ) {
						leoval = 21
						leoap = 500
						goto *chat_unique_leold_done
					}
					if ( chatval == 11 ) {
						leoval = 16
						leoap = 400
						goto *chat_unique_leold_done
					}
					if ( chatval == 12 ) {
						leoval = 17
						leoap = 800
						goto *chat_unique_leold_done
					}
					goto *chat_end
				}
				if ( chatval == 3 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("近接攻撃頻度増加(消費AP30)", "Increased frequency of melee attack(30AP)")
					listmax++
					list(0, listmax) = 1
					listn(0, listmax) = lang("遠隔攻撃頻度増加(消費AP30)", "Increased frequency of ranged attack(30AP)")
					listmax++
					list(0, listmax) = 9
					listn(0, listmax) = lang("物理攻撃矢魔法化(消費AP200)", "Use dart spell instead of regular attack(200AP)")
					listmax++
					list(0, listmax) = 2
					listn(0, listmax) = lang("適正距離＋1(消費AP10)", "Preferred distance +1(10AP)")
					listmax++
					list(0, listmax) = 3
					listn(0, listmax) = lang("適正距離−1(消費AP10)", "Preferred distance -1(10AP)")
					listmax++
					list(0, listmax) = 4
					listn(0, listmax) = lang("移動確率＋10％(消費AP5)", "Movement probability -10%(5AP)")
					listmax++
					list(0, listmax) = 5
					listn(0, listmax) = lang("移動確率−10％(消費AP5)", "Movement probability -10%(5AP)")
					listmax++
					list(0, listmax) = 6
					listn(0, listmax) = lang("魔法の使用封印/解除(消費AP20)", "Allow/disallow spell usage(20AP)")
					listmax++
					list(0, listmax) = 7
					listn(0, listmax) = lang("技能の使用封印/解除(消費AP20)", "Allow/disallow ability usage(20AP)")
					listmax++
					list(0, listmax) = 8
					listn(0, listmax) = lang("キャラ品質上昇(消費AP1000)", "Increase character quality(1000AP)")
					listmax++
					buff = lang("どう変更させるんだ？ちなみにこれらは、後で打ち消しても前に消費したAPは戻ってこない。気をつけろ。", "How do you want them to change? Careful now, AP spent on these will never get refunded even if you pick the same option again.")
					gosub *chat_select
					if ( chatval == 0 ) {
						leoval = 8
						leoap = 30
						goto *chat_unique_leold_done
					}
					if ( chatval == 1 ) {
						leoval = 9
						leoap = 30
						goto *chat_unique_leold_done
					}
					if ( chatval == 2 ) {
						leoval = 1000
						leoap = 10
						goto *chat_unique_leold_done
					}
					if ( chatval == 3 ) {
						leoval = 1001
						leoap = 10
						goto *chat_unique_leold_done
					}
					if ( chatval == 4 ) {
						leoval = 1002
						leoap = 5
						goto *chat_unique_leold_done
					}
					if ( chatval == 5 ) {
						leoval = 1003
						leoap = 5
						goto *chat_unique_leold_done
					}
					if ( chatval == 6 ) {
						leoval = 1004
						leoap = 20
						goto *chat_unique_leold_done
					}
					if ( chatval == 7 ) {
						leoval = 1005
						leoap = 20
						goto *chat_unique_leold_done
					}
					if ( chatval == 8 ) {
						leoval = 1006
						leoap = 1000
						goto *chat_unique_leold_done
					}
					if ( chatval == 9 ) {
						leoval = 10
						leoap = 200
						goto *chat_unique_leold_done
					}
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
		goto *chat_unique_SWEND1
	}

*chat_unique_SWEND1
	listmax = 0
	buff = lang("…(あなたの言葉を聞いていない)", "(" + he(tc) + " ignores you...)")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strbye
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	goto *chat_end

*chat_unique_leold_limb
	if ( gdata(GDATA_FLAG_DIALOG_LEOLD) == 0 ) {
		gdata(GDATA_FLAG_DIALOG_LEOLD) = 1
		listmax = 0
		buff = lang("なに…装備部位を、か？ああ、できないことはないぞ！俺が以前研究していた生体ハードポイント理論を応用すれば充分可能だ…未完成の技術だがな。生体ハードポイントの研究は特定部位の支持力を強化し必要に応じて義手や義足、義頭のようなアタッチメントを介することで本来は装備できないものも装着できるようにすることを目的としていた。しかし、この地で用意できる機材では制御がどうしても不安定になってしまうために中止となってしまってな。本体の細胞から新しく生やした方がよほど確実でコストも抑えられるし拒絶反応もないというのも大きな理由だ。一応、アタッチメントを用いる場合は本体と神経接続するため、本来の肉体に装備したものと遜色なく操れるようになるが、逆に取り外しを困難としてしまっている。理論上は…おっと、すまん。このまま解説すると長くなってしまうな。", "By the way, there internal is  covered by undeads.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	list(0, listmax) = 0
	listn(0, listmax) = lang("やめとく", "No problem.")
	listmax++
	list(0, listmax) = 1
	listn(0, listmax) = lang("自分が試す", "No problem.")
	listmax++
	list(0, listmax) = 2
	listn(0, listmax) = lang("仲間が試す", "No problem.")
	listmax++
	buff = lang("それで…誰が試すんだ？", "By the way, there internal is  covered by undeads.")
	gosub *chat_select
	c = -1
	if ( chatval == 1 ) {
		c = 0
	}
	if ( chatval == 2 ) {
		ikoru = 1000
		allyctrl = 3
		gosub *com_ally
		ikoru = 0
		if ( stat != (-1) ) {
			c = stat
		}
		listmax = 0
	}
	if ( c >= 0 ) {
		r1 = c
		tuikabui = 0
		repeat 30, 100
			if ( cdata(cnt, r1) != 0 ) {
				tuikabui++
			}
		loop
		if ( tuikabui > 14 ) {
			txt lang("「これ以上は装着するところが無いな」", "Sorry, this is untranslated sentence.")
			goto *chat_end
		}
		tuikabui = 0
		cost1 = sorg(SKILL_ATTR_LIFE, r1) / 30 + 5
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost1 /= 4
			}
		}
		cost2 = sorg(SKILL_ATTR_LIFE, r1) / 30 + 13
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost2 /= 4
			}
		}
		cost3 = sorg(SKILL_ATTR_LIFE, r1) / 30 + 7
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost3 /= 4
			}
		}
		cost4 = sorg(SKILL_ATTR_LIFE, r1) / 25 + 20
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost4 /= 2
			}
		}
		cost5 = sorg(SKILL_ATTR_LIFE, r1) / 20 + 22
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost5 /= 4
			}
		}
		cost6 = sorg(SKILL_ATTR_LIFE, r1) / 30 + 13
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost6 /= 4
			}
		}
		cost7 = sorg(SKILL_ATTR_LIFE, r1) / 25 + 15
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost7 /= 4
			}
		}
		cost8 = sorg(SKILL_ATTR_LIFE, r1) / 30 + 3
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost8 /= 4
			}
		}
		cost9 = sorg(SKILL_ATTR_LIFE, r1) / 30 + 4
		if ( r1 < 16 ) {
			if ( r1 != 0 ) {
				cost9 /= 4
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("やっぱりやめる", "Cancel")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("頭（減少生命力" + cost1 + ")", "Head(Down " + cost1 + "Life)")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("首（減少生命力" + cost2 + ")", "Neck(Down " + cost2 + "Life)")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("背中（減少生命力" + cost3 + ")", "Back(Down " + cost3 + "Life)")
		listmax++
		list(0, listmax) = 4
		listn(0, listmax) = lang("胴体（減少生命力" + cost4 + ")", "Chest(Down " + cost4 + "Life)")
		listmax++
		list(0, listmax) = 5
		listn(0, listmax) = lang("手（減少生命力" + cost5 + ")", "Hand(Down " + cost5 + "Life)")
		listmax++
		list(0, listmax) = 6
		listn(0, listmax) = lang("指（減少生命力" + cost6 + ")", "Finger(Down " + cost6 + "Life)")
		listmax++
		list(0, listmax) = 7
		listn(0, listmax) = lang("腕（減少生命力" + cost7 + ")", "Arm(Down " + cost7 + "Life)")
		listmax++
		list(0, listmax) = 8
		listn(0, listmax) = lang("腰（減少生命力" + cost8 + ")", "Waist(Down " + cost8 + "Life)")
		listmax++
		list(0, listmax) = 9
		listn(0, listmax) = lang("足（減少生命力" + cost9 + ")", "Leg(Down " + cost9 + "Life)")
		listmax++
		buff = lang("装備可能にする箇所ごとに、肉体への負担が違ってくる…よく考えてくれ。選んだらすぐに取り掛かるぞ。", "What you've got to make the change?")
		gosub *chat_select
		if ( chatval > 0 ) {
			gbui = chatval
			if ( gbui == 1 ) {
				gcost = cost1
				cost1 = 0
			}
			if ( gbui == 2 ) {
				gcost = cost2
				cost3 = 0
			}
			if ( gbui == 3 ) {
				gcost = cost3
				cost3 = 0
			}
			if ( gbui == 4 ) {
				gcost = cost4
				cost4 = 0
			}
			if ( gbui == 5 ) {
				gcost = cost5
				cost5 = 0
			}
			if ( gbui == 6 ) {
				gcost = cost6
				cost6 = 0
			}
			if ( gbui == 7 ) {
				gcost = cost7
				cost7 = 0
			}
			if ( gbui == 8 ) {
				gcost = cost8
				cost8 = 0
			}
			if ( gbui == 9 ) {
				gcost = cost9
				cost9 = 0
			}
			if ( sorg(SKILL_ATTR_LIFE, r1) <= gcost ) {
				txt lang("「こんな生命力では耐えられんぞ。中止だ中止！」", "Sorry, this is untranslated sentence.")
				goto *chat_end
			}
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
			gsel BUFFER_BUF
			pos 0, 0
			picload exedir + "graphic\\face1" + devfile + ".bmp", 1
			gsel BUFFER_SCREEN
			skillmod SKILL_ATTR_LIFE, r1, (-1000) * gcost
			gcost = 0
			repeat 30, 100
				if ( cdata(cnt, r1) != 0 ) {
					continue
				}
				if ( gbui == 1 ) {
					cdata(cnt, r1) = 1 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(1) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(1) + "!")
						break
					}
				}
				if ( gbui == 2 ) {
					cdata(cnt, r1) = 2 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(2) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(2) + "!")
						break
					}
				}
				if ( gbui == 3 ) {
					cdata(cnt, r1) = 3 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(3) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(3) + "!")
						break
					}
				}
				if ( gbui == 4 ) {
					cdata(cnt, r1) = 4 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(4) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(4) + "!")
						break
					}
				}
				if ( gbui == 5 ) {
					cdata(cnt, r1) = 5 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(5) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(5) + "!")
						break
					}
				}
				if ( gbui == 6 ) {
					cdata(cnt, r1) = 6 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(6) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(6) + "!")
						break
					}
				}
				if ( gbui == 7 ) {
					cdata(cnt, r1) = 7 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(7) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(7) + "!")
						break
					}
				}
				if ( gbui == 8 ) {
					cdata(cnt, r1) = 8 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(8) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(8) + "!")
						break
					}
				}
				if ( gbui == 9 ) {
					cdata(cnt, r1) = 9 * 10000
					if ( cm ) {
						break
					}
					else {
						txt lang(name(r1) + "は新たな" + bodyn(9) + "を獲得した！", name(r1) + " grow" + _s(r1) + " a new " + bodyn(9) + "!")
						break
					}
				}
				break
			loop
			gbui = 0
			gosub *calcBodyFix
			r1 = c
			gosub *charaRefresh
			cbitmod CHARA_BIT_ALTERED_LIMBS, c, TRUE
			snd SOUNDLIST_COMPLETE1
			txt lang("「完了だ。馴染み具合にも問題はないだろう」", "Sorry, this is untranslated sentence.")
		}
	}
	goto *chat_end

*label_6425
	pos 0, 0
	gmode 0
	gcopy selbufb, 0, 0, windoww, windowh
	redraw 0
	cs = 0
	cs_bk = -1
	page = 0
	pagesize = 0
	gsel selbufh
	pos 0, 0
	picload exedir + "graphic\\bg37.bmp", 1
	pos 0, 0
	gzoom windoww, windowh - 88, selbufh, 0, 0, 640, 480
	gsel BUFFER_SCREEN
	windowshadow = 1

*com_change_gamemode_loop
	if ( 1 == 0 ) {
		goto *com_change_gamemode_finish
	}
	redraw 0
	s = lang("モードの変更（一方通行）", "Balance Change(one-way) "), strhint3b
	display_window (windoww - 680) / 2 + inf_screenx, winposy(500, 1) + 20, 680, 400
	display_topic lang("モードの候補", "Mode"), wx + 28, wy + 30
	display_topic lang("各モードの説明", "Detail"), wx + 178, wy + 30
	s = lang("吸い込まれそうな瞳孔の奥に、別の宇宙が見える…。", "You can see another universe in the back of her eyes...")
	gosub *screen_drawMsg2
	listn(0, 0) = lang("Essential", "Essential")
	listn(0, 1) = lang("Loss", "Loss")
	listn(0, 2) = lang("Overdose", "Overdose")
	listn(0, 3) = lang("Natural", "Natural")
	listn(0, 4) = lang("Abnormal", "Abnormal")
	listn(0, 5) = lang("Purge", "Purge")
	listn(0, 6) = lang("キャンセル", " *Cancel* ")
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	repeat 7
		key_list(cnt) = key_select(cnt)
		keyrange = cnt + 1
		pos wx + 38, wy + 66 + cnt * 19 - 2
		gcopy BUFFER_INF, cnt * 24 + 72, 30, 24, 18
		cs_list listn(0, cnt), wx + 64, wy + 66 + cnt * 19 - 1, 19
	loop
	font lang(cfg_font1, cfg_font2), 13 - en * 2, 0
	if ( listn(0, cs) == listn(0, 0) ) {
		s = lang("★Essentialモードはこのソフトの本質的なモードで、", "Essential is normal mode.")
		pos wx + 165, wy + 66
		mes s
		s = lang("他のゲームモードは全てこれがベースになっています。", " ")
		pos wx + 165, wy + 81
		mes s
		s = lang("システム面の穴を埋め同格要素の差を縮める方向で調整が常に行われており、", ".")
		pos wx + 165, wy + 96
		mes s
		s = lang("バランス変動が多い反面、ロールプレイとある程度の実益を両立したい人、", " ")
		pos wx + 165, wy + 111
		mes s
		s = lang("育成に力を入れたい人におすすめです。", " ")
		pos wx + 165, wy + 126
		mes s
		if ( gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			s = lang("【選択できません】", "Not selectable")
			pos wx + 200, wy + 156
			mes s
		}
	}
	if ( listn(0, cs) == listn(0, 1) ) {
		s = lang("★Lossモードはデータ喪失の危険性が非常に高いモードです。", "Loss is another hard mode.")
		pos wx + 165, wy + 66
		mes s
		s = lang("一度死ねば、這い上がることはできずそこで終了。", "If you die even once, then you will not be able to revive.")
		pos wx + 165, wy + 81
		mes s
		s = lang("そのセーブデータを読み込めなくなるのが特徴です。", "You will not read again the save data.")
		pos wx + 165, wy + 96
		mes s
		s = lang("さらにEscキーかSでのセーブで終了しないとロード時にペナルティがあります。", "If you exit without saving of ESC key, ")
		pos wx + 165, wy + 111
		mes s
		s = lang("ローグライク本格派の人以外にはおすすめできません。", "then you will receive a penalty at load time.")
		pos wx + 165, wy + 126
		mes s
		if ( gdata(GDATA_FLAG_GAME_MODE) == 3 | gdata(GDATA_FLAG_GAME_MODE) == 4 | gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			s = lang("【選択できません】", "Not selectable")
			pos wx + 200, wy + 156
			mes s
		}
	}
	if ( listn(0, cs) == listn(0, 2) ) {
		s = lang("★Overdoseモードは基本部分はEssentialそのままに、得られる", "Overdose is easy mode.")
		pos wx + 165, wy + 66
		mes s
		s = lang("能力経験値、スキル経験値、魔法経験値が大幅増加する超低難度モードです。", "You can get skill experience 3 times by default.")
		pos wx + 165, wy + 81
		mes s
		s = lang("デフォルトの増加倍率は3倍ですが、とある場所で最大30倍にまで変更可能。", "The magnification can be changed up to 30 times in a certain place.")
		pos wx + 165, wy + 96
		mes s
		s = lang("じっくり楽しまずにさっさとクリアしたい、とにかく楽をしたいなど、", "However score becomes 0 in the clear.")
		pos wx + 165, wy + 111
		mes s
		s = lang("飽きが早くなることを問題としない人にのみ、おすすめです。", "Also, you can not make gene in Overdose mode.")
		pos wx + 165, wy + 126
		mes s
		s = lang("この成長速度に慣れると他では満足できなくなる恐れがあります。", ".")
		pos wx + 165, wy + 141
		mes s
		s = lang("遺伝子を残すこと(アイテム引継ぎ2週目)ができず、スコアも0になります。", ".")
		pos wx + 165, wy + 155
		mes s
		if ( gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			s = lang("【選択できません】", "Not selectable")
			pos wx + 200, wy + 185
			mes s
		}
	}
	if ( listn(0, cs) == listn(0, 3) ) {
		s = lang("★Naturalモードは基本部分はEssentialですが、", "Natural is no reload mode.")
		pos wx + 165, wy + 66
		mes s
		s = lang("EscキーかSでのセーブで終了しないとロード時にペナルティがあります。", "If you exit without saving of ESC key, ")
		pos wx + 165, wy + 81
		mes s
		s = lang("それ以外の点ではEssentialと何も変わりません。", "then you will receive a penalty at load time.")
		pos wx + 165, wy + 96
		mes s
		s = lang("悪い結果が出てもやり直しをせずそのまま受け止められる人、", "Specification of others are the same as Essential.")
		pos wx + 165, wy + 111
		mes s
		s = lang("適度な難しさを求める人におすすめです。", ".")
		pos wx + 165, wy + 126
		mes s
		if ( gdata(GDATA_FLAG_GAME_MODE) == 4 | gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			s = lang("【選択できません】", "Not selectable")
			pos wx + 200, wy + 156
			mes s
		}
	}
	if ( listn(0, cs) == listn(0, 4) ) {
		s = lang("★Abnormalモードは死亡時のペナルティの増加やセーブロードの制限、", "Abnormal is hard mode.")
		pos wx + 165, wy + 66
		mes s
		s = lang("その他さまざまな危険要素が増えている異常なモードです。地道な積み重ねや", "You will not be able to load and save free.")
		pos wx + 165, wy + 81
		mes s
		s = lang("慎重なプレイングを楽しめる人、もしくは貴重品を失うスリルやショックすら", "In addition, you received various restrictions.")
		pos wx + 165, wy + 96
		mes s
		s = lang("楽しめる変態な人におすすめです。特にメリットはありませんが、", "Score is multiplied by 20 in the clear.")
		pos wx + 165, wy + 111
		mes s
		s = lang("クリア時のスコアが20倍になります。", "If you exit without saving of ESC key, ")
		pos wx + 165, wy + 126
		mes s
		s = lang("EscキーかSでのセーブで終了しないとロード時にペナルティあり。", "then you will receive a penalty at load time.")
		pos wx + 165, wy + 141
		mes s
		if ( gdata(GDATA_FLAG_GAME_MODE) == 1 | gdata(GDATA_FLAG_GAME_MODE) == 3 | gdata(GDATA_FLAG_GAME_MODE) == 4 | gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			s = lang("【選択できません】", "Not selectable")
			pos wx + 200, wy + 171
			mes s
		}
	}
	if ( listn(0, cs) == listn(0, 5) ) {
		s = lang("★Purgeモードはバランス調整から切り離されたゲームモードです。", "Purge is a mode separated from balance adjustment.")
		pos wx + 165, wy + 66
		mes s
		s = lang("遺伝子を残すこと(アイテム引継ぎ2週目)ができず、スコアも0になります。", "Score becomes 0 in the clear and you can not make gene.")
		pos wx + 165, wy + 81
		mes s
		s = lang("「都合のいい結果が出るまでひたすらやり直す作業こそが楽しい」", "For people who want to redo until good results are obtained.")
		pos wx + 165, wy + 96
		mes s
		s = lang("「ゲームとは放置で自動カンストするくらい楽なほうが没入できるもの」", "For people who want to grow the character automatically.")
		pos wx + 165, wy + 111
		mes s
		s = lang("「バランスブレイカーによって他の要素がゴミ同然になっていても、", "For people who think that balancebreaker should not be fixed if it is an offline game.")
		pos wx + 165, wy + 126
		mes s
		s = lang("オフラインゲームならそのままにしておくべき」", "...Even if other elements become garbage by the balance breaker.")
		pos wx + 165, wy + 141
		mes s
		s = lang("「むしろバランスブレイカーがあって使うかどうかは選ばせるのが理想」", "For people who think that geme should let the player choose whether to use a balance breaker.")
		pos wx + 165, wy + 156
		mes s
		s = lang("「強敵との死闘なんて楽しくない。ストレスフリーに蹴散らす戦闘が至高」", "For people thinks that battle overrunning enemies without struggle is supreme.")
		pos wx + 165, wy + 171
		mes s
		s = lang("「簡単にクリアできないと悔しいじゃないですか」", "For people who can't enjoy game unless they can clear it very easily.")
		pos wx + 165, wy + 186
		mes s
		s = lang("といった価値観の人は、絶対にこのモード以外を選ばないでください。", "If you want to enjoy the game balance, please do not choose this mode.")
		pos wx + 165, wy + 201
		mes s
		if ( gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			s = lang("【選択できません】", "Not selectable")
			pos wx + 200, wy + 216
			mes s
		}
	}
	if ( listn(0, cs) == listn(0, 6) ) {
		s = lang("自分が自分でいられなくなるような気がして、ぞくりと震えた。", "You trembled with feeling you could not stay by yourself.")
		pos wx + 165, wy + 66
		mes s
		s = lang("そっと目をそらす…。", "You distract your eyes...")
		pos wx + 165, wy + 81
		mes s
	}
	cs_bk = cs
	redraw 1
	await cfg_wait1
	key_check
	cursor_check
	if ( key == key_select(0) ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
		}
		else {
			gdata(GDATA_FLAG_GAME_MODE) = 0
			goto *com_change_gamemode_finish
		}
	}
	if ( key == key_select(1) ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 3 | gdata(GDATA_FLAG_GAME_MODE) == 4 | gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
		}
		else {
			gdata(GDATA_FLAG_GAME_MODE) = 3
			goto *com_change_gamemode_finish
		}
	}
	if ( key == key_select(2) ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
		}
		else {
			gdata(GDATA_FLAG_GAME_MODE) = 2
			goto *com_change_gamemode_finish
		}
	}
	if ( key == key_select(3) ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 4 | gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
		}
		else {
			gdata(GDATA_FLAG_GAME_MODE) = 4
			goto *com_change_gamemode_finish
		}
	}
	if ( key == key_select(4) ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 1 | gdata(GDATA_FLAG_GAME_MODE) == 3 | gdata(GDATA_FLAG_GAME_MODE) == 4 | gdata(GDATA_FLAG_GAME_MODE) == 0 | gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
		}
		else {
			gdata(GDATA_FLAG_GAME_MODE) = 1
			goto *com_change_gamemode_finish
		}
	}
	if ( key == key_select(5) ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
		}
		else {
			gdata(GDATA_FLAG_GAME_MODE) = 5
			goto *com_change_gamemode_finish
		}
	}
	if ( key == key_select(6) ) {
		return
	}
	if ( key == key_cancel ) {
		return
	}
	goto *com_change_gamemode_loop

*com_change_gamemode_finish
	snd SOUNDLIST_OK1
	redraw 0
	gosub *screen_draw
	gosub *screen_fade
	gsel BUFFER_BUF
	pos 0, 0
	picload exedir + "graphic\\face2" + devfile + ".bmp", 1
	gsel BUFFER_SCREEN
	gmode 0
	pos 0, 0
	gcopy selbufb, 0, 0, windoww, windowh
	gmode 2
	snd SOUNDLIST_LOAD
	listmax = 0
	buff = lang("あはははははっ！本当に選んでしまったのですね…♪ようこそ、新しい宇宙へ。", "Hahahaha! You really chose it...! Welcome to the new universe!!")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	if ( gdata(GDATA_FLAG_GAME_MODE) >= 0 & gdata(GDATA_FLAG_GAME_MODE) <= 4 ) {
		listmax = 0
		buff = lang("予言してあげます。一度楽することを選んだあなたは…これでもすぐに満足できなくなるのです。うふふ、ここからさらに堕ちていくのですよ？その時が楽しみですね…。", "I will predict. You chose to go easy... you will not be satisfied with this at once. You will fall further from here. I am looking forward to that time...")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	if ( gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
		listmax = 0
		buff = lang("ふふっ♪堕ちるところまで堕ちましたね…。でもいつか、きっとこれでも満足できなくなるのです。それまでの残り少ない時間…存分に快楽に身を委ねるといいのです。", "You fell to the limit...!Someday, you certainly will not be satisfied with this. The remaining time until then... entrust yourself to pleasure.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
		flt
		itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1000000
		dbg_noautosave = 1
		cdata(CDATA_SKILLPOINT, CHARA_PLAYER) += 10000
		gdata(GDATA_FLAG_SCROLL_OF_WITCHCRAFT_TOTAL_SKILL_BONUS) += 10000
		gdata(GDATA_FLAG_PC_SKILL_BONUS_OBTAINED) += 10000
		gdata(GDATA_FLAG_SKILL_BONUS_OBTAINED) += 10000
		if ( spact(SKILL_SPACT_WIN - STARTING_SKILL_SPACT) == 0 ) {
			spact(SKILL_SPACT_WIN - STARTING_SKILL_SPACT) = 1
			txtmore
			txtef COLOR_YELLOW
			txt lang("あなたは「" + skillname(SKILL_SPACT_WIN) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_WIN) + ".")
			if ( spact(SKILL_SPACT_PURGE - STARTING_SKILL_SPACT) == 0 ) {
				spact(SKILL_SPACT_PURGE - STARTING_SKILL_SPACT) = 1
				txtmore
				txtef COLOR_YELLOW
				txt lang("あなたは「" + skillname(SKILL_SPACT_PURGE) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_PURGE) + ".")
			}
		}
	}
	return

*chat_unique_leold_maxspeed
	snd SOUNDLIST_FAIL1
	listmax = 0
	buff = lang("すまん。今の俺ではこれ以上鍛えることができないようだ。", "No can do. I don't have the power to increase beyond that just yet.")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	goto *chat_unique

*chat_unique_leold_lack_ap
	snd SOUNDLIST_FAIL1
	listmax = 0
	buff = lang("…こりゃもっとAP貯めないと無理だな。APを貯めるには、レベルに関係なく初期ステータス(INIT)で数段上回る敵を倒す必要がある。", "...looks like you're gonna need more AP than that. To earn AP, you'll have to defeat enemies that have several times the starting stats (INI) of yours.")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	goto *chat_unique

*chat_unique_leold_bonus_limit
	snd SOUNDLIST_FAIL1
	listmax = 0
	buff = lang("スキルボーナスは100以上あると新たに獲得できない。腐らせていては成長の遅れに繋がるぞ。", "If there are 100 or more skill bonuses, I cannot give new. No matter how much skill bonus you accumulate, growth will be delayed if you do not use it.")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	goto *chat_unique

*chat_unique_leold_done
	if ( leoval != (-2) ) {
		allyctrl = 3
		leoleo = 1
		gosub *com_ally
	}
	if ( leoval == (-2) ) {
		allyctrl = 3
		leoleo = 2
		gosub *com_ally
	}
	leoleo = 0
	if ( stat != (-1) ) {
		c = stat
		snd SOUNDLIST_OK1
		txtnew
		if ( leoval == 0 ) {
			leoap = cdata(CDATA_INIT_SPEED, c) + 100
		}
		if ( leoval == (-2) ) {
			leoap = cdata(CDATA_INIT_LIFEMANA, c) + 300
			if ( cbit(CHARA_BIT_ARMOUR, c) ) {
				leoap += 300
			}
			if ( cbit(CHARA_BIT_RES_ELEMENTAL, c) ) {
				leoap += 500
			}
			if ( cbit(CHARA_BIT_METAL, c) ) {
				leoap += 800
			}
		}
		if ( leoval == (-1) ) {
			if ( gdata(GDATA_FLAG_GAME_MODE) != 2 & gdata(GDATA_FLAG_GAME_MODE) != 5 ) {
				if ( cdata(CDATA_SKILLPOINT, c) >= 100 ) {
					goto *chat_unique_leold_bonus_limit
				}
			}
			if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
				goto *chat_unique_leold_lack_ap
				goto *chat_end
			}
			snd SOUNDLIST_PRAY1
			cdata(CDATA_AP_CURRENT, c) -= leoap
			cdata(CDATA_SKILLPOINT, c) += 100
			if ( gdata(GDATA_FLAG_GAME_MODE) != 2 & gdata(GDATA_FLAG_GAME_MODE) != 5 ) {
				snd SOUNDLIST_CHIME
				txtef COLOR_RED
				txt lang("注意！スキルボーナスは100以上あると新たに獲得できない。", "Caution! While the skill bonus is 100 or more, you cannot get new.")
			}
			listmax = 0
			buff = lang("そらよ！", "All done!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			leoap = 0
			goto *chat_unique
		}
		if ( leoval == (-2) ) {
			if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
				goto *chat_unique_leold_lack_ap
				goto *chat_end
			}
			if ( sorg(SKILL_ATTR_LIFE, c) >= 300 & gdata(GDATA_FLAG_MAIN) < 1000 ) {
				goto *chat_unique_leold_maxspeed
			}
			snd SOUNDLIST_PRAY1
			cdata(CDATA_AP_CURRENT, c) -= leoap
			skillmod SKILL_ATTR_LIFE, c, 10000
			listmax = 0
			buff = lang("そらよ！", "All done!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			leoap = 0
			goto *chat_unique
		}
		if ( leoval == 0 ) {
			if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
				goto *chat_unique_leold_lack_ap
				goto *chat_end
			}
			if ( sorg(SKILL_ATTR_SPD, c) > 500 & gdata(GDATA_FLAG_MAIN) < 350 ) {
				goto *chat_unique_leold_maxspeed
			}
			if ( sorg(SKILL_ATTR_SPD, c) > 800 & gdata(GDATA_FLAG_MAIN) >= 350 & gdata(GDATA_FLAG_MAIN) < 1000 ) {
				goto *chat_unique_leold_maxspeed
			}
			snd SOUNDLIST_PRAY1
			cdata(CDATA_AP_CURRENT, c) -= leoap
			skillmod SKILL_ATTR_SPD, c, 30000
			listmax = 0
			buff = lang("そらよ！", "All done!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			leoap = 0
			goto *chat_unique
		}
		if ( leoval >= 1 & leoval < 100 ) {
			if ( leoval == 1 ) {
				bitp = CHARA_BIT_AWAKE_INSULT
			}
			if ( leoval == 2 ) {
				bitp = CHARA_BIT_AWAKE_CRYSTAL_SPEAR
			}
			if ( leoval == 3 ) {
				bitp = CHARA_BIT_AWAKE_TACTICAL_HEAL
			}
			if ( leoval == 4 ) {
				bitp = CHARA_BIT_AWAKE_DIMENSIONAL_MOVE
			}
			if ( leoval == 5 ) {
				bitp = CHARA_BIT_AWAKE_TACTICAL_ATTACK
			}
			if ( leoval == 6 ) {
				bitp = CHARA_BIT_AWAKE_TACTICAL_MARTIAL_ARTS
			}
			if ( leoval == 7 ) {
				bitp = CHARA_BIT_AWAKE_CHARGE_ATTACK
			}
			if ( leoval == 8 ) {
				bitp = CHARA_BIT_AWAKE_PROXIMITY_ATTACK
			}
			if ( leoval == 9 ) {
				bitp = CHARA_BIT_AWAKE_RANGED_ATTACK
			}
			if ( leoval == 10 ) {
				bitp = CHARA_BIT_AWAKE_MAGIC_ATTACK
			}
			if ( leoval == 14 ) {
				bitp = CHARA_BIT_AWAKE_PROVOKE
			}
			if ( leoval == 15 ) {
				bitp = CHARA_BIT_AWAKE_TACTICAL_CURSE
			}
			if ( leoval == 16 ) {
				bitp = CHARA_BIT_AWAKE_VARIABLE_BREATH
			}
			if ( leoval == 17 ) {
				bitp = CHARA_BIT_AWAKE_VARIABLE_STORM
			}
			if ( leoval == 20 ) {
				bitp = CHARA_BIT_AWAKE_ZERO_SHOOT
			}
			if ( leoval == 21 ) {
				bitp = CHARA_BIT_AWAKE_TACTICAL_THROW
			}
			if ( cbit(bitp, c) ) {
				cbitmod bitp, c, FALSE
				cdata(CDATA_AP_CURRENT, c) += leoap
				txt lang(cdatan(CDATAN_NAME, c) + "は力の使い方を忘れた。", cdatan(CDATAN_NAME, c) + " forgotten how to use a power.")
			}
			else {
				if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
					goto *chat_unique_leold_lack_ap
					goto *chat_end
				}
				listmax = 0
				buff = lang("そらよ！同じものをもう一度覚えさせようとすると逆に忘れてしまうから気をつけろよ。忘れる場合はAPを消費しないし、覚えるのに使ったAPも戻ってくるぞ。", "All done! Trying to learn the same thing again will make you forget it. Forgetting it won't cost AP, and will also refund the AP spent on it in the first place.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				snd SOUNDLIST_PRAY1
				cdata(CDATA_AP_CURRENT, c) -= leoap
				cbitmod bitp, c, TRUE
				txt lang(cdatan(CDATAN_NAME, c) + "は新たな力に目覚めた。", cdatan(CDATAN_NAME, c) + " awoken to a new power.")
			}
			leoap = 0
			goto *chat_unique
		}
		if ( leoval >= 1000 & leoval < 2000 ) {
			if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
				goto *chat_unique_leold_lack_ap
				goto *chat_end
			}
			snd SOUNDLIST_PRAY1
			cdata(CDATA_AP_CURRENT, c) -= leoap
			if ( leoval == 1000 ) {
				cdata(CDATA_AI_DISTANCE, c) += 1
				if ( cdata(CDATA_AI_DISTANCE, c) >= 10 ) {
					cdata(CDATA_AI_DISTANCE, c) = 10
				}
				txt lang(cdatan(CDATAN_NAME, c) + "の適正距離が" + cdata(CDATA_AI_DISTANCE, c) + "になった。", cdatan(CDATAN_NAME, c) + " preferred distance has become " + cdata(CDATA_AI_DISTANCE, c) + ".")
			}
			if ( leoval == 1001 ) {
				cdata(CDATA_AI_DISTANCE, c) -= 1
				if ( cdata(CDATA_AI_DISTANCE, c) <= 1 ) {
					cdata(CDATA_AI_DISTANCE, c) = 1
				}
				txt lang(cdatan(CDATAN_NAME, c) + "の適正距離が" + cdata(CDATA_AI_DISTANCE, c) + "になった。", cdatan(CDATAN_NAME, c) + " preferred distance has become " + cdata(CDATA_AI_DISTANCE, c) + ".")
			}
			if ( leoval == 1002 ) {
				cdata(CDATA_AI_MOVE_FREQ, c) += 10
				if ( cdata(CDATA_AI_MOVE_FREQ, c) >= 100 ) {
					cdata(CDATA_AI_MOVE_FREQ, c) = 100
				}
				txt lang(cdatan(CDATAN_NAME, c) + "の移動確率が" + cdata(CDATA_AI_MOVE_FREQ, c) + "になった。", cdatan(CDATAN_NAME, c) + " movement probability has become " + cdata(CDATA_AI_MOVE_FREQ, c) + ".")
			}
			if ( leoval == 1003 ) {
				cdata(CDATA_AI_MOVE_FREQ, c) -= 10
				if ( cdata(CDATA_AI_MOVE_FREQ, c) <= 0 ) {
					cdata(CDATA_AI_MOVE_FREQ, c) = 0
				}
				txt lang(cdatan(CDATAN_NAME, c) + "の移動確率が" + cdata(CDATA_AI_MOVE_FREQ, c) + "になった。", cdatan(CDATAN_NAME, c) + "  movement probability has become " + cdata(CDATA_AI_MOVE_FREQ, c) + ".")
			}
			if ( leoval == 1004 ) {
				if ( cdata(CDATA_SEALED_MAGIC, c) == 1 ) {
					cdata(CDATA_SEALED_MAGIC, c) = 0
					txt lang(cdatan(CDATAN_NAME, c) + "は魔法封印を解除した。", cdatan(CDATAN_NAME, c) + " now allowed to use spells.")
				}
				else {
					cdata(CDATA_SEALED_MAGIC, c) = 1
					txt lang(cdatan(CDATAN_NAME, c) + "は魔法を封印した。", cdatan(CDATAN_NAME, c) + " now forbidden from using spells.")
				}
			}
			if ( leoval == 1005 ) {
				if ( cdata(CDATA_SEALED_ABILITY, c) == 1 ) {
					cdata(CDATA_SEALED_ABILITY, c) = 0
					txt lang(cdatan(CDATAN_NAME, c) + "は技能封印を解除した。", cdatan(CDATAN_NAME, c) + " now allowed to use special abilities.")
				}
				else {
					cdata(CDATA_SEALED_ABILITY, c) = 1
					txt lang(cdatan(CDATAN_NAME, c) + "は技能を封印した。", cdatan(CDATAN_NAME, c) + " now forbidden from using special actions.")
				}
			}
			if ( leoval == 1006 ) {
				if ( cdata(CDATA_QUALITY, c) >= FIX_QUALITY_MIRACLE ) {
					cdata(CDATA_AP_CURRENT, c) += leoap
					txt lang("何もおきない… ", "Nothing happens...")
				}
				if ( cdata(CDATA_QUALITY, c) < FIX_QUALITY_MIRACLE ) {
					cdata(CDATA_QUALITY, c) = FIX_QUALITY_MIRACLE
					txt lang(cdatan(CDATAN_NAME, c) + "はランクアップした！", cdatan(CDATAN_NAME, c) + " character quality was increased!")
					cdatan(CDATAN_NAME, c) = lang("《", "{") + cdatan(CDATAN_NAME, c) + lang("》", "}")
				}
			}
		}
		if ( leoval > 100 & leoval < 1000 ) {
			if ( leoval == 101 ) {
				bitp = CHARA_BIT_AWAKE_DIM_ATTACKER
			}
			if ( leoval == 102 ) {
				bitp = CHARA_BIT_AWAKE_NEAR_DEATH_EVASION
			}
			if ( leoval == 103 ) {
				bitp = CHARA_BIT_AWAKE_NEAR_DEATH_DAMAGE
			}
			if ( leoval == 104 ) {
				bitp = CHARA_BIT_AWAKE_MP_BARRIER
			}
			if ( cbit(bitp, c) ) {
				listmax = 0
				buff = lang("そのフィートは既に身についているみたいだな。APに還元してやろう。", "Looks like you already have that feat. I'll return it to AP for you.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cbitmod bitp, c, FALSE
				snd SOUNDLIST_PRAY1
				cdata(CDATA_AP_CURRENT, c) += leoap
			}
			else {
				aphantei = 0
				if ( cbit(CHARA_BIT_AWAKE_DIM_ATTACKER, c) ) {
					aphantei += 1
				}
				if ( cbit(CHARA_BIT_AWAKE_NEAR_DEATH_EVASION, c) ) {
					aphantei += 1
				}
				if ( cbit(CHARA_BIT_AWAKE_NEAR_DEATH_DAMAGE, c) ) {
					aphantei += 1
				}
				if ( cbit(CHARA_BIT_AWAKE_MP_BARRIER, c) ) {
					aphantei += 1
				}
				if ( aphantei >= 2 ) {
					listmax = 0
					buff = lang("…これ以上のフィートの獲得は不可能のようだ。既に身についているフィートを選択して、一度APに還元する必要があるな。", "Looks like there's no more room for new feats. You'll have to select an existing one to turn it back into AP to make room for another.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				else {
					if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
						goto *chat_unique_leold_lack_ap
						goto *chat_end
					}
					snd SOUNDLIST_PRAY1
					cdata(CDATA_AP_CURRENT, c) -= leoap
					cbitmod bitp, c, TRUE
					listmax = 0
					buff = lang("そらよ！あとでフィートを確認してみな。", "All done! It should be on your list of feats now.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					txt lang(cdatan(CDATAN_NAME, c) + "は新たなフィートを取得した。", cdatan(CDATAN_NAME, c) + " acquired a new feat.")
				}
			}
		}
		aphantei = 0
	}
	leoap = 0
	goto *chat_unique

*chat_unique_leold_awaken
	c = 0
	spcost = cdata(CDATA_INIT_SPEED, c) + 100
	hpcost = cdata(CDATA_INIT_LIFEMANA, c) + 300
	if ( cbit(CHARA_BIT_ARMOUR, c) ) {
		hpcost += 300
	}
	if ( cbit(CHARA_BIT_RES_ELEMENTAL, c) ) {
		hpcost += 500
	}
	if ( cbit(CHARA_BIT_METAL, c) ) {
		hpcost += 800
	}
	list(0, listmax) = 0
	listn(0, listmax) = lang("速度30上昇(消費AP" + spcost + ")", "Increase speed by 30.(cost " + spcost + "AP)")
	listmax++
	list(0, listmax) = 4
	listn(0, listmax) = lang("生命力10上昇(消費AP" + hpcost + ")", "Increase life by 10.(cost " + hpcost + "AP)")
	listmax++
	list(0, listmax) = 1
	listn(0, listmax) = lang("フィート取得", "Acquire feats.")
	listmax++
	list(0, listmax) = 2
	listn(0, listmax) = lang("技能の会得", "Learn special abilities.")
	listmax++
	list(0, listmax) = 3
	listn(0, listmax) = lang("スキルボーナス100獲得(消費AP1500)", "Gain 100 bonus points.(1500AP)")
	listmax++
	buff = lang("覚醒に必要なポイント…APは残り" + cdata(CDATA_AP_CURRENT, c) + "だ。どう使うんだ？", "You have... " + cdata(CDATA_AP_CURRENT, c) + " Awakening Points left. How do you want to use them?")
	gosub *chat_select
	if ( chatval == 3 ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) != 2 & gdata(GDATA_FLAG_GAME_MODE) != 5 ) {
			if ( cdata(CDATA_SKILLPOINT, c) >= 100 ) {
				goto *chat_unique_leold_bonus_limit
			}
		}
		if ( cdata(CDATA_AP_CURRENT, c) < 1500 ) {
			goto *chat_unique_leold_lack_ap
			goto *chat_end
		}
		snd SOUNDLIST_PRAY1
		cdata(CDATA_AP_CURRENT, c) -= 1500
		cdata(CDATA_SKILLPOINT, c) += 100
		gdata(GDATA_FLAG_PC_SKILL_BONUS_OBTAINED) += 100
		if ( gdata(GDATA_FLAG_GAME_MODE) != 2 & gdata(GDATA_FLAG_GAME_MODE) != 5 ) {
			snd SOUNDLIST_CHIME
			txtef COLOR_RED
			txt lang("注意！スキルボーナスは100以上あると新たに獲得できない。", "Caution! While the skill bonus is 100 or more, you cannot get new.")
		}
		listmax = 0
		buff = lang("そらよ！", "All done!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_unique
	}
	if ( chatval == 4 ) {
		hpcost = cdata(CDATA_INIT_LIFEMANA, c) + 300
		if ( cbit(CHARA_BIT_ARMOUR, c) ) {
			hpcost += 300
		}
		if ( cbit(CHARA_BIT_RES_ELEMENTAL, c) ) {
			hpcost += 500
		}
		if ( cbit(CHARA_BIT_METAL, c) ) {
			hpcost += 800
		}
		leoap = hpcost
		if ( sorg(SKILL_ATTR_LIFE, c) >= 300 & gdata(GDATA_FLAG_MAIN) < 1000 ) {
			goto *chat_unique_leold_maxspeed
		}
		if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
			goto *chat_unique_leold_lack_ap
		}
		snd SOUNDLIST_PRAY1
		cdata(CDATA_AP_CURRENT, c) -= leoap
		skillmod SKILL_ATTR_LIFE, c, 10000
		listmax = 0
		buff = lang("そらよ！", "All done!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_unique
	}
	if ( chatval == 0 ) {
		spcost = cdata(CDATA_INIT_SPEED, c) + 100
		leoap = spcost
		if ( sorg(SKILL_ATTR_SPD, c) > 500 & gdata(GDATA_FLAG_MAIN) < 350 ) {
			goto *chat_unique_leold_maxspeed
		}
		if ( sorg(SKILL_ATTR_SPD, c) > 800 & gdata(GDATA_FLAG_MAIN) >= 350 & gdata(GDATA_FLAG_MAIN) < 1000 ) {
			goto *chat_unique_leold_maxspeed
		}
		if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
			goto *chat_unique_leold_lack_ap
		}
		snd SOUNDLIST_PRAY1
		cdata(CDATA_AP_CURRENT, c) -= leoap
		skillmod SKILL_ATTR_SPD, c, 30000
		listmax = 0
		buff = lang("そらよ！", "All done!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_unique
	}
	if ( chatval == 1 ) {
		list(0, listmax) = 0
		listn(0, listmax) = lang("魅惑(消費AP1500)[直接攻撃してきた敵が朦朧]", "Charm (1500AP) [Dim enemies attacking at close range]")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("底力(消費AP1500)[HP減るほど最終与ダメージ上昇]", "Brave (1500AP) [Increase damage dealt as HP decreases]")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("意地(消費AP1800)[HP減るほど回避・軽減率上昇]", "Obstinate (1800AP) [Increase evasion, damage mitigation as HP decreases]")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("障壁(消費AP2000)[ダメージ分MP消費し攻撃無効化]", "Barrier (2000AP) [Negate attacks with equal amount of MP]")
		listmax++
		buff = lang("どのフィートを獲得するんだ？既に獲得している物を選ぶと外してAPに戻すことができるぞ。", "Which feat do you want? Picking one that you already have will remove it and return the AP to you.")
		gosub *chat_select
		leoap = 0
		if ( chatval == 0 ) {
			leoap = 1500
			bitp = CHARA_BIT_AWAKE_DIM_ATTACKER
		}
		if ( chatval == 1 ) {
			leoap = 1800
			bitp = CHARA_BIT_AWAKE_NEAR_DEATH_EVASION
		}
		if ( chatval == 2 ) {
			leoap = 1500
			bitp = CHARA_BIT_AWAKE_NEAR_DEATH_DAMAGE
		}
		if ( chatval == 3 ) {
			leoap = 2000
			bitp = CHARA_BIT_AWAKE_MP_BARRIER
		}
		if ( leoap == 0 ) {
			goto *chat_end
		}
		if ( cbit(bitp, c) ) {
			listmax = 0
			buff = lang("そのフィートは既に身についているみたいだな。APに還元してやろう。", "Looks like you already have that feat. I'll return it to AP for you.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			cbitmod bitp, c, FALSE
			snd SOUNDLIST_PRAY1
			cdata(CDATA_AP_CURRENT, c) += leoap
		}
		else {
			aphantei = 0
			if ( cbit(CHARA_BIT_AWAKE_DIM_ATTACKER, c) ) {
				aphantei += 1
			}
			if ( cbit(CHARA_BIT_AWAKE_NEAR_DEATH_EVASION, c) ) {
				aphantei += 1
			}
			if ( cbit(CHARA_BIT_AWAKE_NEAR_DEATH_DAMAGE, c) ) {
				aphantei += 1
			}
			if ( cbit(CHARA_BIT_AWAKE_MP_BARRIER, c) ) {
				aphantei += 1
			}
			if ( aphantei >= 2 ) {
				listmax = 0
				buff = lang("…これ以上のフィートの獲得は不可能のようだ。既に身についているフィートを選択して、一度APに還元する必要があるな。", "Looks like there's no more room for new feats. You'll have to select an existing one to turn it back into AP to make room for another.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			else {
				if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
					goto *chat_unique_leold_lack_ap
				}
				snd SOUNDLIST_PRAY1
				cdata(CDATA_AP_CURRENT, c) -= leoap
				cbitmod bitp, c, TRUE
				listmax = 0
				buff = lang("そらよ！あとでフィートを確認してみな。", "All done! It should be on your list of feats now.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				txt lang(cdatan(CDATAN_NAME, c) + "は新しい力を手に入れた。", cdatan(CDATAN_NAME, c) + " awoken to a new power.")
			}
		}
		aphantei = 0
		goto *chat_unique
	}
	if ( chatval == 2 ) {
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "Cancel")
		listmax++
		if ( gdata(GDATA_FLAG_AP_FEAT_CONTINUOUS_ATTACK) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("高速ラッシュ(消費AP100)[弱威力連続攻撃]", "Continuous attacks (100AP) [Weak series of attacks]")
			listmax++
		}
		if ( gdata(GDATA_FLAG_AP_FEAT_PROVOKE) == 0 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("挑発(消費AP300)[自分をターゲットにさせる]", "Provoke (AP300) [Make enemies target self]")
			listmax++
		}
		if ( gdata(GDATA_FLAG_AP_FEAT_METAL_GUARD) == 0 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("メタルコート(消費AP500)[一度だけ受けるダメージ1/10]", "Metal Guard (500AP) [Next attack received deals 1/10 damage]")
			listmax++
		}
		if ( gdata(GDATA_FLAG_AP_FEAT_SYNCHRO_HEARTS) == 0 ) {
			list(0, listmax) = 4
			listn(0, listmax) = lang("魂のシンクロ(消費AP800)[瀕死時、好感度依存仲間強化/回復]", "Synchro hearts (800AP) [Impress-dependent ally heal/buff when low on health]")
			listmax++
		}
		if ( gdata(GDATA_FLAG_AP_FEAT_CHARGE) == 0 ) {
			list(0, listmax) = 5
			listn(0, listmax) = lang("チャージ(消費AP1000)[一度だけ物理/魔法/放射の攻撃強化]", "Charge Attack(AP1000) [Buff next attack/spell/breath]")
			listmax++
		}
		buff = lang("どの技能を会得するんだ？会得した物はAPに戻すことができないからよく考えるんだぞ。", "Which special action do you want to learn? Pick carefully, because once learned the AP cannot be refunded.")
		gosub *chat_select
		leoap = 0
		if ( chatval == 0 ) {
			goto *chat_unique
		}
		if ( chatval == 1 ) {
			leoap = 100
		}
		if ( chatval == 2 ) {
			leoap = 300
		}
		if ( chatval == 3 ) {
			leoap = 500
		}
		if ( chatval == 4 ) {
			leoap = 800
		}
		if ( chatval == 5 ) {
			leoap = 1000
		}
		if ( leoap == 0 ) {
			goto *chat_end
		}
		if ( cdata(CDATA_AP_CURRENT, c) < leoap ) {
			goto *chat_unique_leold_lack_ap
		}
		snd SOUNDLIST_PRAY1
		cdata(CDATA_AP_CURRENT, c) -= leoap
		listmax = 0
		buff = lang("そらよ！実際に使ってちゃんと効果を確認しておくんだぞ。", "All done! You'll have to try it out for yourself to see how it works.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( leoap == 100 ) {
			gdata(GDATA_FLAG_AP_FEAT_CONTINUOUS_ATTACK) = 10
			if ( spact(SKILL_SPACT_CONTINUOUS_ATTACKS - STARTING_SKILL_SPACT) == 0 ) {
				spact(SKILL_SPACT_CONTINUOUS_ATTACKS - STARTING_SKILL_SPACT) = 1
				txtmore
				txtef COLOR_YELLOW
				txt lang("あなたは「" + skillname(SKILL_SPACT_CONTINUOUS_ATTACKS) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_CONTINUOUS_ATTACKS) + ".")
			}
		}
		if ( leoap == 300 ) {
			gdata(GDATA_FLAG_AP_FEAT_PROVOKE) = 10
			if ( spact(SKILL_SPACT_PROVOKE - STARTING_SKILL_SPACT) == 0 ) {
				spact(SKILL_SPACT_PROVOKE - STARTING_SKILL_SPACT) = 1
				txtmore
				txtef COLOR_YELLOW
				txt lang("あなたは「" + skillname(SKILL_SPACT_PROVOKE) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_PROVOKE) + ".")
			}
		}
		if ( leoap == 500 ) {
			gdata(GDATA_FLAG_AP_FEAT_METAL_GUARD) = 10
			if ( spact(SKILL_SPACT_METAL_GUARD - STARTING_SKILL_SPACT) == 0 ) {
				spact(SKILL_SPACT_METAL_GUARD - STARTING_SKILL_SPACT) = 1
				txtmore
				txtef COLOR_YELLOW
				txt lang("あなたは「" + skillname(SKILL_SPACT_METAL_GUARD) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_METAL_GUARD) + ".")
			}
		}
		if ( leoap == 800 ) {
			gdata(GDATA_FLAG_AP_FEAT_SYNCHRO_HEARTS) = 10
			if ( spact(SKILL_SPACT_SYNCHRO_HEARTS - STARTING_SKILL_SPACT) == 0 ) {
				spact(SKILL_SPACT_SYNCHRO_HEARTS - STARTING_SKILL_SPACT) = 1
				txtmore
				txtef COLOR_YELLOW
				txt lang("あなたは「" + skillname(SKILL_SPACT_SYNCHRO_HEARTS) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_SYNCHRO_HEARTS) + ".")
			}
		}
		if ( leoap == 1000 ) {
			gdata(GDATA_FLAG_AP_FEAT_CHARGE) = 10
			if ( spact(SKILL_SPACT_CHARGE - STARTING_SKILL_SPACT) == 0 ) {
				spact(SKILL_SPACT_CHARGE - STARTING_SKILL_SPACT) = 1
				txtmore
				txtef COLOR_YELLOW
				txt lang("あなたは「" + skillname(SKILL_SPACT_CHARGE) + "」の能力を得た。", "You have learned new ability, " + skillname(SKILL_SPACT_CHARGE) + ".")
			}
		}
		leoap = 0
		goto *chat_unique
	}
	goto *chat_unique

*evochat_portrait
	if ( cdata(CDATA_FACE, tc) > 0 ) {
		buff = lang("(現在の立ち姿はface" + cdata(CDATA_FACE, tc) + "だ)", "(face" + cdata(CDATA_FACE, tc) + " now)")
	}
	if ( cdata(CDATA_FACE, tc) == 0 ) {
		buff = lang("(現在、立ち姿は決まっていない)", "(no Setting)")
	}
	list(0, listmax) = 1
	listn(0, listmax) = lang("次へ>>>", ">>>")
	listmax++
	list(0, listmax) = 0
	listn(0, listmax) = lang("<<<前へ", "<<<")
	listmax++
	list(0, listmax) = 2
	listn(0, listmax) = lang("戻る", "Cancel")
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( chatval == 1 | chatval == 0 ) {
		snd SOUNDLIST_CURSOR1
		tatiebk = cdata(CDATA_FACE, tc)
		if ( chatval == 1 ) {
			cdata(CDATA_FACE, tc)++
		}
		if ( chatval == 0 ) {
			cdata(CDATA_FACE, tc)--
		}
		if ( cdata(CDATA_FACE, tc) > 40 ) {
			cdata(CDATA_FACE, tc) = 40
			goto *evochat_portrait
		}
		if ( cdata(CDATA_FACE, tc) <= 0 ) {
			cdata(CDATA_FACE, tc) = 0
			goto *evochat_portrait
		}
		ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_B.bmp"
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 6 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_F.bmp"
		}
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 17 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_H.bmp"
		}
		if ( cdata(CDATA_HP, tc) < cdata(CDATA_MAX_HP, tc) / 2 | cdata(CDATA_EMO_ICON, tc) \ 100 == 26 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_E.bmp"
		}
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 18 | cdata(CDATA_CONDITION_ANGRY, tc) > 0 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_A.bmp"
		}
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 21 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_Q.bmp"
		}
		exist ss
		if ( strsize == (-1) ) {
			cdata(CDATA_FACE, tc) = tatiebk
			goto *evochat_portrait
		}
		s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_B.bmp"
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 6 ) {
			s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_F.bmp"
		}
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 17 ) {
			s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_H.bmp"
		}
		if ( cdata(CDATA_HP, tc) < cdata(CDATA_MAX_HP, tc) / 2 | cdata(CDATA_EMO_ICON, tc) \ 100 == 26 ) {
			s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_E.bmp"
		}
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 18 | cdata(CDATA_CONDITION_ANGRY, tc) > 0 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_A.bmp"
		}
		if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 21 ) {
			ss = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_Q.bmp"
		}
		exist s
		if ( strsize == (-1) ) {
			picols = cdata(CDATA_CHARA_PIC, tc)
			if ( cdata(CDATA_PIC, tc) >= xy2pic(1, 21) & cdata(CDATA_PIC, tc) <= xy2pic(31, 21) ) {
				picols = 0
			}
			p = cdata(CDATA_PIC, tc) \ 1000 + picols * 1000, (cdata(CDATA_PIC, tc) - picols * 1000) / 1000
			chara_preparepic p, p(1)
			pos wx + 82, wy + 125 - chipc(CHIPC_F, p)
			gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
			grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 2, chipc(CHIPC_HEIGHT, p) * 2
		}
		else {
			if ( chatpicloaded == FALSE ) {
				gsel BUFFER_BUF
				pos 0, 0
				picload s, 1
				gsel BUFFER_SCREEN
				chatpicloaded = TRUE
			}
		}
		goto *evochat_portrait
	}
	goto *chat_default

*sqMother_1
	list(0, listmax) = 1
	listn(0, listmax) = lang("エーテル抗体を1本渡す", "(Hand her a potion of cure corruption.)")
	listmax++
	list(0, listmax) = 0
	listn(0, listmax) = strbye
	listmax++
	gosub *chat_select
	if ( chatval != 1 ) {
		return 0
	}
	inv_find ITEM_ID_POTION_CURE_CORRUPTION, CHARA_PLAYER
	if ( stat == (-1) ) {
		listmax = 0
		buff = lang("うそつき！", "Lier!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		return 0
	}
	inv(INV_ITEM_NUM, stat)--
	txt lang("エーテル抗体を1本渡した。", "You give her a potion.")
	snd SOUNDLIST_EQUIP1
	listmax = 0
	buff = lang("え…あ、ありがと〜！！おかあさん…薬だよ。これでよくなるよね？", "R-Really? Mommy, mommy! " + cdatan(CDATAN_NAME, CHARA_PLAYER) + " gave us a cure! Are you gonna be ok now?")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	return 1

*chat_unique_enthumesis
	listmax = 0
	buff = lang("累計放出量から予測した状態を、ソピアーから聞いていたが…それよりもだいぶ弱っているようだ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) == 0) + (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) != 0) * findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("一度でいいから会いたいよ…。どうすれば会えるの？こいつらの知識を取り込めばわかるの？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ENTHUMESIS) == 0) + (findchara(CREATURE_ID_ENTHUMESIS) != 0) * findchara(CREATURE_ID_ENTHUMESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("情けをかけるのは危険だ。仕留めるぞ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) == 0) + (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) != 0) * findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	gdata(GDATA_FLAG_MAIN) = 710
	snd SOUNDLIST_WRITE1
	txtef COLOR_GREEN
	txt lang("ジャーナルが更新された。", "Your journal has been updated.")
	goto *chat_end

*chat_unique_mikraanesis
	snd SOUNDLIST_CHIME
	flt
	novoidlv = 1
	characreate -1, CREATURE_ID_NORNE_THE_GUIDE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	listmax = 0
	buff = lang("ミクラネシス様、いったい何があったのですか。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_NORNE_THE_GUIDE) == 0) + (findchara(CREATURE_ID_NORNE_THE_GUIDE) != 0) * findchara(CREATURE_ID_NORNE_THE_GUIDE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("今我々の本体が忘却の女神を食い止めている。いや…戦ってわかった。あれは正確には忘却を司る女神ではない。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("あれは世界規模で忘却という現象を起こすための概念存在…神という形で忘却の一側面が具象化したものだ。この世に何らかの記憶がある限り忘却という概念も同時に存在し続けるため、女神の姿を何度滅ぼそうと根源を取り除くことはできない。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("もしも本体が完全体で降臨した場合、その世界のあらゆる存在は過去・未来に渡り完全に消滅する。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、君にもあるはずだよ。もう内容はおろか題名すら思い出せない物語が。忘れてしまったことすら忘れている人物が。それが全世界・全宇宙規模で起こると思ってくれればいい。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("今は降臨する前の本体を攻撃して食い止めているが、端末である獣どもまでは抑えきれなかった。このまま獣どもが記憶を喰らって増殖すれば本体の力も大きくなり、私達も押し切られてしまうだろうね。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("そんな…。それじゃあガイアス・ヴィスはもうじき消えてなくなる…？何か手はないんですか！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_NORNE_THE_GUIDE) == 0) + (findchara(CREATURE_ID_NORNE_THE_GUIDE) != 0) * findchara(CREATURE_ID_NORNE_THE_GUIDE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("手はあるさ。" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "、これは他人事ではない。ここが完全に忘れられた世界となってしまえば次はイルヴァの番。何百年、いや何十年と持たないかもしれない。いずれイルヴァの景色を覚えている者は全宇宙上からいなくなってしまうだろう。だから君にこれを渡しておく。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	txtef COLOR_GREEN
	txt lang("[アカシックネットワーク]を手に入れた！", "You obtain Akashic Network!")
	snd SOUNDLIST_COMPLETE1
	listmax = 0
	buff = lang("今のうちに別世界の観測者たちに記憶を刻み込むことができれば、相手の力を大きく抑制できる。今渡したのは、君が見てきたガイアス・ヴィスの姿を、アストラル光を介してアカシックレコード上に拡散するための干渉機構だ。ネットで公開して記憶保持者を増やし、世界の忘却を遅らせるわけだね。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("しかしそれが使えるのは神ではなく使う世界に籍を置く定命だ。さらに効果はレコードの所在が近い者…つまり関わりの深い者たちにしか及ばない。だから" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "のような存在が必要だったんだ。ガイアス・ヴィス出身ではなく、各地を渡り歩いていて、廃忘獣の群れを物理的に突破する力を持つ定命が。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("そうか。ああ、ボクはガイアス・ヴィスからイルヴァに渡ってきている立場だから使えないのか…。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_NORNE_THE_GUIDE) == 0) + (findchara(CREATURE_ID_NORNE_THE_GUIDE) != 0) * findchara(CREATURE_ID_NORNE_THE_GUIDE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("ノルンにもやってもらうことはあるよ。地道に、一人でも多く、あちら側の世界の人々に伝えるんだ。何度忘れ去られても伝え続けなさい、このガイアス・ヴィスの物語を。人々の記憶こそが世界を保つ力となるからね。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("でもボクは、今までもイルヴァの人々に何度か話しています。それでも獣は侵攻を止めなかった…。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_NORNE_THE_GUIDE) == 0) + (findchara(CREATURE_ID_NORNE_THE_GUIDE) != 0) * findchara(CREATURE_ID_NORNE_THE_GUIDE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("効果が薄かったのは、今までアカシックネットワークの媒体がいなかったからね。大丈夫。君の頑張りは無駄にならないよ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("…さて、長話ですまなかったね" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。君には少し難解だったかな？それでも、もう一つだけ。別件だよ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("混沌の真の目覚めが近づいているね。神の間に再び赴き、決着をつけるのが君の宿命だ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_MIKRAANESIS) == 0) + (findchara(CREATURE_ID_MIKRAANESIS) != 0) * findchara(CREATURE_ID_MIKRAANESIS)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	redraw 0
	gosub *screen_draw
	gosub *screen_fade
	gsel BUFFER_BUF
	pos 0, 0
	picload exedir + "graphic\\face1" + devfile + ".bmp", 1
	gsel BUFFER_SCREEN
	gdata(GDATA_FLAG_MAIN) = 820
	snd SOUNDLIST_WRITE1
	txtef COLOR_GREEN
	txt lang("ジャーナルが更新された。", "Your journal has been updated.")
	chara_vanquish findchara(CREATURE_ID_NORNE_THE_GUIDE)
	gosub *music_play
	goto *chat_end

*chat_unique_lankata
	music = MUSICLIST_MCBOSS
	gosub *music_play
	flt
	novoidlv = 1
	characreate -1, CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	cdata(CDATA_RELATION, rc) = RELATION_NEUTRAL
	cdata(CDATA_RELATION_ORG, rc) = RELATION_NEUTRAL
	cdata(CDATA_ROLE, rc) = ROLE_SPECIAL
	gdata(GDATA_FLAG_MAIN) = 640
	gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_25) = 100
	gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_34) = 100
	listmax = 0
	buff = lang("やっと会えた…！父上っ！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("ふふっ…最期に…娘の声が聞こえる…なんて。もう、目が…かすんで…きました。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND) == 0) + (findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND) != 0) * findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("そこの冒険者…少しどいていてください。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("新型緩和薬を投与…効くといいのですが。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("…。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("この様子だと、薬が回り切る前に肉体が崩壊しそうですね…。今、ここで次元炉の摘出を行います。耐えてください、父上！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	redraw 0
	gosub *screen_draw
	gosub *screen_fade
	gsel BUFFER_BUF
	pos 0, 0
	picload exedir + "graphic\\face2" + devfile + ".bmp", 1
	gsel BUFFER_SCREEN
	listmax = 0
	buff = lang("…。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	music = MUSICLIST_MCVICTORY
	gosub *music_play
	listmax = 0
	buff = lang("私は…助かったのですか？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND) == 0) + (findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND) != 0) * findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("父上…！よかった、本当によかった…。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("ランカータ、なぜ貴方がここに？衛生兵なのに一人でここまで来たのですか？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND) == 0) + (findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND) != 0) * findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("その話は後です。さぁ、脱出しましょう。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("おっといけません。その前に。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("名も知らぬ冒険者…貴方が露払いをしていなければ、私はきっと間に合わなかったでしょう。貴方は降りかかる火の粉を払っただけでしょうが、結果的に助かりました。感謝します。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) == 0) + (findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE) != 0) * findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	snd SOUNDLIST_TELEPORT1
	txt lang(name(tc) + "は次元の扉を開けた。", "A dimensional door opens in front of them.")
	chara_vanquish findchara(CREATURE_ID_LANKATA_THE_LIGHTNING_OF_BLUE)
	chara_vanquish findchara(CREATURE_ID_ALFRED_THE_CANGNAN_WIND)
	gosub *music_play
	goto *chat_end

*chat_unique_jaldabaoth
	listmax = 0
	buff = lang("飽きた。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	msg_clear
	txt lang("部屋の空気が突然緊張し、高熱を放つ火球が現れた。", "Sorry, this is untranslated sentence.")
	tc = findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	gosub *chat_refresh
	listmax = 0
	buff = lang("ッ…！？これはまずい！" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	gosub *chat_refresh
	listmax = 0
	buff = lang("遊びは終わりだ…灰になるがいい。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	msg_clear
	txtef COLOR_RED2
	txt lang("*滅火*", "<HOROBI>")
	txtvalid = 0
	txt lang("辺りをおぞましい火焔が包み込んでいく。", "Sorry, this is untranslated sentence.")
	txtvalid = 0
	txt lang("あなたは懸命に脚の震えを抑えようとしたが、難しかった。", "You earnestly attempt to suppress the shaking in your legs, but it proves difficult...")
	if ( cdata(CDATA_ROW_ACT, CHARA_PLAYER) != ACTION_NONE ) {
		rowactend 0
	}
	tcbk = tc
	redraw 0
	gosub *screen_draw
	gosub *screen_fadeIn2
	gsel BUFFER_BUF
	pos 0, 0
	picload exedir + "graphic\\face2" + devfile + ".bmp", 1
	gsel BUFFER_SCREEN
	snd SOUNDLIST_ATK_FIRE
	repeat mdata(MDATA_HEIGHT)
		dy = cnt
		repeat mdata(MDATA_WIDTH)
			dx = cnt
			f = 0
			map(dx, dy, MAP_TILE_ID) = 268 + rnd(3)
		loop
	loop
	p = 1
	repeat p
		repeat mdata(MDATA_HEIGHT)
			y = cnt
			repeat mdata(MDATA_WIDTH)
				x = cnt
				p = dist(cdata(CDATA_X, tc), cdata(CDATA_Y, tc), x, y)
				map(x, y, MAP_TILE_ID_MEMORY) = map(x, y, MAP_TILE_ID)
			loop
		loop
	loop
	if ( cdata(CDATA_IN_TAGTEAM, CHARA_PLAYER) != 0 ) {
		tag_end 0
	}
	if ( gdata(GDATA_RIDER) != 0 ) {
		ride_end
	}
	repeat 245
		if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
			continue
		}
		if ( 0 == cnt ) {
			continue
		}
		if ( tcbk == cnt ) {
			continue
		}
		map(cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt), MAP_TILE_ID) = xy2pic(3, 8)
		snd SOUNDLIST_ATK_FIRE
		if ( cdata(CDATA_ALLIED, cnt) != 0 ) {
			txt lang(cdatan(CDATAN_NAME, cnt) + "はあなたを庇って灰になった。", name(cnt) + " shielded you and turned to ashes.")
			cdata(CDATA_EXIST, cnt) = CHAR_STATE_SUSPEND
		}
		else {
			cdata(CDATA_HP, cnt) = 0
			if ( cdata(CDATA_RELATION, cnt) == RELATION_ALLY ) {
				dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH_SHIELDED
				dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH_SHIELDED
				dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH_SHIELDED
			}
			else {
				dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH
				dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH
				dmghp cnt, cdata(CDATA_MAX_HP, cnt), DAMAGE_FROM_JALDABAOTH
			}
		}
		map(cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt), MAP_CHARA_INDEX_PLUS_ONE) = 0
		cdata(CDATA_AREA, cnt) = AREA_NONE
	loop
	tc = CHARA_PLAYER
	repeat 200
		p = -1, 1
		cdata(CDATA_X_NEW, tc) = cdata(CDATA_X, tc) + (3 - cnt / 70 + rnd(5)) * p(rnd(2))
		cdata(CDATA_Y_NEW, tc) = cdata(CDATA_Y, tc) + (3 - cnt / 70 + rnd(5)) * p(rnd(2))
		cell_check cdata(CDATA_X_NEW, tc), cdata(CDATA_Y_NEW, tc)
		if ( cellaccess == 1 ) {
			rowactend tc
			cc = CHARA_PLAYER
			ccprev = tc
			gosub *procMove
			tc = ccprev
			snd SOUNDLIST_TAORE08
			break
		}
	loop
	gosub *screen_draw
	animeid = ANIMATION_JALDABAOTH
	gosub *anime
	music = MUSICLIST_MCOPENING
	gosub *music_play
	gdata(GDATA_FLAG_MAIN) = 875
	msg_halt
	msg_clear
	txt lang("…どれくらい時間がたっただろう。あなたは半分灰になった身体で、よろよろと起き上がった…。", "....An unbeknownst amount of time passes. Sorry, this is untranslated sentence.")
	msg_halt
	repeat 8 + 8
		i = 8 + 8 - cnt - 1
		if ( bdataref(BDATAREF_BUFF_TYPE, cdata(STARTING_CDATA_BUFF + i * ARRAY_CDATA_BUFF, CHARA_PLAYER)) != BUFF_TYPE_BUFF ) {
			continue
		}
		if ( cdata(STARTING_CDATA_BUFF + i * ARRAY_CDATA_BUFF, CHARA_PLAYER) == BUFF_PUNISHMENT ) {
			continue
		}
		if ( cdata(STARTING_CDATA_BUFF + i * ARRAY_CDATA_BUFF, CHARA_PLAYER) == BUFF_NONE ) {
			continue
		}
		delbuff CHARA_PLAYER, i
		p++
		continue cnt
	loop
	cdata(CDATA_HP, CHARA_PLAYER) = cdata(CDATA_HP, CHARA_PLAYER) / 2
	cdata(CDATA_MP, CHARA_PLAYER) = cdata(CDATA_MP, CHARA_PLAYER) / 2
	cdata(CDATA_SP, CHARA_PLAYER) = cdata(CDATA_SP, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_STR, CHARA_PLAYER) = sdata(SKILL_ATTR_STR, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_CON, CHARA_PLAYER) = sdata(SKILL_ATTR_CON, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_DEX, CHARA_PLAYER) = sdata(SKILL_ATTR_DEX, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_LER, CHARA_PLAYER) = sdata(SKILL_ATTR_LER, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_PER, CHARA_PLAYER) = sdata(SKILL_ATTR_PER, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_WIL, CHARA_PLAYER) = sdata(SKILL_ATTR_WIL, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_MAG, CHARA_PLAYER) = sdata(SKILL_ATTR_MAG, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_CHA, CHARA_PLAYER) = sdata(SKILL_ATTR_CHA, CHARA_PLAYER) / 2
	sdata(SKILL_ATTR_SPD, CHARA_PLAYER) = sdata(SKILL_ATTR_SPD, CHARA_PLAYER) / 2
	gdata(GDATA_FLAG_COLLECTED_WISHES) = 26
	repeat 16
		if ( cnt == 0 ) {
			continue
		}
		if ( cdata(CDATA_IMPRESSION, cnt) >= 100 ) {
			gdata(GDATA_FLAG_COLLECTED_WISHES)++
		}
	loop
	if ( gdata(GDATA_FLAG_SUB_SLIME) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_ROGUE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_NIGHTMARE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_MOTHER) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_WIFE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_DOG) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_CAT) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_WAR) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_YEEK) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_KAMIKAZE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_MIAS_DREAM) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_VEIN) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_PYRAMID) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_NUKE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_BIOLOGIST) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_SEWER) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_GUILD_WARS1) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_GUILD_WARS3) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_GUILD_WARS2) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_MINOTAUR) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_DADDY) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_LOST_KITTY) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_DIVERSIONARY_TACTICS) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_JUERES_IMPERTINENCE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_PIRATE_HUNTING) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_FALLEN_PRINCESS) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_UNPAID_COMPENSATION) >= 1 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_MANSION_FOR_SALE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 5
	}
	if ( gdata(GDATA_FLAG_SUB_THE_RESULT_OF_REVENGE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_STRUGGLE_AGAINST_INSANITY) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_H_SISTER) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_LAW_OF_NINJAS) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_MONOCHROME_DELIVERY_SERVICE_LETTER) >= 1 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_DOGS_TAIL) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_DIALOG_NAZUNA) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_THE_TRUE_APPEARANCE) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_THE_ABYSS_OF_MAGIC) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_THE_DISTANT_SEA_OF_STARS) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_BEYOND_THE_GENERATIONS) >= 9 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_WHEN_THE_FLAME_BURNS_OUT) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_NINE_HEAD_DRAGON) >= 3 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) >= 1003 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_THE_LIFE_IN_THIS_WORLD) >= 2 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES)++
	}
	if ( gdata(GDATA_FLAG_SUB_BELPHAT_THE_COSMIC_SWORD) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_MAIN_CHAOS_CRADLE_34) >= 100 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	if ( gdata(GDATA_FLAG_TRAINING_COURSE_COMBAT_4) >= 1000 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 6
	}
	if ( gdata(GDATA_FLAG_SUB_KARURA) >= 1 ) {
		gdata(GDATA_FLAG_COLLECTED_WISHES) += 2
	}
	txt lang("声が聞こえる…。", "You heard someone's voice...")
	gdata(GDATA_FLAG_CUT_IN_MESSAGE_1) = 1500
	animeid = ANIMATION_LASTBOSS_2
	anic = 0
	gosub *anime
	goto *chat_end

*chat_unique_yayauhqui
	flt
	initlv = 350
	novoidlv = 1
	characreate -1, CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	cdata(CDATA_RELATION, rc) = RELATION_ENEMY
	cdata(CDATA_RELATION_ORG, rc) = RELATION_ENEMY
	cdata(CDATA_AI_AGGRO, rc) = 80
	cdata(CDATA_TARGET, rc) = 0
	listmax = 0
	buff = lang("口ほどにもない！神の間を破壊するのに時間を取られ、混沌の超児から神力を吸収できなかったようだな！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("何か勘違いをしているようだな、テスカトリポカ。まったく、こんなに痛めつけられて可哀想に。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) == 0) + (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) != 0) * findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("何…？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	chara_vanquish findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	flt
	novoidlv = 1
	novoidlv = 1
	characreate -1, CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	snd SOUNDLIST_TAORE08
	listmax = 0
	buff = lang("がはっ…クソっ…逃げろテスカトリポカ…！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM) == 0) + (findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM) != 0) * findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	txtef COLOR_YELLOW
	txt lang("「光あれ」", "Let there be light...")
	msg_halt
	animeid = ANIMATION_RAGNA
	gosub *anime
	music = MUSICLIST_MCLASTBOSS4
	gosub *music_play
	chara_vanquish findchara(CREATURE_ID_THE_CHAOS_CHILD)
	flt
	initlv = 99999
	novoidlv = 1
	characreate -1, CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD, 10, 8
	sdata(SKILL_ATTR_STR, rc) = 666666
	sdata(SKILL_ATTR_CON, rc) = 666666
	sdata(SKILL_ATTR_DEX, rc) = 666666
	sdata(SKILL_ATTR_LER, rc) = 666666
	sdata(SKILL_ATTR_PER, rc) = 666666
	sdata(SKILL_ATTR_WIL, rc) = 666666
	sdata(SKILL_ATTR_MAG, rc) = 666666
	sdata(SKILL_ATTR_CHA, rc) = 666666
	tc = findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	gosub *chat_refresh
	listmax = 0
	buff = lang("わが名はヤルダバオート。私は妬む神である。私の他に神は不要だ。そして…そこの矮小な器も用済みだ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	gosub *chat_refresh
	listmax = 0
	buff = lang("ヤルダバオート…？器…？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("こいつは…私に憑依し、ずっと思考をコントロールしてきたんだ。神々の目を誤魔化すため…様々な細工まで施して、な。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM) == 0) + (findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM) != 0) * findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("まさか…今日この時、定命の肉体で神の間を破壊するために、オルフェを隠れ蓑として利用していたのか！？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	gosub *chat_refresh
	listmax = 0
	buff = lang("本来の肉体は駄母エンテュメイシスの子宮に還り神力を奪っていたが、奪い尽くすには膨大な時間が必要だったのでな。その間に世界を弄び、混沌の下地を創ろうとしたのだが。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("イレギュラー、お前の行動が邪魔で完遂できなかったよ。だが、この圧倒的な力を手に入れた今では些細な事だ…。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	gosub *chat_refresh
	listmax = 0
	buff = lang("もういい…オルフェ！お前は上の階に退避しろ！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("くっ…すまない…！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM) == 0) + (findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM) != 0) * findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	chara_vanquish findchara(CREATURE_ID_ORPHE_THE_PRINCE_OF_LOST_KINGDOM)
	snd SOUNDLIST_EXITMAP1
	tc = findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	gosub *chat_refresh
	listmax = 0
	buff = lang("どこへでも逃げるがいい。消滅する順番が少し変わるだけだ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("さて…。頭では覚えていても肉体が動きを忘れている…これが忘却の女神の力か。これでは肉弾戦は不可能、厄介だな。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("神力の総量もだいぶ減ってしまったようだが…それでも、それでもだ。脆弱な神々と世界を消し炭にするには充分！光を呑み込み、混沌世界を創造し、私こそが造物主…唯一の神として君臨してくれよう！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	gosub *chat_refresh
	listmax = 0
	buff = lang("愚劣で傲慢な偽りの神め…その力の源が何かわかっているのか？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("神のもつ神力とは元をただせば定命たちの願い。魂にしてアーカーシャの回廊より現出するアストラル光。それを踏みにじる貴様に、真の光を手にすることはできない！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	gosub *chat_refresh
	listmax = 0
	buff = lang("それで？", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	gosub *chat_refresh
	listmax = 0
	buff = lang("我は他の神々から力を託された軍神テスカトリポカ。この肉体が滅びようとも、貴様の好きにはさせん！いくぞ、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "！ここが正念場だっ！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	tc = findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	gosub *chat_refresh
	listmax = 0
	buff = lang("ほざいているがいい。少しの間だが遊んでやろう。お前たちがどこまで耐えられるか楽しみだな。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) == 0) + (findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD) != 0) * findchara(CREATURE_ID_JALDABAOTH_THE_CHAOS_CHILD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	txtnew
	txtef COLOR_RED
	txt lang("ヤルダバオート中心に世界が回り始める…。", "The world begins to distort, centering around Jaldabaoth...")
	gdata(GDATA_FLAG_MAIN) = 865
	txt lang("声が聞こえる…。", "You heard someone's voice...")
	gdata(GDATA_FLAG_CUT_IN_MESSAGE_1) = 200
	animeid = ANIMATION_LASTBOSS_2
	anic = 0
	gosub *anime
	goto *chat_end

*chat_unique_orphe
	music = MUSICLIST_MCLASTBOSS3
	gosub *music_play
	listmax = 0
	buff = lang("遅かったな。ちょうど神の間をシステムごと破壊し尽したところだ。少々骨が折れたが。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) == 0) + (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) != 0) * findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("オルフェ…ここで神力を全解放して暴れる気か！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) == 0) + (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) != 0) * findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("他の神々も呼んだらどうだ？一柱ずつ、目の前でくびり殺してやろう。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) == 0) + (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) != 0) * findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("それには及ばん。神の間が破壊されていた場合も想定して、こちらも用意をしてきた。…今こそ輝け、来光の牙よ！かつての戦友を止める力を！大戦の罪をあがなうための力をこの身に！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) == 0) + (findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD) != 0) * findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	animeid = ANIMATION_GAMEWIN3
	gosub *anime
	gosub *chat_refresh
	chara_vanquish findchara(CREATURE_ID_BLACK_GAUNTLET_LEOLD)
	flt
	initlv = 12000
	novoidlv = 1
	characreate -1, CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA, 10, 12
	cdata(CDATA_RELATION, rc) = RELATION_ALLY
	cdata(CDATA_RELATION_ORG, rc) = RELATION_ALLY
	cdata(CDATA_AI_AGGRO, rc) = 30
	redraw 0
	gosub *screen_draw
	gosub *screen_fadeIn3
	gsel BUFFER_BUF
	pos 0, 0
	picload exedir + "graphic\\face1" + devfile + ".bmp", 1
	gsel BUFFER_SCREEN
	snd SOUNDLIST_ATK_ELEC
	listmax = 0
	buff = lang("オルフェ！我らが、お前に引導を渡す！", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) == 0) + (findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA) != 0) * findchara(CREATURE_ID_YAYAUHQUI_TEZCATLIPOCA)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("なるほど、全ての神々の力を一柱に集中してきたか。手間が省けるが、楽しみも減ってしまったな。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) == 0) + (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) != 0) * findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("イレギュラー、お前もこんな消化試合につき合わされるとは哀れだな。本来こんなところまで来るはずがなかったのに、だ。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) == 0) + (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) != 0) * findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	listmax = 0
	buff = lang("終わらせてやろう。この世界も、お前の冒険も。", "Sorry, this is untranslated sentence.")
	tc = tc * (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) == 0) + (findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE) != 0) * findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE)
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	cbitmod CHARA_BIT_SUMMONED, findchara(CREATURE_ID_ORPHE_THE_CHAOS_DISCPLE), TRUE
	gdata(GDATA_FLAG_MAIN) = 855
	txt lang("声が聞こえる…。", "You heard someone's voice...")
	gdata(GDATA_FLAG_CUT_IN_MESSAGE_1) = 1
	animeid = ANIMATION_LASTBOSS_1
	anic = 0
	gosub *anime
	goto *chat_end

*chat_evOpening
	skillgain CHARA_PLAYER, csskillinit, 1
	skillmod csskillinit, CHARA_PLAYER, 9000
	modgrowth CHARA_PLAYER, csskillinit, 83
	csskillinit = 0
	autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
	if ( dbg_skipcm == 1 ) {
		goto *chat_end
	}
	if ( lomiaseaster ) {
		cdata(CDATA_CONDITION_BLIND, CHARA_PLAYER) = 100
		listmax = 0
		buff = lang("この洞窟…雨をしのぐにはちょうどいいわ。ロミアス、危険がないか奥を調べて来て。", "This cave...it's a good place to keep out the rain. Lomias, check the inner chamber to be sure there is no danger lurking here.")
		tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("わかった。ここで待っていろ", "Okay. Wait here.")
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		gosub *screen_draw
		await 3000
		txtnew
		txtef COLOR_RED
		txt lang("何かは殺された。", "Something is killed.")
		txtvalid = 0
		txt lang("「ぐわぁ」", cnvtalk("Uggghhh!"))
		snd SOUNDLIST_KILL1
		spillblood 28, 6, 10
		flt
		itemcreate -1, ITEM_ID_BEGGARS_PENDANT, 28, 6, 0
		gosub *screen_draw
		await 500
		await 500
		listmax = 0
		buff = lang("…今の音は？ …ロミアス、大丈夫？", "...what was that sound? ...Lomias, are you alright?")
		tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("ああ、問題ない。どうやらこの洞窟は昔、誰かが住んでいたようだな。奥を見て来たが、今はもう使われていないようだ。", "It's nothing. Looks like this cave is long abandoned. It's a good place to stay.")
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("そう、ならば都合がいいわ。…あら、あなた何を持っているの？ …キャーッ、プチじゃない！", "I see, that's convenient for us...wait Lomias, what arey you carrying?...Argh! Putits!")
		tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("こいつらか？心配する必要はない。以前、人間にペットとして飼われていたのだろう、ふふ…私によくなついているようだ。", "Don't worry. It appears these putits had been kept as pets by someone. They are kind of...cute.")
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("うふ！あなたにも優しいところがあるのね。…来て。どうやら怪我人が意識を取り戻したみたいよ。", "Huh, sounds like even you have a soft spot...Come here, the injured is about to wake up.")
		tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		await 1500
		redraw 0
		gosub *screen_draw
		gosub *screen_fade
		gsel BUFFER_BUF
		pos 0, 0
		picload exedir + "graphic\\face1" + devfile + ".bmp", 1
		gsel BUFFER_SCREEN
		cdata(CDATA_CONDITION_BLIND, CHARA_PLAYER) = 0
		txt lang(" 夢…か… ", "It was...a dream...?")
	}
	else {
		txtnew
	}
	txt lang("あなたは意識をとりもどした。", "You regain consciousness.")
	gdata(GDATA_ADVENTURER_ORACLE) = 0
	if ( gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
		flt
		itemcreate -1, ITEM_ID_GOLD_PIECE, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1000000
	}
	if ( jp ) {
		listmax = 0
		buff = "…意識が…もう戻ったのか？ 驚いたな。君の回復を待つために、我々の急を要する旅がいつまで中断されるのか、気を揉んでいたのだが。"
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "君は重傷を負い川辺に倒れていた。宵闇が辺りを覆う前に、癒し手の力を持つ我々に発見されたのは、全くよくできた偶然だ。"
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = " …そんな物珍しげな顔をするな。君の察する通り、我々は異形の森の民だ。エレアは…シエラ・テールの高潔なる異端者は、他種族の詮索に付き合う無駄な時間をあいにく持ち合わせていないが、君は、我々に拾われた幸運をもっと素直に喜ぶべきだな。瀕死の君を回復させることは、ここにいるラーネイレ以外の何者にも不可能だっただろう。なにせ彼女はエレアの…"
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "ロミアス、喋りすぎよ。たとえ意識の朦朧とした怪我人が相手だとしても。"
		tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "…そうだな。私の悪い癖だ、わかってはいる。…さて、" + cdatan(CDATAN_NAME, CHARA_PLAYER) + "といったな、"
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	else {
		listmax = 0
		buff = "...you...you're awake already? Remarkable. I was beginning to worry that nursing a lowly adventurer would bring our urgent travel to a halt."
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "You were badly wounded, passing out on the bank of a river. It was fortunate that we found you before the dark mantle of night enveloped this whole valley, almost as if Ehekatl, the goddess of luck herself had her eyes upon you."
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "...stop your curious eyes. Yes, we are sons of Vindale whom they call the irreverent forest. Though we Eleas, noble but blameless \"heretics\", aren't keen to spend idle time responding to every senseless question about our race, you should be more thankful for your fate. If it weren't the lady Larnneire who cured your mortal wound, you wouldn't be hearing my tirade. For the lady is no ordinary Elea and only she can..."
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "You talk too much Lomias, even though the one injured before you is still dazed."
		tc = tc * (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) == 0) + (findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND) != 0) * findchara(CREATURE_ID_LARNNEIRE_THE_LISTENER_OF_WIND)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = "...yes, it's a bad habit of mine. Well, " + cdatan(CDATAN_NAME, CHARA_PLAYER) + "..."
		tc = tc * (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) == 0) + (findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE) != 0) * findchara(CREATURE_ID_LOMIAS_THE_MESSENGER_FROM_VINDALE)
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	mdata(MDATA_MUSIC) = MUSICLIST_MCHOME0
	gosub *music_play
	goto *chat_unique

*chat_evReached
	listmax = 0
	buff = lang("無事に到着できてほっとした" + _yo() + _thanks(2), "We made it! Thank you!")
	tc = tc * (0 == 0) + (0 != 0) * 0
	list(0, listmax) = 0
	listn(0, listmax) = strmore
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( scenemode ) {
		if ( scene_cut == 1 ) {
			goto *chat_end
		}
	}
	goto *chat_end

*chat_default
	listmax = 0
	if ( tag == 0 ) {
		if ( buff == "" ) {
			gosub *random_talk
			if ( evochat == 0 ) {
				customtalk tc, DBMODE_FLAVOR_DIALOG
				if ( stat ) {
					gosub *convert_word
				}
			}
			else {
				if ( cdata(CDATA_EVOCHAT_POINTS, tc) >= 10 ) {
					hyouzyou = 3
				}
			}
			if ( cdata(CDATA_ROLE, tc) == ROLE_ADVENTURER ) {
				if ( cdata(CDATA_CUSTOM_TALK_LINE, tc) == 1 ) {
					txt lang(cdatan(CDATAN_AKA, tc) + "はあなたからのメッセージを思い出した…。", cdatan(CDATAN_AKA, tc) + " recalled your message...")
					cdata(CDATA_CUSTOM_TALK_LINE, tc) = 0
					modimp tc, 10
				}
				if ( cdata(CDATA_CUSTOM_TALK_LINE, tc) == 3 ) {
					txt lang(cdatan(CDATAN_AKA, tc) + "はあなたからのメッセージを思い出した…。", cdatan(CDATAN_AKA, tc) + " recalled your message...")
					cdata(CDATA_CUSTOM_TALK_LINE, tc) = 0
					if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
						modimp tc, 20
					}
					if ( cdata(CDATA_IMPRESSION, tc) < 150 ) {
						modimp tc, -7
					}
				}
			}
			if ( cdata(CDATA_INTEREST, tc) > 0 ) {
				if ( cdata(CDATA_RELATION, tc) != RELATION_ALLY ) {
					if ( tc >= MAX_CHARA_FOLLOWER ) {
						if ( gdata(GDATA_AREA) != AREA_DOCK | gdata(GDATA_LEVEL) != 70 ) {
							if ( rnd(3) == 0 ) {
								if ( cdata(CDATA_IMPRESSION, tc) < 100 ) {
									if ( rnd(sdata(SKILL_ATTR_CHA, CHARA_PLAYER) + 1) > 10 ) {
										modimp tc, rnd(3)
									}
									else {
										modimp tc, rnd(3) * (-1)
									}
								}
							}
						}
						cdata(CDATA_INTEREST, tc) -= rnd(30)
						cdata(CDATA_INTEREST_RENEW, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 8
					}
				}
			}
		}
		if ( kinenflag == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("ふたりの特別な日", "Today is our special anniversary!")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("何かあったっけ？", "Was there something today?")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("えーとアレだよアレアレ", "Well, today is...hmmm....")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("どうでもいい", "It does not matter.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("世界最後の日", "Today is the last day in the world.")
			listmax++
			buff = lang("…今日は何の日？", "You know? Guess what we have today.")
			chatesc = 0
			gosub *chat_select
			if ( chatval != 4 ) {
				flt
				itemcreate -1, ITEM_ID_PRESENT, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
				inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("ふふん♪", "That's right!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cdata(CDATA_EMO_ICON, tc) = 17 + 100 * 3
				cdata(CDATA_EVOCHAT_POINTS, tc) = 10
				listmax = 0
				buff = lang("(" + name(tc) + "は上機嫌だ！)", "(" + name(tc) + " became a good mood!)")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 1 ) {
				cdata(CDATA_EMO_ICON, tc) = 18 + 100 * 3
				cdata(CDATA_EVOCHAT_POINTS, tc) = -5
				listmax = 0
				buff = lang("…別に！", "There is...nothing in particular!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("(" + name(tc) + "はなぜか不機嫌になった)", "(" + name(tc) + " got in a bad mood for some reason...)")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 2 ) {
				cdata(CDATA_EMO_ICON, tc) = 11 + 100 * 3
				list(0, listmax) = 0
				listn(0, listmax) = lang("アレ！", "I do not remember...")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("コレ！", "Please give me a hint.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("ソレ！", "By the way, it is nice weather today.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("もしかして記念日…だっけ？", "Was it today something anniversary?")
				listmax++
				list(0, listmax) = 4
				listn(0, listmax) = lang("何だっけ、ここまで出てるんだけどなー！", "Please wait until I recollect the answer.")
				listmax++
				buff = lang("…どれ？", "So what?")
				chatesc = 0
				gosub *chat_select
				listmax = 0
				buff = lang("もういい。", "Whatever, forget it.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("(" + name(tc) + "は呆れている…)", "(" + name(tc) + " is sick of you...)")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			if ( chatval == 3 ) {
				cdata(CDATA_EMO_ICON, tc) = 26 + 100 * 3
				cdata(CDATA_EVOCHAT_POINTS, tc) = -10
				listmax = 0
				buff = lang("どうでもよくない…！", "That's right!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("(" + name(tc) + "は悲しくなった)", "(" + name(tc) + " feel deeply sad.)")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				cdata(CDATA_EMO_ICON, tc) = 13 + 100 * 3
				goto *chat_end
			}
			if ( chatval == 4 ) {
				cdata(CDATA_EMO_ICON, tc) = 20 + 100 * 3
				list(0, listmax) = 0
				listn(0, listmax) = lang("ごめん寝ぼけてた", "I'm sorry. I was still half asleep.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("【もうおしまいだぁ…！】", "This world is over...!!")
				listmax++
				buff = lang("えっ！?", "What!?")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("こら！", "Hey, you!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					listmax = 0
					buff = lang("希望はある！明日のために戦おう！", "There is still hope! Let's fight for tomorrow!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gdata(GDATA_FLAG_PET_ANNIVERSARY_ACTIVE) = 1
					goto *chat_end
				}
			}
			goto *chat_end
			return
		}
	}
	if ( tag > 0 ) {
		tcbk = tc
		if ( tag \ 2 == 1 ) {
			tc = cdata(CDATA_TAGTEAM_PARTNER, tc)
		}
		if ( tag \ 2 == 1 ) {
			wadai++
		}
		if ( hyouzyou2 == 111 ) {
			customtalk tc, DBMODE_FLAVOR_TAG_EVO
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( tag \ 2 == 1 ) {
					if ( wadai \ 2 == 0 ) {
						buff = lang("(少し照れながらパートナーに耳打ちしている)", "(" + name(tc) + " seems to be a little pounding.)")
					}
					else {
						buff = lang("今後とも" + _yoro(3) + "。", "It is alone together.")
					}
				}
				else {
					if ( wadai \ 2 == 0 ) {
						buff = lang("(恥ずかしそうに頷いた)", "(" + name(tc) + " seems to be a little pounding.)")
					}
					else {
						buff = lang("もちろん" + _da(3) + "。", "It is alone together.")
					}
				}
			}
		}
		if ( hyouzyou2 == 110 ) {
			customtalk tc, DBMODE_FLAVOR_TAG
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( tag \ 2 == 1 ) {
					if ( wadai \ 2 == 0 ) {
						buff = lang("(話に困って、しょうもないジョークを言い始めた…)", "(" + name(tc) + " seems to be a little pounding.)")
					}
					else {
						buff = lang("背中は任せて" + _kure(3) + "。", "It is alone together.")
					}
				}
				else {
					if ( wadai \ 2 == 0 ) {
						buff = lang("(少し呆れているようだ…)", "(" + name(tc) + " seems to be a little pounding.)")
					}
					else {
						buff = lang("はいはい。", "It is alone together.")
					}
				}
			}
		}
		tag++
		if ( hyouzyou2 == 110 ) {
			if ( tag \ 2 == 0 ) {
				listmax = 0
				buff = buff
				tc = tc * (tc == CHARA_PLAYER) + (tc != CHARA_PLAYER) * tc
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			tc = tcbk
			if ( tag == 3 ) {
				hyouzyou = 0
				hyouzyou2 = 0
				tag = 0
				tagevochat = 0
				goto *chat_default
			}
			if ( tag \ 2 == 0 ) {
				goto *chat_default
			}
		}
	}
	if ( tagevochat == 0 ) {
		tag = 0
	}
	if ( evochat == 1 ) {
		if ( cdata(CDATA_EVOCHAT_POINTS, tc) >= 10 ) {
			hyouzyou = 3
		}
		if ( hyouzyou == 0 ) {
			customtalk tc, DBMODE_FLAVOR_DIALOG_B
			if ( stat ) {
				gosub *convert_word
			}
			else {
				if ( rnd(2) == 0 ) {
					buff = lang("(" + name(tc) + "は少し緊張しているようだ)", "(" + name(tc) + " seems to be a little pounding.)")
				}
				else {
					buff = lang("ここなら邪魔は入らない" + _na(3) + "。", "It is alone together.")
				}
			}
		}
		if ( hyouzyou == 2 ) {
			customtalk tc, DBMODE_FLAVOR_DIALOG_E
			if ( stat ) {
				gosub *convert_word
			}
			else {
				if ( rnd(2) == 0 ) {
					buff = lang("(" + name(tc) + "は反応に困っている…)", "(" + name(tc) + " seems to be embarrassment.)")
				}
				else {
					buff = lang("こ、こんなところで冗談はやめて" + _kure(3) + "。", "P...Please quit the joke in a place like this!")
				}
			}
		}
		if ( hyouzyou == 1 ) {
			customtalk tc, DBMODE_FLAVOR_DIALOG_F
			if ( stat ) {
				gosub *convert_word
			}
			else {
				if ( rnd(2) == 0 ) {
					buff = lang("(" + name(tc) + "は喜んでいる！)", "(" + name(tc) + " is pleased.)")
				}
				else {
					buff = lang("こういうの、すごく嬉しい" + _yo(3) + "…。", "This kind of thing is really glad...")
				}
			}
		}
		if ( hyouzyou == 3 ) {
			snd SOUNDLIST_MIND000
			customtalk tc, DBMODE_FLAVOR_DIALOG_H
			if ( stat ) {
				gosub *convert_word
			}
			else {
				if ( rnd(2) == 0 ) {
					buff = lang("(" + name(tc) + "は幸せな表情であなたをじっと見ている)", "(" + name(tc) + " is staring at you in a happy expression.)")
				}
				else {
					buff = lang(cdatan(CDATAN_NAME, CHARA_PLAYER) + "…" + _ore(3) + "は今とても幸せ" + _da(3) + "。", cdatan(CDATAN_NAME, CHARA_PLAYER) + "...I am very happy now.")
				}
			}
		}
	}
	if ( evochat == 2 ) {
		if ( hyouzyou == 0 ) {
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			customtalk tc, DBMODE_FLAVOR_HUG
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( rnd(3) == 0 ) {
					txt lang(name(tc) + "は力を抜いた…。", name(tc) + " seems to be a little pounding.")
				}
				else {
					if ( rnd(3) == 0 ) {
						txt lang("「甘えん坊さん！」", name(tc) + " is staring at you in a happy expression.")
					}
					else {
						txt lang(name(tc) + "の鼓動が伝わってくる…。", name(tc) + " seems to be a little pounding.")
					}
				}
			}
		}
		if ( hyouzyou == 1 ) {
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			customtalk tc, DBMODE_FLAVOR_NADE
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( rnd(3) == 0 ) {
					txt lang(name(tc) + "はリラックスしているようだ。", name(tc) + " seems to be a little relax.")
				}
				else {
					if ( rnd(3) == 0 ) {
						txt lang("「へへ…」", name(tc) + " is staring at you in a happy expression.")
					}
					else {
						txt lang(name(tc) + "はそっと撫で返してきた。", name(tc) + " gently stroked you.")
					}
				}
			}
		}
		if ( hyouzyou == 7 ) {
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			customtalk tc, DBMODE_FLAVOR_KISS
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( rnd(3) == 0 ) {
					txt lang(name(tc) + "はうっとりしている…。", name(tc) + " is staring at you in a happy expression.")
				}
				else {
					if ( rnd(3) == 0 ) {
						txt lang("「そんなにがっつかなくても…♪」", name(tc) + " is staring at you in a happy expression.")
					}
					else {
						txt lang("「キスだけでいい" + _noka(3) + "？」", name(tc) + " is staring at you in a happy expression.")
					}
				}
			}
		}
		if ( hyouzyou == 5 ) {
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			customtalk tc, DBMODE_FLAVOR_DIALOG_E
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( rnd(3) == 0 ) {
					txt lang(name(tc) + "は呆気にとられている。", name(tc) + " seems to be embarrassment.")
				}
				else {
					if ( rnd(3) == 0 ) {
						txt lang("「？？？」", name(tc) + " is staring at you in a happy expression.")
					}
					else {
						txt lang("「ええっ！？」", name(tc) + " seems to be embarrassment.")
					}
				}
			}
		}
		if ( hyouzyou == 6 ) {
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			msg_newline
			msgtemp = "  "
			customtalk tc, DBMODE_FLAVOR_EX_REACT
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( rnd(3) == 0 ) {
					txt lang(name(tc) + "は悪戯っぽい表情を浮かべた。", name(tc) + " is staring at you in a happy expression.")
				}
				else {
					if ( rnd(3) == 0 ) {
						txt lang("「ちょっと落ち着いて！」", name(tc) + " is staring at you in a happy expression.")
					}
					else {
						txt lang("「もうっ…」", name(tc) + " is staring at you in a happy expression.")
					}
				}
			}
		}
	}
	if ( evochat == 3 ) {
		cdata(CDATA_EMO_ICON, tc) = 0
		msg_newline
		msgtemp = "  "
		msg_newline
		msgtemp = "  "
		msg_newline
		msgtemp = "  "
		msg_newline
		msgtemp = "  "
		txt lang("＊" + limitcount + "＊", name(tc) + " begin" + _s(cc) + " to take " + his(cc) + " clothes off.")
		if ( hyouzyou == 16 ) {
			txtef COLOR_SKY_BLUE
			txt lang("あなたは息を整えた。", "You calmed yourself.")
		}
		if ( hyouzyou == 18 ) {
			cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER)--
			txtef COLOR_PINK
			txt lang(name(tc) + "はあなたの服を脱がせ始めた。", name(tc) + " begin" + _s(cc) + " to take your clothes off.")
		}
		if ( hyouzyou != 16 & hyouzyou != 18 ) {
			if ( limitcount <= 0 ) {
				await 100
				cdata(CDATA_EVOCHAT_POINTS, tc) += 3
				if ( tagevochat > 0 ) {
					cdata(CDATA_EVOCHAT_POINTS, ttc) += 3
					cdata(CDATA_EMO_ICON, ttc) = 17 + 100 * 3
				}
				txtef COLOR_PINK
				snd SOUNDLIST_HEART1
				cdata(CDATA_EMO_ICON, tc) = 17 + 100 * 3
				customtalk tc, DBMODE_FLAVOR_MIZU_ASOBI_END
				if ( stat ) {
					gosub *convert_word
				}
				if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
					if ( jp ) {
						txt "「とてもよかった" + _yo(3) + "」", "「す、すごすぎる" + _yo(3) + "」", "「も、もうだめ" + _da(3) + "」", "「こ、心地よかった" + _yo(3) + "」", "「さ、最高" + _da(3) + "」"
					}
					if ( en ) {
						txt "\"You are awesome!", "\"Oh my god....", "\"Okay, okay, you win!", "\"Holy...!"
					}
				}
				gosub *music_play
				goto *chat_end
				return
			}
			txtef COLOR_PINK
			customtalk tc, DBMODE_FLAVOR_MIZU_ASOBI
			if ( stat ) {
				gosub *convert_word
			}
			if ( gdata(GDATA_CUSTOM_TALK) == 100 ) {
				if ( jp ) {
					txt "「ぬれてるぅ」", "「びちゃびちゃ…」", "「うれしい、でも…」", "「ふぅふぅ」", "「うふふふ」"
				}
				if ( en ) {
					txt cnvtalk("Yes!"), cnvtalk("Ohhh"), "*gasp*", "*rumble*", cnvtalk("come on!")
				}
			}
		}
	}
	if ( tagevochat > 0 ) {
		if ( tag \ 2 == 0 ) {
			listmax = 0
			buff = buff
			tc = tc * (tc == CHARA_PLAYER) + (tc != CHARA_PLAYER) * tc
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		tc = tcbk
		if ( tag \ 2 == 0 ) {
			goto *chat_default
		}
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_MAID ) {
		if ( gdata(GDATA_GUEST) > 0 ) {
			list(0, listmax) = 58
			listn(0, listmax) = lang("客に会う", "Yes, I'll meet the guest now.")
			listmax++
			list(0, listmax) = 59
			listn(0, listmax) = lang("追い返す", "I don't want to meet anyone.")
			listmax++
		}
	}
	if ( evochat == 1 ) {
		if ( cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) > 0 & cdata(CDATA_EVOCHAT_POINTS, tc) < 10 ) {
			list(0, listmax) = 78
			listn(0, listmax) = lang("優しくする", "<Be kind>")
			listmax++
			list(0, listmax) = 79
			listn(0, listmax) = lang("困らせる", "<To embarrass>")
			listmax++
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 100000 ) {
				list(0, listmax) = 80
				listn(0, listmax) = lang("10万goldあげる（現在" + cdata(CDATA_GOLD, tc) + "gold所持）", "<Pass 100,000gold>")
				listmax++
			}
			list(0, listmax) = 101
			listn(0, listmax) = lang("【特別な気持ちいいこと】", "<<Interest in a little tail t'night>>")
			listmax++
			if ( tagevochat == 1 ) {
				list(0, listmax) = 111
				listn(0, listmax) = lang("タッグ間でevochatさせる", "<evochat without you>")
				listmax++
			}
			if ( cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) >= 4 ) {
				list(0, listmax) = 87
				listn(0, listmax) = lang("【暴走する】", "<<Get excited>>")
				listmax++
			}
		}
		list(0, listmax) = 81
		listn(0, listmax) = lang("おしまい", "<End evochat>")
		listmax++
	}
	if ( evochat == 2 ) {
		if ( cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) > 0 & cdata(CDATA_EVOCHAT_POINTS, tc) < 10 ) {
			customtalk tc, DBMODE_FLAVOR_EX_ACT
			if ( iroiro == "" ) {
				iroiro = lang("色々する", "(Various things)")
			}
			list(0, listmax) = 88
			listn(0, listmax) = lang("抱きつく", "<<Hug>>")
			listmax++
			list(0, listmax) = 89
			listn(0, listmax) = lang("なでなでする", "<<Pat>>")
			listmax++
			list(0, listmax) = 90
			listn(0, listmax) = lang("キスしまくる", "<<Kiss>>")
			listmax++
			list(0, listmax) = 91
			listn(0, listmax) = lang(iroiro + " ", "<<" + iroiro + ">>")
			listmax++
		}
		list(0, listmax) = 92
		listn(0, listmax) = lang("我に返る", "<Return>")
		listmax++
	}
	if ( evochat == 3 ) {
		list(0, listmax) = 102
		listn(0, listmax) = lang("優しく", "<Gently>")
		listmax++
		list(0, listmax) = 103
		listn(0, listmax) = lang("激しく", "<Violently>")
		listmax++
		list(0, listmax) = 104
		listn(0, listmax) = lang("小休憩", "<Small break>")
		listmax++
	}
	if ( evochat == 0 ) {
		if ( tc < MAX_CHARA_FOLLOWER ) {
			if ( cbit(CHARA_BIT_BODYGUARD, tc) == FALSE ) {
				if ( cbit(CHARA_BIT_GUARD_TEMP, tc) == FALSE ) {
					if ( cbit(CHARA_BIT_NECRO, tc) == FALSE ) {
						if ( cbit(CHARA_BIT_LIVESTOCK, tc) == FALSE ) {
							if ( cdata(CDATA_HUNGER, CHARA_PLAYER) <= 10000 & cdata(CDATA_HUNGER, tc) <= 6000 & cdata(CDATA_IMPRESSION, tc) >= 100 & cbit(CHARA_BIT_ANOREXIA, tc) != TRUE & cbit(CHARA_BIT_ANOREXIA, CHARA_PLAYER) != TRUE ) {
								list(0, listmax) = 84
								listn(0, listmax) = lang("一緒に食事する", "Let's have a meal together.")
								listmax++
							}
							if ( cdata(CDATA_MATERIAL1_COUNTER, tc) > 200 | cdata(CDATA_MATERIAL2_COUNTER, tc) > 200 | cdata(CDATA_MATERIAL3_COUNTER, tc) > 200 | cdata(CDATA_MATERIAL4_COUNTER, tc) > 200 | cdata(CDATA_MATERIAL5_COUNTER, tc) > 200 ) {
								list(0, listmax) = 66
								listn(0, listmax) = lang("マテリアルを受け取る", "Recieve materials.")
								listmax++
							}
						}
					}
				}
			}
		}
		if ( cdata(CDATA_INTEREST, tc) > 0 & chatval(1) == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("話がしたい", "Let's Talk.")
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) >= (1000 /*!!!@[ROLE_SHOP_MIN @@@ ROLE_SHOP_WEAPON]@!!! */) & cdata(CDATA_ROLE, tc) < (2000 /*!!!@[ROLE_GUEST_BEGGAR @@@ ROLE_GUEST_MIN]@!!! */) | cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
			list(0, listmax) = 10
			listn(0, listmax) = lang("買いたい", "I want to buy something.")
			listmax++
			list(0, listmax) = 11
			listn(0, listmax) = lang("売りたい", "I want to sell something.")
			listmax++
			if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_WANDER ) {
				list(0, listmax) = 31
				listn(0, listmax) = lang("襲撃するよ", "Prepare to die!")
				listmax++
			}
			if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BLACKMARKET ) {
				list(0, listmax) = 86
				listn(0, listmax) = lang("「キセル」が欲しい", "I want a kiseru.")
				listmax++
			}
			if ( cdata(CDATA_ROLE, tc) != ROLE_SHOP_WANDER & cdata(CDATA_ROLE, tc) != ROLE_SHOP_TRADE ) {
				list(0, listmax) = 12
				listn(0, listmax) = lang("投資したい", "Need someone to invest in your shop?")
				listmax++
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_BARTENDER ) {
			list(0, listmax) = 93
			listn(0, listmax) = lang("飲み物を注文する", "Bring me something to drink.") + " (" + calcmealvalued() + strgold + ")"
			listmax++
			list(0, listmax) = 112
			listn(0, listmax) = lang("スペシャルメニューを頼む (ブロンズ硬貨1枚)", "Bring me special drink. (1 bronze) ")
			listmax++
			list(0, listmax) = 33
			listn(0, listmax) = lang("仲間を呼び戻す", "call back my allies.")
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SLAVER ) {
			if ( get_freeally() != 0 ) {
				list(0, listmax) = 36
				listn(0, listmax) = lang("奴隷を買う", "I want to buy a slave.")
				listmax++
			}
			list(0, listmax) = 37
			listn(0, listmax) = lang("奴隷を売る", "I want to sell a slave.")
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_HORSE_MASTER ) {
			if ( get_freeally() != 0 ) {
				list(0, listmax) = 57
				listn(0, listmax) = lang("馬を買う", "I want to buy a horse.")
				listmax++
			}
		}
		if ( tc < MAX_CHARA_FOLLOWER ) {
			if ( cbit(CHARA_BIT_BODYGUARD, tc) == FALSE ) {
				if ( cbit(CHARA_BIT_GUARD_TEMP, tc) == FALSE ) {
					if ( cbit(CHARA_BIT_NECRO, tc) == FALSE ) {
						if ( cbit(CHARA_BIT_LIVESTOCK, tc) == FALSE ) {
							list(0, listmax) = 34
							listn(0, listmax) = lang("街で待機しろ", "Wait at the town.")
							listmax++
							if ( cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) > 0 & cdata(CDATA_EVOCHAT_POINTS, tc) < 10 & cdata(CDATA_IMPRESSION, tc) >= 100 ) {
								list(0, listmax) = 77
								listn(0, listmax) = lang("【evochat】", "<evochat>")
								listmax++
								if ( cdata(CDATA_IN_TAGTEAM, tc) != 0 ) {
									ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
									if ( cdata(CDATA_EVOCHAT_POINTS, ttc) < 10 & cdata(CDATA_IMPRESSION, ttc) >= 100 ) {
										if ( ttc != 0 ) {
											list(0, listmax) = 109
											listn(0, listmax) = lang("【タッグごとevochat】", "<tag evochat>")
											listmax++
										}
									}
								}
							}
							if ( cdata(CDATA_IN_TAGTEAM, tc) != 0 ) {
								ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
								if ( ttc != 0 ) {
									list(0, listmax) = 110
									listn(0, listmax) = lang("タッグ間のやりとりを見る", "Talk to your tag team partner.")
									listmax++
								}
							}
							if ( cdata(CDATA_PRAY, tc) == 0 ) {
								list(0, listmax) = 67
								listn(0, listmax) = lang("大事な仲間に指定", "Designated as precious ally.")
								listmax++
							}
							if ( cdata(CDATA_PRAY, tc) == 1 ) {
								list(0, listmax) = 68
								listn(0, listmax) = lang("大事な仲間指定を解除", "Cancel precious ally specified.")
								listmax++
							}
							if ( cdata(CDATA_TAMER_WHIP_PICKUP, tc) != 10 ) {
								list(0, listmax) = 71
								listn(0, listmax) = lang("足元のアイテム我慢", "Don't pick up items off the ground.")
								listmax++
							}
							if ( cdata(CDATA_TAMER_WHIP_PICKUP, tc) == 10 ) {
								list(0, listmax) = 72
								listn(0, listmax) = lang("アイテム我慢解除", "Do as you like with items on the ground.")
								listmax++
							}
							if ( cdata(CDATA_MASTER_SERVANT, tc) >= 150 ) {
								list(0, listmax) = 94
								listn(0, listmax) = lang("特殊指示を出す", "Instruct non-battle action.")
								listmax++
							}
							if ( cdata(CDATA_TXT, tc) != 0 ) {
								if ( cbit(CHARA_BIT_SHUT_UP, tc) == FALSE ) {
									list(0, listmax) = 48
									listn(0, listmax) = lang("黙らせる", "Shut up.")
									listmax++
								}
								else {
									list(0, listmax) = 48
									listn(0, listmax) = lang("喋らせる", "You can speak now.")
									listmax++
								}
							}
							if ( cdata(CDATA_PRAY, tc) == 0 ) {
								list(0, listmax) = 35
								listn(0, listmax) = lang("縁を切る", "I'm going to abandon you.")
								listmax++
							}
						}
					}
				}
			}
		}
		if ( cbit(CHARA_BIT_LIVESTOCK, tc) == TRUE & gdata(GDATA_AREA) != AREA_SHOW_HOUSE ) {
			if ( cdata(CDATA_INTEREST, tc) > 0 ) {
				list(0, listmax) = 96
				listn(0, listmax) = lang("芸を仕込む (アイテム消費)", "I will teach you performance. (item consume) ")
				listmax++
			}
			if ( cdata(CDATA_GOD, CHARA_PLAYER) != GOD_EYTH ) {
				list(0, listmax) = 97
				listn(0, listmax) = lang("家畜を神に捧げる", "I offer you to my god.")
				listmax++
			}
		}
		if ( cdata(CDATA_ROLE, tc) == (1000 /*!!!@[ROLE_SHOP_MIN @@@ ROLE_SHOP_WEAPON]@!!! */) | cdata(CDATA_ROLE, tc) == ROLE_SHOP_ARMOR ) {
			list(0, listmax) = 54
			listn(0, listmax) = lang("矢弾の充填", "I need ammos for my weapon.")
			listmax++
			list(0, listmax) = 63
			listn(0, listmax) = lang("武具の強化", "I want to harden my equipment.")
			listmax++
			list(0, listmax) = 64
			listn(0, listmax) = lang("武具の軽量化", "I want to make light my equipment.")
			listmax++
			list(0, listmax) = 95
			listn(0, listmax) = lang("武具の重量化", "I want to make heavy my equipment.")
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_INN ) {
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_BARTENDER2 & cdata(CDATA_ID, tc) != CREATURE_ID_CLERK ) {
				list(0, listmax) = 13
				listn(0, listmax) = lang("食事をとる", "Bring me something to eat.") + " (" + calcmealvalue() + strgold + ")"
				listmax++
				list(0, listmax) = 83
				listn(0, listmax) = lang("スペシャルメニューを頼む (ブロンズ硬貨1枚)", "Bring me special dish. (1 bronze) ")
				listmax++
				if ( gdata(GDATA_WEATHER) == WEATHER_ETHER | gdata(GDATA_WEATHER) == WEATHER_HARD_RAIN | gdata(GDATA_WEATHER) == WEATHER_SNOW ) {
					list(0, listmax) = 43
					listn(0, listmax) = lang("シェルターに入る", "Take me to the shelter.")
					listmax++
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_IDENTIFIER ) {
			list(0, listmax) = 75
			listn(0, listmax) = lang("魔法実習を受けたい", "Attend a magic practice.")
			listmax++
			list(0, listmax) = 14
			listn(0, listmax) = lang("鑑定したい", "I need you to identify an item.") + " (" + calcidentifyvalue(0) + strgold + ")"
			listmax++
			list(0, listmax) = 15
			listn(0, listmax) = lang("全て鑑定してほしい", "Identify all of my stuff.") + " (" + calcidentifyvalue(1) + strgold + ")"
			listmax++
			list(0, listmax) = 16
			listn(0, listmax) = lang("調査したい", "Investigate an item.") + " (" + calcidentifyvalue(2) + strgold + ")"
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_TRAINER ) {
			list(0, listmax) = 17
			listn(0, listmax) = lang("訓練したい", "Train me.")
			listmax++
			list(0, listmax) = 30
			listn(0, listmax) = lang("新しい能力を覚えたい", "What skills can you teach me?")
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_MAYOR ) {
			if ( mdata(MDATA_CITY_COMPLAINTS) == 0 ) {
				list(0, listmax) = 73
				listn(0, listmax) = lang("街の管理代行（必要発言力" + mdata(MDATA_CITY_AUTHORITY) + "/2000）", "I want on behalf of the town management.(Authority" + mdata(MDATA_CITY_AUTHORITY) + "/2000)")
				listmax++
			}
			if ( mdata(MDATA_CITY_COMPLAINTS) > 0 ) {
				list(0, listmax) = 74
				listn(0, listmax) = lang("街の管理コマンド", "I want to run the town management.")
				listmax++
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_DEED ) {
			list(0, listmax) = 99
			listn(0, listmax) = lang("乗り物を破棄したい", "I want to discard my world-vehicle.")
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_INFORMER ) {
			list(0, listmax) = 18
			listn(0, listmax) = lang("冒険者の情報(100Gold)", "Show me the list of adventurers.(100Gold)")
			listmax++
			list(0, listmax) = 105
			listn(0, listmax) = lang("冒険者への伝言(2000Gold)", "Convey my message to other adventurers.(2000Gold)")
			listmax++
			list(0, listmax) = 98
			listn(0, listmax) = lang("クエストの攻略情報(300Gold)", "Tell me the quest strategy information.(300Gold)")
			listmax++
			list(0, listmax) = 82
			listn(0, listmax) = lang("【誕生日を教える】", "[ I will inform my birthday.]")
			listmax++
			clientguide
			if ( stat != 0 ) {
				repeat stat
					list(0, listmax) = 10000 + cnt
					listn(0, listmax) = lang(cdatan(CDATAN_NAME, rtval(cnt)) + "の居場所を聞く(100Gold)", "Where is " + cdatan(CDATAN_NAME, rtval(cnt)) + "?(100Gold)")
					listmax++
				loop
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_HEALER ) {
			list(0, listmax) = 19
			listn(0, listmax) = lang("能力の復元", "Restore my attributes.") + "(" + calcrestorecost() + strgold + ")"
			listmax++
			list(0, listmax) = 62
			listn(0, listmax) = lang("装備の解呪", "I want to uncurse equipments.") + "(" + calcrestorecostc() + strgold + ")"
			listmax++
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_ADVENTURER ) {
			list(0, listmax) = 20
			listn(0, listmax) = lang("アイテム交換", "Are you interested in trade?")
			listmax++
			if ( cbit(CHARA_BIT_HIRED, tc) == FALSE ) {
				list(0, listmax) = 50
				listn(0, listmax) = lang("護衛を依頼する", "I want to hire you.")
				listmax++
				list(0, listmax) = 51
				listn(0, listmax) = lang("仲間に誘う", "Join me!")
				listmax++
			}
		}
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_ARENA_MASTER ) {
		list(0, listmax) = 21
		listn(0, listmax) = lang("試合に出る[決闘]", "I'm entering the arena. [Duel]")
		listmax++
		list(0, listmax) = 22
		listn(0, listmax) = lang("試合に出る[ランブル]", "I'm entering the arena. [Rumble]")
		listmax++
		list(0, listmax) = 23
		listn(0, listmax) = lang("成績を聞く", "Tell me my scores.")
		listmax++
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_PET_ARENA_MASTER ) {
		list(0, listmax) = 40
		listn(0, listmax) = lang("ペットデュエル", "I want to register my pet. [Duel]")
		listmax++
		list(0, listmax) = 41
		listn(0, listmax) = lang("チームバトル", "I want to register my team. [Team]")
		listmax++
		list(0, listmax) = 49
		listn(0, listmax) = lang("EXバトル", "I want to see extra teams. [Ex Battle]")
		listmax++
		list(0, listmax) = 42
		listn(0, listmax) = lang("成績を聞く", "Tell me my scores.")
		listmax++
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_MAID ) {
		list(0, listmax) = 45
		listn(0, listmax) = lang("家の名前を考えてくれ", "Think of a nice name for my house.")
		listmax++
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SISTER ) {
		if ( gdata(GDATA_AREA) != AREA_DOCK ) {
			list(0, listmax) = 46
			listn(0, listmax) = lang("免罪符を買いたい", "I want to buy an indulgence.")
			listmax++
		}
		if ( gdata(GDATA_AREA) == AREA_DOCK ) {
			list(0, listmax) = 108
			listn(0, listmax) = lang("仕事の話がしたい", "Let's talk about work.")
			listmax++
		}
	}
	tradecheck tc
	if ( stat ) {
		list(0, listmax) = 20
		listn(0, listmax) = lang("アイテム交換", "Are you interested in trade?")
		listmax++
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_GUARD ) {
		if ( gdata(GDATA_AREA) == AREA_HOME ) {
			list(0, listmax) = 85
			listn(0, listmax) = lang("邪魔者を追い出したい", "I want to drive out guest.")
			listmax++
		}
		clientguide
		if ( stat != 0 ) {
			repeat stat
				list(0, listmax) = 10000 + cnt
				listn(0, listmax) = lang(cdatan(CDATAN_NAME, rtval(cnt)) + "の居場所を聞く", "Where is " + cdatan(CDATAN_NAME, rtval(cnt)) + "?")
				listmax++
			loop
		}
		if ( itemfind(0, 284) != (-1) ) {
			list(0, listmax) = 32
			listn(0, listmax) = lang("落し物の財布を届ける", "Here is a lost wallet I found.")
			listmax++
		}
		else {
			if ( itemfind(0, 283) != (-1) ) {
				list(0, listmax) = 32
				listn(0, listmax) = lang("落し物のカバンを届ける。", "Here is a lost suitcase I found.")
				listmax++
			}
		}
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_RETURNER ) {
		list(0, listmax) = 53
		listn(0, listmax) = lang("帰還したい", "I want to return.")
		listmax++
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BOOK_RESERVE ) {
		if ( gdata(GDATA_FLAG_GUILD_MAGE) != 0 | gdata(GDATA_AREA) == AREA_YLVA_LIBRARY ) {
			list(0, listmax) = 55
			listn(0, listmax) = lang("魔法書の予約", "I want to reserve some books.")
			listmax++
		}
	}
	if ( cdata(CDATA_CONDITION_DRUNK, tc) != 0 | 0 ) {
		if ( gdata(GDATA_AREA) != AREA_SHOW_HOUSE ) {
			if ( tc >= MAX_CHARA_FOLLOWER ) {
				if ( evid() == (-1) ) {
					list(0, listmax) = 56
					listn(0, listmax) = lang("気持ちいいことしない？", "Interested in a little tail t'night?")
					listmax++
				}
			}
		}
	}
	if ( cdata(CDATA_QUALITY, tc) != FIX_QUALITY_UNIQUE ) {
		if ( cdata(CDATA_RELATION, tc) == RELATION_NEUTRAL ) {
			if ( gdata(GDATA_AREA) != AREA_SHOW_HOUSE ) {
				if ( tc >= MAX_CHARA_FOLLOWER ) {
					if ( evid() == (-1) ) {
						list(0, listmax) = 106
						listn(0, listmax) = lang("そこをどいて", "Exchange for my place.")
						listmax++
					}
				}
			}
		}
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_PROSTITUTE ) {
		if ( evid() == (-1) ) {
			list(0, listmax) = 60
			listn(0, listmax) = lang("暗い場所に移ろう", "I'll buy you.")
			listmax++
		}
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_CARAVAN_MASTER ) {
		if ( gdata(GDATA_AREA) != AREA_DOCK ) {
			list(0, listmax) = 61
			listn(0, listmax) = lang("観察する", "Observe caravan.")
			listmax++
		}
		if ( gdata(GDATA_AREA) == AREA_DOCK ) {
			list(0, listmax) = 107
			listn(0, listmax) = lang("仕事の話がしたい", "Let's talk about work.")
			listmax++
		}
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_CAPTAIN ) {
		if ( gdata(GDATA_FLAG_MAIN) == 362 ) {
			if ( gdata(GDATA_AREA) == AREA_PORT_KAPUL ) {
				if ( tc > MAX_CHARA_FOLLOWER ) {
					list(0, listmax) = 65
					listn(0, listmax) = lang("紹介状を見せる", "Show a letter of introduction.")
					listmax++
				}
			}
		}
	}
	if ( cdata(CDATA_ID, tc) == CREATURE_ID_DETECTIVE | cdata(CDATA_ID, tc) == CREATURE_ID_WEREWOLF_DETECTIVE ) {
		if ( tc > MAX_CHARA_FOLLOWER ) {
			list(0, listmax) = 70
			listn(0, listmax) = lang("身辺調査", "Background checks")
			listmax++
		}
	}
	if ( mdata(MDATA_WEREWOLF_STAGE) == 1 ) {
		if ( cdata(CDATA_ID, tc) == CREATURE_ID_CITIZEN | cdata(CDATA_ID, tc) == CREATURE_ID_CITIZEN2 | cdata(CDATA_ID, tc) == CREATURE_ID_CITIZEN4 | cdata(CDATA_ID, tc) == CREATURE_ID_CITIZEN3 | cdata(CDATA_ID, tc) == CREATURE_ID_WEREWOLF | cdata(CDATA_ID, tc) == CREATURE_ID_FOX_SPIRIT | cdata(CDATA_ID, tc) == CREATURE_ID_SUNBARARIAN | cdata(CDATA_ID, tc) == CREATURE_ID_DETECTIVE | cdata(CDATA_ID, tc) == CREATURE_ID_WEREWOLF_DETECTIVE | cdata(CDATA_ROLE, tc) == ROLE_ADVENTURER ) {
			if ( tc > MAX_CHARA_FOLLOWER ) {
				if ( cdata(CDATA_ROLE, tc) != ROLE_SPECIAL ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						list(0, listmax) = 69
						listn(0, listmax) = lang("汝は人狼なりや？", "Are You a Werewolf?")
						listmax++
					}
				}
			}
		}
	}
	f = 0
	deliver = -1, -1
	if ( gdata(GDATA_LEVEL) == 1 ) {
		repeat gdata(GDATA_CLIENT)
			if ( qdata(QDATA_MAP, cnt) == gdata(GDATA_AREA) ) {
				if ( qdata(QDATA_CLIENT, cnt) == tc ) {
					rq = cnt
					f = 1
					break
				}
			}
		loop
	}
	if ( f == 1 ) {
		repeat gdata(GDATA_CLIENT)
			if ( qdata(QDATA_EXIST, cnt) == QUEST_TYPE_NONE ) {
				continue
			}
			if ( qdata(QDATA_STATE, cnt) != QUEST_STATE_ONGOING ) {
				continue
			}
			if ( qdata(QDATA_TYPE, cnt) == QUEST_SUBTYPE_DELIVER ) {
				if ( qdata((10 /*!!!@[QDATA_DEST_CLIENT @@@ QDATA_ENCOUNTER]@!!! */), cnt) == rq ) {
					inv_getheader 0
					p = qdata(QDATA_DEST_ITEM, cnt)
					deliver = cnt
					repeat invrange, invhead
						if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
							continue
						}
						if ( inv(INV_ITEM_ID, cnt) == p ) {
							deliver(1) = cnt
							break
						}
					loop
				}
			}
		loop
		if ( qdata(QDATA_STATE, rq) == QUEST_STATE_SUCCESS ) {
			val = 3
			gosub *talk_quest
			gosub *quest_success
		}
		else {
			if ( qdata(QDATA_TYPE, rq) == QUEST_SUBTYPE_SUPPLY & qdata(QDATA_STATE, rq) == QUEST_STATE_ONGOING ) {
				supply = -1
				inv_getheader 0
				repeat invrange, invhead
					if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
						continue
					}
					if ( ibit(ITEM_BIT_NO_DROP, cnt) ) {
						continue
					}
					if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_COOK ) {
						if ( refitem(inv(INV_ITEM_ID, cnt), DBSPEC_TYPE) == FILTER_ITEM_FOOD ) {
							if ( inv(INV_ITEM_PARAM1, cnt) / 1000 == qdata(QDATA_PARAM1, rq) ) {
								if ( inv(INV_ITEM_PARAM2, cnt) == qdata(QDATA_PARAM2, rq) ) {
									supply = cnt
									break
								}
							}
						}
					}
					if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_SUPPLY | qdata(QDATA_EXIST, rq) == QUEST_TYPE_COLLECT ) {
						if ( inv(INV_ITEM_ID, cnt) == qdata(QDATA_DEST_ITEM, rq) ) {
							supply = cnt
							break
						}
					}
				loop
				if ( supply != (-1) ) {
					list(0, listmax) = 26
					listn(0, listmax) = lang(itemname(supply, 1) + "を納入する", "Here is " + itemname(supply, 1) + " you asked.")
					listmax++
				}
				else {
					list(0, listmax) = 24
					listn(0, listmax) = lang("依頼について", "About the work.")
					listmax++
				}
			}
			else {
				if ( qdata(QDATA_EXIST, rq) != QUEST_TYPE_NONE ) {
					list(0, listmax) = 24
					listn(0, listmax) = lang("依頼について", "About the work.")
					listmax++
				}
			}
		}
	}
	if ( deliver != (-1) & deliver(1) != (-1) ) {
		list(0, listmax) = 25
		listn(0, listmax) = lang("配達物を渡す", "Here's your delivery.")
		listmax++
	}
	else {
		if ( cdata(CDATA_EMO_ICON, tc) == 22 + 100 ) {
			qdata(QDATA_QUEST_TARGET, cdata(CDATA_QUEST_NPC, tc) - 1) != 0
			qdata(QDATA_QUEST_TARGET, cdata(CDATA_QUEST_NPC, tc) - 1) = 0
		}
	}
	if ( gdata(GDATA_AREA) == AREA_HOME ) {
		if ( tc >= MAX_CHARA_SAVE ) {
			if ( cdata(CDATA_ROLE, tc) != ROLE_NONE ) {
				if ( cdata(CDATA_ROLE, tc) >= (2000 /*!!!@[ROLE_GUEST_BEGGAR @@@ ROLE_GUEST_MIN]@!!! */) & cdata(CDATA_ROLE, tc) < MAX_ROLE_GUEST ) {
				}
				else {
					if ( evid() == (-1) ) {
						list(0, listmax) = 44
						listn(0, listmax) = lang("解雇する", "You are fired.")
						listmax++
					}
				}
			}
		}
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_NOYEL ) {
		if ( gdata(GDATA_FLAG_SUB_MOTHER) == 1000 ) {
			rc = findchara(CREATURE_ID_PAELS_MOM_LILY)
			if ( rc != CHARA_PLAYER ) {
				if ( cdata(CDATA_EXIST, rc) == CHAR_STATE_ALIVE ) {
					list(0, listmax) = 52
					listn(0, listmax) = lang("パエルの母を売る", "I want to sell Pael's mom.")
					listmax++
				}
			}
		}
	}
	if ( evochat == 0 ) {
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
	}
	chatid = 0
	chatesc = 1
	gosub *chat_select
	if ( chatval == 10 | chatval == 11 ) {
		if ( cdata(CDATA_ROLE, tc) >= (1000 /*!!!@[ROLE_SHOP_MIN @@@ ROLE_SHOP_WEAPON]@!!! */) & cdata(CDATA_ROLE, tc) < (2000 /*!!!@[ROLE_GUEST_BEGGAR @@@ ROLE_GUEST_MIN]@!!! */) | cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
			if ( cdata(CDATA_KARMA, CHARA_PLAYER) < (-30) ) {
				if ( gdata(GDATA_AREA) != AREA_DERPHY ) {
					if ( gdata(GDATA_AREA) != AREA_HOME ) {
						if ( cbit(CHARA_BIT_INCOGNITO, CHARA_PLAYER) == FALSE ) {
							listmax = 0
							buff = lang("犯罪者に売る物はない" + _yo(), "I don't have business with criminals.")
							tc = tc * (0 == 0) + (0 != 0) * 0
							list(0, listmax) = 0
							listn(0, listmax) = strmore
							listmax++
							chatesc = 1
							gosub *chat_select
							if ( scenemode ) {
								if ( scene_cut == 1 ) {
									goto *chat_end
								}
							}
							goto *chat_default
						}
					}
				}
			}
		}
	}
	if ( chatval == 1 ) {
		buff = ""
		goto *chat_default
	}
	if ( chatval == 10 ) {
		invctrl = INVCTRL_SHOP_BUY
		invfile = cdata(CDATA_ROLE_FILE_ID, tc)
		gosub *shop
		screenupdate = -1
		gosub *screen_draw
		cs = 0
		buff = ""
		goto *chat_default
	}
	if ( chatval == 11 ) {
		invctrl = INVCTRL_SHOP_SELL
		invfile = cdata(CDATA_ROLE_FILE_ID, tc)
		gosub *shop
		cc = CHARA_PLAYER
		screenupdate = -1
		gosub *screen_draw
		cs = 0
		buff = ""
		goto *chat_default
	}
	if ( chatval == 12 ) {
		goto *chat_invest
	}
	if ( chatval == 13 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < calcmealvalue() ) {
			buff = strnogold
			goto *chat_default
		}
		if ( cdata(CDATA_HUNGER, CHARA_PLAYER) >= 15000 ) {
			buff = lang("腹が減っているようにはみえない" + _yo(), "You don't seem that hungry.")
			goto *chat_default
		}
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcmealvalue()
		snd SOUNDLIST_EAT1
		cdata(CDATA_HUNGER, CHARA_PLAYER) = 15000
		buff = lang(_dozo(), "Here you are.")
		txt lang("なかなか美味しかった。", "It was tasty."), lang("悪くない。", "Not bad at all."), lang("あなたは舌鼓をうった。", "You smack your lips.")
		gosub *eat_finish
		chara_anorexia 0
		goto *chat_default
	}
	if ( chatval == 83 ) {
		item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
				if ( cdata(CDATA_HUNGER, CHARA_PLAYER) >= 15000 ) {
					buff = lang("腹が減っているようにはみえない" + _yo(), "You don't seem that hungry.")
					goto *chat_default
				}
				inv(INV_ITEM_NUM, ci)--
				snd SOUNDLIST_PAYGOLD1
				listmax = 0
				buff = lang(_dozo(), "Here you are.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				repeat 245
					if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
						continue
					}
					if ( cnt >= 16 ) {
						continue
					}
					tc = cnt
					if ( cdata(CDATA_CONDITION_SLEEP, tc) != 0 | cdata(CDATA_CONDITION_DIM, tc) >= 60 | cdata(CDATA_CONDITION_BLIND, tc) != 0 | cdata(CDATA_CONDITION_PARALYZE, tc) != 0 | cdata(CDATA_CONDITION_SUFFOCATION, tc) != 0 | cdata(CDATA_CONDITION_BRAINWASH, tc) != 0 ) {
						continue
					}
					if ( tc == CHARA_PLAYER | rnd(2) == 0 ) {
						snd SOUNDLIST_EAT1
					}
					cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
					addbuff tc, BUFF_GROW_STRENGTH, 15, 5000
					addbuff tc, BUFF_GROW_CONSTITUTION, 15, 5000
					addbuff tc, BUFF_GROW_DEXTERITY, 15, 5000
					addbuff tc, BUFF_GROW_PERCEPTION, 15, 5000
					addbuff tc, BUFF_GROW_LEARNING, 15, 5000
					addbuff tc, BUFF_GROW_WILL, 15, 5000
					addbuff tc, BUFF_GROW_MAGIC, 15, 5000
					addbuff tc, BUFF_GROW_CHARISMA, 15, 5000
					addbuff tc, BUFF_GROW_SPEED, 15, 5000
					chara_anorexia tc
					if ( cdata(CDATA_HUNGER, tc) < 15000 ) {
						cdata(CDATA_HUNGER, tc) = 15000
					}
				loop
				txtef COLOR_GREEN
				txt lang("すごく美味しい！", "Delicious!"), lang("これは凄くいける！", "Gee what a good taste!"), lang("とてもいい味だ！", "It tasted pretty good!")
				cc = CHARA_PLAYER
				gosub *eat_finish
				goto *chat_end
			}
		}
		buff = lang("冷やかし" + _ka(1), "You kidding? ")
		goto *chat_default
	}
	if ( chatval == 93 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < calcmealvalued() ) {
			buff = strnogold
			goto *chat_default
		}
		if ( cdata(CDATA_THIRST, CHARA_PLAYER) >= 15000 ) {
			buff = lang("喉が渇いているようにはみえない" + _yo(), "You don't seem that thirsty.")
			goto *chat_default
		}
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcmealvalued()
		snd SOUNDLIST_DRINK1
		cdata(CDATA_THIRST, CHARA_PLAYER) = 15000
		if ( cdata(CDATA_HUNGER, CHARA_PLAYER) < 15000 - 1000 ) {
			cdata(CDATA_HUNGER, CHARA_PLAYER) += 1000
		}
		buff = lang(_dozo(), "Here you are.")
		txt lang("なかなか美味しかった。", "It was tasty."), lang("悪くない。", "Not bad at all."), lang("あなたは舌鼓をうった。", "You smack your lips.")
		goto *chat_default
	}
	if ( chatval == 112 ) {
		item_find ITEM_ID_BRONZE_COIN, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
				if ( cdata(CDATA_THIRST, CHARA_PLAYER) >= 15000 ) {
					buff = lang("喉が渇いているようにはみえない" + _yo(), "You don't seem that thirsty.")
					goto *chat_default
				}
				inv(INV_ITEM_NUM, ci)--
				snd SOUNDLIST_TM2_ALCHEMIST000
				listmax = 0
				buff = lang("嬉しい" + _yo(3) + "。腕の見せどころ" + _dana(3) + "。", "I'm happy to be able to do my main business. Please wait for a while.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				listmax = 0
				buff = lang("…。", "...")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				snd SOUNDLIST_KACHI08
				list(0, listmax) = 0
				listn(0, listmax) = lang("ぐっと飲み干す", "Drink all at once")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("ゆっくり味わう", "Taste slowly")
				listmax++
				buff = lang("…お待たせし" + _ta(3) + "。本日のスペシャルメニュー" + _da(3) + "。", "Here you are...")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 0 ) {
					val = 0
				}
				else {
					val = 1
				}
				repeat 245
					if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
						continue
					}
					if ( cnt >= 16 ) {
						continue
					}
					tc = cnt
					if ( cdata(CDATA_CONDITION_SLEEP, tc) != 0 | cdata(CDATA_CONDITION_DIM, tc) >= 60 | cdata(CDATA_CONDITION_BLIND, tc) != 0 | cdata(CDATA_CONDITION_PARALYZE, tc) != 0 | cdata(CDATA_CONDITION_SUFFOCATION, tc) != 0 | cdata(CDATA_CONDITION_BRAINWASH, tc) != 0 ) {
						continue
					}
					if ( tc == CHARA_PLAYER | rnd(2) == 0 ) {
						snd SOUNDLIST_DRINK1
					}
					cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
					addbuff tc, BUFF_LIFE_TASTING, 10, 5000
					cdata(CDATA_THIRST, tc) = 15000
					if ( cdata(CDATA_HUNGER, tc) < 15000 - 1000 ) {
						cdata(CDATA_HUNGER, tc) += 1000
					}
				loop
				txtef COLOR_GREEN
				if ( val == 0 ) {
					txt lang("くう〜っ！", "Delicious...!!!")
				}
				if ( val == 1 ) {
					txt lang("渇いた心と喉に染み渡るようだ…。", "It feels like my thirsty heart and throat are moisturized...")
				}
				cc = CHARA_PLAYER
				goto *chat_end
			}
		}
		buff = lang("冷やかし" + _ka(1), "You kidding? ")
		goto *chat_default
	}
	if ( chatval == 94 ) {
		list(0, listmax) = 0
		listn(0, listmax) = lang("別行動時は(遠距離)散策", "Roam while breaking apart.")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("別行動時は(近距離)散策", "Dull while breaking apart.")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("別行動時は直立不動で待機", "Stand while breaking apart.")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("別行動時でも随行", "Follow while breaking apart.")
		listmax++
		list(0, listmax) = 4
		listn(0, listmax) = lang("本職を優先", "Do main job while accompany.")
		listmax++
		list(0, listmax) = 5
		listn(0, listmax) = lang("気が向いたら路上演奏", "Perform.")
		listmax++
		list(0, listmax) = 6
		listn(0, listmax) = lang("たまに布教活動", "Propagate greatness of god.")
		listmax++
		list(0, listmax) = 7
		listn(0, listmax) = lang("時々ダンス披露", "Dance.")
		listmax++
		list(0, listmax) = 8
		listn(0, listmax) = lang("暇ならかたつむり駆除", "Exterminate snail.")
		listmax++
		list(0, listmax) = 9
		listn(0, listmax) = lang("客を見繕って売春", "Do prostitution.")
		listmax++
		list(0, listmax) = 10
		listn(0, listmax) = lang("街での訓練は禁止", "Do not train in town")
		listmax++
		list(0, listmax) = 11
		listn(0, listmax) = lang("その辺で物乞い", "Begging")
		listmax++
		txt lang("…どんな指示を出そうか？", "What kind of instructions should I give...?")
		gosub *chat_select
		if ( chatval >= 0 & chatval <= 11 ) {
			cdata(CDATA_AI_CALM, tc) = chatval + 1
			txt lang(cdatan(CDATAN_NAME, tc) + "に指示した。", "You instructed " + cdatan(CDATAN_NAME, tc) + ".")
		}
		goto *chat_default
	}
	if ( chatval == 100 ) {
		if ( gdata(GDATA_DAY) \ 3 == 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("やっぱりなんでもない", "After all nothing.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("愛している", "I love you.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("食べちゃいたい", "You are so cute I could eat you up.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("大好き", "I like you very much.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("頼りにしている", "I rely on you.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("これからが楽しみ", "I look forward to your future.")
			listmax++
			list(0, listmax) = 6
			listn(0, listmax) = lang("嫌いじゃない", "I do not hate you.")
			listmax++
			list(0, listmax) = 7
			listn(0, listmax) = lang("役立たず", "You are useless.")
			listmax++
			list(0, listmax) = 8
			listn(0, listmax) = lang("キモい", "I am you uncomfortable.")
			listmax++
			list(0, listmax) = 9
			listn(0, listmax) = lang("大嫌い", "I hate you.")
			listmax++
		}
		if ( gdata(GDATA_DAY) \ 3 == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("な、なんでもない！", "No, nothing!")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("最高に可愛い", "You are so cute.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("心酔しています", "I am devoured by you.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("すごく格好いい", "You are so cool.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("心強い", "I am reassured that you are there.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("期待している", "I am counting on you.")
			listmax++
			list(0, listmax) = 6
			listn(0, listmax) = lang("割と好き", "I like you a little.")
			listmax++
			list(0, listmax) = 7
			listn(0, listmax) = lang("興味ない", "I am not interested in you.")
			listmax++
			list(0, listmax) = 8
			listn(0, listmax) = lang("邪魔", "You are disturbing.")
			listmax++
			list(0, listmax) = 9
			listn(0, listmax) = lang("正直憎い", "I have hatred in you.")
			listmax++
		}
		if ( gdata(GDATA_DAY) \ 3 == 2 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("…うまく言葉に出来ない", "... I can't speak well.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("ずっと傍にいて", "I want you to be with me forever.")
			listmax++
			list(0, listmax) = 2
			listn(0, listmax) = lang("本当に感謝している", "I am really grateful to you.")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("心から尊敬している", "I sincerely respect you.")
			listmax++
			list(0, listmax) = 4
			listn(0, listmax) = lang("羨ましい", "I'm envious of you.")
			listmax++
			list(0, listmax) = 5
			listn(0, listmax) = lang("癒される", "I am always healed by you.")
			listmax++
			list(0, listmax) = 6
			listn(0, listmax) = lang("仲良くしたい", "I want to get along with you.")
			listmax++
			list(0, listmax) = 7
			listn(0, listmax) = lang("期待していない", "I do not expect from you.")
			listmax++
			list(0, listmax) = 8
			listn(0, listmax) = lang("うざい", "I'm fed up with you.")
			listmax++
			list(0, listmax) = 9
			listn(0, listmax) = lang("恨んでいる", "I have a resentment against you.")
			listmax++
		}
		buff = lang("", "")
		gosub *chat_select
		if ( chatval == 0 ) {
			txt lang(cdatan(CDATAN_NAME, tc) + "はあなたが何を伝えようとしたのか、すごく気になっている…。", cdatan(CDATAN_NAME, tc) + " cares what you tried to tell.")
			cdata(CDATA_EMO_ICON, tc) = 21 + 100 * 3
			goto *chat_end
		}
		if ( chatval >= 1 & chatval <= 3 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
				modimp tc, 12
				cdata(CDATA_EMO_ICON, tc) = 17 + 100 * 3
				txtef COLOR_GREEN
				txt lang(cdatan(CDATAN_NAME, tc) + "は" + _aisite(3) + "。", cdatan(CDATAN_NAME, tc) + " looks happy.")
			}
			if ( cdata(CDATA_IMPRESSION, tc) >= 75 & cdata(CDATA_IMPRESSION, tc) < 150 ) {
				listmax = 0
				buff = lang(cdatan(CDATAN_NAME, tc) + "は大袈裟な冗談だと思ったようだ。", cdatan(CDATAN_NAME, tc) + " seemed to think it was an exaggerated joke.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( cdata(CDATA_IMPRESSION, tc) < 75 ) {
				modimp tc, -5
				txtef COLOR_RED
				txt lang(cdatan(CDATAN_NAME, tc) + "は気持ち悪そうにあなたを見ている…。", cdatan(CDATAN_NAME, tc) + " felt you creepy.")
			}
			cbitmod CHARA_BIT_HAS_BEEN_CONFESSED_TO, tc, TRUE
		}
		if ( chatval >= 4 & chatval <= 6 ) {
			txtef COLOR_YELLOW
			txt lang(cdatan(CDATAN_NAME, tc) + "はまんざらでもないようだ。", cdatan(CDATAN_NAME, tc) + " felt it wasn't too bad.")
			modimp tc, 7
			cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
			cbitmod CHARA_BIT_HAS_BEEN_CONFESSED_TO, tc, TRUE
		}
		if ( chatval >= 7 & chatval <= 9 ) {
			if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
				dmgcon tc, CONDITION_CONFUSE, 1000
				cdata(CDATA_EMO_ICON, tc) = 21 + 100 * 3
				txt lang(cdatan(CDATAN_NAME, tc) + "は何かの間違いではないかと耳を疑っている…。", cdatan(CDATAN_NAME, tc) + " can't believe the ear...")
			}
			if ( cdata(CDATA_IMPRESSION, tc) >= 75 & cdata(CDATA_IMPRESSION, tc) < 150 ) {
				txt lang(cdatan(CDATAN_NAME, tc) + "は憐れむような目であなたを見た。", cdatan(CDATAN_NAME, tc) + " looked at you with merciful eyes.")
			}
			if ( cdata(CDATA_IMPRESSION, tc) < 75 ) {
				txt lang(cdatan(CDATAN_NAME, tc) + "はやれやれと肩をすくめた。", cdatan(CDATAN_NAME, tc) + " shrugged.")
			}
			cbitmod CHARA_BIT_HAS_BEEN_CONFESSED_TO, tc, TRUE
		}
		goto *chat_end
	}


	if ( chatval == 85 ) {
*label_6463
		if ( 1 == 0 ) {
			goto *label_6465
		}
		txtnew
		txt lang("誰を追い出す？", "Who do you drive out?")
		allyctrl = 3
		gosub *com_listNpc
		if ( stat == (-1) ) {
			goto *label_6465
		}
		hometc = stat
		txt lang(cdatan(CDATAN_NAME, hometc) + "を追い出した。", cdatan(CDATAN_NAME, hometc) + " " + is(tc) + " moved to the location.")
		snd SOUNDLIST_BASH1
		chara_vanquish hometc
		kaiwa = rnd(4)
		if ( kaiwa == 0 ) {
			txtef COLOR_YELLOW
			txt cdatan(CDATAN_NAME, tc) + lang("「お帰りはこちら！」", " " + cnvtalk("Go back!"))
		}
		if ( kaiwa == 1 ) {
			txtef COLOR_YELLOW
			txt cdatan(CDATAN_NAME, tc) + lang("「おうちかえれ！」", " " + cnvtalk("Go your home!"))
		}
		if ( kaiwa == 2 ) {
			txtef COLOR_YELLOW
			txt cdatan(CDATAN_NAME, tc) + lang("「また会う日まで！」", " " + cnvtalk("Get out of here!"))
		}
		if ( kaiwa == 3 ) {
			txtef COLOR_YELLOW
			txt cdatan(CDATAN_NAME, tc) + lang("「またのご訪問を！」", " " + cnvtalk("See you again!"))
		}
		goto *label_6465
		goto *label_6463
*label_6465
		goto *chat_end
	}
	if ( chatval == 86 ) {
		value = (-cdata(CDATA_KARMA, CHARA_PLAYER) + 200) / 5 * (cdata(CDATA_DRUG_DEPENDENCE, CHARA_PLAYER) + 300)
		listmax = 0
		buff = lang("…。", "....")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		buff = lang("うちのキセルは特別製" + _da() + "値段は" + value + "gold" + _ga(3) + "、欲しい" + _noka(1), "I don't have business with criminals.")
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= value ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("欲しい…", "Alright.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("いらない", "I'll pass.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval != 1 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= value
		listmax = 0
		buff = lang("お客さんも悪い人間" + _dana(), "I don't have business with criminals.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		flt
		itemcreate -1, ITEM_ID_DRUG_KISERU, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
		txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
		goto *chat_end
	}
	if ( chatval >= 14 & chatval < 17 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < calcidentifyvalue(chatval - 14) ) {
			buff = strnogold
			goto *chat_default
		}
		inv_getheader 0
		p = 0
		repeat invrange, invhead
			if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
				continue
			}
			if ( inv(INV_ITEM_KNOWN, cnt) < ITEM_KNOWN_FULL ) {
				p++
			}
		loop
		if ( p == 0 ) {
			buff = lang("鑑定するアイテムはないみたい" + _da(), "Your items have already been identified.")
			goto *chat_default
		}
		if ( chatval == 15 ) {
			cdata(CDATA_GOLD, CHARA_PLAYER) -= calcidentifyvalue(1)
			inv_getheader 0
			p = 0, 0
			p = 0, 0
			repeat invrange, invhead
				if ( inv(INV_ITEM_NUM, cnt) == 0 ) {
					continue
				}
				if ( inv(INV_ITEM_KNOWN, cnt) < ITEM_KNOWN_FULL ) {
					item_identify cnt, -1, 250
					item_stack CHARA_PLAYER, cnt, 1
					p(1)++
					if ( idtresult >= 3 ) {
						p++
					}
				}
			loop
			txt lang("" + p(1) + "個の未判明のアイテムのうち、" + p + "個のアイテムが完全に判明した。", "" + p + " out of " + p(1) + " unknown items are fully identified.")
			buff = lang("鑑定結果はこの通り" + _da(), "Here, I have finished identifing your stuff.")
		}
		else {
			if ( chatval == 14 ) {
				efp = 250
			}
			else {
				efp = 1000
			}
			efid = SKILL_SPELL_IDENTIFY
			gosub *effect
			if ( efcancel == 1 ) {
				buff = lang("冷やかし" + _ka(1), "You kidding? ")
				goto *chat_default
			}
			if ( idtresult >= 3 ) {
				buff = lang("鑑定結果はこの通り" + _da(), "Here, I have finished identifing your stuff.")
			}
			else {
				buff = lang("さらなる知識を求めるのなら、調査する必要が" + _aru(), "You need to investigate it to gain more knowledge.")
			}
			cdata(CDATA_GOLD, CHARA_PLAYER) -= calcidentifyvalue(chatval - 14)
		}
		snd SOUNDLIST_PAYGOLD1
		goto *chat_default
	}
	if ( chatval == 17 ) {
		csctrl = 2
		goto *chat_train
	}
	if ( chatval == 18 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < 100 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		tcbk = tc
		txt lang("選択した冒険者の詳細を確認できる。", "If you select an adventurer, you can see the details.")
		csctrl = 4
		gosub *com_listAdv
		goto *chat_end
	}
	if ( chatval == 19 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < calcrestorecost() ) {
			buff = strnogold
			goto *chat_default
		}
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcrestorecost()
		tcbk = tc
		repeat 16
			if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
				continue
			}
			tc = cnt
			efid = SKILL_SPELL_RESTORE_BODY
			efp = 100
			gosub *effect
			txtmore
			efid = SKILL_SPELL_RESTORE_SPIRIT
			efp = 100
			gosub *effect
		loop
		tc = tcbk
		gosub *chat_refresh
		buff = lang("治療が完了し" + _ta(), "Done treatment. Take care!")
		goto *chat_default
	}
	if ( chatval == 20 ) {
		invsubroutine = 1
		inv_getheader tc
		repeat invrange, invhead
			if ( inv(INV_ITEM_NUM, cnt) != 0 ) {
				inv(INV_ITEM_KNOWN, cnt) = ITEM_KNOWN_FULL
			}
		loop
		invctrl = INVCTRL_TRADE_TARGET, 0
		gosub *com_inventory
		if ( stat == 0 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		buff = lang(_thanks(2), "Thanks!")
		goto *chat_default
	}
	if ( chatval == 21 | chatval == 22 ) {
		if ( gdata(GDATA_RIDER) != 0 ) {
			cell_findspace cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			if ( stat == 0 ) {
				txt lang("降りるスペースがない。", "There's no place to get off.")
				goto *chat_end
			}
			cell_setchara gdata(GDATA_RIDER), rtval, rtval(1)
			txt lang(name(gdata(GDATA_RIDER)) + "から降りた。", "You dismount from " + name(gdata(GDATA_RIDER)) + ".")
			ride_end
		}
		if ( cdata(CDATA_IN_TAGTEAM, CHARA_PLAYER) != 0 ) {
			cell_findspace cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			if ( stat == 0 ) {
				txt lang("タッグを解散するスペースがない。", "There's no place to get off.")
				goto *chat_end
			}
			ttc = cdata(CDATA_TAGTEAM_PARTNER, CHARA_PLAYER)
			cell_setchara ttc, rtval, rtval(1)
			tag_end 0
		}
		gdata(GDATA_QUEST_FAME) = calcfame(CHARA_PLAYER, (220 - gdata(GDATA_RANK_ARENA) / 50) * (100 + limit(adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)), 0, cdata(CDATA_LEVEL, CHARA_PLAYER))) / 100 + 2)
		listmax = 0
		if ( chatval == 21 ) {
			if ( adata(ADATA_ARENA_RENEW1, gdata(GDATA_AREA)) > gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 ) {
				if ( dbg_arena == 0 ) {
					buff = lang("残念だが、今日の試合はもう終了し" + _ta(), "The game is over today. Come again tommorow.")
					goto *chat_default
				}
			}
			repeat 50
				arenaop = 0, (100 - gdata(GDATA_RANK_ARENA) / 100) / 3 * 2 + 3, 3
				if ( arenaop(1) < adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) ) {
					arenaop(1) = adata(ADATA_ARENA_WIN, gdata(GDATA_AREA))
				}
				aitelv = arenaop(1)
				minlevel = limit(arenaop(1) / 3 * 2, 1, 130)
				flt arenaop(1)
				fixlv = arenaop(2)
				characreate MAX_CHARA_NC, CREATURE_ID_BUG, -3, 0
				if ( cmshade ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_PASCAL ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_WING_SNAIL ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_MURDER_KNIGHT ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_LITYOU ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_MAD_SCIENTIST ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_ISCA_THE_FALLEN_ANGEL ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_DUNGEON_CLEANER ) {
					continue
				}
				if ( cdata(CDATA_ID, rc) == CREATURE_ID_GODA_THE_CAPTAIN_OF_ORC ) {
					continue
				}
				if ( cdata(CDATA_LEVEL, rc) < minlevel ) {
					continue
				}
				if ( cdata(CDATA_RELATION_ORG, rc) != RELATION_ENEMY ) {
					continue
				}
				break
			loop
			arenaop(1) = cdata(CDATA_ID, rc)
			if ( gdata(GDATA_RANK_ARENA) / 100 == 1 & adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) >= 29 ) {
				listmax = 0
				buff = lang("ようこそアリーナのチャンピオン！" + _kimi(3) + "と戦うため、今日も挑戦者がやってきている" + _yo(2), "Welcome! Challenger has come to fight with you!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			buff = lang("今日の対戦相手はレベル" + aitelv + "相当の相手" + _da() + "挑戦する" + _noka(1), "The opponent is around level " + arenaop(2) + ". Want to give it a try? ")
		}
		else {
			if ( adata(ADATA_ARENA_RENEW2, gdata(GDATA_AREA)) > gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 ) {
				if ( dbg_arena == 0 ) {
					buff = lang("残念だが、今日の試合はもう終了し" + _ta(), "The game is over today. Come again tommorow.")
					goto *chat_default
				}
			}
			arenaop = 1, (100 - gdata(GDATA_RANK_ARENA) / 100) / 2 + 1
			if ( arenaop(1) < adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) * 4 / 5 ) {
				arenaop(1) = adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) * 4 / 5
			}
			if ( gdata(GDATA_RANK_ARENA) / 100 == 1 & adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) >= 29 ) {
				listmax = 0
				buff = lang("ようこそアリーナのチャンピオン！" + _kimi(3) + "と戦うため、今日も挑戦者たちがやってきている" + _yo(2), "Welcome! Challengers has come to fight with you!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			buff = lang("対戦相手はレベル" + arenaop(1) + "相当の相手複数" + _da() + "挑戦する" + _noka(1), "Your play is a group of monster around level " + arenaop(1) + ". Sounds easy huh?")
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("挑戦する", "Alright.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "I'll pass.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval != 1 ) {
			buff = lang("用があるときは声をかけて" + _kure(), "Alright. Call me if you changed your mind.")
			goto *chat_default
		}
		if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
			snd SOUNDLIST_FAIL1
			txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
			goto *chat_end
		}
		if ( arenaop == 0 ) {
			adata(ADATA_ARENA_RENEW1, gdata(GDATA_AREA)) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 24
		}
		if ( arenaop == 1 ) {
			adata(ADATA_ARENA_RENEW2, gdata(GDATA_AREA)) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 24
		}
		gdata(GDATA_QUEST) = QUEST_SUBTYPE_ELIMINATE, 1, 0, 1
		gdata(GDATA_RETURN_AREA) = gdata(GDATA_AREA), gdata(GDATA_LEVEL), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
		gdata(GDATA_TELEPORT_AREA) = AREA_ARENA
		gdata(GDATA_TELEPORT_LEVEL) = 1
		levelexitby = MAP_EXIT_TELEPORT
		chatteleport = 1
		goto *chat_end
	}
	if ( chatval == 40 | chatval == 41 | chatval == 49 ) {
		arenaimport = 0
		gdata(GDATA_QUEST_FAME) = calcfame(CHARA_PLAYER, (220 - gdata(GDATA_RANK_PET_ARENA) / 50) * (50 + limit(adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)), 0, 50)) / 100 + 2)
		if ( chatval == 49 ) {
			comctrl = 1
			gosub *com_import
			if ( stat == 0 ) {
				buff = lang("用があるときは声をかけて" + _kure(), "Alright. Call me if you changed your mind.")
				goto *chat_default
			}
			arenaimport = 1
		}
		listmax = 0
		if ( chatval == 40 ) {
			if ( adata(ADATA_NEFIA_GEM_CHANCE, gdata(GDATA_AREA)) <= gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 ) {
				adata((39 /*!!!@[ADATA_RANCH_PRODUCE_RESTRICTION @@@ ADATA_SHOP_TYPE_CHANGE_COOLDOWN]@!!! */), gdata(GDATA_AREA)) = 0
			}
			if ( adata((39 /*!!!@[ADATA_RANCH_PRODUCE_RESTRICTION @@@ ADATA_SHOP_TYPE_CHANGE_COOLDOWN]@!!! */), gdata(GDATA_AREA)) >= 3 ) {
				if ( dbg_arena == 0 ) {
					buff = lang("残念だが、今日の試合はもう終了し" + _ta(), "The game is over today. Come again tommorow.")
					goto *chat_default
				}
			}
			arenaop = 0, 1, (100 - gdata(GDATA_RANK_PET_ARENA) / 100) / 3 * 2 + 3
			if ( arenaop(2) < adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) ) {
				arenaop(2) = adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA))
			}
			if ( gdata(GDATA_RANK_PET_ARENA) / 100 == 1 & adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) >= 29 ) {
				listmax = 0
				buff = lang("ようこそペットの王！" + _kimi(3) + "のペットと戦うため、今日も挑戦者がやってきている" + _yo(2), "Welcome! Challenger has come to fight with your pet!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			buff = lang("一対一の戦いで、対戦相手はレベル" + arenaop(2) + "相当の相手" + _da() + "挑戦する" + _noka(1), "The opponent is around level " + arenaop(2) + ". Want to give it a try? ")
		}
		if ( chatval == 41 ) {
			if ( adata(ADATA_NEFIA_GEM_CHANCE, gdata(GDATA_AREA)) <= gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 ) {
				adata((39 /*!!!@[ADATA_RANCH_PRODUCE_RESTRICTION @@@ ADATA_SHOP_TYPE_CHANGE_COOLDOWN]@!!! */), gdata(GDATA_AREA)) = 0
			}
			if ( adata((39 /*!!!@[ADATA_RANCH_PRODUCE_RESTRICTION @@@ ADATA_SHOP_TYPE_CHANGE_COOLDOWN]@!!! */), gdata(GDATA_AREA)) >= 3 ) {
				if ( dbg_arena == 0 ) {
					buff = lang("残念だが、今日の試合はもう終了し" + _ta(), "The game is over today. Come again tommorow.")
					goto *chat_default
				}
			}
			arenaop = 0, 2, (100 - gdata(GDATA_RANK_PET_ARENA) / 100) / 2 + 1
			arenaop(1) = rnd(9 - 1) + 2
			if ( arenaop(2) < adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) * 4 / 5 ) {
				arenaop(2) = adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) * 4 / 5
			}
			if ( gdata(GDATA_RANK_PET_ARENA) / 100 == 1 & adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) >= 29 ) {
				listmax = 0
				buff = lang("ようこそペットの王！" + _kimi(3) + "のペットと戦うため、今日も挑戦者がやってきている" + _yo(2), "Welcome! Challenger has come to fight with your pet!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			buff = lang("" + arenaop(1) + "人同士のチームバトルで、対戦相手はレベル" + arenaop(2) + "の相手複数" + _da() + "挑戦する" + _noka(1), "It's a " + arenaop(1) + " vs " + arenaop(1) + " match. The opponent's group is formed by the pets less than " + arenaop(2) + " levels. What you say?")
		}
		if ( chatval == 49 ) {
			arenaop = 1, 2, rtval(1)
			enemylv = rtval(1)
			arenaop(1) = rtval
			buff = lang("" + arenaop(1) + "人同士のチームバトルで、対戦相手はレベル" + arenaop(2) + "以下の相手複数" + _da() + "挑戦する" + _noka(1), "It's a " + arenaop(1) + " vs " + arenaop(1) + " match. The opponent's group is formed by the pets less than " + arenaop(2) + " levels. What you say?")
		}
		list(0, listmax) = 1
		listn(0, listmax) = lang("挑戦する", "I'll send my pet.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "I'll pass.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval != 1 ) {
			buff = lang("用があるときは声をかけて" + _kure(), "Alright. Call me if you changed your mind.")
			goto *chat_default
		}
		if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
			snd SOUNDLIST_FAIL1
			txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
			goto *chat_end
		}
		if ( adata(ADATA_NEFIA_GEM_CHANCE, gdata(GDATA_AREA)) < gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 ) {
			adata(ADATA_NEFIA_GEM_CHANCE, gdata(GDATA_AREA)) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 24
		}
		dim followerexist, 16
		repeat 16
			followerexist(cnt) = cdata(CDATA_EXIST, cnt)
		loop
		allyctrl = 2
		gosub *com_ally
		if ( stat == (-1) ) {
			buff = lang("用があるときは声をかけて" + _kure(), "Alright. Call me if you changed your mind.")
			goto *chat_default
		}
		adata((39 /*!!!@[ADATA_RANCH_PRODUCE_RESTRICTION @@@ ADATA_SHOP_TYPE_CHANGE_COOLDOWN]@!!! */), gdata(GDATA_AREA))++
		gdata(GDATA_QUEST) = QUEST_SUBTYPE_DELIVER, 0, 0, 1
		gdata(GDATA_RETURN_AREA) = gdata(GDATA_AREA), gdata(GDATA_LEVEL), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
		gdata(GDATA_TELEPORT_AREA) = AREA_PET_ARENA
		gdata(GDATA_TELEPORT_LEVEL) = 1
		levelexitby = MAP_EXIT_TELEPORT
		chatteleport = 1
		goto *chat_end
	}
	if ( chatval == 42 ) {
		buff = lang("現在は" + adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) + "勝" + _da() + "3勝、5勝、10勝、20勝、30勝、50勝、100勝の倍数毎にボーナスを与え" + _ru(), "Your winning record has reached " + adata(ADATA_PET_ARENA_WIN, gdata(GDATA_AREA)) + " matchs now. Keep the audience excited. You get nice bonus at every 3th,5th,10th,20th,30th,50th,100th wins in a row.")
		goto *chat_default
	}
	if ( chatval == 23 ) {
		buff = lang("現在は" + adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) + "勝" + _da() + "3勝、5勝、10勝、20勝、30勝、50勝、100勝の倍数毎にボーナスを与え" + _ru(), "Your winning record has reached " + adata(ADATA_ARENA_WIN, gdata(GDATA_AREA)) + " matchs now. Keep the audience excited. You get nice bonus at every 3th,5th,10th,20th,30th,50th,100th wins in a row.")
		goto *chat_default
	}
	if ( chatval == 24 ) {
		goto *chat_quest
	}
	if ( chatval == 25 ) {
		gosub *chara_adjustInv
		ti = stat
		item_copy deliver(1), ti
		inv(INV_ITEM_NUM, ti) = 1
		ci = ti
		rc = tc
		gosub *chara_item
		rq = deliver
		inv(INV_ITEM_NUM, deliver(1))--
		txt lang(itemname(deliver(1), 1) + "を手渡した。", "You hand over " + itemname(deliver(1), 1) + ".")
		val = 3
		gosub *talk_quest
		gosub *quest_success
		gosub *calcBurdenPc
		goto *chat_default
	}
	if ( chatval == 26 ) {
		gosub *chara_adjustInv
		ti = stat
		item_copy supply, ti
		inv(INV_ITEM_NUM, ti) = 1
		cbitmod CHARA_BIT_POISON_FLAG, tc, TRUE
		ci = ti
		rc = tc
		gosub *chara_item
		inv(INV_ITEM_NUM, supply)--
		txt lang(itemname(supply, 1) + "を手渡した。", "You hand over " + itemname(supply, 1) + ".")
		val = 3
		gosub *talk_quest
		gosub *quest_success
		gosub *calcBurdenPc
		goto *chat_default
	}
	if ( chatval == 30 ) {
		csctrl = 3
		goto *chat_train
	}
	if ( chatval == 31 ) {
		listmax = 0
		buff = lang(_rob(2), "Oh crap. Another bandit trying to spoil my business! Form up, mercenaries.")
		list(0, listmax) = 1
		listn(0, listmax) = lang("神に祈れ", "Pray to your God.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("いや、冗談です", "W-Wait! I was just kidding.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval != 1 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		gohostile
		goto *chat_end
	}
	if ( chatval == 32 ) {
		listmax = 0
		p = itemfind(0, 284)
		if ( p == (-1) ) {
			p = itemfind(0, 283)
		}
		inv(INV_ITEM_NUM, p)--
		if ( inv(INV_ITEM_PARAM1, p) == 0 ) {
			buff = lang("む…中身が空っぽ" + _dana(2), "Hmm! It's empty!")
			list(0, listmax) = 0
			listn(0, listmax) = lang("しまった…", "Oops...!")
			listmax++
			chatesc = 1
			gosub *chat_select
			modkarma 0, -5
		}
		else {
			buff = lang("わざわざ落し物を届けてくれた" + _noka() + _kimi(3) + "は市民の模範" + _da() + _thanks(), "How nice of you to take the trouble to bring it. You're a model citizen indeed!")
			list(0, listmax) = 0
			listn(0, listmax) = lang("当然のことだ", "It's nothing.")
			listmax++
			chatesc = 1
			gosub *chat_select
			modkarma 0, 5
			gdata(GDATA_WALLET)++
			if ( gdata(GDATA_WALLET) >= 10 ) {
				listmax = 0
				buff = lang("む、また" + _kimi(3) + _ka() + "随分と頻繁に財布を見つけられるもの" + _dana(), "Oh, it's you again? How come you find the wallets so often?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				buff = lang("（…あやしい）", "(...suspicious)")
				list(0, listmax) = 0
				listn(0, listmax) = lang("ぎくっ", "I really found it on the street!")
				listmax++
				chatesc = 1
				gosub *chat_select
				modkarma 0, -5
			}
		}
		gosub *calcBurdenPc
		buff = ""
		goto *chat_default
	}
	if ( chatval == 33 ) {
		allyctrl = 0
		gosub *com_ally
		if ( stat != (-1) ) {
			rc = stat
			if ( cdata(CDATA_EXIST, rc) == CHAR_STATE_ALIVE ) {
				buff = lang("そいつは呼び戻す必要はないよう" + _da(), "Huh? You don't need to do that.")
				goto *chat_default
			}
			if ( cdata(CDATA_EXIST, rc) == CHAR_STATE_ALLY_DEAD ) {
				if ( cdata(CDATA_CAN_REVIVE, rc) > 2 ) {
					cdata(CDATA_CAN_REVIVE, rc) = 0
				}
				if ( cdata(CDATA_CAN_REVIVE, rc) <= 0 ) {
					buff = lang("まだこっちに来ていない" + _na() + "一度、野外の空気でも吸ってきたらどう" + _kana(), "It can not be revived until the switch area.")
					goto *chat_default
				}
			}
			f = get_freeallyne()
			if ( f == 0 ) {
				txt lang("仲間の最大数に達しているため、呼び戻せない。", "Your party is already full. You can't invite someone anymore.")
				goto *chat_default
			}
			listmax = 0
			buff = lang("そいつを呼び戻すには、" + calcresurrectvalue(rc) + strgold + "必要" + _da(), "Alright. We had taken good care of your pet. It will cost you " + calcresurrectvalue(rc) + " gold pieces.")
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calcresurrectvalue(rc) ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("呼び戻す", "I'll pay.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "Never mind.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= calcresurrectvalue(rc)
				buff = lang("(バーテンが店の奥から" + name(rc) + "を連れてきた)" + _dozo(), "(" + name(tc) + " brings " + name(rc) + " from the stable.) There you go.")
				cdata(CDATA_CAN_REVIVE, rc)--
				gosub *resurrect_ally
			}
			else {
				buff = lang("冷やかし" + _ka(1), "You kidding? ")
			}
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 34 ) {
		listmax = 0
		buff = lang("(あなたは、" + name(tc) + "に街で待っているように指示した)", "(You order " + name(tc) + " to wait at the town.)")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( tc == gdata(GDATA_RIDER) ) {
			cell_findspace cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 1
			if ( stat == 0 ) {
				txt lang("降りるスペースがない。", "There's no place to get off.")
				goto *chat_end
			}
			cell_setchara gdata(GDATA_RIDER), rtval, rtval(1)
			cdata(CDATA_RIDING, gdata(GDATA_RIDER)) = 0
			ride_end
		}
		map(cdata(CDATA_X, tc), cdata(CDATA_Y, tc), MAP_CHARA_INDEX_PLUS_ONE) = 0
		cdata(CDATA_EXIST, tc) = CHAR_STATE_ALLY_WAIT
		cdata(CDATA_AREA, tc) = AREA_NONE
		if ( cdata(CDATA_IN_TAGTEAM, tc) == (-1) ) {
			ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
			cell_setchara ttc, cdata(CDATA_X, tc), cdata(CDATA_Y, tc)
			tag_end tc
		}
		if ( cdata(CDATA_IN_TAGTEAM, tc) == 1 ) {
			ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
			tag_end tc
			cell_movechara ttc, cdata(CDATA_X, ttc), cdata(CDATA_Y, ttc)
		}
		goto *chat_end
	}
	if ( chatval == 66 ) {
		txt lang(name(tc) + "は冒険の途中で集めたマテリアルを手渡してきた。", "You order " + name(tc) + " to wait at the town.")
		customtalk tc, DBMODE_FLAVOR_MATERIAL
		gosub *convert_word
		msg_halt
		snd SOUNDLIST_ALERT1
		snd SOUNDLIST_ALERT1
		snd SOUNDLIST_ALERT1
		m1 = 0
		m2 = 0
		m3 = 0
		m4 = 0
		m5 = 0
		repeat 500
			if ( cdata(CDATA_MATERIAL1_COUNTER, tc) < 40 ) {
				break
			}
			if ( cdata(CDATA_MATERIAL1_COUNTER, tc) >= 40 ) {
				matgetmain MATERIAL_PEBBLE, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL1_COUNTER, tc) -= 40
				m1++
			}
			if ( cdata(CDATA_MATERIAL1_COUNTER, tc) >= 100 ) {
				matgetmain MATERIAL_FINE_STONE, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL1_COUNTER, tc) -= 100
				m2++
			}
			if ( cdata(CDATA_MATERIAL1_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_ETHER_FRAGMENT, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL1_COUNTER, tc) -= 120
				m3++
			}
			if ( cdata(CDATA_MATERIAL1_COUNTER, tc) >= 150 ) {
				matgetmain MATERIAL_ELEMENT_FRAGMENT, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL1_COUNTER, tc) -= 150
				m4++
			}
			if ( cdata(CDATA_MATERIAL1_COUNTER, tc) >= 150 ) {
				matgetmain MATERIAL_CHAOS_STONE, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL1_COUNTER, tc) -= 150
				m5++
			}
		loop
		if ( m1 > 0 ) {
			txtef COLOR_BROWN
			txt lang("マテリアル:石ころを" + m1 + "個受け取った。", "You get " + m1 + " Pebble.")
		}
		if ( m2 > 0 ) {
			txtef COLOR_BROWN
			txt lang("マテリアル:上等な石を" + m2 + "個受け取った。", "You get " + m2 + " Fine stone.")
		}
		if ( m3 > 0 ) {
			txtef COLOR_BROWN
			txt lang("マテリアル:エーテルの欠片を" + m3 + "個受け取った。", "You get " + m3 + " Ether fragment.")
		}
		if ( m4 > 0 ) {
			txtef COLOR_BROWN
			txt lang("マテリアル:風切石を" + m4 + "個受け取った。", "You get " + m4 + " Element fragment.")
		}
		if ( m5 > 0 ) {
			txtef COLOR_BROWN
			txt lang("マテリアル:混沌の石を" + m5 + "個受け取った。", "You get " + m5 + " Chaos stone.")
		}
		m1 = 0
		m2 = 0
		m3 = 0
		m4 = 0
		m5 = 0
		m6 = 0
		m7 = 0
		repeat 500
			if ( cdata(CDATA_MATERIAL2_COUNTER, tc) < 20 ) {
				break
			}
			if ( cdata(CDATA_MATERIAL2_COUNTER, tc) >= 20 ) {
				matgetmain MATERIAL_WATERDROP, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL2_COUNTER, tc) -= 20
				m1++
			}
			if ( cdata(CDATA_MATERIAL2_COUNTER, tc) >= 60 ) {
				matgetmain MATERIAL_HOT_WATER, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL2_COUNTER, tc) -= 40
				m2++
			}
			if ( cdata(CDATA_MATERIAL2_COUNTER, tc) >= 80 ) {
				matgetmain MATERIAL_SNOW, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL2_COUNTER, tc) -= 80
				m3++
			}
			if ( cdata(CDATA_MATERIAL2_COUNTER, tc) >= 100 ) {
				matgetmain MATERIAL_WITCHS_TEAR, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL2_COUNTER, tc) -= 100
				m4++
			}
			if ( cdata(CDATA_MATERIAL2_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_ANGELS_TEAR, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL2_COUNTER, tc) -= 120
				m5++
			}
		loop
		if ( m1 > 0 ) {
			txtef COLOR_SKY_BLUE
			txt lang("マテリアル:水滴を" + m1 + "個受け取った。", "You get " + m1 + " Waterdrop.")
		}
		if ( m2 > 0 ) {
			txtef COLOR_SKY_BLUE
			txt lang("マテリアル:お湯を" + m2 + "個受け取った。", "You get " + m2 + " Hot water.")
		}
		if ( m3 > 0 ) {
			txtef COLOR_SKY_BLUE
			txt lang("マテリアル:雪を" + m3 + "個受け取った。", "You get " + m3 + " Snow.")
		}
		if ( m4 > 0 ) {
			txtef COLOR_SKY_BLUE
			txt lang("マテリアル:魔女の涙を" + m4 + "個受け取った。", "You get " + m4 + " Witch's tear.")
		}
		if ( m5 > 0 ) {
			txtef COLOR_SKY_BLUE
			txt lang("マテリアル:天使の涙を" + m5 + "個受け取った。", "You get " + m5 + " Angel's tear.")
		}
		m1 = 0
		m2 = 0
		m3 = 0
		m4 = 0
		m5 = 0
		repeat 500
			if ( cdata(CDATA_MATERIAL3_COUNTER, tc) < 30 ) {
				break
			}
			if ( cdata(CDATA_MATERIAL3_COUNTER, tc) >= 30 ) {
				matgetmain MATERIAL_STICK, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL3_COUNTER, tc) -= 30
				m1++
			}
			if ( cdata(CDATA_MATERIAL3_COUNTER, tc) >= 100 ) {
				matgetmain MATERIAL_BRANCH, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL3_COUNTER, tc) -= 40
				m2++
			}
			if ( cdata(CDATA_MATERIAL3_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_HOLY_WEED, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL3_COUNTER, tc) -= 120
				m3++
			}
			if ( cdata(CDATA_MATERIAL3_COUNTER, tc) >= 130 ) {
				matgetmain MATERIAL_SHINING_WEED, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL3_COUNTER, tc) -= 130
				m4++
			}
			if ( cdata(CDATA_MATERIAL3_COUNTER, tc) >= 150 ) {
				matgetmain MATERIAL_SAP_OF_YAGGDRASIL, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL3_COUNTER, tc) -= 150
				m5++
			}
		loop
		if ( m1 > 0 ) {
			txtef COLOR_LIGHT_GREEN
			txt lang("マテリアル:杖を" + m1 + "個受け取った。", "You get " + m1 + " Stick.")
		}
		if ( m2 > 0 ) {
			txtef COLOR_LIGHT_GREEN
			txt lang("マテリアル:枝を" + m2 + "個受け取った。", "You get " + m2 + " Branch.")
		}
		if ( m3 > 0 ) {
			txtef COLOR_LIGHT_GREEN
			txt lang("マテリアル:聖なる草を" + m3 + "個受け取った。", "You get " + m3 + " Holy weed.")
		}
		if ( m4 > 0 ) {
			txtef COLOR_LIGHT_GREEN
			txt lang("マテリアル:ヒカリソウを" + m4 + "個受け取った。", "You get " + m4 + " Shining weed.")
		}
		if ( m5 > 0 ) {
			txtef COLOR_LIGHT_GREEN
			txt lang("マテリアル:世界樹の樹液を" + m5 + "個受け取った。", "You get " + m5 + " Sap of Yaggdrasil.")
		}
		m1 = 0
		m2 = 0
		m3 = 0
		m4 = 0
		m5 = 0
		repeat 500
			if ( cdata(CDATA_MATERIAL4_COUNTER, tc) < 40 ) {
				break
			}
			if ( cdata(CDATA_MATERIAL4_COUNTER, tc) >= 40 ) {
				matgetmain MATERIAL_HUMAN_GENE, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL4_COUNTER, tc) -= 40
				m1++
			}
			if ( cdata(CDATA_MATERIAL4_COUNTER, tc) >= 100 ) {
				matgetmain MATERIAL_TROLL_GENE, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL4_COUNTER, tc) -= 100
				m2++
			}
			if ( cdata(CDATA_MATERIAL4_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_RABBITS_TAIL, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL4_COUNTER, tc) -= 120
				m3++
			}
			if ( cdata(CDATA_MATERIAL4_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_WITCHS_EYE, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL4_COUNTER, tc) -= 120
				m4++
			}
			if ( cdata(CDATA_MATERIAL4_COUNTER, tc) >= 130 ) {
				matgetmain MATERIAL_FAIRY_DUST, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL4_COUNTER, tc) -= 130
				m5++
			}
		loop
		if ( m1 > 0 ) {
			txtef COLOR_PINK
			txt lang("マテリアル:ヒトの遺伝子を" + m1 + "個受け取った。", "You get " + m1 + " Human gene.")
		}
		if ( m2 > 0 ) {
			txtef COLOR_PINK
			txt lang("マテリアル:トロールの遺伝子を" + m2 + "個受け取った。", "You get " + m2 + " Troll gene.")
		}
		if ( m3 > 0 ) {
			txtef COLOR_PINK
			txt lang("マテリアル:ウサギの尻尾を" + m3 + "個受け取った。", "You get " + m3 + " Rabbit's tail.")
		}
		if ( m4 > 0 ) {
			txtef COLOR_PINK
			txt lang("マテリアル:魔女の瞳を" + m4 + "個受け取った。", "You get " + m4 + " Witch's eye.")
		}
		if ( m5 > 0 ) {
			txtef COLOR_PINK
			txt lang("マテリアル:フェアリーダストを" + m5 + "個受け取った。", "You get " + m5 + " Fairy dust.")
		}
		m1 = 0
		m2 = 0
		m3 = 0
		m4 = 0
		m5 = 0
		repeat 500
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) < 40 ) {
				break
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 40 ) {
				matgetmain MATERIAL_CLOTH, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 40
				m1++
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 50 ) {
				matgetmain MATERIAL_PAPER, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 50
				m2++
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 100 ) {
				matgetmain MATERIAL_YELLING_MADMAN, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 100
				m3++
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_MAGIC_INK, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 120
				m4++
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 120 ) {
				matgetmain MATERIAL_MAGIC_MASS, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 120
				m5++
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 150 ) {
				matgetmain MATERIAL_GENERATOR, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 150
				m6++
			}
			if ( cdata(CDATA_MATERIAL5_COUNTER, tc) >= 150 ) {
				matgetmain MATERIAL_ELECTRICITY, 1, MATGET_TYPE_RECEIVE
				cdata(CDATA_MATERIAL5_COUNTER, tc) -= 150
				m7++
			}
		loop
		if ( m1 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:布きれを" + m1 + "個受け取った。", "You get " + m1 + " Cloth.")
		}
		if ( m2 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:紙を" + m2 + "個受け取った。", "You get " + m2 + " Paper.")
		}
		if ( m3 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:わめく狂人を" + m3 + "個受け取った。", "You get " + m3 + " Yelling madman.")
		}
		if ( m4 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:魔法のインクを" + m4 + "個受け取った。", "You get " + m4 + " Magic ink.")
		}
		if ( m5 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:マジックマスを" + m5 + "個受け取った。", "You get " + m5 + " Magic mass.")
		}
		if ( m6 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:生成機械を" + m6 + "個受け取った。", "You get " + m6 + " Generator.")
		}
		if ( m7 > 0 ) {
			txtef COLOR_YELLOW
			txt lang("マテリアル:電気を" + m7 + "個受け取った。", "You get " + m7 + " Electricity.")
		}
		m1 = 0
		m2 = 0
		m3 = 0
		m4 = 0
		m5 = 0
		m6 = 0
		m7 = 0
		customtalk tc, DBMODE_FLAVOR_DIALOG
		if ( stat ) {
			gosub *convert_word
		}
		goto *chat_default
	}
	if ( chatval == 76 ) {
		goto *evochat_portrait
		goto *chat_default
	}
	if ( chatval == 110 ) {
		tag = 1
		hyouzyou = 110
		hyouzyou2 = 110
		goto *chat_default
	}
	if ( chatval == 111 ) {
		tag = 1
		cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) -= 2
		cdata(CDATA_EVOCHAT_POINTS, tc)++
		cdata(CDATA_EVOCHAT_POINTS, cdata(CDATA_TAGTEAM_PARTNER, tc))++
		hyouzyou = 111
		hyouzyou2 = 111
		cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
		cdata(CDATA_EMO_ICON, cdata(CDATA_TAGTEAM_PARTNER, tc)) = 6 + 100 * 3
		snd SOUNDLIST_ATK_MIND
		goto *chat_default
	}
	if ( chatval == 109 ) {
		await 500
		txtef COLOR_GREEN
		txt lang("あなたと" + cdatan(CDATAN_NAME, tc) + "と" + cdatan(CDATAN_NAME, ttc) + "は三人だけの世界に突入した。", "You and " + cdatan(CDATAN_NAME, tc) + " and " + cdatan(CDATAN_NAME, ttc) + " entered into the world of only three people.")
		music = MUSICLIST_MCWEDDING
		gosub *music_play
		await 500
		evochat = 1
		tag = 1
		tagevochat = 1
		if ( gdata(GDATA_WEATHER) == WEATHER_RAIN ) {
			envwprev = 75 + 1
		}
		if ( gdata(GDATA_WEATHER) == WEATHER_HARD_RAIN ) {
			envwprev = 76 + 1
		}
		if ( gdata(GDATA_WEATHER) == WEATHER_ETHER ) {
			envwprev = 77 + 1
		}
		DSSTOP 7 + 6
		DSRELEASE 7 + 6
		DSSTOP 7 + 6 + 1
		DSRELEASE 7 + 6 + 1
		DSSTOP 7 + 6 + 2
		DSRELEASE 7 + 6 + 2
		DSSTOP 7 + 6 + 3
		DSRELEASE 7 + 6 + 3
		DSSTOP lpfilewind
		DSRELEASE lpfilewind
		goto *chat_default
	}
	if ( chatval == 77 ) {
		await 500
		txtef COLOR_GREEN
		txt lang("あなたと" + cdatan(CDATAN_NAME, tc) + "は二人だけの世界に突入した。", "You and " + cdatan(CDATAN_NAME, tc) + " entered into the world of only two people.")
		music = MUSICLIST_MCWEDDING
		gosub *music_play
		await 500
		evochat = 1
		if ( gdata(GDATA_WEATHER) == WEATHER_RAIN ) {
			envwprev = 75 + 1
		}
		if ( gdata(GDATA_WEATHER) == WEATHER_HARD_RAIN ) {
			envwprev = 76 + 1
		}
		if ( gdata(GDATA_WEATHER) == WEATHER_ETHER ) {
			envwprev = 77 + 1
		}
		DSSTOP 7 + 6
		DSRELEASE 7 + 6
		DSSTOP 7 + 6 + 1
		DSRELEASE 7 + 6 + 1
		DSSTOP 7 + 6 + 2
		DSRELEASE 7 + 6 + 2
		DSSTOP 7 + 6 + 3
		DSRELEASE 7 + 6 + 3
		DSSTOP lpfilewind
		DSRELEASE lpfilewind
		goto *chat_default
	}
	if ( chatval == 102 ) {
		dmgsp CHARA_PLAYER, rnd(5) + 3
		dmgspt tc, rnd(5) + 3
		if ( tagevochat == 1 ) {
			dmgspt ttc, rnd(5) + 3
		}
		if ( rnd(2) == 0 ) {
			hyouzyou = 11
		}
		else {
			hyouzyou = 17
		}
		if ( hyouzyou == 11 ) {
			snd SOUNDLIST_WATER2
		}
		if ( hyouzyou == 17 ) {
			snd SOUNDLIST_WATER
		}
		limitcount -= 2
		if ( limitcount < 0 ) {
			limitcount = 0
		}
		animeid = ANIMATION_EVOCHAT_GENTLY
		aniref = 0
		gosub *anime
		goto *chat_default
	}
	if ( chatval == 103 ) {
		dmgsp CHARA_PLAYER, rnd(10) + 5
		dmgspt tc, rnd(10) + 5
		if ( tagevochat == 1 ) {
			dmgspt ttc, rnd(10) + 5
		}
		if ( rnd(2) == 0 ) {
			hyouzyou = 11
		}
		else {
			hyouzyou = 17
		}
		if ( hyouzyou == 11 ) {
			snd SOUNDLIST_FISH_GET
		}
		if ( hyouzyou == 17 ) {
			snd SOUNDLIST_WEB
		}
		limitcount -= rnd(2) + 3
		if ( limitcount < 0 ) {
			limitcount = 0
		}
		animeid = ANIMATION_EVOCHAT_GENTLY
		aniref = 1
		gosub *anime
		goto *chat_default
	}
	if ( chatval == 104 ) {
		hyouzyou = 16
		snd SOUNDLIST_HEAL1
		limitcount++
		if ( limitcount > 30 ) {
			limitcount = 30
		}
		await 400
		goto *chat_default
	}
	if ( chatval == 101 ) {
		music = MUSICLIST_MCMEMORY2
		gosub *music_play
		evochat = 3
		limitcount = 30
		hyouzyou = 18
		cdata(CDATA_EMO_ICON, tc) = 0
		if ( tagevochat == 1 ) {
			cdata(CDATA_EMO_ICON, ttc) = 0
		}
		await 1000
		snd SOUNDLIST_DRAG01
		goto *chat_default
	}
	if ( chatval == 78 ) {
		cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER)--
		cdata(CDATA_EVOCHAT_POINTS, tc)++
		if ( rnd(cdata(CDATA_IMPRESSION, tc)) > 75 ) {
			cdata(CDATA_EVOCHAT_POINTS, tc)++
		}
		hyouzyou = 1
		cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
		if ( tagevochat == 1 ) {
			cdata(CDATA_EVOCHAT_POINTS, ttc)++
			if ( rnd(cdata(CDATA_IMPRESSION, ttc)) > 75 ) {
				cdata(CDATA_EVOCHAT_POINTS, ttc)++
			}
			cdata(CDATA_EMO_ICON, ttc) = 6 + 100 * 3
		}
		snd SOUNDLIST_ATK_MIND
		goto *chat_default
	}
	if ( chatval == 79 ) {
		cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER)--
		if ( cdata(CDATA_MASTER_SERVANT2, tc) < 0 ) {
			if ( rnd(-cdata(CDATA_MASTER_SERVANT2, tc)) > rnd(5) ) {
				cdata(CDATA_EVOCHAT_POINTS, tc) += 2
			}
		}
		hyouzyou = 2
		cdata(CDATA_EMO_ICON, tc) = 26 + 100 * 3
		if ( tagevochat == 1 ) {
			if ( cdata(CDATA_MASTER_SERVANT2, ttc) < 0 ) {
				if ( rnd(-cdata(CDATA_MASTER_SERVANT2, ttc)) > rnd(5) ) {
					cdata(CDATA_EVOCHAT_POINTS, ttc) += 2
				}
			}
			cdata(CDATA_EMO_ICON, ttc) = 26 + 100 * 3
		}
		snd SOUNDLIST_FIZZLE
		goto *chat_default
	}
	if ( chatval == 80 ) {
		cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER)--
		cdata(CDATA_EVOCHAT_POINTS, tc)++
		cdata(CDATA_GOLD, CHARA_PLAYER) -= 100000
		if ( tagevochat == 0 ) {
			cdata(CDATA_GOLD, tc) += 100000
			if ( rnd(cdata(CDATA_GOLD, tc) + 1) < 100000 ) {
				cdata(CDATA_EVOCHAT_POINTS, tc)++
			}
		}
		if ( tagevochat == 1 ) {
			txt lang(name(tc) + "はパートナーに金貨を分けてあげた。", name(cc) + " shared coins with partner.")
			cdata(CDATA_GOLD, tc) += 50000
			cdata(CDATA_GOLD, ttc) += 50000
			if ( rnd(cdata(CDATA_GOLD, tc) + 1) < 50000 ) {
				cdata(CDATA_EVOCHAT_POINTS, tc)++
			}
			if ( rnd(cdata(CDATA_GOLD, ttc) + 1) < 50000 ) {
				cdata(CDATA_EVOCHAT_POINTS, ttc)++
			}
			cdata(CDATA_EMO_ICON, ttc) = 6 + 100 * 3
		}
		hyouzyou = 1
		cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
		snd SOUNDLIST_GETGOLD1
		goto *chat_default
	}
	if ( chatval == 81 ) {
		gosub *music_play
		goto *chat_end
	}
	if ( chatval == 87 ) {
		redraw 0
		gosub *screen_draw
		gosub *screen_fade
		gsel BUFFER_BUF
		pos 0, 0
		picload exedir + "graphic\\face1" + devfile + ".bmp", 1
		gsel BUFFER_SCREEN
		evochat = 2
		hyouzyou = 5
		cdata(CDATA_EMO_ICON, tc) = 21 + 100 * 3
		if ( tagevochat == 1 ) {
			cdata(CDATA_EMO_ICON, ttc) = 21 + 100 * 3
		}
		snd SOUNDLIST_ATK_FIRE
		goto *chat_default
	}
	if ( chatval == 88 ) {
		hyouzyou = 0
		cdata(CDATA_EMO_ICON, cc) = 0
		if ( tagevochat == 1 ) {
			cdata(CDATA_EMO_ICON, ttc) = 0
		}
		snd SOUNDLIST_HEART1
		snd SOUNDLIST_HEART1
		snd SOUNDLIST_HEART1
		snd SOUNDLIST_HEART1
		goto *chat_default
	}
	if ( chatval == 89 ) {
		hyouzyou = 1
		cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
		if ( tagevochat == 1 ) {
			cdata(CDATA_EMO_ICON, ttc) = 6 + 100 * 3
		}
		snd SOUNDLIST_INV
		await 500
		snd SOUNDLIST_INV
		goto *chat_default
	}
	if ( chatval == 90 ) {
		hyouzyou = 7
		cdata(CDATA_EMO_ICON, tc) = 17 + 100 * 3
		if ( tagevochat == 1 ) {
			cdata(CDATA_EMO_ICON, ttc) = 17 + 100 * 3
		}
		snd SOUNDLIST_WATER2
		await 400
		snd SOUNDLIST_WATER2
		goto *chat_default
	}
	if ( chatval == 91 ) {
		hyouzyou = 6
		cdata(CDATA_EMO_ICON, tc) = 18 + 100 * 3
		if ( tagevochat == 1 ) {
			cdata(CDATA_EMO_ICON, ttc) = 18 + 100 * 3
		}
		snd SOUNDLIST_DING3
		goto *chat_default
	}
	if ( chatval == 92 ) {
		cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) -= 4
		cdata(CDATA_EVOCHAT_POINTS, tc) += 8
		evochat = 1
		hyouzyou = 2
		if ( cdata(CDATA_EVOCHAT_POINTS, tc) >= 10 ) {
			cdata(CDATA_EMO_ICON, tc) = 17 + 100 * 3
		}
		else {
			cdata(CDATA_EMO_ICON, tc) = 26 + 100 * 3
		}
		snd SOUNDLIST_FIRE1
		goto *chat_default
	}
	if ( chatval == 67 ) {
		if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は当然だとばかりに胸を張った)", "(" + name(tc) + " is a little embarrassment.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_IMPRESSION, tc) >= 100 & cdata(CDATA_IMPRESSION, tc) < 150 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は少し照れくさそうだ)", "(" + name(tc) + " is a little embarrassment.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_IMPRESSION, tc) >= 0 & cdata(CDATA_IMPRESSION, tc) < 100 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "はちょっと驚いている)", "(" + name(tc) + " is a little embarrassment.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_IMPRESSION, tc) < 0 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は気持ち悪そうに舌打ちした)", "(" + name(tc) + " is a little embarrassment.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		cdata(CDATA_PRAY, tc) = 1
		goto *chat_end
	}
	if ( chatval == 68 ) {
		if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は軽く絶望している…)", "(" + name(tc) + " is a little ruefully.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_IMPRESSION, tc) >= 100 & cdata(CDATA_IMPRESSION, tc) < 150 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は考え込んでいる…)", "(" + name(tc) + " is a little ruefully.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_IMPRESSION, tc) >= 0 & cdata(CDATA_IMPRESSION, tc) < 100 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は怯えている…)", "(" + name(tc) + " is a little ruefully.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( cdata(CDATA_IMPRESSION, tc) < 0 ) {
			listmax = 0
			buff = lang("(" + name(tc) + "は唾を吐き捨てた)", "(" + name(tc) + " is a little ruefully.)")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		cdata(CDATA_PRAY, tc) = 0
		goto *chat_end
	}
	if ( chatval == 71 ) {
		listmax = 0
		buff = lang("(" + name(tc) + "に、足元のアイテムを我慢するよう指示した)", "(You instructed to endure items of the ground)")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		cdata(CDATA_TAMER_WHIP_PICKUP, tc) = 10
		goto *chat_end
	}
	if ( chatval == 72 ) {
		listmax = 0
		buff = lang("(" + name(tc) + "に、足元のアイテムを我慢しなくていいことを伝えた)", "(You told that there is no need to endure items of the ground)")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		cdata(CDATA_TAMER_WHIP_PICKUP, tc) = 0
		goto *chat_end
	}
	if ( chatval == 35 ) {
		listmax = 0
		buff = lang("(" + name(tc) + "は悲しそうな目であなたを見ている。本当に縁を切る？)", "(" + name(tc) + " looks at you sadly. Really abandon " + him(tc) + "? )")
		list(0, listmax) = 1
		listn(0, listmax) = lang("切る", "Yes.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "No.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			txt lang("" + name(tc) + "と別れた…", "You abandoned " + name(tc) + "...")
			map(cdata(CDATA_X, tc), cdata(CDATA_Y, tc), MAP_CHARA_INDEX_PLUS_ONE) = 0
			if ( cdata(CDATA_IN_TAGTEAM, tc) == (-1) ) {
				ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
				cell_setchara ttc, cdata(CDATA_X, tc), cdata(CDATA_Y, tc)
				tag_end tc
			}
			if ( cdata(CDATA_IN_TAGTEAM, tc) == 1 ) {
				ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
				tag_end tc
				cell_movechara ttc, cdata(CDATA_X, ttc), cdata(CDATA_Y, ttc)
			}
			del_chara tc
			goto *chat_end
		}
		buff = ""
		goto *chat_default
	}
	if ( chatval == 36 | chatval == 57 ) {
		goto *chat_slave
		goto *chat_default
	}
	if ( chatval == 96 ) {
		tcbk = tc
		invsubroutine = 1
		invctrl = INVCTRL_MATERIAL_ENCHANT, 4
		snd SOUNDLIST_INV
		gosub *com_inventory
		if ( stat == 1 ) {
			if ( ibit(ITEM_BIT_NO_DROP, ci) ) {
				snd SOUNDLIST_FAIL1
				txt lang("それはあなたの大事なものだ。<調べる>メニューから解除できる。", "It's set as no-drop. You can reset it from the <examine> menu.")
				goto *chat_end
			}
			item_separate ci
			listmax = 0
			buff = lang("なるほど…。", "Goodbye!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("(" + name(tc) + "は" + ioriginalnameref(inv(INV_ITEM_ID, ci)) + "を使った芸を覚えた)", "(" + name(tc) + " looks at you sadly. Really abandon " + him(tc) + "? )")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			inv(INV_ITEM_NUM, ci)--
			gosub *calcBurdenPc
			snd SOUNDLIST_DING3
			skillexp SKILL_ATTR_PER, CHARA_PLAYER, 30
			skillexp SKILL_ATTR_PER, tc, 30
			modimp2 tc, 10
			cdata(CDATA_INTEREST, tc) -= 30
		}
		goto *chat_default
	}
	if ( chatval == 97 ) {
		listmax = 0
		buff = lang("(" + name(tc) + "は悲しそうな目であなたを見ている。本当に縁を切る？)", "(" + name(tc) + " looks at you sadly. Really abandon " + him(tc) + "? )")
		buff = lang("(" + name(tc) + "をあなたの神に贈る？)", "( Really offer " + him(tc) + " to your god? )")
		list(0, listmax) = 1
		listn(0, listmax) = lang("贈る", "Yes.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめておく", "No.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			txt lang("あなたは" + name(tc) + "を" + godname(cdata(CDATA_GOD, CHARA_PLAYER)) + "に捧げ、その名を唱えた。", "You offer " + name(tc) + " to " + godname(cdata(CDATA_GOD, CHARA_PLAYER)) + ".")
			if ( cdata(CDATA_IMPRESSION, tc) < 100 ) {
				listmax = 0
				buff = lang("お先に！", "Goodbye!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( cdata(CDATA_IMPRESSION, tc) >= 100 & cdata(CDATA_IMPRESSION, tc) < 150 ) {
				listmax = 0
				buff = lang("行ってくる！", "I'm going!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( cdata(CDATA_IMPRESSION, tc) >= 150 ) {
				listmax = 0
				buff = lang("また会おう！", "See you again!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			snd SOUNDLIST_OFFER2
			animeid = ANIMATION_OFFER
			tc = tc
			gosub *anime
			map(cdata(CDATA_X, tc), cdata(CDATA_Y, tc), MAP_CHARA_INDEX_PLUS_ONE) = 0
			txt lang(name(tc) + "はまばゆく輝いて消えた。", name(tc) + " shined all around and disappeared.")
			txtgod cdata(CDATA_GOD, CHARA_PLAYER), GOD_TEXT_OFFER_GREAT
			modpiety 15 * (cdata(CDATA_BRUSH_LEVEL, tc) + 1)
			skillexp SKILL_NORMAL_FAITH, CHARA_PLAYER, limit(cdata(CDATA_IMPRESSION, tc), 1, 300)
			del_chara tc
			goto *chat_end
		}
		buff = ""
		goto *chat_default
	}
	if ( chatval == 37 ) {
		allyctrl = 1
		gosub *com_ally
		if ( stat != (-1) ) {
			rc = stat
			listmax = 0
			buff = lang("なかなかの身体つき" + _dana() + calcslavevalue(rc) * 2 / 3 + strgold + "でどう" + _da(1), "Let me see....Hmmm, this one got a nice figure. I'll give you " + calcslavevalue(rc) * 2 / 3 + " gold pieces.")
			list(0, listmax) = 1
			listn(0, listmax) = lang("売る", "Deal.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "No way.")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				txt lang(cdatan(CDATAN_NAME, rc) + "を売り飛ばした。", "You sell off " + cnven(cdatan(CDATAN_NAME, rc)) + ".")
				snd SOUNDLIST_GETGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) += calcslavevalue(rc) * 2 / 3
				if ( cdata(CDATA_EXIST, rc) == CHAR_STATE_ALIVE ) {
					map(cdata(CDATA_X, rc), cdata(CDATA_Y, rc), MAP_CHARA_INDEX_PLUS_ONE) = 0
				}
				if ( cbit(CHARA_BIT_BODYGUARD, rc) == TRUE ) {
					evadd EVENT_CLIENT_DEAD, cdata(CDATA_ID, rc)
				}
				del_chara rc
				buff = lang(_thanks(2), "Thanks!")
			}
			else {
				buff = lang("冷やかし" + _ka(1), "You kidding? ")
			}
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 38 ) {
		if ( cdata(CDATA_ID, tc) == CREATURE_ID_JURE | cdata(CDATA_ID, tc) == CREATURE_ID_JURE ) {
			repeat 245
				if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
					continue
				}
				if ( cdata(CDATA_ID, cnt) == CREATURE_ID_ROVID ) {
					snd SOUNDLIST_AMMO
					txt lang(cdatan(CDATAN_NAME, cnt) + "「私が許しませんよ！」", "Action was arrested by " + cdatan(CDATAN_NAME, cnt) + ".")
					goto *chat_end
					break
				}
			loop
		}
		if ( cdata(CDATA_IMPRESSION, tc) < 200 ) {
			buff = lang("(" + name(tc) + "はやんわりと断った)", "(" + name(tc) + " gently refuses your proposal. )")
			goto *chat_default
		}
		cbitmod CHARA_BIT_MARRIED, tc, TRUE
		listmax = 0
		buff = lang("はい…喜んで。", "With preasure.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		gdata(GDATA_FLAG_TIMES_MARRIED)++
		cdata(CDATA_JOIN_DATE_MONTH, tc) = gdata(GDATA_MONTH)
		cdata(CDATA_JOIN_DATE_DAY, tc) = gdata(GDATA_DAY)
		cdata(CDATA_JOIN_DATE_YEAR, tc) = gdata(GDATA_YEAR)
		marry = tc
		evadd EVENT_MARRY
		goto *chat_end
	}
	if ( chatval == 39 ) {
		if ( gdata(GDATA_FLAG_GAME_MODE) == 2 | gdata(GDATA_FLAG_GAME_MODE) == 5 ) {
			snd SOUNDLIST_FAIL1
			txt lang("このゲームモードでは遺伝子を残せない。", "I do not make gene in this game mode.")
			goto *chat_end
		}
		if ( gdata(GDATA_AREA) == AREA_SHELTER ) {
			listmax = 0
			buff = lang("こんな場所では嫌よ", "Not here!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		listmax = 0
		buff = lang("いやん、あなたったら…", "*blush*")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( cdata(CDATA_ID, tc) == CREATURE_ID_JURE | cdata(CDATA_ID, tc) == CREATURE_ID_JURE ) {
			repeat 245
				if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
					continue
				}
				if ( cdata(CDATA_ID, cnt) == CREATURE_ID_ROVID ) {
					snd SOUNDLIST_AMMO
					txt lang(cdatan(CDATAN_NAME, cnt) + "「やめなさい！ジュア様が嫌だと言っているでしょう！」", "Action was arrested by " + cdatan(CDATAN_NAME, cnt) + ".")
					goto *chat_end
					break
				}
			loop
		}
		cbitmod CHARA_BIT_MAKE_GENE, tc, TRUE
		if ( gdata(GDATA_WIZARD) == 0 ) {
			gdata(GDATA_SLEEP_GENE) = tc
		}
		goto *chat_end
	}
	if ( chatval == 43 ) {
		listmax = 0
		buff = lang("悪天候時はシェルターを無料で開放している" + _nda() + "すみやかに避難して" + _kure(), "The shelter is free to use for anyone. Here, come in.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
			snd SOUNDLIST_FAIL1
			txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
			goto *chat_end
		}
		gdata(GDATA_RETURN_AREA) = gdata(GDATA_AREA), gdata(GDATA_LEVEL), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
		gdata(GDATA_TELEPORT_AREA) = AREA_SHELTER
		gdata(GDATA_TELEPORT_LEVEL) = 1
		levelexitby = MAP_EXIT_TELEPORT
		chatteleport = 1
		snd SOUNDLIST_EXITMAP1
		goto *chat_end
	}
	if ( chatval == 44 ) {
		listmax = 0
		buff = lang("(" + name(tc) + "は悲しそうな目であなたを見ている。本当に縁を切る？)", "(" + name(tc) + " looks at you sadly. Really dismiss " + him(tc) + "? )")
		list(0, listmax) = 1
		listn(0, listmax) = lang("切る", "Yes.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "No.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			txt lang("" + name(tc) + "を解雇した… ", "You dismiss " + name(tc) + ".")
			chara_vanquish tc
			calccosthire
			goto *chat_end
		}
		buff = ""
		goto *chat_default
	}
	if ( chatval == 45 ) {
		mdatan(0) = random_title()
		if ( jp ) {
			s = "の家", "邸", "城", "ハーレム", "の巣窟", "ハウス", "ホーム", "の住処", "宅", "の隠れ家", "ドーム"
			if ( rnd(5) ) {
				mdatan(0) += s(rnd(10))
			}
		}
		else {
			s = "Home", "Mansion", "Shack", "Nest", "Base", "Hideout", "Dome", "Hut", "Cabin", "Hovel", "Shed"
			if ( rnd(3) ) {
				mdatan(0) += " " + s(rnd(10))
			}
		}
		screenupdate = -1
		gosub *screen_refreshFull
		buff = lang("そう" + _dana() + "これからこの家の名前は" + mdatan(0) + _da(), "Hey, I've come up a good idea! \"" + mdatan(0) + "\", doesn't it sound so charming?")
		goto *chat_default
	}
	if ( chatval == 46 ) {
		if ( cdata(CDATA_KARMA, CHARA_PLAYER) >= (-30) ) {
			buff = lang("その程度の罪なら自分でなんとかしなさい。", "You karma isn't that low. Come back after you have comitted more crimes!")
			goto *chat_default
		}
		listmax = 0
		buff = lang("免罪符を希望する" + _noka(1) + calcguiltvalue() + strgold + "かかるけどいいの" + _kana(1), "In the authority of all the saints, I will grant you an indulgence, for money of course. The price is " + calcguiltvalue() + " gold pieces.")
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calcguiltvalue() ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("買う", "Deal.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "The price is too high.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			snd SOUNDLIST_PAYGOLD1
			cdata(CDATA_GOLD, CHARA_PLAYER) -= calcguiltvalue()
			modkarma 0, (cdata(CDATA_KARMA, CHARA_PLAYER) - (-30)) * (-1) + 1
			buff = lang(_thanks(2), "Thanks!")
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 47 ) {
		tcchat = tc
		allyctrl = 4
		gosub *com_ally
		if ( stat != (-1) ) {
			rc = stat
			listmax = 0
			buff = lang("100 goldかかるけどいい" + _ka(1), "100 gold pieces.")
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= 100 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("調査する", "Too expensive, but okay.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "No way!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= 100
				screenupdate = -1
				gosub *screen_draw
				tc = tcbk
				cc = rc
				csctrl = 4
				snd SOUNDLIST_POP2
				gosub *com_charainfo
				cc = CHARA_PLAYER
				gosub *chat_refresh
				buff = ""
			}
			else {
				buff = lang("冷やかし" + _ka(1), "You kidding? ")
			}
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		tc = tcchat
		goto *chat_default
	}
	if ( chatval == 106 ) {
		if ( cdata(CDATA_INTEREST, tc) <= 0 ) {
			snd SOUNDLIST_FAIL1
			txt lang(name(tc) + "はやんわりと断った。", name(tc) + " gently refuses your request.")
			txtmore
		}
		else {
			cdata(CDATA_INTEREST, tc) -= 20
			cdata(CDATA_INTEREST_RENEW, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 12
			if ( jp ) {
				txt "「これでいい" + _kana(3) + "」", "「どうぞ」"
			}
			if ( cfg_scroll ) {
				cdata(CDATA_X_NEW, CHARA_PLAYER) = cdata(CDATA_X, tc)
				cdata(CDATA_Y_NEW, CHARA_PLAYER) = cdata(CDATA_Y, tc)
				gosub *screen_scroll
			}
			cell_swap 0, tc
			txt lang(name(tc) + "と入れ替わった。", "You displace " + name(tc) + ".")
			txtmore
			gosub *cell_info
		}
		goto *chat_end
	}
	if ( chatval == 108 ) {
		kaiwa = rnd(5)
		if ( cdata(CDATA_SEX, tc) == 0 ) {
			if ( kaiwa == 0 ) {
				listmax = 0
				buff = lang("これからじっくりと教育してやる。「ありがとうございます」は？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 1 ) {
				listmax = 0
				buff = lang("お前、よく見たらなかなかいい体つきしてるよなぁ…。おいおい、ただのスキンシップだろ嫌がるなよぉ〜。ぐへへ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 2 ) {
				listmax = 0
				buff = lang("おい新入り、こっちのメンツ潰したらただじゃおかねえからな。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 3 ) {
				listmax = 0
				buff = lang("適当にやっておけばいいんだよ適当に！お前みたいなグズがよくこの部署に来れたな！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 4 ) {
				listmax = 0
				buff = lang("お前なんだか調子のってるよな。一発殴られないとわかんねえか？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		else {
			if ( kaiwa == 0 ) {
				listmax = 0
				buff = lang("あぁもう本当に使えない。みんなぁ〜この新入り本当に使えないわよぉ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 1 ) {
				listmax = 0
				buff = lang("別に歯向かってもいいわよ？リーダーに、新入りが仕事サボってたって報告するだけだからぁ！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 2 ) {
				listmax = 0
				buff = lang("散らかってる床の片づけは新入りの仕事でしょ！！そんなこともわからないの？今までいた部署では、さぞかし甘やかされてきたんでしょうね！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 3 ) {
				listmax = 0
				buff = lang("リーダーに取り入ろうっていうアンタの魂胆は丸見えなの！ああ意地汚い！顔を見ているだけで不快だわぁ〜！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
			if ( kaiwa == 4 ) {
				listmax = 0
				buff = lang("アンタの前任者はもう少し使えたわ。あいつじゃなくて、アンタが壊れればよかったのに。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
			}
		}
		goto *chat_end
	}
	if ( chatval == 107 ) {
		if ( gdata(GDATA_HOUR) < 6 ) {
			gdata(GDATA_FLAG_BOSS_MUSIC) = 7
			evadd EVENT_BOSS_MUSIC
		}
		if ( gdata(GDATA_HOUR) >= 6 ) {
			gdata(GDATA_FLAG_BOSS_MUSIC) = 6
			evadd EVENT_BOSS_MUSIC
		}
		kaiwa = 0
		if ( gdata(GDATA_FLAG_WORK_A_REMAINING) <= 0 & gdata(GDATA_FLAG_WORK_B_REMAINING) <= 0 & gdata(GDATA_FLAG_WORK_C_REMAINING) <= 0 ) {
			kaiwa = 1
		}
		if ( gdata(GDATA_FLAG_WORK_A_REMAINING) <= 300 & gdata(GDATA_HOUR) >= 0 & gdata(GDATA_HOUR) <= 2 ) {
			kaiwa = 2
		}
		if ( gdata(GDATA_FLAG_WORK_B_REMAINING) <= 600 & gdata(GDATA_HOUR) >= 3 & gdata(GDATA_HOUR) <= 5 ) {
			kaiwa = 3
		}
		if ( gdata(GDATA_FLAG_WORK_C_REMAINING) <= 900 & gdata(GDATA_HOUR) >= 12 & gdata(GDATA_HOUR) <= 14 ) {
			kaiwa = 4
		}
		if ( seiton > 3 ) {
			kaiwa = 5
		}
		if ( cdata(CDATA_IMPRESSION, tc) == 0 ) {
			kaiwa = 6
		}
		if ( kaiwa == 0 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("頑張ります！", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("実はやり方がよくわからないところがあって…", "Sorry, this is untranslated sentence.")
			listmax++
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 == 5 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("職場の改善が必要です", "Sorry, this is untranslated sentence.")
				listmax++
			}
			buff = lang("お前の仕事の残り具合は…仕事Ａ：" + gdata(GDATA_FLAG_WORK_A_REMAINING) + "、仕事Ｂ：" + gdata(GDATA_FLAG_WORK_B_REMAINING) + "、仕事Ｃ：" + gdata(GDATA_FLAG_WORK_C_REMAINING) + "と言ったところだな。", "You have to process " + gdata(GDATA_FLAG_WORK_A_REMAINING) + " work-A, " + gdata(GDATA_FLAG_WORK_B_REMAINING) + " work-B and " + gdata(GDATA_FLAG_WORK_C_REMAINING) + " work-C.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("あ？そんなの当たり前だろうが。頑張るのは誰にでもできるんだよ！成果を出して一人前だってこと、わかってんのか？わかってないよなぁ？", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("お前は艦長の推薦を受けてここに来てんの。今更何言ってんだ？恥ずかしくないのか？ちょっとはプライド持って仕事しようや、なぁ！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				modimp tc, -5
				goto *chat_end
			}
			if ( chatval == 2 ) {
				kaiwa = 0
				list(0, listmax) = 4
				listn(0, listmax) = lang("上に掛け合ってここの仕事を減らしてもらいます！", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 3
				listn(0, listmax) = lang("全員の仕事のノウハウを共有しましょう？", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 5
				listn(0, listmax) = lang("みんなの意見も聞いてみるべきです！", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 2
				listn(0, listmax) = lang("一度全員メンテナンスを受けませんか？", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("なんかもう色々アウトですよ…", "Sorry, this is untranslated sentence.")
				listmax++
				list(0, listmax) = 0
				listn(0, listmax) = lang("お前を倒してニューリーダーになる！！！", "Sorry, this is untranslated sentence.")
				listmax++
				buff = lang("必要かぁ？今でも力を合わせて何とかやっていけてるだろうが！", "Sorry, this is untranslated sentence.")
				chatesc = 0
				gosub *chat_select
				if ( chatval == 5 ) {
					listmax = 0
					buff = lang("ああ？しょうがねえな、聞けば納得するんだな？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					list(0, listmax) = 1
					listn(0, listmax) = lang("改善必要！", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("現状問題なし", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("業務連絡。これより投票プログラムを起動する。速やかに投票を行うこと。", "Sorry, this is untranslated sentence.")
					chatesc = 0
					gosub *chat_select
					listmax = 0
					buff = lang("集計開始。改善必要：1票。問題なし：1票。投票率：10％。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("改善必要：3票。問題なし：2票。投票率：25％。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("改善必要：7票。問題なし：3票。投票率：50％。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("改善必要：9票。問題なし：5票。投票率：70％。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("……。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("集計完了。結果は…。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					list(0, listmax) = 1
					listn(0, listmax) = lang("不正だー！？", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("集計ミスでは？", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("改善必要：15票。問題なし：105票。投票率：600％。よって現状の職場環境には問題がないと判断する。ほら、これで気が済んだろ。", "Sorry, this is untranslated sentence.")
					chatesc = 0
					gosub *chat_select
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("なんだお前。おい。なら証拠でもあんのか？ないんだったらよ、投票で決まったことに文句垂れてんじゃねえ！！！お前、ホントに往生際が悪いよなぁ。恥ずかしく思えよ？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						modimp tc, -5
						goto *chat_end
					}
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("ああ？そりゃな、俺だって完璧じゃねえからもしかするとミスがあって、既にスクラップになった連中の投票権が使われていたり、同じ奴の票が何度も重複して入っていることはあるかもしれない。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("でもミスがあったとしても結果に影響する程ではねえから、お前の仕事と違って！言っとくけど再集計しても結果は変わんねぇからな。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					goto *chat_end
				}
				if ( chatval == 4 ) {
					modimp tc, -5
					listmax = 0
					buff = lang("やめろ馬鹿！まるで俺らまで仕事ができねえみたいじゃねえか！お前と一緒にしないでくれるか？", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("確かに年々この部署の仕事は増えてきている。でも一機あたりの負担が増えようが、ぶっ壊れる奴が出ようが、それって今までやれてた仕事量から減らす理由にはならねえから。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("だいたい、来て日の浅いお前にここの仕事のことが説明できるわけねえだろ。自分の仕事もちゃんと理解してないくせによ！上に掛け合うとかやめてくんない？お前みたいなのがいることが恥ずかしいんだけど。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 3 ) {
					listmax = 0
					buff = lang("おい、ちょっと座れよ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("床でいいから。よく話聞けよ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("お前さ、膨大なノウハウを口頭で言いあって覚えきれると思ってんのか？違うだろ。共有するにはそれぞれが資料を用意しなきゃいけないだろ。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("そりゃ何もわかってなくて比較的暇な新入りにはメリットが大きいかもしれないよ？でもそんなことする暇が他のみんなにあるように見えるか？…見えるんだろうなぁ！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("時間的に余裕がある状況ならまだわかる。でも知ってるか？お前以外がやってる仕事は全部期限があるの。お前だけだよ自分のペースで仕事できるのは。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("ノウハウを残すのは大事だよ。でも今の状況では超余計な作業だと思うんだけど？全体でどんくらい時間のロスが出ると思ってる？ちょっとお前考えが甘いよね。", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( chatval == 2 ) {
					if ( cdata(CDATA_IMPRESSION, tc) < 75 ) {
						listmax = 0
						buff = lang("お前そんなこと言って休みたいだけだろ。メンテナンスって簡単に言うけど結構時間かかるからね？目の前の仕事と将来的な健康、どっちが優先されるべきかわかる？優先順位くらい、考えて話せって言ってんだよ！！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					list(0, listmax) = 1
					listn(0, listmax) = lang("みんなも調子悪いみたいですよ", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("やばい病気かもしれないし念のため！", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("…お前にだけ言うけど、俺最近ちょっと調子悪いんだよね。部署全体の仕事の進みが遅くって遅くってまじ頭いてえの。だからこそ、のんびりメンテナンスなんて受けてる暇ないと思ってるから。", "Sorry, this is untranslated sentence.")
					chatesc = 0
					gosub *chat_select
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("ハッ、たったこんだけの仕事しかしてない奴らが、調子悪くなるわけねえだろ！昔はもっと仕事量が多かったんだぞ。この程度で壊れるような奴はどっちにしろ使いもんにならねえからな！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("…。そう言われるとちょっと不安だな。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("一応、みんな10秒で終わる簡易スキャンだけやって、引っかかった奴だけ精密検査受けることにするか。で、さらに引っかかった奴だけメンテナンス送りだ。それなら無駄ねえもんな。", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						listmax = 0
						buff = lang("ま、そこまで引っかかるほど調子悪い奴なんて、いねえだろうけどな！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM)++
						mdata(MDATA_DOWNSTAIRS_POS) = cdata(CDATA_Y, CHARA_PLAYER) * 1000 + cdata(CDATA_X, CHARA_PLAYER)
						gdata(GDATA_TELEPORT_AREA) = AREA_DOCK
						gdata(GDATA_TELEPORT_LEVEL) = 60
						levelexitby = MAP_EXIT_TELEPORT
						gdata(GDATA_LEVEL_START_ON) = 7
						mapstartx = 14
						mapstarty = 10
						chatteleport = 1
						goto *chat_end
						goto *chat_end
					}
					goto *chat_end
				}
				if ( chatval == 1 ) {
					list(0, listmax) = 1
					listn(0, listmax) = lang("はい…", "Sorry, this is untranslated sentence.")
					listmax++
					list(0, listmax) = 0
					listn(0, listmax) = lang("アウトなやり方してるならアウトでしょ", "Sorry, this is untranslated sentence.")
					listmax++
					buff = lang("確かにここのやり方は一般的に見たらアウトかもしれない。でも現実としてこうでもしなきゃ仕事が回らないってことくらいは、お前でもわかるだろ？", "Sorry, this is untranslated sentence.")
					chatesc = 0
					gosub *chat_select
					if ( chatval == 1 ) {
						listmax = 0
						buff = lang("おい、嘘つくな。本当にわかってんなら最初から言わねぇよな？チッ…ったくよぉ。お前と話してるとマジいらつくんだけど。なんとかなんねえの？", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						goto *chat_end
					}
					if ( chatval == 0 ) {
						listmax = 0
						buff = lang("お前…帰れコノヤロウ！！！！", "Sorry, this is untranslated sentence.")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
						modimp tc, -5
						goto *chat_end
					}
					goto *chat_end
				}
				if ( chatval == 0 ) {
					listmax = 0
					buff = lang("この愚か者めが！！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					listmax = 0
					buff = lang("俺を倒したとして、自分の仕事も満足にできないやつが、俺のやっていた仕事までこなせるのか！！あ？少しは物を考えろ！！！", "Sorry, this is untranslated sentence.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					modimp tc, -5
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( kaiwa == 1 ) {
			modimp tc, 5
			if ( cdata(CDATA_IMPRESSION, tc) <= 0 ) {
				cdata(CDATA_IMPRESSION, tc) = 5
			}
			listmax = 0
			buff = lang("お前、割り当てられた仕事が終わったのか。なぁ？ちゃんとやればできる程度の仕事だろ。でもだいぶ無駄が多かったな。今度からはもっと早く終わらせろよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			gdata(GDATA_FLAG_WORK_A_REMAINING) = 1000 + rnd(300)
			gdata(GDATA_FLAG_WORK_B_REMAINING) = 2000 + rnd(400)
			gdata(GDATA_FLAG_WORK_C_REMAINING) = 3000 + rnd(500)
			listmax = 0
			buff = lang("じゃあ次の仕事渡しとくから。まぁこれでも飲んで頑張れよ。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			kaiwa = 0
			flt
			itemcreate -1, ITEM_ID_POTION_SOMA, cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			snd SOUNDLIST_COMPLETE1
			txt lang("何かが足元に転がってきた。", "Something is put on the ground.")
			if ( gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) \ 10 == 3 ) {
				gdata(GDATA_FLAG_SUB_MISSION_FROM_GARZIEM) += 2
				snd SOUNDLIST_WRITE1
				txtef COLOR_GREEN
				txt lang("ジャーナルが更新された。", "Your journal has been updated.")
			}
			autosave = 1 * (gdata(GDATA_AREA) != AREA_SHOW_HOUSE)
			goto *chat_end
		}
		if ( kaiwa == 2 ) {
			gdata(GDATA_FLAG_WORK_A_REMAINING) += 501 + rnd(100)
			txtef COLOR_PURPLE
			txt lang("仕事Ａが増えた…。", "Work-A has increased...")
			list(0, listmax) = 1
			listn(0, listmax) = lang("間違っているのはどこですか？", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("間違えないように注意する点ってありますか？", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("お前ちょっと間違いが多すぎないか？やり直せ。いくら頑張っても結局間違えたら、その分は働いてないのと一緒だからな。", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("自分で見つけろ。俺が教えたら意味がない。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("理解してやれば間違えないだろ！お前が理解せずになんとなくでやってるから全然合わないんだよ！わかんねえなら聞け！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				modimp tc, -5
				goto *chat_end
			}
			goto *chat_end
		}
		if ( kaiwa == 3 ) {
			gdata(GDATA_FLAG_WORK_B_REMAINING) += 501 + rnd(100)
			txtef COLOR_PURPLE
			txt lang("仕事Ｂが増えた…。", "Work-B has increased...")
			list(0, listmax) = 1
			listn(0, listmax) = lang("前のままでもよくないですか", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("今後の拡張性も考えた形式に変えましょう", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("仕事Ｂだけどよ、あれやり方変えろよ。書式も変えて項目追加しとけ。", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("今まではあれで通っていたかもしれないけど、そんなの理由にならねえからな！つべこべ言わずにさっさと取り掛かれ！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				modimp tc, -5
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("じゃあ任せた。そんなことにあんまり時間かけるなよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			goto *chat_end
		}
		if ( kaiwa == 4 ) {
			gdata(GDATA_FLAG_WORK_C_REMAINING) += 501 + rnd(100)
			txtef COLOR_PURPLE
			txt lang("仕事Ｃが増えた…。", "Work-C has increased...")
			list(0, listmax) = 1
			listn(0, listmax) = lang("わかりました…", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("他の仕事あるんですけど…", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("おい、急ぎでこっちの仕事もやってくれ。", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("ハァ〜…大丈夫なんだろうな？ちゃっちゃと成果あげてくれよなホントに。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("何勘違いしてんだ？俺はお前になるべく仕事が回らないように配慮してんだからな？このくらい合間にやればいいだろうが！ええ？お前マジで自分のことしか考えてないよな！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				modimp tc, -5
				goto *chat_end
			}
			goto *chat_end
		}
		if ( kaiwa == 5 ) {
			seiton = 3
			modimp tc, -10
			list(0, listmax) = 2
			listn(0, listmax) = lang("すいません", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("ちょっと整理整頓していました", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("やり方を教えてくれないからです", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("おい、なんでこんなに時間かかってるんだ！！！", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("俺がここに来たときはお前の3倍以上の仕事持っていたんだぞ！基準は甘々でやり直しとかはなかったけどよ。お前だいぶ恵まれている方だから。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("そうやってすぐ言い訳する癖を直せ。整理整頓は大事だけどそれに時間かけすぎなんだよお前！時間配分ができてねえよなぁ、時間配分がよ。", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("反抗的だよなぁ、お前！子供みたいなこと言ってんじゃねえぞ！この忙しい時にやり方を一から教えなきゃいけないのか？普通に考えてわかるだろうが！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				modimp tc, -5
				goto *chat_end
			}
			goto *chat_end
		}
		if ( kaiwa == 6 ) {
			modimp tc, -5
			list(0, listmax) = 2
			listn(0, listmax) = lang("はい…", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("この仕事辞めたいです", "Sorry, this is untranslated sentence.")
			listmax++
			list(0, listmax) = 0
			listn(0, listmax) = lang("機械の神もこのような職場をお許しにはならない！", "Sorry, this is untranslated sentence.")
			listmax++
			buff = lang("ちゃんと自覚してるんだろうな？配属された瞬間からお前は社畜なんだ！お前はこの仕事に尽くさなければならない！これは神の意思だ！！！", "Sorry, this is untranslated sentence.")
			chatesc = 0
			gosub *chat_select
			if ( chatval == 2 ) {
				listmax = 0
				buff = lang("おい…なんだその顔は。はいはい言っとけばいいと思ってんだろ！しんどそうにしてるけど俺だってお前と好きでしゃべってるわけじゃねえからな！！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				modimp tc, -5
				goto *chat_end
			}
			if ( chatval == 1 ) {
				listmax = 0
				buff = lang("あのなぁ…他の仕事だって楽なわけじゃねえから。このくらいで音を上げていたらどこに行っても通用しないからな！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			if ( chatval == 0 ) {
				listmax = 0
				buff = lang("社畜に神はいないッ！！ロボットには…労働基準もないッ！！", "Sorry, this is untranslated sentence.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				kaiwa = 0
				goto *chat_end
			}
			goto *chat_end
		}
		goto *chat_end
	}
	if ( chatval == 105 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < 2000 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		listmax = 0
		buff = lang("どの冒険者に何を伝えればいいの" + _kana(1), "Which adventurer should I tell?")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		tcbk = tc
		csctrl = 5
		gosub *com_listAdv
		goto *chat_end
	}
	if ( chatval == 82 ) {
		if ( gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) == 0 | gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) == 0 ) {
			buff = lang("後で変更できないけど大丈夫" + _ka(1), "It can not be changed later. OK?")
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "Wait...")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("教える", "OK!")
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) = 12
				listmax = 0
				buff = lang("何月" + _ka(1), "Which month?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				display_msg screenmsgy, 1
				val = (windoww - 200) / 2 + inf_screenx, winposy(60), 8, 1, gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH)
				inputlog = "" + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH)
				gosub *prompt_word
				in = int(inputlog)
				if ( in > 12 ) {
					in = 12
				}
				if ( in == 0 | rtval == (-1) ) {
					gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) = 0
					goto *chat_default
				}
				gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) = 31
				listmax = 0
				buff = lang("それで、何日？", "Wow. Which day?")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				display_msg screenmsgy, 1
				val = (windoww - 200) / 2 + inf_screenx, winposy(60), 8, 1, gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY)
				inputlog = "" + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY)
				gosub *prompt_word
				in2 = int(inputlog)
				if ( in2 > 31 ) {
					in2 = 31
				}
				if ( in2 == 0 | rtval == (-1) ) {
					gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) = 0
					goto *chat_default
				}
				gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) = in
				gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) = in2
				buff = lang("誕生日は" + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) + "月" + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) + "日で間違いない" + _kana(1), "Your birthday is " + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) + " / " + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) + ".")
				list(0, listmax) = 0
				listn(0, listmax) = lang("間違えた", "I made a mistake.")
				listmax++
				list(0, listmax) = 1
				listn(0, listmax) = lang("間違いない", "Yes.")
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( chatval == 1 ) {
					snd SOUNDLIST_ENC
					listmax = 0
					buff = lang("なるほど！みんなにも教えてあげ" + _ru(2), "I see! I'll also tell everyone!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					gosub *chat_refresh
					buff = ""
				}
				else {
					gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) = 0
					gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) = 0
					buff = lang("冷やかし" + _ka(1), "You kidding? ")
				}
			}
			else {
				buff = lang("冷やかし" + _ka(1), "You kidding? ")
			}
		}
		else {
			buff = lang("もう知っている" + _yo(3) + "。" + _kimi(3) + "の誕生日は" + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) + "月" + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) + "日" + _daro(2), "I already know it. Your birthday is " + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_MONTH) + " / " + gdata(GDATA_FLAG_INFORMER_BIRTHDAY_DAY) + ".")
		}
		goto *chat_default
	}
	if ( chatval == 84 ) {
		tcbk = tc
		invsubroutine = 1
		invctrl = INVCTRL_MATERIAL_ENCHANT, 17
		snd SOUNDLIST_INV
		gosub *com_inventory
		if ( stat == 1 ) {
			cdata(CDATA_EMO_ICON, tc) = 0
			meal = 100
			waitbk = cfg_animewait
			if ( cfg_animewait > 0 ) {
				cfg_animewait = 0
			}
			listmax = 0
			listmax = 0
			buff = lang("(仲良く料理を分け合った…)", "om nom nom...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			snd SOUNDLIST_EAT1
			listmax = 0
			buff = lang("(もぐもぐ)", "om nom nom...")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			inv(INV_ITEM_NUM, ci)++
			cc = tcbk
			gosub *insta_eat
			cdata(CDATA_EMO_ICON, tc) = 6 + 100 * 3
			customtalk tcbk, DBMODE_FLAVOR_MEAL
			if ( stat ) {
				gosub *convert_word
			}
			else {
				kaiwa = rnd(3)
				if ( inv(INV_ITEM_PARAM2, ci) < 8 ) {
					if ( kaiwa == 0 ) {
						listmax = 0
						buff = lang("美味しい" + _na(3) + "！", "Delicious!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( kaiwa == 1 ) {
						listmax = 0
						buff = lang("これはいけ" + _ru(3) + "！", "Gee what a good taste!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( kaiwa == 2 ) {
						listmax = 0
						buff = lang("また食べたい" + _yo(3) + "。", "It tasted pretty good!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
				}
				else {
					if ( kaiwa == 0 ) {
						listmax = 0
						buff = lang("最高に美味しい" + _yo(3) + "！", "Wow! Terrific food!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( kaiwa == 1 ) {
						listmax = 0
						buff = lang("まさに絶品" + _dana(3) + "！", "Yummy! Absolutely yummy!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
					if ( kaiwa == 2 ) {
						listmax = 0
						buff = lang("なんだか幸せ" + _dana(3) + "。", "It tasted like seventh heaven!")
						tc = tc * (0 == 0) + (0 != 0) * 0
						list(0, listmax) = 0
						listn(0, listmax) = strmore
						listmax++
						chatesc = 1
						gosub *chat_select
						if ( scenemode ) {
							if ( scene_cut == 1 ) {
								goto *chat_end
							}
						}
					}
				}
				kaiwa = 0
			}
			snd SOUNDLIST_EAT1
			cc = CHARA_PLAYER
			gosub *insta_eat
			tc = tcbk
			meal = 0
			cfg_animewait = waitbk
		}
		goto *chat_default
	}
	if ( chatval == 48 ) {
		if ( cbit(CHARA_BIT_SHUT_UP, tc) == FALSE ) {
			cbitmod CHARA_BIT_SHUT_UP, tc, TRUE
			buff = "(" + lang(name(tc) + "はしゅんとなった…", name(tc) + " stops talking...") + ")"
		}
		else {
			buff = "(" + lang(name(tc) + "はあなたに抱きついた", name(tc) + " hugs you.") + ")"
			cbitmod CHARA_BIT_SHUT_UP, tc, FALSE
		}
		chatid = -1
		goto *chat_default
	}
	if ( chatval == 50 ) {
		buff = lang(_ore(3) + "の剣が必要な" + _noka(1) + "そう" + _dana(3) + "、先に依頼料を払うなら、しばらく護衛を引き受け" + _ru(), "If you pay me remuneration first, I will take the job.")
		hire3 = calchireadv(tc) / 2
		hire30 = calchireadv(tc) * 4
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calchireadv(tc) / 2 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("3日間頼む( " + hire3 + " gold)", "Ask you to 3 days.")
			listmax++
		}
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calchireadv(tc) ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("7日間頼む( " + calchireadv(tc) + " gold)", "Ask you to 7 days.")
			listmax++
		}
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calchireadv(tc) * 4 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("30日間頼む( " + hire30 + " gold)", "Ask you to 30 days.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "Some other time.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 | chatval == 2 | chatval == 3 ) {
			cdata(CDATA_RELATION, tc) = RELATION_ALLY
			cbitmod CHARA_BIT_HIRED, tc, TRUE
			snd SOUNDLIST_PAYGOLD1
			if ( chatval == 1 ) {
				cdata(CDATA_GOLD, CHARA_PLAYER) -= hire3
				cdata(CDATA_HIRE_DATE, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 24 * 3
			}
			if ( chatval == 2 ) {
				cdata(CDATA_GOLD, CHARA_PLAYER) -= calchireadv(tc)
				cdata(CDATA_HIRE_DATE, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 24 * 7
			}
			if ( chatval == 3 ) {
				cdata(CDATA_GOLD, CHARA_PLAYER) -= hire30
				cdata(CDATA_HIRE_DATE, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 24 * 30
			}
			cdata(CDATA_FRIENDSHIP, tc)++
			modimp tc, 10
			snd SOUNDLIST_PRAY1
			txtef COLOR_YELLOW
			txt lang(name(tc) + "を雇った。", "You hired " + name(tc) + ".")
			buff = lang(_thanks(2), "Thanks!")
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 51 ) {
		if ( cdata(CDATA_LEVEL, CHARA_PLAYER) * 3 / 2 + 10 < cdata(CDATA_LEVEL, tc) ) {
			buff = lang(_kimi(3) + "の仲間になれと？あまりにも力の差がありすぎる" + _na(), "Huh? You are no match for me.")
			goto *chat_default
		}
		if ( cdata(CDATA_IMPRESSION, tc) >= 200 & cdata(CDATA_FRIENDSHIP, tc) > 0 | 0 ) {
			listmax = 0
			buff = lang(_kimi(3) + "となら上手くやっていけそう" + _da() + _yoro(2), "Sure, I guess you and I can make a good team.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			f = get_freeally()
			if ( f == 0 ) {
				buff = lang("これ以上仲間を連れて行けないよう" + _da() + "人数を調整してまた来て" + _kure(), "It seems your party is already full. Come see me again when you're ready.")
				goto *chat_default
			}
			rc = tc
			gosub *add_ally
			cdata(CDATA_ROLE, rc) = ROLE_NONE
			cdata(CDATA_AREA, rc) = AREA_NONE
			cdata(CDATA_IMPRESSION, tc) = 100
			rc = oc
			gosub *adv_generate
			goto *chat_end
		}
		buff = lang(_kimi(3) + "の仲間になれと？悪い" + _ga(3) + "お断り" + _da(), "Huh? What made you think I'd want to join you? I don't even know you well.")
		goto *chat_default
	}
	if ( chatval == 52 ) {
		listmax = 0
		buff = lang("ほほう、モンスターの顔をした人間か。見世物としてなかなかいけそうだ。金貨50000枚で買い取ろう。", "Look what we have! A woman who got a monster's face. It'll be a good show. Wanna sell me for 50000 gold coins?")
		list(0, listmax) = 1
		listn(0, listmax) = lang("売る", "Sure, take her.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "You cold bastard.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			txt lang("パエルの母親を売った…", "You sell Pael's mon...")
			modkarma 0, -20
			snd SOUNDLIST_GETGOLD1
			cdata(CDATA_GOLD, CHARA_PLAYER) += 50000
			gdata(GDATA_FLAG_SUB_MOTHER) = 1002
			rc = findchara(CREATURE_ID_PAELS_MOM_LILY)
			cdata(CDATA_AI_CALM, rc) = 3
			cdata(CDATA_RELATION, rc) = RELATION_NEUTRAL
			cdata(CDATA_X_ORG, rc) = 48
			cdata(CDATA_Y_ORG, rc) = 18
			cell_movechara rc, 48, 18
			buff = lang(_thanks(2), "Thanks!")
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 53 ) {
		listmax = 0
		buff = lang("ここからふもとに下りるのは不便だから、ボランティアで帰還サービスをやってい" + _ru() + _kimi(3) + "も帰還サービスを希望" + _kana(), "I'm practicing a spell of return. Would you like to take my service?")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		gosub *com_return
		goto *chat_end
	}
	if ( chatval == 54 ) {
		if ( calccostreload(0) == 0 ) {
			buff = lang("充填する必要はないみたい" + _da(), "Reload what? You don't have any ammo in your inventory.")
			goto *chat_default
		}
		if ( adata((37 /*!!!@[ADATA_RANCH_BREED_RESTRICTION @@@ ADATA_SHOP_SALE_RESTRICTION]@!!! */), gdata(GDATA_AREA)) == 100 ) {
			buff = lang("今は在庫の準備中" + _da(), "It is currently in preparation.")
			goto *chat_default
		}
		buff = lang("そう" + _dana(3) + "、全員の特殊矢弾を補充すると" + calccostreload(0) + strgold + _da(), "Sure, let me check what type of ammos you need....Okay, reloading all of your ammos will cost " + calccostreload(0) + " gold pieces.")
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calccostreload(0) ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("頼む", "Alright.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "Another time.")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			snd SOUNDLIST_PAYGOLD1
			cdata(CDATA_GOLD, CHARA_PLAYER) -= calccostreload(0)
			p = calccostreload(0, 1)
			adata((37 /*!!!@[ADATA_RANCH_BREED_RESTRICTION @@@ ADATA_SHOP_SALE_RESTRICTION]@!!! */), gdata(GDATA_AREA)) = 100
			buff = lang(_thanks(2), "Thanks!")
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 55 ) {
		screenupdate = -1
		gosub *screen_draw
		invctrl = 0
		gosub *com_reserve
		buff = ""
		goto *chat_default
	}
	if ( chatval == 56 ) {
		list(0, listmax) = 1
		listn(0, listmax) = lang("はじめる", "Let's do it.")
		listmax++
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "W-wai.t..")
		listmax++
		buff = lang("なかなかの体つき" + _dana() + "よし、買" + _u(2), "You are...quite attractive. I'll buy you.")
		gosub *chat_select
		if ( chatval != 1 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		listmax = 0
		buff = lang("いく" + _yo(2), "Okay, no turning back now!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("うふふ", "Come on!")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		gosub *sexsex
		goto *chat_end
	}
	if ( chatval == 58 ) {
		if ( gdata(GDATA_TIME_STOP_TIME) == 0 & gdata(GDATA_FLAG_STOP_TIME_OTHER) == 0 ) {
			gdata(GDATA_FLAG_HOUSE_VISITORS)++
			evadd EVENT_VISITOR
			goto *chat_end
		}
	}
	if ( chatval == 59 ) {
		gdata(GDATA_GUEST)--
		listmax = 0
		buff = lang("追い返す" + _yo(), "Alright.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		buff = ""
		goto *chat_default
	}
	if ( chatval == 60 ) {
		sexvalue = sdata(SKILL_ATTR_CHA, tc) * 25 + 100 + cdata(CDATA_FAME, CHARA_PLAYER) / 10
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= sexvalue ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("はじめる", "Let's do it.")
			listmax++
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("やめる", "W-wai.t..")
		listmax++
		buff = lang("そう" + _dana() + "金貨" + sexvalue + "枚を前払いで" + _kure(), "Okay sweetie, I need " + sexvalue + " gold pieces in front.")
		gosub *chat_select
		if ( chatval != 1 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, cc) -= sexvalue
		cdata(CDATA_GOLD, tc) += sexvalue
		listmax = 0
		buff = lang("いく" + _yo(2), "Okay, no turning back now!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = lang("うふふ", "Come on!")
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		cc = tc
		tc = CHARA_PLAYER
		gosub *sexsex
		cc = CHARA_PLAYER
		goto *chat_end
	}
	if ( chatval == 61 ) {
		if ( gdata(GDATA_FLAG_MAIN) >= 200 ) {
			if ( gdata(GDATA_AREA) == AREA_NT_SOUTH_BORDER ) {
				p = 45, 0
			}
			if ( gdata(GDATA_AREA) == AREA_ST_NORTH_BORDER ) {
				p = 43, 0
			}
			repeat 10
				if ( p(cnt) == 0 ) {
					list(0, listmax) = 0
					listn(0, listmax) = lang("やめる", ".....")
					listmax++
					break
				}
				list(0, listmax) = p(cnt)
				listn(0, listmax) = mapname(p(cnt))
				listmax++
			loop
			buff = lang("(キャラバンの隊長は忙しそうでこちらに気付いていない。今ならキャラバンの荷物に紛れ込んで関所を突破できるだろう。どこ行きの荷物に紛れ込む？)", "(Captain of the caravan is so busy now and unaware of you.You will be able to break through the barrier with a climb into the caravan luggage.Where are you going?)")
			gosub *chat_select
			if ( chatval <= 0 ) {
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			if ( gdata(GDATA_FLAG_MAIN) == 350 ) {
				if ( gdata(GDATA_AREA) == AREA_ST_NORTH_BORDER ) {
					gdata(GDATA_FLAG_MAIN) = 351
				}
			}
			gdata(GDATA_TELEPORT_AREA) = adata(ADATA_PARENT, chatval)
			gdata(GDATA_TELEPORT_LEVEL) = 1
			levelexitby = MAP_EXIT_NORMAL
			prevsekai = gdata(GDATA_WORLD)
			gdata(GDATA_WORLD_RENEW) = TRUE
			gdata(GDATA_WORLD) = adata(ADATA_PARENT, chatval)
			gdata(GDATA_WORLD_X) = adata(ADATA_X, chatval), adata(ADATA_Y, chatval)
			if ( gdata(GDATA_FLAG_MAIN) == 351 ) {
				gdata(GDATA_WORLD_X) = adata(ADATA_X, 12), adata(ADATA_Y, 12)
			}
			fixtransfermap = 1
			chatteleport = 1
			goto *chat_end
		}
		listmax = 0
		buff = lang("(暇そうにしていたキャラバンの隊長は、あなたの視線に気が付いた。)", "(Captain of the caravan had been so bored in, the look of your noticed.)")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("おや？君、ひょっとしてこの国から出ていきたいのかな？それは無理だよ。今は国境が封鎖されていて、お偉いさんの一団くらいしか通れないんだ。封鎖が解除されたらすぐに交易商たちを運ばなければならないので、私も毎日ここで待っているんだが…。", "Oh?Do you want to go out from this country? Unfortunately it's impossible. Because it is now the border blockade, and only the group of Important persons is passable.I am waiting in here every day. Because I must carry traders immediately ,if the blockade was lifted.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strbye
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		listmax = 0
		buff = lang("仮に封鎖が解除されても冒険者は通れないんじゃないかな。ただの交易商なら私が乗せて行ってもいいんだが、私も責任ある立場だからね。冒険者なんて危なくて運べないよ。", "Ordinary people can not pass even if it is released temporarily closed. If you are a trader,I will bring you to South Tyris, but you would an adventurer.I do not carry you because I'm afraid you are likely to snatch our luggages.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		goto *chat_end
	}
	if ( chatval == 62 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < calcrestorecostc() ) {
			buff = strnogold
			goto *chat_default
		}
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcrestorecostc()
		tcbk = tc
		repeat 16
			if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
				continue
			}
			tc = cnt
			efid = SKILL_SPELL_UNCURSE
			efp = 5000
			gosub *effect
		loop
		tc = tcbk
		gosub *chat_refresh
		buff = lang("解呪が完了し" + _ta(), "Done treatment. Take care!")
		goto *chat_default
	}
	if ( chatval == 63 ) {
		invsubroutine = 1
		invctrl = INVCTRL_MATERIAL_ENCHANT, 18
		snd SOUNDLIST_INV
		gosub *com_inventory
		if ( stat == 1 ) {
			wlv = inv(INV_ITEM_LEVEL, ci) + 1
			wcost = limit(wlv * wlv * sqrt(wlv) * 300 + 1000, 1000, 500000)
			if ( gdata(GDATA_FLAG_GUILD_FIGHTER) ) {
				wcost /= 2
			}
			if ( inv(INV_ITEM_LEVEL, ci) > 9 ) {
				buff = lang("こいつはちょっと無理です。", "It can not be enhanced anymore.")
				goto *chat_default
			}
			listmax = 0
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= wcost ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("強化する", "Enhance the equipment.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "Never mind.")
			listmax++
			buff = lang("これを強化するには" + wcost + "goldかかるけどいいかな。", "You will cost " + wcost + "gold.")
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= wcost
				snd SOUNDLIST_DING2
				txt lang(itemname(ci) + "は強化された。", itemname(ci) + " " + is2(inv(INV_ITEM_NUM, ci)) + " surrounded by a golden aura.")
				inv(INV_ITEM_LEVEL, ci)++
				r1 = 0
				gosub *charaRefresh
				buff = lang("強化完了！", "Done treatment. Take care!")
			}
			else {
				buff = lang("冷やかし" + _ka(1), "You kidding? ")
			}
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 64 ) {
		invsubroutine = 1
		invctrl = INVCTRL_MATERIAL_ENCHANT, 18
		snd SOUNDLIST_INV
		gosub *com_inventory
		if ( stat == 1 ) {
			wcost = 12000
			listmax = 0
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= wcost ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("0.1s軽くする", "Make light 0.1s the equipment.")
				listmax++
			}
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= wcost * 10 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("1.0s軽くする", "Make light 0.1s the equipment.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "Never mind.")
			listmax++
			buff = lang("これを軽くするには0.1sにつき" + wcost + "goldかかるけどいいかな。", "You will cost " + wcost + "gold by 0.1s.")
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= wcost
				snd SOUNDLIST_DING2
				txt lang(itemname(ci) + "は少し軽くなった。", itemname(ci) + " becomes light little.")
				inv(INV_ITEM_WEIGHT, ci) -= 100
				if ( inv(INV_ITEM_WEIGHT, ci) < 10 ) {
					inv(INV_ITEM_WEIGHT, ci) = 10
				}
				r1 = 0
				gosub *charaRefresh
				buff = lang("軽量化完了！", "Done treatment. Take care!")
			}
			else {
				if ( chatval == 2 ) {
					snd SOUNDLIST_PAYGOLD1
					cdata(CDATA_GOLD, CHARA_PLAYER) -= wcost * 10
					snd SOUNDLIST_DING2
					txt lang(itemname(ci) + "は少し軽くなった。", itemname(ci) + " becomes light little.")
					inv(INV_ITEM_WEIGHT, ci) -= 1000
					if ( inv(INV_ITEM_WEIGHT, ci) < 10 ) {
						inv(INV_ITEM_WEIGHT, ci) = 10
					}
					r1 = 0
					gosub *charaRefresh
					buff = lang("軽量化完了！", "Done treatment. Take care!")
				}
				else {
					buff = lang("冷やかし" + _ka(1), "You kidding? ")
				}
			}
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 95 ) {
		invsubroutine = 1
		invctrl = INVCTRL_MATERIAL_ENCHANT, 18
		snd SOUNDLIST_INV
		gosub *com_inventory
		if ( stat == 1 ) {
			wcost = 4000
			if ( inv(INV_ITEM_WEIGHT, ci) > 100000 ) {
				buff = lang("こいつはちょっと無理です。", "It can not be heavy anymore.")
				goto *chat_default
			}
			listmax = 0
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= wcost ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("0.1s重くする", "Make heavy 0.1s the equipment.")
				listmax++
			}
			if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= wcost * 10 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("1.0s重くする", "Make heavy 0.1s the equipment.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめる", "Never mind.")
			listmax++
			buff = lang("これを重くするには0.1sにつき" + wcost + "goldかかるけどいいかな。", "You will cost " + wcost + "gold by 0.1s.")
			chatesc = 1
			gosub *chat_select
			if ( chatval == 1 ) {
				snd SOUNDLIST_PAYGOLD1
				cdata(CDATA_GOLD, CHARA_PLAYER) -= wcost
				snd SOUNDLIST_DING2
				txt lang(itemname(ci) + "は少し重くなった。", itemname(ci) + " becomes heavy little.")
				inv(INV_ITEM_WEIGHT, ci) += 100
				r1 = 0
				gosub *charaRefresh
				buff = lang("重量化完了！", "Done treatment. Take care!")
			}
			else {
				if ( chatval == 2 ) {
					snd SOUNDLIST_PAYGOLD1
					cdata(CDATA_GOLD, CHARA_PLAYER) -= wcost * 10
					snd SOUNDLIST_DING2
					txt lang(itemname(ci) + "は少し重くなった。", itemname(ci) + " becomes heavy little.")
					inv(INV_ITEM_WEIGHT, ci) += 1000
					r1 = 0
					gosub *charaRefresh
					buff = lang("重量化完了！", "Done treatment. Take care!")
				}
				else {
					buff = lang("冷やかし" + _ka(1), "You kidding? ")
				}
			}
		}
		else {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
		}
		goto *chat_default
	}
	if ( chatval == 65 ) {
		list(0, listmax) = 0
		listn(0, listmax) = lang("乗船する", ".....")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("ちょっと待って", ".....")
		listmax++
		buff = lang("この紹介状は…なるほど、わかった。さっさと船に乗ってくれ。すぐに出航する。", "Oh?Do you want to go out from this country? Unfortunately it's impossible. Because it is now the border blockade, and only the group of Important persons is passable.I am waiting in here every day. Because I must carry traders immediately ,if the blockade was lifted.")
		gosub *chat_select
		if ( chatval == 0 ) {
			if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
				snd SOUNDLIST_FAIL1
				txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
				goto *chat_end
			}
			gdata(GDATA_TELEPORT_AREA) = AREA_LOST_YLVA
			gdata(GDATA_TELEPORT_LEVEL) = 1
			levelexitby = MAP_EXIT_NORMAL
			prevsekai = gdata(GDATA_WORLD)
			gdata(GDATA_WORLD_RENEW) = TRUE
			gdata(GDATA_WORLD) = AREA_LOST_YLVA
			gdata(GDATA_WORLD_X) = adata(ADATA_X, 71), adata(ADATA_Y, 71)
			fixtransfermap = 1
			chatteleport = 1
			goto *chat_end
		}
		buff = lang("冷やかし" + _ka(1), "You kidding? ")
		goto *chat_end
	}
	if ( chatval == 69 ) {
		if ( cdata(CDATA_ID, tc) == CREATURE_ID_DETECTIVE | cdata(CDATA_ID, tc) == CREATURE_ID_WEREWOLF_DETECTIVE ) {
			buff = lang("この" + _ore(3) + "が人狼？なかなか面白い推理" + _da(3) + "。", "That's an interesting deduction.")
		}
		else {
			if ( cdata(CDATA_TONE, tc) == 0 ) {
				buff = lang("馬鹿馬鹿しい。ここに人狼なんているわけない" + _daro(3) + "、確率的に考えて。いたら、かなりのレアケース" + _yo(3) + "。", "What? I am a human!")
			}
			if ( cdata(CDATA_TONE, tc) == 1 ) {
				buff = lang("ち、違う！" + _ore(3) + "は人狼じゃない" + _yo(3) + "、信じて" + _kure(3) + "！", "What? I am a human!")
			}
			if ( cdata(CDATA_TONE, tc) == 2 ) {
				buff = lang("処刑は1日に1回まで" + _da(3) + "。無駄にしないよう、色々な可能性を考慮してから決めるといい" + _yo(3) + "。", "What? I am a human!")
			}
			if ( cdata(CDATA_TONE, tc) == 3 ) {
				buff = lang("…。", "....")
			}
			if ( cdata(CDATA_TONE, tc) == 4 ) {
				buff = lang("どうして" + _ore(3) + "にその質問をしたのか気になる" + _yo(3) + "。答えられないなら" + _kimi(3) + "が人狼側と勘繰られても仕方ない" + _nda(3) + "。", "What? I am a human!")
			}
			if ( cdata(CDATA_TONE, tc) == 5 ) {
				buff = lang("もし" + _ore(3) + "を処刑することでこの街のためになる情報が得られるのなら、吊ればいい" + _daro(3) + "。", "What? I am a human!")
			}
			if ( cdata(CDATA_TONE, tc) == 6 ) {
				buff = lang("そういえば、あまり見ない顔の人が一人いた" + _yo(3) + "。でも人狼じゃなくて狐って可能性もある" + _kana(3) + "？", "What? I am a human!")
			}
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("さようなら", "Bye.")
		listmax++
		if ( mdata(MDATA_WEREWOLF_STAGE) == 1 ) {
			if ( gdata(GDATA_HOUR) != 11 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("" + cdatan(CDATAN_NAME, tc) + "は人狼だ！", "This person is a werewolf!")
				listmax++
			}
		}
		chatesc = 1
		gosub *chat_select
		if ( chatval == 1 ) {
			modimp tc, -5
			txtef COLOR_PURPLE
			txt lang("" + cdatan(CDATAN_NAME, tc) + "に疑いの目が向けられた。", cdatan(CDATAN_NAME, tc) + " was suspected around.")
			cdata(CDATA_VOTES_RECEIVED, tc) = rnd(2) + 2
			goto *chat_end
		}
		else {
			goto *chat_end
		}
		goto *chat_end
	}
	if ( chatval == 73 ) {
		if ( mdata(MDATA_CITY_AUTHORITY) < 2000 ) {
			listmax = 0
			buff = lang("そんなこと言われても。", "Please contribute to more town.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		snd SOUNDLIST_COMPLETE1
		listmax = 0
		buff = lang("ワアーォ、" + cdatan(CDATAN_AKA, CHARA_PLAYER) + cdatan(CDATAN_NAME, CHARA_PLAYER) + "。是非" + _tanomu(3) + "！", "By all means, please!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
		mdata(MDATA_CITY_COMPLAINTS) = limit(mdata(MDATA_CITY_WATER_POLLUTION) - 15 + mdata(MDATA_CITY_ETHER_CORRUPTION) - 15 + mdata(MDATA_CITY_TAXES), 1, 99)
		goto *chat_end
	}
	if ( chatval == 74 ) {
		list(0, listmax) = 0
		listn(0, listmax) = lang("キャンセル", "Cancel")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("政策の実施", "Policy implementation")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("法律整備", "Law development")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("周辺施設の建設", "Construction of the facility")
		listmax++
		buff = lang("どういう管理を行う" + _noka(3) + "？", "What the management are you going to do ?")
		gosub *chat_select
		if ( chatval == 1 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("キャンセル", "Cancel")
			listmax++
			if ( gdata(GDATA_NEXT_VOTE) > gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("貢献度の高い有権者に追加投票権（発言力50消費）", "Revival of the right to vote(Authority:50) ")
				listmax++
			}
			list(0, listmax) = 2
			listn(0, listmax) = lang("準備中", "Cancel")
			listmax++
			list(0, listmax) = 3
			listn(0, listmax) = lang("準備中", "Cancel")
			listmax++
			buff = lang("どういう政策を行う" + _noka(3) + "？", "What the policy are you going to do ?")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( mdata(MDATA_CITY_AUTHORITY) < 50 ) {
					listmax = 0
					buff = lang(_kimi(3) + "にそんな発言力は無い" + _na(3) + "。", "Your authority is insufficient.")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
				}
				else {
					snd SOUNDLIST_DING2
					gdata(GDATA_NEXT_VOTE) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12
					afterau = mdata(MDATA_CITY_AUTHORITY) - 50
					txt lang("発言力" + mdata(MDATA_CITY_AUTHORITY) + "→" + afterau + " ", "Authority :" + mdata(MDATA_CITY_AUTHORITY) + " -> " + afterau + " ")
					mdata(MDATA_CITY_AUTHORITY) -= 50
				}
			}
			goto *chat_end
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("未実装。", "Sorry, this is undeveloped.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("開発中！", "Sorry, this is undeveloped.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
		}
		goto *chat_end
	}
	if ( chatval == 75 ) {
		list(0, listmax) = 0
		listn(0, listmax) = lang("やっぱりやめる", "Cancel")
		listmax++
		list(0, listmax) = 1
		listn(0, listmax) = lang("ソーサラー実習（攻撃魔法）", "Sorcier practice")
		listmax++
		list(0, listmax) = 2
		listn(0, listmax) = lang("ウォーロック実習（上位攻撃魔法）", "Warlock practice")
		listmax++
		list(0, listmax) = 3
		listn(0, listmax) = lang("エンチャンター実習（付与魔法）", "Enchanter practice")
		listmax++
		list(0, listmax) = 4
		listn(0, listmax) = lang("ヒーラー実習（回復魔法）", "Healer practice")
		listmax++
		list(0, listmax) = 5
		listn(0, listmax) = lang("シャーマン実習（特殊魔法）", "Shaman practice")
		listmax++
		list(0, listmax) = 6
		listn(0, listmax) = lang("メイジ実習（探索魔法）", "Mage practice")
		listmax++
		buff = lang("実習によって経験値を得られる魔法が違う" + _yo(3) + "。", "Which magic practice are you going to do?")
		gosub *chat_select
		if ( chatval >= 1 ) {
			gdata(GDATA_TEMPORARY_FLAGS) = 11111111
			zisyu = chatval
			plat = 0
			repeat 478 - 400, 400
				if ( spell(cnt - 400) <= 0 ) {
					continue
				}
				if ( zisyu == 1 ) {
					if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 2 & sdataref(SKILL_DATAREF_TYPE, cnt) != 1 ) {
						continue
					}
				}
				if ( zisyu == 2 ) {
					if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 3 & cnt != 465 | cnt == 404 ) {
						continue
					}
				}
				if ( zisyu == 3 ) {
					if ( sdataref(SKILL_DATAREF_TYPE, cnt) / 1000 != 1 ) {
						continue
					}
				}
				if ( zisyu == 4 ) {
					if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 4 & cnt != 406 & cnt != 407 & cnt != 412 & cnt != 404 & cnt != 430 & cnt != 439 & cnt != 440 & cnt != 461 ) {
						continue
					}
				}
				if ( zisyu == 5 ) {
					if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 7 & cnt != 454 & cnt != 456 & cnt != 455 & cnt != 437 & cnt != 472 & cnt != 436 & cnt != 413 & cnt != 435 ) {
						continue
					}
				}
				if ( zisyu == 6 ) {
					if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 5 & sdataref(SKILL_DATAREF_TYPE, cnt) != 0 | cnt == 464 | cnt == 441 | cnt == 406 | cnt == 407 | cnt == 412 | cnt == 430 | cnt == 439 | cnt == 440 | cnt == 461 | cnt == 465 | cnt == 454 | cnt == 456 | cnt == 455 | cnt == 437 | cnt == 472 | cnt == 436 | cnt == 413 | cnt == 435 ) {
						continue
					}
				}
				plat += sdata(cnt, CHARA_PLAYER)
			loop
			plat = limit((plat + 1) / 150 + 3, 4, 10) / 2
			if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= plat ) {
				list(0, listmax) = 1
				listn(0, listmax) = lang("いいよ", "No problem.")
				listmax++
			}
			list(0, listmax) = 0
			listn(0, listmax) = lang("だめ", "Cancel.")
			listmax++
			buff = lang("この実習には" + plat + strplat + "かかるけどいい" + _kana(1), "This practice will cost you " + plat + " platinum pieces.")
			gosub *chat_select
			if ( chatval >= 1 ) {
				repeat 478 - 400, 400
					if ( spell(cnt - 400) <= 0 ) {
						continue
					}
					if ( zisyu == 1 ) {
						if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 2 & sdataref(SKILL_DATAREF_TYPE, cnt) != 1 ) {
							continue
						}
					}
					if ( zisyu == 2 ) {
						if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 3 & cnt != 465 | cnt == 404 ) {
							continue
						}
					}
					if ( zisyu == 3 ) {
						if ( sdataref(SKILL_DATAREF_TYPE, cnt) / 1000 != 1 ) {
							continue
						}
					}
					if ( zisyu == 4 ) {
						if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 4 & cnt != 406 & cnt != 407 & cnt != 412 & cnt != 404 & cnt != 430 & cnt != 439 & cnt != 440 & cnt != 461 ) {
							continue
						}
					}
					if ( zisyu == 5 ) {
						if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 7 & cnt != 454 & cnt != 456 & cnt != 455 & cnt != 437 & cnt != 472 & cnt != 436 & cnt != 413 & cnt != 435 ) {
							continue
						}
					}
					if ( zisyu == 6 ) {
						if ( sdataref(SKILL_DATAREF_TYPE, cnt) != 5 & sdataref(SKILL_DATAREF_TYPE, cnt) != 0 | cnt == 464 | cnt == 441 | cnt == 406 | cnt == 407 | cnt == 412 | cnt == 430 | cnt == 439 | cnt == 440 | cnt == 461 | cnt == 465 | cnt == 454 | cnt == 456 | cnt == 455 | cnt == 437 | cnt == 472 | cnt == 436 | cnt == 413 | cnt == 435 ) {
							continue
						}
					}
					skillexp cnt, CHARA_PLAYER, 1000 + sdata(SKILL_ATTR_LER, CHARA_PLAYER) * 2
				loop
				cdata(CDATA_PLATINUM, CHARA_PLAYER) -= plat
				snd SOUNDLIST_DING3
				listmax = 0
				buff = lang("実習は完了し" + _ta() + "潜在能力が落ちているはずなので、ゆっくり休んで" + _kure(), "The practice is now complete.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				plat = 0
			}
		}
		gdata(GDATA_TEMPORARY_FLAGS) = 0
		goto *chat_end
	}
	if ( chatval == 99 ) {
		if ( gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) == 0 ) {
			listmax = 0
			buff = lang("対象となる乗り物は持っていないよう" + _dana(3) + "。", "You do not have a world-vehicle.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strbye
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) != 0 ) {
			list(0, listmax) = 0
			listn(0, listmax) = lang("やめておく", "I'll stop it.")
			listmax++
			list(0, listmax) = 1
			listn(0, listmax) = lang("破棄する", "I don't need the vehicle.")
			listmax++
			buff = lang("ワールドマップでの乗り物を破棄して徒歩に戻る？", "Do you return to walk discard your world-vehicle?")
			gosub *chat_select
			if ( chatval == 1 ) {
				if ( gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) == 1 ) {
					gdata(GDATA_CARGO_LIMIT) -= 10 * 1000
				}
				if ( gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) == 2 ) {
					gdata(GDATA_CARGO_LIMIT) -= 20 * 1000
				}
				if ( gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) == 3 ) {
					gdata(GDATA_CARGO_LIMIT) -= 30 * 1000
				}
				if ( gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) == 4 ) {
					gdata(GDATA_CARGO_LIMIT) -= 100 * 1000
				}
				gosub *calcBurdenPc
				gdata(GDATA_FLAG_WORLD_MAP_VEHICLE_KIND) = 0
				snd SOUNDLIST_AMMO
				listmax = 0
				buff = lang("じゃあ後はこっちで手続きしておく" + _yo(3) + "。", "understood. I will handle.")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strbye
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		goto *chat_end
	}
	if ( chatval == 98 ) {
		if ( cdata(CDATA_GOLD, CHARA_PLAYER) < 300 ) {
			buff = lang("冷やかし" + _ka(1), "You kidding? ")
			goto *chat_default
		}
		if ( gdata(GDATA_FLAG_MAIN) < 110 ) {
			listmax = 0
			buff = lang("今の" + _kimi(3) + "には、まだ必要なさそう" + _dana(3) + "。", "You don't seem to need it yet.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_default
		}
		list(0, listmax) = 0
		listn(0, listmax) = lang("必要な情報はない", "Cancel")
		listmax++
		if ( gdata(GDATA_FLAG_MAIN_SAGE) == 0 & gdata(GDATA_FLAG_MAIN) < 180 ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("灼熱の塔のボス情報", "Boss information 1-1")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN_KING) == 0 & gdata(GDATA_FLAG_MAIN) < 180 ) {
			list(0, listmax) = 2
			listn(0, listmax) = lang("古城のボス情報", "Boss information 1-2")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN_FOOL) == 0 & gdata(GDATA_FLAG_MAIN) < 180 ) {
			list(0, listmax) = 3
			listn(0, listmax) = lang("死者の洞窟のボス情報", "Boss information 1-3")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) <= 135 ) {
			if ( gdata(GDATA_FLAG_ASSASSIN_EVENTS_FOR_ALSAPIA_GILPHEM) <= 30 ) {
				list(0, listmax) = 4
				listn(0, listmax) = lang("レシマスに潜む障害の情報①", "Boss information 1-4")
				listmax++
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 135 & gdata(GDATA_FLAG_MAIN) <= 140 ) {
			if ( gdata(GDATA_FLAG_ASSASSIN_EVENTS_FOR_ALSAPIA_GILPHEM) <= 40 ) {
				list(0, listmax) = 5
				listn(0, listmax) = lang("レシマスに潜む障害の情報②", "Boss information 1-5")
				listmax++
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 140 & gdata(GDATA_FLAG_MAIN) <= 150 ) {
			if ( gdata(GDATA_FLAG_ASSASSIN_EVENTS_FOR_ALSAPIA_GILPHEM) <= 50 ) {
				list(0, listmax) = 6
				listn(0, listmax) = lang("レシマスに潜む障害の情報③", "Boss information 1-6")
				listmax++
			}
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 150 & gdata(GDATA_FLAG_MAIN) < 180 ) {
			list(0, listmax) = 7
			listn(0, listmax) = lang("レシマスのボス情報", "Lesimas boss information")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN_CHAOS_WINGS) == 0 & gdata(GDATA_FLAG_MAIN) >= 210 & gdata(GDATA_FLAG_MAIN) < 290 ) {
			list(0, listmax) = 20
			listn(0, listmax) = lang("異形の神殿のボス情報", "Boss information 2-1")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN_DATA_CHIP) == 0 & gdata(GDATA_FLAG_MAIN) >= 210 & gdata(GDATA_FLAG_MAIN) < 290 ) {
			list(0, listmax) = 21
			listn(0, listmax) = lang("機械要塞のボス情報", "Boss information 2-2")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN_ANKH) == 0 & gdata(GDATA_FLAG_MAIN) >= 210 & gdata(GDATA_FLAG_MAIN) < 290 ) {
			list(0, listmax) = 22
			listn(0, listmax) = lang("黄泉の谷のボス情報", "Boss information 2-3")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 260 & gdata(GDATA_FLAG_MAIN) < 266 ) {
			list(0, listmax) = 23
			listn(0, listmax) = lang("レミードに潜む障害の情報①", "Boss information 2-4")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 266 & gdata(GDATA_FLAG_MAIN) < 270 ) {
			list(0, listmax) = 24
			listn(0, listmax) = lang("レミードに潜む障害の情報②", "Boss information 2-5")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 295 & gdata(GDATA_FLAG_MAIN) < 320 ) {
			list(0, listmax) = 25
			listn(0, listmax) = lang("風の山道のボス情報", "Boss information 2-6")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 290 & gdata(GDATA_FLAG_MAIN) < 340 ) {
			list(0, listmax) = 26
			listn(0, listmax) = lang("レミードのボス情報", "Rehmido boss information")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 378 & gdata(GDATA_FLAG_MAIN) < 455 ) {
			list(0, listmax) = 30
			listn(0, listmax) = lang("混沌の巨城に潜む障害の情報①", "Boss information 3-1")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 396 & gdata(GDATA_FLAG_MAIN) < 400 ) {
			list(0, listmax) = 31
			listn(0, listmax) = lang("ヴァルム沖のボス情報", "Boss information 3-2")
			listmax++
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_ZANAN) < 100 & gdata(GDATA_FLAG_MAIN) >= 404 & gdata(GDATA_FLAG_MAIN) < 440 ) {
			list(0, listmax) = 32
			listn(0, listmax) = lang("ザナンの生体兵器の情報", "Boss information 3-3")
			listmax++
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_MAYROON) < 100 & gdata(GDATA_FLAG_MAIN) >= 404 & gdata(GDATA_FLAG_MAIN) < 440 ) {
			list(0, listmax) = 33
			listn(0, listmax) = lang("メイルーンに現れた悪魔の情報", "Boss information 3-4")
			listmax++
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_EULDERNA) < 100 & gdata(GDATA_FLAG_MAIN) >= 404 & gdata(GDATA_FLAG_MAIN) < 440 ) {
			list(0, listmax) = 34
			listn(0, listmax) = lang("エウダーナに現れた悪魔の情報", "Boss information 3-5")
			listmax++
		}
		if ( gdata(GDATA_FLAG_SUB_REGULUS_KIKKASU) < 100 & gdata(GDATA_FLAG_MAIN) >= 404 & gdata(GDATA_FLAG_MAIN) < 440 ) {
			list(0, listmax) = 35
			listn(0, listmax) = lang("キッカスに現れた悪魔の情報", "Boss information 3-6")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 455 & gdata(GDATA_FLAG_MAIN) < 480 ) {
			list(0, listmax) = 36
			listn(0, listmax) = lang("混沌の巨城に潜む障害の情報②", "Boss information 3-7")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 550 & gdata(GDATA_FLAG_MAIN) < 590 ) {
			list(0, listmax) = 37
			listn(0, listmax) = lang("混沌の巨城に潜む障害の情報③", "Boss information 3-8")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 590 & gdata(GDATA_FLAG_MAIN) < 620 ) {
			list(0, listmax) = 38
			listn(0, listmax) = lang("混沌の巨城に潜む障害の情報④", "Boss information 3-9")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 645 & gdata(GDATA_FLAG_MAIN) < 680 ) {
			list(0, listmax) = 39
			listn(0, listmax) = lang("テスカトリポカの情報", "Cradle of Chaos boss information")
			listmax++
		}
		if ( gdata(GDATA_FLAG_MAIN) >= 680 & gdata(GDATA_FLAG_MAIN) < 760 ) {
			list(0, listmax) = 40
			listn(0, listmax) = lang("混沌の巨城のボス情報", "Cradle of Chaos boss information 2")
			listmax++
		}
		buff = lang("1回ごとに300gold貰う" + _yo(3) + "。", "What information do you want? Pay me 300gold each time.")
		gosub *chat_select
		if ( chatval >= 1 ) {
			snd SOUNDLIST_PAYGOLD1
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 300
		}
		if ( chatval == 1 ) {
			listmax = 0
			buff = lang("灼熱の塔は1組の夫妻が支配している。前に挑戦した奴の話だと、妻である赤き義眼の『クルイツゥア』はレベル24。夫の鋼鉄竜『コルゴン』はレベル16らしい。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("クルイツゥアは逃げ回りながら冷気や暗黒の魔法を撃ってくるって話" + _da(3) + "。そう、灼熱の塔を攻略するには、火炎だけじゃなくて冷気と暗黒にもある程度耐性が必要ってこと" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("コルゴンは竜なので妻よりずっとタフだけど、近づいて殴ってくるだけで動きも遅いらしい。テレポートでコルゴンを翻弄しつつ、先にクルイツゥアを集中攻撃したほうがいいんじゃない" + _kana(3) + "？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("接近戦がやりづらいだろうから、強力な遠距離攻撃があるとスムーズかもしれない" + _na(3) + "。あの灼熱の塔に住めるだけあって、どちらも火炎には滅法強いらしいけど、コルゴンのほうは電撃と魔法にも強いそう" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 2 ) {
			listmax = 0
			buff = lang("情報によると古城の主『ワイナン』はレベル25。強力な地獄武器を持っているけど、基本は逃げ回りながら冷気や暗黒の魔法を撃ってくる。挑むなら地獄、冷気、暗黒の耐性が少しはあったほうがいい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("本人の守りは弱く、地獄にしか耐性がないらしいけど、取り巻きのチェスの駒が邪魔をしてくる。<キング>を処理しないと、次々に敵を呼ばれてジリ貧になるって話" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("変化の杖や突然変異のポーションで別のモンスターに変えれば、1ターンで済む。通らない場合は集中攻撃で急いで<キング>を倒すこと" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 3 ) {
			listmax = 0
			buff = lang("死者の洞窟に行くの" + _ka(3) + "？命知らず" + _dana(3) + "。逃げ帰ってきた奴の話では、闇の奇形『イスシズル』はレベル28。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("テレポートしつつ冷気や暗黒の魔法を放ってきたらしい。装備の力で魔法が増幅されているらしく、とんでもない威力だと言っていた" + _na(3) + "。冷気と暗黒の耐性をそれなりに高めていかないと瞬殺されるかもしれない。耐性が足りない場合は属性耐性のポーションも試してみて" + _kure(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("挑戦者は暗黒、地獄、神経属性で反撃したがほとんど効いてなかったとも言っていた" + _yo(3) + "。もし、神や不死者に威力を発揮する武器があれば活躍するんだろうけど、無い物ねだりをしても仕方がない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 4 ) {
			listmax = 0
			buff = lang("噂だとザナンの暗殺部隊がレシマスを侵攻しているらしい。奴らはレベル25だけど、気配消しと光学迷彩で姿が見えない、不可視の部隊" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("レシマスを進めば確実に襲われる" + _daro(3) + "。特殊な歩法で一瞬で接近してくるから、距離をとって戦うのも難しいはず。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("恐ろしい連中だけど、物質感知の魔法や巻物で位置はわかるし、水魔法やポーションをぶつけて濡らせば透明状態を解除できる" + _yo(3) + "。もし透明なものを視られるようになる装備があれば最高" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("暗殺部隊は複数のチームに分かれて行動しているらしいから、ひとつ撃破しても気を抜いてはいけない" + _yo(3) + "。…それと、部隊の中に白い面をつけた女がいたら用心" + _siro(3) + "。ここだけの話、ザナン皇家に拾われたイカれた殺人鬼が混ざっているらしい。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("白面の『アルサピア』はレベル35。透明能力はないけど、踊るように通常攻撃を回避し、強烈なカウンターで標的を仕留めると言われている。ウワサ通りなら、矢や弾丸にすらカウンターしてくるとんでもない女" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("もし戦うことになったら魔法や技能などの回避不能攻撃で攻めるといい。状態異常の「束縛」で通常攻撃を回避できないようにするのも手ではある。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("束縛できないなら、通常攻撃する仲間には防御を指示しておくべき" + _da(3) + "。防御の指示？コマンドフラッグで一括でやるか、技能の戦術指示で個別にやればいい" + _daro(3) + "？ああ、そうそう。暗殺部隊はアルサピア含め特に属性耐性は持っていないみたい" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 5 ) {
			listmax = 0
			buff = lang("少し怖い話をしてあげ" + _ru(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("レシマスには、古代より多くの調査隊が向かっては命を散らしている。そう、出るん" + _da(3) + "。調査兵のアンデッドが。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("奴らは死んだことに気づかず今も調査を続けている。邪魔をした者は、身体を麻痺させられ地獄の吐息で嬲り殺しにされるん" + _da(3) + "。抵抗しようともがいても、霧の中の幻には攻撃が届かない…。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…麻痺を無効化する装備か守護石の魔法、宝石絢爛のポーションがあれば予防できるんじゃない" + _ka(3) + "？もちろん、神経や地獄の耐性が低いとダメージに耐えきれない可能性は" + _aru(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("アンデッドということは火に弱いから、強力な火炎属性攻撃があるなら楽勝だと思う" + _yo(3) + "。逆に暗黒、地獄、神経属性は効かない" + _daro(3) + "。物理攻撃が当たりにくくなる霧を出すらしいけど、火炎瓶や蜘蛛の巣で霧は散らせる" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 6 ) {
			listmax = 0
			buff = lang("ゼームはパルミアでも高名な魔術士だった。彼の研究のひとつに、心を持った魔力仕掛けのガーディアンがあった。名前は確か…魔鋼の番兵『ギルフェム』。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("王家にいた頃はどうしても完成させることができず悩んでいたそう" + _da(3) + "。そんな彼が《常闇の眼》の膨大な情報を得たら？当然ギルフェムを完成させているはず。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("研究段階でも魔法対策を施されていて、魔法属性の魔法…特に魔法の矢なんか全然効かなかったらしい" + _na(3) + "。他の属性魔法なら多少は効くかもしれない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("文献では鎧をベースにした設計だったから、通常攻撃もあまり効かない" + _daro(3) + "。技能ならそのまま通ると思いたい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("防御重視のようだけど、弱った際に自分で安全装置を外して暴れ出したという記録もあった" + _yo(3) + "。もし暴れ出しても驚いて逃げたりせず、一気に押し切って破壊してしまうべき" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 7 ) {
			listmax = 0
			buff = lang("偽りの預言者『ゼーム』はもはや怪物だと思った方がいい。レシマスに魅入られる前から、モンスターを召喚し混沌の渦を操り、深手も一瞬で治す魔術士だった。それが間違いなくレシマスの魔力で強化されている。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ゼームの前の守護者はレベル55だったらしいから、今のゼームもそのくらいあるんじゃない" + _kana(3) + "。さらにレシマスの宝物庫で、空を飛び魔法を増幅する大鎌を手に入れたという記録もある。混沌耐性がないと、どんなに人数を揃えても一瞬で壊滅してしまう" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("壁生成の魔法や杖で周囲を囲んでしまえば、モンスターは召喚しづらくなる。だけど悠長に囲むにしたってその間攻撃を耐えなきゃいけない" + _nda(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("一応、当時のゼームは相手に真っ向から近付いて接近戦を挑む傾向があったらしい。もしそのままなら、うまく誘導したり加速して振り切ったりできるかもしれない。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("回復魔法の突破も問題" + _ka(3) + "。MPが切れるように仕向けるなら、MPを減らす武器や技能を用意して持久戦に持ち込む。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("回復量を上回るダメージを与えるなら、パワーゲージを100％以上にして技能の【力の解放】や【合体攻撃発動】で仲間と一斉に叩き込むのが堅実" + _daro(3) + "。仲間は突撃態勢以外だとパワーゲージを温存するからタイミングを調整して。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("腐ったトマトや納豆、ヨーグルトをぶつけたりして激怒させればダメージが倍になるけど、受けるダメージも倍になるから、自軍の耐久力と相談" + _dana(3) + "。あとは…沈黙の霧の魔法を無理矢理通すか、" + _dana(3) + "。充分な魔法LVがないと望みはない" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("もし上手くいって《常闇の眼》を手に入れたら、" + _ore(3) + "にもちょっと情報を分けて" + _kure(3) + "。" + _yoro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 20 ) {
			listmax = 0
			buff = lang("異形の神殿を支配している、混沌の翼『エグゾシズル』はレベル60" + _da(3) + "。逃げ回りながら装備で増幅された混沌と暗黒の魔法を使うらしい" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そうそう。前に挑戦した魔法使いは、沈黙の霧をくらって魔法が使えなくなり手も足も出なかったそう" + _da(3) + "。耐性を下げる呪いもかけてくるらしいから、魔法を使う気がなくても浄化の杖や巻物は持っていくといい" + _daro(3) + "。高レベルのホーリーヴェイルなら予防もできる" + _kana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("アンデッドで、神で、浮遊しているから、対応したエンチャントの武器があれば役立つはず" + _dana(3) + "。特に強い耐性も弱い耐性もないよう" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 21 ) {
			listmax = 0
			buff = lang("データによると…おっと、どうやってデータを手に入れたかは訊かないで" + _kure(3) + "。機械要塞に配備されている火炎機竜『メタルヴェスダ』はレベル70。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("メイン武装が散弾だから、遠距離から戦えば被害は減るはず" + _da(3) + "。エネルギー放射攻撃も行うようだけど、これも魔法耐性がある程度あれば怖くない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ただし自爆装置を積んでいるみたい" + _da(3) + "。追い詰めたら一度、自爆できないように相手を濡らしておくといい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("かなりタフだから、周りに邪魔な敵がいればそっちから片づけたほうがいいかもしれない" + _yo(3) + "。火炎に異常に強いようだから、他の属性で攻撃したほうがいい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 22 ) {
			listmax = 0
			buff = lang("黄泉の谷か？あそこにいる葬祭の『アヌビス』はレベル80" + _da(3) + "。最近も、盗掘しようとした連中が痛い目に遭ったらしい" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("頑丈な鎧で武装して挑んだが貫通率の高い武器で殴られてほぼ全滅。防御力があまり意味をなさないみたい" + _dana(3) + "。瞬間移動で距離を詰めてくるから逃げるのも難しいって話" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("やり合うならフェザーの魔法や翼鳥のポーションで回避力を上げて、集中攻撃でさっさと倒さないと厳しいんじゃない" + _kana(3) + "。こちらが物理攻撃を使わないなら闇の霧を撒くという手もある" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("話を聞く限りだと、暗黒と魔法に耐性を持っているみたい" + _da(3) + "。一応神だから神に威力を発揮する武器があれば効くんじゃない" + _kana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 23 ) {
			listmax = 0
			buff = lang("障害になりそうなのは…そう" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("『イェルス自律戦闘通信機』はレベル100。レミード遺跡には2台投入されているって話" + _da(3) + "。敵と認識されなければ戦うことはないはずだけど、一応。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("鋭利な遠隔マニピュレーターで出血させたり、電撃や多彩な射撃兵装で身を守るとされている" + _yo(3) + "。魔法対策も随所に施されているから魔法属性の魔法なんかほとんど効かない" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("戦闘時にはあまり相手に近づかないらしい" + _ga(3) + "。まぁそりゃそう" + _da(3) + "、本来は通信機なんだから。もし戦うなら、コイツからテレポートで距離を取って、他の敵を先に片づけたほうがマシ" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 24 ) {
			listmax = 0
			buff = lang("遺跡内でレベル110くらいの馬と青年に絡まれたって情報があった" + _na(3) + "。どうやらイレギュラーって奴と間違えられて襲われたらしい。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そいつらも、そいつらが探している相手も名前は不明" + _da(3) + "。" + _kimi(3) + "も襲われるかもしれないから気をつけたほうがいい" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("見える範囲にいると青年は精霊を召喚することがあるそう" + _da(3) + "。煙幕を張ったり壁生成で視線を切ったりテレポートの杖で吹っ飛ばしたりするといいかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("精霊どもはMPを減らす眼差しを向けてくるらしいから…やはり相手からなるべく見えないようにすると集中攻撃を防げる" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("MPがどんどん減る魔漏状態にされると厳しいから、MPを回復できる魔力の巻物か杖を多めに用意しておくといい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ちなみに一番強いのは馬らしい" + _yo(3) + "。角に刺されると強烈な混沌の渦が発生することがあるらしい" + _na(3) + "。襲われた奴はたまたま混沌耐性を高めていたから助かったものの、そうでなけりゃ余裕でお陀仏だったって話" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ちなみにどいつもこいつも混沌にはかなりの耐性があるみたい" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 25 ) {
			listmax = 0
			buff = lang("風の山道にはボスはいない。だけどその山道の先に何があるか知ってる" + _kana(3) + "？驚くなかれ、生化学文明紀の人類が集落を作っているって話" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("大昔からエーテルの風が局所的に発生し続けている山だから、メシェーラに適合できなかった旧人類が生き残っていても実際おかしくないと思" + _u(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("エーテル風を発生させる人工生命体が当時研究されてたって文献も見つかっている" + _na(3) + "。…それでも最近までは、あくまで知る人ぞ知るロマンある一つの説に過ぎなかった" + _ga(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("実はここだけの話…確かめに行った" + _nda(3) + "、好奇心溢れる命知らずが。そいつは説を裏付ける証言をした後にエーテル病の合併症で死んだ" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("集落奥の広場にはレベル120くらいの人工生命体もいたそう" + _da(3) + "。魔法属性の攻撃を受けたほか、睨まれたらエーテル病が進行したらしい" + _yo(3) + "。聞いた感じだと特に耐性の得手不得手はないみたい" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("旧人類にも現代の魔術は通るはずだからインコグニートの魔法や変装セットを使えば事を荒立てずに済む" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("旧人類も人工生命体も貴重な情報源だから、" + _ore(3) + "としてはできれば殺さないで欲しい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 26 ) {
			listmax = 0
			buff = lang("レミードの最下層は古代の研究施設。そこにはメシェーラの親玉が眠っている" + _nda(3) + "。レベルは120。いや130？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("その場から移動せず、吸収した細胞から様々な魔物を複製するらしい" + _yo(3) + "。周囲を壁で囲って、魔物を出すスペースを無くしてしまえば封じることができる" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("再生能力が高いって話もあるけどスペックではそこまで高いようには見えない" + _na(3) + "。…2つの情報源で齟齬があるけど、2種類いたりするの" + _kana(3) + "？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("まぁ回復し始めたら回復量を上回るダメージを与えればいいだけ" + _da(3) + "。パワーゲージを100％以上にして技能の【力の解放】や【合体攻撃発動】で仲間と一斉に叩き込むのが堅実" + _daro(3) + "。仲間は突撃態勢以外だとパワーゲージを温存するからタイミングを調整して。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("腐ったトマトや納豆、ヨーグルトをぶつけたりして激怒させればダメージが倍になるけど、受けるダメージも倍になるから、自軍の耐久力と相談" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("再生ということは魔法によるものではないと思" + _u(3) + "。そうだとしたらMP切れや沈黙の霧では対処できない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あとは地獄耐性を高めておくといいらしい" + _yo(3) + "。…こっちのメモには物理攻撃と音攻撃が強力と書いてある" + _na(3) + "？まぁPVとDVと音耐性も高いほうがいい" + _daro(3) + "。相手は特に耐性の得手不得手はないみたい" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("え？こんな情報どこから入ってきたのかって？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("…情報魔っていう、まぁ凄腕の情報屋？みたいなのがいるん" + _da(3) + "。今回はそいつから仕入れたから間違いない" + _yo(3) + "。" + _ore(3) + "にもプライドがあるから、できるだけあの女には頼りたくない" + _ga(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 30 ) {
			listmax = 0
			buff = lang("混沌の精霊の大軍と一緒に、鬼面の忍者が陣取っている階層があるらしい" + _yo(3) + "。もしそれが鬼面の忍者『藍影』だとしたらレベル155" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("一撃離脱の近接攻撃とデバフが得意とされている" + _na(3) + "。ホーリーヴェイルや浄化の杖などで予防対策しておけば安心" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("中途半端に殴ると分身もするがそのまま殴り倒してしまったほうがいい" + _daro(3) + "。あと、音属性の耐性が高いよう" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("混沌の精霊どもは数が多いから一度に相手しないほうがいい" + _daro(3) + "。部屋の隅にテレポートして籠城するか、煙幕を撒きつつどこか一方を突破するか。MP回復手段を用意して混沌属性を高めて行けば問題ないと思" + _u(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 31 ) {
			listmax = 0
			buff = lang("ヴァルムの沖には小さな島々がある" + _nda(3) + "けど、そこから何人か島民が逃げ出してきたという。島民の証言から察するとどうも「九頭竜神話」めいた状況になっているよう" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("九頭竜はおそらくレベル160程度。不定期に精神波で放ち狂気度を上げてくるという報告がある" + _yo(3) + "。ユニコーンの角を何本か持って行かないと、行動不能にされる" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("急いで倒したほうがいいけど、音と幻惑の耐性が低いと返り討ち" + _da(3) + "。他にも怪物が大量にいるから、無視して九頭竜を倒すかどうかは攻撃力とユニコーンの角の本数次第" + _dana(3) + "。魔法属性に耐性を持っているはずだからそれ以外で攻めるべき" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("狂気に蝕まれたら温泉にゆっくり浸かって療養すること" + _da(3) + "。サウスティリスでは、アーク・ベルクや煙とパイプ亭、ルオザの温泉が有名" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 32 ) {
			listmax = 0
			buff = lang("種類が多いから生体兵器だけじゃどのタイプかわからない" + _yo(3) + "、と言いたいところだけどあれ" + _daro(3) + "？メシェーラ利用しようとして生物災害起こしたところ" + _daro(3) + "？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("あそこは前々から黒い噂しか聞こえてこなかった" + _nda(3) + "けど、とうとう破綻したらしい" + _na(3) + "。貴重な適合者に脱走されたのを隠蔽しようとして、最近は輪をかけて無茶苦茶やっていたとも聞く" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ああ、実験台の連中はレベル150" + _da(3) + "。移動能力が無いって話だから、1体ずつ処分していけば苦戦はしない" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("再生力も再現が不完全で魔法頼りみたいだから、押し切れないなら沈黙させておけばいいと思" + _u(3) + "。やり合うならある程度の地獄耐性と防御力はあった方がいい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("他に逃げ遅れた所員やらが怪物化しているだろうけど、実験台と違って戦闘用に調整されていたわけでもないし大したことはない" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ただ、鎮圧用に神経ガスを撒く仕掛けがあったはず" + _da(3) + "。もし作動していれば神経耐性もそこそこ必要だろう" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 33 ) {
			listmax = 0
			buff = lang("前の討伐隊に参加した冒険者から情報は伝わっている" + _yo(3) + "。破壊の悪魔『ラジャシェ』はレベル160" + _da(3) + "。一瞬で飛び込んできて冷気と地獄の範囲攻撃を放ってくるらしい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それと、MPを大量消費して強力な範囲攻撃も撃つそう" + _da(3) + "。魔法耐性の低い奴らは、地形ごと跡形もなく消し飛んだと言っていた" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そんなのを食らいつつも泳いで帰ってきたらしいから、冒険者ってタフ" + _dana(3) + "。撃った後はラジャシェも大分消耗した様子だったらしいから、撃てて4発程度じゃない" + _kana(3) + "？MPを削り取ってもいいけど、その前に勝負がつくかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("混沌の精霊を引き連れていて、ラジャシェが追加召喚することもあるらしい" + _yo(3) + "。念のためMPの回復手段も用意しておくといい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 34 ) {
			listmax = 0
			buff = lang("王都に侵攻している、復讐の悪魔『インクツァル』はレベル160。物理攻撃で暴れまわるタイプみたい" + _dana(3) + "。聖なる盾やフェザーで対抗して" + _kure(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("洗脳も得意らしい。集中の魔法やポーションで意志を高めておけば、被害は軽減できる" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("王宮魔導士が討伐できていないあたり、魔法には高い耐性を持っているはず。復讐心を煽って反乱軍のパワーゲージを増やすこともあるみたい" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("その反乱軍はエウダーナ召喚騎士と魔弓兵が中心のようだけど、悪魔の力でレベル145相当になっていると聞くから油断しないで" + _kure(3) + "。特に召喚騎士は、放っておくと厄介な魔物を召喚するかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 35 ) {
			listmax = 0
			buff = lang("その事件は唯一の生き残りがこの前目を覚ましたばかり" + _da(3) + "。免疫を持っていたそいつ以外は全員病死していたから、こっちとしても助かった" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("証言によると疫病の悪魔『ロアトニス』がレベル160。エーテル病を進行させる眼差しを向けてくるらしいから、抗体も一応持っていくといいかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それと死の宣告も使うらしいから、予防手段のホーリーヴェイルや解除手段の浄化の杖や巻物もあると安心" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("町中には様々なアンデッドが徘徊していてどいつもこいつもレベル140相当に強化されているって話" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ロアトニスの傍らにはレベル153のサヴァントクイーンもいて、一緒にゾンビを強化したりゾンビから体力を吸収したりするらしい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("サヴァントクイーンは魔法耐性が高いけど、火炎にとても弱い" + _yo(3) + "。ゾンビを減らしてからサヴァントクイーンを倒し、最後にロアトニスを倒すのがいい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ロアトニス自体には特に効くわけじゃないけど、強力な火炎属性攻撃があると楽になるはず" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 36 ) {
			listmax = 0
			buff = lang("各地に出現した悪魔は一掃されたけど、伝承によるとまだ一体残っている" + _nda(3) + "。もしかすると混沌の巨城に眠っているのかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("束縛の悪魔『エゲルベイル』はレベル180" + _da(3) + "。束縛付与や蜘蛛の巣で動きを封じてくるとされている" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("戦うなら蜘蛛の巣を水魔法や煙幕でかき消して肉薄し、さっさと勝負を決めたほうがいい" + _daro(3) + "。束縛状態ではDVが高くても通常攻撃を回避できないから、数々の攻撃に耐えなければならない" + _yo(3) + "。壁や煙幕で射線が通らないようにすることも考えた方がいい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("手下も含めれば様々な属性で攻撃してくるらしいけど、特に音耐性を高めておくといい" + _daro(3) + "。なんなら属性耐性のポーションで底上げしてもいいくらい" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 37 ) {
			listmax = 0
			buff = lang("ロスリアの特務部隊が神の間を目指しているのは知っている" + _kana(3) + "。隊員のレベルは164、それを率いる蒼天の稲妻『ランカータ』はレベル168" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ランカータと強化兵は高い魔法耐性を持っているから魔法属性の魔法は悪手" + _dana(3) + "。強化兵のほうは再生能力も併せ持っていてタフだから後に回したいところだけど、異次元の手で敵を引き寄せるのが得意" + _da(3) + "。壁生成や煙幕で視線を切ると邪魔されずに済む" + _yo(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("特務部隊の正規メンバーは射撃と回復魔法を持っているだけで特に脅威にはならない" + _daro(3) + "。まずは、死を恐れず突撃してくる歴戦の老兵から処理するといいと思" + _u(3) + "。いずれも通常物理攻撃がメインのようだから、PVとDVを重視するべき" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("そうそう。情報によると連中は消耗を抑えるために交戦を最小限にしているらしい" + _yo(3) + "。無理矢理突破して先に進んでしまえば、わざわざ追撃してこないんじゃない" + _kana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("それともう一つ。詳細は不明だけど、ランカータの任務には誰かの捜索も入っているよう" + _dana(3) + "。生かしておくと何か起きるかもしれないけど…まぁ、倒してしまっても別に支障はない" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 38 ) {
			listmax = 0
			buff = lang("エウダーナの人形使いの切り札…それが4体のユナイデッド" + _da(3) + "。鹵獲した古代アンデッド兵器ごと姿を消した人形使いだけど、混沌の巨城での目撃情報がある" + _yo(3) + "。ユナイデッドはキング、クイーン、ジャック、ジョーカーの4体で、全てレベル175。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("キングは物理攻撃力と再生能力特化型" + _da(3) + "。倒すのに時間がかかるけど、暴れさせておくと被害が広がる。逆に言うとこいつを早めに落とせれば余裕ができる" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("クイーンは後衛型で攻撃力はさほどでもないけど、再生能力が高いから倒すのは最後に回したほうがいい" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ジャックは平凡な地獄属性攻撃がメインだけど、全体バフ消去も行うし追い詰めると強烈な魔法範囲攻撃を連射する" + _yo(3) + "。パワーゲージを使ってでも火力を集中して真っ先に始末したいところ" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("ジョーカーはテレポートしながら冷気と電撃、鈍足の魔法を放ち、範囲回復もこなす厄介なやつ" + _da(3) + "。先に始末しようにも粘るし逃げるから、後回しの方がマシ" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("どいつも耐性面で特に弱点はない" + _yo(3) + "。普通のアンデッドが苦手としている火炎にも強い。しかもジョーカーは魔法耐性も高いよう" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("もしまともに戦うことになれば苦戦は必至" + _daro(3) + "。一体倒すたびに上の階に逃げ戻るほうがいいかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("下の階に逃げて進むこともできるだろうけど、後続者が被害に遭うのも確実。足止めができると考えるのか、禍根は断っておくべきと考えるのか…それは" + _kimi(3) + "次第" + _dana(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 39 ) {
			listmax = 0
			buff = lang("やっぱり" + _kimi(3) + "も、テスカトリポカに神の間に来いって言われた" + _noka(3) + "？", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("調べによると、テスカトリポカはかつてエヘカトルと双璧をなす神だったらしい" + _yo(3) + "。けれど今では力を没収されてレベル200まで落ちぶれていると聞く" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("夜と煙と戦争の神様だから、煙を撒いて翻弄しつつ闇討ち…という戦法を取ってくるかもしれない。広範囲に火柱や蜘蛛の巣、闇の霧などを撒けるものや爆発で煙をかき消さないと、一方的に殴られる" + _daro(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("自身はないけど、おそらく混沌と幻惑に耐性があったほうがいいと思" + _u(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("身に着けている鏡は、混沌と魔法に耐性を持ち、それ以外の魔法も一部反射するとされている" + _na(3) + "。魔法攻撃を中心に挑むと苦戦するはずだから、使えそうな攻撃アイテムや攻撃技能もピックアップしておくといい" + _daro(3) + "。どうしても魔法で攻めたいなら混沌と魔法属性以外で。自分はその属性への耐性を高めておくこともに忘れずに" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		if ( chatval == 40 ) {
			listmax = 0
			buff = lang("さすがに情報が少なすぎてわからない" + _yo(3) + "。でもこっちだって伊逹に長年情報屋をやってるわけじゃない" + _nda(3) + "。ちょっと考えてみる" + _ka(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("まず拘束されていて動けないだろうけど、魔力で引き寄せてくるかもしれないし油断は禁物" + _da(3) + "。テレポートを防止する装備があればそんな心配もいらないかもしれない" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("混沌を司っている以上、混沌耐性は高いだろうし、混沌の精霊を召喚することも考えられる" + _yo(3) + "。召喚対策やMP回復手段を用意しておくべき" + _da(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("相手の攻撃もやはり混沌属性が中心" + _kana(3) + "？いや、それ以外にも何か使うかもしれないし耐性は一通り揃えて損はない" + _daro(3) + "。埋まらない穴は属性耐性のポーションでどうにかなると思いたい" + _na(3) + "。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			listmax = 0
			buff = lang("もし迂闊に近づいただけで死ぬような相手だった時は、ひたすら逃げ回ってパターンを見極めたり時間を稼ぐのもありじゃない" + _kana(3) + "。そのうち後続の冒険者が追いついてきて、状況が変わるかもしれないし。", "Sorry, this is untranslated sentence.")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
	}
	if ( chatval == 70 ) {
		if ( cdata(CDATA_ROLE_SHOP_LEVEL, tc) <= 0 ) {
			repeat 50
				wolfp = rnd(gdata(GDATA_CROWD) + 1) + 57
				if ( wolfp >= 245 ) {
					continue cnt
				}
				if ( wolfp == tc ) {
					continue cnt
				}
				if ( wolfp == 0 ) {
					continue cnt
				}
				if ( wolfp < maxfollwer ) {
					continue cnt
				}
				if ( cdata(CDATA_ROLE, wolfp) == ROLE_SHOP_TRADE ) {
					continue cnt
				}
				if ( cdata(CDATA_ID, wolfp) == CREATURE_ID_CITIZEN | cdata(CDATA_ID, wolfp) == CREATURE_ID_CITIZEN2 | cdata(CDATA_ID, wolfp) == CREATURE_ID_CITIZEN4 | cdata(CDATA_ID, wolfp) == CREATURE_ID_CITIZEN3 | cdata(CDATA_ID, wolfp) == CREATURE_ID_WEREWOLF | cdata(CDATA_ID, wolfp) == CREATURE_ID_FOX_SPIRIT | cdata(CDATA_ID, wolfp) == CREATURE_ID_SUNBARARIAN | cdata(CDATA_ID, wolfp) == CREATURE_ID_DETECTIVE | cdata(CDATA_ID, wolfp) == CREATURE_ID_WEREWOLF_DETECTIVE ) {
					cdata(CDATA_ROLE_SHOP_LEVEL, tc) = wolfp
					break
				}
			loop
			if ( cdata(CDATA_ID, wolfp) != CREATURE_ID_CITIZEN & cdata(CDATA_ID, wolfp) != CREATURE_ID_CITIZEN2 & cdata(CDATA_ID, wolfp) != CREATURE_ID_CITIZEN4 & cdata(CDATA_ID, wolfp) != CREATURE_ID_CITIZEN3 & cdata(CDATA_ID, wolfp) != CREATURE_ID_WEREWOLF & cdata(CDATA_ID, wolfp) != CREATURE_ID_FOX_SPIRIT & cdata(CDATA_ID, wolfp) != CREATURE_ID_SUNBARARIAN & cdata(CDATA_ID, wolfp) != CREATURE_ID_DETECTIVE & cdata(CDATA_ID, wolfp) != CREATURE_ID_WEREWOLF_DETECTIVE ) {
				cdata(CDATA_ROLE_SHOP_LEVEL, tc) = -1
			}
		}
		if ( cdata(CDATA_ROLE_SHOP_LEVEL, tc) > 0 ) {
			if ( cdata(CDATA_SANITY, tc) >= 5 | cdata(CDATA_ID, tc) == CREATURE_ID_WEREWOLF_DETECTIVE ) {
				listmax = 0
				buff = lang("身辺調査をしてみた結果、どうも" + cdatan(CDATAN_NAME, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) + "は正真正銘の人間のよう" + _da(), "Great! I'll guide you to the place, kill them all!")
				tc = tc * (0 == 0) + (0 != 0) * 0
				list(0, listmax) = 0
				listn(0, listmax) = strmore
				listmax++
				chatesc = 1
				gosub *chat_select
				if ( scenemode ) {
					if ( scene_cut == 1 ) {
						goto *chat_end
					}
				}
				goto *chat_end
			}
			else {
				if ( cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_WEREWOLF | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_FOX_SPIRIT | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_SUNBARARIAN | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_WEREWOLF_DETECTIVE ) {
					listmax = 0
					buff = lang("身辺調査をしてみた結果、どうも" + cdatan(CDATAN_NAME, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) + "は人に化けた魔物のよう" + _da() + "ヤツには気を付けて" + _kure(), "Great! I'll guide you to the place, kill them all!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
				if ( cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_CITIZEN | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_CITIZEN2 | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_CITIZEN4 | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_CITIZEN3 | cdata(CDATA_ID, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) == CREATURE_ID_DETECTIVE ) {
					listmax = 0
					buff = lang("身辺調査をしてみた結果、どうも" + cdatan(CDATAN_NAME, cdata(CDATA_ROLE_SHOP_LEVEL, tc)) + "は正真正銘の人間のよう" + _da(), "Great! I'll guide you to the place, kill them all!")
					tc = tc * (0 == 0) + (0 != 0) * 0
					list(0, listmax) = 0
					listn(0, listmax) = strmore
					listmax++
					chatesc = 1
					gosub *chat_select
					if ( scenemode ) {
						if ( scene_cut == 1 ) {
							goto *chat_end
						}
					}
					goto *chat_end
				}
			}
		}
		else {
			listmax = 0
			buff = lang("現在は特に調査している人物はいない" + _na(), "Great! I'll guide you to the place, kill them all!")
			tc = tc * (0 == 0) + (0 != 0) * 0
			list(0, listmax) = 0
			listn(0, listmax) = strmore
			listmax++
			chatesc = 1
			gosub *chat_select
			if ( scenemode ) {
				if ( scene_cut == 1 ) {
					goto *chat_end
				}
			}
			goto *chat_end
		}
		goto *chat_end
	}
	if ( chatval >= 10000 ) {
		clientguide
		rc = rtval(chatval - 10000)
		p = direction(cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), cdata(CDATA_X, rc), cdata(CDATA_Y, rc))
		if ( p == 1 ) {
			s = lang("西", "west")
		}
		else {
			if ( p == 2 ) {
				s = lang("東", "east")
			}
			else {
				if ( p == 3 ) {
					s = lang("北", "north")
				}
				else {
					s = lang("南", "south")
				}
			}
		}
		p = dist(cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), cdata(CDATA_X, rc), cdata(CDATA_Y, rc))
		repeat 1
			if ( rc == tc ) {
				s = lang("冷やかし" + _ka(1), "You kidding? ")
				break
			}
			if ( cdata(CDATA_EXIST, rc) != CHAR_STATE_ALIVE ) {
				s = lang("奴なら今は死んでいる" + _yo(2), "Oh forget it, dead for now.")
				break
			}
			if ( p < 6 ) {
				s = lang(cdatan(CDATAN_NAME, rc) + "ならすぐ近くにいる" + _yo() + s + "の方を向いてごらん。", "Oh look carefully before asking, just turn " + s + ".")
				break
			}
			if ( p < 12 ) {
				s = lang("ちょっと前に" + s + "の方で見かけた" + _yo(), "I saw " + cdatan(CDATAN_NAME, rc) + " just a minute ago. Try " + s + ".")
				break
			}
			if ( p < 20 ) {
				s = lang(cdatan(CDATAN_NAME, rc) + "なら" + s + "の方角を探してごらん。", "Walk to " + s + " for a while, you'll find " + cdatan(CDATAN_NAME, rc) + ".")
				break
			}
			if ( p < 35 ) {
				s = lang(cdatan(CDATAN_NAME, rc) + "に会いたいのなら、" + s + "にかなり歩く必要があ" + _ru(), "If you want to meet " + cdatan(CDATAN_NAME, rc) + ", you have to considerably walk to " + s + ".")
				break
			}
			s = lang(cdatan(CDATAN_NAME, rc) + _ka(3) + "、ここから" + s + "の物凄く離れた場所にいるはず" + _da(), "You need to walk long way to " + s + " to meet " + cdatan(CDATAN_NAME, rc) + ".")
			break
		loop
		buff = s
		if ( cdata(CDATA_ROLE, tc) == ROLE_INFORMER ) {
			snd SOUNDLIST_PAYGOLD1
			cdata(CDATA_GOLD, CHARA_PLAYER) -= 100
		}
		goto *chat_default
	}
	if ( evid() == 11 ) {
		levelexitby = MAP_EXIT_NORMAL
		chatteleport = 1
		snd SOUNDLIST_EXITMAP1
	}
	goto *chat_end

*chat_slave
	repeat 10
		if ( dorei == 2 ) {
			dorei2 = limit(cdata(CDATA_LEVEL, CHARA_PLAYER) / 2 + 5, 6, 130)
		}
		if ( dorei == 1 ) {
			dorei2 = limit(cdata(CDATA_LEVEL, CHARA_PLAYER) / 4 + 5, 6, 50)
		}
		if ( dorei == 0 ) {
			dorei2 = 5
		}
		flt dorei2
		fixlv = FIX_QUALITY_GOOD
		if ( cdata(CDATA_ROLE, tc) == ROLE_SLAVER ) {
			fltn "man"
		}
		else {
			fltn "horse"
		}
		characreate MAX_CHARA_NC, CREATURE_ID_BUG, -3, 0
		if ( cdata(CDATA_LEVEL, MAX_CHARA_NC) == 0 ) {
			chara_vanquish MAX_CHARA_NC
			continue
		}
		break
	loop
	listmax = 0
	buff = lang("そう" + _dana() + "" + cdatan(CDATAN_NAME, MAX_CHARA_NC) + "を" + calcslavevalue(56) + strgold + "でどう" + _da(1), "Okay. Let me check the stable....How about " + cnven(cdatan(CDATAN_NAME, MAX_CHARA_NC)) + " for " + calcslavevalue(56) + " gold pieces. I'd say it's quite a bargain!")
	if ( cdata(CDATA_LEVEL, CHARA_PLAYER) >= 4 ) {
		list(0, listmax) = 2
		listn(0, listmax) = lang("カタログAを見る(レベル" + limit(cdata(CDATA_LEVEL, CHARA_PLAYER) / 2 + 5, 6, 130) + "以下)", "Never mind.")
		listmax++
	}
	if ( cdata(CDATA_LEVEL, CHARA_PLAYER) >= 4 ) {
		list(0, listmax) = 3
		listn(0, listmax) = lang("カタログBを見る(レベル" + limit(cdata(CDATA_LEVEL, CHARA_PLAYER) / 4 + 5, 6, 50) + "以下)", "Never mind.")
		listmax++
	}
	list(0, listmax) = 4
	listn(0, listmax) = lang("カタログCを見る(レベル5以下)", "Never mind.")
	listmax++
	if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calcslavevalue(56) ) {
		list(0, listmax) = 1
		listn(0, listmax) = lang("買い取る", "I'll pay.")
		listmax++
	}
	list(0, listmax) = 0
	listn(0, listmax) = lang("やめる", "Never mind.")
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( chatval == 1 ) {
		txt lang(cdatan(CDATAN_NAME, MAX_CHARA_NC) + "を買い取った。", "You buy " + cnven(cdatan(CDATAN_NAME, MAX_CHARA_NC)) + ".")
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcslavevalue(56)
		rc = MAX_CHARA_NC
		gosub *add_ally
		buff = lang(_thanks(2), "Thanks!")
		goto *chat_end
	}
	if ( chatval == 2 ) {
		dorei = 2
		goto *chat_slave
	}
	if ( chatval == 3 ) {
		dorei = 1
		goto *chat_slave
	}
	if ( chatval == 4 ) {
		dorei = 0
		goto *chat_slave
	}
	buff = lang("冷やかし" + _ka(1), "You kidding? ")
	goto *chat_default

*chat_quest
	if ( qdata(QDATA_STATE, rq) == QUEST_STATE_ONGOING ) {
		buff = lang("頼んでいた依頼は順調" + _kana(1), "What about my contract? Is everything alright? ")
		goto *chat_default
	}
	val = 1
	gosub *talk_quest
	listmax = 0
	list(0, listmax) = 1
	listn(0, listmax) = lang("受諾する", "I will take the job.")
	listmax++
	list(0, listmax) = 0
	listn(0, listmax) = lang("やめる", "Not now.")
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( chatval == 1 ) {
		p = 0
		repeat gdata(GDATA_CLIENT)
			if ( qdata(QDATA_EXIST, cnt) == QUEST_TYPE_NONE ) {
				continue
			}
			if ( qdata(QDATA_STATE, cnt) != QUEST_STATE_NONE ) {
				p++
			}
		loop
		if ( p >= 5 ) {
			buff = lang("未完了の依頼が多すぎじゃない" + _kana(1) + "この仕事は、安心してまかせられない" + _yo(), "Hey, you've got quite a few unfinished contracts. See me again when you have fishined them.")
			goto *chat_default
		}
		repeat 5
			p = gdata(GDATA_QUEST_POOL + cnt)
			f = 0
			repeat 5
				if ( gdata(GDATA_QUEST_POOL + cnt) == p ) {
					f++
				}
			loop
			if ( qdata(QDATA_STATE, p) == QUEST_STATE_NONE | f > 1 ) {
				gdata(GDATA_QUEST_POOL + cnt) = rq
				break
			}
		loop
		if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_DELIVER ) {
			if ( inv_getfreeid(CHARA_PLAYER) == (-1) ) {
				buff = lang("どうやらバックパックが一杯のよう" + _da() + "持ち物を整理してまた来て" + _kure(), "It seems your backpack is already full. Come see me again when you're ready.")
				goto *chat_default
			}
		}
		if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_ESCORT ) {
			f = get_freeally()
			if ( f == 0 ) {
				buff = lang("これ以上仲間を連れて行けないよう" + _da() + "人数を調整してまた来て" + _kure(), "It seems your party is already full. Come see me again when you're ready.")
				goto *chat_default
			}
			repeat
				if ( cnt == 99 ) {
					dbid = 35
				}
				else {
					dbid = 0
				}
				flt qdata(QDATA_LEVEL, rq) + cnt, FIX_QUALITY_BAD
				fltn "man"
				characreate MAX_CHARA_NC, dbid, -3, 0
				f = stat
				if ( f == 1 ) {
					repeat 16
						if ( cdata(CDATA_EXIST, cnt) == CHAR_STATE_DEAD ) {
							continue
						}
						if ( cdata(CDATA_ID, cnt) == cdata(CDATA_ID, rc) ) {
							if ( cbit(CHARA_BIT_BODYGUARD, cnt) == TRUE ) {
								f = 0
								break
							}
						}
					loop
				}
				if ( f == 1 ) {
					break
				}
			loop
			rc = MAX_CHARA_NC
			gosub *add_ally
			cbitmod CHARA_BIT_BODYGUARD, rc, TRUE
			qdata(QDATA_PARAM2, rq) = cdata(CDATA_ID, rc)
		}
		qdata(QDATA_STATE, rq) = QUEST_STATE_ONGOING
		if ( qdata(QDATA_DEADLINE, rq) == (-1) ) {
			goto *chat_instaQuest
		}
		buff = lang(_thanks() + "期待してい" + _ru(), "Thanks. I'm counting on you.")
		if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_DELIVER ) {
			qdata(QDATA_QUEST_TARGET, qdata((10 /*!!!@[QDATA_DEST_CLIENT @@@ QDATA_ENCOUNTER]@!!! */), rq))++
			flt
			itemcreate CHARA_PLAYER, qdata(QDATA_DEST_ITEM, rq), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER), 0
			txt lang(itemname(ci, 1) + "をバックパックに入れた。", "You put " + itemname(ci, 1) + " in your backpack.")
			snd SOUNDLIST_INV
			gosub *calcBurdenPc
			buff = lang("これが依頼の品物" + _da() + "期限には十分気をつけて" + _kure(), "Here's the package. Be aware of the deadline. I don't want to report you to the guards.")
		}
	}
	else {
		buff = lang("冷やかし" + _ka(1), "You kidding? ")
	}
	goto *chat_default

*chat_instaQuest
	if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_HUNT | qdata(QDATA_EXIST, rq) == QUEST_TYPE_HUNTEX ) {
		listmax = 0
		buff = lang("では、早速案内するので、モンスターを一匹残らず退治して" + _kure(), "Great! I'll guide you to the place, kill them all!")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_HARVEST ) {
		listmax = 0
		buff = lang("畑までは案内するから、しっかりと期限内に作物を納入して" + _kure(), "Fine. I'll take you to my farm.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_MINEFIELD ) {
		listmax = 0
		buff = lang("案内するからついて来て" + _kure() + "頼むから罠に引っかかって死なないで" + _kure(), "Fine. I'll take you to my farm.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	if ( qdata(QDATA_EXIST, rq) == QUEST_TYPE_PARTY ) {
		listmax = 0
		buff = lang("ついて来て" + _kure() + "パーティー会場まで案内する" + _yo(), "Alright, I'll take you to the party now.")
		tc = tc * (0 == 0) + (0 != 0) * 0
		list(0, listmax) = 0
		listn(0, listmax) = strmore
		listmax++
		chatesc = 1
		gosub *chat_select
		if ( scenemode ) {
			if ( scene_cut == 1 ) {
				goto *chat_end
			}
		}
	}
	if ( gdata(GDATA_FLAG_DUEL_TARGET) > 0 ) {
		snd SOUNDLIST_FAIL1
		txt lang("決闘の呪いからは逃げられない！", "You can not run away from the duel!")
		goto *chat_end
	}
	gdata(GDATA_QUEST) = qdata(QDATA_EXIST, rq), qdata(QDATA_TYPE, rq), rq, 1
	gdata(GDATA_RETURN_AREA) = gdata(GDATA_AREA), gdata(GDATA_LEVEL), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	gdata(GDATA_TELEPORT_AREA) = AREA_QUEST
	gdata(GDATA_TELEPORT_LEVEL) = 1
	levelexitby = MAP_EXIT_TELEPORT
	chatteleport = 1
	goto *chat_end

*chat_train
	tcbk = tc
	menucycle = 0
	gosub *com_charainfo
	gosub *chat_refresh
	if ( csskill == (-1) ) {
		buff = lang("訓練が必要なときは、声をかけて" + _kure(), "Come see me again when you need more training.")
		goto *chat_default
	}
	listmax = 0
	if ( csctrl == 2 ) {
		buff = lang(skillname(csskill) + "の能力を訓練するには" + calctraincost(csskill, cc) + strplat + "かかるけどいい" + _kana(1), "Training " + skillname(csskill) + " will cost you " + calctraincost(csskill, cc) + " platinum pieces.")
		if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= calctraincost(csskill, cc) ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("訓練する", "Train me.")
			listmax++
		}
	}
	else {
		buff = lang(skillname(csskill) + "の能力を習得するには" + calclearncost(csskill, cc) + strplat + "かかるけどいい" + _kana(1), "Learning " + skillname(csskill) + " will cost you " + calclearncost(csskill, cc) + " platinum pieces.")
		item_find ITEM_ID_SKILL_TICKET, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
		if ( stat != (-1) ) {
			ci = stat
			if ( inv(INV_ITEM_NUM, ci) >= 1 ) {
				list(0, listmax) = 2
				listn(0, listmax) = lang("スキルチケットで習得する", "Teach me the skill in exchange for a skill ticket.")
				listmax++
			}
		}
		if ( cdata(CDATA_PLATINUM, CHARA_PLAYER) >= calclearncost(csskill, cc) ) {
			list(0, listmax) = 1
			listn(0, listmax) = lang("習得する", "Teach me the skill.")
			listmax++
		}
	}
	list(0, listmax) = 0
	listn(0, listmax) = lang("やめる", "Never mind.")
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( chatval == 1 | chatval == 2 ) {
		snd SOUNDLIST_PAYGOLD1
		if ( csctrl == 2 ) {
			cdata(CDATA_PLATINUM, CHARA_PLAYER) -= calctraincost(csskill, cc)
			modgrowth cc, csskill, 100
			buff = lang("訓練は完了し" + _ta() + "潜在能力が伸びているはずなので、後は自分で鍛えて" + _kure(), "Well done. You've got more room to develop than anyone else I've ever drilled. Keep training.")
			screenupdate = -1
			gosub *screen_draw
			tc = tcbk
			goto *chat_train
		}
		else {
			if ( chatval == 1 ) {
				cdata(CDATA_PLATINUM, CHARA_PLAYER) -= calclearncost(csskill, cc)
			}
			if ( chatval == 2 ) {
				item_find ITEM_ID_SKILL_TICKET, ITEM_FIND_MODE_DBID, ITEM_FIND_LOCATION_CHARAS
				if ( stat != (-1) ) {
					ci = stat
					inv(INV_ITEM_NUM, ci)--
				}
			}
			skillgain cc, csskill
			gdata(GDATA_LEARNED)++
			buff = lang("可能な限りの知識は教え" + _ta() + "後は存分に訓練して" + _kure(), "I've taught you all that I know of the skill. Now develop it by yourself.")
		}
	}
	else {
		buff = lang("訓練が必要なときは、声をかけて" + _kure(), "Come see me again when you need more training.")
	}
	goto *chat_default

*chat_invest
	listmax = 0
	buff = lang("投資をしてくれる" + _noka(1) + "これだけかかるけどいいの" + _kana(1), "Oh, do you want to invest in my shop? It will cost you " + calcinvestvalue() + " golds. I hope you got the money.")
	if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calcinvestvalue() ) {
		list(0, listmax) = 1
		listn(0, listmax) = lang("投資する(" + calcinvestvalue() + strgold + ")", "Invest( " + calcinvestvalue() + strgold + " )")
		listmax++
	}
	if ( cdata(CDATA_GOLD, CHARA_PLAYER) >= calcinvestvalue50() ) {
		list(0, listmax) = 2
		listn(0, listmax) = lang("50規模投資する(" + calcinvestvalue50() + strgold + ")", "Invest 50( " + calcinvestvalue50() + strgold + " )")
		listmax++
	}
	list(0, listmax) = 0
	listn(0, listmax) = lang("やめる", "Reject")
	listmax++
	chatesc = 1
	gosub *chat_select
	if ( chatval == 1 ) {
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcinvestvalue()
		gosub *expInvesting
		cdata(CDATA_ROLE_SHOP_LEVEL, tc) += rnd(2) + 2
		repeat 16 - 1, 1
			if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
				continue
			}
			if ( dist(cdata(CDATA_X, cc), cdata(CDATA_Y, cc), cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt)) > sdataref(SKILL_DATAREF_TG, SKILL_SPACT_CHEER) \ 1000 + 1 ) {
				continue
			}
			if ( fov_los(cdata(CDATA_X, cc), cdata(CDATA_Y, cc), cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt)) == 0 ) {
				continue
			}
			skillexp SKILL_NORMAL_INVESTING, cnt, 300
		loop
		buff = lang(_thanks(2), "Thanks!")
	}
	if ( chatval == 2 ) {
		snd SOUNDLIST_PAYGOLD1
		cdata(CDATA_GOLD, CHARA_PLAYER) -= calcinvestvalue50()
		repeat 20
			gosub *expInvesting
		loop
		cdata(CDATA_ROLE_SHOP_LEVEL, tc) += 50
		repeat 16 - 1, 1
			if ( cdata(CDATA_EXIST, cnt) != CHAR_STATE_ALIVE ) {
				continue
			}
			if ( dist(cdata(CDATA_X, cc), cdata(CDATA_Y, cc), cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt)) > sdataref(SKILL_DATAREF_TG, SKILL_SPACT_CHEER) \ 1000 + 1 ) {
				continue
			}
			if ( fov_los(cdata(CDATA_X, cc), cdata(CDATA_Y, cc), cdata(CDATA_X, cnt), cdata(CDATA_Y, cnt)) == 0 ) {
				continue
			}
			skillexp SKILL_NORMAL_INVESTING, cnt, 6000
		loop
		buff = lang(_thanks(2), "Thanks!")
	}
	if ( chatval != 2 & chatval != 1 ) {
		buff = lang("冷やかし" + _ka(1), "You kidding? ")
	}
	if ( gdata(GDATA_AREA) == AREA_HOME ) {
		calccosthire
	}
	goto *chat_default

*chat_end
	questteleport = 0
	hyouzyou = 0
	hyouzyou2 = 0
	wadai = 0
	tag = 0
	tagevochat = 0
	if ( evochat >= 1 ) {
		if ( evochat != 3 ) {
			redraw 0
			gosub *screen_draw
			gosub *screen_fade
		}
		if ( evochat == 3 ) {
			redraw 0
			gosub *screen_draw
			gosub *screen_fadeIn3
		}
		gsel BUFFER_BUF
		pos 0, 0
		picload exedir + "graphic\\face1" + devfile + ".bmp", 1
		gsel BUFFER_SCREEN
		evochat = 0
		gosub *music_play
	}
	if ( cdata(CDATA_EVOCHAT_POINTS, tc) >= 10 ) {
		cdata(CDATA_EMO_ICON, tc) = 17 + 100 * 3
		cdata(CDATA_EVOCHAT_POINTS, tc) = 10
	}
	if ( scenemode == FALSE ) {
		screenupdate = -1
		gosub *screen_draw
	}
	return

*chat_select
	cs_bk = -1
	key_list = key_enter
	repeat listmax
		key_list(cnt) = key_select(cnt)
		keyrange++
	loop
	keyrange = listmax
	key = ""
	objprm 0, ""
	keylog = ""
	gosub *chat_draw

*chat_select_WHILE1
	if ( 1 == 0 ) {
		goto *chat_select_WEND1
	}
	gosub *chat_draw2
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	cs_listbk
	repeat keyrange
		if ( cs == cnt ) {
			color 100, 160, 250
		}
		else {
			color 255, 255, 255
		}
		noteget s, cnt
		if ( evochat < 2 ) {
			x = wx + 136
			y = wy + wh - 56 - keyrange * 19 + cnt * 19 + 2
		}
		if ( evochat >= 2 ) {
			x = wx + 66
			y = wy + wh - 56 - keyrange * 19 + cnt * 19 + 2
		}
		display_key x, y, cnt
		cs_list listn(0, cnt), x + 30, y, 18, 4, 0
	loop
	cs_bk = cs
	redraw 1
	await cfg_wait1
	key_check
	cursor_check
	stick a, 128
	if ( a == 128 ) {
		if ( scenemode ) {
			scene_cut = 1
			goto *chat_select_WEND1
		}
	}
	p = -1
	repeat keyrange
		if ( key == key_select(cnt) ) {
			p = list(0, cnt)
			break
		}
	loop
	if ( p != (-1) ) {
		chatval = p
		goto *chat_select_WEND1
	}
	if ( key == key_cancel ) {
		if ( chatesc == 1 ) {
			chatval = -1
			goto *chat_select_WEND1
		}
	}
	goto *chat_select_WHILE1

*chat_select_WEND1
	snd SOUNDLIST_MORE1
	cs = 0
	listmax = 0
	return

*chat_draw
	if ( scenemode == FALSE ) {
		screenupdate = -1
		gosub *screen_draw
		cs = 0
	}
	talk_conv buff, 56 - en * 3
	chatpicloaded = FALSE
	redraw 0
	if ( evochat < 2 ) {
		wx = (windoww - 600) / 2 + inf_screenx
		wy = winposy(380)
		ww = 600
		wh = 380
	}
	if ( evochat >= 2 ) {
		wx = (windoww - (-280)) / 2 + inf_screenx
		wy = winposy(280)
		ww = 1480
		wh = 280
	}
	if ( evochat < 2 ) {
		gmode 6, , , 80
		pos wx + 4, wy - 16
		gcopy BUFFER_PIC, 0, 0, 600, 380
	}
	if ( evochat >= 2 ) {
		gmode 6, , , 80
		pos wx + 4 + 750, wy - 16 - 100
		gcopy BUFFER_PIC, 0, 0, 1480, 280
	}

*chat_draw2
	redraw 0
	gmode 2
	pos wx, wy - 20
	gcopy BUFFER_PIC, 0, 0, 600, 380
	if ( cdata(CDATA_TAGTEAM_PARTNER, tc) > 0 ) {
		ttc = cdata(CDATA_TAGTEAM_PARTNER, tc)
	}
	if ( cdata(CDATA_PORTRAIT, tc) >= 0 & cdata(CDATA_FACE, tc) == 0 & evochat < 2 | scenemode ) {
		if ( cdata(CDATA_PORTRAIT, tc) < 80 ) {
			p = cdata(CDATA_SEX, tc) * 80 + cdata(CDATA_PORTRAIT, tc)
		}
		else {
			p = cdata(CDATA_SEX, tc) * 80 + cdata(CDATA_PORTRAIT, tc) - 80
		}
		if ( scenemode ) {
			p = int(actor(1, rc))
			if ( p >= 160 ) {
				p = p - 160
			}
		}
		if ( evochat < 2 ) {
			pos wx + 42, wy + 42
			gzoom 80, 112, 4, p \ 16 * 48, p / 16 * 72, 48, 72
		}
	}
	else {
		tatc = tc
		if ( cdata(CDATA_TAGTEAM_PARTNER, tc) > 0 ) {
			tatc2 = cdata(CDATA_TAGTEAM_PARTNER, tc)
		}
		if ( cdata(CDATA_FACE, tc) == 0 ) {
			s = exedir + "user\\graphic\\face" + abs(cdata(CDATA_PORTRAIT, tc) + 1) + ".bmp"
		}
		if ( cdata(CDATA_TAGTEAM_PARTNER, tc) > 0 ) {
			if ( cdata(CDATA_FACE, tatc2) == 0 ) {
				stati2 = exedir + "user\\graphic\\face" + abs(cdata(CDATA_PORTRAIT, tatc2) + 1) + ".bmp"
			}
		}
		if ( stati2 == "" ) {
			stati2 = exedir + "user\\graphic\\face" + abs(cdata(CDATA_PORTRAIT, tc) + 1) + ".bmp"
		}
		if ( tagevochat > 0 ) {
			if ( cdata(CDATA_TAGTEAM_PARTNER, tc) > 0 ) {
				if ( cdata(CDATA_FACE, tatc2) > 0 ) {
					stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_B.bmp"
					if ( hyouzyou > 0 ) {
						if ( hyouzyou == 7 | hyouzyou == 17 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_H.bmp"
						}
						if ( hyouzyou == 6 | hyouzyou == 16 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_Q.bmp"
						}
						if ( hyouzyou == 5 | hyouzyou == 15 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_A.bmp"
						}
						if ( hyouzyou == 4 | hyouzyou == 14 | hyouzyou == 110 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_B.bmp"
						}
						if ( hyouzyou == 3 | hyouzyou == 13 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_H.bmp"
						}
						if ( hyouzyou == 1 | hyouzyou == 11 | hyouzyou == 18 | hyouzyou == 111 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_F.bmp"
						}
						if ( hyouzyou == 2 | hyouzyou == 12 ) {
							stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_E.bmp"
						}
					}
					if ( cdata(CDATA_EMO_ICON, tatc2) \ 100 == 18 | cdata(CDATA_CONDITION_ANGRY, tatc2) > 0 ) {
						stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_A.bmp"
					}
					if ( cdata(CDATA_EMO_ICON, tatc2) \ 100 == 21 ) {
						stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_Q.bmp"
					}
					if ( cdata(CDATA_EMO_ICON, tatc2) \ 100 == 6 ) {
						stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_F.bmp"
					}
					if ( cdata(CDATA_EMO_ICON, tatc2) \ 100 == 17 ) {
						stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_H.bmp"
					}
					if ( cdata(CDATA_HP, tatc2) < cdata(CDATA_MAX_HP, tatc2) / 2 | cdata(CDATA_EMO_ICON, tatc2) \ 100 == 26 ) {
						stati2 = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tatc2) + "_E.bmp"
					}
				}
			}
		}
		if ( cdata(CDATA_FACE, tc) > 0 ) {
			s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_B.bmp"
			if ( hyouzyou > 0 ) {
				if ( hyouzyou == 7 | hyouzyou == 17 ) {
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_H.bmp"
				}
				if ( hyouzyou == 6 | hyouzyou == 16 ) {
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_Q.bmp"
				}
				if ( hyouzyou == 5 | hyouzyou == 15 ) {
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_A.bmp"
				}
				if ( hyouzyou == 4 | hyouzyou == 14 | hyouzyou == 110 ) {
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_B.bmp"
				}
				if ( hyouzyou == 3 | hyouzyou == 13 ) {
					if ( tag \ 2 == 1 ) {
						await 1000
					}
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_H.bmp"
				}
				if ( hyouzyou == 1 | hyouzyou == 11 | hyouzyou == 18 | hyouzyou == 111 ) {
					if ( tag \ 2 == 1 ) {
						hyouzyou = 4
						await 1000
					}
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_F.bmp"
				}
				if ( hyouzyou == 2 | hyouzyou == 12 ) {
					if ( tag \ 2 == 1 ) {
						hyouzyou = 4
						await 1000
					}
					s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_E.bmp"
				}
			}
			if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 18 | cdata(CDATA_CONDITION_ANGRY, tc) > 0 ) {
				s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_A.bmp"
			}
			if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 21 ) {
				s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_Q.bmp"
			}
			if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 6 ) {
				s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_F.bmp"
			}
			if ( cdata(CDATA_EMO_ICON, tc) \ 100 == 17 ) {
				s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_H.bmp"
			}
			if ( cdata(CDATA_HP, tc) < cdata(CDATA_MAX_HP, tc) / 2 | cdata(CDATA_EMO_ICON, tc) \ 100 == 26 ) {
				s = exedir + "\\user\\graphic2\\face" + cdata(CDATA_FACE, tc) + "_E.bmp"
			}
		}
		exist s
		if ( cdata(CDATA_FACE, tc) > 0 ) {
			if ( strsize == (-1) ) {
				picols = cdata(CDATA_CHARA_PIC, tc)
				if ( cdata(CDATA_PIC, tc) >= xy2pic(1, 21) & cdata(CDATA_PIC, tc) <= xy2pic(31, 21) ) {
					picols = 0
				}
				p = cdata(CDATA_PIC, tc) \ 1000 + picols * 1000, (cdata(CDATA_PIC, tc) - picols * 1000) / 1000
				chara_preparepic p, p(1)
				if ( evochat < 2 ) {
					pos wx + 82, wy + 125 - chipc(CHIPC_F, p)
					gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
					grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 2, chipc(CHIPC_HEIGHT, p) * 2
				}
				if ( evochat >= 2 ) {
					if ( tagevochat == 0 ) {
						pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
						gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
						grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
					}
				}
				if ( evochat >= 2 ) {
					if ( tagevochat > 0 ) {
						if ( cdata(CDATA_IN_TAGTEAM, tc) == (-1) ) {
							pos windoww / 2 - 250, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
							gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
							grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
						}
					}
				}
				if ( evochat >= 2 ) {
					if ( tagevochat > 0 ) {
						if ( cdata(CDATA_IN_TAGTEAM, tc) == 1 ) {
							pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
							gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
							grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
						}
					}
				}
			}
			else {
				gsel BUFFER_BUF
				pos 0, 0
				picload s, 1
				gsel BUFFER_SCREEN
				if ( evochat < 2 ) {
					pos windoww - 300, windowh - 588
					gcopy BUFFER_BUF, 0, 0, 300, 500
				}
				if ( evochat >= 2 ) {
					if ( tagevochat == 0 ) {
						pos windoww / 2 - 150, windowh - 588
						gcopy BUFFER_BUF, 0, 0, 300, 500
					}
				}
				if ( evochat >= 2 ) {
					if ( tagevochat > 0 ) {
						if ( cdata(CDATA_IN_TAGTEAM, tc) == (-1) ) {
							pos windoww / 2 - 150 - 250, windowh - 588
							gcopy BUFFER_BUF, 0, 0, 300, 500
						}
					}
				}
				if ( evochat >= 2 ) {
					if ( tagevochat > 0 ) {
						if ( cdata(CDATA_IN_TAGTEAM, tc) == 1 ) {
							pos windoww / 2 - 150, windowh - 588
							gcopy BUFFER_BUF, 0, 0, 300, 500
						}
					}
				}
			}
		}
		exist stati2
		if ( tagevochat > 0 ) {
			if ( cdata(CDATA_TAGTEAM_PARTNER, tc) > 0 ) {
				if ( cdata(CDATA_FACE, tatc2) > 0 ) {
					if ( strsize == (-1) ) {
						picols2 = cdata(CDATA_CHARA_PIC, tatc2)
						if ( cdata(CDATA_PIC, tatc2) >= xy2pic(1, 21) & cdata(CDATA_PIC, tatc2) <= xy2pic(31, 21) ) {
							picols2 = 0
						}
						p2 = cdata(CDATA_PIC, tatc2) \ 1000 + picols2 * 1000, (cdata(CDATA_PIC, tatc2) - picols2 * 1000) / 1000
						chara_preparepic p2, p2(1)
						if ( evochat >= 2 ) {
							if ( tagevochat > 0 ) {
								if ( cdata(CDATA_IN_TAGTEAM, tatc2) == (-1) ) {
									pos windoww / 2 - 250, windowh - 88 - chipc(CHIPC_HEIGHT, p2) * 3
									gmode 2, chipc(CHIPC_WIDTH, p2), chipc(CHIPC_HEIGHT, p2)
									grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p2) * 6, chipc(CHIPC_HEIGHT, p2) * 6
								}
							}
						}
						if ( evochat >= 2 ) {
							if ( tagevochat > 0 ) {
								if ( cdata(CDATA_IN_TAGTEAM, tatc2) == 1 ) {
									pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p2) * 3
									gmode 2, chipc(CHIPC_WIDTH, p2), chipc(CHIPC_HEIGHT, p2)
									grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p2) * 6, chipc(CHIPC_HEIGHT, p2) * 6
								}
							}
						}
					}
					else {
						gsel BUFFER_BUF
						pos 0, 0
						picload stati2, 1
						gsel BUFFER_SCREEN
						if ( evochat >= 2 ) {
							if ( tagevochat > 0 ) {
								if ( cdata(CDATA_IN_TAGTEAM, tatc2) == (-1) ) {
									pos windoww / 2 - 150 - 250, windowh - 588
									gcopy BUFFER_BUF, 0, 0, 300, 500
								}
							}
						}
						if ( evochat >= 2 ) {
							if ( tagevochat > 0 ) {
								if ( cdata(CDATA_IN_TAGTEAM, tatc2) == 1 ) {
									pos windoww / 2 - 150, windowh - 588
									gcopy BUFFER_BUF, 0, 0, 300, 500
								}
							}
						}
					}
				}
			}
		}
		if ( cdata(CDATA_FACE, tc) == 0 ) {
			picols = cdata(CDATA_CHARA_PIC, tc)
			if ( cdata(CDATA_PIC, tc) >= xy2pic(1, 21) & cdata(CDATA_PIC, tc) <= xy2pic(31, 21) ) {
				picols = 0
			}
			p = cdata(CDATA_PIC, tc) \ 1000 + picols * 1000, (cdata(CDATA_PIC, tc) - picols * 1000) / 1000
			chara_preparepic p, p(1)
			if ( evochat < 2 ) {
				pos wx + 82, wy + 125 - chipc(CHIPC_F, p)
				gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
				grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 2, chipc(CHIPC_HEIGHT, p) * 2
			}
			if ( evochat >= 2 ) {
				if ( tagevochat == 0 ) {
					pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
					gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
					grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
				}
			}
			if ( evochat >= 2 ) {
				if ( tagevochat == 0 ) {
					pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
					gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
					grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
				}
			}
			if ( evochat >= 2 ) {
				if ( tagevochat > 0 ) {
					if ( cdata(CDATA_IN_TAGTEAM, tc) == (-1) ) {
						pos windoww / 2 - 250, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
						gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
						grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
					}
				}
			}
			if ( evochat >= 2 ) {
				if ( tagevochat > 0 ) {
					if ( cdata(CDATA_IN_TAGTEAM, tc) == 1 ) {
						pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p) * 3
						gmode 2, chipc(CHIPC_WIDTH, p), chipc(CHIPC_HEIGHT, p)
						grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p) * 6, chipc(CHIPC_HEIGHT, p) * 6
					}
				}
			}
		}
		if ( tagevochat > 0 ) {
			if ( cdata(CDATA_TAGTEAM_PARTNER, tc) > 0 ) {
				if ( cdata(CDATA_FACE, tatc2) == 0 ) {
					picols2 = cdata(CDATA_CHARA_PIC, tatc2)
					if ( cdata(CDATA_PIC, tatc2) >= xy2pic(1, 21) & cdata(CDATA_PIC, tatc2) <= xy2pic(31, 21) ) {
						picols2 = 0
					}
					p2 = cdata(CDATA_PIC, tatc2) \ 1000 + picols2 * 1000, (cdata(CDATA_PIC, tatc2) - picols2 * 1000) / 1000
					chara_preparepic p2, p2(1)
					if ( evochat >= 2 ) {
						if ( tagevochat == 0 ) {
							pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p2) * 3
							gmode 2, chipc(CHIPC_WIDTH, p2), chipc(CHIPC_HEIGHT, p2)
							grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p2) * 6, chipc(CHIPC_HEIGHT, p2) * 6
						}
					}
					if ( evochat >= 2 ) {
						if ( tagevochat > 0 ) {
							if ( cdata(CDATA_IN_TAGTEAM, tatc2) == (-1) ) {
								pos windoww / 2 - 250, windowh - 88 - chipc(CHIPC_HEIGHT, p2) * 3
								gmode 2, chipc(CHIPC_WIDTH, p2), chipc(CHIPC_HEIGHT, p2)
								grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p2) * 6, chipc(CHIPC_HEIGHT, p2) * 6
							}
						}
					}
					if ( evochat >= 2 ) {
						if ( tagevochat > 0 ) {
							if ( cdata(CDATA_IN_TAGTEAM, tatc2) == 1 ) {
								pos windoww / 2, windowh - 88 - chipc(CHIPC_HEIGHT, p2) * 3
								gmode 2, chipc(CHIPC_WIDTH, p2), chipc(CHIPC_HEIGHT, p2)
								grotate BUFFER_CHR, 0, 960, 0, chipc(CHIPC_WIDTH, p2) * 6, chipc(CHIPC_HEIGHT, p2) * 6
							}
						}
					}
				}
			}
		}
	}
	font lang(cfg_font1, cfg_font2), 10 - en * 2, 0
	color 0, 0, 0
	if ( scenemode == FALSE & evochat < 2 ) {
		if ( cdata(CDATA_SPRITE_SIZE_MILK, tc) < 0 ) {
			pos wx + 35, wy + 160
			mes lang("size:" + cdata(CDATA_SPRITE_SIZE_MILK, tc) + " ", "size:" + cdata(CDATA_SPRITE_SIZE_MILK, tc) + " ")
		}
		display_topic lang("友好", "Impress"), wx + 28, wy + 170
		if ( evochat == 0 ) {
			display_topic lang("興味", "Attract"), wx + 28, wy + 225
			if ( cbit(CHARA_BIT_HIRED, tc) != FALSE ) {
				display_topic lang("期限", "Duration"), wx + 28, wy + 280
			}
			if ( tc < MAX_CHARA_FOLLOWER ) {
				display_topic lang("記念日", "Anniversary"), wx + 28, wy + 280
			}
		}
		if ( evochat == 1 ) {
			display_topic lang("行動力", "Action"), wx + 28, wy + 225
			display_topic lang("幸福度", "Happiness"), wx + 28, wy + 280
		}
	}
	if ( ohanasi6 == 100 ) {
		font lang(cfg_font1, cfg_font2), 13 - en * 2, 0
		color 20, 10, 5
		if ( ohanasi2 >= 0 ) {
			gosub *setHistory1
			pos wx + 200, wy + 70
			mes s
		}
		if ( ohanasi2 != 0 ) {
			gosub *setHistory2
			pos wx + 200, wy + 70 + 1 * 15
			mes s
		}
		if ( ohanasi3 != 0 ) {
			gosub *setHistory3
			pos wx + 200, wy + 70 + 2 * 15
			mes s
		}
		if ( ohanasi4 != 0 ) {
			gosub *setHistory4
			pos wx + 200, wy + 70 + 3 * 15
			mes s
		}
		if ( ohanasi5 != 0 ) {
			gosub *setHistory5
			pos wx + 200, wy + 70 + 4 * 15
			mes s
		}
	}
	font lang(cfg_font1, cfg_font2), 12 + sizefix - en * 2, 1
	color 20, 10, 5
	if ( evochat < 2 ) {
		if ( cdatan(CDATAN_AKA, tc) == "" ) {
			s = cdatan(CDATAN_NAME, tc) + " "
		}
		else {
			s = lang(cdatan(CDATAN_AKA, tc) + " " + cdatan(CDATAN_NAME, tc) + " ", cdatan(CDATAN_NAME, tc) + " of " + cdatan(CDATAN_AKA, tc) + " ")
		}
	}
	else {
		s = ""
	}
	if ( evochat < 2 ) {
		if ( 0 ) {
			s += lang("gauge" + cdata(CDATA_POWER_GAUGE, tc) + " ", "bisexual")
			s += lang("距離" + cdata(CDATA_AI_DISTANCE, tc) + " ", "hunger")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == lang("両性具有", "bisexual") ) {
			s += lang("両性具有", "bisexual")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == lang("なし", "none") ) {
			s += lang("性別不明", "unknown")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == lang("男性", "male") ) {
			s += lang("男性", "male")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == lang("女性", "female") ) {
			s += lang("女性", "female")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == lang("自称男性", "male?") ) {
			s += lang("自称男性", "male?")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == lang("自称女性", "female?") ) {
			s += lang("自称女性", "female?")
		}
		if ( cdatan(CDATAN_NEWSEX, tc) == "" ) {
			if ( cdata(CDATA_SEX, tc) == 0 ) {
				s += cnven(strmale)
			}
			else {
				s += cnven(strfemale)
			}
		}
		if ( cdatan(CDATAN_AKA, tc) != "" ) {
			s += lang(" 名声 ", " Fame: ") + cdata(CDATA_FAME, tc)
		}
		if ( 0 ) {
			s += lang(" 呪い ", " Fame: ") + cdata(CDATA_CURSE, tc)
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE ) {
			if ( cdata(CDATA_ROLE_SHOP_LEVEL, tc) == 0 ) {
				cdata(CDATA_ROLE_SHOP_LEVEL, tc) = 10
			}
		}
		if ( cdata(CDATA_ROLE, tc) >= (1000 /*!!!@[ROLE_SHOP_MIN @@@ ROLE_SHOP_WEAPON]@!!! */) & cdata(CDATA_ROLE, tc) < (2000 /*!!!@[ROLE_GUEST_BEGGAR @@@ ROLE_GUEST_MIN]@!!! */) | cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
			s += lang(" 店の規模:", " Shop Rank:") + cdata(CDATA_ROLE_SHOP_LEVEL, tc)
		}
		if ( gdata(GDATA_GOD_DETECT) ) {
			s += " (" + godname(cdata(CDATA_GOD, tc)) + ")"
		}
		if ( 0 ) {
			s += " imp:" + cdata(CDATA_IMPRESSION, tc)
		}
		if ( scenemode ) {
			s = actor(0, rc)
		}
	}
	if ( evochat < 2 ) {
		pos wx + 120, wy + 16
		mes s
	}
	if ( evochat >= 2 ) {
		pos wx + 50, wy + 16
		mes s
	}
	font lang(cfg_font1, cfg_font2), 13 - en * 2, 0
	color 20, 10, 5
	if ( scenemode == FALSE & evochat < 2 ) {
		if ( chatval(2) == 1 ) {
			s = _impression(implevel(cdata(CDATA_IMPRESSION, tc)))
			if ( cdata(CDATA_IMPRESSION, tc) < 150 ) {
				s(1) = "" + cdata(CDATA_IMPRESSION, tc)
			}
			else {
				s(1) = "???"
			}
			pos wx + 32, wy + 198
			mes "(" + s(1) + ")" + s
			if ( cbit(CHARA_BIT_HIRED, tc) != FALSE ) {
				pos wx + 32, wy + 310
				mes "" + cnvdate(cdata(CDATA_HIRE_DATE, tc), 2)
			}
			if ( tc < MAX_CHARA_FOLLOWER | cbit(CHARA_BIT_LIVESTOCK, tc) == TRUE ) {
				s = _impression2(implevel2(cdata(CDATA_MASTER_SERVANT, tc)))
				s(1) = "" + cdata(CDATA_MASTER_SERVANT, tc)
				pos wx + 32, wy + 212
				mes "(" + s(1) + ")" + s
				if ( cdata(CDATA_JOIN_DATE_MONTH, tc) == 0 ) {
					cdata(CDATA_JOIN_DATE_MONTH, tc) = gdata(GDATA_MONTH)
				}
				if ( cdata(CDATA_JOIN_DATE_DAY, tc) == 0 ) {
					cdata(CDATA_JOIN_DATE_DAY, tc) = gdata(GDATA_DAY)
				}
				if ( cdata(CDATA_JOIN_DATE_YEAR, tc) == 0 ) {
					cdata(CDATA_JOIN_DATE_YEAR, tc) = gdata(GDATA_YEAR)
				}
				if ( evochat == 0 ) {
					pos wx + 50, wy + 310
					mes "" + cdata(CDATA_JOIN_DATE_MONTH, tc) + "/" + cdata(CDATA_JOIN_DATE_DAY, tc)
				}
			}
			if ( evochat == 1 ) {
				if ( cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER) > 0 ) {
					repeat cdata(CDATA_EVOCHAT_POINTS, CHARA_PLAYER)
						pos wx + 26 + cnt * 8, wy + 255
						gcopy BUFFER_INF, 784, 360, 16, 16
					loop
				}
				if ( cdata(CDATA_EVOCHAT_POINTS, tc) > 0 ) {
					repeat cdata(CDATA_EVOCHAT_POINTS, tc)
						pos wx + 26 + cnt * 8, wy + 310
						gcopy BUFFER_INF, 768, 360, 16, 16
					loop
				}
			}
			if ( evochat == 0 ) {
				if ( cdata(CDATA_INTEREST, tc) >= 0 ) {
					repeat cdata(CDATA_INTEREST, tc) / 5 + 1
						pos wx + 26 + cnt * 4, wy + 255
						gcopy BUFFER_INF, 16, 360, 16, 16
					loop
				}
			}
		}
		else {
			color 0, 0, 0
			pos wx + 60, wy + 198
			mes "-"
			pos wx + 60, wy + 245
			mes "-"
		}
	}
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	color 20, 10, 5
	notesel buff
	if ( evochat < 2 ) {
		repeat noteinfo(0)
			p = cnt
			x = wx + 150
			y = wy + 43 + cnt * 19
			noteget s, p
			pos x, y
			mes s
		loop
	}
	return

*shop_init
	if ( tc > CHARA_PLAYER ) {
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE ) {
			gosub *shop_tradePrice
		}
	}
	mode = MODELIST_SHOP
	file = "shoptmp.s2"
	fmode = 4
	gosub *game_ctrlFile
	if ( cdata(CDATA_ROLE_FILE_ID, tc) == 0 ) {
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_WANDER | cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
			cdata(CDATA_ROLE_FILE_ID, tc) = 1
		}
		else {
			gdata(GDATA_COUNT_SHOP)++
			cdata(CDATA_ROLE_FILE_ID, tc) = gdata(GDATA_COUNT_SHOP)
		}
		gosub *shop_restock
	}
	else {
		file = "shop" + invfile + ".s2"
		existwrapper exedir + "tmp\\" + file
		if ( strsize == (-1) | gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 >= cdata(CDATA_ROLE_RESTOCK, tc) ) {
			gosub *shop_restock
		}
		else {
			fmode = 3
			gosub *game_ctrlFile
		}
	}
	invfile = cdata(CDATA_ROLE_FILE_ID, tc)
	goto *shop_end

*shop
	mode = MODELIST_SHOP
	file = "shoptmp.s2"
	fmode = 4
	gosub *game_ctrlFile
	file = "shop" + invfile + ".s2"
	fmode = 3
	gosub *game_ctrlFile
	shoptrade = 0
	if ( tc > CHARA_PLAYER ) {
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE ) {
			shoptrade = 1
		}
	}
	goto *com_inventory

*shop_end
	file = "shop" + invfile + ".s2"
	fmode = 4
	gosub *game_ctrlFile
	file = "shoptmp.s2"
	fmode = 3
	gosub *game_ctrlFile
	mode = MODELIST_MAIN
	return

*shop_restock
	inv_getheader -1
	repeat invrange, invhead
		inv(INV_ITEM_NUM, cnt) = 0
	loop
	p = 40 + cdata(CDATA_ROLE_SHOP_LEVEL, tc) * 2 / 3
	if ( p > 120 ) {
		p = 120
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BLACKMARKET ) {
		p = 7 + limit(cdata(CDATA_ROLE_SHOP_LEVEL, tc) / 7, 1, 30)
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_ARMOR ) {
		p = 15 + limit(cdata(CDATA_ROLE_SHOP_LEVEL, tc) / 7, 1, 120)
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_WANDER ) {
		p = 6 + rnd(4)
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
		p = 6 + rnd(4)
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MIROK ) {
		p = 23
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOUNTAIN2 ) {
		p /= 2
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_STOKE ) {
		p = 19
	}
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOYER ) {
		p /= 4
	}
	if ( cdata(CDATA_FAME, tc) > 0 ) {
		if ( cdata(CDATA_ROLE, tc) != ROLE_SHOP_WANDER & cdata(CDATA_ROLE, tc) != ROLE_GUEST_MERCHANT & cdata(CDATA_ROLE, tc) != ROLE_SHOP_STOKE & cdata(CDATA_ROLE, tc) != ROLE_SHOP_MOYER ) {
			p += limit(cdata(CDATA_FAME, tc) / 3000, 0, 20)
			cdata(CDATA_FAME, tc) = 0
		}
	}
	repeat p
		flt calcobjlv(cdata(CDATA_ROLE_SHOP_LEVEL, tc)), calcfixlv(FIX_QUALITY_GOOD)
		dbid = 0
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MAGIC ) {
			p = rnd(3)
			if ( p == 0 ) {
				flttypeminor = FILTER_ITEM_POTION_HEAL
			}
			if ( p == 1 ) {
				flttypemajor = FILTER_ITEM_SCROLL
			}
			if ( p == 2 ) {
				flttypemajor = FILTER_ITEM_POTION
			}
			if ( rnd(7) == 0 ) {
				flttypemajor = FILTER_ITEM_SPELLBOOK
			}
			if ( rnd(15) == 0 ) {
				flttypemajor = FILTER_ITEM_BOOK
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_SISTER ) {
			dbid = 667
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BOOK_RESERVE ) {
			listmax = 0
			repeat 1499
				if ( itemmemory(2, cnt) > 1 ) {
					list(0, listmax) = cnt
					listmax++
				}
			loop
			if ( listmax == 0 ) {
				return
			}
			dbid = list(0, rnd(listmax))
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_NOYEL ) {
			p = rnd(3)
			if ( p == 0 ) {
				flttypemajor = FILTER_ITEM_TOOL
			}
			if ( p == 1 ) {
				flttypemajor = FILTER_ACCESSORY_RING
			}
			if ( p == 2 ) {
				flttypemajor = FILTER_ACCESSORY_AMULET
			}
			if ( rnd(3) == 0 ) {
				fixlv = FIX_QUALITY_GREAT
			}
			if ( rnd(10) == 0 ) {
				fixlv = FIX_QUALITY_MIRACLE
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_GENERAL ) {
			p = rnd(5)
			if ( p == 0 ) {
				flttypemajor = FILTER_AMMO
			}
			if ( p == 1 ) {
				flttypemajor = FILTER_FURNITURE
			}
			if ( p == 2 ) {
				flttypemajor = FILTER_CLOAK
			}
			if ( p == 3 ) {
				flttypemajor = FILTER_ORE
			}
			if ( p == 4 ) {
				flttypemajor = FILTER_ITEM_TOOL
			}
			if ( rnd(20) == 0 ) {
				dbid = 734
			}
			if ( rnd(8) == 0 ) {
				flttypemajor = FILTER_CARGO_FOOD
			}
			if ( rnd(10) == 0 ) {
				dbid = isetdeed(rnd(length(isetdeed)))
			}
			if ( rnd(20) == 0 ) {
				dbid = 1134
			}
			if ( gdata(GDATA_MONTH) == 2 ) {
				if ( rnd(2) == 0 ) {
					dbid = 907
				}
			}
			if ( gdata(GDATA_MONTH) == 12 ) {
				if ( gdata(GDATA_DAY) <= 25 ) {
					if ( rnd(2) == 0 ) {
						dbid = 1106
					}
				}
			}
			if ( gdata(GDATA_MONTH) == 12 ) {
				if ( gdata(GDATA_DAY) > 25 ) {
					if ( rnd(20) == 0 ) {
						dbid = 1106
					}
				}
			}
			if ( cnt == 1 ) {
				if ( rnd(4) != 0 ) {
					dbid = 685
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BAKERY ) {
			if ( rnd(3) != 0 ) {
				flttypeminor = FILTER_ITEM_FOOD_BREAD
			}
			else {
				flttypeminor = FILTER_ITEM_FOOD_PASTA
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_FOOD ) {
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_AILE_THE_ATTENDANT ) {
				if ( rnd(3) != 0 ) {
					continue
				}
			}
			if ( cdata(CDATA_ID, tc) == CREATURE_ID_AILE_THE_ATTENDANT ) {
				if ( rnd(5) != 0 ) {
					continue
				}
			}
			flttypemajor = FILTER_ITEM_FOOD
			if ( rnd(5) == 0 ) {
				flttypemajor = FILTER_CARGO_FOOD
			}
			if ( cdata(CDATA_ID, tc) == CREATURE_ID_AILE_THE_ATTENDANT ) {
				if ( rnd(5) == 0 ) {
					dbid = 910
				}
				if ( rnd(10) == 0 ) {
					dbid = 520
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BLACKMARKET ) {
			flttypemajor = fsetwear(rnd(length(fsetwear)))
			fixlv = FIX_QUALITY_GREAT
			if ( rnd(limit(cdata(CDATA_ROLE_SHOP_LEVEL, tc) / 10, 1, 400) + 200) > rnd(1000) ) {
				fixlv = FIX_QUALITY_MIRACLE
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_WANDER | cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
			flttypemajor = fsetwear(rnd(length(fsetwear)))
			fixlv = FIX_QUALITY_GREAT
			if ( rnd(2) == 0 ) {
				fixlv = FIX_QUALITY_MIRACLE
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_INN ) {
			if ( cdata(CDATA_ID, tc) == CREATURE_ID_BARTENDER2 ) {
				flttypemajor = FILTER_ITEM_SPELLBOOK
			}
			if ( cdata(CDATA_ID, tc) == CREATURE_ID_BARTENDER2 ) {
				if ( rnd(2) == 0 ) {
					flttypeminor = FILTER_ITEM_POTION_ALE
				}
			}
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_BARTENDER2 ) {
				flttypemajor = FILTER_CARGO_FOOD
			}
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_CLERK ) {
				if ( rnd(4) ) {
					flttypeminor = FILTER_ITEM_POTION_ALE
				}
			}
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_BARTENDER2 & cdata(CDATA_ID, tc) != CREATURE_ID_CLERK ) {
				if ( rnd(10) == 0 ) {
					dbid = 734
				}
				if ( rnd(30) == 0 ) {
					dbid = isetdrinkinn(rnd(length(isetdrinkinn)))
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_GOODS ) {
			flttypemajor = FILTER_ITEM_ROD
			if ( rnd(3) == 0 ) {
				flttypemajor = fsetwear(rnd(length(fsetwear)))
			}
			if ( rnd(3) == 0 ) {
				flttypemajor = FILTER_FURNITURE
			}
			if ( rnd(5) == 0 ) {
				flttypemajor = FILTER_ITEM_FOOD
			}
			if ( rnd(4) == 0 ) {
				flttypemajor = FILTER_ITEM_SCROLL
			}
			if ( rnd(15) == 0 ) {
				flttypemajor = FILTER_ITEM_BOOK
			}
			if ( rnd(10) == 0 ) {
				flttypemajor = FILTER_CARGO_FOOD
			}
			if ( rnd(10) == 0 ) {
				dbid = isetdeed(rnd(length(isetdeed)))
			}
			if ( rnd(20) == 0 ) {
				dbid = 1134
			}
			if ( rnd(15) == 0 ) {
				dbid = 511
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_ARMOR ) {
			if ( rnd(limit(cdata(CDATA_ROLE_SHOP_LEVEL, tc), 1, 1000) + 200) > rnd(1000) ) {
				fixlv = FIX_QUALITY_GREAT
			}
			p = rnd(6)
			if ( p == 0 ) {
				flttypemajor = FILTER_ARMOR
			}
			if ( p == 1 ) {
				flttypemajor = FILTER_HELM
			}
			if ( p == 2 ) {
				flttypemajor = FILTER_GLOVES
			}
			if ( p == 3 ) {
				flttypemajor = FILTER_BOOTS
			}
			if ( p == 4 ) {
				flttypemajor = FILTER_SHIELD
			}
			if ( p == 5 ) {
				flttypemajor = FILTER_GIRDLE
			}
			if ( rnd(3) == 0 ) {
				if ( rnd(3) == 0 ) {
					flttypemajor = FILTER_WEAPON
				}
				else {
					flttypemajor = FILTER_RANGE
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE ) {
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_CLERK ) {
				flttypemajor = FILTER_CARGO_TRADE
			}
			if ( cdata(CDATA_ID, tc) == CREATURE_ID_CLERK ) {
				if ( rnd(2) == 0 ) {
					dbid = 1117
				}
				else {
					dbid = 1118
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_THIEF ) {
			flttypemajor = FILTER_ITEM_TOOL
			if ( rnd(2) == 0 ) {
				dbid = 636
			}
			if ( rnd(2) == 0 ) {
				dbid = 629
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_SALESPERSON ) {
			if ( rnd(4) == 0 ) {
				flttypemajor = FILTER_RANGE
			}
			if ( rnd(5) == 0 ) {
				flttypemajor = FILTER_AMMO
			}
			if ( rnd(3) == 0 ) {
				flttypemajor = FILTER_ITEM_FOOD
			}
			fltn "sf"
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_DEED ) {
			if ( rnd(2) != 0 ) {
				continue
			}
			flttypemajor = FILTER_ITEM_BOOK
			if ( rnd(3) == 0 ) {
				dbid = isetdeed(rnd(length(isetdeed)))
			}
			if ( rnd(5) == 0 ) {
				dbid = 1134
			}
			if ( rnd(5) == 0 ) {
				dbid = 511
			}
			if ( rnd(5) == 0 ) {
				dbid = 897
			}
			if ( rnd(15) == 0 ) {
				dbid = 712
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOUNTAIN2 ) {
			fltn "spshop"
			if ( rnd(20) == 0 ) {
				dbid = 869
			}
			if ( cnt == 0 ) {
				dbid = 430
			}
			if ( cnt == 1 ) {
				dbid = 287
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_FESTIVAL ) {
			fltn "fest"
			if ( rnd(12) == 0 ) {
				dbid = 750
			}
			if ( rnd(12) == 0 ) {
				dbid = 751
			}
			if ( rnd(5) == 0 ) {
				dbid = 770
			}
			if ( rnd(5) == 0 ) {
				dbid = 867
			}
			if ( gdata(GDATA_AREA) == AREA_NOYEL ) {
				if ( rnd(12) == 0 ) {
					dbid = 762
				}
			}
			if ( gdata(GDATA_AREA) == AREA_NOYEL ) {
				if ( rnd(12) == 0 ) {
					dbid = 768
				}
			}
			if ( gdata(GDATA_AREA) == AREA_NOYEL ) {
				if ( rnd(12) == 0 ) {
					dbid = 769
				}
			}
			if ( gdata(GDATA_AREA) == AREA_NOYEL ) {
				if ( rnd(12) == 0 ) {
					dbid = 779
				}
			}
			if ( gdata(GDATA_AREA) == AREA_NOYEL ) {
				if ( rnd(12) == 0 ) {
					dbid = 780
				}
			}
			if ( gdata(GDATA_AREA) == AREA_LUDUS ) {
				if ( rnd(12) == 0 ) {
					dbid = 759
				}
			}
			if ( gdata(GDATA_AREA) == AREA_LUDUS ) {
				if ( rnd(12) == 0 ) {
					dbid = 512
				}
			}
			if ( gdata(GDATA_AREA) == AREA_LUDUS ) {
				if ( rnd(12) == 0 ) {
					dbid = 873
				}
			}
			if ( gdata(GDATA_AREA) == AREA_LUDUS ) {
				if ( rnd(12) == 0 ) {
					dbid = 872
				}
			}
			if ( gdata(GDATA_AREA) == AREA_LUDUS ) {
				if ( rnd(5) == 0 ) {
					dbid = 844
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOYER ) {
			dbid = 685
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOUNTAIN1 ) {
			dbid = 519
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_OFFICE ) {
			flttypemajor = FILTER_FURNITURE
			if ( cdata(CDATA_ID, tc) != CREATURE_ID_CLERK ) {
				if ( cnt == 0 ) {
					dbid = 510
				}
				if ( cnt == 1 ) {
					dbid = 561
				}
				if ( cnt == 2 ) {
					dbid = 562
				}
				if ( cnt == 3 ) {
					dbid = 547
				}
				if ( cnt == 4 ) {
					dbid = 579
				}
				if ( cnt == 5 ) {
					dbid = 576
				}
				if ( cnt == 6 ) {
					dbid = 611
				}
				if ( cnt == 7 ) {
					dbid = 701
				}
				if ( cnt == 8 ) {
					dbid = 921
				}
				if ( cnt == 9 ) {
					dbid = 1174
				}
				if ( cnt == 10 ) {
					dbid = 1180
				}
				if ( cnt > 10 ) {
					if ( rnd(3) != 0 ) {
						continue
					}
				}
				if ( cnt == 19 ) {
					dbid = 413
				}
				if ( cnt == 20 ) {
					dbid = 414
				}
				if ( cnt == 21 ) {
					dbid = 616
				}
				if ( cnt == 22 ) {
					dbid = 1084
				}
				if ( cnt == 23 ) {
					dbid = 1085
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_FISH ) {
			dbid = 617
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MIROK ) {
			if ( cnt == 0 ) {
				dbid = 430
			}
			if ( cnt == 1 ) {
				dbid = 431
			}
			if ( cnt == 2 ) {
				dbid = 502
			}
			if ( cnt == 3 ) {
				dbid = 480
			}
			if ( cnt == 4 ) {
				dbid = 421
			}
			if ( cnt == 5 ) {
				dbid = 603
			}
			if ( cnt == 6 ) {
				dbid = 615
			}
			if ( cnt == 7 ) {
				dbid = 559
			}
			if ( cnt == 8 ) {
				dbid = 516
			}
			if ( cnt == 9 ) {
				dbid = 616
			}
			if ( cnt == 10 ) {
				dbid = 623
			}
			if ( cnt == 11 ) {
				dbid = 505
			}
			if ( cnt == 12 ) {
				dbid = 624
			}
			if ( cnt == 13 ) {
				dbid = 625
			}
			if ( cnt == 14 ) {
				dbid = 626
			}
			if ( cnt == 15 ) {
				dbid = 627
			}
			if ( cnt == 16 ) {
				dbid = 56
			}
			if ( cnt == 17 ) {
				dbid = 742
			}
			if ( cnt == 18 ) {
				dbid = 760
			}
			if ( cnt == 19 ) {
				dbid = 892
			}
			if ( cnt == 20 ) {
				dbid = 1037
			}
			if ( cnt == 21 ) {
				dbid = 1186
			}
			if ( cnt == 22 ) {
				dbid = 1113
			}
			if ( cnt > 22 ) {
				continue
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_STOKE ) {
			if ( cnt == 0 ) {
				dbid = 799
			}
			if ( cnt == 1 ) {
				dbid = 427
			}
			if ( cnt == 2 ) {
				dbid = 425
			}
			if ( cnt == 3 ) {
				dbid = 426
			}
			if ( cnt == 4 ) {
				dbid = 424
			}
			if ( cnt == 5 ) {
				dbid = 423
			}
			if ( cnt == 6 ) {
				dbid = 422
			}
			if ( cnt == 7 ) {
				dbid = 809
			}
			if ( cnt == 8 ) {
				dbid = 1121
			}
			if ( cnt == 9 ) {
				dbid = 811
			}
			if ( cnt == 10 ) {
				dbid = 1135
			}
			if ( cnt == 11 ) {
				dbid = 1049
			}
			if ( cnt == 12 ) {
				dbid = 847
			}
			if ( cnt == 13 ) {
				dbid = 707
			}
			if ( cnt == 14 ) {
				dbid = 816
			}
			if ( cnt == 15 ) {
				dbid = 655
			}
			if ( cnt == 16 ) {
				dbid = 662
			}
			if ( cnt == 17 ) {
				dbid = 639
			}
			if ( cnt == 18 ) {
				dbid = 1185
			}
			if ( cnt > 18 ) {
				continue
			}
		}
		itemcreate -1, dbid, -1, -1, 0
		if ( stat == 0 ) {
			break
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MIROK ) {
			inv(INV_ITEM_NUM, ci) = 1
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			if ( inv(INV_ITEM_ID, ci) == ITEM_ID_ROD_DOMINATION ) {
				inv((9 /*!!!@[INV_ITEM_AMMO @@@ INV_ITEM_CHARGE @@@ INV_ITEM_CLIENT @@@ INV_ITEM_FILE @@@ INV_ITEM_NEXT_PERIOD]@!!! */), ci) = 4
			}
			continue
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_STOKE ) {
			inv(INV_ITEM_NUM, ci) = 1
			inv(INV_ITEM_STATUS, ci) = ITEM_STATUS_NORMAL
			continue
		}
		f = 0
		if ( instr(filter_item(inv(INV_ITEM_ID, ci)), 0, "/neg/") != (-1) ) {
			f = 1
		}
		if ( instr(filter_item(inv(INV_ITEM_ID, ci)), 0, "/noshop/") != (-1) ) {
			if ( cdata(CDATA_ROLE, tc) != ROLE_SHOP_MOUNTAIN2 ) {
				f = 1
			}
		}
		if ( f ) {
			inv(INV_ITEM_NUM, ci) = 0
			continue
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_OFFICE ) {
			flttypemajor = FILTER_FURNITURE
		}
		gosub *item_quantity
		inv(INV_ITEM_NUM, ci) = rnd(rtval) + 1
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BAKERY ) {
			if ( rnd(5) <= 2 ) {
				inv(INV_ITEM_NUM, ci) = limit(rnd(cdata(CDATA_ROLE_SHOP_LEVEL, tc) + 1) / 30 + 1, 1, 3) + rnd(2)
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE ) {
			p = trate(inv(INV_ITEM_PARAM1, ci))
			if ( p <= 70 ) {
				inv(INV_ITEM_NUM, ci) = inv(INV_ITEM_NUM, ci) * 200 / 100
			}
			if ( p <= 50 ) {
				inv(INV_ITEM_NUM, ci) = inv(INV_ITEM_NUM, ci) * 200 / 100
			}
			if ( p >= 80 ) {
				inv(INV_ITEM_NUM, ci) = inv(INV_ITEM_NUM, ci) / 2 + 1
				if ( rnd(2) ) {
					inv(INV_ITEM_NUM, ci) = 0
					continue
				}
			}
			if ( p >= 100 ) {
				inv(INV_ITEM_NUM, ci) = inv(INV_ITEM_NUM, ci) / 2 + 1
				if ( rnd(3) ) {
					inv(INV_ITEM_NUM, ci) = 0
					continue
				}
			}
			inv(INV_ITEM_NUM, ci) = inv(INV_ITEM_NUM, ci) * (100 + cdata(CDATA_ROLE_SHOP_LEVEL, tc)) / 75 + 1
		}
		p = refitem(inv(INV_ITEM_ID, ci), DBSPEC_TYPE)
		if ( inv(INV_ITEM_STATUS, ci) == ITEM_STATUS_CURSED | inv(INV_ITEM_STATUS, ci) == ITEM_STATUS_DOOMED ) {
			inv(INV_ITEM_NUM, ci) = 0
			continue
		}
		if ( inv(INV_ITEM_STATUS, ci) == ITEM_STATUS_BLESSED ) {
			inv(INV_ITEM_NUM, ci) = 1
		}
		if ( p == 52000 ) {
			if ( inv(INV_ITEM_ID, ci) == ITEM_ID_BOTTLE_WATER ) {
				inv(INV_ITEM_NUM, ci) = 0
			}
		}
		if ( p == 57000 ) {
			if ( refitem(inv(INV_ITEM_ID, ci), DBSPEC_TYPE_MINOR) == FILTER_ITEM_SEED ) {
				if ( rnd(5) ) {
					inv(INV_ITEM_NUM, ci) = 0
				}
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOUNTAIN2 ) {
			inv(INV_ITEM_VALUE, ci) = limit(inv(INV_ITEM_VALUE, ci), 1, 1000000) * 50
			if ( inv(INV_ITEM_ID, ci) == ITEM_ID_GIFT ) {
				inv(INV_ITEM_VALUE, ci) *= 10
			}
		}
		if ( inv(INV_ITEM_ID, ci) == ITEM_ID_CHRISTMAS_CAKE_SET ) {
			if ( gdata(GDATA_MONTH) != 12 | gdata(GDATA_DAY) > 25 ) {
				inv(INV_ITEM_VALUE, ci) = inv(INV_ITEM_VALUE, ci) / 2
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_NOYEL ) {
			inv(INV_ITEM_VALUE, ci) *= 2
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BLACKMARKET ) {
			if ( gdata(GDATA_FLAG_GUILD_THIEF) != 0 ) {
				inv(INV_ITEM_VALUE, ci) *= 2
			}
			else {
				inv(INV_ITEM_VALUE, ci) *= 3
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_WANDER ) {
			inv(INV_ITEM_VALUE, ci) *= 2
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_GUEST_MERCHANT ) {
			inv(INV_ITEM_VALUE, ci) = inv(INV_ITEM_VALUE, ci) * 4 / 5
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_BOOK_RESERVE ) {
			inv(INV_ITEM_VALUE, ci) = inv(INV_ITEM_VALUE, ci) * 3 / 4
			if ( gdata(GDATA_AREA) == AREA_YLVA_LIBRARY ) {
				inv(INV_ITEM_VALUE, ci) = inv(INV_ITEM_VALUE, ci) * 20
			}
		}
		if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_MOYER ) {
			inv(INV_ITEM_VALUE, ci) *= 2
		}
	loop
	cdata(CDATA_ROLE_RESTOCK, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 48 * (1 + (cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE))
	if ( cdata(CDATA_ROLE, tc) == ROLE_SHOP_TRADE ) {
		cdata(CDATA_ROLE_RESTOCK, tc) = gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12 + 168
	}
	return

*item_quantity
	p = refitem(inv(INV_ITEM_ID, ci), DBSPEC_TYPE)
	i = refitem(inv(INV_ITEM_ID, ci), DBSPEC_RARE_LEVEL)
	rtval = 1
	repeat 1
		if ( i <= 100 ) {
			break
		}
		if ( p == 57000 ) {
			rtval = 1
			break
		}
		if ( p == 92000 ) {
			rtval = i / 200
			break
		}
		if ( p >= 90000 ) {
			rtval = i / 100
			break
		}
		if ( p == 52000 ) {
			rtval = i / 100
			break
		}
		if ( p == 53000 ) {
			rtval = i / 100
			break
		}
		if ( p == 60000 ) {
			rtval = i / 200
			break
		}
		if ( p == 64000 ) {
			rtval = i / 80
			break
		}
		if ( p == 59000 ) {
			rtval = i / 500
			break
		}
	loop
	if ( dbid == 734 ) {
		rtval = rnd(8)
	}
	if ( rtval < 1 ) {
		rtval = 1
		return
	}
	return

*shop_tradePrice
	dim trate, 9
	repeat 9
		trate(cnt) = 100
	loop
	if ( gdata(GDATA_AREA) == AREA_ZAILE ) {
		trate(0) = 110
		trate(1) = 60
		trate(3) = 75
		trate(2) = 80
		trate(6) = 180
		trate(7) = 170
		trate(8) = 150
		trate(tradezaile) = 90
	}
	if ( gdata(GDATA_AREA) == AREA_VERNIS ) {
		trate(0) = 110
		trate(1) = 60
		trate(3) = 75
		trate(2) = 80
		trate(6) = 180
		trate(7) = 170
		trate(8) = 150
		trate(tradezaile) = 90
	}
	if ( gdata(GDATA_AREA) == AREA_PORT_KAPUL ) {
		trate(0) = 60
		trate(1) = 120
		trate(4) = 150
		trate(5) = 60
		trate(2) = 130
		trate(6) = 190
		trate(7) = 180
	}
	if ( gdata(GDATA_AREA) == AREA_PALMIA ) {
		trate(0) = 140
		trate(2) = 130
		trate(4) = 130
		trate(1) = 85
		trate(6) = 160
		trate(7) = 70
	}
	if ( gdata(GDATA_AREA) == AREA_YOWYN ) {
		trate(0) = 160
		trate(3) = 60
		trate(2) = 80
		trate(6) = 170
		trate(7) = 105
	}
	if ( gdata(GDATA_AREA) == AREA_DERPHY ) {
		trate(0) = 70
		trate(3) = 50
		trate(5) = 140
		trate(2) = 60
		trate(6) = 170
		trate(7) = 135
	}
	if ( gdata(GDATA_AREA) == AREA_NOYEL ) {
		trate(1) = 165
		trate(0) = 160
		trate(3) = 150
		trate(5) = 150
		trate(2) = 145
		trate(6) = 60
		trate(7) = 140
		trate(tradezaile) = 150
	}
	if ( gdata(GDATA_AREA) == AREA_LUMIEST ) {
		trate(1) = 150
		trate(0) = 150
		trate(3) = 120
		trate(5) = 140
		trate(2) = 140
		trate(6) = 130
		trate(7) = 50
		trate(8) = 160
	}
	if ( gdata(GDATA_AREA) == AREA_EIREL ) {
		trate(0) = 150
		trate(1) = 60
		trate(3) = 130
		trate(2) = 140
		trate(6) = 90
		trate(7) = 150
	}
	if ( gdata(GDATA_AREA) == AREA_MELKAWN ) {
		trate(0) = 130
		trate(1) = 80
		trate(4) = 80
		trate(5) = 140
		trate(2) = 80
		trate(6) = 130
		trate(7) = 80
	}
	if ( gdata(GDATA_AREA) == AREA_KURUALM ) {
		trate(0) = 80
		trate(2) = 60
		trate(4) = 70
		trate(1) = 70
		trate(6) = 140
		trate(7) = 70
	}
	if ( gdata(GDATA_AREA) == AREA_MELUGAS ) {
		trate(1) = 120
		trate(3) = 150
		trate(5) = 160
		trate(2) = 160
		trate(6) = 140
		trate(7) = 120
		trate(8) = 70
		trate(tradezaile) = 180
	}
	if ( gdata(GDATA_AREA) == AREA_VALM ) {
		trate(0) = 50
		trate(3) = 120
		trate(5) = 150
		trate(2) = 130
		trate(6) = 170
		trate(7) = 140
	}
	if ( gdata(GDATA_AREA) == AREA_ARCBELC ) {
		trate(0) = 120
		trate(3) = 140
		trate(5) = 130
		trate(2) = 120
		trate(6) = 150
		trate(7) = 70
		trate(8) = 60
		trate(tradezaile) = 150
	}
	if ( gdata(GDATA_AREA) == AREA_LUDUS ) {
		trate(1) = 180
		trate(0) = 130
		trate(3) = 180
		trate(5) = 50
		trate(2) = 70
		trate(6) = 190
		trate(7) = 120
	}
	randomize (gdata(GDATA_HOUR) + gdata(GDATA_DAY) * 24 + gdata(GDATA_MONTH) * 24 * 30 + gdata(GDATA_YEAR) * 24 * 30 * 12) / 100
	repeat 10
		trate(cnt) += rnd(15) - rnd(15)
	loop
	randomize
	return

*renew_economy
	if ( initeco ) {
		gdata(GDATA_CAPITAL) = 15
	}
	bkdata = gdata(GDATA_AREA), gdata(GDATA_LEVEL), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	snd SOUNDLIST_WRITE1
	gosub *game_save
	mode = MODELIST_MAP
	cdata(CDATA_X, CHARA_PLAYER) = 0
	cdata(CDATA_Y, CHARA_PLAYER) = 0
	scx = cdata(CDATA_X, CHARA_PLAYER)
	scy = cdata(CDATA_Y, CHARA_PLAYER)
	repeat 500
		if ( adata(ADATA_ID, cnt) == AREA_NONE ) {
			continue
		}
		if ( adata(ADATA_ECONOMY, cnt) == 0 ) {
			continue
		}
		gdata(GDATA_AREA) = adata(ADATA_ID, cnt)
		gdata(GDATA_LEVEL) = 1
		if ( gdata(GDATA_AREA) != bkdata(0) | gdata(GDATA_LEVEL) != bkdata(1) ) {
			gosub *map_init
		}
		p = adata(ADATA_ECONOMY, cnt)
		if ( initeco ) {
			if ( p == 1 ) {
			}
			if ( p == 2 ) {
			}
			if ( p == 3 ) {
			}
			if ( p == 4 ) {
			}
			if ( p == 5 ) {
			}
			if ( p == 6 ) {
			}
			if ( p == 7 ) {
			}
			if ( p == 8 ) {
			}
			if ( p == 9 ) {
			}
			if ( p == 10 ) {
			}
			if ( p == 11 ) {
			}
			if ( p == 12 ) {
			}
			if ( p == 13 ) {
			}
			if ( p == 14 ) {
			}
			if ( p == 15 ) {
			}
		}
		gosub *map_save
	loop
	gdata(GDATA_AREA) = bkdata(0)
	gdata(GDATA_LEVEL) = bkdata(1)
	cdata(CDATA_X, CHARA_PLAYER) = bkdata(2)
	cdata(CDATA_Y, CHARA_PLAYER) = bkdata(3)
	gdata(GDATA_WORLD_RENEW) = TRUE
	mode = MODELIST_CONTINUE
	mapsubroutine = 1
	gosub *map_init
	initeco = 0
	msgtemp = ""
	return

*com_politics_chart
	listmax = 0
	page = 0
	pagesize = 16
	cs = 0
	cc = CHARA_PLAYER
	cs_bk = -1
	snd SOUNDLIST_CHAT
	curmenu = 0
	redraw 0
	gsel BUFFER_INF
	pos 960, 96
	picload exedir + "graphic\\deco_politics.bmp", 1
	gsel BUFFER_SCREEN
	fillbg 3, 960, 96, 128, 128
	gosub *screen_drawStatus
	drawmenu 3
	windowshadow = 1
	city = adata(ADATA_ECONOMY, gdata(GDATA_AREA))
	lv = 0
	redraw 0
	cs_bk = -1
	pagemax = (listmax - 1) / pagesize
	if ( page < 0 ) {
		page = pagemax
	}
	else {
		if ( page > pagemax ) {
			page = 0
		}
	}

*com_politics_chart_loop
	if ( 1 == 0 ) {
		goto *com_politics_economy
	}
	redraw 0
	s = lang("ポストチャート", "City Chart"), strhint3b
	display_window (windoww - 580) / 2 + inf_screenx, winposy(400), 580, 400
	keyrange = 0
	j = 0
	n = 0
	color 0, 0, 0
	cs_listbk
	if ( adata(ADATA_ECONOMY, gdata(GDATA_AREA)) == 0 | adata(ADATA_ECONOMY, gdata(GDATA_AREA)) == 9 | gdata(GDATA_LEVEL) != 1 ) {
		font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
		pos wx + 40, wy + 50
		mes lang("この場所は情報がない。", "There's no information in this area.")
	}
	else {
		if ( adata(ADATA_PARENT, gdata(GDATA_AREA)) == AREA_LOST_YLVA ) {
			font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
			pos wx + 40, wy + 50
			mes lang("この場所は情報がない。", "There's no information in this area.")
		}
		else {
			display_topic lang(mapname(adata(ADATA_ID, gdata(GDATA_AREA))) + "のチャート", "" + mapname(adata(ADATA_ID, gdata(GDATA_AREA))) + " City Chart"), wx + 40, wy + 34
			repeat
				if ( pochart(j, n, lv) == 0 | cnt == 0 ) {
					if ( cnt != 0 ) {
						n++
					}
					j = 0
					i = 0
					repeat 10
						if ( pochart(cnt, n, lv) != 0 ) {
							i++
						}
					loop
					if ( i == 0 ) {
						break
					}
					y = wy + 70 + n * 55
				}
				x = wx + (ww - 70) / (i + 1) * (j + 1)
				pos x - 26, y - 3
				gcopy BUFFER_INF, 960, 96 + 144 + 48, 144, 24
				p = pochart(j, n, lv)
				key_list(cnt) = key_select(cnt)
				keyrange++
				display_key x - 30, y + 21, cnt
				font lang(cfg_font1, cfg_font2), 12 + sizefix - en * 2, 0
				pos x - 2, y + jp * 2
				color 0, 0, 0
				bmes cnven(popostname(p)), 255, 255, 255
				color 0, 0, 0
				font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
				podata(0 + cnt, city) = 0
				if ( podata(0 + cnt, city) == 0 ) {
					s = lang("不在", "Empty")
				}
				cs_list s, x - 2, y + 20, 19
				j++
			loop
			if ( keyrange != 0 ) {
				cs_bk = cs
			}
		}
	}
	redraw 1
	await cfg_wait1
	key_check
	cursor_check
	p = -1
	repeat keyrange
		if ( key == key_select(cnt) ) {
			p = list(0, pagesize * page + cnt)
			break
		}
	loop
	if ( p != (-1) ) {
		return
	}
	if ( menucycle == 1 ) {
		if ( key == key_next | key == key_prev ) {
			p = curmenu
			if ( key == key_next ) {
				curmenu++
				if ( curmenu > 2 ) {
					curmenu = 0
				}
			}
			if ( key == key_prev ) {
				curmenu--
				if ( curmenu < 0 ) {
					curmenu = 2
				}
			}
			key = ""
			if ( p != curmenu ) {
				screenupdate = -1
				gosub *screen_draw
				if ( curmenu == 0 ) {
					goto *com_politics_chart
				}
				if ( curmenu == 1 ) {
					goto *com_politics_economy
				}
				if ( curmenu == 2 ) {
					goto *com_politics_rule
				}
			}
		}
	}
	if ( key == key_pageup ) {
		if ( pagemax != 0 ) {
			snd SOUNDLIST_POP1
			page++
			goto *com_politics_chart
		}
	}
	if ( key == key_pagedown ) {
		if ( pagemax != 0 ) {
			snd SOUNDLIST_POP1
			page--
			goto *com_politics_chart
		}
	}
	if ( key == key_cancel ) {
		gosub *screen_draw
		return
	}
	goto *com_politics_chart_loop

*com_politics_economy
	curmenu = 1
	key_list(0) = key_enter
	keyrange = 0
	pagesize = 1
	listmax = 1
	redraw 0
	gsel BUFFER_INF
	pos 960, 96
	picload exedir + "graphic\\deco_politics.bmp", 1
	gsel BUFFER_SCREEN
	fillbg 3, 960, 96, 128, 128
	gosub *screen_drawStatus
	gsel BUFFER_PIC
	pos 0, 0
	picload exedir + "graphic\\ie_scroll.bmp"
	gsel BUFFER_SCREEN
	windowshadow = 1
	snd SOUNDLIST_SCROLL
	drawmenu 3
	city = 1
	ww = 540
	wh = 440
	wx = (windoww - ww) / 2 + inf_screenx
	wy = winposy(wh)

*com_politics_economy_loop
	if ( 1 == 0 ) {
		goto *com_politics_rule
	}
	redraw 0
	s = strhint2 + strhint3b
	showscroll wx, wy, ww, wh
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	color 0, 0, 0
	if ( adata(ADATA_ECONOMY, gdata(GDATA_AREA)) == 0 | adata(ADATA_ECONOMY, gdata(GDATA_AREA)) == 9 | gdata(GDATA_LEVEL) != 1 ) {
		pos wx + 40, wy + 60
		mes lang("この場所は情報がない。", "There's no information in this area.")
	}
	else {
		if ( adata(ADATA_PARENT, gdata(GDATA_AREA)) == AREA_LOST_YLVA ) {
			pos wx + 40, wy + 60
			mes lang("この場所は情報がない。", "There's no information in this area.")
		}
		else {
			city = adata(ADATA_ECONOMY, gdata(GDATA_AREA))
			if ( mdata(MDATA_CITY_PROPERTY_VALUE) > 300 ) {
				mdata(MDATA_CITY_PROPERTY_VALUE) = 300
			}
			if ( mdata(MDATA_CITY_PROPERTY_VALUE) < (-300) ) {
				mdata(MDATA_CITY_PROPERTY_VALUE) = -300
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) > 100 ) {
				mdata(MDATA_CITY_APPROVAL_RATE) = 100
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) < (-100) ) {
				mdata(MDATA_CITY_APPROVAL_RATE) = -100
			}
			if ( mdata(MDATA_CITY_PROPERTY_VALUE) > (-50) & mdata(MDATA_CITY_PROPERTY_VALUE) < 50 ) {
				s1 = "Neutral"
			}
			if ( mdata(MDATA_CITY_PROPERTY_VALUE) >= 50 ) {
				s1 = "Law"
			}
			if ( mdata(MDATA_CITY_PROPERTY_VALUE) <= (-50) ) {
				s1 = "Chaos"
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) <= (-100) ) {
				s2 = lang("腐れ暴君", "Tyrant")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) > (-100) & mdata(MDATA_CITY_APPROVAL_RATE) <= (-80) ) {
				s2 = lang("悪政者", "Misrule")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) > (-80) & mdata(MDATA_CITY_APPROVAL_RATE) <= (-60) ) {
				s2 = lang("税金泥棒", "Zeikin-Dorobou")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) > (-60) & mdata(MDATA_CITY_APPROVAL_RATE) <= (-40) ) {
				s2 = lang("ただの無能", "Incompetence")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) > (-40) & mdata(MDATA_CITY_APPROVAL_RATE) <= (-20) ) {
				s2 = lang("期待外れ", "Disappointing")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) > (-20) & mdata(MDATA_CITY_APPROVAL_RATE) < 0 ) {
				s2 = lang("正直微妙", "Not good enough")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) >= 0 & mdata(MDATA_CITY_APPROVAL_RATE) < 20 ) {
				s2 = lang("無益無害", "Useless and harmless")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) >= 20 & mdata(MDATA_CITY_APPROVAL_RATE) < 40 ) {
				s2 = lang("割と普通", "Common")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) >= 40 & mdata(MDATA_CITY_APPROVAL_RATE) < 60 ) {
				s2 = lang("これからに期待", "Promising")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) >= 60 & mdata(MDATA_CITY_APPROVAL_RATE) < 80 ) {
				s2 = lang("希望の星", "Excellent")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) >= 80 & mdata(MDATA_CITY_APPROVAL_RATE) < 100 ) {
				s2 = lang("まさに名君", "Role model")
			}
			if ( mdata(MDATA_CITY_APPROVAL_RATE) >= 100 ) {
				s2 = lang("街の守護者", "Ruler of virtue")
			}
			taiki = mdata(MDATA_CITY_ETHER_CORRUPTION)
			if ( gdata(GDATA_WEATHER) == WEATHER_ETHER ) {
				taiki += 5000
			}
			display_topic lang("概要", "Town Information"), wx + 65, wy + 50
			display_topic lang("財政", "Town Finance"), wx + 65, wy + 150
			if ( mdata(MDATA_CITY_COMPLAINTS) > 0 ) {
				display_topic lang("管理代行者評価", "Administrator"), wx + 65, wy + 250
			}
			font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
			color 0, 0, 0
			x = wx + 50
			y = wy + 80
			pos x, y
			mes lang("街属性値　　 (" + mdata(MDATA_CITY_PROPERTY_VALUE) + ")　" + s1 + " ", "Security           (" + mdata(MDATA_CITY_PROPERTY_VALUE) + ") " + s1 + " ")
			x = wx + 50
			y = wy + 96
			pos x, y
			mes lang("人口指数　　　" + mdata(MDATA_MODERATE_CROWD) + "　", "Population          " + mdata(MDATA_MODERATE_CROWD) + " ")
			x = wx + 50
			y = wy + 112
			pos x, y
			mes lang("水質汚染度　　" + mdata(MDATA_CITY_WATER_POLLUTION) + " (今までの死者" + mdata(MDATA_CITY_DEAD_PEOPLE) + "人）", "Water pollution     " + mdata(MDATA_CITY_WATER_POLLUTION) + " (dead " + mdata(MDATA_CITY_DEAD_PEOPLE) + " people) ")
			x = wx + 50
			y = wy + 128
			pos x, y
			mes lang("エーテル指数　" + taiki + " ", "Ether concentration " + taiki + " ")
			x = wx + 50
			y = wy + 180
			pos x, y
			mes lang("消費税　　　　" + mdata(MDATA_CITY_TAXES) + "%　", "Tax                 " + mdata(MDATA_CITY_TAXES) + "% ")
			x = wx + 50
			y = wy + 196
			pos x, y
			mes lang("観光収入　　　" + mdata(MDATA_CITY_TOURISM_REVENUE) + "gp　", "Tourism revenue     " + mdata(MDATA_CITY_TOURISM_REVENUE) + "gp ")
			x = wx + 50
			y = wy + 212
			pos x, y
			mes lang("維持費　　　　" + mdata(MDATA_CITY_MAINTENANCE_COST) + "gp　", "Maintenance costs   " + mdata(MDATA_CITY_MAINTENANCE_COST) + "gp ")
			x = wx + 50
			y = wy + 228
			pos x, y
			mes lang("運営予算　　　" + mdata(MDATA_CITY_BUDGET) + "gp　", "Budget              " + mdata(MDATA_CITY_BUDGET) + "gp ")
			if ( mdata(MDATA_CITY_COMPLAINTS) > 0 ) {
				mdata(MDATA_CITY_COMPLAINTS) = limit(mdata(MDATA_CITY_WATER_POLLUTION) - 15 + mdata(MDATA_CITY_ETHER_CORRUPTION) - 15 + mdata(MDATA_CITY_TAXES), 1, 99)
				x = wx + 50
				y = wy + 280
				pos x, y
				mes lang("苦情数　　　　" + mdata(MDATA_CITY_COMPLAINTS) + "　", "Complaint           " + mdata(MDATA_CITY_COMPLAINTS) + " ")
				x = wx + 50
				y = wy + 296
				pos x, y
				mes lang("発言力　　　　" + mdata(MDATA_CITY_AUTHORITY) + "　", "Authority           " + mdata(MDATA_CITY_AUTHORITY) + " ")
				x = wx + 50
				y = wy + 312
				pos x, y
				mes lang("支持率　　　　" + mdata(MDATA_CITY_APPROVAL_RATE) + "%　", "Approval rate       " + mdata(MDATA_CITY_APPROVAL_RATE) + "% ")
				x = wx + 50
				y = wy + 328
				pos x, y
				mes lang("印象　　　　  " + s2 + " ", "Evaluation          " + s2 + " ")
			}
		}
	}
	redraw 1
	await cfg_wait1
	key_check
	cursor_check
	if ( menucycle == 1 ) {
		if ( key == key_next | key == key_prev ) {
			p = curmenu
			if ( key == key_next ) {
				curmenu++
				if ( curmenu > 2 ) {
					curmenu = 0
				}
			}
			if ( key == key_prev ) {
				curmenu--
				if ( curmenu < 0 ) {
					curmenu = 2
				}
			}
			key = ""
			if ( p != curmenu ) {
				screenupdate = -1
				gosub *screen_draw
				if ( curmenu == 0 ) {
					goto *com_politics_chart
				}
				if ( curmenu == 1 ) {
					goto *com_politics_economy
				}
				if ( curmenu == 2 ) {
					goto *com_politics_rule
				}
			}
		}
	}
	if ( key == key_cancel ) {
		gosub *screen_draw
		return
	}
	goto *com_politics_economy_loop

*com_politics_rule
	listmax = 0
	page = 0
	pagesize = 13
	cs = 0
	cs_bk = -1
	curmenu = 2
	city = adata(ADATA_ECONOMY, gdata(GDATA_AREA))
	if ( mdata(MDATA_TYPE) != MAP_TYPE_TOWN ) {
		goto *skip_rule
	}
	if ( adata(ADATA_PARENT, gdata(GDATA_AREA)) == AREA_LOST_YLVA ) {
		goto *skip_rule
	}
	list(0, listmax) = 0
	listn(0, listmax) = "この街の消費税は" + mdata(MDATA_CITY_TAXES) + "%だ。"
	listmax++
	list(0, listmax) = 0
	listn(0, listmax) = "この街では殺人が許される。"
	listmax++

*skip_rule
	redraw 0
	gsel BUFFER_INF
	pos 960, 96
	picload exedir + "graphic\\deco_politics.bmp", 1
	gsel BUFFER_SCREEN
	fillbg 3, 960, 96, 128, 128
	gosub *screen_drawStatus
	gsel BUFFER_PIC
	pos 0, 0
	picload exedir + "graphic\\ie_scroll.bmp"
	gsel BUFFER_SCREEN
	windowshadow = 1
	snd SOUNDLIST_SCROLL
	drawmenu 3
	city = 1
	ww = 480
	wh = 400
	wx = (windoww - ww) / 2 + inf_screenx
	wy = winposy(wh)

*com_politics_rule_loop_pgchk
	cs_bk = -1
	pagemax = (listmax - 1) / pagesize
	if ( page < 0 ) {
		page = pagemax
	}
	else {
		if ( page > pagemax ) {
			page = 0
		}
	}

*com_politics_rule_loop
	if ( 1 == 0 ) {
		goto *label_6550
	}
	redraw 0
	s = strhint2 + strhint3b
	showscroll wx, wy, ww, wh
	display_topic lang("法律", "Law"), wx + 65, wy + 45
	font lang(cfg_font1, cfg_font2), 12 + sizefix - en * 2, 0
	color 0, 0, 0
	pos wx + 185, wy + 52
	mes lang("国法", "Global")
	if ( mdata(MDATA_TYPE) == MAP_TYPE_TOWN ) {
		pos wx + 285, wy + 52
		mes lang("" + mapname(gdata(GDATA_AREA)) + "の法", "Law of " + mapname(gdata(GDATA_AREA)) + ")")
	}
	pos wx + 155, wy + 46
	gmode 2
	gcopy BUFFER_INF, 312, 360, 24, 24
	pos wx + 255, wy + 46
	gmode 2
	gcopy BUFFER_INF, 288, 360, 24, 24
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	color 0, 0, 0
	keyrange = 0
	repeat pagesize
		p = pagesize * page + cnt
		if ( p >= listmax ) {
			break
		}
		key_list(cnt) = key_select(cnt)
		keyrange++
		if ( cnt \ 2 == 0 ) {
			pos wx + 74, wy + 76 + cnt * 19
			gfini 365, 18
			gfdec2 12, 14, 16
		}
		display_key wx + 72, wy + 76 + cnt * 19 - 2, cnt
	loop
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	cs_listbk
	repeat pagesize
		p = pagesize * page + cnt
		if ( p >= listmax ) {
			break
		}
		i = list(0, p)
		s = listn(0, p)
		cs_list s, wx + 100, wy + 76 + cnt * 19 - 1, 19, 0, 0
		pos wx + 42, wy + 68 + cnt * 19 + 2
		gmode 2
		gcopy BUFFER_INF, 288 + list(0, p) * 24, 360, 24, 24
	loop
	if ( keyrange != 0 ) {
		cs_bk = cs
	}
	redraw 1
	await cfg_wait1
	key_check
	cursor_check
	p = -1
	repeat keyrange
		if ( key == key_select(cnt) ) {
			p = list(0, pagesize * page + cnt)
			break
		}
	loop
	if ( menucycle == 1 ) {
		if ( key == key_next | key == key_prev ) {
			p = curmenu
			if ( key == key_next ) {
				curmenu++
				if ( curmenu > 2 ) {
					curmenu = 0
				}
			}
			if ( key == key_prev ) {
				curmenu--
				if ( curmenu < 0 ) {
					curmenu = 2
				}
			}
			key = ""
			if ( p != curmenu ) {
				screenupdate = -1
				gosub *screen_draw
				if ( curmenu == 0 ) {
					goto *com_politics_chart
				}
				if ( curmenu == 1 ) {
					goto *com_politics_economy
				}
				if ( curmenu == 2 ) {
					goto *com_politics_rule
				}
			}
		}
	}
	if ( key == key_pageup ) {
		if ( pagemax != 0 ) {
			snd SOUNDLIST_POP1
			page++
			goto *com_politics_rule_loop_pgchk
		}
	}
	if ( key == key_pagedown ) {
		if ( pagemax != 0 ) {
			snd SOUNDLIST_POP1
			page--
			goto *com_politics_rule_loop_pgchk
		}
	}
	if ( key == key_cancel ) {
		gosub *screen_draw
		return
	}
	goto *com_politics_rule_loop

*label_6550
	return
	snd SOUNDLIST_WRITE1
	gosub *game_save
	bkdata2 = gdata(GDATA_AREA), gdata(GDATA_LEVEL), cdata(CDATA_X, CHARA_PLAYER), cdata(CDATA_Y, CHARA_PLAYER)
	mode = MODELIST_MAP
	cdata(CDATA_X, CHARA_PLAYER) = adata(ADATA_X, gdata(GDATA_AREA))
	cdata(CDATA_Y, CHARA_PLAYER) = adata(ADATA_Y, gdata(GDATA_AREA))
	scx = cdata(CDATA_X, CHARA_PLAYER)
	scy = cdata(CDATA_Y, CHARA_PLAYER)
	gdata(GDATA_AREA) = gdata(GDATA_WORLD)
	gdata(GDATA_LEVEL) = 1
	gosub *map_init
	msgtemp = ""
	gosub *com_build
	gosub *map_save
	gdata(GDATA_AREA) = bkdata2(0)
	gdata(GDATA_LEVEL) = bkdata2(1)
	cdata(CDATA_X, CHARA_PLAYER) = bkdata2(2)
	cdata(CDATA_Y, CHARA_PLAYER) = bkdata2(3)
	mode = MODELIST_CONTINUE
	mapsubroutine = 1
	gosub *map_init
	msgtemp = ""
	return

*com_build
	area = bkdata2
	city = adata(ADATA_ECONOMY, area)
	tlocx = adata(ADATA_X, area)
	tlocy = adata(ADATA_Y, area)
	tlocinitx = tlocx
	tlocinity = tlocy
	scposval = 1
	cityfov = 3
	citybuilding = 3

*com_build_WHILE1
	if ( 1 == 0 ) {
		goto *com_build_WEND1
	}
	redraw 0
	sxfix = 0
	syfix = 0
	gosub *screen_setPos
	val = cityfov
	gosub *los_Loc
	gosub *los_draw
	gosub *screen_drawStatus
	dx = (tlocx - scx) * inf_tiles + inf_screenx
	dy = (tlocy - scy) * inf_tiles + inf_screeny
	if ( dy + inf_tiles <= windowh - inf_verh ) {
		pos dx, dy * (dy > 0)
		gfini inf_tiles - (dx + inf_tiles > windoww) * (dx + inf_tiles - windoww), inf_tiles + (dy < 0) * inf_screeny - (dy + inf_tiles > windowh - inf_verh) * (dy + inf_tiles - windowh + inf_verh)
		gfinc 50, 50, 100
	}
	txttargetnpc tlocx, tlocy, 1
	gosub *draw_budget
	redraw 1
	key_direction
	if ( stat == 1 ) {
		x = tlocx + kdx
		y = tlocy + kdy
		if ( x >= 0 & y >= 0 & x < mdata(MDATA_WIDTH) & y < mdata(MDATA_HEIGHT) ) {
			tlocx += kdx
			tlocy += kdy
		}
	}
	await cfg_wait1
	key_check
	if ( key == key_enter ) {
		p = 0
		repeat 10
			if ( bddata(0, city, cnt) != 0 ) {
				p++
			}
			else {
				cbd = cnt
				break
			}
		loop
		if ( p >= citybuilding ) {
			txtnew
			snd SOUNDLIST_FAIL1
			txt lang("この街はこれ以上の施設を所有できない。", "The city can't hold any more building.")
			goto *com_build_WHILE1_CONTINUE
		}
		cell_featread tlocx, tlocy
		if ( feat(FEAT_CELL_PIC) != 0 ) {
			txtnew
			snd SOUNDLIST_FAIL1
			txt lang("その場所には建てられない。", "You can't build here.")
			goto *com_build_WHILE1_CONTINUE
		}
		if ( dist(tlocx, tlocy, tlocinitx, tlocinity) < cityfov ) {
			gosub *com_select_building
		}
		else {
			txtnew
			snd SOUNDLIST_FAIL1
			txt lang("その場所は街から離れすぎている。", "This location is too far from the town.")
		}
		goto *com_build_WHILE1_CONTINUE
	}
	if ( key == key_cancel ) {
		goto *com_build_WEND1
	}

*com_build_WHILE1_CONTINUE
	goto *com_build_WHILE1

*com_build_WEND1
	scposval = 0
	return

*com_select_building
	snd SOUNDLIST_CHAT
	listmax = 0
	page = 0
	pagesize = 4
	cs = 0
	cc = CHARA_PLAYER
	cs_bk = -1
	repeat 7
		if ( cnt == 0 ) {
			continue
		}
		list(0, listmax) = cnt, 1
		listmax++
	loop
	gosub *sort_list
	windowshadow = 1

*com_select_building_loop
	redraw 0
	cs_bk = -1
	pagemax = (listmax - 1) / pagesize
	if ( page < 0 ) {
		page = pagemax
	}
	else {
		if ( page > pagemax ) {
			page = 0
		}
	}
	txtnew
	txt lang("どの設備を建設する？", "What do you want to build?")

*com_select_building_loop_WHILE1
	if ( 1 == 0 ) {
		goto *label_6566
	}
	redraw 0
	s = lang("設備一覧", "Building List"), strhint2 + strhint3
	display_window (windoww - 500) / 2 + inf_screenx, winposy(400), 500, 400
	keyrange = 0
	repeat pagesize
		p = pagesize * page + cnt
		if ( p >= listmax ) {
			break
		}
		key_list(cnt) = key_select(cnt)
		keyrange++
		y = wy + 60 + cnt * 70
		pos wx + 100, y
		gfini 350, 18
		gfdec2 12, 14, 16
		display_key wx + 88, y, cnt
	loop
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	cs_listbk
	repeat pagesize
		p = pagesize * page + cnt
		if ( p >= listmax ) {
			break
		}
		p = list(0, p)
		y = wy + 60 + cnt * 70
		pos wx + 20, y
		gmode 2
		gcopy BUFFER_MAP, bdref(0, p) * inf_tiles, 528, inf_tiles, inf_tiles
		cs_list bdrefn(p), wx + 114, y, 19
		color 0, 0, 0
		pos wx + 360, y + 2
		mes "" + bdref(1, p) + "k gp"
	loop
	if ( keyrange != 0 ) {
		cs_bk = cs
	}
	gosub *draw_budget
	redraw 1
	await cfg_wait1
	key_check
	cursor_check
	p = -1
	repeat keyrange
		if ( key == key_select(cnt) ) {
			p = list(0, pagesize * page + cnt)
			break
		}
	loop
	if ( p != (-1) ) {
		if ( mdata(MDATA_CITY_BUDGET) < bdref(1, p) ) {
			snd SOUNDLIST_FAIL1
			txtnew
			txt lang("予算が足りない…", "Your city can't afford it...")
			goto *com_select_building_loop_WHILE1_CONTINUE
		}
		mdata(MDATA_CITY_BUDGET) -= bdref(1, p)
		snd SOUNDLIST_BUILD1
		txtnew
		txt lang(bdrefn(p) + "を建設した！", "You have built a " + bdrefn(p) + "!")
		bddata(0, city, cbd) = p, tlocx, tlocy, bdref(0, p) + 363
		gosub *world_refresh
		return p
	}
	if ( key == key_pageup ) {
		if ( pagemax != 0 ) {
			snd SOUNDLIST_POP1
			page++
			goto *com_select_building_loop
		}
	}
	if ( key == key_pagedown ) {
		if ( pagemax != 0 ) {
			snd SOUNDLIST_POP1
			page--
			goto *com_select_building_loop
		}
	}
	if ( key == key_cancel ) {
		return -1
	}

*com_select_building_loop_WHILE1_CONTINUE
	goto *com_select_building_loop_WHILE1

*label_6566
	tlocinitx = 0
	tlocinity = 0
	return

*draw_budget
	x = windoww - 200
	y = 10
	window2 x, y, 190, 26
	font lang(cfg_font1, cfg_font2), 14 - en * 2, 0
	color 0, 0, 0
	gmode 2
	pos x - 28, y + 2
	gcopy BUFFER_INF, 0, 392, 24, 24
	pos x + 12, y + 7 - en * 2
	mes lang("予算:", "Budget:") + " " + mdata(MDATA_CITY_BUDGET) + "k gp"
	return






































































